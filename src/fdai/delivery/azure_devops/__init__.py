"""Azure DevOps delivery adapters (change feed).

CSP/VCS-neutral vendor adapter that attaches at the composition root;
``core/`` never imports it. See :mod:`fdai.delivery.azure_devops.change_feed`.
"""
