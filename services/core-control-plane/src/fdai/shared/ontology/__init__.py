"""Shared ontology-projection utilities used by ``core/`` and ``delivery/``.

Modules here MUST stay import-free of ``core/`` and ``delivery/`` -
they realize the projection contracts that both callers depend on.
"""
