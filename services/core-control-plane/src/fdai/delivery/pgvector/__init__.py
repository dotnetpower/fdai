"""pgvector delivery adapters for RAG and governed document indexes.

CSP-neutral adapter bound at the composition root; ``core/`` never imports
it. See :mod:`fdai.delivery.pgvector.knowledge` and
:mod:`fdai.delivery.pgvector.document_index`.
"""
