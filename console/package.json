{
  "name": "aiopspilot-console",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "description": "AIOpsPilot operator console — read-only SPA (KPI dashboard, audit log, HIL queue). See app-shape.instructions.md § Operator console.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@azure/msal-browser": "^3.28.1",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0",
    "vite": "^6.3.5"
  }
}
