# LinkType: attached_to (Resource -> Resource, M:N)
#
# Lifetime-bound attachment: NIC -> VM, disk -> VM, private-endpoint
# -> target. Removing the parent breaks the child. Not transitive:
# `attached_to` chains are unusual and the traversal engine treats
# each hop as an independent check.
schema_version: "1.0.0"
name: attached_to
version: "1.2.0"
from_type: Resource
to_type: Resource
cardinality: many_to_many
forward_role: attached_to
reverse_role: anchors_attachment
semantic_traits: [attachment]
description: Lifetime-bound attachment; removing the parent breaks the child.
provenance:
  source_url: https://github.com/dotnetpower/fdai
  resolved_ref: link-type:attached_to@1.2.0
  content_hash: sha256:8684886381d5d1f5980b174306750cfe0c9e563f711052ba29bcb5edf79cb404
  license: MIT
  retrieved_at: "2026-07-29T00:00:00Z"
