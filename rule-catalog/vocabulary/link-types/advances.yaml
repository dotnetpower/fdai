# LinkType: advances (Process -> Finding, M:M, temporal)
#
# The ordered Findings a Process advanced through during its run. The
# `temporal_order` flag means a query over this link MUST respect the
# Finding timestamps, so a replay reconstructs the process step sequence
# in the order it happened.
#
# See docs/roadmap/decisioning/process-automation.md 3.2.
schema_version: "1.0.0"
name: advances
version: "1.0.0"
from_type: Process
to_type: Finding
cardinality: many_to_many
temporal_order: true
order_by_property: evaluation_ts
description: The ordered Findings a Process advanced through during its run.
provenance:
  source_url: https://github.com/dotnetpower/fdai
  resolved_ref: link-type:advances@1.0.0
  content_hash: sha256:8c5286b08c340fee097444452f649e7dd7af1bc5b7bdc823e192145dbde36d6c
  license: MIT
  retrieved_at: "2026-07-29T00:00:00Z"
