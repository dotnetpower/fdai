---
title: Code Map
---
# Code Map

This page maps each FDAI runtime service and shared package to its physical source, tests, and
owning design. Use it to find the current service-owned implementation without relying on the
retired top-level application tree.

> **Scope:** This map describes the validated local IS-08 repository ownership and IS-07 local
> upgrade and rollback proof. IS-09 owns the deferred remote verification.

## Design at a glance

- **Five service distributions:** Every runtime process owns one package under `services/`.
- **One shared SDK:** `packages/service-contracts/` contains cross-service contracts without service
  implementation.
- **Service-owned tests:** Unit and component tests live beside their owning service or package.
- **Virtual root:** The root `pyproject.toml` has `package = false` and coordinates the uv workspace.
- **Integration-only root tests:** `tests/integration/` owns cross-service compatibility, topology,
  and repository checks.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Service-owned source and test map | in-progress | This map, `tests/integration/`, and the scoped IS-08 and IS-07 evidence named above | Local ownership and rollback evidence are mapped; IS-09 remote verification remains open. |
| Exact-generation Rule retrieval | implemented | `shared/providers/catalog_search.py`; `delivery/catalog_search/generation.py`; `delivery/catalog_search/in_memory.py`; `delivery/catalog_search/postgres.py`; focused catalog, ontology query, schema, composition, and live PostgreSQL tests (`44 passed`) | Results are candidate-only, bind the active Rule generation to the exact ontology release and a bounded ordered-document manifest, and carry no judgment, approval, or execution authority. PostgreSQL activation checks the expected prior generation in the same transaction. |
| Objective-aware Rule candidate resolution | implemented | `core/ontology_platform/objective_rule_resolution.py`; `core/ontology_platform/catalog_queries.py`; `shared/providers/catalog_search.py`; `delivery/catalog_search/in_memory.py`; focused ontology query tests (`8 passed`) | Reviewed or promoted active relations narrow the exact-generation candidate set before ranking. Invalid or incomplete context falls back atomically, and objective context changes query identity without adding evaluation or execution authority. |
| Observation conflict adjudication | implemented | `core/ontology_platform/observation_adjudication.py`; `core/ontology_platform/inventory_projection.py`; `delivery/inventory_relationship_verifier.py`; focused ontology, inventory, and runtime tests (`507 passed, 1 skipped`) | Repeated authoritative observations of one resource identity inside a generation are decided deterministically. Equal content with a different clock read is one fact and keeps the earliest time; any disagreement stays an explicit `StateFactMetadata.conflicts` entry that withholds the contested value. Cross-authority adjudication between independent sources remains open in [Operating Ontology](operating-ontology.md#conflict-adjudication-scope). |
| Cross-source resource-state adjudication | implemented | `core/read_investigation/resource_state_shadow_evidence.py`; `core/read_investigation/resource_state_shadow_service.py`; `composition/wire_read_investigation.py`; focused read-investigation and composition tests (`110 passed`) | The live provider read and the inventory-projected graph state are adjudicated into one `derived` fact with `deterministic_function` authority. A state or identity disagreement is an explicit conflict retained in the shadow receipt digest; an observation-time difference is not. The verdict only lowers the disposition and grants no approval, mutation, or execution authority. |
| Receipt-bound operational Context presentation | implemented | `core/operational_context/console_projection.py`; `tests/core/operational_context/test_console_projection.py`; focused tests (`5 passed`) | Purpose, release, cutoff, execution-authority, and graph coverage must match before bounded metadata is projected. Raw properties are excluded, and principal-scoped transport remains unbound. |
| Operating-scope `unknown_service` coverage | implemented | `core/operational_context/operating_scope.py`; `tests/core/operational_context/test_operating_scope.py`; focused tests (`6 passed`) | Every observed Resource stays visible, an unmapped or conflicting resource keeps the reserved marker instead of a synthetic service, and the projection is read-only. No runtime consumer is bound yet. |
| Inventory-backed analyzer target resolution | implemented | `core/investigation/analyzers.py`; `delivery/analyzer_targets.py`; `test_analyzer_targets.py`; `test_analyzer_tick_routed.py`; focused analyzer tests | A reviewed map resolves only the five supported catalog resource types to reference analyzers. Explicit and inventory-backed targets merge deterministically within one configured bound; unsupported types are omitted rather than guessed, and an unreadable durable projection fails the tick instead of silently narrowing coverage. |
| Distributed-trace continuity detection | implemented | `core/detection/trace_continuity.py`; `delivery/azure/trace_continuity.py`; `delivery/trace_continuity_tick.py`; `test_trace_continuity.py`; `test_trace_continuity_chain.py`; focused trace checks | Core owns the pure expected-hop comparison, Azure delivery owns strict bounded Application Insights normalization, and the existing analyzer Job publishes shadow findings. Huginn and Heimdall preserve allowlisted evidence and open a repeated-finding Incident without changing agent roles or action authority. |
| Runtime-bound planner visibility | implemented | `composition/wire_semantic_query.py`; `core/conversation/semantic_manifest.py`; `core/ontology_platform/query_manifest.py`; focused composition and manifest tests | Planner descriptors expose only functions registered in the composed runtime. Readable but unbound declarations remain typed structural coverage and gain no authority. |
| Exact-release principal manifest query | implemented | `core/ontology_platform/manifest_queries.py`; `core/ontology_platform/query_source_handlers.py`; `composition/wire_semantic_query.py`; focused manifest, handler, composition, and prompt tests (`42 passed`) | `query.manifest` returns bounded role- and purpose-filtered declaration rows through the existing function receipt and generic `QueryTable` path. Unbound declarations lower completeness, and every row fixes `execution_authority=false`. |
| Exact-release schema relationship query | implemented | `core/ontology_platform/relationship_queries.py`; `composition/wire_semantic_query.py`; `fdai_core_service/semantic_relationship_projection.py`; focused composition and processor tests (`42 passed`) | `query.ontology_relationships` reads ObjectType and LinkType declarations, preserves direction, cardinality, and description, and carries no judgment, approval, mutation, or execution authority. Authenticated Browser evidence remains open. |
| Question-bound semantic frame scope | implemented | `core/conversation/semantic_planning_cascade.py`; `tests/conversation/test_semantic_planning_tier_routing.py`; focused tier-routing checks (`32 passed`) | Plan verification compares a plan with its frame, so a misclassified frame agreed with itself and answered a different question than the operator asked. A schema-level frame is now rejected when the question or its subject carries an identifier-shaped token absent from every declared name, property, and value, and the rejection retries only the frame stage with T2. Exhausting both tiers reports the turn unsupported instead of answering it with declaration rows. |
| Declaration-driven finite question universe | implemented | `core/conversation/question_universe.py`; `core/conversation/__init__.py`; `core/conversation/epistemic_coverage.py`; `core/conversation/coverage_gate.py`; `tests/conversation/test_question_universe.py`; `tests/conversation/test_epistemic_coverage.py`; `tests/conversation/test_coverage_gate.py`; focused release-gate tests (`41 passed`, generator branch coverage `100%`, epistemic branch coverage `99%`, coverage-gate branch coverage `100%`) | Complete principal-scoped manifests expand through a canonical bounded grammar into stable case identities exposed by the conversation package. The grammar and receipt share one 10,000-case ceiling; epistemic and final gate receipts verify their release identity, immutable and serializable evidence counts, pass and production states, and content digests; unavailable declarations remain typed exclusions; overflow fails before expansion; and generated records grant no execution authority. |
| Production Rule semantic readiness | implemented | `runtime/bootstrap.py`; `runtime/bootstrap_lifecycle.py`; `composition/wire_semantic_query.py`; `tests/runtime/test_catalog_semantic_bootstrap.py`; focused bootstrap and composition checks (`46 passed`) | Production startup registers Rule semantic search only when the active generation exactly matches the current Rule catalog, semantic schema, ontology release, and embedder dimension. Stable optional-readiness degradation preserves startup without exposing a stale function. |
| Durable Rule generation closure | implemented | `core/rule_semantic_generation/activation.py`; `core/rule_semantic_generation/ledger.py`; `core/rule_semantic_generation/publication.py`; `rule_catalog/schema/rule_semantic_generation_events.py`; focused activation, contract, ledger, publication, and live PostgreSQL checks | Core verifies the exact validation receipt and expected prior active identity before activation, suppresses completed-command provider replay, atomically binds the first terminal result to one lease-fenced publication record, and marks it published only after an exact-topic broker acknowledgement. Delivery state grants no policy, approval, mutation, or execution authority. |
| Rule generation publication ownership | implemented | `agents/mimir.py`; `agents/_framework/runtime.py`; `runtime/bootstrap.py`; `runtime/bootstrap_bindings.py`; `runtime/bootstrap_lifecycle.py`; focused Mimir, runtime, bootstrap, activation, and publication checks (`32 passed`) | Mimir is the only activation-command and result subscriber. It delegates commands to the exact binder and stores safe-to-retry projection-only result receipts. Readiness-independent draining retries only released transport failures and grants Mimir no index, policy, approval, mutation, or execution authority. |
| Ordinary effect-reconciliation requests | implemented | `core/ontology_platform/reconciliation_producer.py`; `core/ontology_platform/reconciliation_request_outbox.py`; `delivery/reconciliation_request.py`; `delivery/reconciliation_request_publication.py`; focused reconciliation, ControlLoop, runtime, and composition checks (`163 passed`) | An executed Action must already cite a matching exact V2 plan. Independent observation is durably queued before publication, and downstream failure remains held or pending evidence without rewriting the executor outcome. Production artifact and observation adapters remain open. |
| Exact kinetic proposal handoff | implemented | `core/operational_planning/kinetic_proposal.py`; `delivery/kinetic_proposal.py`; `agents/forseti.py`; `agents/thor.py`; focused producer, Forseti, Thor, factory, and framework checks | A complete operational plan can resolve one durable existing exact V2 proposal through the existing Verdict path. Missing proposals preserve legacy behavior, invalid records lower authority to deny, and production source composition plus the pre-dispatch receipt writer remain open. |
| Read-investigation activity identity | implemented | `composition/wire_read_investigation.py`; `test_wire_read_investigation.py`; focused tests | Each invocation shares one opaque correlation across live and durable activity, separate invocations use distinct correlations, and logical request idempotency remains stable. |
| Browser-evidence metadata read contract | implemented | `fdai_service_contracts/operator.py`; `fdai_operator_service/browser_evidence_projection.py`; Operator migration; focused Operator and shared-contract checks (`148 passed`) | `BrowserEvidenceQuery` adds one bounded read-only method to the implementation-free Operator contract. The service role can select only a security-barrier metadata view; captured and structured payloads remain outside the cross-service contract. |
| Incident roster read filters | implemented | `fdai_service_contracts/operator.py`; `fdai_operator_service/routes.py`; `fdai_operator_service/postgres_sql.py`; focused Operator and shared-contract checks | `IncidentQuery.search` and `severity` add optional bounded read filters to the implementation-free Operator contract. The service normalizes search, matches only recorded subject evidence before the page limit, and binds search, status, vertical, and severity to the same snapshot and cursor identity. |
| Cross-service semantic Rule projection | implemented | `fdai_service_contracts/semantic_turn.py`; `fdai_core_service/semantic_turn_processor.py`; `fdai_operator_service/postgres_semantic_turn_store.py`; focused semantic tests passed 94 cases | The shared version 1.2 contract, Core processing, and Operator persistence preserve the exact validated function-invocation receipt and its canonical digest in addition to candidate-only authority, bounded deadlines, recoverable ownership, and exact principal-scoped reads. Contract validation rejects content, digest, task, intent, capability, and terminal-status drift. Governed live assurance remains open in the [ontology query coverage plan](../interfaces/ontology-query-coverage-implementation-plan.md#remaining-work). |
| Console semantic receipt projection | implemented | `semantic_turn.py`; `semantic_turn_processor.py`; `semantic_turn_runtime.py`; `console/src/deck/backend-normalizers.ts`; focused shared, Core, Operator, and Console tests | The typed route, specific clarification answer, unavailable reason, four assurance digests, evidence references, and `execution_authority=false` cross the shared contract, Core result, exact Operator read, terminal stream, durable transcript, replay, and Console presentation without prose inference. Passing governed browser and randomized-assurance records remain open in the ontology query coverage plan. |
| Deterministic missing incident context | implemented | `core/conversation/semantic_planning.py`; `tests/conversation/test_semantic_planning.py`; focused planner and terminal-projection tests (`43 passed`) | A first-turn reference to "this incident" returns one bounded clarification before manifest or model work. Prior incident context continues through normal semantic planning, and neither path grants execution authority. |
| Semantic temporal and evidence composition | implemented | `delivery/persistence/postgres_topology_history.py`; `composition/wire_semantic_query.py`; `runtime/bootstrap.py`; `runtime/bootstrap_bindings.py`; focused composition and provider-selection tests passed 16 cases | PostgreSQL topology history is available only with the state-store DSN. Metric-series and evidence-join capabilities require both the reviewed metric registry and a non-noop provider. One handler map controls verifier and executor availability, and every result remains read-only with `execution_authority=False`. |
| T1-first semantic planning | implemented | `core/conversation/semantic_planning.py`; `core/conversation/semantic_planning_cascade.py`; `core/conversation/semantic_planning_models.py`; `composition/semantic_query_model_targets.py`; `composition/wire_semantic_query.py`; focused tier-routing and composition tests | Semantic planning always uses the resolved T1 mini-model first. Only an unavailable or deterministically invalid T1 frame or plan proposal can retry that same stage once with the optional T2 primary reasoner. Typed clarification requirements distinguish legitimate missing user context, which terminates at T1, from a request for server-bound principal scope or purpose, which invalidates the T1 frame before one frame-only retry. Core binds one timezone-aware evaluation time to the plan proposal and rebinds every verified ObjectSet cutoff to that same value; a bounded T2 plan retry receives it unchanged, so neither tier controls current query time. Aggregate fields are verified against the exact query-table row schema propagated through ObjectSet, Project, Order, and set-operation dependencies before I/O. The planner never borrows the secondary quality-cross-check role. Scope denial and evidence holds do not invoke T2. |
| Event-bus subscription lifecycle | implemented | `shared/providers/event_bus.py`; `runtime/consumers.py`; `delivery/event_bus_multiplex.py`; `delivery/agent_introspection_bus.py`; `delivery/reconciliation_runtime.py`; `shared/streaming/broadcaster.py`; `agents/_framework/bus_bridge.py`; `fdai_core_service/semantic_turn_consumer.py`; `tests/agents/test_subscription_lifecycle.py`; focused agent, delivery, runtime, and provider tests (`3127 passed`) | Every consumer drives its subscription through the shared `subscription` context manager, so the broker adapter closes inside the consumer task instead of at interpreter finalization. A residual consumer stop timeout names its topic and consumer group. Teardown is best effort: a failed broker leave is recorded against its topic and consumer group and never replaces the in-flight exception, so a cancelled consumer still reports as cancelled. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-08-19 | implemented | Declared seven resource types the Azure scan was skipping. `delivery/azure/arg_query.py` issues one Resource Graph query per declared `azure_arm_type`, so an undeclared type is never queried and the snapshot reports its resources as absent rather than as unmapped. Two of the seven closed a containment hole instead of adding breadth: `sql-database` was declared without its `Microsoft.Sql/servers` parent, and `data-collection-rule` without its endpoint counterpart. | `current change`; `tests/rule_catalog`, `tests/delivery/azure`, and `tests/providers` passed 2395 focused cases; `check-ontology-query-coverage`, `check-property-semantic-coverage`, and `check-independent-services` passed. Measured against a live subscription scope: Resource Graph reported 533 objects against a 504-resource snapshot, and 22 undeclared ARM types held all 69 missing resources. A forced full reconciliation moved the promoted snapshot to 516, matching the twelve resources the seven types own, with projection `status=available` and no drops. | 57 resources stay unmapped across fifteen platform-auto-created or out-of-vertical types that are undeclared by choice. Their snapshot coverage record and the missing SQL server-to-database containment link remain open. The ResourceType seed-drift failure is closed by the next row. |
| 2026-08-19 | implemented | Downgraded a reviewed ResourceType mapping that reaches the inventory process before its catalog-owned instance seed from a generation-wide endpoint failure to the stable drop `unseeded_resource_type`. The projector omits only that derived classification edge, writes the rest of the complete generation, and keeps unrelated endpoint or instance validation fail closed. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); focused runtime regression reproduces `ontology link endpoints do not exist` before the change and passes with an available projection, one persisted Resource, no classification link, and the exact drop reason after the change. The two focused projection files pass 32 cases; task-scoped Ruff and strict mypy pass. | Record the measured 57-resource provider coverage in snapshot metadata, then add deterministic SQL server-to-database containment. |
| 2026-08-19 | implemented | Aligned the shipped bilingual database value-domain regression with the declared `sql-server` type. The registry had correctly added the new database-category member, but the fixed expected set still named the six pre-change values and failed committed-diff validation. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); the exact composition test fails on commit `36a060bb4` with unexpected `sql-server` and passes after adding that one expected member. | None for this guard; the provider coverage and SQL containment work remains unchanged. |
| 2026-08-19 | implemented | Added the CSP-neutral provider-scope coverage record and carried it only from a complete inventory final fence into promoted snapshot metadata. The record separates provider-native object totals from materialized snapshot resources and rejects unreconciled or unbounded counts. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); focused provider and inventory-sync tests pass 31 cases; task-scoped Ruff and strict mypy pass. | The Azure adapter still needs to produce the scope-wide aggregation; no 57-resource runtime record is claimed by this contract-only slice. |
| 2026-08-19 | implemented | Added the Azure provider-scope producer for snapshot coverage. One bounded aggregation counts raw ARG resources and resource groups by normalized ARM type, compares them with the full reviewed vocabulary, and carries undeclared type/count rows on the atomic final fence without declaring support. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); focused ARG, Azure inventory, composition, and inventory-job tests pass 136 cases; task-scoped Ruff and strict mypy pass. | Run a promoted full reconciliation and verify the retained 57-resource, 15-type measurement in snapshot metadata; SQL containment remains separate. |
| 2026-08-19 | implemented | Repaired the live provider-scope ARG grammar after the first committed run returned HTTP 400. The aggregation now pipes the `Resources` result into the resource-group union and names `resource_count` explicitly; partial failure still retains the prior active snapshot. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); isolated committed callback reproduces the 400, the corrected bounded Azure CLI query returns type/count rows, and focused ARG and composition tests pass 99 cases with Ruff and strict mypy. | Rerun full reconciliation and verify promoted coverage metadata before closing the 57-resource gap. |
| 2026-08-19 | validated | Promoted the measured provider-scope coverage into the active local inventory snapshot. Provider-native counts and materialized snapshot counts remain separate, so undeclared resources are visible without being treated as supported ResourceTypes. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); committed ARG callback reports 533 provider objects, 476 vocabulary-mapped objects, 57 unmapped objects, 68 provider types, and 15 unmapped types; the promoted loopback PostgreSQL snapshot is active from ARG with 516 Resource rows and the same complete coverage metadata. | None for the measured 57-resource gap; add SQL server-to-database containment next. |
| 2026-08-19 | implemented | Added reviewed Azure SQL logical-parent containment without replacing resource-group containment. The candidate uses only a structurally valid immediate provider parent and remains inactive until complete-generation endpoint verification. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); focused ARG, exact D1 mapping-direction audit, and relationship-verifier tests pass 116 cases; Ruff, strict mypy, ontology-query coverage, and Property semantic coverage pass. | Run full reconciliation and verify the promoted SQL server-to-database edge. |
| 2026-08-19 | implemented | Corrected SQL containment cardinality after live projection proved that one database cannot keep both wildcard resource-group and exact logical-server parents. Exact source-type containment now shadows the wildcard only for the same child; different hierarchy levels still compose, and `Resource.parent_id` follows the selected mapping. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); snapshot promotion succeeded while ontology projection failed with `contains violates one_to_many cardinality`; SQL and VNet controls plus focused ARG, mapping, and verifier tests pass 117 cases; strict mypy passes. | Commit and rerun full reconciliation, then verify snapshot and ontology agree. |
| 2026-08-19 | validated | Promoted the corrected SQL logical-parent generation and verified snapshot-to-ontology parity. Each observed database has the logical server as both `Resource.parent_id` and its single `contains` parent. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); loopback PostgreSQL reports 2 snapshot databases, 2 parent ids, 2 snapshot SQL containment edges, 2 ontology databases, and 2 ontology SQL containment edges after ARG promotion, with no observer failure. | None for the SQL server-to-database gap. |
| 2026-08-19 | implemented | Hardening Round 1 reviewed 13 concerns and fixed the only accepted Medium defect: multiple exact parent-containment mappings for one source type now fail catalog load instead of reaching promotion and violating one-to-many cardinality. The empty-scope, post-yield coverage failure, null ARM mapping, enum, and final-fence findings were rejected against the executed code path; two residual observations are Low. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); mutation-style catalog regression fails without the ownership guard and passes with it. | Run Round 2 with at least 10 lenses and stop only when Medium-or-higher residuals are absent. |
| 2026-08-19 | implemented | Hardening Round 2 reviewed 14 concerns and fixed one independently verified Medium contract defect: boolean counts and impossible positive-object/zero-type coverage records are now rejected. Repeated claims about ARG filtering, final-fence ordering, glob overlap, parent parsing, digest provenance, and unseeded enrichment were rejected against code, focused checks, and retained live evidence. | [Issue #216](https://github.com/dotnetpower/fdai/issues/216); six negative cases fail before the guard and seven cases pass after it. | Run Round 3 with at least 10 lenses; only Low or rejected observations may remain. |
| 2026-08-19 | implemented | Routed every `ExecutionPath` and `PromotionGate` import in `core/executor/` and its tests through the `fdai.shared.contracts.models` facade. Five executor modules were reaching into `models.enums` directly, which the G-4 facade test forbids and which had been failing on `main` since the safeguard contract landed. | `current change`; the core and shared-package suites passed 11913 cases with 131 skips, including the previously red `test_models_facade_only`; task-scoped Ruff and format passed. | None for this seam. |
| 2026-08-19 | implemented | Closed two review findings in the safety core. `core/assurance_twin/model_promotion.py` keyed its bound registry with `type(self).__name__`, so a subclass would raise `KeyError` from inside a safety check; the two semantic ids are now resolved once against the fixed owner name. `core/executor/executor.py` gained a raise its docstring still denied, so the docstring now names it and separates a composition error from a business-logic failure. | `current change`; `tests/core/standing_authority`, `tests/core/assurance_twin`, `tests/core/operational_learning`, `tests/core/executor`, `tests/scenarios`, and the venue gate's integration test passed 520 focused cases; task-scoped Ruff, format, and mypy passed; the venue gate reported OK across 6 source trees. | None for these two findings. |
| 2026-08-19 | implemented | Gave `core/executor/executor.py` the execution path that selected it. `pr_manual` and `pr_native` share `ShadowExecutor`, so a manual-merge receipt and both audit phases recorded `pr_native`. `execute()` now takes the path, refuses one it does not serve, and `core/control_loop/_execution.py` passes the ActionType's selected path; `core/executor/port.py` carries the new keyword. | `current change`; `tests/core`, `tests/pipeline`, `tests/scenarios`, `tests/providers`, `tests/runtime`, and `tests/delivery` passed 7294 focused cases with 4 skips; task-scoped Ruff, format, mypy, and the core import gate passed. | No shipped ActionType selects `pr_manual`, so upstream behavior is unchanged; a fork that selects it now gets an accurate label. |
| 2026-08-19 | implemented | Bound the last two adaptive thresholds to a declaration. `shared/contracts/ontology/action-type.json` now declares `min_fidelity` and `max_recurrence_rate` as optional `promotion_gate` ratio bounds, `shared/contracts/models/ontology.py` carries them as optional fields, `shared/ontology/threshold_bounds.py` registers them, and `core/assurance_twin/model_promotion.py` derives its accepted range from the declaration instead of the literal it restated. `UNBOUND_ADAPTIVE_THRESHOLDS` is empty. | `current change`; `tests/core/operational_learning/test_threshold_bounds.py`, `tests/core/assurance_twin`, `tests/contracts`, `tests/rule_catalog`, and `tests/core/measurement` passed 1640 focused cases; task-scoped Ruff, format, and mypy passed; `check-core-imports` and `check-property-semantic-coverage` passed. | Detection and routing thresholds outside the promotion gate are still literals and are not registered. |
| 2026-08-18 | implemented | Made `core/executor/direct_api.py` and `core/executor/tool_call.py` carry the dry-run receipt from the pre-effect intent entry into the terminal audit entry, so a closure record proves safeguard 4 on its own. Both now match the PR path's shape: the receipt rides on `ExecutionResult.audit_context` and is absent when the action was refused before a dry run existed. | `current change`; `tests/core/executor` passed 259 focused cases including three new two-phase receipt tests per path; task-scoped Ruff, format, and mypy passed. | Enforce-mode promotion evidence still does not exist for any path. |
| 2026-08-18 | implemented | Moved the execution-venue contract into the shared SDK as `fdai_service_contracts/venue.py`. It had lived in `fdai/runtime/venue.py`, which an independent service cannot import, so four services kept their own parser and literal comparisons and the gate could only scan the core tree. `fdai/runtime/venue.py` now re-exports the shared module and declares no binding of its own. | `current change`; `packages/service-contracts/tests`, `services/core-control-plane/tests/runtime`, the gate's integration test, and the four independent service suites passed 874 focused cases with 1 skip; `tests/delivery` passed 1689 cases with 3 skips; the venue gate reported OK across 6 source trees. | The gate's detection stays textual, so an indirect reintroduction through a computed key is still not caught. |
| 2026-08-18 | implemented | Moved stated-value-filter grounding out of `semantic_planning.py` into `semantic_planning_value_filters.py`. Adding that feature took the planner to 875 LOC, past the 800 fail threshold, so the file LOC guard failed on every branch. The extracted unit reads only the plan and the declared descriptors, so it has no dependency on planner state. Also realigned the semantic-turn roundtrip test, which asserted a stream backbone that no longer held once observed phases became timeline steps. | `current change`; planner and conversation suites `1343 passed, 6 skipped`; `tests/integration` `1654 passed, 1 skipped`; operator suite `406 passed, 1 skipped`; `check-file-loc.sh` in enforce mode reports `failed=0`; task-scoped Ruff check and format pass. Measured cause: `check-file-loc` reported `FAIL 875 LOC` on the planner, and the roundtrip test failed with six unexpected `activity` events. | None for this split; the planner is 727 LOC and stays above the 400 warn threshold. |
| 2026-08-18 | implemented | Recorded the verified frame `output_shape` on the `plan_verify` stage record and added it to the local plain-log allowlist. Which capability family a turn framed was previously undiagnosable, so a wrong-source answer could not be separated from a wrong-frame answer. Free-text frame fields stay out of the log because they can carry operator utterance content. | `current change`; focused planner checks `29 passed` and local service log runner checks `13 passed`; task-scoped Ruff passed. A live local turn asking for an audit-log row count logged `output_shape="aggregation_table"` with `plan_nodes="function:query.manifest,aggregate"`, proving the frame was correct and the plan selected the declaration inventory as its aggregate input. | Decide whether an `aggregation_table` plan may take `query.manifest` as its aggregate input when the question does not ask about declarations; tracked on issue #174. |
| 2026-08-13 | implemented | Adopted the implementation ledger without reconstructing earlier provenance and recorded exact-generation Rule retrieval. | Current change in `catalog_queries.py`, `operational_functions.py`, and `test_catalog_queries.py`; focused tests and diff-scoped validation passed. | Complete the IS-09 remote verification item below. |
| 2026-08-13 | implemented | Bound planner function visibility to actual runtime registrations while retaining unbound readable declarations in typed structural coverage. | Current change in `wire_semantic_query.py`, `semantic_manifest.py`, `query_manifest.py`, and their focused tests. | A durable production semantic index remains outside this change; complete the IS-09 remote verification item below. |
| 2026-08-13 | implemented | Separated invocation correlation from logical request idempotency and replaced durable requester and conversation identities with opaque references. | `current change`; `wire_read_investigation.py` and `test_wire_read_investigation.py`; focused tests passed 5 cases. | Complete the IS-09 remote verification item below. |
| 2026-08-13 | implemented | Bound catalog generations to provider-neutral, bounded ordered-document manifests and made generation identity independently reproducible from the exact document set. | `current change`; `shared/providers/catalog_search.py`, `delivery/catalog_search/generation.py`, `delivery/catalog_search/in_memory.py`, and focused catalog, ontology query, schema, and composition tests passed 41 cases. | Compose and validate the durable production semantic index; complete the IS-09 remote verification item below. |
| 2026-08-13 | implemented | Hardened the shared semantic Rule contract and the Core-to-Operator durable projection boundary. | `current change`; focused semantic tests passed 88 cases, task-scoped Ruff passed, and strict mypy passed 6 production files. | Record the governed live receipts in the ontology query coverage plan and complete IS-09 remote verification. |
| 2026-08-13 | implemented | Added bounded objective-aware candidate resolution to exact-generation Rule retrieval before scoring and top-k. | `current change`; `objective_rule_resolution.py`, `catalog_queries.py`, `catalog_search.py`, `in_memory.py`, and focused ontology query tests passed 8 cases. | P1 binding backfill, P2 durable generation evidence, and P4 rollout assurance remain open in the policy abstraction plan. |
| 2026-08-13 | implemented | Added the durable PostgreSQL generation adapter and expected-prior activation compare-and-swap for exact-generation Rule retrieval. | `current change`; `postgres.py`, lifecycle publishers, direct callers, and focused catalog tests passed 44 cases against in-memory and live PostgreSQL paths; Ruff and strict mypy passed the touched lifecycle sources. | Bind the adapter in production bootstrap and record governed IS-09 remote verification evidence. |
| 2026-08-13 | implemented | Bound the durable Rule semantic index into production startup behind exact active-generation checks and optional readiness degradation. | `current change`; `bootstrap.py`, `bootstrap_lifecycle.py`, `wire_semantic_query.py`, and focused runtime and composition checks passed 46 cases; Ruff and strict mypy passed. | Record governed IS-09 remote verification evidence. |
| 2026-08-13 | implemented | Added Core-owned typed activation closure and a durable terminal-result/outbox aggregate for Rule semantic generations. | `current change`; focused contracts and ledger checks passed 15 cases; task-scoped Ruff, strict mypy, and editor diagnostics passed. | Add exact activation binding, bounded EventBus publication, owning-agent wiring, and governed runtime evidence. |
| 2026-08-13 | implemented | Bound validated Rule generation commands to the exact target receipt and expected prior active identity before changing the semantic index. Completed-command replay returns the durable terminal result before provider access, and effect-after-error reconciliation remains fail closed. | `current change`; `activation.py`, `ledger.py`, provider and delivery activation contracts, and focused activation, ledger, generation, and live PostgreSQL checks passed; Ruff and strict mypy passed the touched lifecycle files. | Add bounded EventBus outbox publication, owning-agent wiring, and governed runtime evidence. |
| 2026-08-13 | implemented | Added bounded at-least-once EventBus publication for durable Rule generation activation results. Exact-topic acknowledgement completes the lease-fenced outbox record; failures release it for deterministic retry, cancellation preserves lease recovery, and acknowledgement persistence failure recovers by lease-expired replay. | `current change`; `publication.py`, package export, and focused publication tests passed 7 cases; task-scoped Ruff and strict mypy passed. | Wire the accountable agent and record governed runtime publication evidence. |
| 2026-08-13 | implemented | Wired Mimir as the sole Rule generation command and result subscriber, composed one shared durable ledger, and started readiness-independent outbox draining. Released transport failures retry while receipt-contract and durable-state failures remain fatal. | `current change`; focused Mimir, runtime, bootstrap, activation, and publication checks passed 32 cases; Ruff, strict mypy, translation freshness, and Korean quality checks passed. | Record a governed live runtime publication receipt; IS-09 remote verification remains open. |
| 2026-08-13 | implemented | Bound PostgreSQL topology history and reviewed metric/evidence providers into exact-release semantic query composition with fail-closed optional capability exposure. | `current change`; `postgres_topology_history.py`, `wire_semantic_query.py`, `bootstrap.py`, `bootstrap_bindings.py`, `test_wire_semantic_query.py`, and `test_bootstrap_config.py`; focused checks passed 16 cases. | Record governed live receipts in the ontology query coverage plan and complete IS-09 remote verification. |
| 2026-08-13 | in-progress | Extended the semantic projection through exact Operator reads and durable Console rendering, and added authenticated governed-receipt and seeded bilingual assurance runners. | `current change`; focused shared, Core, Operator, and Console checks pass. | Run both authenticated browser paths and link the two passing retained evidence records before claiming readiness. |
| 2026-08-13 | in-progress | Corrected strict Core receipt typing and prepared one-time Browser Entra session restoration for the authenticated evidence paths without changing principal or authority validation. | `current change`; strict mypy, Ruff, Console typecheck, design-route, and append-only checks pass. | Run both authenticated browser paths and link the two passing retained evidence records before claiming readiness. |
| 2026-08-13 | implemented | Preserved the planner's bounded clarification question in the Core semantic projection instead of replacing it with a generic terminal answer. | `current change`; `fdai_core_service/semantic_turn_processor.py`, `test_semantic_turn_processor.py`, and the focused Core processor suite passed 30 tests. | Record the governed browser and randomized-assurance evidence before claiming runtime validation. |
| 2026-08-13 | implemented | Classified a first-turn unbound incident reference as missing context before manifest or model work while preserving normal planning when prior incident context exists. | `current change`; `semantic_planning.py`, `test_semantic_planning.py`, and focused planner and terminal-projection tests passed 43 cases; task-scoped Ruff and strict mypy passed. | Retain and link governed authenticated-browser evidence before raising this scope to validated. |
| 2026-08-13 | implemented | Added deterministic declaration-driven generation for the finite question-universe denominator. Complete exact-release manifests expand through a canonical bounded grammar, while unavailable declarations remain typed exclusions. | `current change`; `question_universe.py`, `epistemic_coverage.py`, and `test_question_universe.py`; focused question-universe and epistemic-coverage tests passed 10 cases, and task-scoped Ruff and strict mypy passed. | Runtime and governed assurance evidence remain tracked by their owning coverage plan. |
| 2026-08-14 | implemented | Connected ordinary execution to effect-reconciliation request production through an existing exact V2 plan and a durable lease-fenced outbox. | `d3c0437fd`; focused reconciliation, ControlLoop, runtime, and composition checks passed 163 cases; strict mypy passed 12 production files. | Bind production exact-plan artifact and independent-observation adapters, then retain governed live closure evidence. |
| 2026-08-14 | implemented | Added an exact-release read-only FunctionType and localized projection for schema relationship questions. | `current change`; focused composition and processor tests passed 42 cases; Ruff, format, and strict mypy passed. | Restart the local stack and retain one authenticated Browser receipt for the original relationship question. |
| 2026-08-14 | implemented | Bound delivery-owned exact kinetic proposal production to Forseti's existing Verdict path without changing topics or action authority. | `current change`; focused producer, Forseti, Thor, factory, framework, Ruff, and strict mypy checks. | Bind the source in production composition and complete the pre-dispatch receipt and independent-observation path before claiming runtime validation. |
| 2026-08-14 | implemented | Added receipt-bound operational Context presentation without creating a global runtime graph route. | `current change`; `console_projection.py` and 5 focused mismatch and projection tests passed. | Bind only through an existing principal-scoped evidence response and retain authenticated Console evidence. |
| 2026-08-14 | implemented | Replaced immediate T2 semantic planning with a T1-first, deterministically evaluated cascade. | `current change`; focused semantic planner and composition tests prove T1 success, clarification, scope denial, and evidence holds do not spend T2 capacity. | Retain authenticated tier-selection evidence with the existing ontology assurance campaign. |
| 2026-08-15 | implemented | Added a bounded payload-free browser-evidence metadata method to the shared Operator read contract and implemented it behind a security-barrier service view. | `current change`; focused Operator and service-contract checks `148 passed`; service migration inventory checks `46 passed`; strict mypy passed. | Retain authenticated deployed read evidence and add the Console metadata panel. |
| 2026-08-15 | implemented | Bound one trusted evaluation timestamp to each semantic plan proposal and reused it unchanged for a bounded T2 plan retry. | `current change`; focused planner and Azure adapter checks plus task-scoped Ruff and strict mypy. | Retain authenticated evidence that an ObjectSet plan uses the server-bound cutoff. |
| 2026-08-15 | implemented | Added a shared event-bus `subscription` context manager and routed every Core consumer through it, so a cancelled consumer closes its broker subscription in its own task instead of at interpreter finalization. | `current change`; `shared/providers/event_bus.py`, all Core consumers, and `tests/agents/test_subscription_lifecycle.py`; focused agent, delivery, runtime, and provider checks passed 3127 cases; Ruff, format, and strict mypy passed. | Confirm on a live local shutdown that `event_bus_consumer_stop_timed_out` no longer appears. |
| 2026-08-16 | implemented | Made subscription teardown best effort, so a broker leave that fails records itself instead of replacing the in-flight cancellation and reporting a cancelled consumer as a crashed one. | `current change`; `shared/providers/event_bus.py`, `delivery/azure/event_bus.py`, and `tests/agents/test_subscription_lifecycle.py`; focused subscription, event-bus, multiplex, runtime, and readiness checks passed 119 cases. | Confirm on a live local shutdown that no supervisor reports a fatal consumer exit. |
| 2026-08-16 | implemented | Separated the shadow dwell ledger's bounded observation detail from its counted evidence, so evicting the oldest observations can no longer forget a policy-violation escape or under-report the sample, and a genuinely short window no longer prints the threshold it missed. | `current change`; `core/operational_learning/shadow_dwell.py` and `tests/core/operational_learning/test_shadow_dwell.py`; focused shadow-dwell, discovery, and wave governance checks passed 147 cases. | Retain governed live discovery-loop evidence before claiming operational validation of the dwell gate. |
| 2026-08-16 | implemented | Made a contested inventory resource that reports no observation time fail closed, because its conflict can only travel on a state fact that needs one and the projection otherwise published it as uncontested. | `current change`; `core/ontology_platform/inventory_projection.py` and `tests/core/ontology_platform/test_inventory_projection.py`; focused projection, adjudication, and relationship-verifier checks passed 46 cases. | Adjudicate two independent providers and projected state against telemetry, as the operating ontology already records. |
| 2026-08-15 | implemented | Added exact-release `query.manifest` for schema inventory questions and converted validated function table envelopes into the existing generic query renderer. | `current change`; focused manifest, handler, composition, relationship, semantic composition, and prompt checks passed 42 cases; task-scoped Ruff and strict mypy passed. | Retain a clean bilingual 14-cell answer-coverage artifact, then run the seeded 100-case cohort without weakening readiness. |
| 2026-08-15 | implemented | Rejected aggregate field paths that are absent from a direct ObjectSet dependency before query execution. | `current change`; focused verifier and T1/T2 tier-routing checks passed 16 cases; task-scoped Ruff and strict mypy passed. | Rerun the held Korean aggregation cell, then retain clean 14-cell and seeded 100-case Browser evidence. |
| 2026-08-15 | implemented | Propagated exact aggregate field schemas through Project, Order, and set-operation dependencies and preserved dotted projected fields for downstream aggregation. | `current change`; focused verifier, handler, and tier-routing checks passed 24 cases; task-scoped Ruff and strict mypy passed. | Rerun the held Korean aggregation cell before the clean 14-cell and seeded 100-case Browser gates. |
| 2026-08-15 | implemented | Rebound every model-proposed ObjectSet cutoff to Core's single trusted evaluation time before deterministic plan verification. | `current change`; the focused semantic-planning suite passed 15 cases, including a stale model cutoff regression. | Restart the exact-source Core stack and retain clean 14-cell and seeded 100-case Browser evidence. |
| 2026-08-16 | implemented | Bound the semantic planner and the ObjectSet query gateway to one injected clock, because the server-bound cutoff split a single `as_of` invariant across two clock reads. | `current change`; `wire_semantic_query.py`; the six composition tests reported in #134 pass and the focused composition and planner suites passed 32 cases; task-scoped Ruff passed. | None for this defect. The pre-existing ObjectSet cutoff evidence item above still stands. |
| 2026-08-16 | not-applicable | Corrected the `quality_gate/gate.py` module docstring, which described a debate `PROCEED` flipping the outcome from `DISAGREE` to `ELIGIBLE`. Outcome selection returns `DISAGREE` whenever `cross_check_below_quorum` is present and nothing removes it, so no such flip exists. Behavior is unchanged; only the description is. | `current change`; `gate.py` docstring only; `pytest services/core-control-plane/tests/core/quality_gate/` passed 145 cases; task-scoped Ruff check and format passed. | None. `test_debate_proceed_keeps_disagreement_for_human_review` is the standing guard. |
| 2026-08-16 | implemented | Restored enforcement of the composition boundary. `test_no_external_caller_reaches_into_wire_files` globbed the pre-extraction `src/**` path, matched zero files, and had passed unconditionally since sources moved under `services/*/src`. Repointing it surfaced one real violation: `runtime/control_loop.py` imported `LlmBindings` from the private `composition._helpers` while already importing `Container` from the facade, so both imports now come from the facade. | `current change`; `tests/integration/test_composition_package_split.py` passed 32 cases; `services/core-control-plane/tests/runtime/` passed 179 cases; the guard reports the `_helpers` import on the pre-fix file and nothing on the fixed file, so it is falsifiable; task-scoped Ruff check and format passed. | None for this guard. Composition seams with no runtime caller remain tracked in their owning documents. |
| 2026-08-16 | implemented | Bound each special-purpose semantic function to its exact frame output contract before execution. A manifest, schema-relationship, or incident-evidence function selected for another output is now an invalid proposal, and a T1 mismatch can retry only the plan stage with T2. | `current change`; focused tier-routing checks passed 12 cases, including mismatch escalation and a matching T1 control. | Retain clean bilingual 14-cell and seeded 100-case Browser evidence from a centrally validated descendant. |
| 2026-08-16 | implemented | Replaced the free-form semantic output shape with ten typed capability families and required each operational frame to select its minimum verified DAG capability before I/O. Frame prompt v6 and plan prompt v5 carry the same mapping, while specialized-function mismatches and missing aggregate, topology, filter, temporal, causal, or evidence nodes trigger only the bounded plan-stage retry. | `current change`; focused shape-routing and prompt checks passed 21 cases, tier-routing checks passed 14 cases, and task-scoped Ruff and strict mypy passed. | Retain clean bilingual 14-cell and seeded 100-case Browser evidence from the centrally validated descendant. |
| 2026-08-16 | implemented | Refreshed current-state ObjectSet cutoffs after bounded model planning completes. The proposal evaluation time remains fixed across T1 and T2 plan attempts, then Core rebinds only ObjectSet nodes to a fresh trusted execution time, recomputes the exact plan digest, and reverifies before I/O; topology and historical cutoffs remain unchanged. | `current change`; focused planner, tier-routing, composition, and terminal processor checks passed 93 cases, including a model delay beyond the gateway's five-second current-state skew; task-scoped Ruff and strict mypy passed. | Retain clean bilingual 14-cell and seeded 100-case Browser evidence from the centrally validated descendant. |
| 2026-08-16 | implemented | Narrowed special-purpose function alignment to final output nodes, so an exact manifest can feed a verified aggregate without masquerading as the final aggregation answer. Frame prompt v7 and plan prompt v6 classify every declaration count as `aggregation_table`, prescribe `query.manifest -> aggregate`, and keep visible resources with a declared type on the filtered-resource path with a nonempty predicate. | `current change`; focused planner, tier-routing, manifest composition, and prompt checks passed 39 cases, including production execution of a manifest count; task-scoped Ruff and strict mypy passed. | Retain clean bilingual 14-cell and seeded 100-case Browser evidence from the centrally validated descendant; generic causal questions still require a bounded visible-resource metric composition. |
| 2026-08-16 | implemented | Wrote the Hangul matching ranges in `behavior_index.py` and the web-evidence route corpus as readable `가-힣` literals instead of escaped code points. The escapes failed the `readable-hangul` gate inside central validation, which blocked the campaign branch lane at 1 of 24 commits and stalled every later batch. | `current change`; `readable-hangul` reports OK across 14304 files and the 30 tests covering the two touched modules passed. | None for this change. |
| 2026-08-16 | implemented | Added `metric_scope_series` as a read-only bridge from one secured visible-resource `QueryTable` to one canonical reviewed metric read. Empty, incomplete, or multi-resource scope remains explicitly incomplete; provider and scope gaps are both retained, and exact provider identity, unit, and window drift fails closed. The planner receives only bounded registered metric concept IDs, and plan prompt v8 prescribes `ObjectSet -> two metric_scope_series -> evidence_join` without inventing resource identities or scope-wide causal claims. | `current change`; focused service-contract, verifier, metric-semantics, composition, planner-adapter, and prompt checks passed 86 cases; Console assurance checks passed 96 cases; task-scoped Ruff, format, strict mypy, and Console typecheck passed. | Retain clean bilingual 14-cell and seeded 100-case Browser evidence from a centrally validated descendant. |
| 2026-08-17 | implemented | Canonicalized redundant transitive scope edges in candidate causal joins before Core builds the exact plan digest. The canonical plan still passes the principal-scoped manifest, reviewed metric-concept, closed argument-schema, and execution-handler checks before any read. | `current change`; the focused semantic composition regression fails as `semantic_plan_invalid` without canonicalization and passes with the verified four-node read-only DAG. | Retain a fresh strict bilingual artifact from a centrally validated source before starting the seeded cohort. |
| 2026-08-17 | implemented | Moved principal-scope evidence-validation plan formation behind the typed frame and into Core. The read uses one secured `Resource` ObjectSet and still passes the exact release, manifest, role, purpose, schema, digest, and handler checks; it adds no model-selected scope or execution authority. Frame prompt v8 and plan prompt v10 keep the capability-family contract aligned. | `current change`; focused tier-routing and prompt-registry regressions pass with zero T1 or T2 plan calls. | Retain fresh bilingual strict evidence from a centrally validated source. |
| 2026-08-17 | implemented | Bound semantic outbox ownership to an explicit durable namespace token. Prefixes remain an index selector, while append, claim, authenticated read, and projection joins use exact namespace equality so a standard Operator cannot publish an assurance-owned request through the standard physical stream. Legacy default rows remain readable through the empty token. | `current change`; focused Operator bridge checks pass 64 cases, and task-scoped Ruff and strict mypy pass. | Retain exact run-scoped transport evidence in the strict assurance artifact. |
| 2026-08-17 | implemented | Added self-contained transport evidence to raw and repository-safe assurance artifacts. The source-bound runner atomically stores only hashed topic identities and exact phase counts, and immutable gates reject missing, malformed, or incomplete proof. | `current change`; focused runner and projector checks pass 13 cases; task-scoped Ruff and strict mypy pass. | Retain a fresh exact-source strict artifact with 14/14 proof. |
| 2026-08-16 | implemented | Added the deterministic read-only operating-scope coverage projection that keeps an unmapped Resource visible as `unknown_service`. | `current change`; `core/operational_context/operating_scope.py`, `tests/core/operational_context/test_operating_scope.py`; focused operational-context, catalog, and conversation checks passed 1461 cases; task-scoped Ruff and strict mypy passed. | Bind the projection to one read-only consumer and record that consumer's focused check. |
| 2026-08-16 | implemented | Resolved analyzer targets from the durable inventory projection through a reviewed resource-type map, then merged them with explicit deployment targets under one deterministic bound. Unsupported types stay unselected, duplicate identities collapse, and projection read failures stop the tick rather than hiding a coverage loss. | `current change`; `analyzers.py`, `analyzer_targets.py`, `test_analyzer_targets.py`, and `test_analyzer_tick_routed.py`; focused analyzer checks pass. | No remaining implementation work for target resolution; governed deployment evidence remains with the deployment owner. |
| 2026-08-17 | implemented | Recorded the operational-readiness remediation bridge and the operator SRE command path in the Core map. Both were added as new modules under a routed surface without an owning design document, so the `design-doc-impact` gate rejected the branch centrally while every focused check on those batches passed. | `current change`; `core/readiness/remediation.py`, `core/incident/sre_request.py`, `shared/providers/operator_request.py`, and `composition/readiness.py`; their focused tests passed 43 cases. | None for this record; the campaign now runs `design-doc-impact` over its own batch so the omission surfaces where it is caused. |
| 2026-08-17 | implemented | Added the physical ownership map for distributed-trace continuity detection without representing a trace topology as an inventory Resource or adding a runtime service. | `current change`; detector, Azure source, shared tick runner, agent evidence handoff, and focused trace checks passed within the 55-case slice. | Retain governed live detection, Incident, approval, and recovery evidence before claiming runtime validation. |
| 2026-08-17 | implemented | Added bounded Incident search identity to the implementation-free shared Operator query while keeping normalization, evidence matching, pagination, and metrics in the Operator Service projection. | `current change`; `fdai_service_contracts/operator.py`, Operator route and PostgreSQL projection, and focused checks passed 80 Operator cases plus 49 Console cases. | No remaining implementation work for the shared Incident search contract. |
| 2026-08-17 | implemented | Recorded the principal-free observer identity record for effect-reconciliation attribution. It is a content-addressed projection of the authenticated observation context carrying stable identity handles and derived independence results, and it holds no authority to close an effect, promote a hypothesis, or execute. | `current change`; `core/ontology_platform/reconciliation_identity.py`, `reconciliation.py`, `reconciliation_contracts.py`, and `reconciliation_state_store.py`; focused reconciliation checks passed 40 cases. | None for this record; the coordinator still decides scorability from its own rules. |
| 2026-08-17 | implemented | Moved scalar and graph dynamic-simulation evidence recording from the T1/T2 fallback orchestrator into a focused adjacent mixin. The existing method names, audit records, and fail-closed hold behavior remain unchanged. | `current change`; `_dynamic_simulation.py`, `_fallback.py`, and `test_control_loop_t1_wire.py`; 16 focused cases, Ruff, format, and strict mypy passed. | None for this structural extraction. |
| 2026-08-17 | implemented | Split HIL resume executor routing, compare-and-set terminal persistence, request integrity, and canonical audit construction into focused adjacent modules. Approval policy, public results, self-approval refusal, idempotency, and fail-closed dispatch remain on the same coordinator path. | `current change`; `hil_resume/{audit,dispatch,integrity}.py`, `coordinator.py`, and focused HIL tests; 45 cases, Ruff, format, and strict mypy passed. | None for this structural extraction. |
| 2026-08-17 | implemented | Moved Heimdall's pure bounded-map, trace-evidence, and severity normalization helpers under the private agent framework. Read-investigation ownership, Event handling, incident correlation, and observer authority remain in `heimdall.py`. | `current change`; `heimdall_helpers.py`, `heimdall.py`; 21 focused observer cases and 11 framework-layout cases passed. | None for this structural extraction. |
| 2026-08-17 | implemented | Moved Norns' deterministic learner transitions under the private agent framework while keeping state, candidate construction, consensus, and publication in `norns.py`. | `current change`; `norns_learning.py`, `norns.py`; 97 focused learner cases and 11 framework-layout cases passed. | None for this structural extraction. |
| 2026-08-17 | implemented | Moved Forseti's deterministic judgment implementation under the private agent framework while keeping agent state, typed ingress, arbitration, kinetic proposal resolution, and owned publication on `forseti.py`. | `current change`; `forseti_judgment.py`, `forseti.py`; 93 focused judgment cases and 11 framework-layout cases passed. | None for this structural extraction. |
| 2026-08-17 | implemented | Moved shadow Workflow assembly, pending-approval projection bridging, approval-load policy loading, and stewardship-to-HIL escalation mapping into focused runtime support. `control_loop.py` keeps the existing private helper aliases and authoritative assembly order. | `current change`; `control_loop_support.py`, `control_loop.py`; 8 focused workflow and Thor-port assembly cases, Ruff, format, and strict mypy passed. | None for this structural extraction. |
| 2026-08-17 | implemented | Split runtime bootstrap into focused Pantheon initialization and task-supervision modules while retaining startup readiness before task launch, the authority ceiling, task isolation, callbacks, shutdown ordering, and the existing `main()` entry point. | `current change`; `bootstrap_pantheon.py`, `bootstrap_tasks.py`, `bootstrap.py`; 66 focused bootstrap and catalog-ordering cases, Ruff, format, strict mypy, and the enforce-mode LOC gate passed. | None for this structural extraction. |
| 2026-08-17 | implemented | Assigned each supervised ontology-assurance Console a run-owned Vite dependency cache. This prevents concurrent Console optimizers from invalidating a measured Browser import while leaving ordinary Console cache behavior unchanged. | `current change`; `console/vite.config.ts`, `run_ontology_assurance.py`, and focused Console and supervisor checks passed 11 cases; Console typecheck, Ruff, and strict mypy passed. | Retain the governed strict and seeded artifacts in the owning ontology-assurance ledger. |
| 2026-08-17 | implemented | Moved optional namespaced Operator semantic outbox keys to a sibling physical prefix. Production default keys remain unchanged, exact namespace checks remain, and a stale process using the former broad default claim cannot match run-owned rows. | `current change`; `postgres_semantic_turn_store.py` and focused bridge checks passed 62 cases; Ruff and strict mypy passed. | Retain governed strict and seeded evidence in the owning ontology-assurance ledger. |
| 2026-08-17 | implemented | Kept evidence-family classification and temporal cutoff validation in their owning Core boundaries. The semantic cascade rejects a `validate` frame outside `evidence_validation` before plan selection, and the ontology query verifier rejects `topology_at` event cutoffs after their knowledge cutoffs before provider execution. | `current change`; focused semantic-planning and query-verifier checks passed 41 cases; task-scoped Ruff and strict mypy passed. | Retain strict 14/14 answered and complete-evidence proof before the runner starts the seeded cohort. |
| 2026-08-17 | implemented | Strengthened the Bragi frame boundary for evidence completeness without adding a question route. Prompt v10 requires the typed `validate` and `evidence_validation` pair, while Core continues to build the principal-scoped ObjectSet and verify it before I/O. | `current change`; focused prompt-registry regression passes. | Retain strict 14/14 answered and complete-evidence proof before seeded starts. |
| 2026-08-17 | implemented | Pinned Bragi's temporal plan grammar to the existing verified topology query algebra. Prompt v11 requires two `topology_at` sources and one ordered `topology_diff` output, while the deterministic verifier still owns cutoff ordering, dependency types, and provider eligibility. | `current change`; focused prompt-registry regression passes. | Retain strict 14/14 evidence before seeded starts. |
| 2026-08-17 | implemented | Let Core answer a subject clarification only when the typed evidence-validation frame targets the complete principal scope. The resolver supplies `Resource` from the readable manifest and leaves concrete or mixed clarifications untouched before the server-owned ObjectSet path. | `current change`; focused tier-routing regressions pass. | Retain strict 14/14 evidence before seeded starts. |
| 2026-08-17 | implemented | Kept two remaining frame decisions at the semantic cascade boundary. A broad evidence frame may resolve either `subject` or `resource_identity` from the readable principal scope, while `explain_change` and `causal_evidence` must agree before any plan proposal. Concrete identities and causal measure roles remain model-proposed and deterministically verified rather than inferred from sorted concepts. | `current change`; focused evidence and causal frame regressions pass. | Retain strict 14/14 evidence before seeded starts. |
| 2026-08-18 | implemented | Added a prompt-level final audit for two schema-valid frame families that deterministic plan verification cannot reconstruct from language. Frame v11 keeps count or grouping requests on `aggregation_table` and evidence sufficiency requests on `validate` plus `evidence_validation`; Core still rebuilds identities and verifies every resulting plan before I/O. | `current change`; focused prompt-registry contract passes. | Retain strict 14/14 evidence before seeded starts. |
| 2026-08-18 | implemented | Kept principal-scope denial terminal while tightening causal plan generation. Plan prompt v12 requires the principal-visible `Resource` ObjectSet to omit predicates, traversal, and root ids before two metric-scope reads and the evidence join; Core still verifies the closed DAG before I/O. | `current change`; focused prompt-registry and no-T2 scope-denial regressions pass. | Retain strict 14/14 evidence before seeded starts. |
| 2026-08-18 | implemented | Split evidence selection from evidence judgment at the frame boundary. Frame prompt v12 sends membership questions to a filtered ObjectSet and reserves server-owned evidence validation for completeness and gap decisions; the existing frame and plan verifiers remain authoritative. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Split evidence-contract subjects from ontology-object predicates in frame prompt v13. Claims and evidence-reference coverage stay on server-owned validation, while object membership by an evidence-valued property stays on the filtered ObjectSet path; no utterance literal or runtime authority was added. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Bound model-proposed ObjectSet predicates to the verifier's direct descriptor-key contract in plan prompt v13. This removes ambiguity between definition-time property keys and projected table paths without changing the manifest, ACL checks, or query authority. | `current change`; focused ObjectSet prompt contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Preserved temporal and current-state membership axes in frame prompt v14. Retained-generation deltas cannot collapse into current object filters, and current evidence-state filters cannot expand into evidence-sufficiency judgments; deterministic plan verification remains unchanged. | `current change`; focused frame prompt contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Hardened the frame cascade at its deterministic boundary. Schema-manifest frames must carry canonical declaration kinds, while measured resource listings must choose an aggregation output instead of a plain list. Both violations remain candidate-only and trigger the existing bounded T2 retry without expanding execution authority. | `current change`; full adjacent tests, Ruff, and strict mypy pass. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Central changed-tests proved the proposed typed-subject and measure-concept invariants were overbroad for the existing Pod telemetry path. Removed both runtime checks and retained only candidate prompt v15 guidance that distinguishes declaration inventory, current runtime membership, and explicit grouping without changing deterministic authority boundaries. | Failed central source `90c592bfcc673e4764e4480be7ffa54c5b66b0b8`; focused Pod telemetry composition and prompt tests now pass. | Retain strict and seeded evidence from a centrally validated corrected source. |
| 2026-08-18 | implemented | Refined candidate frame prompt v16 so lexical mention of an incident cannot select the specialized incident function. Only exact trusted incident binding selects `incident_evidence`; generic causal support and refutation stay on the causal evidence contract. Runtime authority and deterministic verifier behavior remain unchanged. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Added a final causal DAG self-audit in plan prompt v14. Generic causal evidence must close over one principal-scoped Resource set, two scoped metric windows, and one evidence join, without selecting an incident function or narrowing the server-visible scope. Deterministic verifier and execution authority remain unchanged. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Added a final candidate-family audit in frame prompt v17. Chronology used to test a stated causal claim cannot downgrade the output to temporal comparison, and current-generation ontology declaration inventory cannot collapse into a runtime ObjectSet. Runtime authority and deterministic verifier behavior remain unchanged. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Added a final relationship-scope audit in frame prompt v18. Schema relationship lookup remains restricted to exact ObjectType declarations, while principal-scoped current inventory traversal remains operational topology. Runtime authority and deterministic verifier behavior remain unchanged. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Added a generic causal completeness audit in frame prompt v19. A cause and effect stated over visible principal-scoped resources cannot be converted into an operator clarification for identities or provider concepts; deterministic grounding remains downstream and authority-neutral. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | implemented | Added a relation-verb precedence audit in frame prompt v20. Runtime traversal verbs cannot collapse into declaration inventory merely because the wording names ontology objects or relationship types. Runtime authority and deterministic verifier behavior remain unchanged. | `current change`; focused prompt-registry contract passes. | Retain strict and seeded evidence from a centrally validated source. |
| 2026-08-18 | validated | Retained the first full governed ontology-query certification from centrally validated source `a38922762ed805794b11bb9c6aaef43916f6f6c4`. The independently supervised runtime passed strict 14/14 and seeded 100/100 with exact event-bus transport, exact semantic-plan capabilities, complete evidence for every answered turn, and all authority and safety counters at zero. | Run `issue63-a38922762e-20260818T034940Z`; repository-safe baseline [`ontology-query-randomized-assurance-2026-08-18.json`](../../baselines/ontology-query-randomized-assurance-2026-08-18.json). | Preserve the raw local artifact and validate the baseline commit on the latest integration source. |
| 2026-08-17 | implemented | Bound the catalog resource-type vocabulary to the readable `Resource.type` property so the planner reads declared values and their bilingual groups, and made the plan verifier reject an operand or fragment the declared domain cannot hold. The Console intent projection now admits the six-level nesting a membership predicate needs. | `current change`; `core/ontology_platform/property_values.py`, `query_manifest.py`, `query_verification.py`, `composition/semantic_query_value_domains.py`, `packages/service-contracts/.../ontology_query.py`, `console/src/deck/intent-graph.ts`; 2317 focused ontology, composition, conversation, catalog, and contract cases plus the Console intent-graph suite passed. | Only `Resource.type` declares values; another enumerated property still grounds nothing. |
| 2026-08-18 | implemented | Projected each resource subtype's declared request terms as a single-value group of the `Resource.type` domain, and made the plan-shape log name the selected ObjectType and each filtered property with its operator. Category groups alone left a subtype word bound to no declared value, so the planner fell back to an existence predicate over the whole ObjectType and the log could not tell that plan apart from a narrow one. | `current change`; [Issue #183](https://github.com/dotnetpower/fdai/issues/183); `composition/semantic_query_value_domains.py`, `core/conversation/semantic_planning.py`; focused composition, conversation, ontology-platform, and rule-catalog checks passed 2222 cases; Ruff and strict mypy passed. | Predicate operands stay out of the log, so a narrowing defect is diagnosed from shape alone. |
| 2026-08-18 | implemented | Grounded a stated value filter deterministically before plan verification. When an utterance carries the request terms of exactly one declared value group, a bare existence predicate on that property is rewritten to `equals` or `in` over that group's declared values. Exposing the terms to the planner did not bind them, so a stated subset was still answered with the whole ObjectType. | `current change`; [Issue #190](https://github.com/dotnetpower/fdai/issues/190); `core/conversation/semantic_planning.py`; focused conversation, composition, and ontology-platform checks passed 934 cases; Ruff and strict mypy passed. | Grounding narrows only; an utterance naming two groups for one property stays with the planner. |
| 2026-08-18 | implemented | Validated the semantic frame against the question that produced it. A schema-level frame that names a runtime resource instance is rejected before plan selection and retried with T2, and an exhausted retry reports the turn unsupported rather than answering a named-resource question with declaration rows. A hyphenated token that belongs to the declared vocabulary keeps a schema question answerable. | `current change`; `core/conversation/semantic_planning_cascade.py`, `tests/conversation/test_semantic_planning_tier_routing.py`; 32 focused tier-routing cases and 931 focused conversation, composition, and ontology cases passed; task-scoped Ruff, format, and strict mypy passed. | The `Resource` declaration still exposes no state property, so a named-resource status question reaches its state through the open property bag rather than a declared field. |
| 2026-08-18 | implemented | Widened deterministic Korean current-state recognition so a resource-kind noun before `상태`, or `상태` followed by a request verb, routes as a read investigation. Peering, health, history, and attribution keep their existing precedence. | `current change`; `core/read_investigation/routing.py`, `tests/core/read_investigation/test_routing.py`; 1269 focused read-investigation and agent cases passed; task-scoped Ruff and format passed. | The deterministic route informs agent selection only; the Console semantic turn still plans every question through the model cascade. |
| 2026-08-18 | withdrawn | Lifting the provider-reported status and location onto the declared `Resource` shape was implemented and then withdrawn, because the ontology release digest it moves invalidates the promoted semantic surface's validation receipt subject. The answer path already reads and orders those fields, so it needs no further change once the declaration lands. | Full suites on the withdrawn revision: 5 failed with `validation receipt subject mismatch`; the same suites passed 1490 cases on its parent. | Reserve the declaration change for a design pass that includes the surface revalidation run. |
| 2026-08-18 | implemented | Made the state-fact lane and authority separation exhaustively executable in `shared/providers/state_evidence.py`. Every lane and authority pair is asserted, an `execution_ledger` fact is rejected in the `observed`, `derived`, and `desired` lanes, and a lane with no matrix row fails closed with the documented rejection instead of a latent `KeyError`. | `current change`; `tests/providers/test_state_evidence.py` passed 39 focused cases; task-scoped Ruff and format passed. | Bind the same separation at every projection write site so a future writer cannot persist a state fact without constructing `StateFactMetadata`. |
| 2026-08-18 | implemented | Added `shared/ontology/threshold_bounds.py` so an adaptive threshold reads its hard bound from the shipped `ontology/action-type` contract instead of restating it. `ShadowDwellThresholds` derives its floors and accuracy ceiling from that declaration, and one focused sweep proves every registered threshold stays inside its bound while the pydantic model and the JSON contract cannot drift. | `current change`; `tests/core/operational_learning` passed 88 focused cases; risk-gate, measurement, assurance-twin, and rule-catalog suites passed 1716 cases; task-scoped Ruff, format, and strict mypy passed. | Declare bounds for the two unbound `GraphModelPromotionPolicy` rate thresholds and extend the registry beyond the promotion gate. |
| 2026-08-18 | implemented | Added `core/executor/safeguards.py` as one pre-dispatch safeguard contract for every execution path. It owns the invariant check, execution fingerprint, dry-run receipt, and both lock keys; `direct_api` and `tool_call` gained a dry-run receipt and an unconditional pre-effect audit intent, and `tool_call` gained the idempotency lock. | `current change`; `tests/core/executor` passed 251 focused cases; `tests/core` passed 4961 cases; risk-gate, HIL-resume, agent, runtime, and delivery suites passed 3174 cases; task-scoped Ruff, format, and strict mypy passed. | Carry the dry-run receipt into the terminal audit entry on `direct_api` and `tool_call`. |
| 2026-08-18 | implemented | Added `runtime/venue.py`, the one module that resolves the execution venue and enumerates every venue-selected capability flag, plus a gate that fails on a reintroduced ad hoc venue read or literal comparison anywhere else in the core-control-plane source tree. | `current change`; `tests/runtime` passed 209 focused cases; the gate's integration test passed 4 cases with negative fixtures; `tests/delivery` passed 1489 cases; task-scoped Ruff, format, and strict mypy passed. | Bring the three other services under the same contract. |
| 2026-08-18 | in-progress | Added `core/standing_authority/`, the A3-E catalog schema, typed record, and deterministic evaluator. Eligibility requires every constitutional condition; an absent record, an unparsable document, or a naive clock is ineligible. The schema admits only shadow mode and resource or resource-group scope. The package is unwired by design and a test fails if the risk gate, executor, HIL resume, or control loop imports it. | `current change`; `tests/core/standing_authority` passed 38 focused cases; task-scoped Ruff, format, and strict mypy passed; the core import boundary gate passed. | Retain a governed shadow cohort with zero envelope escapes, then take an independent promotion review before any decision path may consult it. |
| 2026-08-18 | implemented | Hardened the preceding slices against their own review. The durable idempotency fingerprint keeps its original formula, because routing it through the shared safeguard helper changed a persisted comparison value and would have turned a legitimate retry against a pre-existing record into an audited conflict. A missing citing rule now refuses as `evidence_citation` instead of misreporting the audit-intent safeguard. `ExecutionVenueError` inherits both `RuntimeError` and `ValueError` so callers of either replaced signature keep working. The threshold-bound loader fails closed on an exclusive bound it does not model instead of reporting the field as unbounded. The A3-E non-wiring test asserts each guarded subsystem exists before scanning it. Two new scenarios dropped an `expected_mttr_s_max` value that nothing measures. | `current change`; executor, standing-authority, runtime, scenario, and operational-learning suites passed 783 focused cases; task-scoped Ruff, format, and strict mypy passed. | Continue the critique rounds until only low-severity findings remain. |
| 2026-08-18 | implemented | Rounds 3 and 4 removed two ways the new contracts could look stronger than they were. The dry-run refusal was unreachable because every caller passed a hash, which is never empty; the PR path now passes an empty digest when the render produced no content, and a focused test proves an empty render is refused instead of receipted. Three of the five declared venue capabilities had no production consumer, so the table documented intent rather than selecting behavior; the remaining two are split into `bus_identity_binding` and `workload_identity_source`, the CLI identity branches read the capability, and a test fails when a declared capability has no accessor in production code. `tool_call` gained the audit-intent gate test `direct_api` already had. | `current change`; executor, runtime, delivery, scenario, standing-authority, and venue-gate suites passed 1956 focused cases; task-scoped Ruff, format, and strict mypy passed; the venue contract gate passed. | `pr_manual` still records `pr_native` in its receipt because it shares the PR executor; give the executor its selected path when a manual-merge action needs a distinct label. |

### Remaining work

- [ ] Record governed IS-09 remote verification evidence and update the service-owned map state when that evidence passes.
- [ ] Bind `project_operating_scope` to a read-only consumer so `unknown_service` reaches an operator surface, and record that consumer's focused check.
- [x] Wire Mimir as the sole accountable pantheon subscriber for Rule generation commands and results, with focused proof of durable publication and safe-to-retry projection.
- [ ] Record a governed live runtime receipt for durable activation-result publication and safe-to-retry consumption.
- [ ] Retain a governed authenticated-browser receipt proving that a first-turn unbound incident reference returns `semantic_clarification_required` with the caller request id preserved and `execution_authority=false`.

## Physical service ownership

| Owner | Source | Tests | Distribution |
|-------|--------|-------|--------------|
| Core Control Plane | [fdai](../../../services/core-control-plane/src/fdai/) and [fdai_core_service](../../../services/core-control-plane/src/fdai_core_service/) | [Core tests](../../../services/core-control-plane/tests/) | `fdai-core-control-plane` |
| Operator Service | [fdai_operator_service](../../../services/operator-service/src/fdai_operator_service/) | [Operator tests](../../../services/operator-service/tests/) | `fdai-operator-service` |
| Document Ingestion API | [fdai_ingestion_api_service](../../../services/document-ingestion-api/src/fdai_ingestion_api_service/) | [Ingestion API tests](../../../services/document-ingestion-api/tests/) | `fdai-document-ingestion-api` |
| Document Processing Worker | [fdai_document_worker_service](../../../services/document-processing-worker/src/fdai_document_worker_service/) | [Worker tests](../../../services/document-processing-worker/tests/) | `fdai-document-processing-worker` |
| Isolated Executor | [fdai_executor_service](../../../services/isolated-executor/src/fdai_executor_service/) | [Executor tests](../../../services/isolated-executor/tests/) | `fdai-isolated-executor-service` |
| Service contracts | [fdai_service_contracts](../../../packages/service-contracts/src/fdai_service_contracts/) | [Contract tests](../../../packages/service-contracts/tests/) | `fdai-service-contracts` |
| Cross-service integration | Not applicable | [Root integration tests](../../../tests/integration/) | Virtual root only |

## Core Control Plane map

The Core distribution retains the complete `fdai` namespace. Internal module boundaries remain
unchanged by the physical move.

| Area | Responsibility | Source | Tests |
|------|----------------|--------|-------|
| Control loop and decisioning | Event normalization, tier routing, exact Rego allow/deny evaluation receipts, quality, risk, approval, execution coordination, recovery, and audit | [core](../../../services/core-control-plane/src/fdai/core/) | [core tests](../../../services/core-control-plane/tests/core/) |
| Execution authorization | Provider-neutral requirement outcomes, least-permissive reduction of non-empty decision sets, canonical request and inventory binding, and rejection of ambiguous identity or unbound grant proposals | [execution_authorization](../../../services/core-control-plane/src/fdai/core/execution_authorization/) | [execution authorization tests](../../../services/core-control-plane/tests/core/execution_authorization/) |
| Ontology safety platform | Exact semantic releases with catalog-loaded Interface and FunctionType declarations, release-aware query profiles and function registration, principal-scoped manifests, verified Resource-to-ResourceType classification, generic and temporal query algebra, bitemporal topology and diffs, immutable direction-generation shadow comparisons with bounded blast-radius deltas and authoritative-inventory rebuild pointers, reviewed metric concepts, topology-aware causal joins, mutation plans with separate planner-function and operational-plan lineage plus documented fail-closed argument, evidence, target, and effect validation contracts, compact typed effect-reconciliation events, authenticated independent-observer binding, and lease-fenced durable terminal outbox delivery | [ontology_platform](../../../services/core-control-plane/src/fdai/core/ontology_platform/) | [ontology platform tests](../../../services/core-control-plane/tests/core/ontology_platform/) |
| Semantic conversation planning | Whole-turn schema proposals, server-owned frame/plan identity, principal-manifest verification, async verified execution, total terminal disposition, deterministic intent graphs, exact-command compatibility cutover, declaration-driven bounded question-universe generation, epistemic-closure release receipts, and continuous coverage gates without execution authority | [conversation](../../../services/core-control-plane/src/fdai/core/conversation/) | [conversation tests](../../../services/core-control-plane/tests/conversation/) |
| Rule semantic generation closure | Typed activation commands and terminal results, exact target-receipt and expected-prior compare-and-swap, replay-before-provider suppression, atomic StateStore result/outbox persistence, lease fencing, retry scheduling, corruption rejection, and broker-acknowledged publication state without policy or execution authority | [rule_semantic_generation](../../../services/core-control-plane/src/fdai/core/rule_semantic_generation/) | [Rule semantic generation tests](../../../services/core-control-plane/tests/core/rule_semantic_generation/) |
| Ontology semantic generations | Provider-neutral bounded ordered-document manifests, self-verifying generation identity, candidate-only concrete indexes, durable PostgreSQL persistence, expected-prior activation compare-and-swap, full/incremental declaration and deployment-object documents, independent validation receipts, stale detection, and rollback | [catalog_search provider](../../../services/core-control-plane/src/fdai/shared/providers/catalog_search.py) and [catalog_search delivery](../../../services/core-control-plane/src/fdai/delivery/catalog_search/) | [catalog search tests](../../../services/core-control-plane/tests/delivery/catalog_search/) |
| Metric semantic provider binding | Alias-free reviewed metric concepts and exact `MetricProvider` windows that distinguish observed zero from provider gaps | [metric_window.py](../../../services/core-control-plane/src/fdai/delivery/metric_window.py) and [metric_semantic_catalog.py](../../../services/core-control-plane/src/fdai/runtime/metric_semantic_catalog.py) | [metric semantic catalog tests](../../../services/core-control-plane/tests/runtime/test_metric_semantic_catalog.py) |
| Operational Hypothesis Loop | Complete graph Dynamic evidence binding, deadline-bounded independent trajectory closure, supervised typed effect reconciliation from ordinary exact-plan execution, authority-free exact kinetic proposal handoff, immutable operational lineage, and Owner-HIL-governed graph-model pointer promotion | [graph evidence](../../../services/core-control-plane/src/fdai/delivery/azure/graph_dynamic_evidence.py), [closure](../../../services/core-control-plane/src/fdai/core/assurance_twin/graph_closure.py), [reconciliation](../../../services/core-control-plane/src/fdai/delivery/reconciliation_runtime.py), [ordinary request producer](../../../services/core-control-plane/src/fdai/delivery/reconciliation_request.py), [kinetic proposal producer](../../../services/core-control-plane/src/fdai/delivery/kinetic_proposal.py), [Forseti binding](../../../services/core-control-plane/src/fdai/agents/forseti.py), [lineage](../../../services/core-control-plane/src/fdai/core/operational_planning/hypothesis_lineage.py), and [promotion](../../../services/core-control-plane/src/fdai/delivery/graph_model_promotion.py) | [graph evidence tests](../../../services/core-control-plane/tests/delivery/azure/test_graph_dynamic_evidence.py), [closure tests](../../../services/core-control-plane/tests/assurance_twin/test_graph_closure.py), [reconciliation tests](../../../services/core-control-plane/tests/delivery/test_reconciliation_runtime.py), [kinetic proposal tests](../../../services/core-control-plane/tests/delivery/test_kinetic_proposal.py), [Forseti tests](../../../services/core-control-plane/tests/agents/test_decision_case_e2e.py), [lineage tests](../../../services/core-control-plane/tests/core/operational_planning/test_hypothesis_lineage.py), and [promotion tests](../../../services/core-control-plane/tests/delivery/test_graph_model_promotion.py) |
| Discovery-loop shadow dwell | Per-candidate retention of judge-and-log-only observations, self-verifying dwell evidence, and the fail-closed threshold gate Mimir re-derives before a candidate may be considered for promotion | [shadow_dwell.py](../../../services/core-control-plane/src/fdai/core/operational_learning/shadow_dwell.py) | [shadow dwell tests](../../../services/core-control-plane/tests/core/operational_learning/test_shadow_dwell.py) and [discovery dwell tests](../../../services/core-control-plane/tests/agents/test_discovery_shadow_dwell.py) |
| Operational readiness remediation bridge | Deterministic grounded proposals from one composed readiness report, emitted only where a finding's cited rule or control maps to a remediation ActionType, carrying no approval or execution authority | [remediation.py](../../../services/core-control-plane/src/fdai/core/readiness/remediation.py) and [readiness composition](../../../services/core-control-plane/src/fdai/composition/readiness.py) | [remediation tests](../../../services/core-control-plane/tests/core/readiness/test_remediation.py) and [readiness remediation service tests](../../../services/core-control-plane/tests/composition/test_readiness_remediation_service.py) |
| Operator SRE command path | One operator problem-response request bound to exactly one Incident and one idempotent typed ActionProposal under a single correlation, returning authoritative Incident, Trace, Process, and Approval links | [sre_request.py](../../../services/core-control-plane/src/fdai/core/incident/sre_request.py) and [operator_request.py](../../../services/core-control-plane/src/fdai/shared/providers/operator_request.py) | [SRE request tests](../../../services/core-control-plane/tests/core/incident/test_sre_request.py) |
| Agent pantheon | Fifteen fixed agents and their typed event runtime | [agents](../../../services/core-control-plane/src/fdai/agents/) | [agent tests](../../../services/core-control-plane/tests/agents/) |
| Composition | Provider and runtime dependency injection, including exact-release semantic query assembly, request-role executor factories, and resource-state activity publication with invocation-scoped opaque correlation | [composition](../../../services/core-control-plane/src/fdai/composition/) | [composition tests](../../../services/core-control-plane/tests/composition/) |
| Core adapters | Provider, persistence, notification, and platform adapters retained by Core | [delivery](../../../services/core-control-plane/src/fdai/delivery/) | [delivery tests](../../../services/core-control-plane/tests/delivery/) |
| Rule-catalog profile binding | One startup resolution of the governed `FDAI_PROFILE_ID` into the immutable rule tuple the T0 index and workflow guard validation both read, with fail-closed selection and grading and tenant-free startup diagnostics | [rule_profile.py](../../../services/core-control-plane/src/fdai/runtime/rule_profile.py) | [rule profile tests](../../../services/core-control-plane/tests/runtime/test_rule_profile.py) |
| Runtime | Core process lifecycle, readiness, event transport, supervision, and semantic runtime availability binding | [runtime](../../../services/core-control-plane/src/fdai/runtime/) | [runtime tests](../../../services/core-control-plane/tests/runtime/) |
| Core contracts and provider seams | Core-only types, provider Protocols, configuration, streaming, and telemetry | [shared](../../../services/core-control-plane/src/fdai/shared/) | [shared tests](../../../services/core-control-plane/tests/shared/) |
| Rule Catalog pipeline | Catalog schema loading, collection, validation, distillation, and promotion support | [rule_catalog](../../../services/core-control-plane/src/fdai/rule_catalog/) | [Rule Catalog tests](../../../services/core-control-plane/tests/rule_catalog/) |
| Reviewed Property semantic coverage | Measured coverage of reviewed Property semantics over rule-evaluated references, evidence rules for declared provider paths, a non-regression floor, and a deterministic priority backlog | [check-property-semantic-coverage.py](../../../scripts/quality/architecture/check-property-semantic-coverage.py) and [property-semantics.yaml](../../../rule-catalog/vocabulary/property-semantics.yaml) | [coverage gate tests](../../../tests/integration/scripts/test_property_semantic_coverage.py) |
| Core service entry point | Core distribution startup and service composition | [fdai_core_service](../../../services/core-control-plane/src/fdai_core_service/) | [Core package tests](../../../services/core-control-plane/tests/) |

The safety-core coverage floor applies to the deterministic tier and risk gate inside the Core
package. Their tests remain under the Core-owned test tree.

Ontology query execution rechecks the exact release, manifest, role, and purpose at runtime. Its
bounded dependency waves include queue wait in each node deadline, propagate in-flight
cancellation, and skip blocked descendants. Stable handler type, value, and runtime failures remain
`capability_failed`; their structured diagnostic allowlists only `node_kind` and `failure_type`,
without exception text, arguments, node identifiers, provider payloads, or operator data.
Composition issues bounded secured ObjectSet receipts and registers the source-derived network
and Pod telemetry functions in the exact release. Function dependencies resolve only an issued
content digest. The `catalog.search_rules` function accepts only the active Rule generation bound to
that exact release and its provider-neutral bounded ordered-document manifest. The generation digest
is independently reproducible from the exact ordered document set, so count, chunk, root, or row
drift fails validation. The PostgreSQL adapter serializes each corpus lifecycle and checks the exact
expected prior generation in the activation transaction before replacing the active pointer.
Retrieval returns candidate-only Rules with a `CatalogRetrievalReceipt` and
grants no judgment, approval, or execution authority. The resource-state investigation path keeps promoted
inventory as answer authority, runs the ontology query in shadow, and stores principal-scoped parity
receipts through StateStore. Each actual invocation receives one opaque `correlation_ref` shared by
its live and durable activity lifecycle, while opaque requester and conversation references keep the
logical question `idempotency_key` stable across retries. Separate invocations do not reuse the
correlation.
The public composition facade exports only the optional resource-state composer; implementation
types remain in the focused binder so the facade stays below its structural ceiling.
Planner manifests apply identical role and purpose filtering to ObjectType and Interface
properties. Function descriptors are emitted only for declarations whose handlers are registered
in the composed runtime. Readable but unbound function declarations remain in structural coverage
as `runtime_binding_unavailable`; this accounting grants no judgment, approval, mutation,
promotion, or execution authority. Intent evidence preserves a terminal reason while also
disclosing bounded evidence-reference truncation.
The verifier rejects outputs that don't name declared DAG nodes before I/O. Answered turns render
only bounded verified query tables, and transient projection publication retries the same durable
idempotent result before dead-lettering.
Azure semantic planning uses existing `httpx` and `WorkloadIdentity` adapters for two validated
JSON-object proposals. Composition binds resolved narrator or `t1.judge` candidates as the T1
planner and keeps `t2.reasoner.primary` candidates in a separate optional escalation adapter. Core
invokes T2 only after the T1 proposal is unavailable or fails deterministic schema, manifest,
build, or plan verification. Each proposal has a 90-second default budget and retries one
throttled candidate at most once when its bounded `Retry-After` delay fits that budget. Composition
exposes only handlers with bound authoritative providers. The
frame proposal applies the shared wire identifier constraints before Core rebuilds server-owned
digests. Structured diagnostics record only the planning stage, candidate index, failure class,
and input-free validation locations; they omit operator text and provider details. The
public composition facade re-exports the dedicated semantic query binder while remaining below its
400-line structural ceiling. Its module contract retains the `composition`, `seam`, and `container`
anchors enforced by the package layout gate. The validated `llm.mode` string selects Azure semantic
composition by value, consistent with every other LLM binder. The ObjectSet handler is rebuilt for each request role,
so a Reader cannot inherit Owner visibility and an Owner is not silently reduced to Reader. Missing
model, release, store, or transport prerequisites
remain explicit startup-readiness failures rather than an implicit `runtime=None`.
Continuous coverage receipts separate deterministic fixture structural validation from production
readiness. Only externally produced `cross_service_e2e` or `live_assurance` question receipts can
set `production_ready`; a committed `deterministic_fixture` keeps it false.
Runtime bootstrap delegates semantic readiness and vertical workload-identity construction to its
existing lifecycle and binding helpers, keeping the primary composition root below the reviewed
fanout ceiling. A thin bootstrap wrapper preserves the injected identity-builder test and fork seam.
The Operational Hypothesis Loop adds no service or agent. Complete graph prerequisites bind at
composition. Ordinary execution produces an effect-reconciliation request only from an existing
matching exact V2 plan and commits it to a durable outbox before broker publication. Missing
observation or publication failure remains held or pending evidence and never rewrites the executor
outcome. Model pointer changes remain inside the existing governance ActionType, risk, Owner
approval, Thor execution, rollback, and Saga audit path.

## Independent service map

| Service | Package responsibility | Package map |
|---------|------------------------|-------------|
| Operator Service | Authenticated route families, durable semantic bridge, process-owned bridge health, and ordered managed-identity Kafka lifecycles including bounded Live and Agent SSE fan-out | [families](../../../services/operator-service/src/fdai_operator_service/families/), [adapters](../../../services/operator-service/src/fdai_operator_service/adapters/), [streaming](../../../services/operator-service/src/fdai_operator_service/streaming/), and [composition.py](../../../services/operator-service/src/fdai_operator_service/composition.py) |
| Document Ingestion API | Upload intake, API-owned transitions, and service adapters | [package](../../../services/document-ingestion-api/src/fdai_ingestion_api_service/) |
| Document Processing Worker | Durable document processing and worker-owned adapters | [package](../../../services/document-processing-worker/src/fdai_document_worker_service/) |
| Isolated Executor | Thor-owned command handling, provider effects, receipts, and executor adapters | [package](../../../services/isolated-executor/src/fdai_executor_service/) |

These packages may depend on `fdai-service-contracts`. They do not import another service's
implementation package.
Local composition binds service-owned client lifecycles and loopback adapters inside each package.
The Operator semantic bridge, ingestion publisher, document worker consumer, and isolated Executor
therefore preserve the same logical topics, idempotency, readiness, and receipt boundaries as their
deployed managed-identity adapters.

## Shared contract SDK

[fdai_service_contracts](../../../packages/service-contracts/src/fdai_service_contracts/) owns the
versioned wire descriptors, codecs, compatibility checks, readiness records, document contracts,
operator contracts, and executor contracts shared across processes. It contains no service
composition, provider implementation, database access, or business workflow.
`IncidentPageProjection` keeps the generic `PageProjection` wire shape stable while binding an
Incident roster page to its same-snapshot outcome metrics under one read-only Operator contract.

The shared SDK also owns the no-authority ontology-query records used across the Core and Operator
boundary: semantic problem frames, bounded query DAGs, intent graphs, task receipts, and structural
coverage receipts. These records contain no provider client, ontology store, planner model, or
execution handler.
It also owns the versioned, no-authority operational activity record used to carry bounded inventory
scan, ontology projection, and current-state read evidence from Core to Operator surfaces. The
record separates logical agent ownership from the producing process and fixes
`execution_authority=false`.

Version 1.2 of the existing Operator/Core envelopes adds one bounded semantic-turn request and one
evidence-bound terminal result. The request pins authenticated roles, session ordering, purpose,
deadline, and idempotency. An answered result requires exact release, manifest, plan, execution
receipt, and evidence references. The SDK rejects semantic downgrade to N-1 instead of dropping
those fields. Runtime publication and consumption remain service-owned implementations.

The SDK also owns the logical-topic marker and deterministic consumer-group derivation used when
those two semantic channels share a physical Event Hub. Core and Operator keep separate adapters,
codecs, identities, logical topics, and offset groups; neither imports the other's implementation.
The same contract exports the canonical physical-topic default used when targeted Terraform state
has not yet materialized newly declared outputs.

The SDK also owns the execution-venue contract: the one resolver for `FDAI_EXECUTION_VENUE` and
the one table of venue-selected capability flags. It lives here rather than in a service because
every process resolves the same variable, and an independent service cannot import the core
control plane. `fdai/runtime/venue.py` re-exports it and declares no binding of its own.

The five service distributions use deployable `0.1.2` images as N-1 and `0.1.3` as N. Their existing contract-set
`1.0.0`/`1.1.0` matrix remains the cross-process compatibility boundary.
Content-addressed live evidence also binds the exact service and observation kind and requires
`observed=true`; recomputing a digest cannot convert an unobserved claim into a live receipt.

The package test tree validates SDK behavior. Cross-service N/N-1 and topology checks remain under
[root integration tests](../../../tests/integration/).

## Other repository owners

| Path | Responsibility |
|------|----------------|
| [evaluation-sdk/](../../../evaluation-sdk/) | Independently packaged evaluation contracts and runner. |
| [benchmarks/](../../../benchmarks/) | Independent benchmark drivers. |
| [extensions/](../../../extensions/) | Optional independently packaged capabilities. |
| [rule-catalog/](../../../rule-catalog/) | Catalog-as-code data. |
| [policies/](../../../policies/) | OPA/Rego policy-as-code. |
| [console/](../../../console/) | Thin operator SPA. |
| [cli/](../../../cli/) | Operator command-line client. |
| [scripts/agent/design_context.py](../../../scripts/agent/design_context.py) | Record design-context reads, reserve dirty edit paths, hard-block stale context for framework and constitutional edits, guard commit scope and destructive Git, and route repository-wide validation to explicit integration or release boundaries. |

## Related docs

| To learn about | Read |
|----------------|------|
| Complete package boundaries and dependency injection | [Project Structure](project-structure.md) |
| Conversation and ontology query implementation sequencing | [Ontology Query Coverage Implementation Plan](../interfaces/ontology-query-coverage-implementation-plan.md) |
| IS work packages and local-first sequencing | [Service Decomposition Execution Plan](service-decomposition-execution-plan.md) |
| Graduation, data ownership, and rollback gates | [Service Graduation and Data Ownership](service-graduation-and-ownership.md) |
| Control-loop authority | [Architecture instructions](../../../.github/instructions/architecture.instructions.md) |
| Agent roles and permissions | [Agent Pantheon](../agents/agent-pantheon.md) |
