# Continuous Operational Instance Graph implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Provider-native type coverage presentation | implemented | active inventory `provider_scope_coverage` metadata; `postgres_family_store.py`; `instance_explorer.py`; strict `ontology-instances.model.ts`; bilingual Inspector labels; focused Operator and Console checks | The active snapshot exposes reconciled mapped and unmapped counts, bounded unknown provider types, capture method, cutoff, and identity completeness without raw object identities, properties, catalog mutation, or execution authority. Bounded unknown change history and live latency evidence remain open. |
| Push events and durable delta overlay | implemented | `delivery/azure/activity_log.py`; realtime inventory projector and focused tests | Resource changes can update a bounded overlay. Deployment evidence remains separate. |
| Private-safe change acceleration | in-progress | `arg_resource_changes.py`; `inventory_change_acceleration.py`; inventory job composition; Operator durable invalidation SSE; Console SSE consumer and polling countdown; focused source, route, replay, and Console checks | The bounded implementation is locally assembled. Change-feed orchestration is isolated from the full reconciliation CLI without changing cursor or completeness authority. Final integrated validation remains open. Complete reconciliation remains the only relationship-completeness authority. |
| Complete inventory promotion and ontology projection | validated | `delivery/inventory_sync.py`; `runtime/inventory_ontology.py`; focused inventory and projection tests; bounded live ARG collection and authenticated Console inspection | Complete generations replace the owned subgraph atomically. Reviewed nested operational values retain the Resource observation time and generation metadata. Missing values distinguish expected-but-unrecorded, provider-not-exposed, not-applicable, unclassified, and unreviewed types without inferring health from provisioning. |
| Relationship generation convergence | implemented | `generation_relationships.py`, `inventory_redaction.py`, `arm_inventory.py`, `postgres_inventory_snapshot.py`, `inventory_projection.py`, `inventory_ontology.py`, PostgreSQL source coverage, Operator/Console evidence projection, and focused regression checks | Reviewed parents shadow generic fallback, snapshot and ontology cardinality gates agree, and unmatched bare PostgreSQL hosts retain a typed unavailable disposition. Runtime environment names and binding values are available only for the in-memory exact-identity join and are redacted before snapshot or ontology persistence. |
| Kubernetes workload observations | validated | `kubernetes_api_inventory.py`; Kubernetes live and durable Event readers; rollout, Pod recovery, and Pod diagnosis FunctionTypes; lifecycle collector and PostgreSQL store; focused inventory, event, migration, persistence, planner, receipt, composition, and runtime checks; authenticated Event API and durable cursor receipts | The UID-grounded generation preserves allowlisted rollout state. `query.resource_event_history` can narrow one exact child through immutable `uid` and `cluster_ref`. A leased bookmark watch treats `resourceVersion` as opaque, atomically appends typed observations with local monotonic cursor progress, and reports expiry, authorization, source, retention, and result-limit gaps. `query.kubernetes_pod_diagnosis` joins one exact UID with bounded lifecycle and content-free log evidence only when a real log provider is bound. It retains content digests, counts, timestamps, source identity, and explicit gaps while fixing causal and execution authority false; zero log rows remain `zero_records_unverified`. Raw Event messages, log bodies, provider payloads, and ontology writes remain excluded. The isolated validation database reached the merged Core migration head; five consecutive live cycles advanced sequence 0 to 5 and retained about 60 seconds of complete coverage. A 60-second zero-row durable read was complete only after that observed interval. This local receipt does not claim deployed retention or Pod cause/recovery. |
| Immutable Pod replacement correlation | in-progress | `kubernetes_pod_replacement_evidence.py`, `core/investigation/kubernetes_pod.py`, `delivery/pod_evidence_binding.py`; validated lifecycle retention; focused replacement reducer and CLI analyzer-path tests | The deterministic reducer admits exactly one candidate through cluster, namespace, and root-controller UIDs, and recovery requires the fresh Deployment UID to match that root controller. The production `KubernetesPodLifecycleAnalyzer` is bound in `default_analyzers` through the analyzer CLI composition root, and it derives evidence completeness and recovery closure only from the canonical replacement and recovery reducers, carried in a typed finding assessment instead of free-form metadata. A bounded scenario drives the real CLI entry point, distinguishes a same-UID container restart from a distinct-UID Pod replacement, and joins detection latency, evidence completeness, broker publication, and recovery closure in one finding receipt. Pod lifecycle evidence reaches that analyzer only through the `FDAI_POD_LIFECYCLE_EVIDENCE_JSON` composition seam; no live Kubernetes lifecycle collector is bound, so an unbound or malformed binding leaves Pod targets unsupported instead of assumed. Durable lifecycle ingestion supplies the required UID and termination evidence without name correlation or execution authority. Retained receipts are reduced into a Pod lifecycle projection that the authenticated Operator API `/detection-readiness` family and its existing Console route report as four separate answers: current state, failure history, recovery, and evidence gaps. A projection that outlives its own freshness budget withdraws its current state and recovery instead of restating them, a missing, malformed, conflicting, cause-claiming, authority-claiming, or unverified-recovery row makes the section unavailable with a named reason rather than a shortened history, and the surface carries no execution control. An authenticated live delete/recreate scenario remains open under [issue #291](https://github.com/dotnetpower/fdai/issues/291), outside issue #295. |
| Bitemporal topology history | implemented | `core/ontology_platform/topology_history.py`; PostgreSQL topology history adapter and focused tests | Current production retention, rollup, archive, and restore evidence remains open. |
| Adaptive continuous scheduling | implemented | `inventory_source_policy.py`, `inventory_scheduler.py`, PostgreSQL reconciliation state, collection health, analyzer tick CLI and local VS Code task, durable publication ledger, and focused collection checks | Source policy and deterministic scheduling are implemented. The deployed Container Apps Job and local background task run the same one-shot analyzer logic and use the same PostgreSQL-backed claim before publication. A completed broker receipt suppresses a repeated same-window finding across process restarts. Active claims fail the tick and stale unsent claims are reclaimable after a bounded lease. The runner records send intent durably before it calls the broker, so a claim is released only when the bus attests that the record was provably not sent; every other publish failure keeps the claim uncertain and requires reconciliation before a retry. An expired send lease and a broker acknowledgement whose receipt could not be recorded both stay uncertain instead of being republished. A claim-store read or write failure fails that finding closed without publishing it. Readiness separates scheduling, target discovery, metric access, event publication, and configured Log Analytics and Prometheus delay floors. Deployed operational measurement remains separate validation evidence. |
| Typed rollup | implemented | `semantic_rollup*.py`, `inventory_rollup.py`, and focused integration checks | Fact-specific aggregation and coverage contracts are implemented and locally verified. |
| Durable normalized observation history | implemented | `inventory_observation.py`, `operational_history_lifecycle.py`, `postgres_inventory_observation*.py`, `postgres_observation_lifecycle.py`, Core migrations through `20260907_core_oi16_certification_support.py`, typed replay and source-coverage checks | OI-13 and OI-14 retain exact object and relationship observations, incarnation identity, late correction partitions, deterministic correction closure, and case or legal-hold pins. Pending corrections lower source completeness, and a corrected base partition requires a newer checkpoint before purge. |
| Operational archive and bounded-history purge | validated | `operational_history_archive.py`, Azure Blob artifact adapter, PostgreSQL lifecycle store and database purge gate, deployment policy loader, lifecycle planner and fixed schedule, OI-16 synthetic campaign runner, protected workflow, exact required CI `34058713875`, supply-chain run `34058973580`, plan `34059171071`, request `34059338073`, approved campaign `34059357427`, and persisted receipt `sha256:6c9e7b5bc731776f065e25672d116c3f278ab02b2c09636191e6566a50552f0e` | The exact revision `80b5892aa176e4a71eb2b3448982825b526d175b` passed all 13 operational scenarios with no reason codes. The campaign verified the restored durable OI-15 receipt, exact attested image, complete active projection migration, independent database restart effect, append-only PostgreSQL receipt, and private merged artifact `sha256:aaefe0ec525c22ecf596e5e9169904f5a68fe8341cbfa8a85a165508259b2409`. The scope remained `synthetic/oi16-certification/`; ordinary observation retention and production resources were unchanged. |
| Active-scope projection and partial read-only answers | implemented | `postgres_inventory_{observation,projection_checkpoints}.py`, `inventory_ontology{,_state}.py`, `semantic_runtime.py`, `semantic_turn_processor.py`, and focused persistence and conversation tests | The generation-bound checkpoint scans exact active scopes while retained inactive-scope history remains pending separately. Incomplete exact-target reads return verified rows with limitations and recovery guidance; unsafe subsets still hold. |
| Operational instance semantic correctness | implemented | `operational_instance_competency.py`; focused bilingual action-draft routing checks; typed no-authority receipts | Representative typed competency and OI-11 bilingual positive and negative classification checks pass without answer-text or keyword routing. Full-corpus and scheduled validation are owned by [Continuous Semantic Assurance](../../roadmap/interfaces/continuous-semantic-assurance.md). |
| Runtime-call evidence binding | implemented | `semantic_turn_runtime.py`; `semantic_turn_consumer.py`; `runtime_calls.yaml`; `runtime_call_projection.py`; `runtime_call_telemetry.py`; `delivery/azure/runtime_call_telemetry.py`; `runtime_call_inventory.py`; `inventory_sync_cli.py`; inventory single-writer and focused endpoint checks | Operator emits a content-free caller witness after authenticated broker acceptance, and Core emits the matching target witness when broker delivery reaches its consumer boundary. The authenticated Azure Monitor source activates only in the deployed venue under an explicit platform flag, accepts only the reviewed Container Apps log schema, keeps an empty query unavailable, re-reads each platform revision and replica beneath the claimed exact ARM ID, joins only equal observation and endpoint tuples, converts both independently verified ARM identities through the canonical graph mapping, and reduces repeated joined calls to the newest exact pair. A 60-second trailing guard and extended query window retain in-flight and cutoff-straddling pairs without accepting stale evidence. Any invalid, expired orphan, partial, redacted, or malformed candidate makes the batch incomplete. The reviewed `runtime_calls` LinkType is registered in the projection contract, so verified endpoint direction and Resource cardinality survive current and historical projection. Authenticated deployed runtime evidence remains open. |
| Authorization and PostgreSQL role evidence | implemented | `postgres_role_evidence.py`; `arg_relationships.py`; focused principal-redaction and authorization-scope checks | Database roles remain a separate principal-safe projection with content-addressed references and no Resource or Link shape. Unmodeled role-assignment child scopes retain `authorization_child_scope_unmodeled` and never become inferred edges. |
| Operator instance exploration | validated | `instance_explorer.py`, `postgres_family_store.py`, Operator realtime-overlay read migration, `ontology-instance-graph*.ts*`, focused Operator and Console checks, and authenticated standard-port evidence | The read-only Console retains the complete depth-8, 200-Resource, 1,600-link bounded response for inspection and context without using Resource Groups as transit hubs. The existing Instances canvas uses a strict left-to-right (`LR`) coordinate contract: every rendered stored source occurrence is left of its target occurrence, including reciprocal links and bounded cycles, without reversing the stored edge. Incoming occurrences stay left of the selected Resource and outgoing occurrences stay right. Semantic columns use a 288 px pitch. Operators can zoom from 10% to 180% with an ordinary wheel, enter native full screen, and drag empty canvas space to pan; node coordinates and node selection remain fixed. `contains` is a solid hierarchy line, `attached_to` is dashed, and reciprocal `peered_with` is dotted. Root-direct containment and at most three ancestor edges restore Resource Group, VNet, and Subnet hierarchy, while Resource Group and Subscription remain visible non-transit context. Exact AKS node-Resource-Group evidence adds a branch-local hierarchy without inventing an AKS-to-VMSS edge. AgentPool and VMSS cards retain their latest committed provider capacities as explicitly labeled node and instance counts; absence never becomes zero and NodePool capacity never claims Kubernetes Node readiness. Acyclic same-side nodes use the longest predecessor rank; reciprocal and cyclic edges alone use duplicate occurrences. Only exact reviewed gateway, load-balancer, and AKS outbound mappings can add verified traffic meaning; other relationships retain honest graph-direction labels. The Inspector separates direct incoming, direct outgoing, verified ingress, verified egress, access, containment, and connected path segments with mapping evidence. Azure Activity Log, Resource Health, and the runtime call graph remain explicitly unavailable. |
| Application-centric provider relationships | in-progress | `azure-arg-v1.yaml`, ARG plus bounded ARM child collection, Kubernetes API pre-promotion enrichment, complete-generation verification, endpoint closure, snapshot classification metadata, focused Core/Operator/Console checks, Terraform service-root checks, and authenticated `5273` evidence | The 84 reviewed mappings cover exact containment, identity, authorization, registry, observability, network, ingress, configured data-service references, Private DNS closure, AKS AgentPools, and Kubernetes runtime topology. Azure nested children use reviewed parent or root extraction. UID-grounded Kubernetes objects and independently verified links join the same generation before atomic promotion when exact TLS, workload-identity, and cluster bindings are configured. The read-only identity acquires a short-lived token at request time; no static Kubernetes token enters Terraform. Existing local Azure evidence remains unchanged and no live Kubernetes receipt is claimed. |
| Source-to-store implementation audit | implemented | `config/continuous-operational-instance-graph-audit.json`; `check-continuous-operational-instance-graph-audit.py`; focused audit tests (`3 passed`) | OI-01 fixes the exact owner, binding, focused tests, state, and missing binding for 16 stages without claiming runtime validation. |
| Kubernetes container termination projection | implemented | `delivery/kubernetes_api_inventory.py`; `test_kubernetes_api_inventory.py` (`9 passed`) | Current and previous terminated container state retains bounded identity, reason, exit code, signal, and finish time while excluding messages, runtime ids, image data, and raw payloads. Durable lifecycle ingestion and log diagnosis remain separate work. |
| Exact-Pod termination and runtime-log diagnosis | implemented | `kubernetes_pod_diagnosis_{evidence,queries}.py`; `kubernetes_pod_log_evidence.py`; `azure/telemetry_query.py`; focused reducer, provider, FunctionType, composition, and bootstrap checks (`106 passed`) | A real configured log provider makes the exact-Pod function available. The result retains no raw log body and grants no causal or execution authority. Source-specific unavailable reasons, deterministic semantic planning, authenticated Console evidence, and durable lifecycle input remain open. |
| Typed rollup and archive lifecycle | implemented | `semantic_rollup*.py`, `archive_*.py`, `inventory_rollup.py`, PostgreSQL archive adapter, Core service migration, and focused integration checks | Rollup and archive contracts are implemented and locally verified. No Azure archive store or deployed purge was invoked. |
| Graph-first conditional live enrichment | in-progress | `graph_evidence_refresh.py`, `inventory_live_evidence.py`, partial-overlay persistence, and focused tests | The pure decision and safe write-through path are implemented. Ordinary semantic query composition does not yet connect them end to end. |
| Resource Health exact-denominator evidence | implemented | `resource_health_evidence.py`, `resource_health_queries.py`, `delivery/azure/resource_health_collection.py`, semantic assurance projection, Operator presentation, and focused replay checks | The secured caller's exact Resource set is the denominator. Canonical availability, per-target coverage, provider time, and the collection window stay separate. Missing, blank, duplicate, invalid, unmodeled, modeling-unknown, unreadable, unresolved, and truncated evidence cannot produce a complete result. The tuple-set reread fence and out-of-band answer validator remain open below. |
| Runtime-call evidence foundation | implemented | `runtime_calls.yaml`; `runtime_call_projection.py`; `runtime_call_telemetry.py`; `delivery/azure/runtime_call_telemetry.py`; focused catalog, endpoint, source, and authorization checks | A bounded Azure Monitor adapter carries exact caller and target Resource IDs into separately authenticated telemetry records. The existing inventory enricher remains the single writer; malformed, incomplete, stale, redacted, or out-of-generation records add no edge. Deployed evidence remains separate. |
| Operational state-transition ledger | implemented | `state_transitions.py`; `postgres_state_transitions.py`; Core service migration; inventory promotion publisher; focused contract, persistence, migration, and publisher checks | Content-addressed bitemporal transitions and positive coverage are append-only. Inventory snapshots produce real state edges but keep interval coverage incomplete until a continuous source proves the whole window. |

### Implementation history
| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-12 | implemented | Exposed the active snapshot's machine-readable provider-native type coverage through a fail-closed Operator decoder and strict Console model, including bounded unknown type counts and identity completeness without raw provider objects. | `current change`; focused Operator projection suites passed 83 tests; Console model passed 79 tests; Ruff, strict mypy, Console source/test typecheck, and catalog parity passed. | Add bounded unknown-type change history and retain representative provider-change-to-observation and observation-to-screen latency evidence under Issue #444. |
| 2026-09-12 | implemented | Aligned realtime ARG change hydration with full-snapshot provider coverage by retaining unmapped types as bounded `unclassified-resource` evidence. Explicit runtime collection filters remain authoritative, and no provider row creates an ontology declaration or execution authority. | `current change`; focused unknown-type and allowlist checks passed 3 cases. | Expose provider-type coverage and bounded unknown history through Operator and Console, then retain representative live latency evidence under Issue #444. |
| 2026-09-12 | implemented | Bound every protected Core image transition to its exact source revision and limited the fixed Heimdall recovery observer to first adoption through the explicit Core evidence transition. | Failed protected Core plan `34637615112`; `guard_plan.py`, `service-deploy.yml`, `service-matrix.json`; focused service-deploy suites passed 299 cases in the current change. | Publish the corrected controls and retain the exact Core plan, apply, health, and rollback evidence under Issue #290. |
| 2026-09-12 | implemented | Split pure ARG change-feed value handling and inventory watermark decoding from oversized delivery modules without changing cursor, completeness, replay, or writer authority. | `current change`; enforced LOC gate; focused inventory checks (`82 passed`, 26 service-migration skips). | No behavioral work remains for this structural correction. |
| 2026-09-12 | validated | Completed Issue #260 runtime-call evidence rollout and authenticated standard-port validation. The current Operator projection reports `runtime_call_graph=available`, retains one `runtime_calls` edge, and keeps execution and mutation authority false. | Recovery merge `01232ef9f`; CI `34640383008`; supply chain `34640382984`; migration merge `fcc5a15c4`; CI `34636617476`; protected plans/applies `34637896418`/`34638209183` and `34641018477`/`34641355939`; authenticated `localhost:5273` Ontology rendering; authenticated `127.0.0.1:8010` `/ontology/graph` and `/me/context` responses; relevant standard-port production source byte-identical to pushed green `01232ef9f`. | No remaining work for Issue #260. Broader inventory relationship completeness remains independently governed. |
| 2026-09-12 | implemented | Allowed a fresh complete snapshot to derive state transitions from retained complete topology history when its declared ontology base is the exact skipped incomplete generation. Other generation mismatches remain blocked. | `current change`; post-migration scheduled scan reached authoritative promotion and then failed with `inventory ontology state base generation is incomplete`; focused retained-history fallback and mismatched-generation checks. | Publish and deploy the recovery fix, request a fresh reconciliation, and retain authenticated available-source evidence. |
| 2026-09-12 | implemented | Extended Core snapshot and real-time inventory link constraints with the reviewed `runtime_calls` LinkType and a rebuildable-link-only downgrade. | `current change`; live requested ARG snapshot returned 1,144 resources, 2,024 links, one final fence, and complete provider coverage; promotion failed with `inventory_snapshot_link_link_type_check`; focused service migration metadata checks. | Publish the migration, run protected Core migration and Inventory image deployment, request a fresh reconciliation, and retain authenticated available-source evidence. |
| 2026-09-11 | implemented | Connected the existing adaptive scheduler `operator_requested` axis to an explicit one-shot Inventory Job environment input. It advances only a healthy idle collection and cannot bypass in-progress or provider-pressure decisions. | `current change`; repeated successful but not-due one-shot executions while the adaptive source policy ignored interval overrides; focused config, CLI, legacy gate, and adaptive scheduling checks. | Publish and deploy the one-shot input, request one fresh reconciliation, and retain authenticated available-source evidence. |
| 2026-09-12 | implemented | Kept the standalone deploy host's exact-registry `AcrPush` assignment in the governed write plane. Import and digest readback do not create provider observations, operational graph facts, or read-plane authority. | `current change`; root Terraform role scope; standalone image-import receipt contract | No operational-instance graph behavior changed. |
| 2026-09-11 | implemented | Moved newest-observation selection ahead of ARM replica verification so superseded observations from replaced replicas cannot poison the current exact endpoint pair. A newer unverified observation still makes the source incomplete. | `current change`; successful reducer apply `34616821912`; exact post-canary caller/target pair; all current pair ARM identities verified; older replaced replicas inside the 21,600-second window still yielded `telemetry_rows_incomplete`. | Publish and deploy the prefilter, reconcile the retained latest pair, and capture authenticated available-source evidence. |
| 2026-09-11 | implemented | Split the runtime-call KQL and endpoint witness value into a focused contract module and removed reducer variable shadowing after exact PR CI found the adapter above its structural ceiling and a strict typing conflict. | `current change`; failed CI `34613156306`; strict mypy passed; source adapter reduced from 803 to 763 lines; focused telemetry and Inventory checks remained green. | Publish the corrected reducer, deploy its attested image, reconcile the retained latest pair, and capture authenticated available-source evidence. |
| 2026-09-11 | implemented | Reduced verified runtime-call observations to the newest observation per exact endpoint pair before evaluating pair completeness. A newer complete pair supersedes older target-only rows, while a newer unpaired observation remains guarded and then incomplete. | `current change`; successful Inventory apply `34604993918`; successful Operator namespace apply `34611920621`; exact live paired witness; deployed 21,600-second freshness window retained older target-only rows and returned `telemetry_rows_incomplete`. | Publish and deploy the reducer, reconcile the retained latest pair, and capture authenticated available-source evidence. |
| 2026-09-11 | implemented | Extended the independent-service runtime-call transition guard to admit only the exact Operator API and channel-edge outbox namespace additions or removals for their matching Container Apps. | `current change`; rejected zero-destroy Operator plan `34608950506`; focused accepted API, accepted channel-edge, and rejected swapped-namespace guard checks. | Publish the guard, rerun and apply the zero-destroy Operator plan, then retain paired authenticated evidence. |
| 2026-09-11 | implemented | Isolated the independent Operator API and channel edge with distinct fixed semantic outbox namespaces so only the API runtime can claim and publish its caller-bound requests. | `current change`; repeated live target-only observations while both deployments shared the default empty namespace; one paired observation after a bounded Operator restart; focused Terraform mapping checks. | Publish and apply the independent Operator service transition, generate a fresh authenticated request, reconcile Inventory, and retain available-source evidence. |
| 2026-09-11 | implemented | Clamped global and active-scope projection checkpoints to the journal high watermark observed by the same snapshot append so concurrent later journal writes cannot create an impossible checkpoint. | `current change`; failed runtime apply `34602649701`; live recovery error `inventory ontology active-scope checkpoint exceeds journal`; focused checkpoint and recovery checks. | Publish the clamp, deploy its attested Inventory image, and retain authenticated available-source evidence. |
| 2026-09-11 | implemented | Made content-addressed state-transition coverage reusable when pending projection recovery recalculates a different batch over identical retained coverage. Batch replay resolves expected children by content identity, while changed transition idempotency keys remain rejected. | `current change`; failed runtime apply `34600264194`; retained `operational_state_transition_coverage_pkey` collision; focused PostgreSQL transition-store checks. | Publish the replay fix, deploy its attested Inventory image, and retain authenticated available-source evidence. |
| 2026-09-11 | implemented | Bound environment-integrated Container Apps log rows with an empty `_ResourceId` to the exact claimed ARM endpoint through the platform-stamped app, revision, replica, container name, and normalized container ID. A non-empty `_ResourceId` must still match exactly. | `current change`; successful runtime plan `34594505011` and apply `34594800594`; exact live caller/target pair; live rows exposed empty `_ResourceId`; both platform container IDs matched the exact ARM replica container identities after normalization. | Publish the source hardening, deploy its attested Inventory image, reconcile the retained pair, and retain authenticated available-source evidence. |
| 2026-09-11 | implemented | Rebased a promoted full snapshot that skipped a degraded base onto the actual manifest generation while retaining fail-closed handling for an unavailable manifest. | `current change`; PostgreSQL recovery metadata helper and focused rebase checks; failed apply `34590968364`. | Publish the rebase fix, deploy its attested image, and retain available-source evidence. |
| 2026-09-11 | implemented | Allowed a degraded pending ontology projection to retain its failure evidence while fresh authoritative collection proceeds, without weakening failure for an incomplete current projection. | `current change`; typed incomplete recovery error and focused observer/recovery checks; failed apply `34587342094`. | Publish the recovery fix, deploy its attested image, and retain available-source evidence. |
| 2026-09-11 | implemented | Allowed only the Inventory image updater's state-only replacement so a newer attested image can replace the replay-incompatible revision without permitting Azure resource destruction. | `current change`; protected plan guard and focused runtime workflow checks; failed apply `34582156767`; replay fix `c9d212049`. | Publish the guard, plan and apply the green `6d60d4b79` image, and retain available-source evidence. |
| 2026-09-11 | implemented | Added a separate workspace transition resource so an already-created flag transition cannot suppress the newly required Log Analytics binding update. | `current change`; transition Terraform, three-address scope and convergence checks; failed apply `34576608318`. | Publish the new transition resource, run a fresh zero-destroy plan/apply, and retain available-source evidence. |
| 2026-09-11 | implemented | Added exact Log Analytics workspace binding, paired rollback, and workspace-digest readback after the fixed image required the missing monitor workspace at startup. | `current change`; binding updater, Terraform transition, and readback checks; failed apply `34576608318`. | Publish the workspace transition, run a fresh zero-destroy plan/apply, and retain available-source evidence. |
| 2026-09-11 | implemented | Added an exact Inventory Job image transition and digest readback after the enabled Job exposed the already-fixed pending-relationship recovery defect in its stale image. | `current change`; transition Terraform, runtime request validation, image updater rollback, and readback checks; failed verification apply `34572099320`; source fix #711. | Publish the image transition, plan the attested fixed revision with zero destroys, apply, and retain available-source evidence. |
| 2026-09-11 | implemented | Replaced the insufficient no-refresh Job target with a dedicated one-time transition resource after configuration dependencies still entered the bounded plan. | `current change`; `runtime-call-evidence-transition.tf`; verified updater and rollback checks; rejected plan `34564679010`. | Publish the transition resource, rerun the exact plan, and apply only a one-address zero-destroy result. |
| 2026-09-11 | implemented | Isolated the runtime-call transition plan from unrelated dependency refresh after the bounded scope guard rejected scheduler, network, and PostgreSQL drift. | `current change`; protected deploy workflow; focused Inventory Job workflow contract. | Publish the workflow fix, rerun the exact plan, and apply only a one-Job zero-destroy result. |
| 2026-09-11 | implemented | Refreshed the generated semantic-intent coverage inventory after the ontology-platform projection hardening changed its complete source digest. | `current change`; `build_semantic_intent_coverage.py`; generated-artifact equality test. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Added an exact runtime-call-evidence plan-scope mode that rejects every dependency-induced change outside the Inventory Job. | `current change`; `enforce_plan_scope.py`; focused accepted and rejected scope checks; broad plan `34528218674` retained but not applied. | Publish the scope guard and rerun the bounded plan after prerequisite drift is resolved. |
| 2026-09-11 | implemented | Restored the runtime-call platform transition input to the block mapping required by GitHub Actions while keeping the workflow at its review-size ceiling. | `current change`; deploy workflow; focused input-shape, actionlint, and workflow-budget checks; failed push validation `34524781897`. | Publish the workflow repair and rerun the bounded plan. |
| 2026-09-11 | implemented | Corrected the bounded platform transition to `plan-runtime-*` and `apply-runtime-*` request prefixes after GitHub rejected a 26th dispatch input. The target, mixed-operation denial, and context binding remain unchanged. | `current change`; protected deploy workflow, request validator, focused workflow, input-budget, and context-digest checks; failed push validation `34524781897`. | Publish the corrected workflow, run the zero-destroy plan and apply, then retain available-source evidence. |
| 2026-09-11 | implemented | Added a context-bound platform `runtime_call_evidence_transition` that targets only the Inventory Job and rejects every mixed deployment target. | `current change`; protected deploy workflow, request validator, focused workflow and context-digest checks. | Set the reviewed variable, run the bounded zero-destroy plan and apply, then retain available-source evidence. |
| 2026-09-11 | implemented | Decoupled the deployed Inventory Job runtime-call source from legacy Operator API ownership and bound it to repository Variable `ENABLE_RUNTIME_CALL_EVIDENCE`. | `current change`; root Terraform variable, compute wiring, protected workflow, and focused infrastructure contract check. | Set the reviewed deployment variable, apply a zero-destroy platform plan, and retain available-source evidence. |
| 2026-09-11 | implemented | Prevented disabled change accelerators from reading omitted collection-policy entries or contributing cursor deadlines. | `current change`; `inventory_sync_cli.py`; focused disabled-accelerator reconciliation check. | No remaining work for this fail-closed composition correction. |
| 2026-09-11 | implemented | Restricted unavailable projection-source reasons to canonical machine tokens so principal text, endpoints, and provider details cannot cross the Operator read boundary. | `current change`; `postgres_family_store.py`; focused principal-text rejection check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Bound Operator runtime-call link decoding to the exact selected inventory generation so stale or substituted metadata cannot be relabeled as current evidence. | `current change`; `postgres_family_store.py`; focused mismatched-generation rejection check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Recovered a migrated Operator runtime name from independent peer state for Core plans through a mode-0600 transient file, then resolved both exact endpoint IDs through Azure without adding the name to the sanitized manifest. | `current change`; `peer_state.py`; `capture_peer_states.sh`; protected service workflow; focused peer-state and workflow checks. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Required explicit runtime-call and PostgreSQL-role source states in current Console instance-detail decoding so omission cannot become availability or measured zero. | `current change`; `ontology-instances.model.ts`; focused missing-source decoder check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Required canonical unpadded Container App ARM IDs in both independent service roots and rejected partial, malformed, trailing-slash, whitespace-padded, or same-endpoint runtime-call bindings. | `current change`; Core and Operator service variables; focused workflow contract and Terraform service-root tests. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Removed the low-entropy PostgreSQL role name from principal-handle derivation and bound the handle to opaque authenticated references plus scoped source context. | `current change`; `postgres_role_evidence.py`; focused role-renaming privacy check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Enforced the no-authority invariant on PostgreSQL role evidence and projection construction at runtime. | `current change`; `postgres_role_evidence.py`; focused authority-forgery checks. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Restricted runtime-call incomplete-source coverage to three fixed count keys and rejected arbitrary labels that could disclose provider identifiers. | `current change`; `runtime_call_inventory.py`; focused raw-key rejection check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Guaranteed Core semantic-consumer cancellation stops its progress publisher even while progress draining is blocked. | `current change`; `semantic_turn_consumer.py`; focused cancellation and task-lifecycle check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Moved the Operator caller-witness timestamp and record construction after authenticated broker acceptance so evidence time cannot precede the observed boundary. | `current change`; `semantic_turn_runtime.py`; focused ordered publish and witness checks. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Bound each runtime-call log witness to the platform-stamped exact Container App Resource ID before accepting its name, revision, replica, or claimed endpoint. | `current change`; `delivery/azure/runtime_call_telemetry.py`; focused mismatched-source-identity checks. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Bound runtime-call verification receipts and projection digests to both endpoint Resource types as well as their exact IDs. | `current change`; `runtime_call_projection.py`; focused replay and type-variance checks. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Rejected non-finite and non-positive runtime-call authentication deadlines so a stalled verifier cannot disable bounded fail-closed behavior. | `current change`; `runtime_call_telemetry.py`; focused timeout checks and Ruff. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Removed the untrusted runtime-call envelope's public projection conversion. Only the authenticated producer now creates projection input after exact digest and independent-context checks. | `current change`; `runtime_call_telemetry.py`; focused telemetry producer tests and Ruff. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-10 | implemented | Added an account-specific operational-history Blob A record to the ops-owned private DNS zone linked to the deploy runner VNet. The bounded operational-history target pulls it through the lifecycle Job dependency, and the scope guard admits only the exact record address without enabling public access or key authentication. | `current change`; protected OI-12 run `34447462177` completed all seven axes and wrote its private receipt before runner readback failed; runner guest DNS diagnostic; provider-observed missing ops-zone record; focused Terraform and scope checks. | Publish the runner DNS binding, execute and review the protected operational-history plan/apply, prove private Blob resolution from the runner, and retain a passing protected OI-12 artifact under Issue #262. |
| 2026-09-10 | implemented | Fenced the OI-12 provider failure/recovery query to the current active generation's source and observation kind. Retired sources cannot keep the current operational instance axis permanently unavailable; recovery still requires a later successful snapshot with the exact failed source, observation kind, scopes, and resource types. | `current change`; failed protected certification `34445258249`; sanitized read-only production aggregate showed no later success for retired `arm/observed` and an exact 194.335387-second recovery for active `arg/observed`; focused PostgreSQL certification checks. | Publish the active-source fence, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-10 | implemented | Narrowed the inventory ARM start body to the stable `JobExecutionTemplate` schema after the live endpoint rejected Job-owned `volumes`. The materializer validates the complete reviewed Job and preserves container command, arguments, environment, resources, secret references, volume mounts, and init containers while emitting only `containers` and `initContainers`; configured volumes remain inherited from the Job. | `current change`; failed protected certification `34442888325`; stable Container Apps `2024-03-01` OpenAPI schema; focused materializer and workflow checks. | Publish the schema-correct start body, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-10 | implemented | Replaced the inventory CLI image override with a stable ARM start request that copies the reviewed live template and changes only the canonical container image. The transient mode-0600 body preserves command, arguments, environment, resources, volumes, and secret references. | `current change`; failed protected certification `34440577232`; sanitized failed-execution template; focused workflow checks. | Publish the template-preserving start path, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-10 | implemented | Corrected the resource-group equality guard to one valid Bash binary predicate after the protected runner rejected a newline after `!=`. The exact provider-observed equality requirement is unchanged. | `current change`; failed protected certification `34439221020`; focused workflow contract checks; actionlint. | Publish the syntax correction, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-10 | implemented | Extended OI-12 legacy binding to a missing platform resource-group output. The fallback reads only `Microsoft.App/jobs` under the 64-resource bound and adopts a group only when the unique inventory and history runtimes report the same provider-observed value. | `current change`; failed protected certification `34431383809`; sanitized exact-runner guard reproduction; focused workflow checks. | Publish the resource-group fallback, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-10 | implemented | Split OI-12 job resolution, exact OCI provenance verification, and ACR binding into separate protected steps. Only the verified repository, revision, and digest cross from verification to binding through `GITHUB_ENV`. | `current change`; failed protected certification `34429999806`; focused workflow contract checks. | Publish the split workflow, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-10 | implemented | Removed the Docker CLI dependency from OCI registry authentication after the protected runner rejected that bootstrap step. Credentials remain confined to a mode-0600 file inside a mode-0700 temporary directory and never enter process arguments or output. | `current change`; failed plan-only run `34427330193`; executable binder regression and focused workflow checks. | Publish the correction, pass exact CI, and retain a successful registry-hosted provenance verification in the protected plan. |
| 2026-09-10 | implemented | Authenticated OCI attestation verification through a mode-0700 transient Docker config after the initialized runner had no GHCR credential store. The token enters through stdin, credentials are removed at exit, and authentication or verification failures are explicit. | `current change`; failed protected certification `34419767892`; local ephemeral-config digest verification; focused credential-hygiene and image-binding checks. | Publish the verifier authentication fix, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-10 | implemented | Extended the bounded ARM fallback to the legacy history Job ID and archive URL after their Terraform root outputs were empty. One enumeration now resolves exactly one inventory runtime and one history runtime, cross-checks any non-empty output, and re-reads the selected history Job through the stable resource API. | `current change`; failed protected certification `34416935783`; sanitized runner reproduction; bounded live ARM validation found one exact inventory and one exact history contract; focused workflow checks. | Publish the history fallback, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-09 | implemented | Replaced the unavailable provider collection endpoint with bounded generic ARM resource enumeration and stable per-resource Container Apps reads. The fallback admits at most 64 Job IDs, gives each resource read 30 seconds, and still requires exactly one reviewed inventory runtime contract. | `current change`; failed protected certification `34410700086`; bounded live enumeration found 12 Jobs and one exact contract match; focused workflow contract checks. | Publish the bounded enumeration, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-09 | implemented | Pinned the ARM inventory Job lookup to the stable Container Apps `2024-03-01` API after the runner CLI extension selected an unsupported default version. Exact resource-group and reviewed runtime-contract cardinality checks remain unchanged. | `current change`; failed protected certification `34406488996`; stable-API live read returned one reviewed contract match; focused workflow contract checks. | Publish the API pin, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-09 | implemented | Replaced the unavailable state snapshot fallback with an ARM-observed inventory Job lookup restricted to the exact platform resource group. The fallback requires one container with the reviewed runtime name, command, and empty arguments, and does not derive identity from the Job name. | `current change`; failed protected certification `34403565287`; bounded live ARM cardinality diagnostic; focused workflow contract checks. | Publish the ARM fallback, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-09 | implemented | Replaced implicit Terraform state rendering in the OI-12 legacy inventory Job fallback with a mode-0600 point-in-time state snapshot. The workflow resolves exactly one tracked address and deletes the snapshot at step exit. | `current change`; failed protected certification `34400981555`; actual private-state address diagnostic; focused workflow contract checks. | Publish the snapshot fix, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-09 | implemented | Recovered protected OI-12 inventory Job identity from the exact tracked Terraform state address when the deployed platform state predates the root output. The fallback requires exactly one state resource and never derives identity from a name. | `current change`; failed protected certification `34389423964`; `.github/workflows/operational-instance-certification.yml`; focused workflow contract checks; the bounded private-state diagnostic resolved the exact address. | Publish the compatibility fix, pass required CI, publish the exact attested Core image, and retain a passing protected OI-12 receipt under Issue #262. |
| 2026-09-09 | implemented | Closed the bare PostgreSQL-host evidence gap and removed runtime environment bindings after exact complete-generation relationship extraction. Added an exact-source protected OI-12 workflow that refreshes inventory before measuring seven internally generation-fenced axes. | `current change`; 347 focused deployment, graph, OI-12, and assurance checks; Ruff, strict mypy, workflow validation, design-route validation, and Console typecheck. | Run the protected five-service deployment and OI-12 workflow, then retain the exact deployed graph and receipt in Issue #262. |
| 2026-09-09 | implemented | Bound the previously unavailable production runtime-call source. Operator now emits a content-free caller witness after authenticated broker acceptance, while Core emits the matching target witness when broker delivery reaches its consumer boundary, before semantic rejection. The inventory Job reads one 60-second guard interval beyond the freshness window, re-reads each platform revision and replica beneath the claimed exact ARM ID with four-way bounded concurrency and one 30-second deadline, rejects every expired unpaired or invalid candidate, joins equal observation and endpoint tuples, captures freshness after verification, reauthenticates the pair under distinct ingestion and query lineages, reduces repeated joined calls to the newest exact endpoint pair, and enters the existing active-generation single-writer path. Protected deployment requires the platform binding whenever Operator exists and admits its first rollout only through the independent `runtime_call_evidence_transition` plan mode; headless and local profiles remain explicitly unavailable. | `current change`; `semantic_turn_runtime.py`, `semantic_turn_consumer.py`, `delivery/azure/runtime_call_telemetry.py`, `inventory_sync_cli.py`, protected service binding, Terraform inventory Job wiring, plan guard and bundle sealing, focused source, producer, consumer, deployment, and Terraform checks. The exact KQL executed successfully against the configured workspace without returning a fabricated row. | Commit and push the focused change, require green CI and supply-chain runs, apply the zero-destroy protected platform, Operator, and Core plans, generate one authenticated semantic request, run inventory reconciliation, and retain authenticated `8010` and `5273` available-source evidence under issue #260. |
| 2026-09-08 | implemented | Updated the OI-01 checker after the roadmap split so normative ownership remains in the design and implementation status remains in the delivery ledger. | `current change`; focused audit checker tests passed 3 cases. | Keep the checker and split ledger paths synchronized when the roadmap structure changes. |
| 2026-09-08 | implemented | Preserved verified positive operational state transitions when the Resource scope is incomplete. The result carries `resource_scope_incomplete` and cannot prove that no other transition exists. | `current change`; `uv run pytest -q --no-cov services/core-control-plane/tests/core/ontology_platform/test_state_transitions.py services/core-control-plane/tests/core/ontology_platform/test_state_transition_query_function.py` passed 6 tests. | Retain deployed transition coverage and complete-absence evidence as separate operational validation. |
| 2026-09-08 | implemented | Split active-scope checkpoint validation, serialization, and PostgreSQL queries into focused runtime and persistence modules without changing checkpoint or answer behavior. | `current change`; focused checks passed 729 tests with Ruff, strict mypy, and the enforced 800-line structural gate. | Keep both focused modules below the structural ceiling as checkpoint behavior evolves. |
| 2026-09-08 | implemented | Bound the current-graph checkpoint to exact active snapshot scopes so retained observations from inactive scopes no longer pin the active projection. Read-only conversations now lead with verified partial results and append limitation and retry guidance instead of replacing a safe subset with a generic refusal. | `current change`; focused inventory persistence, source coverage, projection wiring, semantic runtime, and bilingual answer checks passed 729 tests; Ruff and strict mypy passed. | Retain integration and deployed evidence separately; active post-snapshot observations continue to lower completeness until reconciliation catches up. |
| 2026-09-07 | validated | Completed OI-16 protected operational certification after repairing durable OI-15 receipt retention, additive deployer-role migration, certification scenario persistence, exact execution-log selection, and bounded Activity Log propagation. The independently approved campaign passed all 13 scenarios and persisted the append-only certification receipt. | Source `80b5892aa176e4a71eb2b3448982825b526d175b`; required CI `34058713875`; supply-chain `34058973580`; zero-destroy plan `34059171071`; bot request `34059338073`; approved campaign `34059357427`; receipt `sha256:6c9e7b5bc731776f065e25672d116c3f278ab02b2c09636191e6566a50552f0e`; private artifact `sha256:aaefe0ec525c22ecf596e5e9169904f5a68fe8341cbfa8a85a165508259b2409`. | Keep production retention unchanged and require a new exact-revision campaign for any later certification claim. |
| 2026-09-06 | implemented | Made storage-pressure projection lag honor a complete active provider snapshot as a superseding coverage boundary. The pressure query excludes only older records in the exact active scopes at or before the snapshot start when the snapshot declares `full_provider_scope`, modern projection completeness, and the same generation as the current manifest. Current-generation rows remain bounded by the manifest journal watermark, while later, outside-scope, null-scope, and non-certification records remain pending. | `current change`; 37 focused pressure-read and campaign-fixture tests passed with Ruff and strict mypy. An exact-image provider refresh produced a complete schema `1.3.0` active generation with matching manifest and complete relationship coverage. The proposed query executed read-only against that generation and measured projection lag 5, below the policy limit of 1000. | Publish and attest the corrected exact image, import it, and require a new independently approved 13/13 campaign before certification persists. |
| 2026-09-06 | implemented | Allowed repeat exact-release replay after the one-time schema `1.1.0` bootstrap without treating unchanged legacy snapshot metadata as current metadata. The bypass applies only when a complete schema `1.3.0` manifest for the same generation has a valid content digest, complete relationship status, canonical object and link content, and no dropped reasons. Mixed metadata or an incomplete manifest remains blocked, and the projector still compares reconstructed journal content with the content-addressed manifest before committing. | `current change`; 54 focused journal, replay CLI, and ontology projector tests passed. Independently approved campaign `34034548028` verified exact CI, durable OI-15 receipt, runtime image, and attestation, then stopped before scenarios because the current manifest replay still required legacy snapshot `projection_complete`. | Publish and attest the repeat-replay correction, import the exact image, and run a new independently approved campaign. |
| 2026-09-06 | implemented | Split PostgreSQL inventory journal record conversion and ontology graph-query reads into focused persistence modules without changing their public stores or write boundaries. The journal store is 767 lines, the ontology store is 794 lines, and both extracted helpers remain below the structural fail threshold. The obsolete ontology LOC debt baseline was removed rather than widened. | `current change`; 76 focused inventory journal, replay, ontology persistence, and service-suite tests passed with 6 environment-dependent skips. Ruff, formatter, strict mypy, JSON parsing, and the enforced LOC gate passed. | Retain the structural split in exact CI and continue OI-16 certification from the corrected projection-backlog revision. |
| 2026-09-06 | implemented | Corrected operational projection lag to count actual pending journal records rather than the raw numeric watermark gap. Records already covered by the current ontology generation are excluded only through that manifest's journal high-watermark, later records from the same generation remain pending, and isolated OI-16 synthetic scopes do not reduce active-graph completeness. Repeat release migration now replays the normalized journal directly once the manifest is current, instead of trying to bootstrap the unchanged legacy snapshot metadata again. | `current change`; 89 focused snapshot, replay, ontology, pressure-read, and campaign-fixture tests passed with Ruff and strict mypy. Independently approved campaign `34027297848` passed 12 scenarios plus exact projection migration and database restart, but `bounded_storage` failed because the raw watermark gap was 2200. Read-only measurements recorded database size below the hard limit, purge backlog 1, and an exact manifest-bounded pending count of 652, below the policy limit of 1000. | Publish and attest the corrected exact image, import it without applying the destructive plan, and require a new independently approved 13/13 campaign before persisting certification. |
| 2026-09-06 | implemented | Made the schema `1.1.0` snapshot-to-manifest identity gate independent of PostgreSQL collation order. Snapshot rows must still be unique, and sorting them with the same Python canonical order used by the historical manifest must exactly reproduce every manifest object id. A duplicate or any missing or additional identity remains blocked. | `current change`; 51 focused active-snapshot, replay CLI, and ontology projector tests passed with Ruff, formatter, and strict mypy. Independently approved campaign `34023322829` passed exact CI, durable OI-15 receipt, image, and attestation verification, then stopped before scenario collection with `legacy inventory snapshot object identities changed`. A sanitized read-only execution confirmed 664 unique rows and 664 unique manifest identities with zero set differences and exact Python-canonical equality, while only database tuple order differed. | Publish and attest the canonical-order fix, import the exact image without applying any destructive plan, and run a new independently approved campaign. |
| 2026-09-06 | implemented | Made OI-15 deployment provenance independent of the one-day GitHub artifact transport. Certification now accepts the exact plan-named unexpired artifact or, only when none remains, reads the exact append-only receipt from private deployment-plan storage after Managed Identity login. Both paths require the successful apply workflow revision and path, plan id, apply run id, and content digest to agree. Multiple artifacts, a missing private receipt, or any mismatch stops before image verification or scenario execution. | `current change`; 55 focused workflow-contract tests and actionlint passed. Independently approved campaign `34022506641` stopped before scenario collection when the GitHub artifact had expired. A read-only VNet runner check then verified the durable `plan-33951584532-1` receipt against apply run `33951784309` and digest `sha256:3fe1b7d77ed4511c9e88283bae9798a8e74e2c79fc502e234fe36477d736cade`. No certification receipt or artifact was persisted. | Publish the corrected workflow, pass exact required CI, publish or import its exact runtime image, and run a newly bot-requested and independently approved 13-scenario campaign. |
| 2026-09-06 | implemented | Added an explicit identity-only migration for the deployed schema `1.1.0` inventory manifest. The path accepts only one complete active generation with canonical unique identities, complete provider-identity coverage, no recorded drops, exact snapshot-to-manifest object equality, and every snapshot relationship present in the legacy manifest. It derives typed relationship verification from the immutable snapshot row and content-addressed legacy manifest, then requires the new projection to reproduce every legacy object and relationship identity before replacing the manifest. Schema `1.2.0` still cannot cross ontology releases, and malformed, mixed, incomplete, or conflicting evidence remains blocked. | `current change`; 50 focused active-snapshot, replay CLI, and ontology projector tests passed with Ruff, formatter, and strict mypy. Protected campaign `34014240389` verified the exact image and then stopped before scenario collection because the deployed active snapshot lacked modern projection-completeness metadata; it persisted no certification receipt or artifact. | Publish and attest the corrected exact image, run a new independently approved campaign, and retain `operationally_validated=false` unless all 13 scenarios pass. |
| 2026-09-06 | implemented | Hardened the provider-free ontology release migration for an active generation that predates its normalized journal rows. The migration now rehydrates only the complete active snapshot under the promotion lock, passes it through the existing safe-to-retry dual-write path, advances a monotonic journal fence, and then verifies unchanged observed content under the new ontology release. Missing relationship metadata, incomplete coverage, a changed generation, a regressed fence, or changed content still stops the migration before certification scenarios run. | `current change`; 42 focused snapshot bootstrap, journal replay, replay CLI, and ontology projector tests passed with Ruff and strict mypy. Protected campaign `34011288621` verified the exact image and then failed closed before scenario collection with `active inventory snapshot has no replayable journal records`; it persisted no certification receipt or artifact. | Publish and attest the hardened exact image, run a new independently approved campaign, and retain `operationally_validated=false` unless all 13 scenarios pass. |
| 2026-09-06 | implemented | Added a protected, provider-free inventory ontology release migration before OI-16 evidence collection. It rebuilds the active promoted observation from immutable journal records, uses the prior current-schema manifest's generation, content, freshness, and watermark fences, reconstructs legacy reviewed relationship drops, and refuses incomplete or changed content before any durable write. The migration records both prior and resulting releases and runs within the existing human-approved dev campaign boundary. | `current change`; focused inventory synchronization, journal replay, projector, replay CLI, workflow, Ruff, strict mypy, actionlint, and package-build checks. | Publish and attest the exact image, execute the protected migration, and require a new isolated 13/13 campaign before changing OI-16 validation state. |
| 2026-09-06 | validated | Closed the recorded operational-state path from Azure inventory through ontology projection, Operator read models, and Console presentation. Three resource-specific provider fields now retain exact values, while every canonical ResourceType has a reviewed missing-state outcome. | `current change`; 268 focused backend tests, 85 focused Console tests, typecheck, production build, one bounded live ARG collection, local PostgreSQL inspection, and authenticated standard-port browser checks. | No remaining work in this bounded recorded-state projection. |
| 2026-09-06 | in-progress | Completed the final bounded all-phase OI-16 attempt without overstating success. Twelve scenarios passed: bounded storage, warm replay, archive restore, safe partition purge, schema replay, database recovery, hold enforcement, duplicate delivery, delete/recreate, provider failure, database restart, and archive outage. Late-observation closure remained unavailable because the exact inventory projection refresh exceeded its deadline and the persisted ontology projection release remained conflicting. | Exact source `344d7cbf5`; required CI `33999441973`; runtime image attestation `33999441974`; bot request `33999842911`; campaign `33999858271`; Core plan `33983501478` (`0 add / 1 change / 0 destroy`); Core apply `33983775852`; OI-15 receipt digest `sha256:3fe1b7d77ed4511c9e88283bae9798a8e74e2c79fc502e234fe36477d736cade`. The summary recorded `operationally_validated=false`, `persisted=false`, and a null receipt and artifact digest. | Repair or complete an exact-source inventory ontology projection within its bound, then rerun a new isolated campaign. Do not close OI-16 or issue #262 until all 13 scenarios pass and the append-only receipt exists. |
| 2026-09-06 | implemented | Made PostgreSQL purge-receipt lookup prefer the highest attempt and a terminal status over `pending` when audit rows share one operation timestamp. The first complete all-phase campaign retained 11 passed scenarios, one unavailable late-correction closure, and one failed purge audit; exact inventory projection refresh exceeded its bounded execution deadline, so the global ontology release remained conflicting and no certification receipt or final artifact was persisted. | `current change`; focused archive-store and campaign checks. Campaign run `33999035116` completed before-restart collection, independently verified the Azure PostgreSQL restart, and emitted sanitized per-scenario statuses: all scenarios except `late_observation` and `safe_partition_purge` passed. | Publish and import the terminal-ordering fix, run one final isolated campaign, and keep OI-16 operationally unvalidated if the projection or any scenario remains unavailable. |
| 2026-09-06 | implemented | Changed split-phase orchestration so pre-restart scenario failures remain evidence instead of terminating the workflow before database-restart and post-restart scenarios run. Internal exceptions still fail the Job immediately; finalization remains the only all-scenario gate and now emits sanitized per-scenario status and reason-code maps for diagnosis without persisting a certification receipt. | `current change`; focused campaign phase-exit, finalization-summary, workflow, Ruff, and strict mypy checks. | Publish and import the corrected exact image, refresh the exact ontology projection, and run a new isolated campaign through every phase. |
| 2026-09-06 | implemented | Added an installed inventory synchronization wrapper that translates `once` or `loop` positional Container Apps modes into the existing CLI contract. A live inventory start without positional arguments completed on the previously deployed image, proving that the image override had not applied; this wrapper provides an exact-image projection refresh path without option-shaped arguments or new authority. | `current change`; focused inventory CLI, package entrypoint, Ruff, strict mypy, and wheel build checks. | Publish and import the wrapper-bearing image, execute one exact-image inventory refresh, then rerun certification on a new isolated scope. |
| 2026-09-06 | implemented | Anchored each restart-phase envelope to a real content-addressed archive manifest before inserting its artifact index row, and added exact storage-reference lookup for recovery. A read-after-write comparison produces the matching verification receipt, so campaign bookkeeping does not degrade instance-wide archive coverage. The PostgreSQL manifest and artifact digest remain independent of the private Blob metadata, while the envelope binds campaign, phase, purpose, and scope digest. The seventh protected attempt completed fixture replay and pre-restart scenario collection, persisted the private phase Blob, and then failed closed when the former synthetic manifest key violated the artifact foreign key. It did not restart the database or persist certification. | `current change`; focused phase store, archive manifest and verification, artifact lookup, campaign, finalization, Ruff, and strict mypy checks. Protected campaign run `33991048058` retained the failed phase-index evidence. | Publish and import the corrected exact image, then run a new exact-revision isolated campaign scope. |
| 2026-09-06 | implemented | Made lifecycle binding replay preserve an existing content-addressed observation binding before recalculating late-arrival state. The binder returns replayed observation identities so the journal also avoids recreating a cleared pending-tombstone row. This prevents a previously bound base observation from moving to a correction partition merely because later observations now exist and keeps confirmed deletion state monotonic. The isolated sixth attempt completed real fixture archive writes, then the second no-growth replay exposed and safely stopped on the retained-binding conflict; no restart, purge, or certification receipt occurred. | `current change`; focused PostgreSQL lifecycle, journal, fixture, and campaign checks with strict mypy and Ruff. Protected campaign run `33989424436` retained the failed replay evidence. | Publish and deploy the idempotent binder image, import it under the exact source tag, and run a new exact-revision isolated campaign scope. |
| 2026-09-06 | implemented | Corrected the operational-history Blob metadata name from the invalid hyphenated form to the Azure-compatible `fdai_sha256` identifier and strengthened the managed-identity round-trip test to assert the exact write and read header. The fourth protected attempt reached real fixture preparation and then failed closed on the first archive write with HTTP 400; it did not restart the database, purge data, or persist certification. | `current change`; focused Azure archive adapter and campaign checks. Protected campaign run `33988596207` and its failed execution log identified the metadata rejection. | Publish and deploy the corrected adapter image, import it under the exact source tag, and retry the stable campaign request. |
| 2026-09-06 | implemented | Added a dedicated installed campaign entrypoint with a positional Container Apps contract. Protected executions no longer depend on Azure CLI forwarding option-shaped Python arguments; the entrypoint translates bounded positional values into the existing typed CLI. A live no-data argv probe and the third campaign attempt both showed that string and JSON `--args` overrides remained one argv value, and the campaign failed before fixture preparation, database restart, purge, or receipt persistence. | `current change`; focused entrypoint, campaign CLI, workflow contract, Ruff, strict mypy, and actionlint checks. Protected campaign run `33987284431` stopped in the before-restart execution with no scenario evidence. | Publish, deploy, and attest the entrypoint-bearing exact image, then rerun the stable campaign request. |
| 2026-09-06 | implemented | Removed the Docker-dependent ACR login from the OI-16 image preflight. The workflow uses the already authenticated stable Azure identity for read-only manifest metadata and independently authenticates GHCR through a bounded private token file; no Docker daemon is required. The second protected attempt stopped before image selection or scenario execution, so it created no operational evidence or receipt. | `current change`; workflow contract and actionlint. Protected campaign run `33986535214` failed closed with `DOCKER_COMMAND_ERROR` in the exact-image step. | Publish and attest the corrected exact revision, import its exact image, and retry the unchanged campaign request. |
| 2026-09-06 | implemented | Added the repository-pinned GitHub CLI installation to the protected OI-16 runner before any required-check, artifact, or attestation read. The first protected attempt failed closed before Azure login or scenario execution because the ephemeral candidate runner did not provide `gh`; no operational evidence or receipt was created. | `current change`; workflow contract and actionlint. Protected campaign run `33985808647` stopped in `Verify required CI for exact revision` with exit `127`. | Publish and attest the corrected exact revision, import its exact image, and rerun the same stable campaign request through bot dispatch and human Environment approval. |
| 2026-09-06 | implemented | Decoupled OI-16 execution from the lifecycle Job's previously deployed image. The campaign resolves both the exact `fdai-core-control-plane` GHCR digest and the matching source-bound ACR tag, requires digest equality plus GitHub attestation, and passes that digest-pinned ACR image only to the bounded campaign executions. This avoids promoting the rejected archive-role replacement from plan `33978778160`; schema migration remains owned by a separate zero-destroy Core service plan/apply. | `current change`; campaign workflow contract and actionlint. The rejected root plan recorded `1 add / 1 change / 1 destroy`, while the independent handover service plan demonstrated the zero-destroy service deployment boundary. | Obtain successful exact-revision CI and image publication, import the exact image without applying the rejected root plan, complete a zero-destroy Core service apply for the migration, then run the protected campaign. |
| 2026-09-06 | implemented | Bound the OI-15 archive deployer-role transition to the repository-configured deploy UAMI after the protected plan exposed the legacy runner principal. The plan guard requires an unchanged storage scope and `Storage Blob Data Owner` role with create-before-destroy ordering; every other replacement remains blocked. | `current change`; failed protected plan `33978555300`; focused destructive-guard, plan-scope, and operational-history Terraform checks passed. | Publish the change, require exact-revision CI, and retain a zero-unexpected-destroy protected plan before any apply or OI-16 certification. |
| 2026-09-05 | implemented | Removed two API-key-like source literals that made the exact OI-16 revision fail the required gitleaks job. The phase digest domain value and runtime behavior are unchanged; only its internal identifier and one synthetic schema-replay fixture value changed. | `current change`; focused schema-replay and campaign CLI checks passed 75 cases, with Ruff passing. Required CI run `33974940666` identified the two `generic-api-key` false positives. | Push the corrected exact revision and require a new successful required CI run before any protected deployment or certification. |
| 2026-09-05 | implemented | Preserved reviewed nested operational values through ARG hydration and the inventory-owned ontology projection, and required Dashboard state reads to match the committed ontology generation. | `current change`; focused Core projection, ARG, Operator state-page, and Dashboard checks. | Retain authenticated operator-screen evidence before claiming live deployment readiness. |
| 2026-09-05 | implemented | Added the fail-closed OI-16 protected certification campaign. The dev-only runner creates and evaluates bounded synthetic evidence, splits execution around an independently observed Azure PostgreSQL restart, stores phase evidence in private principal-scoped Blob storage, and accepts a safe synthetic purge only with dry-run, blast-radius, target-lock, stable-idempotency, two-phase audit, rollback, and independent effect checks. The protected workflow requires a bot-owned request plus a distinct Environment approval and binds the exact source revision, successful required CI run, matching attested runtime image, OI-15 deployment revision, apply run, and apply-receipt artifact digest. Receipt persistence and the private sanitized final artifact are reachable only when every required scenario passes. | `current change`; certification model, CLI, campaign runner and probes, phase store, protected request and campaign workflows, audit and design routing, and focused model, CLI, campaign, workflow, identity, and CI-contract checks. | Commit and push the exact revision, obtain required CI and runtime-image attestation, review a zero-destroy protected plan, apply through the bot-owned request and human Environment approval, then run all 13 live scenarios. Until that campaign passes, keep `operationally_validated=false`, do not append a certification receipt, and keep OI-16 and issue #262 open. |
| 2026-09-05 | validated | Deployed and exercised the OI-15 runtime composition. The independently targeted Container Apps Job uses the non-executor inventory identity, a versionless Key Vault DSN reference, private versioned Blob storage, and a fixed shadow command. Specialized exact apply is bot-owned so the maintainer remains the distinct Environment approver. | Revisions `c209b0896`, `3bef1a800`, and `69bbdf6c1`; required CI `33951293573`; runtime supply chain `33948715715`; plan `33951584532` (`8 add / 0 change / 0 destroy`); bot request `33951765704`; apply `33951784309`; apply-receipt artifact digest `sha256:3fe1b7d77ed4511c9e88283bae9798a8e74e2c79fc502e234fe36477d736cade`; deployed Job contract verified; shadow execution succeeded from `2026-09-05T07:16:06Z` through `2026-09-05T07:16:43Z`. | OI-16 remains open until every required operational scenario has independent evidence. This shadow execution does not prove archive restore, safe purge, schema replay, database recovery/restart, or outage behavior. |
| 2026-09-05 | implemented | Completed the OI-15 runtime composition. A dedicated Container Apps Job uses the non-executor inventory identity, Core database secret, private versioned Blob storage, bounded partition selection, append-only lifecycle events, checkpoints, archive write/read verification, restore sampling, holds, pressure reporting, and the database purge gate. Scheduled runs remain shadow-only; enforce requires an external receipt and cannot purge, while certify requires an external receipt before the database gate can run. | `current change`; lifecycle runner and PostgreSQL repository, dedicated storage/private endpoint/Job Terraform, protected bounded-plan input, focused runner, workflow, CLI, and Terraform tests. | Apply the exact protected plan, retain a successful shadow Job receipt, then run OI-16 certification. |
| 2026-09-05 | validated | Projected latest committed AKS AgentPool and VMSS capacity into the no-authority instance response and displayed the values with type-specific labels in the graph and Inspector. Unsupported or malformed capacity remains absent, and the Console decoder rejects capacity on an unsupported Resource type. | `current change`; focused Operator checks passed 10 cases, focused Console checks passed 102 cases, Ruff, strict mypy, Console typecheck, and production build passed. The authenticated graph and Inspector matched the active snapshot at `nodepool1=2` and VMSS capacity `2`. | Retain a fresh scale-operation latency receipt after a future operator change. Exact Kubernetes Node count and readiness remain unavailable until runtime inventory is configured. |
| 2026-09-05 | implemented | Hardened the first operator-driven AKS start observation. SSE replay now admits `Last-Event-ID` through CORS, and Resource Graph change hydration applies allowlisted operational-state changes from the authoritative change record when the current `Resources` row still carries the previous state. | `current change`; focused ARG checks passed 39 cases and Operator CORS checks passed 2 cases. The observed transition added the VM and NIC graph branch after about five minutes and changed the selected cluster from `Stopped` to `Running` after about ten minutes; the SSE remained connected and re-read the graph automatically. | Repeat one stop or start transition to measure the corrected change-record state merge without the two defects present in this first run. |
| 2026-09-05 | implemented | Completed synchronized validation and local runtime probing for private-safe change acceleration. The live ARG probe exposed and fixed nested `properties.*` field projection and the unsupported direct string relational comparison in the resume predicate. The durable SSE returned `200 text/event-stream` and caused an immediate authoritative selected-instance re-read. | `current change`; synchronized backend checks passed 160 cases, Console checks passed 112 cases, Ruff, strict mypy, Console typecheck and production build passed. Live local ARG probes returned `published=3` from the initial window and `published=0` after the durable cursor, and the continuing inventory loop reported no change-feed unavailable state. | Have the operator change one Azure Resource state and retain the measured provider-change-to-screen interval. |
| 2026-09-05 | in-progress | Added a private-safe Resource Graph change accelerator, durable no-payload inventory invalidation SSE, and Console immediate revalidation with a visible 15-second fallback countdown. | `current change`; source adapter checks passed 38 cases before the requested pause, Operator SSE checks passed 140 cases, and Console SSE/countdown checks passed 112 cases. | Synchronize the shared main checkout, run combined focused and clean-build validation, critique the integrated diff, and retain one local state-transition timing receipt. |
| 2026-09-05 | implemented | Clarified the negative sparse-property contract after tombstones became explicitly typed. A delete with `properties_complete=false` is a tombstone unless `observation_kind=partial` is explicitly declared; the negative test now exercises the prohibited partial-delete combination before database access. | `current change`; `test_inventory_live_evidence.py`; 5 focused partial and tombstone semantics checks passed with Ruff and formatting. | Rerun required CI for the exact corrected revision before the protected deployment plan. |
| 2026-09-05 | implemented | Restored migration-safe shadow dual-write adoption for pre-journal active snapshots. The projector resolves a missing legacy scope only from the single active coverage scope, materializes deterministic baseline incarnations before sparse object and relationship binding, keeps tombstone-first ordering monotonic, and isolates root rollback tests from the service-owned migration database. | `current change`; PostgreSQL delta and lifecycle adapters, CI database ordering, migration inventory and workflow contracts; fresh pgvector checks passed 13 root tests with 25 service-only skips followed by 31 service-owned tests, plus 123 workflow and migration contracts, 187 focused OI and deployment CLI checks, Ruff, formatting, and strict mypy. | The protected plan and apply plus the dedicated OI-15 runtime Job and OI-16 operational certification receipt remain open. |
| 2026-09-05 | in-progress | Implemented OI-14 and the local code surfaces for OI-15 and OI-16. Observation ingestion now binds Resources and relationship endpoints to exact incarnations and time-and-scope partitions. Late observations open correction partitions, lower source completeness, and close only after a content-addressed ontology replay receipt. Case, investigation, approval, execution, rollback, legal-hold, and replay-lease pins block purge. Deployment policy loading, storage-pressure degradation, verified principal-scoped Blob archive access, database-gated source purge, N/N-1 schema replay, database recovery comparison, a fixed shadow schedule, and pinned-revision certification are implemented. | `current change`; lifecycle and certification types, Core migration `20260906_core_operational_history_lifecycle.py`, PostgreSQL and Azure adapters, inventory journal integration, source-completeness reduction, certification CLI, and 322 passing focused checks; Ruff, formatting, and strict mypy passed. | Compose the fixed lifecycle schedule as a dedicated runtime Job, then commit and push a green revision and run the protected operational certification. Until that receipt exists, OI-15 and OI-16 remain open and no deployed bounded-growth or recovery claim is made. |
| 2026-09-05 | implemented | Completed OI-13 and its prerequisite correctness fixes. Event Grid changes are sparse change hints, operation status remains journal metadata rather than resource state, property masks preserve tags, SKU, and unobserved values, and unconfirmed tombstones cannot prove absence. The Core-owned journal dual-writes full snapshot and delta object or relationship observations while the existing overlay remains the read path. Content-bound replay is deterministic across duplicate, reordered, stale, and restart delivery. Journal lag and pending tombstones lower ontology source completeness. | `current change`; normalized observation types, PostgreSQL journal and migration, delta and snapshot dual-write, ontology watermark receipts, source-coverage reduction, audit registry, and focused tests. The final focused OI-13 suite passed 219 cases; Ruff and strict mypy passed; continuous-graph audit and design-route checks passed. | OI-14 remains open for resource incarnation identity, relationship correction coverage, correction partitions, and active-case or legal-hold pins. OI-15 and OI-16 remain open. |
| 2026-09-05 | in-progress | Expanded the bounded-history design after a source-to-store review and corrected archive lifecycle from component completion to an open operational binding. The revised contract now covers normalized observations, property masks, resource incarnations, relationship history, partition states, verified checkpoints, late corrections, active-case pins, storage pressure, schema evolution, and disaster recovery. | `current change`; this design owner and `config/continuous-operational-instance-graph-audit.json`; continuous-graph audit, roadmap tracking, translation, punctuation, and document-size checks. | Implement and validate OI-13 through OI-16 below. No runtime behavior changed in this transition. |
| 2026-08-31 | implemented | Completed issue #295's deterministic analyzer parity boundary. Analyzer event IDs and window keys are stable, a token-owned PostgreSQL claim suppresses only findings with a completed broker receipt, active claims fail closed, stale claims are reclaimable, and failed sends release their claim. A bounded analyzer-path scenario distinguishes same-UID restart from distinct-UID replacement and emits one receipt joining detection latency, evidence completeness, publication, and recovery closure. | `current change`; analyzer runner, CLI, PostgreSQL publication ledger, Pod replacement analyzer-path scenario, and focused checks (`85 passed`); Ruff passed. | No deployed runtime or Azure receipt was created. Protected deployed certification remains external to issue #295. |
| 2026-08-31 | implemented | Completed issue #291's reporting surface. Analyzer receipts now carry the target, kind, occurrence, and recovery status they were missing, a Core reducer turns retained receipts into a bounded Pod lifecycle projection, and the authenticated Operator API `/detection-readiness` family plus its existing Console route report current state, failure history, recovery, and evidence gaps as four separate answers on the same endpoint. Detection states no cause and holds no execution authority. Missing, stale, incomplete, and conflicting evidence fail closed: a projection older than its freshness budget withdraws its current state and recovery and reports a stale gap while keeping its history, and one defective row makes the section unavailable with a named reason instead of silently shortening the retained history. | `current change`; 122 focused Python tests across the reducer, recorder, Operator projection, runtime reader, and analyzer tick passed, including an 8-test cross-service end-to-end proof pinned at one revision that drives the real analyzer, ledger, recorder, and Operator reader for same-UID restart, distinct-UID replacement, missed and expired evidence, duplicate suppression, uncertain-then-reconciled delivery, and independently verified recovery, and pins the Console contract as a fixture. Console checks passed at 2,204 unit tests, strict typecheck, and production build; the configured strict mypy gate added no error over its 7-error baseline. Browser evidence at 1440x900, 993x641, and 390x844 showed the four separated answers, zero horizontal overflow, a 44 px keyboard-reachable disclosure, and no cause or authority claim. | Detection still reads Pod lifecycle evidence only through the `FDAI_POD_LIFECYCLE_EVIDENCE_JSON` composition seam, so an authenticated live delete/recreate scenario and the typed resumable ingestion in issue #292 remain open. |
| 2026-08-31 | implemented | Corrected both issue #295 review findings. An ambiguous broker failure no longer releases the publication claim: the runner records send intent durably before publishing, releases only on a bus attestation that the record was provably not sent, and otherwise holds the claim uncertain until reconciliation, so an acknowledged publish whose receipt was not recorded is never republished after lease expiry. The typed Kubernetes Pod lifecycle analyzer and its independent recovery evidence are bound in production analyzer composition, and receipts derive evidence completeness and recovery closure from the canonical replacement and recovery reducers instead of free-form metadata. | `current change`; `shared/providers/event_bus.py` publish contract, `delivery/azure/event_bus.py`, `delivery/analyzer_tick.py`, `delivery/persistence/postgres_analyzer_publication.py`, `core/investigation/kubernetes_pod.py`, `delivery/pod_evidence_binding.py`, CLI-driven Pod scenario, and focused checks. | No reconciler implementation ships, so an uncertain claim stays awaiting reconciliation until an operator binds one. No live Kubernetes Pod lifecycle collector exists and no deployed runtime receipt was created. |
| 2026-08-31 | implemented | Completed issue #295's deterministic analyzer parity boundary. Analyzer event IDs and window keys are stable, a token-owned PostgreSQL claim suppresses only findings with a completed broker receipt, active claims fail closed, claim-store read or write failures fail that finding closed without publishing, stale claims are reclaimable, and failed sends release their claim. A bounded scenario runs the local analyzer loop over the analyzer path, distinguishes same-UID restart from distinct-UID replacement, and emits one receipt joining detection latency, evidence completeness, publication, and recovery closure. | `current change`; analyzer runner, CLI local loop, PostgreSQL publication ledger, Pod replacement analyzer-path scenario, and focused checks (`116 passed`); Ruff passed. | No deployed runtime or Azure receipt was created. Protected deployed certification remains external to issue #295. |
| 2026-08-27 | implemented | Preserved reciprocal `runtime_calls` observations as two directional edges. Mutual service calls no longer suppress the complete topology baseline as an orientation conflict. | `current change`; focused inventory projection and topology-history checks (`29 passed`); Ruff and strict mypy. | Retain authenticated runtime-call evidence separately; no source observation was fabricated. |
| 2026-08-27 | implemented | Aligned inventory manifest reload and PostgreSQL topology replay with the projection publisher's canonical link order. Multi-LinkType generations now replay idempotently, and a persisted 1.2.0 manifest can rebuild to 1.3.0 without rejecting its own link keys. | `current change`; focused inventory ontology and PostgreSQL topology-history checks (`23 passed`); Ruff and strict mypy. | Retain deployed restart and replay evidence separately; no external source was queried. |
| 2026-08-29 | implemented | Hardened immutable Pod replacement and recovery evidence. The reducer now requires independently verified Pod-to-owner and owner-to-root-controller links, then binds fresh observed Pod, termination, and Deployment state to matching cluster, namespace, workload revision, and root-controller UID. It requires positive integer replica and container state, bounds termination to the correlation window and source revision, and rejects observations that predate Pod creation or place creation after termination. Same-UID restart classification requires matching creation identity and an increasing restart count. Rollout classification requires a changed workload revision. Abnormal replacement additionally requires complete, time-ordered desired-replica history with no scale change across termination and replacement. Ambiguous candidate identities and references remain in the result for replay, and every missing recovery field becomes an explicit gap. | `current change`; focused Pod replacement reducer tests, including verified ownership links, scope, revision, source, lifecycle ordering, authority-lane, controller, replica history, restart, ambiguity, and correlation-window regressions. | Bind the reducer to retained lifecycle query composition and preserve an authenticated live replacement and recovery receipt under issue #291. |
| 2026-08-27 | validated | Reconciled the Kubernetes topology and lifecycle migration heads, rebuilt the isolated validation database from the canonical legacy schema, and applied the merged Core service head. Five authenticated seed/watch cycles reacquired the lease and advanced the opaque cursor from sequence 0 to 5 with no limitation; the durable reader then returned a complete 60-second zero-row result. | `current change`; one Core migration head; migration contracts (`60 passed`); PostgreSQL lease, duplicate, and reorder integration (`1 passed`); authenticated cluster `Running/Succeeded`; Event API HTTP 200; exact UID live read 17 rows | Deployed retention and a live Pod replacement/recovery join remain separate operational evidence under issues #292 and #291 respectively. |
| 2026-08-27 | in-progress | Added leased, typed, resumable Kubernetes lifecycle collection and indexed durable Event reads. The collector seeds coverage at the current time, watches with bookmarks, treats `resourceVersion` as opaque, commits observations and cursor progress atomically, and records authorization, expiry, source, and result-limit gaps. | `current change`; lifecycle and existing Event cohort (`76 passed`); service-migration contracts (`60 passed`); Ruff, formatter, and strict mypy passed; authenticated live Event API returned HTTP 200 and an exact UID read returned 17 rows | Resolve the validation schema fingerprint and competing untracked migration head, apply the service migration, persist two consecutive seed/watch cycles, and retain a complete lookback window before zero rows can prove historical absence. |
| 2026-08-27 | implemented | Preserved the newest bounded Event rows through answer projection and disclosed the displayed count, total count, and `display_truncated` state. The answer shows the latest eight Events in chronological order instead of silently keeping the oldest eight. | `current change`; focused bilingual Resource Event answer regression checks | Restore live Kubernetes Event API access and retain one authenticated answer with Event rows. Durable retention remains required before zero rows can prove historical absence. |
| 2026-08-27 | implemented | Made exact-target Resource Event reads fail closed when the secured identity predicate resolves to zero or multiple Resources. Broad complete scopes retain their existing empty-result behavior. | `current change`; focused Resource Event FunctionType regression checks | Restore live Kubernetes Event API access and retain one authenticated exact-target receipt. Durable retention remains required before zero rows can prove historical absence. |
| 2026-08-27 | in-progress | Added the no-I/O immutable Pod replacement reducer. Historical evidence is validated against the correlation window rather than current-state freshness, current Pod and Deployment evidence must be fresh, and the result echoes both UID records, ownership, timestamps, window, margin, and evidence references for deterministic replay. | `current change`; focused Pod replacement reducer tests passed 13 cases; Ruff, formatter, and strict mypy passed. | Persist normalized lifecycle observations through issue #292, then bind authenticated query composition and retain an exact replacement and recovery receipt before promoting this row. |
| 2026-08-27 | implemented | Preserved one source-grounded exact Resource in the Event scope and added a limitation-aware bilingual Event answer. Same-name Resources remain ambiguous until reviewed type evidence narrows them, and every answer exposes source completeness and no authority. | `current change`; focused Event vertical cohort (`287 passed`), Ruff, formatter, and strict mypy. Authenticated exact-target execution completed 2/2 nodes and 8/8 evidence checks after the stale Core was replaced, then rendered `source_unavailable` without claiming Event absence, cause, or recovery. | Restore successful Kubernetes Event API access under the runtime identity, retain one identity-aware exact-child receipt, and add durable retention before zero rows can prove historical absence. |
| 2026-08-27 | implemented | Added receipt-bound exact-child Kubernetes Event filtering without widening the existing reader seam. Core passes an immutable two-field identity snapshot only to identity-aware readers, the composite preserves legacy readers, and the Kubernetes adapter accepts a UID selector only after exact cluster and canonical Resource-id recomputation. | `current change`; Resource event FunctionType, composite and Kubernetes readers, legacy compatibility, immutable identity, selector propagation, and forged UID/cluster regressions (`27 passed`); Ruff, formatter, and strict mypy passed. | Retain authenticated exact-child Console evidence. Durable retention remains required before any zero rows prove historical absence. |
| 2026-08-26 | implemented | Added a bounded Kubernetes Event family to the existing Resource event-history FunctionType. Provider routing preserves Azure Resource Health independently, while Kubernetes events bind by immutable UID to an exact selected Resource or to the exact selected cluster when the involved object has already disappeared. The adapter retains only normalized kind, status, classification, occurrence time, and content-addressed evidence identity. | `current change`; Kubernetes and composite event readers, runtime binding, shared UID identity helper, focused adapter, router, FunctionType, semantic planning, composition, and runtime checks; exact typed duration alignment, mixed-scope retention, encoded-response rejection, and raw 256 KiB bounds are covered. Authenticated exact-cluster Console execution used the server-owned ObjectSet plus `query.resource_event_history` plan and completed 2/2 nodes and 8/8 evidence checks in 6.8 seconds with no execution authority. The observed zero is not historical absence because Kubernetes Event retention was not authoritative; the hardened adapter reports `source_retention_unverified`. | Add durable event retention before provider TTL expiry can be treated as historical coverage. Join retained event, change, replica, replacement-UID, impact, and new-cutoff evidence before claiming Pod cause or recovery. |
| 2026-08-25 | implemented | Extended the issued exact-Pod S1 assessment with a reviewed 30-minute restart-count delta and two explicit outgoing ownership hops to the exact ReplicaSet and Deployment. The five-node server plan authenticates all three secured graph receipts, verifies ownership-link evidence, and requires a positive bounded delta plus fully ready and available owner replicas before reporting recovery. Incomplete, ambiguous, stale, or conflicting evidence remains unresolved, and dependency-only inputs reject model-authored substitution. Neither current state nor the metric claims cause or execution authority. | `current change`; focused metric, verifier, reducer, receipt, planner, and production-composition cohort (`49 passed`); semantic routing regression (`370 passed`); Ruff, formatter, and strict mypy passed. | Retain a complete Kubernetes generation with an exact Pod, then add independent Kubernetes event/change, replacement-UID, impact, and new-cutoff evidence before claiming cause or post-replacement recovery. |
| 2026-08-25 | implemented | Preserved a zero-row source-incomplete Resource ObjectSet as a completed typed query result instead of raising a generic capability failure. The targetless S12 question now returns zero verified candidates, `source_incomplete`, completed verification, and no execution authority without treating incomplete coverage as absence. | `current change`; `query_source_handlers.py`; focused rollout and incomplete-source cohort (`19 passed`); authenticated standard-port Console turn completed 4 observed events and 5/5 evidence checks. | Retain a complete Kubernetes generation with one exact Deployment, then run the four-node rollout assessment and independent new-cutoff recovery verification. |
| 2026-08-25 | implemented | Connected rollout observations to the graph-first semantic path. Targetless rollout wording resolves to bounded Kubernetes Deployment candidates, while an exact target compiles two explicit ownership hops and an issued no-cause FunctionType. | `current change`; focused targetless, reducer, receipt, traversal, planner, and production-composition checks passed 17 cases; task-scoped Ruff and strict mypy passed. | Restart Core and retain authenticated candidate and exact-target receipts from a complete Kubernetes generation. |
| 2026-08-25 | implemented | Hardened Azure relationship convergence after a live local generation exposed eight AKS AgentPools with both reviewed AKS parents and evidence-free Resource Group parents. Exact-parent shadowing and a durable cardinality gate remove the duplicate parent path. Classified non-edges can now advance current verified Resources and links while preserving incomplete coverage; distributed projection locking, source-bound query receipts, stale evidence projection, and additive N-1 receipt decode close the generation and evidence gaps. | `current change`; focused ARM, provider contract, projection, runtime, PostgreSQL, ObjectSet, Operator, and Console checks; twelve adversarial rounds below. | Refresh and retain an authoritative local generation after focused validation. Deployed Azure certification remains separate under Issue #262. |
| 2026-08-25 | implemented | Added bounded Kubernetes Deployment and Pod rollout status to the complete inventory generation. The adapter retains only replica counts, the single Progressing condition, Pod readiness, restart counts, and waiting reasons, and rejects malformed status instead of retaining raw provider payloads. | `current change`; `kubernetes_api_inventory.py`; focused Kubernetes API inventory checks passed 7 cases. | Retain a complete live exact-release generation and bind the typed rollout assessment through the graph-first query path. |
| 2026-08-25 | implemented | Corrected complete-generation relationship verification so an observed endpoint with the wrong type is classified as `target_type_mismatch` instead of an unrelated duplicate conflict. The candidate remains absent from the active graph and retains its mapping-specific unavailable reason. | `current change`; focused relationship projection and verification checks passed 65 cases. | Retain deployed complete-generation evidence separately; this correction adds no relationship or authority. |
| 2026-08-22 | in-progress | Adopted the continuous operational instance graph contract. It replaces fixed six-hour freshness as the target with event-driven and adaptive bounded collection, and adds typed rollup, verified archive, restore, and purge requirements. | `current change`; paired design document and focused documentation gates. | Complete OI-01 through OI-12 with focused implementation and operational evidence. |
| 2026-08-22 | implemented | Completed OI-01 with a machine-readable 15-stage source-to-store audit and a deterministic checker for owner, binding, test, state, and missing-gap evidence. | `current change`; audit record, checker, and focused audit tests (`3 passed`). | Start OI-02 with validated source-policy declarations; leave adaptive control, rollup, archive, and live write-through to their owning later packages. |
| 2026-08-22 | implemented | Completed OI-02 through OI-05 with validated source policy, adaptive scheduling, event/delta/snapshot convergence, and principal-safe collection health. | `current change`; collection source, scheduler, convergence, health, reconciliation, sync, topology-history, and loopback PostgreSQL focused checks (`157 passed`). | Integrate the collection observation provenance with typed rollup input before closing preservation. |
| 2026-08-22 | implemented | Completed OI-06 with semantic-policy-driven fact families, explicit zero and missing intervals, conservative partial and conflict handling, replay deduplication, and mergeable statistics that retain count and sum for averages. | `current change`; semantic rollup modules and focused rollup checks (`6 passed`). | Complete immutable archive and purge safety in OI-07. |
| 2026-08-22 | implemented | Completed OI-07 with content-addressed manifests, verification, restore sampling, archive coverage receipts, retention and legal holds, append-only PostgreSQL evidence, and safe-to-retry purge behavior. | `current change`; archive and purge focused checks (`8 passed`) plus Core service migration checks (`51 passed`). | Run the collection-to-preservation integration boundary before OI-08. |
| 2026-08-22 | implemented | Passed the OI-02 through OI-07 integration boundary. The explicit inventory adapter preserves source, generation, ontology release, effective/event/recorded time, completeness, and conflicts. Event, delta, snapshot, duplicate, reorder, incomplete, conflict, tamper, restore, hold, purge failure, and restart cases remain deterministic and authority-free. | `current change`; cross-lane integration checks (`3 passed`), OI-01 audit checks (`3 passed`), and focused static gates. | OI-08 may start. OI-09 through OI-12 remain blocked by their own exit criteria. |
| 2026-08-22 | implemented | Completed OI-08 with a pure graph evidence refresh reducer that returns exactly `use_graph`, `refresh_then_query`, `use_live_evidence`, `query_archive`, or `hold`, with no action authority. | `current change`; focused refresh checks (`5 passed`), Ruff, and strict mypy. | Bind the reducer into ordinary semantic query composition after the canonical campaign blocker is resolved. |
| 2026-08-22 | implemented | Completed OI-09's safe write-through slice. Verified live reads become canonical partial overlay events, and PostgreSQL merges partial properties without deleting complete snapshot properties or relationships. | `current change`; live-evidence checks (`5 passed`), loopback PostgreSQL partial-overlay check (`1 passed`), Ruff, and strict mypy. | Bind the writer after refresh selection in ordinary semantic query composition. |
| 2026-08-24 | implemented | Connected exact-target current-state queries to graph-first freshness reduction, one bounded live read, content-bound canonical partial-overlay write-through, and a secured re-query. | `current change`; graph refresh, semantic composition, live evidence, and runtime focused checks. | Retain deployed provider-pressure and freshness measurements separately; broader or historical queries continue to hold. |
| 2026-08-24 | implemented | Closed a mixed-coverage freshness defect. A multi-Resource result can no longer use one fresh Resource to hide another returned Resource whose state-fact metadata is absent; the query refreshes once when allowed and otherwise holds as incomplete. | `current change`; `graph_query_refresh.py`; focused current, stale, conflict, refresh, and mixed-metadata checks (`5 passed`). | Retain deployed mixed-generation evidence separately; this read-only correction adds no observation, mutation, or execution authority. |
| 2026-08-22 | implemented | Completed OI-10 with a typed representative competency evaluator for exact instances, stored-direction paths, functions, refresh outcomes, archive status, and no-authority. It never inspects answer text. | `current change`; representative and substitution checks (`2 passed`), Ruff, and strict mypy. | Run the canonical bilingual campaign in OI-11. |
| 2026-08-22 | in-progress | Implemented the direct 70-case bilingual campaign loader and typed oracle, hardened free-form assurance concepts with stable machine tokens, and passed a three-turn typed no-authority readiness probe. The full campaign stopped at its first case because `action-incident-mitigation-draft.direct.en` returned `unsupported` instead of the allowed `action_draft` or `held`. | `current change`; OI-01 through OI-10 prerequisite gate (`241 passed`, zero skipped), campaign runner checks (`6 passed`), Console typecheck, assurance projection checks (`9 passed`), and authenticated standard-port readiness (`3/3`). | Correct action-draft frame classification without keyword routing, then rerun all 70 direct cases. OI-12 remains blocked until that gate passes. |
| 2026-08-24 | in-progress | Separated corpus-wide semantic assurance from OI-11. The prior direct 70-case attempt remains historical evidence, while the current 560-case corpus, generated question universe, change-focused cohorts, and future corpus growth are governed by Continuous Semantic Assurance. | `current change`; [Continuous Semantic Assurance](../../roadmap/interfaces/continuous-semantic-assurance.md), structured corpus counts, and existing focused competency and typed oracle contracts. | Complete OI-11 with focused bilingual positive and negative action-draft classification checks and typed no-authority receipts. Run full-corpus and scheduled assurance independently under its owning document. |
| 2026-08-24 | implemented | Completed OI-11 with typed English and Korean draft requests plus phrase-adjacent read requests. Structured `draft_only` and `advise_only` posture, not keyword routing, selects the terminal path, and every proposal, receipt, frame, plan, and outcome remains authority-free. | `current change`; `test_semantic_judgment.py::test_bilingual_action_posture_receipts_are_typed_and_authority_free` and `test_semantic_planning_tier_routing.py::test_bilingual_typed_action_posture_routes_without_keyword_rules` (`8 passed`). | Run the local OI-12 wording regression, then retain the separately governed deployed Azure certification measurements. |
| 2026-08-24 | in-progress | Passed the local OI-12 wording regression for all eight incident draft wording styles in English and Korean. The test consumes the generated corpus and a typed judgment, never a production phrase rule, and proves every case stops before plan execution without authority. | `current change`; `test_semantic_planning_tier_routing.py::test_incident_action_draft_all_bilingual_wording_styles_remain_authority_free` (`1 passed`, 16 corpus cases). | Deployed Azure certification remains open for freshness, API pressure, lag, storage growth, rollup coverage, archive restore, and provider failure measurements. |
| 2026-08-24 | in-progress | Audited the remaining authorization and runtime-call evidence paths without adding an unsupported edge. Azure relationship drops already preserve unmodeled or unobserved endpoints, but PostgreSQL roles have no separate typed observation contract. Trace continuity carries hop labels only, and the active ontology release has no reviewed runtime-call LinkType. | `current change`; `inventory.py`, `trace_continuity.py`, `delivery/azure/trace_continuity.py`, `instance_explorer.py`, and [Issue #260](https://github.com/dotnetpower/fdai/issues/260). | Define and revalidate the missing contracts before binding producers. Do not infer Resource identities from names, Resource Groups, environment variable names, credentials, or RBAC grants. |
| 2026-08-24 | in-progress | Ran a read-only deployment preflight without starting resources or dispatching a workflow. The selected development context matched, all five runtime apps were provisioned, one of five workloads exposed `POSTGRES_HOST`, and the private database and deploy runner were stopped. | [Issue #262](https://github.com/dotnetpower/fdai/issues/262) preflight evidence; no Azure or workflow mutation. | Push an exact tested SHA, pass required CI, review a zero-destroy plan, then apply and certify through the protected runner. |
| 2026-08-24 | implemented | Added and hardened the reviewed `runtime_calls` declaration and pure no-authority projector. A ten-lens review closed exact-release, temporal-order, digest-binding, principal-redaction, freshness-bound, and verification-semantics defects. One observation must supply both exact endpoint identities before a candidate edge can pass active-generation and scope checks. | `current change`; runtime-call projector (`16 passed`), combined projector and catalog checks (`24 passed`), prior LinkType/provenance checks (`15 passed`), prior catalog/release checks (`22 passed`), Ruff, and strict mypy. | Bind a typed telemetry producer and single writer, then retain authenticated Operator and Console evidence. |
| 2026-08-24 | implemented | Bound authenticated typed runtime-call observations to the existing inventory single writer, added an explicit unavailable scheduled binding, separated principal-safe PostgreSQL role evidence from Azure RBAC, classified unavailable authorization child scopes, and projected no-authority source evidence through Operator and Console. Eleven independent hardening rounds left no unresolved finding above Low. | `current change`; focused Core and Operator checks (`111 passed`), Console model checks (`17 passed`), Console typecheck, and zero editor diagnostics on the changed production paths. | Inject an authoritative telemetry source that supplies exact caller and target Resource IDs, then retain authenticated `8010` and `5273` evidence from a pushed green SHA. Keep deployment and Azure certification in Issue #262. |
| 2026-08-22 | validated | Replaced the Console's architecture renderer for Resource instances with a flat rectangular graph. The Operator projection now returns all bounded links among the selected one-hop Resources, distinguishes Resource and relationship truncation, and the Console renders reviewed Azure icons plus a focusable state and FDAI audit timeline without granting authority. | `current change`; focused Operator and Console checks, production build, twenty critique and hardening rounds, and authenticated standard-port active-generation evidence with three Resources and three relationships. | Bind authoritative Azure Activity Log and Resource Health projections separately. Resource types without an allowlisted exact icon retain the generic Azure fallback. |
| 2026-08-22 | validated | Ran an authenticated randomized 20-Resource browser campaign from 200 server-filtered `fdai` results. The campaign fixed sampled icon gaps with provenance-locked Azure assets, clustered dense audit markers, summarized dense relationship types without dropping paths, and re-centered the selected Resource after viewport changes. | `current change`; 20 exact Resource selections across 12 root types, with a maximum of 80 Resources and 79 relationships. The final rerun reported zero HTTP, count, selection, generic-icon, node, label, marker, page-overflow, workbench-overflow, and preview-overflow defects. | Expand the provenance-locked icon allowlist when a future observed Resource type lacks an exact reviewed asset. Azure Activity Log and Resource Health remain separate unavailable sources. |
| 2026-08-22 | validated | Compared one Container App against authoritative Azure configuration and the active ontology graph, then added the smallest common relationship producer. The reviewed ARG mapping now projects the exact managed-environment ARM reference as `Container App -> depends_on -> Container Apps Environment`; snapshot storage and the Operator preserve per-link configuration source, cutoff, completeness, and unavailable state. | `current change`; the anonymous comparison found one environment, three managed identities, one registry, one infrastructure subnet, and one Log Analytics workspace while the prior graph exposed only one Resource Group edge. Focused Core, Operator, and Console checks passed; the refreshed authenticated `8010` response returned three Resources and three relationships, including the new typed dependency, with three available configuration evidence records and no unavailable relationship evidence. Desktop, constrained-desktop, and mobile overflow checks passed. | Add only separately verified managed-identity, registry, observability, network, data-service, and service-call producers. Do not derive any dependency from shared Resource Group membership or unresolved environment-variable names. |
| 2026-08-23 | in-progress | Expanded the reviewed Azure relationship producer across the active provider inventory. Per-row mappings and complete-generation unique alias joins now remain phase-owned, endpoint closure prevents dangling links, snapshot metadata preserves global relationship gaps, and the Operator traverses a bounded depth-8 graph without Resource Group transit. The Console separates relationship coverage from traversal truncation and reports the runtime call graph as unavailable because current telemetry cannot prove both endpoint Resource identities. | `current change`; focused Core checks passed 164 cases, Operator checks passed 3 cases, Console checks passed 26 cases, migration/readiness checks passed 3 cases, and Ruff, strict mypy, and Console typecheck passed. A fresh authoritative local generation retained 519 Resources and 924 links. Excluding Resource Group and Subscription transit, components fell from 319 to 103, isolates from 289 to 81, and the largest component grew from 61 to 190. Exact stored edges yielded 83 ingress-to-egress endpoint pairs across 59 type-path patterns. The authenticated depth-8 `8010` response returned 188 Resources and 444 links for one Container App, schema `1.3.0`, six independent source states, no authority, and explicit coverage and activity limits. Desktop, constrained-desktop, and mobile checks reported zero document or workbench horizontal overflow. | Resolve or classify the remaining `missing_target_endpoint`, `target_type_mismatch`, and `unresolved_reference` candidates by exact provider identity. Project runtime call edges only after telemetry supplies independently verifiable caller and target Resource IDs. |
| 2026-08-23 | validated | Replaced unbounded per-rank vertical stacking in the existing Instances view with a selected-Resource focus layout. Direct incoming and outgoing Resources and links remain prominent, indirect context is dimmed, and pointer or keyboard focus restores the deterministic shortest path to the root without removing any bounded Resource or link. Selecting a neighbor reuses the same view and recenters it as the new root. | `current change`; `ontology-instance-graph.model.test.ts` passed 14 cases, Console typecheck and production build passed, and authenticated standard-port Browser checks retained 188 Resources and 444 links in a 600 px high graph. Root center error stayed within 8 px at `1440x900`, `993x641`, and `390x844`; document, workbench, toolbar, and summary overflow remained zero; an indirect focus restored eight path nodes, edges, and labels; and direct-neighbor selection recentered the same Instances view. | Continue the separately tracked authoritative Activity Log, Resource Health, relationship-coverage, and runtime-call evidence work. No additional ontology screen is required for this presentation contract. |
| 2026-08-23 | validated | Added exact complete-generation projection for Container Apps and Jobs that reference a Key Vault secret URL, added Private DNS zone containment for each VNet link, and changed the existing Instances canvas from an all-link rank graph to a bounded semantic focus projection. Dependency, access, and containment meanings no longer compete for one rank; the Inspector and conversational context now distinguish direct relationships from connected path segments. | `current change`; provider mapping, ARG, and generation checks passed 124 cases; Console instance, graph, and localization checks passed 31 cases; Console typecheck, production build, and catalog parity passed. A fresh active local snapshot retained 519 Resources and 937 links. Authenticated Browser evidence showed 13 exact workload-to-Key Vault dependencies, the Private DNS zone parent plus VNet attachment, and bounded focus canvases of 25 nodes and 55 edges for the 200-Resource, 469-link Key Vault response and 15 nodes and 31 edges for the 188-Resource, 444-link PostgreSQL response. Desktop, constrained-desktop, and mobile checks kept root centering within 8 px and reported zero document, workbench, or legend overflow. | Add exact secret-only database bindings, Private Endpoint DNS zone-group projection, and authorization evidence only from their own typed sources. Existing relationship drops keep the inventory ontology projection unavailable until separately resolved or classified. |
| 2026-08-23 | validated | Enforced strict `LR` graph coordinates, added AuthorizationResources role assignments, resolved provider-less authorization scopes in the complete generation, collected Private Endpoint DNS zone groups through bounded read-only ARM child APIs, and preserved mapping-specific drop classifications. The five service Terraform roots now expose a non-secret PostgreSQL host without exposing the secret DSN. | `current change`; focused ARM, ARG, generation, inventory-sync, hybrid wiring, and catalog checks passed; Operator Terraform format and service-root checks passed. A fresh active local generation retained 765 Resources and 1,439 links with 178 scope, 106 managed-identity, and two sets of 21 DNS closure edges. Authenticated `5273` Browser checks found zero non-LR edges, zero document overflow, exact role-assignment mapping evidence, and a three-node/two-edge DNS focus with no unavailable direct evidence. | Deploy the new service host inputs before claiming database dependency evidence, resolve or accept reviewed unavailable status for the eleven persisted relationship-drop groups, and retain PostgreSQL database-role evidence separately from Azure RBAC. |
| 2026-08-23 | validated | Added root-relative Incoming, Selected Resource, and Outgoing bands to the existing Instances graph. Reciprocal links and bounded cycles use duplicate presentation occurrences so every displayed stored edge remains strict `LR` without changing source or target. The Inspector now separates graph direction, access, containment, connected paths, and verified traffic meaning. Verified ingress is limited to an exact gateway or load-balancer backend target; verified egress is limited to those source mappings and the AKS effective outbound mapping. Alert, telemetry, dependency, attachment, containment, peering, name, DNS, and Resource Group evidence never becomes traffic direction. | `current change`; Console graph and instance checks passed 31 cases, typecheck, production build, and catalog parity passed; focused relationship backend checks passed 192 cases with Ruff and strict mypy. A read-only ARG and bounded ARM GET refresh promoted 832 Resources and 1,513 links. Authenticated `5273` checks across Key Vault, PostgreSQL, Container App, Private Endpoint, role assignment, Managed Identity, Application Gateway, Load Balancer, and AKS roots found zero non-LR edges and zero document or workbench overflow. Root center error stayed within 8 px at `1440x900`, `993x641`, and `390x844`; mobile controls remained at least 44 px. One exact AKS outbound edge rendered as verified egress with complete mapping, source-property, and cutoff evidence. | No active gateway or load-balancer backend mapping proves verified ingress in this generation, so the Console reports none. The same eleven residual groups and 26 candidates remain; no edge was fabricated to reduce them. |
| 2026-08-23 | validated | Restored containment-aware DAG context around the selected Resource. Bounded ancestor closure now places Resource Group before Private Endpoint, restores VNet and Subnet, prevents scope nodes from becoming traversal hubs, and assigns one longest-path occurrence to acyclic nodes while retaining duplicate occurrences only for reciprocal or cyclic edges. Relationship-drop metadata now preserves candidate multiplicity, attempts exact aliases before classifying an open environment value, and ignores values that cannot denote an ARM, URI, or hostname reference. | `current change`; Console checks passed 38 cases, typecheck, production build, and catalog parity passed; focused relationship checks passed 181 cases with Ruff and strict mypy. A read-only ARG and bounded ARM GET refresh retained 832 Resources and 1,513 links. The authenticated PostgreSQL graph rendered 17 nodes and 50 edges in the order Resource Group `-2`, Private Endpoint and VNet `-1`, selected PostgreSQL `0`, NIC `1`, and Subnet `2`. All three viewports had zero duplicate Resource occurrences, collision, non-LR edge, document, workbench, or legend overflow, with root error at most 8 px and 44 px mobile controls. | The prior 11-group/26-candidate history value was an undercount. Truthful multiplicity first exposed 11 groups and 863 candidates; shape filtering removed one invalid ARM-value group and reduced the final residual to 10 groups and 256 candidates without adding an edge. Provider-missing AKS and load-balancer endpoints, unmodeled role principals and child scopes, unresolved registries, and configured external endpoints remain explicit. |
| 2026-08-23 | validated | Added a wide fixed-node graph viewport. Semantic columns increased from 242 px to 288 px, containment changed from peering-like dots to a solid hierarchy line, and zoom-out, fit, zoom-in, Ctrl or Meta plus wheel, and empty-canvas drag pan operate without moving Resource nodes. Registry dependencies retain `workload -> depends_on -> registry`; selecting a registry shows its exact consumers as incoming relationships rather than reversing the ontology edge. | `current change`; focused Console checks, typecheck, production build, and catalog parity passed. Authenticated desktop evidence showed all 16 consumers of the largest registry, 2,232 px canvas width at 100%, fit at 31%, center-preserving zoom, drag pan with unchanged node transforms, and zero non-LR or page overflow. Constrained desktop fit plus zoom reached 43%; mobile reached 40%, retained 44 px controls, and both viewports kept node transforms fixed with zero document, workbench, or legend overflow. Computed styles proved solid containment, `5 4` dashed access, and `2 4` dotted peering. | Registry configuration and edge coverage is complete for all 9 Apps and 11 Jobs. No reverse registry edge or additional relationship is justified. Remaining work stays with the separately listed provider evidence gaps. |
| 2026-08-23 | implemented | Added exact complete-generation AKS ownership evidence from `properties.nodeResourceGroup`. A unique Resource Group name produces `AKS -> attached_to -> managed Resource Group`; an ambiguous cross-subscription name produces `unresolved_reference` and no edge. The Console uses only that mapping plus stored containment and dependency edges to present main Resource Group, AKS, managed Resource Group, VMSS and Load Balancer, and identity and outbound IP in order. | `current change`; provider catalog, complete-generation, and inventory-sync checks passed 38 cases; Console focused checks passed 47 cases; typecheck and production build passed. A bounded read-only ARG/ARM refresh promoted 765 Resources and 1,442 links. The authenticated AKS response exposed 14 Resources and 21 links, including the exact managed-Resource-Group mapping; the focused canvas rendered 7 nodes and all 10 selected stored edges with zero duplicate occurrences, collisions, non-LR edges, or document overflow at `1440x900`, `993x641`, and `390x844`. | The active snapshot retains existing relationship drops, so the inventory ontology projection correctly remains unavailable with zero replacement objects or links. Preserve a governed exact-source Browser receipt before promoting this presentation evidence. |
| 2026-08-23 | validated | Preserved `source_property_path` and exact provider-native endpoint types in mapping-specific drop counts, stopped treating unmatched role-assignment principals and generic Container workload environment strings as Resource candidates, and retained unresolved non-loopback endpoint URIs as unavailable evidence. No edge is created without complete-generation endpoint identity. | `current change`; focused relationship backend checks passed 199 cases, Operator and Console checks passed 41 cases each, Ruff and strict mypy passed, and Console typecheck and production build passed. One authoritative refresh promoted 832 Resources and 1,516 links with ten residual classifications and 69 candidates. The authenticated schema `1.3.0` depth-8 response and all three viewports preserved explicit coverage, six source states, zero page overflow, zero node overlap, and no execution or mutation authority. | Resolve or classify the remaining exact missing targets, child authorization scopes, managed AKS Resource Group names, external registries, and three configured Communication endpoints. Runtime call edges remain unavailable until telemetry proves both Resource IDs. |
| 2026-08-23 | validated | Resolved the three configured Communication endpoints through one exact complete-generation `hostName` alias and assigned a reviewed mapping-specific unavailable reason to every remaining suppressed candidate. The ARG visibility check across all accessible subscriptions found no missing AKS managed endpoint or cross-subscription subnet target; unmatched external registries and unmodeled authorization child scopes remain explicit non-edges. | `current change`; focused relationship backend checks passed 199 cases with Ruff and strict mypy. One authoritative refresh promoted 832 Resources and 1,519 links. Snapshot metadata records nine classifications and 66 candidates: 10 `target_outside_active_generation`, 40 `target_provider_type_unmodeled`, and 16 `reference_not_observed`. Authenticated schema `1.3.0` depth-8 evidence returned three complete incoming Communication edges from three distinct jobs, preserved runtime-call unavailability, and granted no execution or mutation authority. | Mapping-specific relationship coverage work is complete for this generation. Runtime call edges remain unavailable until telemetry proves both Resource IDs; unrelated deployment and OI campaign work remain separately tracked. |
| 2026-08-23 | validated | Restored local full-stack preparation parity with the scheduled ARG inventory by binding complete provider scope coverage and unmapped provider identities in the authoritative refresh path. Preparation can no longer replace the identity-complete graph with a vocabulary-only generation. | `current change`; the local-refresh contract, Ruff, and strict mypy checks passed. The corrected authoritative preparation refresh promoted 832 Resources and 1,519 links with the same nine classifications and 66 candidates, while the scope-transit-excluded graph retained 789 Resources, 800 links, 197 components, 160 isolates, and largest component 319. | Keep the preparation and scheduled inventory bindings identical when adding another provider identity or source overlay. |
| 2026-08-24 | implemented | Added reviewed Azure parent and root containment, bounded AKS AgentPool child collection, and UID-grounded Kubernetes API enrichment before the single atomic promotion. Missing Kubernetes bindings record unavailability and never replace the Azure generation. | `current change`; focused Azure, Kubernetes, inventory, catalog, and composition checks passed 260 cases; Ruff passed; strict mypy passed for 10 source files. | Add deployed environment and secret mounts, then retain a complete live Kubernetes generation before claiming runtime validation. |
| 2026-08-24 | implemented | Replaced the unresolved static token mount with an opt-in AKS workload-identity binding. Terraform grants only AKS RBAC Reader, passes the endpoint, public CA, cluster id, and deployment-supplied audience, and stores no bearer token. | `current change`; focused Kubernetes and inventory checks passed 44 cases; Terraform validation and focused identity contract checks passed. | Retain a complete release-bound live Kubernetes generation before claiming runtime validation. |
| 2026-08-28 | implemented | Hardened bitemporal topology replay completeness and digest integrity in `graph_at`. Completeness now also requires every selected revision batch to carry a non-null source receipt digest, so a batch missing its provenance receipt can no longer be reported as a complete graph. The replay digest now hashes the same canonicalized, deduplicated source receipt digest tuple that the response returns instead of a separate raw list that could diverge from the returned field. | `current change`; `core/ontology_platform/topology_history.py`; focused topology-history checks (`8 passed`), including a reverted-fix regression run that reproduced the prior digest/field mismatch. | Retain deployed replay evidence separately; every current production writer already sets a source receipt digest on each batch. |
| 2026-08-28 | implemented | Hardened exact-Pod diagnosis before log-provider I/O. Termination evidence must fall inside the requested lookback, lifecycle rows must match the exact Pod UID, and projected state must carry complete, current, conflict-free, non-synthetic provider metadata with provenance. Same-UID restart reduction no longer requires historical ownership evidence superseded by the immutable UID. | `current change`; focused Pod diagnosis and replacement checks 27 passed; Ruff and strict mypy passed. | Retain authenticated exact-target diagnosis evidence separately. |
| 2026-08-23 | validated | Separated the AKS outbound Public IP fan-in without adding, reversing, or reclassifying a stored edge. The Public IP remains on the same row as its exact Load Balancer parent. The exact `AKS -> routes_to -> Public IP` path uses an outer upper port, the stored `Load Balancer -> attached_to -> Public IP` path stays centered, and Resource Group containment uses a lower port. All three paths retain their original source, target, relationship type, and mapping evidence. | `current change`; focused Console checks passed 39 cases, and Console typecheck and production build passed. The authenticated standard-port response retained 14 Resources and 21 links; the canvas retained 7 nodes and all 10 selected stored edges. At `1440x900`, `993x641`, and `390x844`, duplicate occurrences, collisions, non-LR edges, node crossings, document overflow, and workbench overflow were all zero. Root center error stayed within 8 px, and mobile controls remained 44 px. | Preserve the governed exact-source Browser receipt separately. Relationship coverage, inventory projection availability, and the remaining provider evidence gaps are unchanged. |
| 2026-08-23 | validated | Preserved stored relationships between Resources already selected as direct children of a Resource Group or Subscription. The scope limit still chooses at most 36 direct child links and does not expand hidden branches, but exact child-to-child edges are no longer discarded. The measured Resource Group now renders the stored `Disk -> attached_to -> VM` and `NIC -> attached_to -> VM` mappings without reversing source or target. | `current change`; focused Console checks passed 39 cases, and Console typecheck and production build passed. The authenticated response retained 26 Resources and 40 links; the bounded canvas rendered 12 nodes and 15 stored edges. At `1440x900`, `993x641`, and `390x844`, both VM attachment edges remained strict `LR`, crossed no node, and produced zero document or workbench overflow. Mobile controls remained 44 px. | Scope-root rendering adds no Resource or relationship and retains the existing response completeness state. Governed Browser artifact retention remains separate. |
| 2026-08-23 | validated | Corrected the prior 36-child Resource Group summary after a high-cardinality group rendered 38 nodes, 72 of 481 stored edges, and 196 edge-to-node crossings. A scope root now shows one seven-row, type-prioritized representative child column. Among selected children it preserves only exact stored `attached_to` relationships; dependency and routing relationships remain available when an operator selects the owning Resource. The smaller Resource Group still renders both exact Disk and NIC attachments to its VM. | `current change`; the graph model passed 27 focused cases. The high-cardinality authenticated response retained 200 Resources, 481 links, and `resource_limit` incompleteness while the canvas rendered 8 nodes and 7 stored containment edges with zero crossings, non-LR edges, collisions, document overflow, or workbench overflow. `993x641` and `390x844` retained the same 8/7 bound; mobile controls remained 44 px. The smaller Resource Group rendered 8 nodes and 9 stored edges, including both strict-LR VM attachments with zero node crossings. | The summary is representative, not a complete Resource Group topology claim. Operators select a child Resource to inspect its bounded dependency, routing, and wider relationship context. |
| 2026-08-23 | validated | Connected the Instances search field to an exact Resource autocomplete selection and replaced native SVG title bubbles with FDAI graph tooltips for Resources and relationships. Duplicate display labels append the stable Resource identity so every suggestion remains selectable. Choosing a suggestion reuses the existing read-only URL selection path and updates the selector and bounded graph together. | `current change`; focused Console checks passed 18 cases, Console typecheck and production build passed, and the authenticated standard-port Browser exposed 200 suggestions. One exact suggestion selected the same Resource in the URL and selector, native SVG titles fell to zero, and the custom tooltip rendered Resource name, type, and state. Desktop, constrained desktop, and mobile checks reported zero document, toolbar, or workbench overflow; the mobile input remained 44 px. | Graph evidence, completeness, relationship direction, and authority are unchanged. Activity Log, Resource Health, and runtime call evidence remain separately tracked. |
| 2026-08-23 | validated | Replaced the browser-native Resource datalist with a Console-owned accessible autocomplete. Case-insensitive containment matching renders no more than five suggestions, keyboard navigation preserves exact Resource selection, and the owned listbox uses Console surface, text, border, focus, height, and overflow tokens. Short desktop and mobile layouts open the list above the input, while mobile keeps five complete 44 px rows. | `current change`; the focused autocomplete model and view checks passed 19 cases, Console typecheck and production build passed, and authenticated standard-port Browser checks passed at `1440x900`, `993x641`, and `390x844`. Every viewport rendered exactly five suggestions inside the list and viewport with zero document or explorer overflow. The desktop list used a 220 px maximum height and the mobile list used 230 px for five complete touch rows. Arrow-key selection synchronized the input, selector, and read-only URL. | Graph evidence, completeness, relationship direction, and authority are unchanged. Activity Log, Resource Health, and runtime call evidence remain separately tracked. |
| 2026-08-23 | validated | Excluded observed `authorization.role-assignment` Resources from the operator-selectable instance directory, selector, autocomplete, and direct URL root. The underlying observed objects and access relationships remain available as graph evidence; this presentation filter does not delete provider evidence or change graph, mutation, or execution authority. | `current change`; the focused instance model and view checks passed 20 cases, Console typecheck and production build passed, and the authenticated standard-port Browser cleared an existing role-assignment URL to the unselected Instances view. The selector and autocomplete exposed zero role-assignment entries, while 49 ordinary Resources and four matching Container Registry suggestions remained available with zero page overflow. | Access evidence and provider-observed role assignments remain in the bounded graph response for relationship inspection. Activity Log, Resource Health, and runtime call evidence remain separately tracked. |
| 2026-08-23 | validated | Expanded the Resource autocomplete from five to ten bounded matches and aligned the Console with the component specimen's rich option hierarchy. Each row now presents a compact type badge, Resource name, exact resource type, and desktop Resource label. The list uses a 480 px desktop width and a 420 px scroll ceiling; mobile keeps the input width, hides the redundant type label, and preserves touch-sized rows. | `current change`; the focused Console checks passed 20 cases, the component specimen contract passed, Console typecheck and production build passed, and authenticated Browser checks covered both the standard Console and design server. Desktop rendered ten rich rows in a 480 px list with no secondary-type truncation or page overflow. Mobile rendered ten scrollable rows at least 52 px high with zero page overflow. The design specimen exposed ten matches under a 360 px scroll ceiling. Both surfaces retained zero role-assignment suggestions. | Exact selection, keyboard behavior, graph evidence, completeness, relationship direction, and authority are unchanged. Activity Log, Resource Health, and runtime call evidence remain separately tracked. |

### Relationship hardening record

| Round | Review lens | Highest verified severity | Evidence and disposition |
|-------|-------------|---------------------------|--------------------------|
| 1 | Direction and parent ownership | Critical, resolved | Reproduced eight AKS AgentPool dual parents. Reviewed provider-parent containment now shadows generic Resource Group fallback for the same child. |
| 2 | LinkType cardinality and durable promotion | High, resolved | Snapshot promotion rejects multiple `contains` parents, while ontology `replace_subgraph` independently runs declaration cardinality validation. |
| 3 | Classified non-edge reason forgery | Low | Exact drop-reason and unavailable-reason combinations validate at the provider contract boundary; unclassified failures remain blocking. |
| 4 | Provider completeness versus relationship coverage | Medium, resolved | `complete` now controls safe generation replacement and `relationship_complete` independently preserves classified missing edges without claiming absence. |
| 5 | Active snapshot, status, and manifest skew | Medium, resolved | PostgreSQL graph reads require exact generation agreement; status remains the final commit marker and mismatch is incomplete evidence. |
| 6 | Empty Resource result false absence | High, resolved | Requested root ObjectTypes reach the store and source coverage remains attached even when zero Resource rows are returned. |
| 7 | Freshness, future cutoff, and clock boundaries | Medium, resolved | Operator evidence uses an injected aware clock, marks expired or future-cutoff evidence non-complete, and keeps the exact cutoff and ceiling. |
| 8 | Configuration evidence versus independent verification | Medium, resolved | API and Console expose `configuration_observed` separately from `independently_verified`; configuration evidence no longer reads as verification. |
| 9 | ACL, source metadata, and digest binding | Medium, resolved | ACL projection, filtering, link closure, immutable freezing, and result digests preserve source generation and completeness. |
| 10 | Rolling N/N-1 compatibility | Medium, resolved | New receipts emit 1.2 while 1.1 payloads without additive source fields still decode with conservative legacy defaults. |
| 11 | Operator, Console, and localization parity | Medium, resolved | Strict decoding accepts current, stale, and unavailable states, validates evidence-kind/verification consistency, and English/Korean catalogs expose the same labels. |
| 12 | Concurrency, crash recovery, and single writer | High, resolved | A process-local lock plus PostgreSQL session advisory lock covers graph and commit-marker writes; failure injection proves manifest-before-status retry recovery. |

A 2026-09-05 source-to-store review found open High-severity retention and current-state gaps that
the earlier critique did not cover: sparse change hints can be treated as complete overlay
properties, pending overlay work is not bound to the ontology projection watermark, and production
archive I/O and purge are not wired. The bounded-history design above and OI-13 through OI-16 now
own their closure. Until those exits pass, realtime ontology freshness and bounded historical
retention remain in progress.
| 2026-09-06 | implemented | Exposed stored model deployment name, version, SKU, and normalized TPM through one bounded Operator allowlist and the ontology instance Console, including screen context. | `current change`; focused Operator and Console tests, typecheck, build, localization checks, and repeated hardening review. | Retain an authenticated 5273 visual check; TPM freshness remains eventual and depends on a committed observation. |
| 2026-09-04 | implemented | Kept the local long-running inventory loop alive after exact all-source exhaustion while preserving one-shot scheduled-job failure semantics. | `current change`; focused inventory CLI retry and propagation checks. | Retain a governed local recovery receipt and deployed scheduled-job failure receipt before raising adaptive scheduling to `validated`. |
| 2026-09-04 | implemented | Corrected adaptive reconciliation so an active snapshot's age supplies the last-attempt age when no newer failed attempt exists. Stale change-demand collection now becomes due instead of resetting the minimum wait on every loop. | `current change`; inventory scheduler and reconciliation gate checks (`42 passed`); authenticated `localhost:5273` validation returned 32 fresh non-running rows, displayed the bounded first 20, and completed in 4.6 seconds after a promoted inventory snapshot. | Retain deployed cadence and provider-pressure evidence before raising adaptive scheduling to `validated`. |
| 2026-09-02 | implemented | Added the Core-owned operational state-transition ledger, atomic fact-plus-coverage batches, bitemporal reads, a read-only FunctionType, PostgreSQL persistence, and inventory-promotion collection. | `current change`; transition contract, persistence, migration ownership, inventory publisher, query registration, planning, and rendering checks. | Bind continuous provider-event coverage for complete intervals and retain a governed deployed transition receipt before raising this row to validated. |
| 2026-09-01 | implemented | Hardened the existing Azure Resource Health seam around the exact secured denominator. Fixed queries use bracket property access, the reader preserves `available`, `unavailable`, `degraded`, `unknown`, and `state_absent`, and every target receives a distinct coverage status. Missing and duplicate rows, failed batches, provider scope widening, invalid rows, unresolved targets, and bounded-response exhaustion keep the result incomplete. Collection start, completion, and provider observation times remain separate, and semantic assurance plus Operator presentation expose limitations without model-selected authority. | `current change`; Resource Health contract, Azure delivery, semantic assurance, Operator presentation, and focused replay tests; Ruff, formatter, and focused strict mypy passed. | Add the tuple-set reread fence and out-of-band answer validator under the exit criteria below. No live Azure or model execution was performed. |
| 2026-08-31 | implemented | Completed issue #295's deterministic analyzer parity boundary with restart-durable completed-receipt suppression, bounded claim recovery, and one analyzer-path receipt joining same-UID restart and distinct-UID replacement detection latency, evidence completeness, publication, and recovery closure. | `current change`; analyzer runner, CLI, PostgreSQL publication ledger, Pod replacement analyzer-path scenario, and focused checks (`85 passed`); Ruff passed. | No deployed runtime or Azure receipt was created. Protected deployed certification remains external to issue #295. |
| 2026-08-31 | implemented | Completed issue #291's reporting surface. `AnalyzerFindingReceipt` now carries `resource_ref`, `resource_kind`, `occurred_at`, and `recovery_status`; `core/readiness/detection_lifecycle.py` reduces retained receipts into a bounded Pod lifecycle projection; `delivery/detection_lifecycle_state.py` persists it fail-closed under `runtime:detection-lifecycle:`; and `detection_lifecycle_projection.py` serves it through the existing authenticated `/detection-readiness` family and its existing Console route as four separate answers. No parallel endpoint, contract, or Console route was added. | `current change`; `test_detection_lifecycle.py` (19), `test_detection_lifecycle_state.py` (12), `test_detection_lifecycle_projection.py` (26), `test_runtime_projection_reader.py` (7), analyzer tick suites (50), and `tests/integration/test_pod_lifecycle_detection_e2e.py` (8) passed, 122 focused Python tests in total; Console `vitest` 2,204 passed, strict typecheck and production build passed, `check-independent-services` OK, and the configured strict mypy gate held its 7-error pre-existing baseline. Three Playwright scenarios passed on desktop and mobile projects at 1440x900, 993x641, and 390x844. | Live Pod lifecycle evidence still arrives only through `FDAI_POD_LIFECYCLE_EVIDENCE_JSON`, so authenticated deployed Pod projections and the issue #292 ingestion path remain open. |
| 2026-08-31 | implemented | Completed issue #295's deterministic analyzer parity boundary with restart-durable completed-receipt suppression, bounded claim recovery, fail-closed claim-store errors, and one local-loop analyzer-path receipt joining same-UID restart and distinct-UID replacement detection latency, evidence completeness, publication, and recovery closure. | `current change`; analyzer runner, CLI local loop, PostgreSQL publication ledger, Pod replacement analyzer-path scenario, and focused checks (`116 passed`); Ruff passed. | No deployed runtime or Azure receipt was created. Protected deployed certification remains external to issue #295. |
| 2026-08-31 | implemented | Added bounded ontology-release transition recovery for the inventory single writer. It validates a 1.3 manifest with its original release digest, carries only verified ownership identities into atomic replacement, and enforces a release-independent content digest before writing the current release. Legacy 1.2 ownership cannot cross releases, and tampered or changed same-generation content remains blocked. | `current change`; focused inventory ontology projection checks passed 26 cases. | Rerun the authoritative Azure inventory refresh on the committed revision and retain the same-release graph and query evidence under Issue #244. |
| 2026-08-30 | implemented | Made PostgreSQL graph replacement and manifest/status advancement atomic under an active-generation fence, added endpoint foreign keys, pending-reconciliation completeness degradation, subset-promotion refusal, a total delta deadline, batched revision reads, and a concurrent JSONB containment index. | `current change`; focused inventory, projection, persistence, migration, and ontology-provider checks. | Retain protected deployment and scale receipts; further set-based write optimization remains evidence-driven work. |
| 2026-08-27 | implemented | Added a standard local background analyzer task over the deployed one-shot CLI. The loop preserves inventory target discovery, metric mappings, idempotency keys, event contracts, and shadow mode; it reports first-tick failure before readiness and stops on publication failure. | `current change`; analyzer CLI, local runner, VS Code task, Container Apps scheduling receipt, and focused analyzer checks (`40 passed`). | Retain a bounded local and deployed restart/replacement scenario receipt with measured detection and ingestion latency under OI-12. |
| 2026-08-27 | implemented | Added a bounded exact-Pod diagnosis FunctionType that joins projected termination state and lifecycle events with content-free AppTraces, AppExceptions, and ContainerLogV2 evidence. The collector stops at 129 records, emits at most 128 digests, rejects widened UID or time scope and oversized bodies, and never retains raw log text. | `current change`; Core reducer and query, Azure provider, runtime composition, and focused checks (`106 passed`); strict mypy passed for the new Core, delivery, and composition paths. | Add source-specific workspace, table, authorization, UID-label, and ingestion coverage reasons; bind deterministic semantic planning and retain authenticated Console evidence after durable lifecycle ingestion lands. |
| 2026-08-27 | implemented | Preserved bounded current and previous container termination facts in the Kubernetes Pod inventory projection without admitting raw diagnostic text or runtime identifiers. | `current change`; `services/core-control-plane/src/fdai/delivery/kubernetes_api_inventory.py`; focused inventory checks (`9 passed`). | Persist lifecycle observations through the typed resumable collector, then join bounded termination and runtime-log evidence without causal overclaim. |
| 2026-08-27 | implemented | Bound Azure Monitor runtime-call rows to typed envelopes and an independently supplied authentication context. Exact caller and target Resource IDs are projected through the existing single-writer enricher; truncated, malformed, stale, redacted, or out-of-generation evidence remains unavailable. | `current change`; Azure runtime-call source, authorization target-identity hold, runtime-call projector, and focused tests (`76 passed`); Ruff and strict mypy. | Authenticated Operator and Console evidence plus protected deployment receipts remain external-only. |
| 2026-08-27 | implemented | Kept usable Kubernetes cluster, namespace, pool, node, workload, Service, endpoint, and Ingress records when a reviewed relationship endpoint is missing. The dropped relationship remains explicit unavailable evidence and no inferred edge is added. | `current change`; Kubernetes enrichment and relationship checks (`14 passed`); inventory promotion metadata preserves the drop classification. | Retain one complete exact-cluster generation from an authorized runtime source; Pod lifecycle and recovery remain separately owned. |
| 2026-08-27 | implemented | Added release and source-receipt bindings to historical topology replay, made duplicate content-addressed appends safe to retry, and lowered completeness when an active historical link is dangling. Inventory projection manifests now share a content digest with their status marker and reject tampered reload state. | `current change`; topology history, PostgreSQL adapter, projection reload, and coverage checks (`76 passed`); Ruff and strict mypy. | Deployed PostgreSQL host propagation, live database-role evidence, and protected Azure validation remain external-only. |
| 2026-08-27 | implemented | Hardened runtime-call collection to require both telemetry tables and retain unavailable, redacted, and malformed row coverage. Any partial candidate now makes the batch incomplete, so no valid subset is projected as complete evidence. | `current change`; Azure runtime-call source coverage tests (`5 passed`); Ruff and strict mypy. | Authenticated Operator and Console evidence remains external-only. |
| 2026-08-27 | implemented | Propagated partial Kubernetes relationship drops into promotion coverage and withheld topology baselines with incomplete relationship evidence. Historical replay now requires every selected batch to carry one consistent exact ontology release. | `current change`; Kubernetes, inventory-promotion, and topology replay tests (`55 passed`). | Retain a complete exact-cluster generation from an authorized runtime source; Pod lifecycle and recovery remain separately owned. |
| 2026-08-27 | implemented | Extended the 1.2.0 inventory manifest migration path and content-addressed 1.3.0 manifest digest to include canonical object types and properties plus link properties and source content. Same-generation content changes are rejected before replacement. | `current change`; projection upgrade, same-generation, content-tamper, mixed-release, and completeness tests (`55 passed`). | Deployed projection reload and Azure receipts remain external-only. |
| 2026-08-27 | implemented | Registered `runtime_calls` in the inventory topology projection contract and verified the complete telemetry-to-current/history path. Valid exact endpoint links remain complete instead of being dropped as unregistered. | `current change`; end-to-end runtime-call inventory, ontology, and topology-history checks (`6 passed`). | Authenticated Operator and Console evidence remains external-only. |

### Remaining work

- [x] `OI-01` records a source-to-store implementation audit and identifies the exact owner, tests,
  and missing binding for every collection, projection, query, retention, and archive stage. The
  focused audit checker passes 3 tests.
- [x] `OI-02` defines validated source policies, freshness objectives, budgets, priority, and
  throttling inputs without hard-coded tenant values.
- [x] `OI-03` implements adaptive due calculation and proves healthy, lagging, changing, `429`,
  timeout, circuit-open, and recovery transitions with a pure deterministic test matrix.
- [x] `OI-04` proves event, delta, complete snapshot, duplicate, reorder, tombstone, and concurrent
  promotion convergence without losing objects or relationships.
- [x] `OI-05` exposes principal-safe collection health for cursor lag, overlay state, freshness,
  coverage, provider pressure, and the next scheduled action.
- [x] `OI-06` implements semantic-policy-driven rollups and proves zero, missing, partial, conflict,
  and merge behavior for every supported statistic.
- [x] `OI-07` implements archive manifests, verification, restore sampling, retention holds, and
  safe-to-retry purge receipts; source deletion remains blocked on any failed gate.
- [x] `OI-08` implements the pure graph evidence refresh policy and proves every `use_graph`,
  `refresh_then_query`, `use_live_evidence`, `query_archive`, and `hold` transition.
- [x] `OI-09` routes bounded live evidence back through observation ingress and proves that partial
  enrichment cannot replace a complete generation or widen authority.
- [x] `OI-10` proves representative questions select the expected instances, paths, functions,
  freshness outcomes, and archive behavior without answer-text matching.
- [x] `OI-11` corrects action-draft frame classification without keyword routing and passes focused
  bilingual positive and negative routing checks with typed no-authority receipts. Full-corpus,
  change-focused, release, and scheduled campaigns follow
  [Continuous Semantic Assurance](../../roadmap/interfaces/continuous-semantic-assurance.md) and are not an
  OI-11 exit criterion.
- [ ] `OI-12` runs wording regression and [deployed Azure certification](https://github.com/dotnetpower/fdai/issues/262) after OI-11; it measures freshness, API pressure, lag, storage growth, rollup coverage, archive restore, and provider failure behavior.
- [x] `OI-13` persists a versioned normalized object and relationship observation journal with
  explicit full, partial, change-hint, and tombstone semantics. Exit requires focused duplicate,
  reorder, sparse-property, operation-status, restart, and current-projection digest checks.
- [x] `OI-14` completes resource incarnation identity, relationship correction coverage, late
  correction partitions, and active-case or legal-hold pins. The prerequisite journal and projection
  watermark gate now makes pending observations and unconfirmed tombstones lower source
  completeness. Exit still requires every accepted correction to invalidate and then
  deterministically close its affected coverage.
- [x] `OI-15` binds a deployment retention policy registry, time-and-scope partitions, verified
  checkpoints, a production archive writer and principal-scoped reader, a concrete source purger,
  scheduled lifecycle coordination, and storage-pressure degradation. The dedicated runtime Job
  composes these surfaces under the non-executor inventory identity. Shadow, enforce, certify,
  archive-outage, restore-failure, hold, and purge tests prove that failed gates preserve source
  partitions and block completeness-dependent work.
- [x] `OI-16` retains one pinned-revision operational receipt proving bounded steady-state storage,
  exact warm replay, archive restore, safe partition purge, N/N-1 schema replay, database recovery,
  hold enforcement, and zero false-complete results under duplicate, late, delete, recreate,
  provider-failure, database-restart, and archive-outage scenarios. The dev-only synthetic campaign,
  protected bot-requested workflow, exact provenance binding, private phase evidence, and gated
  receipt writer are implemented. Campaign `34059357427` passed all 13 scenarios on exact revision
  `80b5892aa176e4a71eb2b3448982825b526d175b`, persisted receipt
  `sha256:6c9e7b5bc731776f065e25672d116c3f278ab02b2c09636191e6566a50552f0e`,
  and retained private merged artifact
  `sha256:aaefe0ec525c22ecf596e5e9169904f5a68fe8341cbfa8a85a165508259b2409`.
- [x] Provide the standard local profile with `analyzer: run continuously (local)`. It reuses the deployed one-shot analyzer CLI, local runtime environment, inventory target discovery, metric mappings, idempotency keys, event contract, and shadow posture without duplicating analyzer logic.
- [x] Suppress repeated same-window analyzer publications through a restart-durable completed broker receipt, and retain a deterministic same-UID restart and distinct-UID replacement receipt that joins detection latency, evidence completeness, publication, and recovery closure.
- [x] Report Pod failure and recovery through the authenticated Operator API `/detection-readiness` family and its existing Console route, keeping current state, failure history, recovery, and evidence gaps separable, failing closed or unavailable on missing, stale, incomplete, or conflicting evidence, and claiming neither a cause nor execution authority. One pinned-revision end-to-end proof drives the real analyzer, ledger, recorder, Operator reader, and Console model path.
- [ ] Complete [typed resumable Kubernetes lifecycle ingestion](https://github.com/dotnetpower/fdai/issues/292), bind its retained old/new UID and termination observations to the replacement reducer, and retain authenticated exact-target replacement and new-cutoff recovery receipts.
- [x] Resolve or assign reviewed unavailable status to every persisted mapping-specific
  relationship candidate. The final generation has nine classifications and 66 candidates:
  10 `target_outside_active_generation`, 40 `target_provider_type_unmodeled`, and
  16 `reference_not_observed`. Three Communication endpoints became exact verified edges. A
  read-only ARG comparison across all accessible subscriptions found no missing AKS managed
  endpoint or cross-subscription subnet target, and no edge was fabricated for an unavailable
  target.
- [ ] [Deploy all five service-root `POSTGRES_HOST` inputs](https://github.com/dotnetpower/fdai/issues/262)
  and retain a generation that projects each exact workload-to-PostgreSQL dependency. Secret names,
  environment-variable names, secret values, and unapplied Terraform remain invalid runtime evidence.
- [x] Complete the Private DNS path with 21 exact Private Endpoint to zone-group containment edges
  and 21 zone-group to zone attachments. The authenticated three-node/two-edge DNS focus exposes
  both reviewed mappings with complete direct evidence and no competing Resource Group parent.
- [x] Preserve exact Container workload registry semantics and interaction evidence. All 9 Apps and
  11 Jobs with registry configuration have projected dependencies; the largest registry exposes
  16 incoming workloads, and zoom, fit, and fixed-node pan make the complete fan-in inspectable.
- [x] Keep inventory projection commit records content addressed. The manifest digest is shared by
  the release-bound status marker and readers reject a mixed or tampered generation after restart.
- [x] Retain partial Kubernetes topology without inferred edges. Observed cluster, namespace, pool,
  node, workload, Service, endpoint, and Ingress records remain available when a reviewed endpoint
  is missing, while the missing relationship is reported as unavailable evidence.
- [x] Complete the local authorization evidence contract beyond the validated Azure slice of 225
  role assignments, 185 scope attachments, and 106 managed-identity attachments. Unmodeled child
  scopes retain `authorization_child_scope_unmodeled`, and typed principal-safe PostgreSQL role
  evidence remains separate from Azure RBAC without creating a Resource `depends_on` edge.
- [x] Add the reviewed `runtime_calls` LinkType and pure projector with exact endpoint, scope, cutoff, freshness, active-generation, exact-release, and no-authority checks.
- [x] Bind a typed runtime-call telemetry producer to the existing inventory single writer. The
  scheduled path records `telemetry_source_unavailable` and adds no edge until an authenticated
  source supplies both exact endpoint Resource IDs.
- [ ] [Retain authenticated `8010` and `5273` runtime-call evidence](https://github.com/dotnetpower/fdai/issues/260)
  from a pushed green SHA. Name-, group-, environment-, credential-, and RBAC-based endpoint
  inference remains rejected.
- [ ] Bind a publication reconciler that reads the broker or a downstream deduplication store keyed
  by the stable analyzer event id. Until then an uncertain claim reports
  `publication_reconciler_unbound` and stays awaiting reconciliation instead of republishing.
- [ ] Bind a live Kubernetes Pod lifecycle evidence source behind
  `build_pod_lifecycle_evidence_source`. Until then Pod findings exist only for evidence supplied
  through `FDAI_POD_LIFECYCLE_EVIDENCE_JSON`, and deployed Pod analysis remains unavailable.
- [x] `OI-11` corrects action-draft frame classification without keyword routing and passes focused
  bilingual positive and negative routing checks with typed no-authority receipts. Full-corpus,
  change-focused, release, and scheduled campaigns follow
  [Continuous Semantic Assurance](../../roadmap/interfaces/continuous-semantic-assurance.md) and are
  not an OI-11 exit criterion.

- [ ] Add a bounded tuple-set reread fence for the exact modeled and readable target partition.
  The first and second reads must use the same target-set digest, fixed query revision, deadline,
  and coverage contract; compare sorted
  `(logical_resource, availability_state, provider_observed_at)` tuples plus non-observation
  coverage. A changed digest may retry once within the original deadline and otherwise must return
  `state_changed`; missing, duplicate, blank, invalid, unreadable, or truncated evidence must never
  pass the fence or produce an all-clear.

- [ ] Add an out-of-band answer validator that consumes only the typed Resource Health receipt and
  proposed narration. It must compute the terminal disposition outside the model, replace any
  all-clear claim unless the exact denominator is complete and every target is canonically
  `available`, preserve every non-healthy or non-observation reason, reject denominator, count,
  time-window, and authority contradictions, and pass adversarial English and Korean tests with
  `execution_authority=false`.

- [ ] Persist the projected container termination facts through
  [Issue #292](https://github.com/dotnetpower/fdai/issues/292). For
  [Issue #294](https://github.com/dotnetpower/fdai/issues/294), add source-specific unavailable
  reasons, deterministic semantic planning, presentation, and an authenticated exact-target receipt.

- [ ] `OI-12` runs wording regression and [deployed Azure certification](https://github.com/dotnetpower/fdai/issues/262)
  after OI-11; it measures freshness, API pressure, lag, storage growth, rollup coverage, archive restore, and provider failure behavior.

- [x] Bind the standard local VS Code profile to the same analyzer CLI, target discovery, metrics,
  idempotency, event, and shadow contracts used by the deployed scheduled Job.

- [x] Suppress repeated same-window analyzer publications through a restart-durable completed
  broker receipt, and retain one deterministic analyzer-path receipt that joins same-UID restart
  and distinct-UID replacement detection latency, evidence completeness, publication, and recovery
  closure.

- [x] Complete authorization beyond the validated Azure slice of 225 role assignments, 185 scope
  attachments, and 106 managed-identity attachments. [Issue #260](https://github.com/dotnetpower/fdai/issues/260)
  requires reviewed unavailable child scopes plus separate typed and principal-safe PostgreSQL role
  evidence; RBAC grants and database credentials never become generic Resource `depends_on` edges.

- [x] Add the reviewed `runtime_calls` LinkType and pure projector with exact endpoint, scope, cutoff,
  freshness, active-generation, exact-release, and no-authority checks.

- [x] [Bind a typed runtime-call telemetry producer and single writer](https://github.com/dotnetpower/fdai/issues/260).
  The Azure Monitor adapter and inventory enricher require independently authenticated exact caller
  and target Resource IDs. Retain authenticated `8010`/`5273` evidence separately. Name-, group-, environment-, credential-, and
  RBAC-based endpoint inference remains rejected.

- [x] Report Pod failure and recovery on the existing authenticated `/detection-readiness`
  family and Console route, keeping current state, failure history, recovery, and evidence
  gaps separable, withdrawing state that outlived its freshness budget, failing the section
  unavailable with a named reason on a defective row, and claiming neither cause nor
  execution authority. One pinned-revision end-to-end proof drives the real analyzer,
  ledger, recorder, Operator reader, and Console model path.
