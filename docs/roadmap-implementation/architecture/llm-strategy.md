# LLM Strategy implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope
| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Capability registry, resolution, and provisioning assessment | implemented | `rule-catalog/llm-registry.yaml`; `rule_catalog/schema/llm_resolver.py`; `provisioning_assessment.py`; focused resolver tests | The secondary reasoner now falls through to the reviewed `MistralAI` `Mistral-Large-3` profile with version `1`, `GlobalStandard`, and 1K TPM when Anthropic capacity is unavailable. Azure quota resolution is SKU-qualified and cannot borrow capacity from another tier. |
| Publisher-qualified Foundry catalog, infrastructure, and endpoint contract | implemented | Resolver, deployment projection, partner module, endpoint resolver, sealer, Terraform endpoint output, and focused checks | Base capabilities and synthesized narrator, web-search, and primary-pool deployments carry catalog-selected stable versions. A preference without a GA version is skipped; a capability with no versioned alternative becomes `hil-only` and produces no synthesized route. |
| Environment model binding policy and PTU planning | implemented | `fdai_service_contracts/model_binding.py`; `model_binding_policy.py`; Operator IAM binding routes and PostgreSQL adapter; `model_binding_proposal.py`; `model-settings-projection.yml`; Console Models editor; protected deploy workflow; focused contract, resolver, Operator, Console, and Terraform checks | Owners can save revisioned `auto`, `pinned`, or `hil-only` intent for every T1/T2 capability. The protected runner joins one exact authority-free plan proposal to the current policy before evaluating PTU and exact model versions. A separate three-way CAS restores the sanitized model Settings projection without changing runtime Settings. Console and Operator retain no provider mutation or execution authority. |
| Candidate-only semantic judgment and planning | implemented | `core/conversation/semantic_judgment.py`; `core/conversation/semantic_planning.py`; `composition/wire_semantic_query.py`; Azure semantic adapters; focused judgment and planning tests | Bounded T1 judgment retries malformed schema output on the same binding before optional T2 escalation. Accepted meaning can guide planning but grants no execution authority. |
| T2 cross-check, verifier, grounding, confidence, and rubric | implemented | `core/quality_gate/`; `delivery/azure/llm/rubric.py`; focused quality-gate and Azure adapter tests | The four required legs and optional subtractive rubric exist. Missing or invalid evidence lowers the result to denial, abstention, or human review. |
| Escalation policy and same-publisher primary latency routing | implemented | `core/quality_gate/escalation_ladder.py`; `delivery/azure/llm/latency_routed_cross_check.py`; `composition/wire_llm.py`; focused routing tests | The ladder remains never-authoritative, and latency selection cannot cross into the secondary publisher. A separate bounded proposer fallback can invoke the registered secondary proposer, but its candidate still enters the same quality gate. |
| T2 proposer failover, durable recovery evidence, and governed route selection | implemented | `core/tiers/t2_reasoning/recovery.py`; `runtime/t2_{recovery,route_registry}.py`; `ops.switch-t2-proposer-route`; focused runtime and pantheon-chain tests | Every attempted proposer reserves budget and emits sanitized durable evidence. Terminal exhaustion reaches human approval before Thor can persist a route change, and Vidar restores only the failed change. No governed deployed recovery campaign is retained here. |
| Model lifecycle expiry review mechanics | implemented | `model_lifecycle_review.py`; `model_lifecycle_reconciler.py` proposal schema v3; focused lifecycle and Key Vault source tests | Proposals bind the exact source digest and affected capabilities. Expired unmerged reviews produce authority-free holds without changing mappings. The direct Key Vault source adapter exists, but startup loading, PR lifecycle observation, decision persistence, and runtime hold application remain open. |
| Operational model evidence and enforce promotion | in-progress | `core/measurement/model_tracking.py`; [Goals and Metrics](goals-and-metrics.md#implementation-status) | Measurement and promotion contracts exist, but one retained live cohort for every active T1/T2 capability is not evidenced here. |
| Weekly model reconciler and reviewed replacement flow | in-progress | `.github/workflows/model-lifecycle-reconcile.yml`; `scripts/deployment/azure/model_lifecycle_reconciler.py`; focused lifecycle and protected-workflow tests | The proposal-only path compares family, publisher, status, SKU, capacity unit, and capacity value, emits sanitized evidence, abstains on provider failure, and can open an idempotent draft with no activation authority. It has no expired-proposal evaluator or runtime hold binding, and no governed run receipt is retained. |
| L4 T2 cache lifecycle | implemented | `20260831_0089_t2_cache_lifecycle.py`; `postgres_t2_cache.py`; focused local PostgreSQL lifecycle and indexed-query checks | Core owns one exact-version writer. TTL reads, promotion invalidation, rollback retention, serialized rotation, idempotent receipts, and transactional failure rollback are implemented. No operational latency receipt is claimed. |
| Publisher-qualified model deployment projection | implemented | `llm_resolver.py`; `llm_endpoint_selection.py`; `resolver_queries.py`; focused checks | Synthesized deployments retain exact catalog version and SKU capacity. Versionless preferences are skipped; capabilities without a versioned alternative become `hil-only` and emit no route. |
| Private Foundry partner module | implemented | `infra/modules/llm/foundry-partner/`; Terraform module test (`1 passed`) | The isolated module owns a private AIServices account/project, allowlisted partner formats, pinned model versions, bounded capacity, and Azure AI User project roles. Root composition remains disabled. |
| Publisher-split root composition | implemented | `infra/main.tf`; `infra/variables.tf`; root Terraform checks (`5 passed`) | Root filters OpenAI and partner capabilities into separate modules and creates a services.ai private endpoint/DNS path only for resolved partner capabilities. Registry activation and runtime endpoint binding remain open. |
| Foundry endpoint binding and delivery contract | implemented | `rule_catalog/schema/model_endpoint.py`; `runtime/configuration.py`; platform Terraform output; protected Core materializer and guard; semantic and post-turn consumers; focused checks | Direct Foundry bindings require `openai-v1`, Entra, and TPM. Terraform supplies exact account-bound origins, and runtime consumers resolve only validated openai.azure.com or services.ai.azure.com entries. |
| Sealed partner endpoint bindings | implemented | `seal_model_endpoint_bindings.py`; `deploy-dev.yml`; focused checks (`6 passed`) | Deployment injects deterministic direct Foundry bindings after resolution and before manifest/digest sealing. Commit time supplies replay-stable discovery time. |
### Implementation history
| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-08-28 | implemented | Added backward-compatible publisher-qualified catalog discovery and stable partner-version lookup for Azure OpenAI and AIServices models. | `current change`; focused resolver checks (`46 passed`); Ruff and strict mypy. | Add publisher-aware Terraform accounts, deployment formats, private endpoints, and runtime endpoint bindings before enabling a partner registry preference. |
| 2026-08-28 | implemented | Delivered Terraform-owned OpenAI and conditional Foundry endpoint origins to measurement jobs and independent Core. Protected materialization, plan guards, semantic planning, and post-turn review resolve exact bindings and reject missing, malformed, account-mismatched, or unbound partner routes. | `current change`; focused service materializer, guard, runtime-consumer, and Terraform root/service checks. | Activate the reviewed partner family, then retain protected plan, apply, readback, and runtime evidence. |
| 2026-08-28 | implemented | Activated the reviewed Mistral secondary profile, made Azure TPM quota lookup SKU-qualified, and added a staging ChatOps gate that seals plan evidence and revalidates the exact Foundry binding before plan and apply. | `current change`; focused registry, Azure catalog/quota, capability-gate, plan-metadata, request-validation, sealer, and workflow checks. | Retain protected plan, apply, model readback, independent runtime binding, and channel evidence. |
| 2026-08-28 | implemented | Propagated catalog stable versions and SKU-qualified capacity into base and synthesized narrator, web-search, and primary-pool deployments after a live staging plan rejected an unversioned Terraform capability. Missing or malformed live stable versions now stop in the resolver. | Failed plan-only run `33115864100`; `current change`; focused resolver, endpoint-selection, CLI, narrator, primary-pool, and Azure query checks. | Rerun the exact staging plan and retain the sealed model artifact. |
| 2026-08-28 | implemented | Corrected the preceding global-stop behavior after live planning found an optional web-search family without a GA version. Versionless preferences are now skipped; capabilities without a versioned alternative become `hil-only` and emit no synthesized route. | Failed plan-only run `33118464379`; `current change`; focused resolver, endpoint-selection, CLI, narrator, primary-pool, and Azure query checks (`98 passed`). | Rerun the exact staging plan and retain the sealed model artifact. |
| 2026-08-14 | in-progress | Adopted the implementation ledger without reconstructing earlier provenance and aligned the quality-gate status with current resolver, rubric, escalation, and latency-routing code. | `current change`; registry, quality-gate, Azure adapter, composition, and measurement paths listed above. | Retain operational model evidence and implement the governed reconciler flow. |
| 2026-08-19 | implemented | Bound live model resolution to protected planning, sealed the exact full and deployment manifests into plan metadata, restored the same JSON and SHA for apply, and added a proposal-only weekly lifecycle reconciler. | `current change`; focused model lifecycle, plan verifier, Operator narrator, Terraform, and privileged-workflow checks. | Retain one governed reconciler run and separately review any draft replacement before registry or deployment change. |
| 2026-08-21 | implemented | Versioned lifecycle proposals to include SKU and effective capacity after an existing `GlobalStandard` 1K TPM embedding deployment and the reviewed `Standard` 200K TPM candidate were incorrectly classified as no change. The protected plan permits only that exact address, family, account binding, source SKU/capacity, target SKU/capacity, and replacement action. | `current change`; `model_lifecycle_reconciler.py`; `deploy-dev.yml`; focused lifecycle checks passed 5 cases and destructive-plan checks passed 2 cases. | Apply only the exact protected plan, verify the replacement and runtime binding, retain the apply receipt, and remove the bounded migration approval after convergence. |
| 2026-08-23 | implemented | Connected candidate-only semantic judgment to read-only semantic planning, added up to three same-binding schema-repair attempts before optional T2 escalation, and enforced action-posture and action-subject alignment. | `current change`; `semantic_judgment.py`; `semantic_planning.py`; `wire_semantic_query.py`; focused semantic judgment, adapter, tier-routing, and composition checks. | Add schema-repair attempt, recovery, escalation, and planning-disposition measures to the retained live-shadow cohort. |
| 2026-08-23 | implemented | Recorded the shipped T2 proposer recovery contract in this owner document. Bounded attempts persist sanitized receipts before Huginn ingress; terminal exhaustion is reduced by Heimdall and judged by Forseti; approved route changes use Thor, append-only audit, and correlation-fenced Vidar rollback. A recovered attempt remains an observation and does not open another approval. | Commits `68f0d4014` and `e96416ce1`; `recovery.py`; `t2_recovery.py`; `t2_route_registry.py`; `test_{t2_recovery,t2_route_registry}.py`; `test_t2_recovery_chain.py`. | Retain one exact-revision governed campaign proving restart recovery, exhaustion-to-approval, route switch, failed verification rollback, stale rollback rejection, and recovery without a new approval. |
| 2026-08-23 | in-progress | Corrected the lifecycle scope after source and workflow review found that proposal expiry does not lower an affected capability to human review. The scheduled workflow remains proposal-only and has no retained run. | `current change`; `.github/workflows/model-lifecycle-reconcile.yml`; `scripts/deployment/azure/model_lifecycle_reconciler.py`; focused lifecycle contract tests. | Implement the expiry-to-hold path against the authoritative runtime model source, then retain one protected scheduled run and separately review any draft replacement. |
| 2026-08-23 | implemented | Added the locally executable expiry-review slice. Lifecycle proposal v3 records the canonical source-model digest and affected capabilities. A pure evaluator holds only an expired, unmerged proposal for that exact source and rejects late merge evidence. The Operator-owned async Key Vault source validates official Azure vault origins and audiences, exact secret identity, size, JSON depth, enabled and expiration state, total deadline, and secret-safe errors and representation. | `current change`; focused lifecycle and Key Vault tests; 15 critique-and-harden rounds ended with no verified Medium-or-higher defect. | Bind asynchronous source loading and trusted PR lifecycle observations into startup, persist and verify decisions, then apply holds before capability binding without changing the model mapping. |
| 2026-08-24 | implemented | Added revisioned environment binding drafts for every T1/T2 capability, complete-candidate TPM/PTU fallback, exact GA version sealing, Owner-only assessment and plan requests, active-artifact digest fencing, and Terraform version pinning. | `current change`; shared policy, resolver, Azure query, Operator IAM, Console Models, protected workflow, and Terraform paths; focused checks recorded in the completion report. | Retain one protected PTU plan/apply/rollback receipt and one post-apply independent binding verification before classifying the path as validated. |
| 2026-08-24 | implemented | Added a model-binding-only protected deployment mode after live assessment showed that an intentionally unavailable secondary publisher made the general completeness gate stop before Terraform. Bounded `plan-model-*` and `apply-model-*` request ids require an environment policy and protected request, target only the Azure OpenAI module, accept only sealed cognitive deployment changes, and verify replacement version, SKU, and capacity against the resolved artifact. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); `deploy-dev.yml`; `test_model_resolution_lifecycle.py`; focused protected workflow checks passed 60 cases, YAML parsing passed, and Ruff passed. | Run the exact PTU plan, apply, independent runtime verification, and reverse-plan rollback before classifying the path as validated. |
| 2026-08-24 | implemented | Bound the protected operation kind and model policy environment, revision, digest, and active-artifact fence into immutable plan metadata. Exact apply rejects a different operation kind or environment, duplicate resolved capabilities fail replay, and an independent management-plane readback compares deployed family, version, SKU, capacity, and provisioning state before writing a sanitized receipt. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); `verify-deployment-plan.py`; `verify_model_deployments.py`; protected workflow and resolver checks passed 107 cases; Ruff, strict mypy, and YAML parsing passed; ten critique rounds left no verified unresolved finding above Low. | Retain the exact PTU plan/apply and reverse-plan rollback receipts, then verify Core and Operator consume the sealed runtime digest. |
| 2026-08-24 | implemented | Restored the model-only scope check as an executable workflow step after YAML indentation had embedded it inside the monitoring script, and allowed a deprecated source family to move only to the exact sealed GA family, version, SKU, and PTU capacity. Source model, SKU, capacity, deployment name, and account binding remain validated. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); executable workflow-step and synthetic replacement checks; 45 focused model checks and 17 embedded Python blocks passed. | Run and retain the exact protected PTU plan, apply, independent readback, runtime binding, and reverse-plan rollback receipts. |
| 2026-08-24 | implemented | Added a bounded Azure CLI resolver deadline after the first protected PTU plan stopped before Terraform when the Model Capacities request exceeded the fixed 30-second adapter timeout. The CLI accepts 5-300 seconds, shares one deadline across catalog, permission, quota, and PTU queries, and the protected workflow selects 90 seconds. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); failed plan `32749593774`; focused resolver and workflow checks passed 62 cases; strict mypy and 17 embedded Python checks passed. | Rerun the protected plan with the new deadline; do not retry again without new evidence if the provider still exceeds it. |
| 2026-08-25 | implemented | Replaced the mutable Terraform-input CAS with an exact healthy active Core revision, digest-pinned image, verified resolved-model attestation, and runtime model digest. A separate Core-only service transition binds the attested artifact before model planning; exact apply re-observes the same revision, image, and model digest. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); active-runtime and attestation verifiers; protected service guard and plan bundle; 249 combined model/service checks passed. | Apply the Core binding transition, then retain the PTU plan/apply/readback and reverse-plan rollback receipts. |
| 2026-08-25 | implemented | Replaced mutable repository model-policy input with a protected-runner pull of one exact Operator plan proposal. The read-only join verifies proposal, request, policy, environment, revision, active-artifact, and authority fences before model resolution. | `current change`; `model_binding_proposal.py`; `deploy-dev.yml`; focused proposal, request, lifecycle, and workflow checks. | Retain one governed proposal-to-plan receipt, then complete exact apply, independent readback, and rollback evidence. |
| 2026-08-25 | implemented | Added a protected producer for the missing deployed model Settings projection. It writes only the model projection after repository artifact, active Core runtime, and signed image-attestation digests agree, then reads back environment and digest. | `current change`; `model-settings-projection.yml`; `materialize-authoritative-settings.py`; focused projection and workflow checks. | Retain one governed projection receipt before submitting the Owner draft and plan proposal. |
| 2026-08-25 | implemented | Restored the protected model Settings producer's executable shell after its Key Vault URI heredoc retained YAML indentation. The workflow now validates the same official vault origin with an anchored Bash expression before reading the migration secret. | `current change`; `model-settings-projection.yml`; focused workflow and privileged CI contracts passed 45 cases; `actionlint` passed. | Retain one governed projection receipt before submitting the Owner draft and plan proposal. |
| 2026-08-31 | implemented | Added one Core-owned PostgreSQL lifecycle for exact T2 cache catalog partitions. Promotion and rollback use exact digests and stable keys; TTL reads use the indexed contract; rotation protects authoritative active and rollback versions and commits DDL plus its receipt atomically. | `current change`; 13 focused local PostgreSQL, migration, and CLI checks passed, including duplicate, partial failure, concurrency, and 250 ms statement-timeout hit/miss coverage. | Retain governed operational query-latency and scheduled-rotation receipts before claiming validation. |
| 2026-08-29 | in-progress | Assigned the remaining all-capability live-shadow cohort and optional rubric promotion evidence to the Phase 2 outcome issue without changing model, quality-gate, or promotion authority. | `current change`; [Issue #343](https://github.com/dotnetpower/fdai/issues/343); P0 baseline [Issue #76](https://github.com/dotnetpower/fdai/issues/76); current strategy scope above. | Retain the governed cohort and independently reviewed exact promotion receipt before raising strategy validation. |
| 2026-08-28 | implemented | Added publisher-qualified Azure OpenAI/AIServices catalog and stable-version discovery without activating partner deployment. | `current change`; focused resolver checks (`46 passed`); Ruff and strict mypy. | Implement partner account, deployment-format, private endpoint, and runtime endpoint routing before changing registry preferences. |
| 2026-08-28 | implemented | Added an isolated private Foundry partner account/project/deployment module without activating it in the root. | `current change`; Terraform module test (`1 passed`); format and validation checks. | Add root split, private endpoint/DNS, endpoint bindings, then activate the reviewed registry family. |
| 2026-08-28 | implemented | Composed publisher-split OpenAI/partner modules and the conditional private services.ai endpoint in the root without changing the registry. | `current change`; root Terraform checks (`5 passed`); module test (`1 passed`). | Add endpoint-specific runtime bindings, then activate and deploy the reviewed partner family. |
| 2026-08-28 | implemented | Added the Azure Foundry endpoint provider contract and strict optional multi-endpoint runtime resolver. | `current change`; focused contract/runtime checks (`22 passed`); Ruff and strict mypy. | Generate sealed partner endpoint bindings before registry activation. |
| 2026-08-28 | implemented | Added deterministic partner binding generation before protected model manifest hashing. | `current change`; focused sealer/workflow checks (`6 passed`); Ruff and strict mypy. | Deliver the endpoint map to runtime, then activate the reviewed partner family. |
| 2026-08-28 | implemented | Delivered the Terraform-owned OpenAI and conditional Foundry endpoint map through measurement jobs and protected independent Core materialization, then bound semantic planning and post-turn review to exact per-capability routes. | `current change`; focused materializer, guard, runtime-consumer, root Terraform, and service-root checks. | Activate `Mistral-Large-3`, then retain protected deployment and independent runtime readback evidence. |
| 2026-08-28 | implemented | Recorded the exact committed endpoint-delivery verification totals. | Commit `bad7eee92`; focused Python checks (`613 passed`), root and service Terraform checks (`10 passed`), Terraform validation, Ruff, and strict mypy. | Activate `Mistral-Large-3`, then retain protected deployment and independent runtime readback evidence. |
| 2026-08-28 | implemented | Activated the reviewed Mistral secondary profile, qualified Azure quota by SKU, and fenced staging ChatOps validation on plan/apply revalidation of its exact sealed Foundry binding. | `current change`; focused registry, resolver, Azure quota, capability-gate, plan-verifier, request, sealer, and workflow checks. | Retain protected deployment, independent runtime binding, and live channel evidence. |
| 2026-08-28 | implemented | Added stable-version and SKU-capacity propagation to base and synthesized model deployments after the live Terraform input rejected a missing version; unavailable or malformed stable versions now fail in the resolver. | Failed plan-only run `33115864100`; `current change`; focused resolver and endpoint-selection checks. | Rerun staging planning and retain its sealed model artifact. |
| 2026-08-28 | implemented | Corrected optional version failure to skip the preference and hold only capabilities without a versioned alternative, with no synthesized route. | Failed plan-only run `33118464379`; `current change`; focused resolver and endpoint-selection checks (`98 passed`). | Rerun staging planning and retain its sealed model artifact. |
| 2026-08-24 | in-progress | Split model capability lifecycle and Rule-to-Decision lookup into dedicated owner documents while preserving this ledger's prior transitions append-only. | `current change`; `model-capability-lifecycle.md`, `rule-to-decision-lookup.md`, and their mirrored implementation ledgers. | Retain the strategy-level live evidence and promotion exits below. |

### Remaining work
- [ ] Retain a pinned live-shadow cohort for every enabled T1/T2 capability with model identity, cost, latency, schema-repair attempts and recovery, escalation, planning disposition, disagreement, grounding, verifier, rubric, outcome, and guard evidence after the live KPI prerequisites in [Goals and Metrics](../../roadmap/architecture/goals-and-metrics.md#remaining-work) and the [Agent Pantheon implementation plan](../../roadmap/agents/agent-pantheon-implementation.md#remaining-work) are satisfied.
- [ ] Retain a governed T2 recovery campaign proving bounded attempt budgets, durable receipt forwarding after restart, terminal exhaustion to human approval, an audited route switch, correlation-fenced rollback, and recovery without a new approval.
- [ ] Bind the implemented expired-unmerged evaluator and direct Key Vault source adapter through an asynchronous startup owner. Add trusted PR lifecycle observation, proposal and decision digest verification, persistence, and pre-binding capability holds. Prove the affected capability moves to human review without changing its model mapping, then retain a governed scheduled-run receipt showing deprecation or family drift creates only a sanitized draft PR. The startup/source contract is owned by [Narrator Routing and Latency](../../roadmap/interfaces/narrator-routing-and-latency.md#remaining-work).
- [ ] Promote optional rubric, escalation invocation, or primary-pool behavior only through the [authoritative ActionType registry](../../roadmap/decisioning/action-ontology.md#33-governance) after frozen replay and independent review; keep missing bindings fail-closed.
- [ ] Retain one protected environment policy campaign that assesses a provisioned SKU, seals the exact model version and PTU capacity, applies the approved plan, independently verifies the runtime binding, and rehearses rollback without a Console or Operator identity receiving provider mutation authority.
- [ ] Under [Issue #343](https://github.com/dotnetpower/fdai/issues/343) and P0 baseline [Issue #76](https://github.com/dotnetpower/fdai/issues/76), retain a pinned live-shadow cohort for every enabled T1/T2 capability with model identity, cost, latency, schema-repair attempts and recovery, escalation, planning disposition, disagreement, grounding, verifier, rubric, outcome, and guard evidence after the live KPI prerequisites and Agent Pantheon implementation prerequisites are satisfied.

- [ ] Under [Issue #343](https://github.com/dotnetpower/fdai/issues/343), promote optional rubric behavior only through the [authoritative ActionType registry](../../roadmap/decisioning/action-ontology.md#33-governance) after frozen replay and independent review; keep missing bindings fail-closed.
