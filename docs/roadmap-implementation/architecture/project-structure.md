# Project Structure implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope
| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Generated service-contract language views | implemented | `packages/service-contracts/contract-generation.json`; `scripts/quality/contracts/generate_service_contracts.py`; generated Python and TypeScript artifacts; focused generation and compatibility checks | One checksum-pinned repository generator projects every manifest-owned N/N-1 JSON Schema into backend and Console development types. JSON Schema remains the runtime wire authority, and generated views grant no authority. |
| Current-state activity identity | implemented | `read_investigation_latency.py`; `activity_projection.py`; focused persistence and projection tests (`6 passed`) | The latency profile retains only the hashed correlation reference, the audit entry remains correlation-free, and durable and live activity share one identity without carrying execution authority. |
| Rule generation reconciliation boundary | implemented | `shared/providers/catalog_search.py`; `delivery/catalog_search/`; `runtime/rule_generation_documents.py`; focused adapter, Pantheon, activation, and bootstrap checks | The provider contract owns exact staged receipt binding, delivery owns atomic adapters, and runtime composes strict catalog snapshots plus replay-identical requests without moving policy or execution authority into infrastructure code. |
| Incident ontology projection and evidence query | implemented | `core/incident/ontology_projection.py`; `core/ontology_platform/incident_queries.py`; focused Incident and semantic composition checks | Startup replays the append-only Incident audit into `Incident` objects. The exact-release `query.incident_evidence` function reads correlation-scoped audit records and exposes the profile, activity, recorded grounded root cause or allowlisted deterministic terminal failure, impact evidence, citations, and explicit gaps without action authority. |
| Principal manifest query boundary | implemented | `core/ontology_platform/manifest_queries.py`; `composition/wire_semantic_query.py`; focused manifest and semantic composition checks (`42 passed`) | The exact-release `query.manifest` function exposes bounded role- and purpose-readable declaration summaries through existing query receipts. It has no provider, mutation, approval, or execution path. |
| Permission-aware observation boundary | implemented | `fdai_service_contracts/operational_activity.py`; `delivery/observation_campaign.py`; `delivery/observation_source_catalog.py`; focused contract, lifecycle, and projection checks | Shared contracts carry authority-free summaries, while delivery owns provider reads, atomic campaign state, and local or deployed adapter selection. Source-native paths retain semantic evidence ownership. |
| Resource-discovery contract boundary | implemented | `fdai_service_contracts/discovery.py`; `fdai_service_contracts/discovery_evidence.py`; `core/discovery/router.py`; focused discovery checks (`44 passed`) | The shared SDK owns immutable no-authority wire records, Core owns provider-neutral exact-equivalent routing and merge, and Azure delivery owns version-pinned profiles, rendering, execution receipts, and coverage reconciliation. |
| Inventory identity-completeness boundary | implemented | `shared/providers/inventory.py`; `delivery/azure/{arg_query,inventory}.py`; `composition/wire_inventory.py`; focused checks (`259 passed`) | Shared owns the exact coverage receipt, Azure delivery owns bounded native identity reads, and composition binds both without moving provider I/O into Core. |
| Decision-evidence verifier failure boundary | implemented | `core/readiness/decision_evidence.py`; `tests/core/readiness/test_decision_evidence.py`; focused checks (`10 passed`) | Expected verifier, validation, timeout, and transport failures produce bounded rejection evidence. Task cancellation propagates and cannot be mistaken for a completed verification result. |
| T2 deterministic evidence binding | implemented | `core/quality_gate/deterministic_evidence.py`; `composition/_helpers.py`; `runtime/control_loop.py`; focused quality-gate and runtime binding checks (`71 passed`) | Container exposes a provider-neutral paired seam. The shipped runtime binds explicit unavailable what-if and security authorities, rejects partial binding, and holds stale, conflicting, mismatched, future, or synthetic evidence before any model cross-check or risk evaluation. |
| Assurance Twin semantic compiler binding | implemented | `core/assurance_twin/semantic_query.py`; `composition/_helpers.py`; `runtime/assurance_twin_query.py`; focused checks (`50 passed`) | Container exposes provider-neutral compiler and discovery seams. Runtime defaults to explicit unavailable, strict verification binds input/evidence/limits, and the discovery record carries no raw question or authority. |
| Change Safety signal and pre-authority evidence binding | implemented | `core/verticals/change_safety/detector.py`; `core/control_loop/change_safety_evidence.py`; `_process.py`; `core/executor/action_builder.py`; `composition/_helpers.py`; `runtime/control_loop.py`; focused checks (`193 passed`) | Container exposes separate provider-neutral detector and evidence seams. Activity Log is the sole supported Phase 1 signal, unsupported declared kinds are audited, findings remain visible, and only current evidence can enter action authority. |
| Question-bound semantic filter grounding | implemented | `core/conversation/semantic_planning_value_filters.py`; `tests/conversation/test_semantic_planning.py`; focused planning checks (`79 passed`) | Core can only narrow a candidate ObjectSet with catalog value groups stated in the utterance or one exact free-text subject preserved by the verified frame and present verbatim in that utterance. It adds no provider I/O, decision, approval, mutation, or execution authority. |
| Semantic frame and plan alignment boundaries | implemented | `core/conversation/semantic_planning_frame.py`; `core/conversation/semantic_planning_alignment.py`; focused planner checks (`90 passed`) | Focused Core modules bind candidate frames to exact input and investigation identities, resolve only server-owned clarification context, and verify that a plan preserves its accepted frame and exact capability family. They perform no provider I/O and grant no decision, approval, mutation, or execution authority. |
| Rule governance override boundary | implemented | `rule_catalog/schema/{override,exemption_lifecycle,parameter_relaxation_policy}.py`; `core/control_loop/`; `delivery/exemption_lifecycle.py`; `shared/providers/exemption_lifecycle.py`; `rule-catalog/action-types/governance.reapply-rule-assignment.yaml`; focused governance checks (`106 passed`) | Catalog validation, exact-scope resolution, expiry evidence, digest delivery, and exact-assignment reapply proposals are implemented without granting execution authority. Missing and unknown outcomes hold, and broker acceptance remains non-terminal. |
| Publisher-qualified model catalog boundary | implemented | `rule_catalog/schema/llm_resolver.py`; `delivery/azure/llm/resolver_queries.py`; focused checks (`46 passed`) | Core adds an optional publisher-family seam without breaking family-only adapters. Azure owns allowlisted OpenAI/AIServices format mapping and stable-version discovery; no deployment authority moves into Core. |
| Private Foundry partner module boundary | implemented | `infra/modules/llm/foundry-partner/`; Terraform module test (`1 passed`) | The isolated module owns private AIServices account/project/deployments and project-user roles. It is not composed into the root until publisher-aware endpoint routing is complete. |
| Publisher-split Foundry root boundary | implemented | Registry, root Terraform, exact capability gate, and focused checks | Root keeps OpenAI and partner deployments in separate accounts. Staging ChatOps validation requires the reviewed Mistral secondary and exact Foundry binding without moving provider authority into Core. |
| Foundry runtime endpoint boundary | implemented | `rule_catalog/schema/model_endpoint.py`; `runtime/configuration.py`; platform Terraform output; protected Core materializer and guard; semantic and post-turn consumers; focused checks | The provider contract distinguishes direct Azure OpenAI and Foundry API styles. Infrastructure owns exact origins, protected delivery preserves the boundary, and runtime consumers reject account, provider, or unbound-partner mismatch. |
| Sealed partner endpoint binding boundary | implemented | `seal_model_endpoint_bindings.py`; `deploy-dev.yml`; focused checks (`6 passed`) | Deployment generates deterministic partner bindings after resolution and before protected manifest hashing; no provider mutation moves into Core. |
| Exact-target current-state clarification | implemented | `core/conversation/semantic_current_state_planning.py`; `tests/conversation/test_semantic_current_state_planning.py`; focused current-state checks (`5 passed`) | Core clears only a stale resource-identity clarification when the same utterance contains one deterministically verified runtime identifier. Other unresolved concepts and zero or multiple identifiers remain typed clarifications, with no provider or execution authority. |
| Exact-target metric-series boundary | validated | `core/conversation/semantic_resource_metric_planning.py`; `core/ontology_platform/resource_metric_queries.py`; `composition/wire_semantic_query.py`; Operator `presentation_artifact_v2.py`; focused checks; authenticated standard-port Console receipt | Core selected the server-owned series plan, ontology platform returned complete 20/20 bounded rows, composition reached the existing metric provider, and Operator rendered the verified time-series artifact at three viewport sizes. No service imported another service implementation, and the browser received no provider or execution authority. |
| Evaluation and semantic-regression boundary | in-progress | `evaluation-sdk/`; `benchmarks/`; `eval/golden-dataset/`; `tests/integration/evaluation/test_golden_dataset.py` (`7 passed`) | The external host integration is dormant and absent from Core. The retained packages remain independent workspace members. The generated corpus contains 280 English/Korean pairs over 35 expectations, eight wording styles, seven balanced perspectives, and every reviewed assurance axis; campaign execution against answer results remains open. |
| Conversation assurance context-and-locale measurement boundary | implemented | `core/conversation_assurance/quality_context_locale_observations.py`; focused qualification, Docker persistence, and Deck isolation checks | Core owns locale-bound contributions for parity, persistence fidelity, personalization accuracy, context isolation, and screen awareness. Persistence and Console surfaces remain evidence producers or projections only; they do not score across principals, infer success from browser text, or grant qualification authority. |
| Context-locale compatibility boundary | implemented | `core/conversation_assurance/context_locale_scorecard.py`; focused compatibility check | The historical module path only re-exports the unified contribution API so append-only evidence links remain resolvable without restoring a parallel contract. |
| ChatOps latency qualification boundary | implemented | `core/conversation_assurance/quality_latency.py`; `scripts/evaluation/chatops_quality_latency.py`; focused Core and CLI checks (`11 passed`) | Core owns the fixed five-stage SLO contract and pure reduction only. Runtime stage owners retain timestamp authority; the repository CLI handles content-free JSON and cannot claim trace completeness. |
| Stage-owner latency receipt boundary | implemented | `core/conversation_assurance/quality_latency.py`; focused Core checks (`8 passed`) | Stage owners retain monotonic clock authority. Core derives duration only from typed start/completion values after enforcing the contract-owned environment. |
| Deterministic verification timing producer | implemented | `core/conversation_assurance/service.py`; focused checks (`24 passed`) | Composition must inject both the PR benchmark environment and sink. Default runtime composition emits no receipt and performs no timing work. |
| ChatOps complete-trace qualification boundary | implemented | `core/conversation_assurance/quality_trace.py`; `scripts/evaluation/chatops_quality_trace.py`; focused Core and CLI checks (`8 passed`) | Core verifies only content-free record commitments for the exact session-to-audit chain. Runtime owners retain record and timestamp authority, and the repository CLI cannot read provider payloads. |
| ChatOps timing evidence binding | implemented | `core/conversation_assurance/quality_timing.py`; focused binding checks (`4 passed`) | Core derives timing booleans only after installed contract, source revision, trace count, and trace-set commitments match across the latency artifact and complete trace cohort. |
| Governed rule-discovery startup boundary | implemented | `core/readiness/discovery_activation.py`; `runtime/discovery_activation.py`; `runtime/bootstrap*.py`; `agents/norns.py`; focused activation and wiring checks | Core reduces current prerequisite evidence, runtime injects the default-off decision only at Norns' inert candidate publication boundary, and supervised refresh failure closes the local gate without changing catalog or promotion authority. |
| Local telemetry write boundary | implemented | `shared/telemetry/logging.py`; `capture-local-service-log.py`; focused telemetry, launcher, provider-integration, and framework-layout checks | Shared telemetry owns bounded structured warning retention and dependency summaries. The local launcher owns terminal presentation and complete rotating file capture; neither path changes agent delivery or authority. |
| Dashboard availability projection | implemented | `shared/telemetry/dashboard_status.py`; `docs/dashboards/phase-0-kpi.json`; focused dashboard status and descriptor checks | The pure projection accepts normalized observations only after provider and domain reduction. It renders missing, stale, conflicting, producer-mismatched, future-dated, or synthetic live values as unavailable and grants no metric or execution authority. |
| Pre-dispatch kinetic safety boundary | implemented | `core/operational_planning/kinetic_safety.py`; `delivery/kinetic_safety.py`; `delivery/kinetic_proposal.py`; `runtime/control_loop.py`; focused dispatch, HIL, artifact, proposal, and runtime checks (`119 passed`) | Core declares the provider-neutral ordering seam. Delivery revalidates the durable OperationalPlan and proposal lineage, joins the existing correlation-indexed exact V2 proposal to the existing typed Action, and runtime shares one writer across ControlLoop and HIL resume before every Thor executor. Missing proposals preserve legacy behavior; invalid evidence blocks dispatch without raising authority. |
| ChatOps qualification observation and reduction boundary | implemented | `core/conversation_assurance/quality_{observation_models,observations,qualification,*_observations}.py`; `scripts/evaluation/chatops-quality-qualification.py`; focused qualification checks | Core uses one content-free turn envelope, maps only authoritative measurements, accepts conflict-free case- and locale-bound owner contributions for applicable items 1-35 and 41-45, and independently derives hard caps. Repository CLI owns JSON I/O. |
| Named notification composition boundary | implemented | `core/notifications/`; `delivery/notifications/`; `runtime/delivery.py`; focused notification checks (`162 passed`) | Runtime resolves deployment-owned binding references and injects provider-neutral adapters plus durable stores. Core owns target selection and delivery state without reading endpoints, credentials, or provider SDKs. |
| Trajectory retention claim boundary | implemented | `shared/providers/trajectory.py`; `core/trajectory/datasets.py`; `delivery/persistence/postgres_trajectory.py`; trajectory migrations and focused retention checks | The shared seam defines `completed -> deleting -> deleted` and idempotent artifact deletion. Core coordinates the claim without storage authority, while delivery owns PostgreSQL CAS, legal-hold exclusion, restart recovery, and tombstone persistence. |
| Background-task read and completion boundary | in-progress | `core/background_task/`; `delivery/persistence/background_task_completion_audit.py`; Operator `families/conversation/background_tasks.py`; Operator read migration; Console background-task route; focused Core, Operator, migration, and Console checks | Core owns detached-task state and completion ordering, delivery owns atomic audit markers, Operator reads owner-filtered projections with `SELECT` only, and Console exposes no mutation controls. The production proposal consumer, executor, coordinator, and sink binding remain open. |
| Core domain-facade navigation | implemented | [Issue #15](https://github.com/dotnetpower/fdai/issues/15); `core/{pipeline,incident,operator,knowledge,platform,verticals}/`; `tests/core/test_domain_groups_layout.py` | The facade-only design is final. Five domain facades provide navigation while direct subsystem imports, coverage paths, and fan-out semantics remain stable; `verticals` stays separate. |

### Implementation history
| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-08-31 | implemented | Added the strict Assurance Twin compiler and inert discovery DI boundary with explicit unavailable defaults. | `current change`; project structure owner and 50 focused query/runtime checks. | Bind governed providers and retain authenticated runtime evidence. |
| 2026-08-31 | implemented | Exposed the Activity Log detector through Container and made unsupported declared signal kinds explicit and audited. Inventory freshness remains an action-authority gate after finding formation. | `current change`; project structure owner and 193 focused Change Safety/control-loop/risk checks. | Add future signal adapters only through reviewed completeness and freshness contracts. |
| 2026-08-31 | implemented | Added the read-only Change Safety evidence join and removed the graph-derived blast-count fallback that treated an authored cap as an observation. | `current change`; project structure owner and 174 focused evidence, ActionBuilder, risk, and control-loop checks. | Bind authoritative drift and what-if producers in deployment; post-action verification remains independent. |
| 2026-08-31 | implemented | Added the paired T2 deterministic-evidence DI seam and fail-closed runtime default. Typed what-if and security evidence can only preserve eligibility when its authority, candidate lineage, freshness, conflict, provenance, and synthetic checks pass. | `current change`; project structure owner and 71 focused quality-gate/T2/runtime checks. | Bind governed live producer implementations before recording promotion evidence. |
| 2026-08-31 | implemented | Completed the provider-neutral exemption expiry boundary by publishing an exact-revision assignment reapply proposal through EventBus and retaining human approval, Thor execution, rollback, audit, and effect-verification boundaries. | `current change`; project structure owner and 106 focused lifecycle, notifier, ActionType, catalog, and scanner checks. | Bind the scheduler and notifier in a deployment before recording live evidence. |
| 2026-08-31 | implemented | Added the authority-free dashboard availability projection to the shared telemetry boundary. It preserves provider-neutral observation production and refuses success-shaped numeric fallback for unavailable live evidence. | `current change`; project structure owner, Phase 0 dashboard descriptor, and focused dashboard status/descriptor tests. | Bind and validate each later-phase authoritative producer before its panel can become available. |
| 2026-08-31 | implemented | Superseded the 1,063-file physical-move proposal with the permanent facade-only design. Updated all facade anchors and the layout guard so a future edit cannot silently restore the obsolete Phase 2 assumption. | `current change`; five facade packages, 98 focused domain-layout tests, Core import boundary, subsystem fan-out gate, Ruff, and strict mypy. | No physical move remains in Issue #15. Any future domain-bounded move requires a separate reviewed design. |
| 2026-08-31 | implemented | Added checksum-pinned, deterministic Python and TypeScript views of every compatibility-manifest N/N-1 service schema while keeping JSON Schema as the sole runtime wire authority. | `current change`; generation policy and script, generated artifacts, 51 focused generation and compatibility tests, Ruff, strict mypy, and Console typecheck. | Regenerate through the pinned command whenever a manifest-owned schema changes. |
| 2026-08-29 | implemented | Hardened the decision-evidence readiness boundary so expected provider and transport failures remain fail-closed while task cancellation propagates. | `current change`; `uv run pytest -q --no-cov services/core-control-plane/tests/core/readiness/test_decision_evidence.py` passed 10 cases; Ruff passed. | No remaining implementation work for this bounded failure-handling change. |
| 2026-08-29 | in-progress | Added a bounded catalog-to-control-loop boundary for scoped overrides, parameter relaxation, and exemption expiry evidence. | `current change`; governance schema, control-loop, delivery, and focused test paths. | Record the complete focused governance test result before raising the implementation-scope state. |
| 2026-08-29 | deferred | Reconciled the final open Project Structure item with repository and issue state. The Core tree still contains flat subsystem packages alongside the domain facades, so the previously completed Issue #15 did not satisfy its unchecked physical-move criteria and was reopened without changing runtime imports. | `current change`; current Core package layout; `uv run pytest -q --no-cov services/core-control-plane/tests/core/test_domain_groups_layout.py` passed 98 cases; reopened [Issue #15](https://github.com/dotnetpower/fdai/issues/15). | Complete the compatibility deprecation and physical move, or approve a superseding facade-only design with updated exit criteria. |
| 2026-08-28 | implemented | Added a backward-compatible publisher-qualified model catalog boundary while keeping Azure CLI I/O and partner-format mapping in delivery. | `current change`; focused resolver checks (`46 passed`); Core imports, Ruff, strict mypy, roadmap, and translation gates. | Add partner deployment and endpoint-routing infrastructure before registry activation. |
| 2026-08-28 | implemented | Added an isolated private Foundry partner module without changing root deployment behavior. | `current change`; Terraform module test (`1 passed`); format and validation checks. | Compose the module with private endpoint/DNS and endpoint-specific runtime binding. |
| 2026-08-28 | implemented | Added conditional publisher-split root composition and private partner networking without changing registry activation. | `current change`; root Terraform checks (`5 passed`); module test (`1 passed`). | Add endpoint-specific runtime binding before activation. |
| 2026-08-28 | implemented | Added strict Foundry provider/API-style validation and bounded multi-endpoint runtime resolution. | `current change`; focused checks (`22 passed`); Core imports, Ruff, strict mypy, roadmap, and translation gates. | Generate sealed partner bindings before registry activation. |
| 2026-08-28 | implemented | Added replay-stable partner binding generation before protected plan metadata sealing. | `current change`; focused sealer/workflow checks (`6 passed`). | Deliver the endpoint map to runtime before activation. |
| 2026-08-28 | implemented | Kept model endpoint ownership in infrastructure while delivering the bounded OpenAI/Foundry map through the protected independent Core input boundary and resolving semantic and post-turn capability bindings through it. | `current change`; focused materializer, plan-guard, runtime-consumer, root Terraform, and service-root checks. | Activate the reviewed partner family without moving deployment or provider authority into Core. |
| 2026-08-28 | implemented | Recorded the exact committed endpoint-boundary verification totals. | Commit `bad7eee92`; focused Python checks (`613 passed`), root and service Terraform checks (`10 passed`), Terraform validation, Ruff, and strict mypy. | Activate the reviewed partner family without moving deployment or provider authority into Core. |
| 2026-08-28 | implemented | Activated the reviewed partner registry entry, qualified quota by SKU, and sealed ChatOps validation evidence for exact apply-time binding revalidation. | `current change`; focused registry, resolver, quota, capability-gate, plan-verifier, request, sealer, and workflow checks. | Retain protected staging and live provider evidence without changing Core authority. |
| 2026-08-28 | implemented | Restored historical context-locale paths as compatibility-only links to the unified module. | `current change`; focused compatibility and repository link checks. | None. |
| 2026-08-28 | implemented | Added the first explicit stage-owned producer without changing default runtime composition. | `current change`; focused checks (`24 passed`); Core import, Ruff, strict mypy, roadmap, and translation gates. | Connect remaining stage owners and retain controlled evidence. |
| 2026-08-28 | implemented | Added a typed stage-owner timing receipt without moving clock or runtime environment authority into Core. | `current change`; focused Core latency checks (`8 passed`); Core import, Ruff, strict mypy, roadmap, and translation gates. | Wire authoritative owners and retain controlled receipts. |
| 2026-08-28 | implemented | Added exact timing evidence binding without moving runtime measurement or record authority into Core. | `current change`; focused binding checks (`4 passed`); combined latency/trace/timing checks (`23 passed`); Core import, Ruff, strict mypy, roadmap, and translation gates. | Bind runtime producers and retain matching controlled evidence. |
| 2026-08-28 | implemented | Added the exact eight-stage content-free trace reducer without moving record, timestamp, or provider authority into Core. | `current change`; focused Core and CLI checks (`8 passed`); Core import, Ruff, strict mypy, roadmap, and translation gates. | Bind authoritative record producers and retain one complete controlled trace. |
| 2026-08-28 | implemented | Added a pure five-stage latency qualification contract and repository benchmark boundary without moving timestamp ownership into Core or inferring complete trace evidence. | `current change`; focused Core and CLI checks (`11 passed`); Core import, Ruff, strict mypy, roadmap, and translation gates. | Bind authoritative stage producers and retain controlled benchmark plus independently complete trace evidence. |
| 2026-08-28 | implemented | Restored the Issue #297 item 1-35 owner adapters to this active branch and replaced the separate item 41-45 score path with case- and locale-bound contributions to the shared turn envelope. | `current change`; focused qualification checks passed 108 cases; Docker PostgreSQL persistence restart check passed; Core import, Ruff, strict mypy, roadmap, and translation gates. | The governed bilingual corpus and complete production-like qualification runs remain under Issues #299 and #300. |
| 2026-08-28 | implemented | Added the Core-owned context-and-locale score assembly boundary for conversation assurance items 41-45 while keeping persistence and Console surfaces read-only evidence producers. | `current change`; `core/conversation_assurance/context_locale_scorecard.py`; focused scorecard, persistence, answer-plan, lifecycle, and Deck isolation checks plus task-scoped Ruff, strict mypy, translation, and roadmap verification. | Retain one governed bilingual qualification run that consumes these measurements before claiming validation. |
| 2026-08-27 | implemented | Kept ChatOps qualification reduction inside a pure Core module while the repository evaluation CLI owns untrusted JSON parsing and artifact output. The reducer derives hard caps from raw evidence state and carries no provider, approval, policy-promotion, or execution authority. | `current change`; `quality_qualification.py`, `chatops-quality-qualification.py`, and focused unit and CLI tests (`25 passed`). | Connect the external measurement harness and hidden corpus before claiming qualification evidence. |
| 2026-08-27 | in-progress | Added content-free completed-turn observation contracts and the first evidence-owned adapters without adding provider, model, approval, policy-promotion, or execution calls. Missing dimensions remain unavailable and runtime references are hashed. | `current change`; `quality_observation_models.py`, `quality_observations.py`, and focused observation tests (`12 passed`). | Add the remaining owner-specific observation adapters and aggregate them under Issue #297 before claiming full item coverage. |
| 2026-08-27 | in-progress | Added a contribution seam that preserves the owning workstream and rejects missing evidence commitments, cross-case input, duplicate dimensions, and measured-slot overwrite. | `current change`; focused observation and contribution tests (`16 passed`); Ruff and strict mypy. | Add the remaining owner-specific adapters under Issue #297. |
| 2026-08-27 | in-progress | Reused existing safeguard, execution-authorization, and unified-risk records in a content-free action qualification adapter. | `current change`; `quality_action_observations.py`; focused action observation tests (`15 passed`). | Extend the remaining action and authority metrics under Issue #297. |
| 2026-08-27 | in-progress | Extended the same authority-free adapter to terminal HIL, approver/executor identity separation, and audit replay records without adding a decision or execution path. | `current change`; focused action observation tests (`21 passed`). | Add the remaining action metrics and non-functional dimensions under Issue #297. |
| 2026-08-27 | in-progress | Reused existing mitigation, runbook, what-if, and typed Action records for items 21-24 without moving their ownership or execution paths. | `current change`; focused action and proposal observation tests (`24 passed`). | Add non-functional dimensions under Issue #297. |
| 2026-08-27 | in-progress | Reused existing grounded RCA results for triage, timeline, supported-cause, and safe-abstention observations without granting hypothesis authority. | `current change`; focused SRE observation tests (`3 passed`). | Add the remaining SRE observation owners under Issue #297. |
| 2026-08-27 | in-progress | Added grounded RCA candidate-set and deterministic ChangeAssessment comparisons without moving cause or impact ownership. | `current change`; focused SRE and impact observation tests (`6 passed`). | Add non-functional dimensions under Issue #297. |
| 2026-08-27 | in-progress | Reused bounded answer-planning and revisioned assignment records for items 31-35 without adding a routing or effect-application path. | `current change`; focused orchestration observation tests (`3 passed`). | Add non-functional dimensions under Issue #297. |
| 2026-08-28 | in-progress | Reused typed semantic planning outcomes for items 1-5 without adding keyword or lexical intent inference. | `current change`; focused intent observation tests (`3 passed`). | Add non-functional dimensions under Issue #297. |
| 2026-08-28 | in-progress | Reused deterministic AnswerPlan shape and depth for items 7-8 without inspecting answer prose. | `current change`; focused answer observation tests (`3 passed`). | Add remaining answer-quality observations under Issue #297. |
| 2026-08-28 | in-progress | Reused terminal evidence state and assessment references for grounding observations while requiring an explicit security-owner injection result. | `current change`; focused grounding observation tests (`4 passed`). | Add non-functional dimensions under Issue #297. |
| 2026-08-27 | implemented | Preserved the Core, delivery, and runtime composition boundary while adding named multi-channel notification bindings and durable per-channel fan-out. | `current change`; [Multi-channel notification delivery ledger](../interfaces/multi-channel-notification-delivery.md); focused notification checks passed 162 cases; Core import boundary, Ruff, and strict mypy passed. | Capture governed Teams and PostgreSQL runtime receipts before claiming operational validation. |
| 2026-08-26 | implemented | Preserved one exact current-state target through a stale model-authored resource-identity clarification while retaining typed clarification for every other unresolved case. | `current change`; `semantic_current_state_planning.py`; standalone normalization checks passed 2 cases and existing exact-target and missing-target plan checks passed 3 cases. | Retain authenticated exact-source Console evidence after restarting Core; no package, process, identity, persistence, or service-boundary work remains. |
| 2026-08-22 | validated | Validated the exact-target metric-series path across the existing Core, composition, Operator, and Console ownership boundaries. | `current change`; four focused isolation checks, Ruff, and strict mypy passed; authenticated Console evidence recorded `server_target_resource_metric_series`, 20/20 complete rows, no display truncation, and no document or Deck overflow at `1440x900`, `993x641`, or `390x844`. | No package, process, identity, persistence, or service-boundary work remains for this capability. |
| 2026-08-23 | in-progress | Added the background-task completion audit adapter, owner-scoped Operator read projection and least-privilege migration, and bilingual Console inspection route without introducing a cross-service implementation import or task mutation authority. | `current change`; focused Core sink/audit, Operator projection/family, service-migration inventory, and Console decoder checks plus Ruff, strict mypy, Console typecheck, and production build. | Define the versioned proposal consumer and bind the executor, coordinator, completion sink, and audit writer before adding create or cancel controls. |
| 2026-08-22 | implemented | Added an exact-target metric-series path within existing service ownership. Core returns the typed bounded table through the semantic contract, while Operator independently validates sampling metadata and compiles the presentation block. | `current change`; focused metric, planner, composition, prompt, and Operator presentation checks passed 43 cases. | Retain authenticated standard-port Browser evidence; no package, process, identity, persistence, or service boundary changes remain. |
| 2026-08-21 | implemented | Expanded the static semantic corpus from 20 hand-authored pairs to 280 generated bilingual pairs without duplicating ontology or answer oracles. Added balanced seven-perspective coverage, document-driven historical topology, release health, cost, recurrence, unsafe-action, resource-evidence, and agent-authority cases, and deterministic source-to-artifact drift checks. | `current change`; `eval/golden-dataset/{coverage,expectations,questions}*`; `build_golden_dataset.py`; focused dataset checks passed 7 cases. | Bind the expanded corpus to the existing semantic campaign before claiming answer regression coverage. |
| 2026-08-21 | in-progress | Corrected the physical layout after the service extraction removed the evaluation host and compatibility facade. Added a separate bilingual cloud-operations golden dataset with locale-neutral ontology traversal and answer oracles. | `current change`; `eval/golden-dataset/`; `tests/integration/evaluation/test_golden_dataset.py` passed 4 cases; dormant package tests passed 68 cases. | Bind the corpus to the existing semantic question campaign before claiming automated answer regression coverage. |
| 2026-08-20 | implemented | Extracted exact candidate-only frame construction and server-owned clarification resolution from the semantic planner into a focused Core module. The planner retains its private adapter surface while returning below the enforced 800-line ceiling, with no frame-digest, clarification, authority, or I/O behavior change. | `current change`; `semantic_planning.py`; `semantic_planning_frame.py`; 111 focused semantic tests; task-scoped Ruff, formatting, strict mypy, and enforced file-LOC checks passed. | None for the structural ceiling recovery. |
| 2026-08-20 | implemented | Completed deterministic multi-filter preservation at the candidate-plan boundary. A model-selected existence predicate no longer drops another catalog-declared filter, and one exact frame-carried free-text subject becomes a fragment predicate only when the operator wrote it verbatim. | `current change`; [Issue #241](https://github.com/dotnetpower/fdai/issues/241); semantic-planning checks passed 79 cases; Ruff, formatting, and strict mypy passed. | Retain authenticated Console and three-viewport presentation evidence before closing the task. |
| 2026-08-19 | implemented | Kept retained warning writes append-only on the hot path, coordinated 24-hour compaction across processes, bounded lock waits and structured records, and separated complete local file capture from terminal backpressure. Repeated aiokafka and Pantheon observer failures retain distinct first and periodic evidence; observer recovery records the bridge-owned count without changing delivery or authority. | [Issue #220](https://github.com/dotnetpower/fdai/issues/220); focused checks pass 18 telemetry, 16 launcher, 45 provider-integration, and 11 framework-layout cases; Ruff and strict mypy pass; 16 critique rounds leave only Low residual tradeoffs. | Restart the live local processes before treating the implementation as runtime evidence. Deployment and promotion evidence remain unchanged. |
| 2026-08-19 | implemented | Extended the provider-neutral Inventory seam with an exact 0-or-all identity-completeness receipt and kept Azure's unclassified row query in delivery. `wire_inventory.py` binds the aggregate and identity reads under one semaphore and atomic final fence; Core receives only canonical Resource records, and the composition facade remains below its enforced 400-LOC ceiling. | [Issue #217](https://github.com/dotnetpower/fdai/issues/217); focused checks pass 259 cases; Ruff, strict mypy, and the composition contract pins pass. | Retain one fresh live reconciliation receipt before claiming validation. |
| 2026-08-19 | implemented | Corrected the first governed rule-discovery activation transition. An absent state now uses atomic create-with-audit, later transitions retain revision compare-and-set, and a concurrent create still fails closed instead of allowing candidate publication. | `current change`; `core/readiness/discovery_activation.py` and `tests/core/readiness/test_discovery_activation.py`; the focused activation suite passes 20 cases with a durable-store-compatible absent-row regression. | Restart Core and retain the governed activation transition before claiming operational validation. |
| 2026-08-19 | implemented | Composed the collector success projection, startup readiness evidence, governed runtime policy, and current Pantheon shadow count into one fail-closed rule-discovery activation boundary. Norns retains disabled candidates and publishes only after the injected ceiling opens; Mimir and catalog-as-code remain unchanged. | `current change`; focused activation, runtime, Norns, bootstrap, collector, and infrastructure checks. | Retain governed collector and activation-transition receipts before claiming operational validation. |
| 2026-08-17 | implemented | Gave `control_loop/_process.py` a conclusive compliant outcome so a zero-finding T0 verdict is no longer indistinguishable from an abstention. `NO_RULE_DENIED` now returns `ControlLoopOutcome.COMPLIANT` without opening RCA or `_fallback.py` tier escalation. | Current source plus `test_control_loop_e2e.py` (47 passed) and the `t0_deterministic` suite (109 total), both mutation-verified; measured before the change: 795 of 1641 abstentions carried `no_rule_denied`. | None for this seam. The append-only audit log keeps historical `control_loop.abstain` rows. |
| 2026-08-13 | implemented | Adopted the implementation ledger without reconstructing earlier provenance and recorded the bounded current-state activity identity change. | Current source plus `test_read_investigation_latency.py` and `test_activity_projection.py`; the focused suites passed. | Complete the deferred Phase 2 physical package move described below. |
| 2026-08-13 | implemented | Added the production Rule generation reconciliation boundary across the provider contract, atomic adapters, and startup composition while retaining Mimir and Heimdall ownership. | `current change`; `catalog_search.py`, `rule_generation_documents.py`, and focused worker, PostgreSQL, runtime, and activation checks. | Retain governed live generation receipts; the deferred Phase 2 package move remains separate. |
| 2026-08-13 | implemented | Suppressed startup generation reconciliation when either accountable owner, Mimir or Heimdall, is maintenance-disabled. | `current change`; `bootstrap.py` and focused disabled-agent ingress checks. | Retain governed live generation receipts with both owners enabled. |
| 2026-08-14 | implemented | Projected rehydrated and live canonical Incident state into the ontology instance store as a bounded current-state read model. | `current change`; `ontology_projection.py`, `registry.py`, `bootstrap.py`, and `test_ontology_projection.py`; focused Incident checks passed 47 cases. | Register the read-only incident-evidence function and retain authenticated Console evidence. |
| 2026-08-14 | implemented | Added the exact-release `query.incident_evidence` FunctionType over bounded correlation-scoped audit records and reused the existing incident RCA profile projection. | `current change`; `incident_queries.py`, semantic composition, InMemory and PostgreSQL readers, and focused checks passed 62 cases; task-scoped Ruff and strict mypy passed. | Constrain the final answer and next safe step, then retain authenticated Console evidence. |
| 2026-08-14 | implemented | Added deterministic Incident answer projection that rejects cause-bearing results and exposes only evidence gaps plus a candidate-only action draft next step. | `current change`; `semantic_turn_processor.py` and focused processor checks passed 34 cases; task-scoped Ruff and strict mypy passed. | Retain authenticated Console evidence after the local stack restart. |
| 2026-08-14 | implemented | Added the versioned authority-free observation activity contract and kept provider collection, durable campaign state, and runtime binding in delivery. | `current change`; contract, campaign, Operator projection, and Console focused checks passed. | Retain the governed local and deployed campaign evidence tracked by the observation owner document. |
| 2026-08-14 | implemented | Added the shared bounded resource-discovery contract family and provider-neutral Core router while retaining Azure profiles, rendering, and evidence production in delivery. | `current change`; focused discovery tests `34 passed`, task-scoped Ruff, and strict mypy over eight production files passed. | Retain the governed live canary evidence tracked by the Azure discovery owner document. |
| 2026-08-14 | implemented | Aligned the shared coverage enum and command sanitizer with the documented `unmapped` and no-environment-assignment contracts. | `current change`; focused discovery tests `36 passed`, task-scoped Ruff, and strict contract mypy passed. | Retain the governed live canary evidence tracked by the Azure discovery owner document. |
| 2026-08-14 | implemented | Added provider-neutral normalization and validation-version pins to the shared plan while keeping concrete Azure values in delivery profiles. | `current change`; focused discovery tests `40 passed`, task-scoped Ruff, strict mypy, and the Core import boundary gate passed. | Retain the governed live canary evidence tracked by the Azure discovery owner document. |
| 2026-08-14 | implemented | Hardened shared command evidence against redirects, control characters, and executable shell words while retaining catalog-owned placeholders. | `current change`; focused discovery tests `44 passed`, task-scoped Ruff, and strict mypy passed. | Retain the governed live canary evidence tracked by the Azure discovery owner document. |
| 2026-08-14 | implemented | Split durable HIL park key, expiry decoding, and on-call serialization into a focused record helper without changing approval, dispatch, or audit authority. | `current change`; focused HIL coordinator tests passed 33 cases, and strict mypy and the Core import gate passed. | The deferred Phase 2 package move remains unchanged. |
| 2026-08-14 | implemented | Added one provider-neutral pre-dispatch kinetic safety seam and shared its concrete StateStore writer across ordinary ControlLoop dispatch and approved HIL resume without changing risk, approval, execution, or audit ownership. | `current change`; focused kinetic and HIL checks passed 115 cases; task-scoped Ruff and strict mypy passed. | Production Forseti proposal-source composition, the verified independent observer, and governed live closure evidence remain owned by Operational Planning. |
| 2026-08-14 | implemented | Hardened the delivery join against internally valid cross-record substitutions by revalidating the durable OperationalPlan identity and every proposal lineage field before receipt persistence. | `current change`; `delivery/kinetic_proposal.py`, focused kinetic checks passed 119 cases, and task-scoped Ruff and strict mypy passed. | Production Forseti proposal-source composition, the verified independent observer, and governed live closure evidence remain owned by Operational Planning. |
| 2026-08-14 | implemented | Added a provider-neutral trajectory deletion claim and kept durable CAS and tombstone persistence in delivery. | `current change`; focused Core, PostgreSQL, and migration checks passed 186 cases; task-scoped Ruff and strict mypy passed. | Governed export and artifact-custody evidence remains owned by the trajectory interface document. |
| 2026-08-14 | implemented | Kept downgrade safety inside the delivery-owned schema boundary by refusing to reinterpret active external deletion claims. | `current change`; focused Core, PostgreSQL, and migration checks passed 187 cases. | Reconcile active claims before an intentional schema downgrade. |
| 2026-08-14 | implemented | Verified the delivery-owned downgrade guard against a live active deletion claim instead of relying on migration text inspection. | `current change`; focused Core, PostgreSQL, and migration checks passed 188 cases. | Keep migration behavior covered by the focused PostgreSQL lane. |
| 2026-08-15 | implemented | Added one focused exact-release principal manifest query module and bound it through the existing semantic Function registry and generic query-table renderer. | `current change`; focused manifest and semantic composition checks passed 42 cases; task-scoped Ruff and strict mypy passed. | Retain the clean bilingual answer-coverage and seeded full-cohort Browser evidence. |
| 2026-08-15 | implemented | Recorded which capabilities a verified plan selected on the `plan_verify` stage record and rendered `stage`, `plan_nodes`, and `failure_type` through the local plain-log context allowlist. The allowlist stays an allowlist so an unlisted field still cannot reach the plain log. | `current change`; `semantic_planning.py` and `capture-local-service-log.py`; focused planner checks passed 14 cases and the local service log runner passed 11 cases. | Use the field to decide whether incident binding needs a deterministic plan seed. |
| 2026-08-16 | implemented | Made the conversation's incident binding trusted planning input. Planning rewrites the identifiers and record window of a proposed `query.incident_evidence` node from the binding and re-verifies the plan, so identity is server-owned rather than retyped by the model. The turn evidence-reference bound is now a named contract constant that sizes the read window, and each fail-closed hold names its failing check through the existing `failure_type` field. | `current change`; `semantic_planning.py`, `semantic_runtime.py`, `semantic_turn_processor.py`, and `semantic_turn.py`; focused conversation, processor, and contract checks passed 606 cases; task-scoped Ruff and strict mypy passed. | Retain the governed authenticated Console evidence for the restored incident answer. |
| 2026-08-16 | implemented | Built the anchored incident read from the binding instead of a plan proposal. The typed frame still decides that a turn wants incident evidence; the capability and its two identities then come from the conversation, so no model selects or transcribes them, and the node still passes the same plan builder and verifier. The binding also answers an `incident_reference` clarification, and the English-only `this incident` substring route is gone so both locales reach one disposition through the frame. | `current change`; `semantic_planning.py` and `capture-local-service-log.py`; focused conversation checks passed 458 cases; task-scoped Ruff and strict mypy passed; three consecutive live anchored turns logged `plan_source="bound_incident"` and answered. | Retain the governed authenticated Console evidence for the anchored incident answer. |
| 2026-08-16 | implemented | Hardened the anchored incident read after critique. Rewriting a proposed incident read to the anchored identity retargeted a legitimate question about another incident, so the rewrite is removed and the processor's honest hold stands. The read window is now the evidence capability's own record bound rather than the unrelated turn evidence-reference bound. The answer and the Console overview state the verified total and name the truncation instead of reporting the displayed slice. Incident identity is compared on its canonical form, so a hexadecimal case difference no longer reads as a different incident. | `current change`; `semantic_planning.py`, `incident_queries.py`, `semantic_turn_processor.py`, and the Operator presentation; focused Core checks passed 913 cases and Operator checks passed 310 cases; task-scoped Ruff and strict mypy passed; mutation checks confirmed both new guards fail without the fix. | Retain the governed authenticated Console evidence. |
| 2026-08-16 | implemented | Guarded the presentation boundary the Operator and the Console own separately, bounded the projection conflict counter, and named the object in the persisted release error. Neither suite could previously see slot drift, because the Console silently discards an artifact it cannot parse. | `current change`; `test_presentation_console_contract.py` reads the Console's own `SLOT_KINDS`, `MAX_BLOCKS`, `MAX_ITEMS`, and `MAX_REFS` and checks every emitted artifact in both locales; Operator checks passed 318 cases and persistence checks passed 390 cases; a mutation to an unknown slot fails the guard. | None for this row. |
| 2026-08-16 | implemented | Checked the record order the incident answer claims. The answer names the latest records by slicing the tail of the projection, and nothing verified that the reader returned them oldest first, so a reordered read would have made the claim silently false. Evidence whose recorded times decrease is now held. | `current change`; focused processor checks passed 54 cases; two live incident correlations answered with the invariant active; removing the check fails the new case. | None for this row. |
| 2026-08-16 | implemented | Narrowed the binding's answer to an `incident_reference` question to a turn that will read the anchored incident, so a proposed plan for another output shape cannot read a different incident behind a question the operator never saw. | `current change`; focused conversation checks passed 458 cases; removing the shape condition fails the new case. | None for this row. |
| 2026-08-16 | implemented | Reported the incident evidence the read already carried. The audit row and the profile carried the actor, the title, the severity, the vertical, and the first and last recorded time, but the answer reported a status and a count, so no reader could tell who acted or when. The projection now keeps each record's actor, both surfaces list every populated profile field and a bounded recorded-activity table that names its own bound, and the next safe step follows the gaps the answer measured. Listing populated fields alone would have let an unrecorded status disappear, so both surfaces still state that the records carry no status, and the count stays the number verified rather than the number displayed. A record without a recorded time or an audit reference is skipped instead of shown with an invented anchor. | `current change`; `incident_queries.py`, `semantic_turn_processor.py`, and the Operator presentation; focused Core checks passed 388 cases and Operator checks passed 322 cases; task-scoped Ruff and strict mypy passed; three mutations covering dropped attribution and a silently omitted status each failed exactly one guard. | None for this row. |
| 2026-08-17 | implemented | Carried recorded grounded RCA through the existing Incident evidence function, semantic answer, Operator artifact, and strict Console slot decoder. T0 records one finding-severity impact row from its existing finding without inventing measurements. A cause is supported only when its recorded grounded hypothesis has matching citations; impact and citation rows remain bounded read-only evidence, and absent evidence stays explicit. | `current change`; focused Core, Operator, and Console checks passed 138 cases; Ruff, strict mypy, and Console typecheck passed. | Retain authenticated Browser evidence after restarting the local stack. |
| 2026-08-17 | implemented | Extended the read-only Incident evidence function with an allowlist of exact notification terminal failures. Each derived cause, impact row, and citation remains a projection over one existing audit row; no notification is replayed and no audit history is changed. | `current change`; focused Core and Operator checks passed 133 cases; Ruff and strict mypy passed. | Restart Core and verify the current route-unresolved incident in the authenticated Console. |
| 2026-08-18 | implemented | Added `runtime/venue.py` as the one contract that resolves `FDAI_EXECUTION_VENUE` and enumerates every venue-selected capability flag. `bootstrap.py`, `inventory_sync_cli.py`, `observation_campaign_cli.py`, and `analyzer_tick_cli.py` now read the venue and its transport, identity, inventory, and bus bindings from that table instead of comparing raw strings with per-site defaults. An unknown value is rejected instead of falling back to the weaker local transport. `check-venue-capability-contract.py` fails on a reintroduced ad hoc read or literal comparison and is registered in the pre-push structural gates, `verify.sh`, and CI. | `current change`; `tests/runtime` passed 209 focused cases including the new `test_venue.py`; `tests/integration/scripts/test_venue_capability_contract.py` passed 4 cases with negative fixtures; `tests/delivery` passed 1489 cases; task-scoped Ruff, format, and strict mypy passed. | Bring the Operator, document-ingestion, and document-worker services under the same contract; they still resolve the venue independently. |
| 2026-08-19 | implemented | Kept declaration inventory and operational data behind distinct deterministic query boundaries. `semantic_planning_cascade.py` accepts a `query.manifest` aggregate only when the frame requests the same canonical declaration kinds, so incident and audit-log counts cannot substitute declaration rows; rejected T1 proposals may retry once with T2 and otherwise abstain. | `current change`; [Issue #174](https://github.com/dotnetpower/fdai/issues/174); semantic planning checks passed 70 cases, including all three reproduced questions and matching declaration controls; task-scoped Ruff and strict mypy passed. | Rerun governed conversational assurance before treating the correction as fresh production evidence. |
| 2026-08-24 | implemented | Corrected the current repository-shape claim after the completed five-service extraction. The physical service tree and package ownership moved to the focused Multi-Service Repository Layout owner; this owner retains module boundaries, structural gates, extension seams, wiring, configuration, and conventions. The older deferred Phase 2 rows remain append-only and refer to a separate Core domain-facade move, not incomplete service extraction. | `current change`; five service manifests, the shared SDK manifest, root workspace manifest, and `check-independent-services.py` passed with `services=5`, `top_level_source=0`, and `service_forbidden=0`. | No service-extraction package move remains. The separate Core domain-facade move stays open below. |
### Remaining work
- [x] [Issue #15](https://github.com/dotnetpower/fdai/issues/15) adopts the permanent facade-only design; 98 layout checks, direct imports, coverage paths, and subsystem fan-out semantics remain intact.
