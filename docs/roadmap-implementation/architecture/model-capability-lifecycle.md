# Model Capability Lifecycle implementation ledger

This delivery ledger tracks endpoint binding, model capability resolution, provisioning, bounded
recovery, and lifecycle reconciliation. Historical transitions recorded before this owner split
remain append-only in the LLM Strategy implementation ledger.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Capability registry, endpoint binding, and provisioning assessment | implemented | `rule-catalog/llm-registry.yaml`; `rule_catalog/schema/llm_resolver.py`; `provisioning_assessment.py`; focused resolver tests | Capability mappings, explicit capacity units, endpoint provenance, mixed-publisher invariants, and fail-closed readiness are executable. |
| Escalation policy and same-publisher primary latency routing | implemented | `core/quality_gate/escalation_ladder.py`; `delivery/azure/llm/latency_routed_cross_check.py`; `composition/wire_llm.py`; focused routing tests | The ladder remains never-authoritative, and primary-pool latency selection cannot replace the independent secondary publisher. |
| T2 proposer failover, durable recovery evidence, and governed route selection | implemented | `core/tiers/t2_reasoning/recovery.py`; `runtime/t2_{recovery,route_registry}.py`; `ops.switch-t2-proposer-route`; focused runtime and pantheon-chain tests | Attempts reserve budget and persist sanitized evidence. Terminal exhaustion reaches human approval before a route change, and rollback is correlation-fenced. |
| Model lifecycle expiry review mechanics | implemented | `model_lifecycle_startup.py`; `model_lifecycle_review.py`; `resolved_models.py`; lifecycle proposal schemas v3 and v4; focused lifecycle, Core binding, Operator binding, and Key Vault source tests | One async owner loads an immutable revision, verifies trusted PR proposal and decision digests, persists decisions, and exposes authority-free pre-binding holds. Core and Operator, including the local Azure narrator, reject a supplied deployment digest mismatch before binding. |
| Weekly model reconciler and reviewed replacement flow | validated | `.github/workflows/model-lifecycle-reconcile.yml`; `model_lifecycle_{provider,reconciler,receipt}.py`; governed run `34348696426` attempt 2; draft PR #554; focused lifecycle and protected-workflow tests | The protected run retained a schema-v4 proposal and receipt bound to the exact source commit, proposal digest, bot-owned draft head, and run attempt. The draft changes one proposal manifest and records activation, mapping, execution, and live-execution eligibility as false. |
| Deployed model inventory discovery | implemented | `rule-catalog/vocabulary/resource-types.yaml`; `provider-relationship-mappings/azure-arg-v1.yaml`; `delivery/azure/model_deployment.py`; ARG, ARM, ontology projection, semantic planning, scheduling, presentation, and relationship-mapping checks | Existing model deployments enter the read-only inventory and ontology through change acceleration plus scheduled reconciliation. Authorized LLM/GPT list phrases resolve to the deployment subtype. An authoritative token rate-limit becomes TPM; raw SKU capacity never does. No conversational deployment ActionType or provider mutation path exists. |
| Governed operational lifecycle evidence | in-progress | Runtime recovery receipts, lifecycle proposals, and model measurements cited above | Mechanics exist; one exact-revision campaign covering expiry, recovery, route change, and rollback is not retained. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-12 | implemented | Added a machine-readable frozen-scenario shadow-replay gate to every governed model lifecycle draft receipt while preserving the singular compatibility field. | `current change`; `model_lifecycle_receipt.py`; protected workflow contract; focused lifecycle receipt and workflow tests passed 2 cases. | Retain the next governed run with the additive gate before claiming operational validation for this revision. |
| 2026-08-24 | in-progress | Split model capability lifecycle ownership from LLM Strategy without reconstructing or moving earlier append-only history. | `current change`; this owner, the LLM Strategy ledger, registry, resolver, recovery, and reconciler paths above. | Complete the observable lifecycle and recovery exits below. |
| 2026-09-05 | implemented | Bound one async resolved-model startup revision to trusted PR lifecycle review and capability eligibility, added Core and Operator revision fences, and persisted digest-verified authority-free decisions before binding. | `current change`; focused lifecycle, configuration, Core composition, Operator narrator, and scheduled reconciler checks. | Retain the governed recovery campaign and one protected scheduled reconciler receipt. |
| 2026-09-05 | implemented | Corrected the preceding incomplete wiring after review found that its startup owner was not called by production composition. Core now supplies one startup-owned revision to its production binders, authenticated workflow-draft observations persist holds through the existing state store, Operator runs its revision owner first, and scheduled reconciliation emits an exact draft receipt. | `current change`; focused lifecycle, Core and Operator production composition, GitHub observation, binder hold, receipt, and protected-workflow checks. | Retain the governed recovery campaign and one protected scheduled reconciler live receipt. |
| 2026-09-05 | implemented | Captured the protected checkout revision before creating a proposal branch so a first-run draft receipt cannot misidentify the proposal commit as its source. | `current change`; scheduled workflow source and focused lifecycle workflow test. | Retain one protected scheduled reconciler live receipt. |
| 2026-09-05 | implemented | Replaced the self-hosted runner's unavailable system `pip` dependency with the repository-pinned uv toolchain and frozen Core package environment. | `current change`; failed governed run `33953816935`, scheduled workflow source, and focused lifecycle workflow test. | Retain one successful protected scheduled reconciler receipt. |
| 2026-09-05 | implemented | Bound reconciliation to the tracked `services/assets/resolved-models.json` artifact instead of a nonexistent repository-root path. | `current change`; failed governed run `33954841686`, scheduled workflow source, and focused lifecycle workflow test. | Retain one successful protected scheduled reconciler receipt; provider metadata remains required for a draft proposal. |
| 2026-09-06 | in-progress | Added a shadow-first, Owner-requested Global Standard model deployment action with exact model and target pinning, 1K TPM unit validation, quota preflight, create-only apply, independent ARM readback, deployment-name progress, account-scoped least-privilege RBAC, and inventory change tracking. | `current change`; `rule-catalog/action-types/ops.deploy-model.yaml`; `executor_targets.py`; both gateway adapters; development operations gateway; `infra/dev_gateway_model_deployment_rbac.tf`; focused catalog, gateway, adapter, Terraform, and Console progress checks. | Configure action authorization and promotion, then retain one governed GPT-5.4 live deployment and rollback receipt. |
| 2026-09-06 | withdrawn | Removed the conversational model-deployment ActionType, executor routing, provider mutation, RBAC, and action-progress behavior after the target-customer boundary was clarified: infrastructure as code owns resource creation. Retained the provider-neutral resource type and parent mapping for read-only ontology discovery. | `current change`; absence of `ops.deploy-model`; resource-type and provider-relationship catalog checks. | Retain an authorized live inventory receipt showing existing model deployments through the normal scheduled collection and semantic query path. |
| 2026-09-06 | implemented | Completed the read-only replacement with bilingual deployed-LLM and GPT-model query terms, ARG property and parent projection, ontology-instance preservation, default resource-type inclusion, and periodic change-feed plus reconciliation coverage. | `current change`; focused semantic value-domain, planning, ARG, ontology projection, inventory scheduling, and chat-to-pipeline checks. | Retain one authorized live inventory receipt showing an existing deployment and a later status change without any resource mutation. |
| 2026-09-06 | implemented | Added exact TPM observation from provider `rateLimits`: count and renewal period normalize to one per-minute value only when token rules agree. Missing or conflicting rules remain unknown, and `sku.capacity` remains a separate provider unit. | `current change`; `delivery/azure/model_deployment.py`; ARG, ARM, ontology, and Operator presentation tests. | Retain one authorized live receipt confirming the target provider exposes token rate limits and that an IaC capacity change advances the ontology observation. |
| 2026-09-06 | implemented | Separated requested `sku.capacity` from current provider capacity and exposed an explicit transition flag so an in-progress IaC scale change cannot look complete. | `current change`; model deployment projection plus ARG, ARM, ontology, and presentation checks. | Confirm the target provider populates current capacity during one governed live observation. |
| 2026-09-06 | implemented | Completed 12 focused hardening rounds over TPM evidence admission, projection parity, ontology freshness, presentation, authorization, and change detection. Fixed four Medium findings: malformed recognized rules, unbounded rule collections, requested/current capacity conflation, and non-object rate entries. No verified residual finding above Low remains. | Commits `4abc1812d`, `c4303b937`, `ff22bd0b5`, and `87136bd68`; final focused gate passed 449 tests plus Ruff, format, strict mypy, translation, Korean quality, punctuation, roadmap, and design-route checks. | Retain the live provider receipt already listed below; no additional code hardening item is open. |
| 2026-09-09 | implemented | Corrected lifecycle reconciliation to accept the tracked empty `hil-only` baseline, compare exact model versions, normalize Azure model- and SKU-level deprecation dates within 60 days, and keep trusted schema v3 observations readable while new proposals use schema v4. Operator local narrator binding now waits for the startup-owned immutable revision instead of rereading the source during synchronous composition. | `current change`; lifecycle provider, reconciler, workflow, Core startup, Operator composition, narrator adapter, and focused lifecycle tests. | Retain one protected draft-only reconciliation receipt for the exact merged revision. |
| 2026-09-09 | implemented | Removed the scheduled publisher's undeclared GitHub CLI dependency after the governed runner resolved a safe proposal but could not open its draft. Git still publishes the idempotent branch; the pinned GitHub Script action restores and checks the branch commit's protected source parent, delegates canonical digest verification to the Python receipt module, creates or reuses one bot-owned draft, and writes immutable PR readback for receipt generation. | Failed governed run `34345748706`; `current change`; scheduled workflow, receipt verifier, and focused lifecycle contract tests. | Retain one successful protected draft-only reconciliation receipt. |
| 2026-09-09 | validated | Retained the governed draft-only reconciliation receipt after enabling the repository's reviewed Actions PR-creation boundary. The run detected family and capability drift from the tracked `hil-only` source, reused the immutable proposal branch, created one bot-owned draft, and performed no mapping, activation, provider, or managed-resource change. | Run `34348696426` attempt 2 at workflow head `34cf5d6b045ac7043f41900447b5bf7e2cc3191c`; source commit `882e3506265810e7428a1dd1bdf2b01eef6df20f`; draft PR #554; receipt digest `a6563c2b5d0a996143ad4c6aa215500da980ed47eb74585e8fdcfcb29a313b89`. | The scheduled reconciler exit is complete; the separate T2 recovery campaign and inventory observation exits remain open. |

### Remaining work

- [ ] Retain a governed T2 recovery campaign proving bounded attempt budgets, durable receipt forwarding after restart, terminal exhaustion to human approval, an audited route switch, correlation-fenced rollback, and recovery without a new approval.
- [x] Bind the implemented expired-unmerged evaluator and direct Key Vault source adapter through an asynchronous startup owner. Add trusted pull-request lifecycle observation, proposal and decision digest verification, persistence, and pre-binding capability holds.
- [x] Retain governed run `34348696426` attempt 2 and draft PR #554, showing that family and capability drift creates one sanitized proposal and an authority-free receipt without changing the live mapping.
- [ ] Retain an authorized live inventory receipt proving that scheduled collection projects existing Azure AI model deployments and their parent accounts into the ontology without exposing a resource-creation action.
