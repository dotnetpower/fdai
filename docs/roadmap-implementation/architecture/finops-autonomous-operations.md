# FinOps Autonomous Operations implementation ledger

This delivery ledger tracks the ontology-grounded, 15-agent FinOps runtime without treating shared
platform foundations as proof that the vertical operates autonomously.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Exact FinOps ontology decision frame | implemented | `extensions/cost-governance/src/fdai_cost_governance/resources/semantic-profile.json`; `rule-catalog/vocabulary/`; semantic profile validator | The standalone profile pins one exact release, references the operational spine, bounds five query profiles and evidence functions, and grants no approval, execution, or promotion authority. |
| F1-F8 ontology competency | implemented | `tests/integration/fixtures/cost_governance_f1_f8/`; `tests/integration/scripts/test_cost_governance_semantic_profile.py` | Eight positive and eight isolated negative fixtures prove deterministic reduction to observation mode for reversed links, stale intent, missing source authority, unknown service, conflicting facts, mixed releases, unverified outcomes, and a truncated graph. |
| FinOps-specific 15-agent choreography | implemented | `cost-governance-w3a-replay.json`; Njord runtime binding and hydration tests; package job, Operator API, disclosure, notification, and Console tests; [responsibility model](../../roadmap/architecture/finops-autonomous-operations.md#the-15-agent-responsibility-model) | The deterministic replay proves all 15 fixed agents start with existing subscriptions while enabled and available Cost Governance reaches Njord only through typed ingress. Bounded startup hydration restores retained complete evidence without replaying historical findings. Server-owned projections preserve availability separately from enablement and skip retained-cost reads for hidden disclosure without changing agent roles or adding authority. This does not claim a live operation. |
| Autonomous recovery ladder | implemented | `fdai_cost_governance.coordination`; `test_coordination.py` | The package coordinator validates the fixed seven-step order over typed event evidence, rejects scope, release, safety, time, and autonomy escalation, requires Saga intent audit before an effect request, and enters sticky observation mode when Saga or Vidar is unavailable. It cannot call agents or perform approval, execution, audit, or rollback. |
| Independent multi-effect settlement | implemented | `CostEffectSettlementService`; Cost Governance decision migration and focused settlement tests | Core settles cost, capacity, service, and recovery effects only from complete independent post-horizon observations. Failed or stopped effects emit a typed Vidar request, independently observed recovery is required for closure, and only fully verified cost effects produce nonnegative realized savings. |
| Governed learning inputs | implemented | `core/operational_learning/cost_governance.py`; focused replay and cohort tests | Immutable Muninn projections require complete lineage and a Saga terminal audit. Norns inputs require distinct complete positive and negative cases, reject censored and unscorable cases, and remain inert without a promotion or catalog-mutation operation. |
| Shadow parity and catalog ownership | implemented | `fdai_cost_governance.parity`; `VerticalPackageManager`; package resource materialization and convergence tests | Dual-read comparison covers decisions, reasons, typed payloads, audit fields, and exact ontology lineage while allowing only one selected publisher. The package owns the 12 rules, 12 policies, 12 fix templates, and cost-aware workflow when enabled. The deprecated Core facade remains inert until the W7 removal review. |
| Operational autonomy campaign | in-progress | `.github/workflows/cost-governance-observation-export.yml`; campaign source and focused tests; issue #902 final collector evidence; corrected campaign `34766958589` | Enabled revision 21 has a completed collector pass, cursor revision 6, and 136 retained raw observations. The first corrected review-only campaign returned six readiness-blocked targets with sample count 0. It still must accumulate at least 100 eligible action episodes over at least 30 days and return `ready=true`. Separate package, ActionType, and Workflow review results grant no authority. |
| Independent campaign reviews | implemented | `cost_governance_review.py`; `postgres_cost_governance_review.py`; `record_cost_governance_review.py`; `.github/workflows/cost-governance-promotion-review.yml`; focused review tests | Six distinct target records can share one attested campaign evidence digest while retaining target-specific report digests and human decisions. Recording never changes package activation, ActionType mode, Workflow mode, or any authority registry. No live review set exists. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-13 | in-progress | Published and deployed the qualified campaign-replay fix, restored enabled revision 21, verified the collector effect, and retained the first successful review-only campaign result. The first disable stopped on a PostgreSQL connection timeout without an artifact; recovered database metrics bounded one successful retry. Fresh plans contained no delete or replace. The campaign changed no activation or review state and correctly retained explicit temporal blocks. | Candidate `34747311873`, source `3b4c088ea20d1d77770912394141847bf9940886`, image `sha256:00f769caaf7c1ebe8c09bde3b254ca8c70ef4c2c5d979de037e71ed9575036b0`; disable `34765720805`/`34765992882`, live-authoritative non-replayed revision 19 receipt `sha256:73c2f3722c3279d410fe3fc2b3857915a9c655ca1f81ad6c3d3be31603efa6ae`; Core plan/apply `34766097573`/`34766298596`; Job plan/apply `34766101394`/`34766556857`; upgrade/enable `34766677628`/`34766753818`, revisions 20/21 and pin `sha256:52f6d931121fb9dd842664bca58abd1557ae8202d7d6bd683f66cd58232f1785`; collector `caj-fdai-dev-cost-collect-5qo21d9`, cursor revision 6 and 136 raw observations; campaign `34766958589`, review artifact `sha256:8b7f58d890164d15768e7e07248de0d357be97af89605716bca235f187c8d931`, six readiness-blocked targets, sample count 0, campaign episodes 0, and promotion reviews 0. | Keep issue #903 open until at least 100 eligible live-authoritative action episodes span 30 days with complete audit, dependency, disclosure, settlement, and rollback evidence. Keep issue #904 blocked until `ready=true`, then obtain six independent human reviews. |
| 2026-09-13 | in-progress | Qualified campaign replay ordering across the retention join after the second final-pin attempt exposed an ambiguous PostgreSQL `revision` reference. The read path now deterministically orders episode fields and covers the real database query. | Run `34743659442`; exact internal traceback identified `psycopg.errors.AmbiguousColumn`; artifact count 0 and final-pin episode count 0; `current change`; focused PostgreSQL and campaign tests (`40 passed`), Ruff, strict mypy. | Deliver and deploy the fix, then begin the temporal campaign on the resulting exact active pin. |
| 2026-09-13 | in-progress | Corrected the final-pin observation workflow's fresh-environment qualification dependency. The failed attempt stopped before export and retained no campaign evidence. | Run `34742858675` failed because the semantic-profile checker could not import `yaml`; `current change`; explicit `--extra dev` on both qualification commands and focused workflow regression. | Merge the fix and retry against the unchanged revision-18 active pin; the 30-day and 100-sample gate remains open. |
| 2026-09-13 | implemented | Hardened independent campaign review replay to treat generated review time as server state rather than request identity. An exact retry returns the original verified receipt while changed request content, retention duration, or stored columns fail closed. | `current change`; shared review Protocol, PostgreSQL store, recorder, and focused exact-retry tests (`2 passed`). | Deliver the fix before starting issue #903; retain the qualifying campaign before issue #904 records any review. |
| 2026-09-13 | validated | Proved issue #902's retained N-1 rollback and exact casefold restoration without changing ActionType or Workflow mode, then independently verified one restored live collector effect. | Core and Job plan/apply runs `34738656368`, `34738866589`, `34738668462`, `34739105713`, `34739299841`, `34739446082`, `34739302019`, and `34739904514`; lifecycle runs `34738142218`, `34739213368`, `34740045835`, and `34740149047`; collector `caj-fdai-dev-cost-collect-kbom2sn`; cursor revision 2 and 71 observations; unchanged ActionType digest; Workflow `shadow`. | Start issue #903 only after the review controls are delivered, then retain the 30-day and 100-sample campaign before issue #904 reviews. |
| 2026-09-13 | implemented | Added the authority-neutral W7 review boundary after campaign readiness. Each protected request verifies and recomputes the exact campaign, rechecks the active pin, and appends one content-addressed package, ActionType, or Workflow review. | `current change`; review contract, store, migration, recorder, workflow, and focused local checks including loopback PostgreSQL replay. | Retain a qualifying live campaign and six review receipts bound to one attested campaign artifact before any separate promotion change. |
| 2026-09-12 | in-progress | Added the protected W7 observation producer and trusted importer. It records no-op, beneficial action, denial, approval, execute, unresolved, and rollback outcomes separately, preserves workflow lineage, rejects mixed activation pins and changed replays, and keeps missing objective or qualification evidence blocked. | `current change`; campaign export/import/validation paths; live disposable-PostgreSQL join and lifecycle tests; strict mypy, Ruff, executor and control-loop lineage tests, and protected-workflow checks. | Deploy one required-CI revision, complete the catalog-owned live sample and dwell windows, retain rollback and zero-escape/disclosure evidence, and obtain independent package, ActionType, and Workflow reviews. |
| 2026-08-31 | implemented | Connected persisted Cost Governance activation, complete service-day evidence, and Budget availability to the fixed Pantheon. The package-neutral advisory provider now hydrates from retained observations before broker subscriptions, so restart preserves Njord evidence while historical anomalies are not republished. | `current change`; canonical cost sample publisher and timestamp tests, Njord and composition tests (`30 passed`), live local restart introspection reported `package_enabled=true`, provider and Budget bound, and one tracked scope; Huginn and Njord broker lag remained zero. | Collect authoritative utilization samples and a governed executed-and-settled action before claiming capacity or Outcomes data. No action promotion or authority change is claimed. |
| 2026-08-28 | in-progress | Merged the hardened fixed-15 Cost Governance runtime to `main` and completed 12 critique rounds with no unresolved finding above Low. Required main CI and image publication passed. The protected exact-revision plan verified Azure context but failed core model capability quorum before Terraform, so no live cohort or promotion evidence was created. | PR #328; merge `6b638db7c6fb928372cd31b4ad8371e3f9ea683a`; CI `33175385098`; image run `33175384996`; protected run `33175771735`; issue #325 evidence comment. | Restore the protected model resolver under a new hypothesis and run the bounded live observation cohort before any package or asset review. |
| 2026-08-29 | in-progress | Implemented W7 local validation mechanics with exact-revision lifecycle receipts, append-only campaign evidence, bounded outcome accounting, every explicit release stop, and separate authority-neutral package, ActionType, and Workflow review results. | `current change`; 27 focused W7 tests, 310 combined W0-W7 Cost Governance tests, and 259 migration tests passed. Strict mypy, Ruff, wheel and source distribution build, direct loading of 38 wheel resources, convergence, Core and agent import boundaries, design routes, roadmap, translation, and punctuation checks passed. Synthetic fixtures prove mechanics and prove that non-live evidence remains blocked. | Run one governed live-authoritative observation campaign on a pinned production revision and independently review package activation and each promoted asset. Retain the deprecated inert Core facade until production cutover and governed previous-version rollback receipts exist. |
| 2026-08-28 | implemented | Completed W6 dual-read single-publish parity, package-only catalog ownership, ordinary-loader materialization, and atomic N-1 upgrade and rollback with failed-transition preservation. Base FDAI now retains generic ActionTypes and ontology but activates no package-owned cost assets when the package is absent or disabled. | `current change`; 84 package tests, 12 lifecycle tests, 1,366 catalog and renderer tests, 62 inventory, convergence, and boundary tests, 65 migration tests, 14 Pantheon tests, and 13 N-1 compatibility tests passed. Focused Ruff and strict mypy, wheel and source distribution build, direct wheel resource loading, and Core and agent import boundaries passed. | Keep the deprecated inert Core compatibility facade until W7 operational validation completes. No live campaign, action replay, active promotion, deployment, or production cutover is claimed. |
| 2026-08-28 | implemented | Completed W4-W5 with authority-neutral episode contracts, the fixed bounded recovery ladder, independent multi-effect settlement and post-recovery observation, append-only retained lineage with legal hold, immutable replay, and inert balanced learning inputs. | `current change`; 131 focused W4-W5 tests, 451 package and Core regression tests, 48 Pantheon tests, and 24 convergence mutation tests passed; focused Ruff and strict mypy, package build, Core and agent import boundaries, file-size policy, and design-route checks passed. | Keep W6 cutover and W7 operational validation open. No live operation, active learning, promotion, deployment, or catalog mutation is claimed. |
| 2026-08-28 | implemented | Separated persisted Cost Governance availability from enablement across package composition, Core activation storage, Operator projection, and Console preflight. Availability now carries bounded derivation reasons and exact package artifact identity; hidden disclosure returns only metadata and performs no cost-table read. | `current change`; the combined hardened W0-W3 suite passed with 253 tests and the focused Console suite passed with 43 tests; Ruff, strict mypy, Console typecheck and build, package build, convergence, import-boundary, design-route, roadmap, localization, and punctuation checks passed. | Implement W4 recovery, W5 settlement and learning, W6 cutover, and W7 operational validation. |
| 2026-08-28 | implemented | Completed the W3 read surface with activation- and per-user-access-gated Operator projections, component-wise disclosure meets, package notification gating, and a source-driven four-tab Console workspace. The LLM Cost route and notifications remain independent. | `current change`; 242 combined W0-W3 focused Python tests and 49 focused Console tests passed; focused Ruff, strict mypy, Console typecheck and build, package convergence, and Core and agent import boundaries passed. | Implement W4 recovery, W5 settlement and learning, W6 cutover, and W7 operational validation. No authenticated live Console or Azure evidence was collected. |
| 2026-08-28 | in-progress | Bound Njord's post-disable drain decision to the package activation revision carried by the typed sample instead of its provider observation time. | `current change`; the final focused W1-W3A regression suite passed with 243 tests, including a reversed-time drain case proving that only the preceding enabled revision drains. | Complete W3B and the full W3 failure-path trace; do not infer operational success from the deterministic replay. |
| 2026-08-28 | in-progress | Added W3A activation-gated cost observation collection, package-owned advisory calculation, retained cursor evidence, and generic fixed-Pantheon Njord injection without changing roles or granting package authority. | `current change`; 214 focused tests passed across package jobs, provider degradation, duplicate/reorder/restart handling, Pantheon parity, migration ownership, and package boundaries; Ruff, strict mypy, Core and agent import checks, package build, and W0-W2 convergence passed. | Complete W3B and a full W3 trace across the responsibility-specific failure paths; do not advance recovery, settlement, learning, or promotion claims. |
| 2026-08-28 | implemented | Materialized the exact-release Cost Governance semantic profile and F1-F8 competency corpus while preserving Njord and Freyr ownership and keeping authority outside ontology and profile fields. | `current change`; semantic profile validator passed with 16 fixtures, 8 positive, and 8 lowered; focused profile tests passed with 7 tests; focused catalog and provenance tests passed with 4 tests; Ruff, design-route, and roadmap tracking checks passed. | Compose W2-W7 runtime, recovery, settlement, choreography, and governed learning evidence without raising autonomy from semantic data. |
| 2026-08-24 | not-started | Defined the ontology-required decision frame, fixed 15-agent responsibilities, bounded recovery order, settlement, and measurement contract. | `current change`; owner document, paired translation, pantheon-name and topic checks, size, link, and roadmap tracking checks. | Implement and validate each bounded scope row without changing AgentSpec or topic ownership. |

### Remaining work

- [x] Pass F1-F8 on one exact ontology release, including reversed-link, stale, incomplete,
  conflicting, mixed-release, unknown-service, missing-source-authority, and unverified-effect
  cases through the standalone semantic profile validator and focused fixture tests.
- [x] Retain one replayable FinOps scenario that reaches all 15 responsibilities through existing
  owned topics, alongside focused duplicate, reorder, restart, deadline, and degradation tests.
- [x] Prove the bounded recovery ladder attempts no authority-raising fallback and routes to Var
  only for policy-required or residual cases.
- [x] Close every expected effect as verified, failed, censored, or unscorable before learning or
  reporting realized savings.
- [x] Implement exact-revision lifecycle and campaign persistence, bounded deterministic accounting,
  and separate review-only readiness gates that reject synthetic evidence and grant no authority.
- [ ] Retain a live observation-mode cohort with autonomy outcomes, no-ops, approvals, rollbacks,
  policy escapes, objective regressions, and settlement completeness on one pinned revision.
