# Ontology-Grounded FinOps Package Architecture implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions, and
resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Shared Pantheon lifecycle-state isolation | implemented | `agents/_framework/{runtime,execution_safety,runtime_operational_agents}.py`; `agents/{norns,var,vidar}.py`; focused Norns, Var, Vidar, and runtime checks | Enforce composition validates explicit Thor, Vidar, and Var durable bindings. Reusing one provider instance persists lifecycle recovery only and cannot install, activate, configure, publish, or grant Cost Governance package access. |
| Extractable FinOps guard baseline | implemented | `services/core-control-plane/src/fdai/core/verticals/cost_governance/finops.py`; `services/core-control-plane/tests/core/verticals/test_finops.py`; 11 focused tests | Pure candidate and guard logic exists inside Core. This does not prove package composition. |
| Shared image-input isolation | implemented | Root `uv.lock`; service-owned Dockerfiles; service-image contract checks | Inputs copied by both Core profiles select both images without adding package activation, data access, or authority to Cost Governance. |
| Independent distribution | implemented | `extensions/cost-governance/`; wheel, source distribution, and package-resource checks | The typed `fdai-cost-governance` package loads 38 digest-bound resources through `importlib.resources`; built-wheel and source-distribution rebuild checks load the same candidates without package source paths. |
| Ontology-bound vertical manifest and bundle | implemented | `core/vertical_packages/`; package and Core focused tests | Immutable generic contracts reuse `ExtensionManifest` and the trusted `extension` artifact kind, pin the exact ontology and semantic profile, and expose no approval, execution, or promotion authority. |
| Atomic activation and provider binding | implemented | `VerticalPackageManager`; `cost_governance_activation.py`; `test_cost_governance_runtime.py`; package boundary tests | Install starts disabled. Availability derives from host, ontology, and provider requirements. An enabled package is discovered through one package-neutral entry point, and bounded retained observations rebuild Njord evidence after restart without importing the optional package into Core or granting authority. |
| Compatibility cutover and rollback | implemented | `fdai_cost_governance.parity`; `VerticalPackageManager`; frozen parity and N-1 lifecycle tests | Dual-read single-publish parity, package-owned catalog cutover, disable, failed-upgrade preservation, and local previous-version rollback are implemented. The deprecated Core facade remains inert because governed production cutover and live previous-version rollback evidence do not exist. |
| Protected W7 lifecycle | validated | `.github/workflows/cost-governance-lifecycle.yml`; `lifecycle_cli.py`; W7 migrations and focused tests; rollback/restore runs through revision 15; corrected-source runs `34765992882`, `34766677628`, and `34766753818` through revision 21 | The protected path verifies exact source, required CI, signed image, deployed jobs, wheel resources, runtime configuration, and complete request identity. Fresh independent deployments proved retained N-1 rollback, final casefold restore, and corrected campaign-query rollout in dev. Campaign readiness and reviews remain separate. |
| Independent W7 review receipts | implemented | `cost_governance_review.py`; `postgres_cost_governance_review.py`; `20260913_core_cost_governance_review.py`; `record_cost_governance_review.py`; `.github/workflows/cost-governance-promotion-review.yml`; focused contract, workflow, recorder, and PostgreSQL tests | One protected request records one target against a common attested campaign digest and target-specific report digest. Reviewer identity, decision, rationale, references, and time are append-only, while approval, execution, and promotion authority remain false. No live review receipt exists. |
| Cost analytics scheduling and read projection | implemented | `scheduled_analytics.py`; analytics-run contract and migration; local coordinator and deployed job wiring; focused package, persistence, Operator, and integration tests | One bounded command records source-aware run receipts and exposes evidence readiness without changing package activation or action authority. A current live run remains operational evidence. |
| Azure FOCUS collection recovery | validated | `azure_focus.py`; `job_cli.py`; focused runtime and Job tests; execution `caj-fdai-dev-cost-collect-5qo21d9`; durable cursor readback | A read-only query retries once only when Azure supplies a valid bounded delay. Numeric zero remains valid and provider service labels are case-folded before allowlist matching. The corrected-source pass returned `complete`, advanced the cursor to revision 6, and retained 136 observations. Unknown services still fail before a cursor write. |
| Operator PostgreSQL projection compatibility | implemented | `services/operator-service/src/fdai_operator_service/postgres_cost_governance.py`; `services/operator-service/tests/test_postgres_cost_governance.py`; `console/src/api-transport.ts`; `console/src/api.ts`; `console/src/api-cost-governance-projection.ts`; bilingual Cost Governance route catalogs; focused Operator and Console tests | The reader uses JSON scope membership, normalizes SQLAlchemy-style psycopg DSNs for direct driver use, and selects analytics with static parameterized SQL. The shared Console transport accepts only an explicitly classified source-gate `503` as projection absence; a generic service `503` remains an error. Concurrent source-manifest reads coalesce and successful provenance expires after 15 seconds so a recovered projection is not pinned unavailable for the SPA lifetime. The client derives its reason type and decoder allowlist from one complete vocabulary and provides English and Korean labels. Unrelated host-route decoders remain outside the initial Cost Governance Console path. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-17 | validated | Validated the corrected readiness vocabulary on all four standard-port Cost Governance Live routes through the explicit authenticated Azure CLI debug profile without claiming Browser Entra evidence. | [Issue #1291](https://github.com/dotnetpower/fdai/issues/1291); `12/12` local services ready; four Operator responses returned `200`; 7 trend points, 2 budgets, 34 Advisor recommendations, and 0 utilization records remained explicit; no panel, page, or API errors; no raw provider identity; no document or main-region overflow at 1440 x 900, 993 x 641, or 390 x 844. | Protected CI and merge remain authoritative for delivery. |
| 2026-09-17 | implemented | Restored Console parity with the complete service-contract Cost Readiness reason vocabulary so bounded projection truncation and a missing analytics snapshot render as typed degraded states instead of decoder failures. | `current change`; [Issue #1291](https://github.com/dotnetpower/fdai/issues/1291); Cost decoder and catalog tests (`13 passed`), Console typecheck and production build, and localization checks. | Retain authenticated standard-port evidence for all four Live routes and protected CI. |
| 2026-09-17 | implemented | Kept the loop degraded-ready after a failed refresh only when PostgreSQL retains an exact-scope snapshot inside the two-day projection threshold. | `current change`; #1280; focused store, package, and readiness tests (`15 passed`); strict mypy; provider `429` receipt retained without retry. | Retain authenticated standard-port evidence from the corrected loop. |
| 2026-09-17 | implemented | Allowed a current partial analytics receipt to satisfy loop process readiness while preserving its unavailable evidence facets and one-shot warning exit. | `current change`; #1280; focused package and developer-workflow tests (`4 passed`); strict mypy; failed standard-stack attempt identified the prior endless waiting state. | Retain authenticated standard-port evidence after the corrected loop starts. |
| 2026-09-17 | implemented | Replaced machine-surface set membership in the Operator route builder with exact canonical equality after the semantic-routing CI guard classified the former shape as an unreviewed lexical path. | `current change`; #1280; CI run `35185995655`, job `105088130753`; focused semantic guard and Operator tests (`36 passed`); strict mypy. | Required protected CI remains authoritative. |
| 2026-09-17 | in-progress | Retained one current local analytics snapshot through the shared scheduled package command after the Query-path correction. | content-free receipt: `partial`, 212 observations, 7 trend points, 2 budgets, 32 recommendations, limitation `utilization_partial`; package activation and action authority unchanged. | Retain a complete run after compatible utilization evidence becomes available. |
| 2026-09-17 | implemented | Reused the validated Cost Management Query request and response contract for scheduled analytics after the date-filtered Usage Details list API exhausted the bounded live attempt. | `current change`; #1280; content-free failed receipt, bounded endpoint probes, focused package tests, and strict mypy. | Retain a successful current analytics receipt on the merged runtime. |
| 2026-09-17 | implemented | Added one local/deployed scheduled analytics command, durable source-aware run receipts, and the authenticated evidence/case/settlement read projection. | `current change`; #1280; focused contract, package, persistence, Operator, migration, script, and Console checks. | Retain one current authoritative run and live case/settlement evidence on the merged revision. |
| 2026-09-16 | implemented | Rebound the unchanged Cost Governance semantic declaration set, package manifest, W6 inventory, and 16 F1-F8 fixtures to the exact active ontology release after logical-service aliases changed two declaration digests. The source-only generator now refreshes exact declaration refs as well as the release digest. | [Issue #1170](https://github.com/dotnetpower/fdai/issues/1170); `current change`; profile release `sha256:f38ed320898be606e639ec4bec951eccd03b37a6e11568273d5848e0ed06a194`; canonical profile `sha256:4b9b0a2304da9d16eedd1bdceedebd9e7b8d251a5fc3a5e0ba87fc1d3200ea36`; exact CI regressions and generator tests passed 24 cases. | Exact-head protected CI remains authoritative. W7 evidence, package enablement, data access, mode, and promotion are unchanged. |
| 2026-09-16 | implemented | Kept the Browser evidence v2 read route and route-local catalog outside optional Cost Governance package behavior, data, activation, and authority while reusing shared host registries. | `current change`; issue #1083; design-route and independent-service checks. | No Cost Governance implementation or validation state changed. |
| 2026-09-16 | implemented | Revalidated shared Console decoder isolation after rebasing onto upstream revision `ee3eab050`. | `current change`; Console unit suite (`3577 passed`), typecheck, production build, and entry-bundle gate (`437903` raw / `137887` gzip bytes). | No Cost Governance package, activation, data access, promotion, or execution authority changed. |
| 2026-09-16 | implemented | Kept unrelated Audit, agent-activity, Insights, and Reporting decoders out of the initial shared Console facade while preserving Cost Governance source-gate behavior. | `current change`; Console unit suite (`3559 passed`), typecheck, production build, and entry-bundle gate (`444885` raw / `139896` gzip bytes). | No Cost Governance package, activation, data access, promotion, or execution authority changed. |
| 2026-09-16 | implemented | Bounded the shared Console source-manifest cache to 15 seconds while retaining exact source-gate classification for Cost Governance projections. | `current change`; `console/src/api.ts`; `console/src/api-source-gating.test.ts`; focused Console test (`5 passed`). | No Cost Governance package, activation, data access, promotion, or execution authority changed. |
| 2026-09-15 | implemented | Corrected shared Console catalog validation to select the actual imported translation binding, not unrelated formatting helpers. | Current change; #1084; `console/src/i18n/catalog-usage.test.ts` passed 4 checks and Console typecheck passed. | No Cost Governance package, activation, data access, or authority changed. |
| 2026-09-14 | implemented | Kept shared analyzer coverage, Live operational-activity replay, and Impact Scope relationship evidence outside optional Cost Governance activation and authority while reusing host Operator routes and stores. | `current change`; focused changed backend tests (`448 passed`, one optional skip), changed Console tests (`415 passed`), typecheck, build, and browser checks. | No Cost Governance package work remains for these shared host capabilities. |
| 2026-09-15 | implemented | Preserved Cost Governance isolation while shared Pantheon factory and runtime composition gained independently admitted test-context reads and case-derived retention. Njord, package activation, and package authority are unchanged. | `current change`; integrated prediction-learning regression: 650 passed, including factory and runtime checks. | No Cost Governance capability or operational validation state changed. |
| 2026-09-14 | implemented | Replaced a shared Operator incident-email placeholder with a packaged synthetic renderer. The Cost Governance route set, package activation, disclosure, data access, and authority remain unchanged. | `current change`; focused Operator composition passed 94 cases with one optional PDF skip; template parity passed 2 cases; responsive Console Playwright passed 3 cases; Console production build passed. | No Cost Governance implementation or validation state changed. |
| 2026-09-15 | implemented | Separated and enforce-required Var's durable StateStore binding from Thor, Vidar, and Muninn parameters while preserving the shared provider instance as package-neutral lifecycle persistence. | `current change`; Issue #977; focused enforce readiness, Var outbox, Pantheon runtime, Cost Governance boundary, and design-route checks. | No Cost Governance package, activation, data, or authority behavior changed. |
| 2026-09-15 | implemented | Separated and enforce-required Vidar's durable StateStore binding from Thor and Muninn parameters while preserving the shared provider instance as package-neutral lifecycle persistence. | `current change`; Issue #977; focused enforce readiness, Pantheon runtime, Cost Governance boundary, and design-route checks. | No Cost Governance package, activation, data, or authority behavior changed. |
| 2026-09-15 | implemented | Extended package-neutral Pantheon startup recovery to bounded Norns pending handoff-learning work and unified its flush completion path without changing Cost Governance inputs or authority. | `current change`; Issue #977; focused Norns startup, runtime, Cost Governance boundary, and design-route checks. | No Cost Governance package, activation, data, or authority behavior changed. |
| 2026-09-14 | implemented | Extended the package-neutral shared StateStore boundary from Var approval replay to Vidar rollback claims without changing Cost Governance package inputs or authority. | `current change`; Issue #977; focused Vidar, Pantheon runtime, Cost Governance boundary, and design-route checks. | No Cost Governance package, activation, data, or authority behavior changed. |
| 2026-09-14 | implemented | Kept Var's durable final-approval outbox package-neutral when the shared Pantheon runtime injects its StateStore. | `current change`; Issue #977; focused Var, Pantheon runtime, Cost Governance boundary, and design-route checks. | No Cost Governance package, activation, data, or authority behavior changed. |
| 2026-09-14 | implemented | Updated the shared panel-registry expectation for the renamed role workspace while preserving the Cost Governance panel set and package boundary. | `current change`; focused panel registry and FinOps boundary checks. | No Cost Governance package, activation, data, or authority behavior changed. |
| 2026-09-14 | implemented | Made shared model endpoint outputs capability-specific and moved a duplicate startup-probe role into its canonical collection address. Cost Governance package installation, provider inputs, activation, data access, promotion, and execution authority are unchanged. | Failed public-development apply under Issue #936; partner-only Terraform plan, actual old-address refresh, and focused deployment recovery checks. | No Cost Governance package behavior changed. |
| 2026-09-13 | in-progress | Published and deployed the campaign-query correction, restored enabled revision 21, verified the collector effect, and retained the first successful review-only campaign result. The first disable stopped on a PostgreSQL connection timeout without an artifact; recovered database metrics bounded one successful retry. Fresh plans contained no delete or replace. The campaign changed no activation or review state and correctly retained explicit temporal blocks. | Candidate `34747311873`, source `3b4c088ea20d1d77770912394141847bf9940886`, image `sha256:00f769caaf7c1ebe8c09bde3b254ca8c70ef4c2c5d979de037e71ed9575036b0`; disable `34765720805`/`34765992882`, live-authoritative non-replayed revision 19 receipt `sha256:73c2f3722c3279d410fe3fc2b3857915a9c655ca1f81ad6c3d3be31603efa6ae`; Core plan/apply `34766097573`/`34766298596`; Job plan/apply `34766101394`/`34766556857`; upgrade/enable `34766677628`/`34766753818`, revisions 20/21 and pin `sha256:52f6d931121fb9dd842664bca58abd1557ae8202d7d6bd683f66cd58232f1785`; collector `caj-fdai-dev-cost-collect-5qo21d9`, cursor revision 6 and 136 raw observations; campaign `34766958589`, review artifact `sha256:8b7f58d890164d15768e7e07248de0d357be97af89605716bca235f187c8d931`, six readiness-blocked targets, sample count 0, campaign episodes 0, and promotion reviews 0. | Keep issue #903 open until at least 100 eligible live-authoritative action episodes span 30 days with complete audit, dependency, disclosure, settlement, and rollback evidence. Keep issue #904 blocked until `ready=true`, then obtain six independent human reviews. |
| 2026-09-13 | in-progress | Fixed final-pin campaign replay after the qualified retry reached evaluation and PostgreSQL rejected an unqualified `revision` column shared by episode and retention tables. Replay now orders by fully qualified episode columns, with a database read-after-append regression. | Run `34743659442` passed exact-release qualification, exported no observations, then failed before review evidence; Actions artifacts 0 and final-pin episodes 0. `current change`; full Cost validation migration and campaign evaluation suite (`40 passed`), Ruff, and strict mypy. | Deliver the persistence fix, deploy its exact image without changing authority, then retry issue #903 from a new final pin. |
| 2026-09-13 | in-progress | Fixed the first final-pin campaign qualification after its fresh exact-release environment omitted the root dev extra and could not import `yaml`. Both ontology and parity commands now request the lockfile-frozen dev toolchain explicitly. | Failed pre-export run `34742858675` at `Qualify exact active package release`; `current change`; focused workflow contract test. No source batch, import receipt, review result, or runtime mutation was produced. | Deliver the workflow fix and retry the same revision-18 campaign without changing the active package pin. |
| 2026-09-13 | implemented | Corrected independent-review request replay so a retry with a newly generated server time verifies and returns the original immutable receipt instead of conflicting with it. Changed request content or retention duration still conflicts, and persisted tampering fails before reuse. | `current change`; review store Protocol, PostgreSQL adapter, recorder, and focused recorder plus loopback PostgreSQL replay tests (`2 passed`). | Deliver this hardening before issue #903 starts the final stable exact-pin clock; issue #904 remains blocked on `ready=true`. |
| 2026-09-13 | validated | Completed issue #902's live dev N-1 rollback and exact casefold restore under independent Core and Job deployment ownership, then verified the restored collector and unchanged promotion state. | Disable `34738142218`; N-1 Core `34738656368`/`34738866589`; N-1 Jobs `34738668462`/`34739105713`; rollback `34739213368` at revision 13; casefold Core `34739299841`/`34739446082`; casefold Jobs `34739302019`/`34739904514`; upgrade `34740045835` at revision 14; enable `34740149047` at revision 15; collector `caj-fdai-dev-cost-collect-kbom2sn`; cursor revision 2 with 71 observations; unchanged ActionType digest and Workflow `shadow`. | Deliver the review controls before starting issue #903's final stable exact-pin clock. Keep issue #904 blocked until issue #903 returns `ready=true`. |
| 2026-09-13 | implemented | Added content-addressed, per-target Cost Governance review receipts and a protected workflow that verifies one attested final campaign artifact, recomputes readiness with the exact active release, re-reads the pin, and records an authenticated human decision without authority. | `current change`; review provider and PostgreSQL adapter; forward Core migration; review recorder and protected workflow; focused contract, workflow, recorder, Ruff, strict mypy, design-route, and loopback PostgreSQL checks. | Complete the live rollback and restore, accumulate the 30-day and 100-sample campaign, then retain six real review receipts against one campaign evidence digest. |
| 2026-09-13 | implemented | Kept generic service `503` failures visible across shared Console projections while retaining unavailable handling for explicitly classified source-gate failures. | `current change`; `console/src/api-transport.ts`; `console/src/api.ts`; 13 focused Vitest files passed 130 tests; Console typecheck passed. | No remaining Cost Governance implementation work for this shared transport correction. |
| 2026-09-13 | validated | Published and independently deployed the numeric-zero parser release, upgraded it at revision 7, enabled it at revision 8, and fail-safe disabled it at revision 9 after collection exposed the next identity-boundary defect. | Image publication `34729569870` produced `sha256:d6e6bfa10e5764030deb08051bd392242d368e7db8c5f48d9f0dcd71864c0c31` from source `d6af43065f6f5dfbc37481de996dcf437a58d95b`; Jobs plan/apply `34729817071` and `34730027811`; Core plan/apply `34729822095` and `34730200041`; lifecycle runs `34731324436`, `34731424909`, and `34731554943`; collector `caj-fdai-dev-cost-collect-s14tez8` failed with `unknown_service`; pre-enable cursor and observation counts were zero. | Successful collection, previous-release rollback, final enablement, the observation campaign, and independent reviews remain open. |
| 2026-09-13 | implemented | Canonicalized Azure Cost Management `ServiceName` labels before allowlist validation after the first collector for the zero-cost fix rejected valid title-cased provider rows as `unknown_service`. Unknown values still fail before a cursor write. | Failed collector execution `caj-fdai-dev-cost-collect-s14tez8`; `current change`; `azure_focus.py`; focused runtime and Job tests (`29 passed`); Ruff and formatting passed. | Publish and deploy the exact parser fix, then retain a successful independent collector execution before previous-release rollback and final validation. |
| 2026-09-13 | implemented | Preserved numeric zero as a valid Cost Management source fact while keeping absent, null, and blank required fields fail-closed with value-free diagnostics. | Upgraded enable run `34726844732`; failed collector execution `caj-fdai-dev-cost-collect-9faitjg`; `current change`; focused runtime and Job tests (`28 passed`); Ruff and strict mypy passed. | Publish and deploy the parser fix, then retain a successful independent collector execution before rollback and final enablement. |
| 2026-09-13 | validated | Completed the changed-release upgrade and second enable, then disabled after the enable-triggered collector failed independent effect verification. | Upgrade run `34726655163` produced disabled revision 4; enable run `34726844732` produced enabled revision 5; disable run `34727178254` produced disabled revision 6; all receipts were live-authoritative and non-replayed. | Successful collection, previous-release rollback, final enablement, and the observation campaign remain open. |
| 2026-09-12 | implemented | Corrected the earlier provider-catalog packaging assumption after both Core image profiles began carrying the same bootstrap catalog. Catalog changes now select both images, and the provider-only request binds the active profile without adding the Job to the Cost Governance target or activation state. | `current change`; image selector, protected request binding, and focused image, workflow, and CLI regressions | Publish and deploy the exact Cost Governance candidate, then retain the provider-schema plan, apply, and durable generation evidence separately from package lifecycle evidence. |
| 2026-09-12 | implemented | Hardened the release-guard repair after independent code review by proving downgrade then re-upgrade preserves the registered function owner, security-definer mode, search path, and ACL metadata. | `current change`; complete Cost validation migration suite passed 12 tests against disposable PostgreSQL; service migration inventory passed 67 tests; Ruff and formatting passed. | This code review is not W7 independent promotion evidence; deployed migration and successful lifecycle retry remain open. |
| 2026-09-12 | implemented | Added a forward Core migration that disambiguates JSONB subtraction in the retained-release guard without rewriting the applied lifecycle migration. The regression executes install, enable, disable, same-version changed-digest upgrade, and retained-release rollback. | Failed lifecycle run `34724591808`; `current change`; complete Cost validation migration suite passed 11 tests against disposable PostgreSQL; service migration inventory passed 67 tests; Ruff and formatting passed. | Merge and deploy the migration, verify activation remains at disabled revision 3, then retry the exact upgrade with a new idempotency key. No upgrade receipt is claimed. |
| 2026-09-12 | validated | Completed the protected disable transition for the first installed release without starting another collector execution or changing any ActionType or Workflow mode. | Lifecycle run `34721613076`; live-authoritative activation revision 3 with `available=true`, `enabled=false`, and `replayed=false`; independent Job execution readback. | Publish and deploy the changed release, then complete upgrade, previous-release rollback, final enablement, and the observation campaign. |
| 2026-09-12 | implemented | Added the Cost Management Query API QPU retry header to the normalized delay set so the live `429` path follows Microsoft's documented backoff signal. | `current change`; Microsoft Learn Cost Management automation rate-limit contract; focused runtime and Job tests (`26 passed`); Ruff and strict mypy passed. | Publish and deploy this exact source before claiming that the observed QPU throttle recovers live. |
| 2026-09-12 | implemented | Removed an unreachable retry-loop guard after independent code review and added explicit coverage for a `429` without provider retry guidance. | `current change`; focused runtime and Job tests (`26 passed`); Ruff and strict mypy passed. | Live recovery evidence remains unchanged. This review is not an independent W7 promotion review. |
| 2026-09-12 | implemented | Added one provider-directed, deadline-bound recovery attempt for read-only Azure Cost Management `429` responses. The transport selects the longest valid standard or Cost Management retry delay, while missing guidance, deadline exhaustion, or retry-budget exhaustion remains an explicit failure without a cursor write. | Failed enable-triggered collector execution after lifecycle run `34720045184`; `current change`; `azure_focus.py`; `job_cli.py`; focused runtime and Job tests (`25 passed`); Ruff and strict mypy passed; lockfile-frozen wheel and source distribution build passed. | Publish and deploy the changed exact release, then retain an independent successful collector readback before continuing the remaining lifecycle transitions. The failed execution is diagnostic evidence, not operational validation. |
| 2026-09-12 | implemented | Corrected the protected lifecycle wheel rebuild to use the pinned toolchain's supported lockfile-frozen environment contract. Unsupported build invocation now remains a pre-authentication, pre-mutation failure instead of blocking every live lifecycle transition. | Failed lifecycle run `34717708656`; `current change`; `.github/workflows/cost-governance-lifecycle.yml`; focused wheel build, lifecycle workflow, privileged-workflow security, Ruff, formatting, and diff checks. | Merge the fix through required CI, then rerun install from the exact merged revision. No live lifecycle receipt is claimed. |
| 2026-09-12 | implemented | Kept Cost Governance package availability and enablement independent while the shared Operator promotion-gate projection joined catalog rows to durable per-ActionType mode for read-only display. | `current change`; focused Operator suites passed 119 tests with one optional PDF skip; Console promotion-gate tests passed 6 cases and typecheck passed. | No Cost Governance package activation, data access, promotion, or execution authority changed. |
| 2026-09-12 | implemented | Reused the root-owned Job isolation pattern for provider-schema delivery without adding that Job to the Cost Governance target, image profile, package activation, data access, or authority. | `current change`; provider root Terraform, exact target, and focused deployment checks. | No Cost Governance package work remains for this shared deployment-boundary change. |
| 2026-09-12 | implemented | Moved the two optional package Jobs from the broad compute module to root ownership while preserving their prior Terraform addresses with `moved` blocks. Existing environment and inventory identity bindings now resolve through read-only data sources, so package planning cannot inherit scheduler, network, or database changes. | `current change`; `infra/cost_governance_jobs.tf`; focused Job, target, workflow, and Terraform validation checks; failed protected plan `34687002689` exposed the prior dependency closure and produced no apply or retained plan artifact. | Publish the isolated ownership change, retain a three-address zero-destroy plan and exact apply, then verify the independently deployed Core and both Jobs share one image digest before lifecycle installation. |
| 2026-09-12 | implemented | Confirmed that packaging the reviewed provider-schema catalog in the shared Core image does not select the Cost Governance image, enable the package, add package resources, or change promotion state. | `current change`; Core Dockerfile and focused service-image and provider-schema deployment checks. | No Cost Governance package work remains for this shared image-closure change. |
| 2026-09-12 | in-progress | Added the protected exact-release lifecycle producer and hardened request replay across install, enable, disable, upgrade, and rollback. Canonical request digests bind all release and settings inputs without joining package activation to access or action mode. | `current change`; lifecycle workflow, CLI, forward migration, disposable-PostgreSQL replay tests, and protected-workflow checks. | Run the workflows from a pushed required-CI SHA and retain live install, enable, disable, upgrade, and previous-version rollback receipts. |
| 2026-09-12 | implemented | Aligned the service-image trigger contract with candidate-only publication so a root README-only change does not request image packaging. | `current change`; service-image, selector, and deployment CLI checks (`66 passed`); CI-contract checker. | No FinOps package behavior changed. |
| 2026-09-12 | implemented | Moved image publication and SBOM/provenance production to explicit protected-main candidates while retaining targeted PR packaging scans. This changes shared delivery timing, not Cost Governance installation, activation, or authority. | `current change`; selector, CI-contract, Genesis application, supervisor, and image tests passed 182 cases; protected CI-contract checker passed. | Retain candidate publication evidence only when an external run is authorized. |
| 2026-09-12 | implemented | Bound candidate workflow titles to the selected image set and made Genesis dispatch and reuse only the three required application images for the exact protected source revision. | `current change`; Genesis application and image helpers; focused workflow and application checks (`176 passed`). | No FinOps package behavior changed. |
| 2026-09-12 | implemented | Moved the shared Core bootstrap import smoke after runtime asset copies so image validation observes the packaged config and rule catalog without adding optional Cost Governance inputs or authority. | `current change`; local Core runtime image build and bootstrap import passed. | No FinOps package behavior changed. |
| 2026-09-12 | implemented | Confirmed that the exact-registry `AcrPush` assignment for the stable deployment host is a shared image-delivery permission. It is not a Cost Governance package input and cannot install, enable, promote, or grant data access to the optional package. | `current change`; root Terraform role scope; standalone signed-image import and readback boundary | No FinOps package behavior changed. |
| 2026-09-11 | implemented | Confirmed that registering the route-local Approvals English catalog in the shared Console static key guard does not add or activate Cost Governance behavior. | `current change`; `console/src/i18n/catalog-usage.test.ts`; full Console unit suite (`2906 passed`). | No FinOps package behavior changed. |
| 2026-09-11 | implemented | Clarified that the global runtime-call evidence source flag remains outside Cost Governance package inputs and activation. | `current change`; root Terraform runtime-call source control and focused infrastructure contract check. | No FinOps package behavior changed. |
| 2026-09-09 | implemented | Advanced the shared OPA `golang.org/x/crypto` override to `v0.56.0` without changing optional Cost Governance package activation, ownership, data access, or authority. | `current change`; failed supply-chain run `34398041249`; three-image pin parity test; local Core image build, module metadata check, and MEDIUM/HIGH/CRITICAL Trivy scan passed. | Publish the exact Core and Cost Governance images and retain a passing supply-chain receipt. |
| 2026-09-08 | implemented | Confirmed that the shared lockfile rebuild trigger preserves optional Cost Governance isolation when Core adds a direct runtime dependency. | `current change`; Core service manifest, Dockerfile bootstrap import smoke, lockfile, and focused service-package/image checks. | No Cost Governance package work remains for this Core image-closure correction. |
| 2026-09-06 | implemented | Confirmed that platform-owned initial PostgreSQL credential generation does not enter the optional Cost Governance package boundary or grant package credential access. | `current change`; root Terraform credential tests and package-boundary design review. | No Cost Governance package work remains for this platform-host clarification. |
| 2026-09-05 | implemented | Confirmed that the shared ontology invalidation replay header changes no Cost Governance route, activation, or data access. | `current change`; focused Operator CORS checks passed. | No Cost Governance package work remains for this host-seam clarification. |
| 2026-09-05 | implemented | Confirmed that the shared Operator handover proposal decorator is isolated to web conversation proposals and does not wrap Cost Governance routes, activation, or data access. | `current change`; focused Operator handover and service migration checks passed. | No Cost Governance package work remains for this host-seam clarification. |
| 2026-08-31 | implemented | Added Owner-controlled exact-revision activation, immutable analytics snapshots, package-neutral provider discovery, and bounded restart hydration from complete retained USD observations. Core reconstructs Njord's advisory baseline and conversational evidence without republishing historical findings or importing the optional package. | `current change`; activation and analytics migration, `cost_governance_activation.py`, PostgreSQL Cost Governance stores, Njord runtime binding, package entry point, focused package-boundary and hydration tests (`30 passed`), strict mypy and Ruff. | Retain governed live lifecycle, production cutover, and previous-version rollback receipts before removing the compatibility facade. |
| 2026-08-29 | in-progress | Aligned the Operator Cost Governance projection with its JSON scope representation and direct psycopg connection contract. | `current change`; Operator reader and focused test paths. | Record the focused Operator test result before raising the implementation-scope state. |
| 2026-08-29 | implemented | Confirmed that restoring the shared authoritative conversation fallback stream and the Azure Monitor contracts facade does not alter optional Cost Governance activation, publication, or ownership. | `current change`; focused semantic composition, Azure Monitor, and facade-only checks (`12 passed`). | No package-boundary work remains for this host correction. |
| 2026-08-29 | implemented | Advanced the package semantic profile and W6 parity corpus to the new active ontology release after additive Approval and Decision `1.1.0` declarations changed the release identity. Refreshed package-resource, profile, manifest, and parity digests together. | `current change`; Cost Governance semantic profile, package manifest, W0 inventory, F1-F8 and W6 fixtures; focused semantic-profile, convergence, and authoritative-catalog checks (`45 passed`). | Governed W7 lifecycle and rollback evidence remains unchanged. |
| 2026-08-29 | implemented | Clarified that shared service-contract exports, Operator composition, and Console catalogs remain multi-capability host seams. An independent Azure Monitor binding cannot activate or become owned by the optional Cost Governance package. | `current change`; rebased platform integration paths and the Cost Governance design owner; design-route, translation, and pre-push checks. | No additional package-boundary implementation is required for this host-seam clarification. |
| 2026-08-28 | in-progress | Corrected the stale W6 scope row and recorded the first protected W7 deployment attempt. The hardened implementation merged to `main`, exact main CI and the image publication passed, but protected model capability quorum failed before Terraform planning. | PR #328; merge `6b638db7c6fb928372cd31b4ad8371e3f9ea683a`; main CI `33175385098`; container publication `33175384996`; protected run `33175771735`. | Restore protected model capability quorum under a new hypothesis, then retain live lifecycle, production cutover, and previous-version rollback receipts before facade removal. |
| 2026-08-28 | implemented | Completed W2 with the independent package, inert candidate assets, generic immutable Core contracts, disabled-first atomic activation, and a supply-chain-selected Core distribution image profile. | `current change`; 17 package and Core lifecycle tests passed; 21 image and supply-chain tests passed; Ruff and strict mypy passed; wheel and source distribution built; direct wheel and source-distribution rebuild each loaded 38 verified resources. | Keep compatibility cutover, parity, upgrade, and previous-version rollback in W6; retain governed image and lifecycle receipts in W7. |
| 2026-08-24 | not-started | Defined the ontology-grounded package boundary and adopted an evidence-bounded ledger without claiming package implementation. | `current change`; owner document, paired translation, route, size, link, and roadmap tracking checks. | Deliver W0-W7 in the linked delivery plan and retain exact-revision lifecycle evidence. |
| 2026-09-15 | implemented | Source-only refresh for [#946](https://github.com/dotnetpower/fdai/issues/946) / [PR #1014](https://github.com/dotnetpower/fdai/pull/1014) after existing ActionType edits changed the ontology release. Refreshed the exact release/profile, packaged profile-byte SHA-256, manifest reference, one W6 source-inventory SHA, and all 16 F1-F8 fixture references. All eight positive and eight negative expected outcomes are unchanged, including the F6 negative case's deliberately foreign release. | `current change`; [generator](../../../scripts/catalog/refresh-release-derived-pins.py); [semantic profile](../../../extensions/cost-governance/src/fdai_cost_governance/resources/semantic-profile.json) pins release `sha256:b03904b29d8dd7d82882654f8723849ba425c44454c2ba60a45efe318aa24414` and canonical profile `sha256:d568f90288439ad02257c1b4a223e0057421be0dbb06aa5b52dbf685391d83b7`. Implementation-session Cost and generator selection: `46 passed` after correcting two exact expected-pin assertions; not rerun for this documentation edit. | [Normal CI run 34921323157, attempt 1](https://github.com/dotnetpower/fdai/actions/runs/34921323157/attempts/1) failed at head `c8edd`; the local repair awaits a new commit and exact-head CI. No W7 live evidence, enablement, data access, mode, or promotion changed. Issues #903 and #904 remain open; earlier live receipts do not qualify this new source release. |

### Remaining work

- [x] Complete the W0 asset and identifier inventory with exactly one future owner per item.
- [x] Build `fdai-cost-governance` wheel and source distributions that load all 38 resources without
  repository-relative package reads.
- [x] Implement ontology-bound vertical manifest and bundle validation that leaves the active
  runtime unchanged on every tested installation and activation failure.
- [x] Prove local atomic upgrade and previous-version rollback on one exact package and ontology
  release during W6.
- [x] Complete issue #902's live dev N-1 rollback, exact casefold restore, revision-15 enable,
  independent collector completion, durable cursor readback, and promotion-state comparison.
- [ ] Run issue #903 on the final stable exact pin until at least 100 eligible samples span at least
  30 days and the attested campaign returns `ready=true`.
- [ ] After issue #903 is ready, retain issue #904's six one-target review receipts against one
  campaign evidence digest before any separately approved promotion change.
- [ ] Retain production cutover and W7 review evidence before removing the deprecated compatibility
  facade.
