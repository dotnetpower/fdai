# Multi-Service Repository Layout implementation ledger

This delivery ledger tracks the physical workspace and package boundaries while the roadmap owner
remains focused on the normative repository layout.

## Implementation status

### Implementation scope
| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Core-owned T2 cache persistence | implemented | Core migration; `delivery/persistence/postgres_t2_cache.py`; service rotation command; focused local PostgreSQL checks | The Core distribution owns the single writer, exact partitions, catalog state, and atomic receipts without adding a service or cross-service implementation import. |
| Validated five-service baseline | validated | Five baseline `services/*/pyproject.toml` manifests; `config/independent-services.json`; retained remote transition evidence | Each validated service owns its source package, tests, image, process identity, migration branch, and rollback evidence. |
| System Knowledge Service candidate | implemented | `services/system-knowledge-service/`; root workspace and lock; focused service distribution, image, and wheel checks | The sixth read-only distribution owns its package, tests, image, and release catalog. Production identity, persistence volume, and rollback evidence remain with the graduation owner. |
| Core cryptographic runtime dependency | implemented | `services/core-control-plane/pyproject.toml`; `uv.lock`; signed observation tests | Core alone owns the Ed25519 verification dependency. The signing seed remains a deployment secret reference and no cross-service implementation import is introduced. |
| Core Azure runtime dependency closure | implemented | `services/core-control-plane/pyproject.toml`; `uv.lock`; Core Dockerfile bootstrap import smoke; focused service-package checks | Core declares `azure-core` directly, and image construction imports the production bootstrap before publication. |
| Shared service-contract SDK | implemented | `packages/service-contracts/pyproject.toml`; `packages/service-contracts/src/fdai_service_contracts/`; independent-service gate | Versioned wire contracts are packaged separately and do not import service implementations. |
| Root workspace coordination | implemented | Root `pyproject.toml`; `uv.lock`; service-owned manifests | The root coordinates development tooling and integration; it does not publish an FDAI runtime distribution. |
| Root standalone CLI test dependencies | implemented | `pyproject.toml`; `uv.lock`; `test_root_cli_test_dependencies.py`; focused progress, terminal, and Genesis tests (`105 passed`) | Root regression and integration collection directly install Rich and pyte through the development extra, while the CLI retains its own lock and stays outside the uv workspace. |
| Root Core telemetry test dependency | implemented | Root `pyproject.toml`; `uv.lock`; exact Application Insights telemetry test; focused mypy check | Root regression collection mirrors the Core-owned Azure Monitor Distro only for tests. The Core manifest remains the runtime owner and the root remains non-installable. |
| Cross-service implementation isolation | implemented | `uv run python scripts/quality/architecture/check-independent-services.py` (`top_level_source=0`, `service_forbidden=0`) | Services communicate through versioned contracts and owned persistence or event-bus surfaces rather than implementation imports. |

### Implementation history
| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-16 | implemented | Mirrored the Core-owned Azure Monitor Distro into the non-installable root CI test environment and scoped mypy's untyped-import exception to the exact provider module without moving runtime ownership. | `current change`; root `pyproject.toml` and `uv.lock`; failed CI run `35130573269`; the exact telemetry test and focused mypy check pass. | Complete protected CI; exact image rollout and live telemetry evidence remain separate. |
| 2026-09-13 | implemented | Repaired root CI collection of standalone deployment CLI tests by declaring the renderer and terminal-emulation dependencies in the root development extra. | PR #899 run `34731319854` and inherited PR #908 run `34738342740`, attempt 1: missing Rich before test execution; `current change`; the focused dependency regression failed before the fix and 105 dependency, progress, terminal, and Genesis tests passed afterward. | Retain exact-head CI for the repaired PRs; no runtime or package activation changed. |
| 2026-09-10 | implemented | Restored root pytest import discovery for the System Knowledge Service and pinned every registered service source root through a regression check. | `current change`; root pytest configuration, focused service-distribution regression, and System Knowledge Service test collection | No physical-layout work remains for this bounded test-discovery correction. |
| 2026-09-10 | implemented | Restored clean-checkout test collection for the System Knowledge Service candidate and synchronized service-image, OPA, venue-scan, and sharded-coverage CI contracts with the six-package repository layout. | `current change`; root pytest configuration, CI runner, structural contract tests, and focused System Knowledge Service checks. | Production graduation evidence remains with its existing owner and was not changed. |
| 2026-09-09 | implemented | Added the independently packaged System Knowledge Service without changing the retained five-service deployment baseline. | `current change`; service manifest, workspace lock, Dockerfile, wheel build, image build, and focused ownership checks. | Complete production graduation evidence before adding it to the validated independent-service manifest. |
| 2026-09-08 | implemented | Added the missing Core-owned Azure runtime dependency and made image construction cold-import the production bootstrap. | `current change`; Core service manifest, lockfile, Dockerfile, and focused package/image checks. | Retain one healthy protected Core apply for the corrected image. |
| 2026-08-31 | implemented | Added T2 cache TTL, exact catalog partitions, promotion/rollback state, and atomic rotation receipts inside the existing Core distribution. | `current change`; migration, Core persistence adapter and command, 13 focused local PostgreSQL checks, 58 service-migration ownership checks, and the independent-service boundary check. | No service-boundary work remains for this cache lifecycle. |
| 2026-08-31 | implemented | Added Core-owned Ed25519 observation verification without changing the five-service distribution inventory or introducing a cross-service implementation import. | `current change`; Core manifest and lockfile; focused signed-observation tests; `check-independent-services.py` passed. | No physical package-boundary work remains for this dependency. |
| 2026-08-24 | implemented | Adopted a focused physical-layout owner after the five-service extraction and separated it from module boundaries, dependency injection, and repository conventions. Earlier decomposition provenance remains in the service-decomposition ledger and was not reconstructed here. | `current change`; five service manifests, shared SDK manifest, root workspace manifest, and `check-independent-services.py` passed with `services=5`, `top_level_source=0`, and `service_forbidden=0`. | No physical package-boundary work remains in this owner. Deployment promotion evidence remains with the service-decomposition and graduation owners. |

### Remaining work
- [x] Keep the validated five service distributions, shared contract SDK, root workspace-only manifest, and cross-service implementation-import prohibition pinned by `check-independent-services.py`.
- [ ] Add the System Knowledge Service to the validated independent-service manifest only after its
  identity, Blob claim, provider canary, cost, disable, and rollback receipts exist.
