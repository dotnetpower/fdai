# Project Structure Dependency Injection Implementation Ledger

This ledger tracks the documentation ownership split for [Project Structure Dependency Injection](../../roadmap/architecture/project-structure-dependency-injection.md).

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Default isolated instance runtime injection | in-progress | `ontology_index_runtime.py`; default semantic, Pantheon, control-loop and task assembly; focused runtime/composition checks | Controlled embedding identity gates adapter construction. Authenticated enrollment and bounded supervised reconciliation preserve owned events. Exact-ID reads are available after audited preparation; unqualified semantic ranking and candidate-specific terminal presentation remain gated. |
| Exact-release instance query injection | implemented | `semantic_query_instance_candidates.py`; semantic and Azure composition; 61 focused composition tests passed | Explicit callback injection preserves source-derived declarations, read sets, principal context, and no-authority output. Default bootstrap, preparation, and live quality remain outside this binding; see the query implementation ledger. |
| Optional extension package facades | implemented | `config/package-assurance.json`; Code Assurance and Cost Governance package facades; independent-service and package-assurance checks | Reviewed authority-neutral factories, resource loaders, readiness types, and adapters can be public to composition while Core imports remain prohibited. |
| Documentation ownership | not-applicable | `current change`; `docs/roadmap/architecture/project-structure-dependency-injection.md` and its Korean pair; document-size and translation checks | This structural split changes no runtime behavior. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-26 | implemented | Clarified that optional extension distributions can expose stable authority-neutral facades to composition without allowing Core imports or coupling public exports to runtime authority. | `current change`; package assurance policy and checker, existing extension facades, synchronized owner documentation. | No new provider, package activation, or runtime authority is introduced. |
| 2026-09-21 | in-progress | Wired the real source/vector/worker runtime through default bootstrap and supervised off-path reconciliation. | `current change`; 178 candidate/composition/bootstrap and 25 runtime-construction tests passed; strict mypy | Complete candidate-only terminal projection and reviewed qualification evidence in the [query ledger](../interfaces/ontology-query-coverage-implementation-plan.md). |
| 2026-09-21 | implemented | Added the explicit instance-candidate callback seam and isolated its exact-release registration from the main composition facade. | `current change`; 61 semantic/adaptive composition tests passed, including real semantic DAG execution and no-provider-I/O Azure forwarding | Production enrollment, activation, and qualification remain tracked by the [query ledger](../interfaces/ontology-query-coverage-implementation-plan.md). |
| 2026-09-20 | not-applicable | Split one oversized roadmap owner into a focused bilingual companion without changing its contract. | `current change`; `docs/roadmap/architecture/project-structure-dependency-injection.md` and its Korean pair | No runtime implementation work is introduced by this split. |

### Remaining work

- [x] Preserve the moved design content and pass the focused documentation gates.
