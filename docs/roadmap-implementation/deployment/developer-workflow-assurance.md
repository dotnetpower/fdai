# Developer workflow assurance implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Self-contained managed restart task | validated | `.vscode/tasks.json`; `restart-console-services.sh`; eight focused task/guard/order checks; actual supervisor replacement, 12/12 readiness and process model-pin readback; two verified authenticated EN/KO canaries | The task owns its terminal directly and allows one bounded replacement invocation without an unattended instance-limit picker. Primary-checkout validation precedes every service effect. The managed supervisor still owns TERM/lock cleanup; no extension binary, authentication mode or runtime execution authority is changed. |
| Local broker housekeeping | implemented | `cleanup-local-broker.py`; `dev-up.sh`; `prepare-local-runtime-env.sh`; focused cleanup, preparation and environment tests | Empty groups require a dead local PID and a fresh membership check. Readiness probes have a separate bounded-retention topic; inactive offsets expire with the ordinary 24-hour topic horizon and a one-minute cleanup check. Existing processes require managed preparation and restart. |
| Shared writes and hooks | implemented | `developer_workflow_repository.py`; workflow tests for overlap and recovery classification | Enforcement remains in existing hooks. |
| Validation and handover | validated | Versioned queue records, bounded latency diagnostics, handover schema v2; current CI scope routing and four-shard regression change passed 78 focused tests plus the CI contract checker; issue #688 fixed 20-run baseline and post-change cohorts | Window uncertainty and malformed state fail closed; authoritative required-leaf queue p95 improved from 38 to 13 seconds while regression-shard p95 improved from 365 to 264 seconds. |
| Campaign continuation | implemented | `install_roadmap_implementation_campaign.py`; shared worktree preparation; 35 campaign and watchdog tests | A timer cycle recreates a deleted registered worktree; the installed legacy unit remains held until explicitly reinstalled. |
| Hermetic checks and local services | validated | Workflow environment preflight, checkout-owned service readiness, focused workflow tests, and a live 12/12 managed-stack readiness check | Managed restart stops the verified same-checkout supervisor before preparation. Readiness requires fresh per-partition primary Core progress and aggregate lag at or below 1,000 without weakening the inventory-coverage or analyzer fences. The diagnostic never starts or restarts a service. |
| Development diagnostic channel | validated | `packages/runtime-diagnostics`; Core and Operator adapters; `semantic_turn_runtime.py`; `scripts/automation/dev-discuss.py`; `config/development-diagnostics-evaluation.json`; focused tests and live digest-bound Core and Operator packets | The standard task-backed local launcher always enables Core and Operator probes. Socket paths remain checkout-private, portable in long worktrees, and bound to the complete runtime identity. Bounded CPU and Python heap capture, digest-bound packets, and explicit Copilot review custody remain separate from product and execution authority. Operator assurance attribution and held states remain product projection data and are excluded from diagnostic capture. The later recorded live packets supersede the earlier bounded startup failure; they do not validate live product attribution. |
| Core StateStore connection lifecycle | validated | `PostgresStateStore`; `RuntimeResources`; focused StateStore, approval-guard, bootstrap, integration, lint, type, lock, and wheel checks; digest-bound Core packet and local PostgreSQL snapshot | One lazy pool reuses at most four Core StateStore connections and closes after dependent runtime resources. The clean local fan-out converged with five database backends including the measurement connection; this is local runtime evidence, not deployed validation. |
| Analyzer StateStore connection lifecycle | validated | `analyzer_tick_cli.py`, `analyzer_tick_cli_composition.py`, `analyzer_run_receipt.py`; focused cleanup tests, Ruff, strict mypy, and a restarted clean local tick | Target-resolution and run-receipt stores close on success and failure before the next local loop interval. The final analyzer epoch reached ready without a pending StateStore worker warning. |
| Browser and editor pressure | implemented | Existing focused Playwright entry points, 10-slot lease pool, and profile pressure controls | Final critique must verify no Medium residual. |
| Remote preflight | implemented | `live_preflight/transport.py`; 6 focused tests | At most three read attempts; permanent errors fail immediately. |
| Ten-round assurance | validated | 13 rounds, final independent re-review, and central receipt for `d3f5257b9` | No residual finding exceeds Low. |
| Developer validation authority | implemented | `.githooks/post-commit`; `.githooks/pre-push`; `scripts/agent/design_context.py`; focused hook, Git-environment isolation, and dispatcher tests | Commits and pushes no longer depend on local queue receipts; CI owns pushed-SHA integration. |
| Local-first validation and candidate publication | in-progress | `scripts/verify.sh`; `scripts/agent/design_context.py`; `scripts/quality/ci/resolve_test_scope.py`; scoped text and changed-test selectors; focused regression tests | Content-based local reuse, bounded guidance, and candidate-only publication are being integrated. Required remote evidence remains independent. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-26 | validated | Verified the shortened, runtime-identity-bound development diagnostic socket path in the long central validation checkout. Required CI and the merged whole-repository batch both passed, including the owner-only socket lifecycle and Core and Operator adapters. | PR #1418; merge `99fa316277`; central `validation-all` at `b546f94b5c6a` passed all gates and drained the queue to 0 reachable pending commits; issue #1417. | No remaining work for the portable development diagnostic socket path. |
| 2026-09-26 | implemented | Shortened the checkout-private development diagnostic socket directory and bound each socket basename to the service, source revision, service input digest, worktree digest, and runtime-scope receipt. Long validation and worktree paths now remain within the 100-byte portable Unix socket limit without sharing socket identity across runtime inputs. | `current change`; `packages/runtime-diagnostics/src/fdai_runtime_diagnostics/config.py`; `scripts/automation/run-local-service.sh`; runtime diagnostics, Core, Operator, and local-launcher focused tests passed 51 cases; Ruff and shell syntax checks passed. | Merge issue #1417 through required CI and record a passing central whole-suite baseline. |
| 2026-09-22 | implemented | Added supervised Rule activation intake and per-replica generation reconciliation to the existing Core task lifecycle. The workers use the production StateStore and EventBus bindings and add no diagnostic socket, capture authority, or restart policy. | `current change`; focused activation runtime and bootstrap supervision tests passed 16 cases. | Retain scaled deployment convergence evidence separately; development diagnostic behavior is unchanged. |
| 2026-09-22 | validated | Verified both task-dispatch repairs through the actual existing VS Code restart task. The managed supervisor was replaced, preparation selected the new observed embedding binding, the complete stack reached 12/12 readiness, and the actual Core process model hash matched its file. Two post-restart authenticated EN/KO object-only manifest queries returned verified evidence with no execution authority. | `fe49eceab6`, `37b886de56`; eight focused checks; sanitized local receipt `sha256:9217d9ce0fa5dc5125b1cdc02181f996bc26cc4ba3dbbe57225695d053845b11`; model version `1`; exact durable Operator projections matched browser receipts. | No remaining blocker for this bounded restart recovery. Concurrent source edits changed the service digest after launch, so this does not certify an immutable current checkout or qualify semantic ranking. The broader ontology quality and final review campaign remains open. |
| 2026-09-22 | implemented | The repaired terminal lookup exposed a second independent blocker: instance limit 1 with default `prompt` opened an unattended restart picker and returned the old task's output. Allowed two bounded task instances with `silent` overflow so the replacement wrapper can stop the verified supervisor through its existing TERM/lock protocol. | `current change`; installed task-service instance-policy branch; actual task returned a terminal but supervisor PID and active model pin remained unchanged; eight focused task/guard/order checks passed. | Verify a replacement invocation changes the supervisor and active model pin; old-process readiness is not restart evidence. |
| 2026-09-22 | implemented | Diagnosed the managed restart tool's empty terminal lookup: when dependencies exist, the installed VS Code task tool queries their terminals instead of the restart task; its lookup excludes the hidden primary-worktree dependency. Moved the same guard into the restart wrapper before any service effect and removed that task dependency. The separate create-task tool constructs a local file URI on the Windows client and cannot create the intended WSL workspace task. | `current change`; read-only installed task-tool control-flow inspection; five workspace-task checks and three isolated shell cases passed, including linked-checkout and Git-failure refusal before effects; shell syntax and ShellCheck passed. | Run the repaired existing task and verify its actual terminal, new supervisor and readiness. Do not infer activation from the health of the old process or use the broken create-task path. |
| 2026-09-22 | validated | Repaired three local startup lifecycle faults: Core settings now reuse the runtime-owned StateStore pool, Pantheon consumer shutdown completes a second bounded finalization phase before dropping task references, and destructive broker reset holds the stack plus all managed service locks. | `current change`; 75 bootstrap/shutdown tests, 60 bridge/layout tests, 15 broker/database tests, focused Ruff and strict mypy; managed restart reached 12/12 readiness with one lock-owned process pair per service and zero repaired error signatures after the new generation marker. | No work remains for this bounded local startup repair. |
| 2026-09-22 | implemented | Added a staged-input fast path to the derived-source pre-commit hook. The hook enters on every commit but runs the complete validation only when a pinned user document, a System Knowledge catalog source, or an owning tool changes. The selector derives source paths from the staged catalog instead of duplicating them in hook configuration. | `current change`; eight focused checker tests cover unrelated-path skip and catalog-derived source detection; unrelated Console source skips the full check while relevant inputs retain complete validation. | No implementation work remains for this bounded hook optimization; CI and direct repository-wide invocation retain the complete check. |
| 2026-09-22 | validated | Removed Terraform initialization and state reads from managed local Console preparation while retaining the explicit Azure CLI read scope, same-checkout supervisor ownership, bounded readiness, and no-local-executor boundary. | `current change`; 112 focused preparation tests; standard stack reached `12/12`; authenticated sweep covered all 61 registered panels without panel failures, page exceptions, or unexpected Operator API error responses. | Actual Azure mutation remains governed separately by durable per-ActionType promotion, approval, safeguards, deployed executor identity, and independent effect verification. |
| 2026-09-21 | validated | Verified the restarted managed analyzer loaded its explicit StateStore decision-evidence binding without weakening readiness or authority. | New process environment key readback, bounded HTTP listener checks, and fresh analyzer receipts. Target resolution remained 23 selected and 18 held because no independently retained analyzer-target admissions exist. | Keep held targets fail-closed until an independently verified admission producer is implemented and reviewed. |
| 2026-09-21 | implemented | Restored the managed local analyzer's explicit StateStore binding so decision-evidence admission is available during target resolution. | `current change`; launcher and analyzer composition checks included in 45 focused passes; the prior process environment lacked the binding. | Restart the committed supervisor generation and retain one clean analyzer tick; no promotion or execution authority changes. |
| 2026-09-21 | implemented | Aligned inactive local consumer-group offset retention with the existing 24-hour topic horizon and reduced the expiry check to one minute without changing active groups or deployed Event Hubs. | `current change`; seven focused local broker policy tests, ShellCheck, and live Redpanda configuration readback. | Observe old startup-probe groups expiring after the configured horizon. |
| 2026-09-21 | implemented | Moved Operator lifecycle ordering and resource cleanup into a focused composition module while retaining identical startup, partial-failure cleanup, reverse shutdown, and readiness behavior. | `current change`; focused lifecycle and readiness tests plus the 120-case composition suite; Ruff and strict mypy passed. | No diagnostic socket, capture authority, restart policy, or runtime topology changed. |
| 2026-09-21 | implemented | Separated asynchronous sleep overshoot from CPU/heap postprocessing so Dev Discuss event-loop lag no longer reports its own capture overhead as scheduler delay. | `current change`; deterministic runtime-diagnostics timing regression; focused package tests, Ruff, format, and strict mypy. | Re-capture one digest-matched Core profile and interpret total measured duration separately from scheduler lag. |
| 2026-09-21 | implemented | Reordered Console and Dev Discuss restart to stop the verified supervisor before preparation and added fresh aggregate primary Core lag to readiness. | `current change`; restart, workspace-task, local-log, and stale/aggregate lag regressions; focused workflow tests, Ruff, strict mypy, and ShellCheck passed. | Retain a digest-stable 12/12 profile packet separately; concurrent Core input edits correctly block Dev Discuss capture. |
| 2026-09-21 | implemented | Added bounded dead-process introspection-group cleanup to local preparation and separated disposable readiness messages from the operational DLQ. Bound the helper into preparation-cache inputs. | `current change`; 78 focused cleanup, startup and environment tests; Ruff and shell syntax checks passed. Local readback confirmed ten obsolete groups removed without changing 110 active groups, and a dedicated probe topic with bounded retention. | Apply refreshed environment through an authorized managed restart and verify new probes use only the dedicated topic. No runtime restart or database retention change is implied. |
| 2026-09-20 | implemented | Added the missing `psycopg-pool` mirror to the root CI development environment without changing runtime image ownership. | `current change`; `pyproject.toml`, `uv.lock`; frozen root dev sync, direct Core persistence import, focused mypy, and six persistence tests passed. | Required CI on the corrected PR head remains authoritative. |
| 2026-09-20 | validated | Recreated the local databases and broker from empty volumes, repaired numeric JSON construction in the ontology writer migration, made concurrent coverage indexes retry-safe after a later migration failure, and closed analyzer target-resolution and run-receipt stores. | `current change`; empty-volume five-service migration completed; 118 migration and analyzer focused tests passed; Ruff and strict mypy passed; the restarted standard stack reached 12/12; Console and design mock servers returned 200; digest-bound Core and Operator snapshots completed; the final analyzer epoch reached ready with no pending StateStore worker warning. A repeated full reconciliation retained one alias and 1,297 ontology Resources with no full journal or Resource Event fan-out; six partial journal rows and eight events were independently classified as `fdai.delivery.inventory_delta`. | No remaining work in this bounded local repair. Deployed migration rollout and production storage evidence remain separate. |
| 2026-09-20 | validated | Replaced per-operation Core StateStore TCP handshakes with one bounded async pool and attached its close to ordered runtime resource cleanup. The restarted profiled runtime recovered one interrupted ingress claim and completed the clean Resource Event fan-out without another Core exit. | `current change`; 42 focused StateStore, approval-guard and shutdown tests, 108 bootstrap tests, 9 isolated PostgreSQL integration tests, Ruff, mypy, lock check and Core wheel build passed; ingress claims converged at `1301/1301`; PostgreSQL held five backends including the measurement connection; Core packet `sha256:b405268353edffecceba4071a1b2969a1fe98e14fef2cef3b967b6239c7aa1a0` completed with 2 ms event-loop lag, 274305024-byte RSS and both authority flags false. | Grouping the current one-observation lifecycle partitions and defining lifecycle retention for snapshot-covered history remain separate Continuous Operational Instance Graph work. |
| 2026-09-20 | validated | Removed authoritative inventory refresh from the managed preparation critical path and preloaded the default Overview route while authentication completes. Standalone preparation remains synchronous, and complete readiness still requires inventory coverage and a clean analyzer tick. | `current change`; focused preparation and workspace-task tests passed; Console startup tests and typecheck passed; live managed preparation emitted `event=deferred` and completed in 26 seconds instead of waiting about 90 seconds for inventory; the stack reached 12/12 readiness; authenticated Overview first-content time improved from about 3.39 seconds to 1.10 seconds; steady Operator packet `sha256:c5aea79e64b11a82d1c10ff52a6a0841102f562c697e14419e39ceae56832d1c` reported 0 ms event-loop lag with unchanged RSS, FD, and thread counts. | No remaining work in this bounded startup path. The separate Core packet remains open below because concurrent Core edits changed its service input digest. |
| 2026-09-20 | implemented | Registered observer proposal consumption as a normal Operator lifecycle/readiness worker without changing diagnostic socket access, capture authority or service restart policy. | `current change`; composition, consumer lifecycle and cancellation tests passed. | Standard-stack restart and authenticated observation remain separately governed; no diagnostic capture was performed. |
| 2026-09-19 | validated | Confirmed the final service-input identity boundary on the repaired standard stack. Both services accepted immediate snapshots at the same source and worktree identity; a later timed capture was correctly rejected after concurrent edits changed actual service inputs. | Core packet `sha256:b3cf1d46043109c2aa599240395d35f0004e97012c335b23c220898e4208a087`; Operator packet `sha256:479e67e4085e5e66451bf6cb29540673d57b7129c19b54ed764a97790d2f150b`; source `20f35529340d2b2672062185ced7a1471dc9bfd4`. | No remaining work in the diagnostic lifecycle and identity repair; steady-state performance characterization remains a separate measurement task. |
| 2026-09-19 | implemented | Corrected the preceding diagnostic identity transition: Git revision is retained as packet provenance but no longer invalidates a healthy process when only unrelated repository content changed. The canonical service digest now includes runtime-diagnostics source and alone governs runtime reuse and capture admission. | `current change`; focused revision-only reuse, service-input drift, digest-only launcher, and capture-admission regressions. | Restart after this correction and confirm both services still capture across an unrelated commit. |
| 2026-09-19 | validated | Confirmed the repaired standard local stack exposed live Core and Operator diagnostic endpoints and accepted source- and service-input-bound snapshots while unrelated worktree edits continued. | Core packet `sha256:6d0466ed33049d7d2d95719132114669f0898352018cbac22e1158bd8068dcb8`; Operator packet `sha256:04c45b181a7beffa11f424d0bb30960b096d108c4b32d2c1d44696560912feca`; 95 focused integration tests passed. | No remaining work in the bounded diagnostic lifecycle repair. |
| 2026-09-19 | implemented | Corrected the diagnostic identity boundary after live capture showed that unrelated concurrent edits invalidated healthy processes. Capture and reuse now compare the Git revision and canonical service input digest, while the full worktree digest remains packet and Copilot-review provenance. | `current change`; `scripts/deployment/local/run-console-service.sh --print-input-digest`; `scripts/automation/dev-discuss.py`; focused capture and reuse regressions. | Capture one digest-matched packet from Core and Operator after restarting the repaired stack. |
| 2026-09-19 | implemented | Replaced path-only diagnostic status with a bounded protocol probe and added an owner-verified supervisor lock handoff for the explicit `dev discuss` restart task. | `current change`; `scripts/automation/dev-discuss.py`; `scripts/deployment/local/start-console-services.sh`; focused stale-socket and supervisor-replacement regressions. | Start the repaired task and capture one digest-matched packet from Core and Operator. |
| 2026-09-19 | implemented | Bound reuse of a profiled local service to its Git revision and worktree digest after live verification exposed reuse of a pre-commit process. | `current change`; `scripts/automation/run-local-service.sh`; `tests/integration/scripts/test_run_local_service.py::test_runner_rejects_diagnostic_reuse_when_worktree_changes`. | Restart the standard stack and confirm both digest-matched sockets capture successfully. |
| 2026-09-19 | implemented | Made development diagnostics mandatory for Core and Operator processes started through the standard local service launcher. Generated environment values can no longer disable the local diagnostic sockets, and the dedicated `dev discuss` restart task now uses the same launcher contract. | `current change`; `scripts/deployment/local/run-console-service.sh`; `tests/integration/scripts/test_start_console_services.py`; focused launcher regression. | Restart the standard stack and capture one digest-matched packet from each service. |
| 2026-09-19 | implemented | Kept answer-generation and evaluator-model attribution plus held-state validation in the Operator product projection while explicitly excluding those fields from development diagnostic probes, packets, exports, and Copilot review. | `current change`; `services/operator-service/src/fdai_operator_service/families/conversation/semantic_turn_runtime.py`; 470 focused Python tests and 63 focused Console tests passed. | No remaining work in this bounded projection-to-diagnostic boundary clarification. |
| 2026-09-19 | validated | Bounded local analyzer recovery after a transient active-generation and ontology-checkpoint mismatch to five seconds instead of the full loop interval. | `current change`; focused 60-second-loop regression passed; the profiled Console recovered to 12/12 readiness after the next generation turnover. | No remaining work in this bounded recovery repair. |
| 2026-09-19 | validated | Completed the profiled startup repair by recognizing the service-owned Core executable in local readiness. | `current change`; focused owner-detection regression passed; the running profiled Console reported 12/12 services ready with no unavailable component. | No remaining work in this bounded startup repair. |
| 2026-09-19 | implemented | Repaired profiled Console startup by indexing scope-bound inventory coverage, preserving runtime-scope receipts through direct local entry points, accepting fresh semantic-consumer progress in bounded readiness logs, and replacing stale VS Code task instances. | `current change`; 237 focused tests passed with 7 optional skips; strict mypy, Ruff, ShellCheck, migration validation, successful authoritative inventory preparation, and two live digest-bound Core and Operator profile imports. | Record one clean committed full-stack `ready` event and keep that profiled supervisor running. |
| 2026-09-19 | implemented | Completed the local development diagnostic channel, closed every reproducible finding above Low across 13 adversarial rounds, and corrected a real source-attribution defect found by the first live packet before completing 20 digest-bound Copilot question and import rounds. | `current change`; `packages/runtime-diagnostics`; Core and Operator adapters; `scripts/automation/dev-discuss.py`; `config/development-diagnostics-evaluation.json`; focused runtime, coordinator, service, launcher, and workspace-task tests; owner-only UDS evaluation packet `sha256:c70521a6f3381dcf5f3459ac8105433aeb586f1af1d45186f466c4aebcb62ee4`; 20/20 reviews passed all five evaluation criteria. | Capture one digest-matched packet from each full Core and Operator service after the local authoritative-inventory preparation path can complete within its bounded readiness attempt. |
| 2026-09-19 | in-progress | Defined an explicitly activated local development diagnostic channel that binds bounded runtime profiling to exact source inputs and separates GitHub Copilot review from runtime authority. | `current change`; paired developer workflow owner and document-size gate. | Implement Core and Operator probes, coordinator, packet custody, focused tests, at least 10 hardening rounds, and 20 evaluated diagnostic questions. |
| 2026-08-15 | in-progress | Adopted the developer workflow assurance owner and bounded the campaign; earlier implementation provenance was not reconstructed. | Current change and existing controls listed in the scope table. | Complete the focused rounds and final residual-risk review. |
| 2026-08-15 | in-progress | Revised the design after independent critique by defining the CLI contract, bounded evidence windows, failure behavior, authority separation, and 12-round sequence. | Current change; roadmap, translation, and punctuation checks. | Implement and verify each accepted finding. |
| 2026-08-15 | implemented | Completed 13 critique-and-hardening rounds and removed every reproducible Medium-or-higher residual. | Current change; 163 focused Python control tests, 6 Playwright port-pool tests, 48 final false-ready tests, Ruff, and the final independent review. | Record the centralized validation receipt for the integrated revision. |
| 2026-08-15 | validated | Central validation accepted the integrated implementation revision. | `validation_queue.py check-commit d3f5257b9` passed. | No remaining work in this bounded campaign. |
| 2026-08-15 | in-progress | Started the measured Top 20 residual campaign. | Issue #118 and the baselines in the residual campaign table. | Complete at least 10 additional critique rounds and central validation. |
| 2026-08-15 | implemented | Completed 14 additional critique and hardening rounds for ranks 11 through 20. | Current change; the Top 20 assurance results above, focused tests, Ruff, and strict mypy. | Integrate the exact revision, obtain central validation, and complete issue #118. |
| 2026-08-15 | implemented | Added two review-evidence rounds that pin validator scratch ownership and focused automation test selection. | Current change; validator and selector suites passed 85 tests. | Integrate the exact revision, obtain central validation, and complete issue #118. |
| 2026-08-15 | implemented | Closed integration-level bypasses in the real pre-tool dispatcher and validator scratch symlink boundary. | Current change; 19 dispatcher and guard tests plus 2 scratch ownership tests passed. | Obtain exact central validation and complete issue #118. |
| 2026-08-15 | implemented | Closed four adversarial parser rounds covering commit scope, approval comments, Git aliases, config-env option forms, and symlink ancestors. | Current change; 40 dispatcher and parser fixtures plus 3 scratch ownership guards passed, and independent acceptance found no residual above Low. | Obtain exact central validation and complete issue #118. |
| 2026-08-15 | validated | Central validation accepted the final Top 20 implementation and assurance ledger revision. | `validation_queue.py check-commit 4a18ce982` passed; the final focused join passed 221 tests. | Complete issue #118 and synchronize the project board. |
| 2026-08-15 | in-progress | Started the measured bounded-wait campaign for issue #122 after session evidence showed 51 wait complaints across 36 sessions. | Issue #122 and the baselines in the bounded wait campaign table. | Implement the bounded budgets and complete at least 10 critique rounds. |
| 2026-08-15 | implemented | Bound the assurance checkpoint to an evidence identity instead of the full run configuration, so a per-run session id or an adjusted pacing, deadline, or retry knob no longer discards completed turns, and attributed every retained result to its producing run. | Current change; console suite 1793 passed and TypeScript project check clean. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Added the target stack origin to the checkpoint binding, stopped persisting budget-derived non-results as permanent failures, constrained the run-budget override to the declared envelope, derived retry counts from retained evidence, validated retained result shape, and corrected six overclaiming ledger rows. | Current change; live-evidence Vitest passed 89 cases and TypeScript project check clean. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Separated the stalled-question bound from the run-budget bound behind a tested policy, forwarded the ignored result predicate on the checkpoint read path, disclosed the question that stopped a budget-limited run, and extended the Console typecheck gate to `console/tests`, which exposed and fixed a real type break this campaign had introduced in an untypechecked spec. | Current change; live-evidence Vitest passed 95 cases; `npm run typecheck` now covers `console/tests` and passed. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Corrected the preceding row: that change extended the Console typecheck script but not the enforced gate. This change runs `npm --prefix console run typecheck` inside `run-operator-surfaces.sh`, publishes the bound that actually truncated each expired attempt, renames that outcome to `per_attempt_deadline_exceeded`, and lets a transient turn error use its remaining attempt. | Current change; live-evidence Vitest passed 98 cases; full Console suite passed 1802 cases; `npm run typecheck` passed. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Removed the resume trap in which a cohort completed but interrupted before publish could never pass and lost its checkpoint: a fully resumed cohort now publishes as `run_mode: resumed_replay`, retirement requires publication, the enforced gate runs only the tests project to avoid a duplicate application typecheck, a parity test pins that gate line, and the artifact counts stalled questions separately. | Current change; live-evidence Vitest and typecheck-parity suites passed. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Closed the replay authority hole the preceding row opened: a run that verified nothing live now publishes as `receipt_source: resumed_replay`, can never be production-ready, and an interrupted run is no longer mislabelled a replay. Also validated every retained field the pass criteria read, scoped the transport retry counter, keyed the checkpoint on evidence identity, and bumped the artifact schema. | Current change; live-evidence Vitest passed 102 cases; `npm run typecheck` and typecheck-parity passed. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Made a resumed cohort prove the stack it reports: a run completed entirely from a checkpoint releases its last question back to the live stack, and every retained answer must describe the same ontology release and principal manifest generation, so stale answers cannot ride along with one live turn. | Commit `344c445b8` and rank 16 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Made that live proof falsifiable: the released tail now runs through the last answer-required question, live authority requires an answered turn bound to an ontology release, republished generations must be confirmed by live answers, a completed but failing cohort keeps its checkpoint, the run budget covers its own preamble, and the checkpoint-trust, pass, retirement, and path policy moved out of the ungated spec into tested pure functions. | Commit `d9adeccb2` and rank 17 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-15 | implemented | Made a resumed cohort converge: only verified turns resume, exactly one answer-required question is released as live proof, every preamble step carries its own bound, a selection that cannot prove a generation is rejected, and retained locale, operation, and attempt outcomes are validated as governed values. | Commit `11635c075` and rank 18 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-16 | implemented | Released a proof question that can prove: the released question is the last one an earlier run answered with a generation digest, a complete cohort retires its checkpoint only when the asserted outcome holds, an abandoned turn no longer leaks an authenticated stream onto the reused page, a missing receipt reports its transport outcome, and the retained result types are shared with the runner. | Commit `d9eed948d` and rank 19 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-16 | implemented | Made every detected fault recoverable: a mixed-generation checkpoint is discarded instead of refused forever, the checkpoint file is keyed by the whole binding so alternating revisions or target stacks keep separate resume state, a page whose execution context cannot be reset stops the run, and adding `console/scripts` to the typecheck project exposed and fixed a real type break. | Commit `6c2f0848b` and rank 20 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-16 | implemented | Restored the gate that never ran: `npm exec` kept the caller's directory, so the operator gate requested `tsconfig.tests.json` from the repository root, failed with TS5058, and under `set -e` took the console build and both CLI checks with it. The gate now names the project relative to the repository root, the parity test pins that form, a truncated run keeps a valid checkpoint, only turns superseded by a mid-run release rotation are pruned, an incoherent governed refusal fails, and the partial checkpoint file is per-process. | Commit `1782464e4` and rank 21 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-16 | implemented | Stopped a blocked cohort from replaying itself: answer-required turns that only refused are released so a later run re-attempts them, a pacing fault or failed checkpoint write becomes a governed stop reason instead of escaping without an artifact, the parity test pins the whole invocation, and an operator-configured checkpoint path is trimmed. | Commit `ddf1e47f5` and rank 22 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-16 | implemented | Guarded the whole question rather than only its pacing: pacing, the intra-question retry wait, and the turn share one stop path, so a page fault during a retry wait or under the permitted zero pacing override still produces an artifact; a budget-truncated attempt no longer spends a reload it cannot use, and a budget stop names the question it stopped on. | Commit `62f68be0e` and rank 23 of the bounded wait campaign table. | Complete the remaining hardening rounds for issue #122. |
| 2026-08-16 | implemented | Bounded the waits round 24 found outside the assurance runner: the Container App health poll now emits one progress line per iteration and fails on its own 900-second deadline instead of falling through as converged, auto-pull bounds `git fetch` and `git pull --rebase` below its interval and reports a fetch that did not finish, every retrying workflow download declares a cumulative retry window, and a resumed cohort prints its restored and outstanding counts before the loop. | Current change; 71 focused deploy-workflow, validation-queue, and gate-parity tests passed, `bash -n` on both scripts, `npm --prefix console run typecheck`, and 143 live-evidence Vitest cases passed. | Rejected three round-24 findings with evidence and continue the critique rounds for issue #122. |
| 2026-08-16 | implemented | Bounded the validation stage itself, which had no deadline of any kind: a stage that prints nothing for 30 minutes or outlives its 4-hour budget is now killed by process group, reported as status 124 with the bound that fired, and excluded from failure localization so an environment stall never blames a commit. Also corrected round 24, whose `timeout` around `git pull --rebase` could have left a developer a half-finished rebase; a strictly-behind branch now advances with a local `merge --ff-only` over the ref already fetched, and the readiness probe declares its retry window and per-request cap. | Current change; 73 focused validation-queue, deploy-workflow, and gate-parity tests passed including two new watchdog tests that kill a stalled and an over-budget stage, strict mypy, Ruff, and `bash -n` on both scripts. | Continue the critique rounds and obtain exact central validation for issue #122. |
| 2026-08-16 | implemented | Removed the shared-state and late-kill hazards round 26 found in the round 25 change: auto-pull now fetches into the per-branch remote-tracking ref and compares and advances against it, so a concurrent fetch on the same Git common directory can no longer redirect the comparison or advance the branch onto an unrelated ref, and the stage watchdog skips a kill once the stage has exited and honors an expiry only when the stage really died from that kill. | Current change; 44 focused validation-queue tests passed, strict mypy, Ruff, and `bash -n` on the auto-pull script. | Continue the critique rounds and obtain exact central validation for issue #122. |
| 2026-08-16 | implemented | Closed the last waits round 27 could still reproduce: the validator's Git helper declares a 600-second bound so no validator step outside the staged watchdog is literally unbounded, the roadmap agent stops waiting forever after its own SIGKILL when a process is wedged in uninterruptible I/O, and auto-pull distinguishes a fetch timeout from a fetch that failed for another reason. Round 27 found no reproducible finding above Low. | Current change; 44 focused validation-queue tests passed, strict mypy on both changed modules, Ruff, and `bash -n` on the auto-pull script. | Obtain exact central validation and complete issue #122. |
| 2026-08-16 | implemented | Declared a budget where only a runner default existed: the protected Terraform job is bounded to 180 minutes and the protected service deploy job to 120 minutes, and the health verifier bounds the recovery verification and readiness-path steps it ran after its own deadline. The round 28 acceptance review found no other reproducible finding above Low across the assurance runner, validation stages, auto-pull, health verification, Azure preflight, deploy workflows, roadmap agent budgets, and the Playwright port pool. | Current change; 24 focused deploy-workflow tests passed, `bash -n` on the health script, and both workflow documents parse as YAML. | Obtain exact central validation and complete issue #122. |
| 2026-08-16 | implemented | Round 29 re-reviewed the round 28 budgets against the real worst case of each job and script and confirmed they sit above it rather than truncating a slow but healthy deploy, that the `timeout` wrappers preserve exit-code propagation under `set -euo pipefail`, and that both job budgets are valid YAML at job level. The campaign exit condition is met: no reproducible finding above Low remains. | Current change; the round 29 confirmation review and the focused suites recorded in the rows above. | Obtain exact central validation and complete issue #122. |
| 2026-08-16 | validated | Central validation accepted the integrated bounded wait revision and the outgoing range was pushed to `origin/main`. | `validation_queue.py check-range origin/main..HEAD` passed for revision `85c5aadf4`, and the push reused that exact receipt and the structural evidence. | Complete issue #122 and synchronize the project board. |
| 2026-08-17 | implemented | Removed centralized validation from the mandatory developer path while preserving focused checks, path reservations, commit scoping, structural pre-push gates, and SHA-addressed CI. | `current change`; issue #148; focused hook, dispatcher, and constitution tests. | Observe CI and push latency after adoption; keep the queue available only for explicit diagnostics. |
| 2026-09-10 | implemented | Routed documentation, operator, evaluation, dependency, frozen-scenario, and Terraform changes to their owning CI jobs and split the full Python regression suite from three balanced shards into four. Unknown paths fall back to every scoped surface, while safety-critical joins and always-required repository, design, secret, and lint controls remain unchanged. | Current change; `uv run pytest -q --no-cov tests/integration/scripts/test_pytest_shard.py tests/integration/scripts/test_check_ci_contracts.py` passed 78 tests; `python3 scripts/quality/ci/check-ci-contracts.py` passed. | After an authorized push, retain at least 20 Python-triggering CI runs and compare queue and regression-shard p95 with the pre-change baseline. |
| 2026-09-10 | implemented | Moved the roadmap implementation timer entry point to the primary project checkout and made each cycle remove only its own stale worktree registration before recreating a missing isolated campaign checkout. Status now distinguishes a missing worktree without claiming the timer is healthy. | Current change; `uv run pytest -q --no-cov tests/integration/scripts/test_roadmap_implementation_campaign.py tests/integration/scripts/test_roadmap_verification_watchdog.py` passed 35 tests; preview and status commands rendered the expected project-owned cycle. | Reinstall the legacy timer only after the active fixed two-document campaign conflict is intentionally resolved. |
| 2026-09-11 | implemented | Cleared repository-local Git environment after the pre-push hook creates and enters its detached validation worktree. Nested structural tests now resolve their temporary repositories instead of moving or rewriting the branch that invoked the hook. | `current change`; issue #766; 133 focused workflow-contract and structural-gate regression tests; Bash syntax, Ruff, and CI-contract checks. | Complete an exact hook-driven push, merge the fix, close #766, and re-synchronize the blocked cohort importer PR #719. |
| 2026-09-12 | in-progress | Reconciled local validation stages, removed unscoped route defaults, made the focused facade pytest-only, scoped translation checks, and stopped implicit whole-suite fallback. A bounded read-only baseline found 12 completed CI runs at 265-391 seconds (mean 337.2) and six supply-chain runs at 168-281 seconds (mean 192.2). These are workflow elapsed times, not queue-only measurements or proof of a 90% waiting ratio. | `current change`; focused selector, facade, routing, and text-gate tests; CI run `34663201915` and supply-chain run `34663201914` anchor the sampled metadata. | Finish cache and candidate integration, retain focused checks, and measure the same cohorts after an authorized push. |
| 2026-09-16 | validated | Recomputed the issue #688 passive cohort with the operator-selected runner-queue metric: per-run maximum start delay across non-skipped direct `required` dependencies that have no upstream `needs`. Dependency-gated jobs and the final aggregator remain separate observations. | The fixed 20-run baseline and 20-run post-change inventories recorded in issue #688; empirical nearest-rank p95 was 38 to 13 seconds for authoritative queue delay, 280 to 241 seconds for dependency-gated delay, 426 to 289 seconds for the final aggregator, and 365 to 264 seconds for per-run maximum regression-shard duration. No workflow was triggered, rerun, or cancelled for this reconciliation. | The CI throughput cohort criterion is satisfied; the separate roadmap timer reinstall remains open. |

### Remaining work

- [x] Verify the self-contained restart task reaches its own terminal and a fresh managed supervisor/readiness outcome: 2026-09-22 replacement reached 12/12, loaded the observed model pin, and passed both authenticated canaries, as recorded above.
- [ ] After authorized managed preparation and restart, verify `fdai.startup.probes` receives new readiness markers while the operational DLQ receives no readiness markers.
- [x] Completed 13 independent critique rounds with focused checks and recorded accepted and
  rejected findings above.
- [x] Central validation accepted integrated implementation revision `d3f5257b9`.
- [x] The final independent review found no residual finding above Low.
- [x] Completed 22 additional rounds with only the bounded Low residuals listed above.
- [x] Integrated Top 20 revision `4a18ce982` and obtained its exact central validation receipt.
- [x] Issue #118 is closed with the `completed` label and its bounded hardening evidence retained.
- [x] Completed the bounded wait campaign critique rounds for issue #122; the round 28 acceptance
  review found no reproducible residual above Low.
- [x] Central validation accepted the bounded wait revision `85c5aadf4` and the range was pushed.
- [x] Removed automatic queue enrollment and per-commit receipt requirements from ordinary commit,
  push, and agent-tool paths in issue #148.
- [x] Issue #122 is closed with the `completed` label and its bounded wait evidence retained.
- [x] Recorded fixed 20-run baseline and post-change Python-triggering CI cohorts for issue #688:
  regression-shard p95 is 264 seconds and authoritative workflow queue p95 improved from 38 to
  13 seconds.
- [x] Implemented the local development diagnostic channel with bounded Core and Operator probes,
  digest-bound GitHub Copilot review, 13 hardening rounds with no residual above Low, and 20
  owner-only UDS evaluation rounds that all passed the five evidence-quality criteria, as recorded
  in `config/development-diagnostics-evaluation.json`.
- [x] Merged issue #1417 through required CI and recorded a passing central whole-suite baseline
  at `b546f94b5c6a` for the portable long-worktree socket path.
- [x] Recorded digest-matched Core packet
  `sha256:b405268353edffecceba4071a1b2969a1fe98e14fef2cef3b967b6239c7aa1a0` after the
  StateStore pool repair and complete clean Resource Event fan-out.
- [ ] After resolving the active fixed two-document campaign conflict, reinstall the roadmap
  implementation timer and verify one cycle recreates its missing worktree without a systemd
  `CHDIR` failure.
