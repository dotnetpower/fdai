# Subscription Genesis Provisioning implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions, and
resumable work while the roadmap owner remains focused on the normative zero-to-ready lifecycle.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Packaged `fdaictl` subscription orchestration | implemented | `packages/deployment-cli`; focused package and productization tests | The installable distribution provides profile and bootstrap reconciliation, request-bound GitHub plan/apply/status transport, verification-only resume, guided application approval, journal replay, and aggregate readiness validation. |
| Private foundation and protected exact apply | in-progress | `infra/bootstrap/`; `.github/workflows/deploy-dev.yml`; `fdai_deployment_cli.bootstrap_reconcile`; `fdai_deployment_cli.github_actions` | Target-pinned foundation planning plus protected application plan, exact apply, resume, and sanitized status are implemented. Approved foundation apply, remote state migration, run lease, and receipts remain open. |
| ARM-only foundation composition and group ownership | implemented | `infra/genesis-foundation/`; `infra/modules/resource-group/`; seven root-contract, five bootstrap, six ownership, and 16 existing bootstrap tests; Terraform validate and dependency graph | New foundation uses an ARM-managed private/keyless account without the key-returning AzureRM account lookup. Platform state references the context-tagged app group; persisted mode changes are blocked. Root tests substitute legacy child outputs; separate child tests and graph checks cover their stated boundaries, not live provisioning. |
| Installed CLI foundation dry-run selection | implemented | `fdai_deployment_cli.foundation_input`; `provision plan --stage foundation`; focused input and command tests | Selects only the signed bundle's locked foundation root, binds the authenticated operator target and offline managed-host profile, and rejects credentials and invalid network/image input. Platform remains the default. Plans are transient unless foundation saving is explicitly selected; protected actuation remains open. |
| Private exact foundation plan capture | implemented | `fdai_deployment_cli.foundation_plan`; `--save-plan` and `provision verify-foundation-plan`; focused tests plus installed-wheel Terraform 1.9.8 drill | Captures and checks exact binary plans, binds artifact/profile/target/input/lock/toolchain digests, and rejects tampering without Azure access. Synthetic signing and a built-in-provider fixture prove packaging, not Azure behavior, source eligibility, authenticated time, origin, or apply authority. |
| Private foundation state comparison | implemented | `fdai_deployment_cli.state_handoff`; `provision verify-state-handoff`; 44 focused tests and provider-free Terraform 1.9.8 fixture | Compares lineage, serial, identities, full state content, and the complete no-change plan's prior managed identities. Private output contains only digests/counts and explicitly grants no backend authority, recovery-cache deletion, or readiness. Real remote-state transport, lease, migration, storage protections, and independent observation remain open. |
| Entra directory bootstrap adapter | implemented | `entra_plan.py`, `entra_bootstrap.py`, `entra_graph.py`; 35 offline tests, Ruff, strict mypy, and installed-wheel imports | Pure planning, bounded Graph reconciliation and separate readback preserve five human roles and explicit initial ownership. Consent defaults off. The library has no public apply command; protected authorization, partial recovery, actual Graph compatibility and authenticated Console access remain open. |
| Database migrations and catalog projection | in-progress | `bootstrap-service-migrations.sh`; service migrations through `20260906_core_operational_history_lifecycle.py`; `DatabaseSemanticReadback`; Operator migration and catalog Jobs | Fresh-database migration and the complete fail-closed readiness evidence contract now succeed. Core owns normalized observations, incarnations, partitions, corrections, checkpoints, pins, archive artifacts, and certification receipts. Pre-runtime marker production and independent runtime-principal evidence remain open. |
| Model resolution and capacity provisioning | in-progress | Resolver and Terraform modules; `fdai_deployment_cli.capacity`; focused capacity tests | Workload-derived TPM, sharing aggregation, reserve, current allocation, and missing-quota rejection are implemented locally. Azure quota race checks and quantitative live probes remain open. |
| Initial inventory and observable progress | in-progress | Inventory Job and coverage manifest; `fdai_deployment_cli.progress`; focused monotonicity and closure tests | Run totals and full-subscription independent closure are implemented locally. Durable provider progress publication and Console integration remain open. |
| Initial inventory state-transition preservation | implemented | `inventory_topology_history.py`; `state_transitions.py`; Core service migration and focused persistence, replay, and migration checks | Complete promotion records verified state edges before topology history advances. Snapshot intervals remain explicitly incomplete and do not raise Genesis readiness. |
| Governed new-subscription validation | in-progress | `fdai_deployment_cli.simulation`; interruption and idempotent-rerun tests | The complete stage chain rehearses without Azure. Governed empty-subscription and rollback receipts remain open. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-05 | in-progress | Added a private-safe Resource Graph change cursor and bounded exact hydration ahead of the existing Activity Log recovery path. | `current change`; focused accelerator and inventory composition checks passed 76 cases. | Synchronize main, rerun the clean integrated gate stack, and retain the protected full-subscription Genesis receipt. |
| 2026-09-05 | implemented | Added relationship candidate disposition counts to each promoted inventory generation without changing provider collection, graph authority, or Genesis readiness. | `current change`; inventory synchronization and Operator projection checks passed 61 cases; Ruff and strict Python type checks passed. | Retain the existing protected full-subscription Genesis receipt before raising initial inventory to validated. |
| 2026-09-05 | in-progress | Extended the Genesis migration and inventory ordering contract through operational-history lifecycle records. Inventory promotion binds exact incarnations and partitions, correction and pressure state can lower completeness, and deployment policy can replace safe-retain defaults. | `current change`; Core lifecycle migration, inventory journal integration, PostgreSQL and Azure archive adapters, source coverage reduction, and focused OI-14 through OI-16 checks. | Bind the lifecycle runner as a dedicated deployed Job and retain the protected pinned-revision certification receipt. |
| 2026-09-05 | in-progress | Added the Core-owned normalized inventory observation migration to the Genesis ordering contract. Initial inventory now dual-writes full observations before ontology projection, and journal lag or pending tombstones block source-complete readiness. | `current change`; Core observation migration, inventory observer wiring, source-coverage reducer, focused OI-13 and migration checks. | Complete the existing P4 runtime-principal readback and P6 durable progress surfaces. Incarnation, correction-partition, retention, and archive closure remain owned by OI-14 through OI-16. |
| 2026-09-04 | implemented | Preserved one-shot all-source failure for subscription genesis while allowing only the local long-running inventory profile to retry after its configured loop interval. | `current change`; focused inventory CLI retry and propagation checks. | Retain protected full-subscription failure and recovery evidence before claiming live validation. |
| 2026-09-02 | implemented | Added Core-owned append-only operational state-transition storage to the initial inventory promotion path while preserving source authority, bitemporal replay, idempotency, bounded batches, and incomplete snapshot-interval coverage. | `current change`; focused transition contract, persistence, inventory publisher, service migration, lint, type, and build checks | Retain protected full-subscription transition and readiness evidence before claiming live validation. |
| 2026-08-30 | implemented | Refused resource-type subset promotion before it can replace the global active inventory snapshot or ontology projection, while preserving full-scope genesis collection. | `current change`; inventory source construction, sync coordinator, and focused subset-promotion checks | Complete durable provider progress publication and retain protected full-subscription evidence. |
| 2026-08-28 | in-progress | Adopted the implementation ledger without reconstructing earlier provenance and defined the dependency-ordered subscription genesis plan. | `current change`; owner document, current source paths, and focused documentation checks | Deliver P0-P7 and retain one governed new-subscription receipt. |
| 2026-08-29 | in-progress | Revised the plan after a separate 40-item adversarial assurance review and split cross-stage gates into a focused owner. | `current change`; subscription genesis provisioning and assurance owners plus their implementation ledgers | Implement G01-G40 and retain the governed acceptance receipts. |
| 2026-08-29 | implemented | Added the independent deployment CLI distribution and deterministic pre-login contracts for profiles, manifests, journals, model capacity, progress, inventory closure, signed artifacts, licenses, and full-stage rehearsal. | `current change`; `packages/deployment-cli`; focused package and productization tests passed 28 cases; Ruff and strict mypy passed | Complete protected Azure adapters, remote evidence, and user-facing progress integration. |
| 2026-08-29 | implemented | Completed the pre-login implementation and 75-round hardening campaign, including target-bound planning, concrete resource manifests, required/optional model capacity, signed artifact snapshots, and shipped-wheel offline execution. | Campaign commits from `dd28b64d9`; final Medium-or-higher review found none; successful network-isolated air-gap drill | Log in to the new subscription, run read-only target inspection, then collect protected plan/apply and live inventory/model evidence. |
| 2026-08-30 | implemented | Added the pre-runner bootstrap reconciliation artifact and removed state-container creation from application plan-only execution. Foundation intent is stable across observation times, every management read is pinned to the verified subscription, and unknown or conflicting state blocks approval. Completed journal stages now require receipt digests, legacy journals are replay-only, and aggregate readiness requires all 13 evidence families, complete child-aware inventory closure, and zero changes across every second-run Terraform root. Thirteen hardening rounds closed every Medium finding. | `current change`; focused bootstrap reconciliation, private-output, journal, readiness, simulation, and workflow diet tests; Ruff and strict mypy; final independent severity audit found no Medium-or-higher issue | Add the separately approved foundation apply, container creation, remote state handoff, and independent zero-change readback. |
| 2026-08-31 | in-progress | Revised the Console handoff so one existing provisioning route replays sanitized early-stage evidence and follows initial inventory plus final verification without deriving readiness from Terraform completion or estimated scan progress. | `current change`; subscription genesis owner, Console provisioning route, deployment CLI manifest, and focused contract checks | Implement the durable Blob-to-Operator projection, protected live orchestration, late-stage status production, and governed Azure receipt. |
| 2026-08-31 | implemented | Added request-bound GitHub Actions plan/apply/status transport, explicit application approval, verification-only resume, sanitized plan metadata retrieval, and database/semantic readiness gates for every migration head, required extension, runtime role, release digest, shadow mode, and independent observer. | `current change`; deployment CLI, protected workflow, focused CLI/workflow tests, Ruff, strict mypy, and Console provisioning checks | Retain approved foundation, protected apply, runtime-principal, inventory, rollback, and second-run no-change evidence before validation. |

| 2026-09-06 | implemented | Added the fresh foundation Terraform root, explicit provider target, key-free account reference, and platform resource-group reference mode. Existing standalone defaults and managed group ownership remain intact. | `current change`; `infra/genesis-foundation/`, `infra/bootstrap/`, `infra/modules/resource-group/`, `infra/foundation_reference.tf`; 34 focused mocked cases plus real configuration and dependency-graph validation on kit-pinned Terraform 1.9.8 with external networking disabled | Package every root's locked providers; implement protected approval, execution-host enrollment, remote-state migration and independent readback before a new-subscription install. |
| 2026-09-06 | implemented | Split Entra planning from directory I/O and preserved the adapter's import interface and default-off consent. | `current change`; 35 offline adapter tests, Ruff and strict mypy on three modules; the rebuilt CLI wheel imports those modules from isolated installed site-packages | Add exact approved-object binding and durable partial recovery in the protected caller; retain directory and authenticated browser receipts. |
| 2026-09-06 | implemented | Connected `provision plan --stage foundation` to the signed foundation root with private allowlisted inputs, explicit operator target, locked offline initialization, and cleanup after initialization, environment, or plan failure. The platform root and result remain the default. | `current change`; 101 focused tests including the post-extraction Entra rerun; strict mypy on the three changed source modules; new-source/test Ruff; rebuilt wheel cold-installed with no network, indexes, or caches and exercised through real kit/bundle signature checks using inert Azure and Terraform tools | Retain actual signed provider delivery and approved foundation plan/apply, enrollment, state handoff, and independent effect evidence. The cold drill proves installed wiring, not cloud readiness or production release eligibility. |
| 2026-09-06 | implemented | Added file-only state-handoff comparison with private inputs/output, full-state preservation, no-change checks, and plan-to-state identity binding. | `current change`; 44 state comparison cases plus 14 foundation command regressions; Ruff and strict mypy; actual Terraform 1.9.8 built-in `terraform_data` local state and saved-plan JSON accepted with networking disabled | The fixture compared two local snapshots, not a real remote backend. Implement protected migration and independently verify remote origin, state protection, lease, and authority before authorizing recovery-cache deletion. |
| 2026-09-06 | implemented | Added opt-in exact foundation plan capture, private binary I/O, normalized-input and provider-lock checks, and file-only verification against a separately retained review digest. Default dry runs remain transient. | `current change`; focused saved-plan tests, 101 existing foundation/artifact/state cases, and 41 existing private-output consumer cases passed; new-source/test Ruff and strict mypy; rebuilt wheel cold-installed without networking, indexes, or cache, then real Terraform 1.9.8 created and inspected a signed synthetic built-in-provider plan and installed verification rejected tampering | Connect independently authorized foundation execution, durable claims/lease, rollback, host enrollment, real private-state handoff, and runtime installation. The local review and one-hour local-clock window confer no approval, source eligibility, plan origin, or readiness. |

### Remaining work

- [ ] Extend the implemented P1 bootstrap classification to policy, quota, network, and complete
  application current-state preflight, then retain a no-mutation live receipt.
- [ ] Complete P2 and P3 with exact-plan, wrong-target, approval, expiry, interruption,
  foundation-state reconstruction, apply-claim-safe resume, conflict, import, resource-manifest
  reconciliation, and zero-unrelated-destroy tests.
- [ ] Retain actual nine-root provider-mirror and complete signed-kit evidence, then a cache-free
  packaged-CLI foundation-to-platform handoff rehearsal; fake Terraform staging checks are not that receipt.
- [ ] Complete P4 with a versioned database bootstrap manifest and runtime-principal readback of
  migration heads, roles, extensions, ontology release, catalogs, defaults, and shadow-only state.
- [ ] Complete P5 Azure adapters for region ranking, quota recheck, private keyless bindings, and
  quantitative live probes on top of the implemented local capacity contract.
- [ ] Complete P6 durable provider progress publication plus Operator and Console integration on
  top of the implemented run totals and full-subscription closure contract.
- [ ] Complete P7 and retain an exact-revision protected new-subscription receipt plus a second-run
  no-change receipt before marking the lifecycle validated.
- [ ] Collect Azure-backed evidence for the controls that require a real subscription, private
  endpoints, managed identities, quota, database readback, and complete inventory observation.
