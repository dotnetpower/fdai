# Installable Deployment CLI implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Existing-development Console-only update | implemented | `console_artifact.py`, `console_update.py`, shared `publish-console.sh`, CLI handlers, and focused artifact, lifecycle, help, publisher, Ruff, and strict typing checks in the current change | Builds candidate and rollback artifacts from protected Git snapshots, seals a 20-minute target-bound plan, requires exact terminal approval, claims before publication, recovers retained claims by readback only, and restores a verified rollback on failure. A governed dev publication receipt remains open. |
| Foundation application group to substrate binding | implemented | `standalone_host.py`; focused application-workload binding tests, 4 passed | Derives the validated workload token from the Foundation-owned `rg-<workload>-<environment>-<region>` name and supplies it to the shared Terraform root. Mismatched environment, region and malformed names fail before planning. Live plan and apply evidence remain open. |
| Foundation group observer CLI compatibility | implemented | `genesis_foundation_apply.py`; focused Foundation/recovery apply tests, 42 passed | Reads the exact handoff group by name and subscription because `az group show` rejects `--ids`; exact ID, private-state and identity checks remain. A completed apply with failed observation is not repeated. |
| Source runtime content verification on the managed-host command surface | implemented | `source_runtime.py`, `standalone_host.py`; `test_runtime_build.py` and `test_standalone_host.py`, 79 passed | Pins snapshot, runtime and bundle hashes, verifies six OCI images and rechecks inputs without login or writes. Opaque support/bundle contents, tool admission, preparation, publication and activation remain incomplete. |
| Recovered source service-image preparation | implemented | `source_recovery.py`; `test_source_application_transport.py` and `test_source_image_build.py`, 64 passed | Reuses the original snapshot and retained image claims after verified host transfer; rejects substituted source, incomplete inventory and false readiness. Dependency images, registry import and activation remain pending. |
| Two-resource successor after partial Foundation recovery | in-progress | `genesis_foundation_recovery_successor.py`, recovery planner/executor and bootstrap offline-host Terraform regressions. | Preserves all recorded resources, permits only absent host cache and role-GUID fixes, and binds the prior review/claim. Actual successor plan/apply and independent host acceptance remain pending. |
| Explicit public source recovery resume | implemented | `fdai_deployment_cli.source_recovery`, `source_recovery.py`, `source_application_transport.py`; focused CLI and coordinator tests. | Retains original application snapshot/settings while recording current executor source separately; exact-approved host/state stages and verified source transfer are connected. Application activation and real recovery acceptance remain incomplete. |
| Recovered Foundation to private state migration | implemented | `genesis_foundation_recovery_state.py`, `genesis_foundation_state_archive.py`, `genesis_foundation_state.py`, official approval prompt; focused archive, authorization, migration and resume tests. | Keeps original state ownership until backend authority permits cleanup; separate exact approval, source CI, host attestation, comparison and no-repeat claims gate migration. Live acceptance and public coordinator routing remain incomplete. |
| Recovery receipt to manual host enrollment | implemented | `genesis_foundation_recovery_handoff.py`, `genesis_runner_enrollment.py`, official prompt; focused recovery/approval/enrollment tests. | Preserves unaltered recovery evidence and original-state ownership; separate current enrollment approval and both source CI checks precede Bastion. State migration and public coordinator routing remain unconnected. |
| Exact Foundation recovery execution | implemented | `genesis_foundation_recovery_apply.py`, official recovery approval prompt; 18 execution/authority regressions and shared approval/observer checks passed locally. | New review-bound human approval and claim precede one original-state apply. VM preflight and late-input drift checks precede the claim. Verification-only resume uses independent observations and zero-change evidence; public coordinator receipt adoption and live recovery remain unvalidated. |
| Partial Foundation collision review | implemented | `genesis_foundation_recovery.py`, `genesis_foundation_recovery_plan.py`; 45 focused Foundation tests passed, including original-state mutation detection, policy-tag bounds and no-copy/no-apply orchestration. | Read-only preparation permits application-only naming and exact equal policy tags, rejects completed-resource changes and expanded roles, and requires the new group to be absent. Separate apply authority and coordinator adoption are not implemented by this planner. |
| Application-only Foundation name selection | implemented | `foundation_input.py`, `infra/genesis-foundation/`; 63 focused input tests and 9 Terraform mock tests passed. | Optional `application_workload` preserves the default and separates only the application group token from operations naming. This does not authorize state adoption or retry a partial apply. |
| Exact residual image execution | implemented | `genesis_runner_image_recovery_apply.py`, `genesis_approval_prompt.py`; 42 focused recovery tests and 24 approval tests passed, including real built-in Terraform local-backend state ownership, provider-link materialization and exact residual approval input. | Fresh residual approval, source CI, state/plan/provider hashes and distinct claim precede apply. Success uses independent observers and zero-change verification. No live residual apply or public-coordinator receipt adoption has occurred. |
| Partial runner-image residual planning | implemented | `genesis_runner_image_recovery.py`, `genesis_runner_image_recovery_plan.py`; 22 focused residual-plan and orchestration tests passed with strict mypy. | Read-only preparation uses the original execution lock, authoritative local state, original variables/review/claim and only the trusted-CLI wait fix. Completed Azure work must remain no-op. This provides no apply command or execution authority. |
| Source-only service image construction retirement | in-progress | Existing `source_image_build.py`, source coordinator wiring and focused builder tests; revised design prohibits tenant-time builds. | The implemented local Docker/Buildx path conflicts with the revised contract and must be removed. Historical test evidence remains valid only for the behavior being retired. |
| Prebuilt runtime artifact admission | not-started | Revised owner document and runtime profile; no parser, verifier or coordinator implementation | Source mode must require a signed manifest for all baseline, dependency, Console and migration artifacts before application planning. |
| Source handoff and managed-host transport | implemented | `source_transport.py`, `source_receiver.py`, `source_azure.py`, `source_application_transport.py`, `source_genesis.py`; real isolated receiver checks, source coordinator tests and 14 host-transfer integration regressions passed. | Source transfer is connected at the locked application boundary with original receipt-chain validation, current host attestation, independent bootstrap hash, immutable claim and verification-only recovery. A local 19,835-file archive/receiver probe passed; actual Azure transfer, image builds and application execution remain unvalidated or unconnected. |
| Audited Run Command private-relay staging | implemented | `execution_bundle.py`, `run_command_receiver.py`, `run_command_authority.py`, `run_command_private_relay.py`, fixed bootstrap/orchestration modules, and 36 focused passing tests | Stages one digest-bound execution bundle from an eligible Linux deployment host to one peered WSL host without cloud artifact storage. Exact profile, current approval, actor, VM, private-address, and UAMI checks precede every effect. Live Azure transfer evidence and automatic public CLI routing remain open. |
| Initial installation settings confirmation | implemented | `installation_scope.py`, public source CLI wiring; 225 focused initial-scope, source-coordinator and help tests passed, including real pseudo-terminal input. | Fresh interactive source installations collect setup cost and one scope confirmation before Foundation execution. Immutable private settings bind preparation, source, target, runtime and budgets; exact resume never re-prompts or renews. Settings grant no apply authority. |
| Start-once bounded deployment authorization | in-progress | Revised initial-confirmation contract in the owning design; exact-checkpoint validators remain unchanged. | Complete price and ownership evidence, least-privilege tuple validation, revocation, cleanup admission, every effect adapter and signed-kit prompt integration remain open. No live effect is authorized from a scope record. |
| Source CLI preparation and planning | in-progress | `source_deploy.py`, `source_azure.py`, `source_foundation.py`, `source_genesis.py`; local preparation, preflight routing and saved source-plan tests. | The public source entrypoint reaches a review-only runner-image plan; source snapshot Foundation plans remain distinct from kit provenance. Source apply, managed-host workload activation and Trial persistence remain unconnected. |
| Exact-source admission, snapshot and plan provenance primitives | implemented | `source_input.py`, `source_snapshot.py`, `foundation_plan.py`; 121 focused package tests pass across source, plan and host changes; actual clean checkout admission verified 19,558 files. | Ignores caller-local outputs, rejects raw blob drift and unsafe links, pins snapshot content, and keeps source plan schemas separate from signed-kit plans. These primitives do not yet expose or execute `provision azure --source`. |
| Connected source deployment and durable Trial | in-progress | Current source-mode design in the owner document; operator-approved implementation scope. | Source preparation, Foundation and managed-host execution without a signed kit, AKS acceptance, and runtime Trial enforcement require separate executable evidence. Existing signed-kit checks remain mandatory for kit mode. |
| Unboxed activity stream and failed-attempt guidance | implemented | [Renderer](../../../packages/deployment-cli/src/fdai_deployment_cli/deployment_progress.py); [failure diagnostic](../../../packages/deployment-cli/src/fdai_deployment_cli/foundation_failure.py); [stream tests](../../../packages/deployment-cli/tests/test_deployment_stream.py); [diagnostic tests](../../../packages/deployment-cli/tests/test_foundation_failure.py) | Colored phase and distinct checkpoint events remain in scrollback; at most six lines refresh current work without a panel or future-phase table. Fixed failure guidance requires the exact failed attempt and source/target context. 268 focused tests pass; this does not recover partial Azure effects or replace a signed deployment kit. |
| Unified Foundation terminal display | implemented | [Presentation adapter](../../../packages/deployment-cli/src/fdai_deployment_cli/foundation_progress.py); [stderr bridge](../../../packages/deployment-cli/src/fdai_deployment_cli/foundation_output.py); [producer-format tests](../../../packages/deployment-cli/tests/test_foundation_progress.py); [pipe and screen tests](../../../packages/deployment-cli/tests/test_foundation_output.py) | Interactive auto mode compacts only known Genesis presentation into the local progress view, now an unboxed stream. Unknown diagnostics and approvals retain native visibility. The earlier 236-test and installed-snapshot evidence remains in history. Signed kit contents and deployment authority are unchanged. |
| Retained online kit acquisition | implemented | [Acquisition](../../../packages/deployment-cli/src/fdai_deployment_cli/deployment_kit.py); [cache revalidation](../../../packages/deployment-cli/src/fdai_deployment_cli/deployment_kit_cache.py); [retry regressions](../../../packages/deployment-cli/tests/test_deployment_kit_retry.py) | Repeated online acquisition revalidates the retained signed kit and exact materialized payload, binds the requested source without treating the marker as trust, and creates a fresh execution copy while preserving prior evidence. Local artifact verification is not an Azure or release receipt. |
| Dedicated `fdaictl` distribution and command entrypoint | implemented | `packages/deployment-cli`; focused package and productization tests | The wheel registers `fdaictl`, provides local commands, seals target-pinned bootstrap reconciliation, and exposes standalone manual tenant deployment without workflow dispatch. The r4 development kit contains the CLI wheel; separate production distribution and trust remain open. |
| Retained r4 development release and cold installed preparation | validated | [Public release](https://github.com/dotnetpower/fdai/releases/tag/deployment-v0.1.0-r4); [release and isolated acceptance receipt](https://github.com/dotnetpower/fdai/issues/803#issuecomment-5653340906) | The exact `c137aa104682a59b979f5f3554a06bf87c555b8e` kit passed 11 no-egress/no-DNS checks including cold CLI installation, 59 payload files, six OCI images, seven support packages, and 11 Terraform roots. Historical artifact proof only; newer source, production trust, and deployed readiness are not certified. |
| Core deployment preflight primitives | implemented | `services/core-control-plane/src/fdai/core/deploy_preflight/` and focused preflight tests | The analyzer, report, Azure live script, toggle primitives, and reassembly logic exist independently of a deployment CLI facade. |
| Standalone tenant deployment transport | implemented | `fdaictl provision azure`; `standalone_deploy.py`; `standalone_application.py`; focused package tests | A locally supplied signed kit can drive both the local coordinator and appliance entry point. Online acquisition remains optional, and the public CLI exposes no GitHub workflow dispatch command. |
| Local terminal deployment progress | implemented | [Renderer](../../../packages/deployment-cli/src/fdai_deployment_cli/deployment_progress.py); [focused progress tests](../../../packages/deployment-cli/tests/test_deployment_progress.py); local entry-point help | The installed CLI displays 13 coordinator phases, elapsed time, observed download bytes, and explicit waiting, failure, and interruption states. Plain and off modes preserve scripting. No signed bundle modification or Azure execution was required for this UI validation. |
| Static CLI discovery and argument help | implemented | [Parser](../../../packages/deployment-cli/src/fdai_deployment_cli/cli_parser.py); [plain help](../../../packages/deployment-cli/src/fdai_deployment_cli/cli_help.py); [help tests](../../../packages/deployment-cli/tests/test_cli_help.py); [hardening tests](../../../packages/deployment-cli/tests/test_cli_help_hardening.py); [fresh-process tests](../../../packages/deployment-cli/tests/test_cli_help_process.py) | Bare root and groups show help with exit 0; invalid executable invocations remain exit 2. Sixteen local review rounds, 200 focused tests, and 14 installed-command checks support this bounded claim. No Azure deployment or signed-kit publication is included. |
| Local progress and supervision hardening | implemented | [Terminal emulation tests](../../../packages/deployment-cli/tests/test_deployment_terminal.py); [status tests](../../../packages/deployment-cli/tests/test_standalone_status.py); [approval tests](../../../packages/deployment-cli/tests/test_standalone_approval.py); [application progress tests](../../../packages/deployment-cli/tests/test_application_progress.py) | Fourteen bounded rounds tested real ANSI screen state and mocked execution boundaries. Failed or stale Foundation state cannot advance setup; malformed or expired reviews cannot request approval. Local focused tests pass; Azure execution and release evidence remain separate. |
| Legacy GitHub deployment transport | not-applicable | `fdai_deployment_cli.github_actions`; repository workflow tests | The compatibility module remains for historical repository automation evidence, but it is not registered by the public CLI and cannot be selected by a provisioning profile. Its request-bound plan/apply helpers preserve evidence binding, claim/receipt guards, verification-only resume, convergence, migrations, and enabled endpoint health checks; offline profiles remain blocked from that legacy public-artifact path. |
| Local supervised Genesis coordinator | implemented | `fdai-up.sh`; private preparation, approval, image, Foundation, Entra, repository configuration, application, and supervisor modules; focused lifecycle tests | The source-checkout command composes the local and protected boundaries in one process. The r4 kit provides a historical published coordinator; current complete readiness evidence remains open. |
| Channel-edge identity selection | implemented | `DeploymentSelection`; `validate_deploy_request.py`; `deploy-dev.yml`; focused CLI, request, and workflow tests | The CLI seals the authority-free edge identity flag into plan and apply context. The runner permits the request and the platform plan targets only the identity and its minimum ACR, Event Hubs, DSN, and provider-secret roles. |
| Bounded RCA reader prerequisite selection | implemented | `DeploymentSelection`; `deploy-dev.yml`; `enforce_plan_scope.py`; focused CLI and workflow checks | The request-bound mode uses no additional workflow input and permits only the dedicated RCA reader identity and Monitoring Reader role before split Core deployment. |
| Signed deployment bundle release path | in-progress | Bundle builder and `fdaictl bundle verify`; focused signature, SBOM, compatibility, extraction, and tamper tests | Construction and verification are executable. Restoring the protected publication workflow remains open. |
| Offline kit and disconnected planning | validated | Locked release tooling, offline-kit verifier, shipped-wheel installation, and successful network-isolated air-gap drill | The drill verified no egress or DNS, exact artifacts, mirror-only Terraform init, Terraform validation and 20 tests, license inspection, and the operator plan path. Connected staging uses the required Python for bounded Terraform ZIP extraction instead of an ambient `unzip`. |
| Complete runtime release assembly and preparation drill | in-progress | `fdai_deployment_cli.runtime_build`; `build-runtime-release.py`; explicit complete mode in `airgap-drill.sh`; focused tests | All seven OCI archives plus Console and support are bound and validated. The retained r4 artifact acceptance is distinct from the still-required current eligible release and newer prescribed complete-mode run; neither alone proves deployment. |
| OCI deployment appliance | implemented | `build-deployment-appliance.sh`; `run-deployment-appliance.sh`; focused script and CLI tests | The builder verifies a complete signed kit, requires a digest-pinned base, installs from the embedded wheelhouse with no build network, and emits an OCI image. A clean production build and Azure deployment receipt remain open. |
| Standalone deployment hardening | implemented | private output, approval, managed-host lock, environment boundary, appliance scripts, 806 package tests, 79 focused integration tests, Ruff, strict mypy, ShellCheck, and independent final severity audit | Twelve critique and hardening rounds closed every evidence-backed finding above Low. Operational Azure evidence remains a separate release gate. |
| Runtime service wheel staging | implemented | `stage-runtime-wheelhouse.py`; `verify-productization.sh`; focused staging and productization tests | The release wheelhouse and productization gate include all six service distributions. The undeployed System Knowledge Service candidate remains outside the validated six-image runtime release until graduation. |
| Protected artifact and identity bootstrap seams | implemented | `fdai_deployment_cli.oci_archive`, `fdai_deployment_cli.acr_publish`, `fdai_deployment_cli.entra_graph`, `fdai_deployment_cli.entra_bootstrap`, and focused tests | The library validates bounded OCI-layout bytes with one coherent OCI or Docker schema 2 media family before ACR credentials and reconciles tenant-bound Entra apps, service principals, groups, roles, and optional explicit-owner consent. Both seams require independent readback and expose no public command or authorization decision. |
| Production-root install and teardown experience | not-started | The target installation and teardown contracts in this document | The retained development kit does not establish the production offline-root ceremony, complete install/upgrade/rollback evidence, or a guarded teardown command. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-19 | implemented | Added deterministic execution bundles and a claim-first, source-address-bound, certificate-pinned private TLS relay for explicit audited Action Run Command staging. Exact `dev` profile and current approval bind authoritative actor and VM/UAMI readback; non-dev quorum remains blocked. Ambiguous recovery is retained-tree verification only and bounded to one claim; receipts grant no apply authority. | `current change`; execution bundle/receiver, authority, relay/bootstrap/orchestration modules, 36 focused tests, Ruff. | Retain one exact live peered-host transfer receipt, then connect reviewed `fdaictl` access-profile routing. Historical AKS adoption, registry import, exact service plan/apply, and operational verification remain separate. |
| 2026-09-18 | implemented | Extended deterministic prebuilt Console artifacts with allowlisted Manual Studio content while preserving the runtime schema accepted by existing rollback artifacts. The installer binds same-origin share metadata separately, replaces only a reserved build placeholder, and leaves old artifacts without manuals readable. | `current change`; focused deployment and Console tests; strict mypy, Ruff, shell syntax, Console typecheck, production build, and bundle check. | Publish one exact merged artifact and verify `/manuals/catalog.json`, `/manuals/library.html`, a representative share page, and the authenticated Guides drawer. |
| 2026-09-17 | implemented | Corrected Console readback after Static Web Apps returned the expected `404` for its consumed configuration file. Remote hash checks now cover only served files while local validation keeps that configuration mandatory. Rollback closure also verifies the static effect independently so unavailable Operator and Ingestion APIs cannot cause repeat publication, and retries reuse validated private attempt directories. | `current change`; value-free readback proved rollback assets served and candidate assets absent; the retained claim closed as `rolled_back` with no recovery mutation; development PostgreSQL availability explained API degradation; a fresh exact plan applied with artifact, API, authorization, and Entra verification; authenticated `/overview` rendered without overflow. | Align the deployed Operator `/kpi/autonomy` HTTP 200 payload with the Console `1.0.0` measurement contract, then repeat the authenticated live Dashboard check. |
| 2026-09-17 | implemented | Hardened the first no-prompt live apply after a mode-0755 extracted directory stopped local configuration before publication. Extraction now tightens to 0700; claimed recovery validates exact claim content, survives plan expiry, uses current reviewed publisher controls, checks rollback before restoring, and never repeats candidate publication. | `current change`; failed pre-publication claim with no terminal receipt; focused private-directory, claim, expiry, control-source, rollback-first, and Console lifecycle tests; Ruff, formatting, and strict mypy. | Run verification-only recovery for the retained claim, then keep its private terminal receipt and authenticated browser result. |
| 2026-09-17 | implemented | Removed the redundant second prompt from an explicitly invoked bounded non-destructive dev Console apply. The command still validates and internally binds the exact plan digest before claim and publication; distinct-approver, destructive, or expanded effects remain outside this path. | `current change`; focused no-prompt CLI binding and Console lifecycle tests, Ruff, formatting, and strict mypy. | Apply the next exact dev Console plan without pausing for machine-value transcription and retain its private receipt. |
| 2026-09-17 | implemented | Corrected existing-Console target validation after the first real plan showed that the SPA client and API audience are separate Entra application registrations. The API scope now validates its own UUID and scope token without requiring the SPA client ID. | Blocked no-mutation local plan; `current change`; focused Console update tests, Ruff, and strict mypy. | Recreate the exact plan from the corrected committed control before any publication. |
| 2026-09-17 | implemented | Added a local existing-development Console update path with deterministic protected-source artifacts, exact target and rollback planning, bounded human approval, claim-before-publish, verification-only recovery, remote hash and access checks, and terminal rollback evidence. It changes no Terraform resource, service image, database state, or runtime authority. | `current change`; deployment CLI source and focused tests, shared publisher contract, Ruff, formatting, strict mypy, and shell syntax checks. | Publish one exact protected revision, retain the private plan and apply receipt, and record repository-safe authenticated browser evidence before advancing runtime validation. |
| 2026-09-17 | in-progress | Corrected the operational evidence contract so public release publication and online Azure convergence are not required. The local coordinator and appliance entry point consume the same locally built and independently verified signed kit. | `current change`; deployment owner documents and Azure deployment skill; no executable or Azure behavior changed. | Build and verify the local complete kit, then retain separately approved convergence receipts from both entry points without publishing it. |
| 2026-09-16 | implemented | Added the PostgreSQL dependency image to the runtime release's closed dependency inventory after an exact-main complete-kit build rejected the builder's seven-image descriptor. | `current change`; `runtime_release.py`; runtime build, image validation, and offline preparation tests report 114 passed. | Rebuild the complete kit from the delivered exact revision, independently verify it, then retain the separately approved online and artifact-offline Azure convergence receipts. |
| 2026-09-16 | implemented | Bound the shared substrate workload token to the exact Foundation-owned application resource-group name instead of silently falling back to the default workload. The parser requires the reviewed environment and region suffix and the existing Foundation workload grammar. | `current change`; `standalone_host.py`; `test_standalone_host.py`; focused application-workload tests report 4 passed. | Retain an exact substrate plan proving reference-only ownership of the Foundation group, then apply only after current approval and independently verify the resulting state. |
| 2026-09-15 | in-progress | Exact-approved successor Terraform apply completed with two additions, no changes or deletions. Its post-apply observer failed because `az group show --ids` requires `--name`. Corrected only the group read command and retained exact returned-ID checks. | Approved plan `aaa435706a0506728b80c0c1189f6f4ecd8bc6515e3ead85beb1148f95320f82` on source `ad447f35096403104d1f577868cd95175edfbb26`; authoritative state serial 41 with 34 normal managed entries. CLI 2.88 parser reproduced exit 2. Current change: 42 focused Foundation/recovery apply tests passed. | Publish the observer fix and retain independent readback plus zero-change evidence through an explicitly sourced verification-only path; never repeat the claimed apply or rewrite its source/approval. Enrollment, state transfer and application deployment remain incomplete. |
| 2026-09-15 | implemented | Corrected the new source-runtime reader from a test-invented `commit` key to the actual producer's `source_commit`. The fixture now calls `SourceDeploymentInput.to_mapping()` instead of duplicating its schema. | Current change; 14 source-runtime checks passed against the canonical producer mapping; actual retained snapshot keys independently matched the producer. The preceding 79-test result did not establish real-manifest compatibility. | Publish this correction before consuming the content verifier; source-host preparation, complete artifacts, private import and full deployment remain pending. |
| 2026-09-15 | implemented | Added read-only source-runtime content verification to the host command surface and separated the support installer's admitted artifact path from the kit layout. | Current change; `test_runtime_build.py` and `test_standalone_host.py`: 79 passed, including input mismatch, during-read mutation, no-login/no-write CLI, and no kit fallback. | Assemble original-source Console/dependency/support artifacts; verify executable bundle/tool/support provenance, connect approved host preparation and private registry publication, and retain full AKS/Trial/health acceptance. This command does not prepare or deploy an application. |
| 2026-09-15 | implemented | Connected recovered source host transfer to the existing five-service OCI builder without changing application-source provenance or rebuilding claimed interrupted images. | Current change; 64 focused coordinator/image tests passed. Actual original-installation service archives passed OCI source/platform/digest validation and all five CLI entrypoints imported in network-disabled, read-only local containers. Aggregate local image receipt `e849ad631681e3627220eee0bbdf015ae59cb312d1a6c0c94c9027fd18db1025`. | Publish this connection after parent recovery delivery, retain live host transfer and exact private-registry import, then complete dependencies, runtime activation and service health. Local image validation is not deployed health evidence. |
| 2026-09-16 | in-progress | Replaced tenant-time source image construction and tenant appliance assembly in the target contract with prebuilt signed runtime artifact admission. | `current change`; owner documents and deployment skill only; existing builders and tests remain unchanged. | Remove the tenant image-build and capture paths, add runtime artifact manifest admission, and prove deployment only mirrors or imports unchanged verified digests. |
| 2026-09-15 | implemented | Removed runtime re-import of a collected successor planner test helper, preserving scope checks when unrelated tests replace the `tests` module namespace. Deployment code and authority are unchanged. | CI `34970830822`, attempt 1, shard job `104386516292`: four `ModuleNotFoundError` failures. Local namespace/cache collision reproduced all four failures; the fixed four tests and 80 focused two-worker planner/executor tests passed. | Publish the corrected candidate and require its exact-head CI before preparing or applying a successor plan. No Azure apply is authorized by this test repair. |
| 2026-09-15 | in-progress | Diagnosed real partial recovery failures: ephemeral OS disk caching must be ReadOnly, and ABAC GuidEquals operands must be role GUIDs rather than scoped resource paths. Added exact source repairs and one predecessor-bound successor with only those two absent creates. | Current change; 14 bootstrap Terraform tests, predecessor file integrity checks and focused successor planner/apply/approval regressions. The previous claimed apply created additional resources but produced no success receipt; it was not repeated. | Publish and verify exact-source CI, generate the successor plan preserving recorded resources, obtain its exact approval, then retain independent recovery, host, state and application acceptance. |
| 2026-09-15 | implemented | Added explicit public Foundation recovery resume without rewriting original source intent, snapshot or status. Separate source provenance, current approvals and retained claims select enrollment, state verification and original-snapshot host transfer. | Current change; `test_source_azure.py` and `test_source_application_transport.py` cover CLI exclusions, unchanged settings, executor-result mismatch, missing recovery success, approved stages, completed-state ordering and source transfer without ordinary receipt fabrication. | Complete exact-source CI and approved live recovery acceptance. Connect verified images, private registry import and full AKS activation; retain Console, cost and durable Trial evidence before declaring deployment readiness. |
| 2026-09-15 | implemented | Connected recovered Foundation and host enrollment evidence to the private state migration engine with separate current approval, exact configuration/provider/source checks, original writer locking and current host attestation. Added separate original-state archive input without making a second local owner. | Current change in `genesis_foundation_recovery_state.py`, `genesis_foundation_state.py`, archive builder and official prompt; 68 focused state/approval tests cover real host archive extraction, approval/host-key/configuration rejection, no-repeat failed effects, raw cleanup and remote-only completed resume. | Publish eligible sources, then retain real recovery, enrollment and backend authority receipts. Connect public coordinator recovery routing and finish source application deployment and durable Trial. |
| 2026-09-15 | in-progress | Added explicit recovery-receipt input to manual host enrollment while retaining the original writer lock, raw receipt and common in-memory correlation reference. A separate official enrollment approval binds the recovery receipt and current enrollment source. | Current change: focused approval, recovery and enrollment tests cover success, absent/wrong approval, stale state, claim/effect tampering, lock contention and verification-only resume; no Azure mutation was performed for these tests. | Publish the eligible enrollment source after parent recovery delivery. Execute approved recovery first, then collect distinct enrollment authority and verify the real host. Private state migration, public coordinator routing and full application deployment remain incomplete. |
| 2026-09-15 | in-progress | The policy-corrected read-only recovery plan preserved all 24 managed instances and proposed exactly ten remaining creates with zero replacements/deletes. Added separate exact-approved execution and verification-only resume without forging the original Foundation receipt. | Planning source `d09c9ab146e0dbc360803e9011df618e8239ec51`; review `002fd5f965057476ab3abb909184e333937a81501c661c433ab1138d56f553ae`; original state serial 27 and digest unchanged. Current change: 18 execution/authority tests, official prompt and existing observer regressions passed, including plan mutation during VM preflight before claiming. The recorded plan predates this executor and is not executable with changed source. | Generate a new fully bound review from the published eligible execution source and obtain its exact approval. Independently verify recovery effects, then connect attestation, state migration and application delivery. No recovery apply has occurred. |
| 2026-09-15 | in-progress | An actual read-only collision recovery plan correctly blocked four replacements: policy-added IP tags forced both public IPs and dependent Bastion/NAT connections to be recreated. Added an explicit bounded policy-tag input and compared preserved resources with their original recorded state instead of unrealized initial defaults. | Current change: 70 Foundation input tests, 45 planning/recovery tests and 10 Terraform mock tests passed. Both retained IPs carried exactly `FirstPartyUsage=/Unprivileged`; no existing state or resource was changed by diagnosis. | Publish the correction and prepare a fresh recovery review proving zero replacements and unchanged original state. A separately reviewed executor and current exact approval remain necessary; full deployment is incomplete. |
| 2026-09-15 | in-progress | Added application-only Foundation naming after an approved apply partially failed and independent readback found an existing application group outside the run's ownership. The shared workload remains unchanged to preserve operations resources. | Current change: 63 input tests and 9 Terraform mock tests passed; actual partial execution and collision evidence are recorded in [issue #1008](https://github.com/dotnetpower/fdai/issues/1008#issuecomment-5678969319). | Produce a separately bound residual plan against the original state, reject changes to completed resources, obtain exact current authority and independently verify effects. Source application/Trial activation remains incomplete. |
| 2026-09-15 | in-progress | Actual residual planning reached the final integrity check but mirror-installed provider symlinks prevented review publication. Added exact-original-package materialization without weakening the no-link execution tree contract, and a residual-review input to the existing human-only approval prompt. | Current change: 42 recovery and 24 approval tests passed. An isolated local probe materialized the actual retained provider links and confirmed unchanged original provider hashes without Azure calls. The previous execution row understated its final test count: commit `b91332c6f3f59f2caac6521be1240aa0734cf747` had 40 passing recovery tests, not 39. | Generate a fresh review from the published source; exact-head CI and a new current human approval remain required before any residual apply. Public coordinator receipt adoption and complete installation remain unfinished. |
| 2026-09-15 | in-progress | Added a separate exact-approved residual apply and verification-only path after proving with actual local Terraform that a saved residual plan updates the original local backend without replacing existing synthetic resources or creating a second state file. New plan reviews bind provider execution bytes and original state lineage. | Current change; 39 focused tests passed. Execution tests cover missing, wrong-plan, wrong-source, wrong-human and expired approvals; state/configuration/plan drift; claim-before-effect; independent observation; and failed-apply non-repetition. The Azure residual review from the preceding row predates the new provider/lineage bindings and is not executable through this path. | Generate a new fully bound review from the published tested recovery source, obtain its current exact approval, execute and independently verify remaining image work, and integrate the separate recovery receipt with the public coordinator. No residual Azure effect is claimed. |
| 2026-09-15 | in-progress | Completed an actual read-only residual review after classifying provider refresh differences. Original authoritative state remained in place, byte-for-byte unchanged at serial 25; no resources were applied or deleted. | Recovery source `202c1c920985fee1cf1e80d6f77e0afbf95bdbf6`; plan `967cbf61fa849db7a2575bce62d8659938c95004a770c5adbaae728b35fefc29`; review `c2d6ecc8ed0b578a59e6822a61f2a6ad6c4c012294a65b0fe204c7add49fa1bc`. The plan preserved 23 managed resources, verified 13 refresh observations, and retained six pending image-finalization steps plus replacement of the failed local wait marker. Current tests: 28 passed, including detection of concurrent original-state modification before review publication. | The expiring review is not an approval or an apply receipt. A separately governed exact residual execution path remains required; no original approval or claim is reusable as authority for these new plan bytes. Image capture, verifier execution and whole deployment remain incomplete. |
| 2026-09-15 | in-progress | Generated the actual residual Terraform plan without copying or modifying original state. Initial strict admission held 13 Azure refresh differences for inspection. Added narrow comparison of original computed fields and equivalent empty/set representations while requiring stable resource IDs and no-op preserved resources. | Current change; 27 focused tests passed. Local revalidation of the retained actual plan accepted 23 preserved managed resources, six pending image-finalization resources and the failed local wait replacement. All six finalization entries matched original known intent; no Terraform apply or repeated provider query was used for this revalidation. | Persist a complete fresh residual review with this verifier, then supply a separately governed exact execution contract and approval before any effects. Existing partial resources remain retained and can incur charges; application deployment is not ready. |
| 2026-09-15 | in-progress | Added a narrowly scoped read-only residual planner for the observed partial runner-image failure. It retains the original state in place, validates the original plan/claim/configuration and permits only the CLI-path correction, then rejects plans that replay completed Azure work or alter original finalization intent. | Current change; 22 tests cover action bounds, known/unknown values, duplicate/missing/extra resources, state drift, configuration substitution, original execution locking, unchanged authoritative state, no apply calls and blocked-output preservation; strict mypy and Ruff passed. | Generate and inspect the actual residual plan. Any further effects need a new exact execution contract and current approval; this planning primitive cannot recover or authorize the existing partial deployment by itself. |
| 2026-09-15 | in-progress | The first exact-approved source runner-image apply created partial infrastructure but stopped at the local builder poweroff wait. Removed its hardcoded `/usr/bin/az` override so it uses the coordinator's existing trusted CLI path. No retained plan, claim or Terraform state was modified, and no apply was retried. | Source `18793af3832c74fef40fb73b5a9fe7bfba0717a0`, plan `49f6362fc9a9d48fbe2ba387c0d6eb8e2252afc9d3d3c557a6514df04961d57b`; read-only Azure observation `ed0cdd19582003f7dfc82c42bcfcbd0bd6d9aad35680ef0e771f82bb951a5668` confirmed builder extension and Firewall succeeded and the builder was stopped. Terraform recorded 24 managed entries, with no image or verifier VM. Current change: three actual-shell trusted-path/CLI-failure regressions passed. | Partial resources can incur charges. Reconcile and govern a new residual recovery plan before further effects; the existing claim allows verification only. Image capture, verifier, Foundation/application activation and overall deployment remain incomplete. The coordinator's false mutation flag was not evidence of zero effects. |
| 2026-09-15 | in-progress | Actual Executor image construction exposed Buildx metadata mode 0644 despite the process umask. Added descriptor-safe tightening of owner-created metadata and explicit verification-only recovery; no build is repeated from an incomplete claim. | Current change: 19 source-image tests passed. From integrated source `1b6e216434c2782845fa71a8474ad3c1c78afcc1` (CI `34935258306` succeeded), the 44552192-byte Executor OCI was independently verified without rebuilding: manifest `sha256:3406b653a72858ba85b43cafe91cf56e46e8da42f6fbe9c81010b851941b8a12`, archive SHA-256 `14f975412b0cef85426804a2ce6a7d58958402acef634a35b6c7d75ca3c1ab88`. | Verify the remaining four service images and dependency/Console artifacts; private import, runtime activation and complete installation authority/cost/Trial/acceptance remain open. No registry publication or Azure mutation is claimed. |
| 2026-09-15 | in-progress | Completed protected delivery of source Foundation/host-transfer/AKS identities in PR #1009, leaving the installation issue open. Added source-only local service image construction instead of adding an unreviewed privileged Docker builder to the attested host. Private registry operations remain host-owned. | PR #1009 merged at `1b6e216434c2782845fa71a8474ad3c1c78afcc1` from `79ac7c75945e16979b55da431a87ab0c1564cc95`; exact-head CI `34934874688` and supply-chain `34934874739` succeeded. Current change: 13 focused source-image tests and 38 coordinator tests passed; local Docker and pinned package Buildx prerequisite observation succeeded. | Retain actual OCI build evidence from an eligible exact source; add dependency/Console artifacts and authenticated image import. Five-service activation, durable Trial, full costs and bounded startup authority, HTTPS/Entra and operational acceptance remain incomplete. |
| 2026-09-15 | in-progress | Connected source transfer to the enrolled Bastion host at the source coordinator's locked application boundary. Added an exact-source dependency-free receiver bootstrap, original Foundation/enrollment/SSH evidence validation, fresh attestation, pre-transfer claim and verification-only recovery. Bounded concurrent durable writes address the observed full-source timeout without skipping file synchronization. | Current change; isolated receiver and source-input tests, public source-coordinator tests, 14 host-transfer/coordinator integration tests, Ruff and focused mypy. A 200-file probe attributed 1.439 of 1.458 seconds to fsync; after bounded concurrency, archive creation and receiver verification for the retained `f57b7511880a3a2fcaa11aec512148674ad3d4b4` snapshot completed in 77.71 seconds for 19,835 files and 177479680 archive bytes. The earlier complete preparation exceeded its 300-second bound and is not success evidence. | Verify the published exact revision, then complete host-side source image builds, five-service configuration, full startup authorization and cost/ownership evidence, HTTPS/Entra, jobs and durable Trial. No live Azure transfer or resource mutation was performed by these local checks. |
| 2026-09-15 | in-progress | Connected verified private Foundation handoff to source-only archive preparation and added an installed receiver that reconstitutes a fresh exact snapshot without executing it or claiming release trust. Corrected original-receipt loading after confirming that portable status strips fields and cannot be rehashed as the full receipt. | Current change; `test_source_input.py` and `test_source_azure.py` (69 passed), strict mypy and Ruff. Tests cover deterministic real-Git round trips, executable/internal-link preservation, archive substitution and unsafe member structures, original handoff source/target/digest and effect fences, immutable retry, and receiver subprocess output. | Connect authenticated Bastion transfer, host-side source build tools and all five service deployment inputs; full startup authorization, cost/ownership, HTTPS/Entra, jobs and durable Trial remain open. No resource mutation or whole-installation readiness is claimed. |
| 2026-09-15 | in-progress | Connected an exact public Linux VM compute price review before source Foundation planning and distinguished the retained fixed runner-image cost from verified pricing. An over-budget compute component blocks while partial in-budget estimates grant no authority. | Current change; pricing and source tests plus common approval regressions (92 passed), one image-review test passed, strict mypy passed, and a bounded real public-price query succeeded without resource effects. | Full installation/setup cost, ownership and minimum-role evidence are still needed before startup scope can become bounded execution authority. Complete source application/Trial and governed live acceptance separately. |
| 2026-09-15 | in-progress | Accepted a start-only installation review after operator feedback. Added explicit setup-cost, Console exposure, dedicated-identity and temporary-cleanup preferences, one bounded initial source confirmation, immutable private retention and unchanged-scope resume without further input. A scope digest is explicitly not human authentication or an exact-plan approval. | Current change; `test_installation_scope.py`, `test_source_azure.py`, `test_cli_help_hardening.py`, `test_cli_help.py` (225 passed), plus strict mypy and Ruff. Tests cover changed target/source/profile/options, budget types, expiry without renewal, denied/closed/partial/timed-out input, real pseudo-terminal input, symlink rejection, non-TTY behavior and no execution before confirmation or prompts at later stages. | Implement and independently verify per-effect bounded authorization, complete price/ownership and least-privilege evidence, and equivalent signed-kit startup-only integration before claiming one confirmation authorizes a whole install. Actual application/Trial deployment remains incomplete. |
| 2026-09-15 | in-progress | Separated public source execution from interactive approval collection after an actual run unexpectedly requested stdin. Text and JSON now run without prompting; an explicit `--approval-file` passes existing exact approval to the shared verifier, while ambient approvals stay unused. Preparation/preflight and kit modes reject that source-only option. | Current change; `test_source_azure.py` and `test_cli_help_hardening.py` (153 passed), Ruff, formatting and four-source-file strict mypy passed. Prior source `5857b77624044c9c3600962e71d4b86131039695` passed CI `34923092657` and supply-chain `34923092659`, then reached the real exact-image prompt without mutation. | Verify installed unattended behavior; retain a valid exact human approval before effects. This change does not authorize future plans, implement standing deployment authorization, or complete application/Trial activation. |
| 2026-09-15 | in-progress | Corrected the remaining fixed Azure CLI path in interactive checkpoint approver lookup after actual source deployment reached the runner-image review but failed before approval. The same fixed-root executable resolver now handles both identity reads; human-only and exact-evidence checks remain intact. | Source `67285095d355754c3818693ab56bff98c3af1b99` passed CI `34921138430` and supply-chain run `34921138346`; its actual interactive run failed with missing `/usr/bin/az`, without mutation. Current change: 14 approval tests passed, including trusted local CLI, non-human rejection, unavailable CLI and exact TTY approval cases; Ruff, formatting and one-file mypy passed. | Publish and validate the corrected exact source, obtain a fresh plan and human approval, then retain image/Foundation effects. Application activation, complete AKS assembly and durable Trial remain open. |
| 2026-09-15 | in-progress | Extended trusted local CLI resolution to runner-image Terraform and operator identity readback, using an owner-only launcher that rejects substitution on resume. | Current change; 56 focused runner-image tests, Ruff and one-file mypy passed. The earlier live source preparation advanced through provider and policy reads but stopped before image planning effects. | Generate and review the exact runner-image plan, obtain required CI and current approval, then retain independent effect evidence; application activation and Trial remain unfinished. |
| 2026-09-15 | in-progress | Replaced fixed Azure CLI execution in Foundation input and VM metadata reads with the existing trusted installation resolver, retaining configuration and timeout boundaries. | Current change; 46 focused Genesis preparation and VM integration tests, Ruff and two-file mypy passed. The repaired metadata reader selected a Foundation VM candidate using authorized read-only provider observations. | Complete exact source planning and required CI before resource effects. AKS supported-SKU admission is separate from Compute SKU/quota feasibility; full activation and durable Trial remain unfinished. |
| 2026-09-14 | in-progress | Connected the public source command to the shared private Foundation coordinator with a source-bound execution lock, read-only provider/policy observations, exact interactive approvals, fresh status validation and expired-approval review. No automatic registration or policy-probe resource creation is introduced. | Current change; source preparation/coordinator and CLI tests verify provider blocking, no approval in noninteractive mode, exact prompt routing, monotonic resumption and false-readiness rejection. No Azure mutation or live Foundation acceptance is claimed. | Complete managed-host source/image transfer, five-service activation, workload identities, HTTPS/Console and persistent Trial enforcement. Confirm full-run deadlines and failure recovery in operational acceptance on an available approved target before publication/merge completion claims. |
| 2026-09-14 | in-progress | Added distinct source inputs to the shared Foundation planner and private coordinator, source-bound exact apply preparation, locked provider acquisition, and source-compatible private state handoff. Included sibling bootstrap/shared modules in the migration archive. | Current change; focused Foundation plan/apply/state/coordinator tests pass. Real Terraform 1.9.8 initialization and mirror creation retained two provider archives without modifying the verified source snapshot or applying resources. Installed CLI help and read-only preflight executed successfully as commands, with deployment blocked by SKU availability. | Connect the public source command through these advanced stages, preserve full-run locking and bounded progress, complete managed-host source/image transfer, five services, identities, HTTPS/Console readiness and durable Trial. Live deployment, access verification, publication and protected merge remain incomplete. |
| 2026-09-14 | in-progress | Connected source preparation and read-only preflight to `fdaictl`, added a source-bound Foundation planner, and routed the source entrypoint toward exact runner-image plan review without kit assembly. Added AKS SKU and quota checks and corrected the two-vCPU system-pool default. | Current change; source admission, CLI, source Foundation, legacy plan and runtime-profile focused tests pass; strict mypy passes for the new planning modules and CLI. No live result is claimed here. | Complete source apply/state transfer and all managed-host stages, persistent Trial, full application readiness, and authorized live acceptance. |
| 2026-09-14 | implemented | Corrected source snapshot link verification after the real repository copy updated symlink access times. Source identity now excludes access time while preserving inode, mode, size, modification and change-time checks. | Current change; 16 source-input tests pass; an actual clean source snapshot of `60d210dadee5f656bb137080e6b7da7dcfd02e3a` verified 19,606 tracked files, excluded Git metadata and the caller environment, and matched its retained digest. | Connect these primitives to the public source deployment path, Foundation, managed-host execution and Trial activation. No source deployment or Azure effect is claimed. |
| 2026-09-14 | in-progress | Accepted a separate connected source deployment path and durable 30-day Trial contract after identifying that Foundation currently consumes signed-kit inputs directly. Rejected signature bypass and automatic mode fallback. | Current change in the owner document and paired Korean translation. | Implement and test source admission, source-bound Foundation and managed-host stages, AKS runtime corrections, Trial persistence and expiry, and exact-target operational acceptance. |
| 2026-09-14 | validated | Reconciled previously published r4 development-kit and cold-installed preparation evidence without rerunning artifact acceptance or changing an installed tool. This corrects blanket unpublished-CLI wording while retaining the exact-source and development-only limits. | [Issue #803 receipt](https://github.com/dotnetpower/fdai/issues/803#issuecomment-5653340906); public asset `561167109`, source `c137aa104682a59b979f5f3554a06bf87c555b8e`, successful CI `34755232779` and `34755464071`; rehashed 866720653-byte archive matches `c7e8b3e99fd77534ad7e2b2321d2e677fb3fe316a946e4c58ef797d5682bbab5`. | [Issue #461](https://github.com/dotnetpower/fdai/issues/461) remains open for current release/target approval and live Foundation, application, inventory, Console, second-run and offline installation evidence. Production trust and appliance execution remain separate. |
| 2026-09-13 | implemented | Integrated the standalone progress display with the installed Foundation process-group supervisor and put optional appliance assembly inside the complete release budget. Status freshness, environment filtering, exact approvals, signed-source fences, fresh output ownership, and valid-checksum requirements remain intact; success follows every requested artifact stage. | PR #899/#914 reconciliation; `current change`; 141 focused process, progress, approval, source-fence, deadline, and release-guard tests passed; Ruff, strict mypy, Bash syntax, and ShellCheck passed. | Retain exact-head CI before protected integration; no release build, signing, or Azure operation was performed by this repair. |
| 2026-09-13 | implemented | Isolated complete builds in a detached pinned-commit worktree and fenced source bytes, modes, and change metadata through support-wheel assembly and immediately before signing. | `current change`; actual Git source-mutation and shell-signing-boundary regressions. | Complete eligible-source build and independent artifact acceptance. |
| 2026-09-13 | implemented | Bounded complete-kit Docker, Console, staging, archive, and checksum commands by per-stage/no-progress deadlines and a shared three-hour budget with process-group termination. | `current change`; real bounded-runner shell regressions. | Verify the bounded complete release from eligible exact source. |
| 2026-09-13 | implemented | Selected the existing env-isolated offline Console build for complete kits and retained the installer archive layout without stale ordinary-build files. | `current change`; executable Console packaging guard and existing Vite/configuration tests. | Verify the produced Console artifact in the complete signed kit. |
| 2026-09-13 | implemented | Prevented archive checksum command failures or malformed output from being reported as successful complete-kit builds. | `current change`; executable checksum-failure regressions. | Complete exact-source CI and artifact verification. |
| 2026-09-13 | implemented | Required a fresh complete-release output directory to preserve prior signed archives and partial image builds on rerun. Lower-level explicitly owned staging retains its separate restaging contract. | `current change`; `test_standalone_kit_release_guards.py`: 2 passing no-provider regressions using the real directory guard. | Complete the signed-kit hardening campaign, exact-source CI, and independent artifact validation. |
| 2026-09-12 | implemented | Added a profile-bearing provider-schema request subtype without another workflow input. Plan, apply, resume, and status now preserve either supported Core image profile and validate provider-specific post-apply artifacts instead of selecting package or general-Core observations from the image profile alone. | `current change`; `fdai_deployment_cli.github_actions`; provider request, metadata, receipt, workflow, and status regressions | Retain one successful Cost-profile provider plan and exact apply, then verify the projected baseline, source generation, and review disposition through `fdaictl deploy status`. |
| 2026-09-13 | implemented | Refreshed the idle installed coordinator with the unboxed stream and current-attempt failure guidance, preserving its noneditable source-directory binding, Python 3.13, and locked runtime versions. | `current change`; 268 focused presentation/supervision tests plus 194 help and existing compatibility-module tests passed; all 56 installed payload files match the local wheel and source; outside-checkout help/version and a synthetic installed display passed with network, child execution, and input denied. All 118811 retained deployment metadata records were unchanged. | No local display-installation work remains. The failed partial image attempt still requires separately reviewed recovery; no Azure operation, signed-kit change, commit, or publication was performed. |
| 2026-09-13 | implemented | Replaced the fixed all-phase panel with colored chronological phase/detail events and a compact current-work view. Distinct child checkpoints append once; heartbeats only refresh activity. Failed child exits can expose fixed runner-image recovery guidance only from bounded, strict, exact-attempt and context-matched private status, without accepting handoff or repeating an effect. | `current change`; two presentation regressions reproduced the prior behavior, then 268 focused stream, diagnostic, coordinator, ANSI-terminal, Foundation adapter, status and application-progress tests passed; Ruff and formatting over six files and strict mypy over three changed sources passed. | Verify the idle installed coordinator before using the display. Existing partial-image recovery still needs its own reviewed exact plan, fresh approval, and independent effect evidence; no Azure deployment or signed release is included. |
| 2026-09-12 | implemented | After the operator's deployment process ended, refreshed only the local coordinator using the same noneditable source-directory binding, Python 3.13, and locked offline dependencies. No deployment was restarted. | `current change`; the local wheel contains all four changed modules, all 55 installed package payload files match that wheel, and an outside-checkout installed-display smoke passed with network, child execution, and input denied. All 76063 retained deployment metadata records were unchanged; the version JSON contract remains 0.1.0. | No local display-installation work remains. Source publication and operational Azure validation are separate outcomes; the existing signed kit and run records were not changed. |
| 2026-09-12 | implemented | Corrected the split Foundation presentation exposed by an operator-run deployment. Earlier terminal work styled the outer 13 phases but deliberately yielded the whole Foundation interval to its 15-stage ASCII output. The local coordinator now adapts only the fixed Genesis banner, exact progress records, and heartbeat-only dots into one dashboard; malformed/native diagnostics and exact approvals remain visible. | `current change`; 236 focused tests passed, including actual producer output, local child exit/stdout, partial UTF-8, warning/prompt visibility, inherited descriptors, original failures and timeouts, and final screens at 36x12, 60x14, 80x24, and 120x30. | Install the reviewed display update only after the active deployment is idle. No running CLI, signed cache, Azure resource, approval, or execution state was changed; this is not a live deployment receipt. |
| 2026-09-12 | implemented | Added profile-specific Cost Governance plan and apply status validation. The client accepts only the sealed Cost observation list, verifies both fixed Job container bindings and their shared image digest, and recomputes the downloaded readback and apply-receipt digests. | `current change`; `fdai_deployment_cli.github_actions`; focused deployment CLI, plan metadata, apply receipt, convergence, and workflow tests. | Retain one successful protected Cost apply artifact and verify it through `fdaictl deploy status` before lifecycle installation. |
| 2026-09-11 | implemented | Added a source-checkout `fdai-up.sh` command that supervises the existing packaged contracts through exact TTY approvals, private Foundation, exact-main image attestations, protected application apply, and a second zero-change plan. | `current change`; focused Genesis and deployment transport tests plus protected workflow contract tests. | Merge and run the exact revision, retain governed receipts, then decide whether the same boundary is ready for the published `fdaictl` surface. |
| 2026-09-11 | implemented | Aligned packaged-client context digests with the server's request-derived runtime-call transition state without resending the removed workflow input. | `current change`; `fdai_deployment_cli.github_actions` and focused package plus request-validation tests. | Retain one accepted protected plan receipt from the aligned client and server contract. |
| 2026-09-10 | implemented | Added a local 15-stage pre-runner coordinator that consumes the packaged Foundation contracts and advances four separately approved private effects with verification-only restart. | `current change`; focused Genesis tests passed 93 cases and saved-plan package tests passed 49 cases. | Keep the command local until one exact revision passes CI and produces governed Foundation evidence; package/publication scope remains unchanged. |
| 2026-09-10 | implemented | Added the System Knowledge Service candidate to deterministic service-wheel staging and productization checks without expanding the validated deployment image set. | `current change`; focused staging and productization tests. | Retain the candidate's production identity, persistence, canary, cost, disable, and timed rollback evidence before adding it to the runtime image release. |
| 2026-09-08 | implemented | Added `--deploy-operator-channel-edge` to the protected plan/apply context and removed the server-side unsupported-input rejection while retaining exact digest validation. | `current change`; deployment CLI, request validation, and platform workflow tests. | Generate one request-bound zero-destroy plan, apply it through the single-maintainer dev policy, and verify the identity roles before enabling the Operator workload. |
| 2026-09-06 | implemented | Made OCI and support-installer tests portable across package-local and repository-root collection, and made the oversized-header regression deterministic on Python 3.13. | `current change`; 52 Python 3.13 OCI tests and 77 repository-root support/ACR tests passed. | No implementation work remains for this bounded test portability correction. |
| 2026-09-04 | implemented | Corrected status validation for the request-bound RCA mode so `plan-rca-*` and `apply-rca-*` remove the complete reviewed mode prefix before comparing the embedded target/context binding. | `current change`; focused status and deployment CLI checks. | Retain the exact plan metadata and apply receipt. |
| 2026-09-04 | implemented | Added an exclusive `--deploy-rca-reader-identity` selection that derives `plan-rca-*` and `apply-rca-*` request ids while preserving the 25-input GitHub workflow ceiling. | `current change`; deployment CLI, request validation, plan-scope, and workflow checks passed 239 cases; Ruff and strict mypy passed. | Retain the exact plan, apply, output, and split Core consumption receipts. |
| 2026-09-03 | implemented | Made a selected Core runtime revision independent of isolated Executor deployment and derived plan-time image promotion from that sealed revision. | `current change`; `fdai_deployment_cli.github_actions`, deployment request validation, and focused CLI/workflow tests | Retain one protected plan and exact apply receipt using a runtime revision without Executor deployment. |
| 2026-08-14 | in-progress | Adopted the implementation ledger; earlier provenance was not reconstructed. Corrected prior claims that the removed deployment CLI package and its commands were currently available. | current change; package metadata, release scripts, protected workflows, and focused workflow checks listed in the scope table | Create the dedicated CLI distribution, restore verification behind that boundary, and prove installation plus disconnected use. |
| 2026-08-19 | deferred | Removed the manual deployment-bundle workflow because it invoked the unavailable `fdaictl bundle verify` command and had no successful run evidence. | issue #222; current workflow inventory and focused CI contract tests | Add the dedicated CLI verifier first, then restore a protected release workflow with clean-checkout evidence. |
| 2026-08-19 | deferred | Removed the retired workflow-only test from productization validation so the release check no longer requires a path that the deferred workflow removed. | current change; `scripts/deployment/release/verify-productization.sh`; focused productization and structural-gate tests passed 55 cases | Restore the workflow test only with an executable CLI verifier and protected release workflow. |
| 2026-08-29 | implemented | Added the independent `fdai-deployment-cli` wheel with the `fdaictl` entry point, secure profile and journal contracts, signed artifact verification, and no-mutation genesis rehearsal. | `current change`; focused package and productization tests passed 28 cases; Ruff and strict mypy passed | Complete protected plan/apply adapters, publish the wheel, and retain the disconnected drill. |
| 2026-08-29 | validated | Hardened the pre-login CLI and release path through 75 focused rounds and completed the shipped-wheel air-gap drill without network or DNS. | Campaign commits from `dd28b64d9`; 89 focused tests, Ruff, strict mypy, locked wheel build, isolated install, Terraform validation, 20 Terraform tests, and successful `airgap-drill.sh` | Complete protected Azure dispatch and publish the first signed release. |
| 2026-08-30 | implemented | Added target-pinned bootstrap reconciliation before the first runner or private backend exists. The command distinguishes not-found from unreadable state, checks global storage-name availability and private keyless posture, and separates deterministic intent from expiring observations without mutating Azure. Journal schema v3 now binds every completed stage to a receipt digest, legacy journals are replay-only, and the aggregate readiness contract requires foundation through system-verification evidence, complete inventory closure, and a zero-change second run. Thirteen hardening rounds closed every Medium finding. | `current change`; `bootstrap_plan.py`; `bootstrap_reconcile.py`; `private_output.py`; `readiness.py`; focused bootstrap, plan-input, journal, readiness, simulation, and workflow tests; final independent severity audit found no Medium-or-higher issue | Add the separately approved foundation apply and remote-state handoff before enabling protected application planning. |
| 2026-09-06 | implemented | Added fail-closed OCI/ACR and Entra bootstrap seams for future protected executors. They perform local validation before credential use, reject ambiguous ownership, preserve unrelated directory entries, and require independent readback without granting deployment authority. | `current change`; 145 focused tests, Ruff, and strict mypy passed. | Bind the seams only from separately authorized protected executors, then retain dispatch, readback, recovery, and audit receipts. |
| 2026-09-09 | implemented | Added a deterministic, digest-bound runtime v2 assembler and a complete air-gap mode that installs the staged CLI and invokes no-network preparation over five services, ClamAV, Console, and deployment support. | `current change`; runtime builder and wrapper, complete-mode drill, focused runtime/offline/productization tests, Ruff, and strict mypy | Produce eligible exact-revision artifacts and retain a passing complete drill; production trust, publication, protected offline apply, and operational receipts remain open. |
| 2026-09-09 | implemented | Bound descriptor-driven runtime assembly to the exact deployment bundle generated inside offline kit staging, removing the circular prebuilt-bundle assumption while preserving the existing prebuilt runtime path. Restored the service-owned bundle-builder import path and added shell-level signing-key symlink rejection. | `current change`; release staging, air-gap argument contract, bundle-builder import, and focused productization checks | Run descriptor-driven complete mode with eligible release inputs and retain its no-network receipt. |
| 2026-09-09 | implemented | Accepted the protected publisher's Docker schema 2 image family without converting its manifest digest, while retaining the strict OCI layout, hash, revision, platform, file-set, and media-family checks. | `current change`; OCI validator and focused positive and negative media-family tests | Produce exact current-revision archives and retain the complete no-network receipt. |
| 2026-09-09 | implemented | Replaced the missing ambient `unzip` prerequisite with a no-follow, bounded Python extractor that accepts only the pinned Terraform archive's official member set and modes and removes partial output on failure. | `current change`; Terraform archive extractor, path and archive rejection tests, release staging, and productization checks | Retain the descriptor-driven complete drill on the release host. |
| 2026-09-09 | implemented | Restored the service-owned Core trust import path for public-key derivation and final kit signing after executable staging exposed the remaining retired-root assumptions. | `current change`; release staging, focused productization checks, and throwaway-key staging smoke | Rerun staging through signed kit completion, then retain the descriptor-driven complete drill. |
| 2026-09-10 | implemented | Corrected complete-mode external materialization after an exact-image drill reached runtime preparation but the authenticated snapshot contained only executable inputs. The signed manifest file set, not a shell mode flag, now selects complete verified materialization before installed tooling reads runtime metadata. Verification also bind-mounts the guarded work directory at a namespace-local top-level path, preserving strict private-output ownership checks when host-root ancestors are unmapped; toolchain-only mode remains bounded to its smaller input set. | `current change`; complete-mode productization regression, shell syntax check, and exact-image resumed drill | Rebuild the exact-revision images after this fix and retain fresh and resumed descriptor-driven complete drill receipts. |
| 2026-09-12 | implemented | Corrected installed online and artifact-offline execution after the first live policy route proved that normalized bundle sources are mode-restricted and cannot be invoked directly. Bundle-owned shell helpers and service migration launchers now run through fixed interpreters while native binaries retain separate executable verification. | `current change`; Azure bootstrap scripts, `standalone_host.py`, and focused normalized-mode policy and migration regressions | Publish a replacement signed kit from exact green `main`, then retain online and artifact-offline Azure convergence receipts. |
| 2026-09-12 | implemented | Removed GitHub workflow dispatch from the public deployment CLI and provisioning profile, made live onboarding use the standalone coordinator, allowed token-free observation-only installation, added explicit deployment readiness, and added a signed-kit OCI deployment-appliance builder and entry point. | `current change`; deployment CLI contracts, standalone modules, appliance scripts, focused package and integration tests | Build one clean appliance from an approved base and retain connected and artifact-offline Azure deployment receipts before advancing validation state. |
| 2026-09-12 | implemented | Completed twelve adversarial hardening rounds over artifact TOCTOU, private paths, approval identity, license binding, checkpoint concurrency, OCI supply, CLI authority, schemas, secret inheritance, cleanup, and documentation. The final independent audit reported zero Critical, High, or Medium findings. | `current change`; re-archived and reverified kit, atomic no-clobber OCI publication, current-target actor binding, exclusive managed-host checkpoint lock and claim, local digest-pinned base check, ambient-secret allowlist, 806 package tests, 79 focused integration tests, Ruff, strict mypy, ShellCheck, and Pylance diagnostics | Build the clean production appliance and retain connected and artifact-offline Azure receipts before changing operational validation state. |
| 2026-09-12 | implemented | Added a Rich-based local deployment dashboard with real phase transitions, elapsed timers, bounded download notices, automatic plain output, explicit progress modes, and failure/cancellation cleanup. Existing approval prompts retain terminal ownership, intermediate Foundation JSON no longer pollutes stdout, and a ready summary requires the existing verified result. | `current change`; 61 focused renderer, coordinator, host, approval, and offline-kit tests passed; Ruff, strict mypy over five changed modules, and the actual source wrapper's help path passed. Rich dependencies are locked and selected by the existing offline wheelhouse export. | Operator-run deployment and release publication remain separate; local rendering and mocked boundaries do not certify an Azure deployment or alter the existing signed kit. |
| 2026-09-12 | implemented | Completed R01-R14 local terminal/supervision hardening. Corrected failed-child and stale-status advancement, unhandled approval EOF, pre-validation approval display, short-screen clipping, approval-text erasure on Live restart, secondary output error masking, FORCE_COLOR log pollution, and interrupted Live startup/resume cleanup. Earlier local completion claims did not cover these cases. | `current change`; 143 focused tests passed; combined statement/branch coverage for progress, status, and review modules is 97.05% (review/status 100%); ANSI emulator sizes 36x12, 60x14, 80x14, and 120x30; source-wrapper help, offline local wheel build, and runtime dependency export passed. R01/R02 also have isolated local checkpoint `fb59d9996088972238c3bb0bb895c9706f49d09b`, with 19 tests and commit hooks passed. | No confirmed finding above Low remains in the reviewed local progress/supervision slice. This is not a release or live Azure safety assertion. Other shared deployment edits remain uncommitted and no push or Azure deployment was performed. |
| 2026-09-12 | implemented | Added successful bare-command and group discovery, command and option descriptions, grouped Azure inputs, copyable examples, exact long-option parsing, next-help error hints, and a top-level version alias. Separated static grammar from execution and deferred home resolution. Completed the 16 local help rounds below; an actual smoke check found an older noneditable installation, which was rebuilt offline using the existing mode and locked dependencies. | `current change`; 200 focused tests passed, including 26 fresh-process no-side-effect cases; combined statement/branch coverage for help/parser is 98.97%; Ruff and format over eight files, strict mypy over five sources, and editor diagnostics passed. Fourteen outside-checkout installed-command checks passed; all five changed source modules byte-match the installed wheel. | No confirmed finding above Low remains in the reviewed help/parser slice. Python 3.14+ rendering and terminals below 40 columns were not exercised. A checkpoint is deferred because the facade and owner documents also contain prior changes; no commit, push, Azure operation, or release was performed for this change. |
| 2026-09-12 | implemented | Reproduced retained-download `FileExistsError` being hidden as a network download failure; earlier help and terminal evidence did not cover repeated acquisition. Added source-request binding, per-directory acquisition exclusion, full cached-input revalidation, fresh bundle execution copies, and separate HTTP, connection, local-conflict, permission, and storage errors. Corrected guidance that incorrectly described the private work directory as credential-free. | `current change`; seven initial reproduction cases failed, then 89 focused tests passed; the cache helper reached 98.10% combined statement/branch coverage. Ruff/format over three files and strict mypy over two sources passed. A local kit-only run with network, subprocess, and input disabled verified 300 signed entries and preserved metadata for 10891 existing artifact and run files. The installed CLI was rebuilt offline; both changed modules byte-match tested source and installed help passes. | This fixes the online acquisition collision, not a historical HTTP diagnosis or Azure deployment. The cached network selector still differs from the corrected checkout source; a replacement signed kit and separately authorized operational verification remain open. Artifact-offline retry parity and bounded execution-copy retention are separate follow-up work. No push, release publication, or Azure operation was performed. |

### Remaining work

- [ ] Publish the exact Console update controls, then retain one private exact-plan/apply receipt and
  repository-safe authenticated browser evidence for an existing development installation.
- [ ] After an approved recovery, retain real host enrollment evidence bound to its unaltered
  recovery receipt and a separate current enrollment approval. Retain actual recovered configuration
  and original-state migration evidence, then retain a successful public recovery resume through
  both receipts and original-source host transfer without fabricating ordinary Foundation success.
- [ ] Integrate the initial scope with independently validated per-effect bounded authorization;
  require exact plan, ownership, cumulative cost, revocation, safeguards and cleanup evidence.
  Prove complete source and signed-kit installation with one initial review and no later stdin.
- [x] Retain the exact r4 development-kit publication and 11-check cold preparation receipt above; no new build, install, provider call, or deployment was performed by this reconciliation.
- [x] Verified the unboxed stream and current-attempt failure guidance in the idle installed CLI.
  All 56 payload files match the local wheel and source; the original installation mode, locked
  runtime versions, and 118811 deployment metadata records are preserved.
- [x] Installed the reviewed unified Foundation display after the idle-process check. All 55
  package payload files match the local wheel, and the installed synthetic display smoke passes
  without network, child execution, input, or deployment-state changes.
- [x] The dedicated CLI distribution, source invocation, isolated wheel, deterministic `version`,
  target-aware inspection, private profile handling, and local security boundaries pass focused tests.
- [x] Static command discovery, strict executable-argument validation, and installed help pass the
  focused checks in the current history row. This does not close deployment or publication evidence.
- [x] Repeated online kit acquisition passes signature, tamper, source-binding, concurrency,
  no-clobber, archive-bound, redacted-error, and local retained-artifact checks in the latest history row.
- [ ] Prove equivalent repeated acquisition for explicit offline directories and archives, and define
  bounded cleanup of obsolete execution copies without removing unfinished-run evidence.
- [ ] Implement the remaining `security audit` checks and pass deterministic text and JSON tests.
- [x] Removed public `deploy plan`, `status`, and `apply` workflow-dispatch commands; live tenant deployment uses `provision azure` with manual transport only.
- [ ] Expose the existing deterministic and live preflight analyzer through the packaged `deploy preflight` facade without duplicating its authority rules.
- [ ] Bind `bootstrap-reconcile` to an approved foundation apply that revalidates its exact target,
  intent digest, observation freshness, and conflict-free state before any provider or resource
  mutation.
- [ ] Retain one exact `fdai-up.sh` Foundation, protected application, and second-plan receipt
  before considering the same supervisor for the published `fdaictl` surface.
- [ ] Retain one exact live private-relay Run Command staging receipt on an eligible peered host,
  then connect the explicit adapter to reviewed `fdaictl` access-profile routing without granting
  VM lifecycle, Terraform apply, or application authority.
- [x] Bundle and offline-kit verification are in the dedicated package, and the shipped-wheel
  network-free air-gap drill completed successfully.
- [ ] Build and independently verify a replacement complete local kit whose bundle invokes every
  mode-restricted source through a fixed interpreter, then retain one live policy-route and
  migration receipt without requiring public publication.
- [ ] Pass the explicit complete air-gap mode with eligible runtime v2 artifacts from a clean exact revision; the historical toolchain-only drill does not satisfy this exit.
- [ ] Accept a release-published, digest-pinned OCI deployment appliance, verify its SBOM,
  provenance and embedded kit, and retain one artifact-offline Azure deployment receipt without
  constructing an image during tenant provisioning.
- [ ] Remove source-mode Docker/Buildx preflight and image-build recovery, then add prebuilt runtime
  artifact manifest admission with source-revision, signature, provenance, SBOM and digest checks.
- [ ] Restore the protected signed-bundle publication workflow after its clean-checkout tests pass.
- [ ] Bind OCI publication and Entra bootstrap to distinct protected executor identities, then retain independently verified ACR, directory, and audit receipts without adding a public mutation command.
- [ ] Publish the first pinned CLI release with the offline trust root and prove install, upgrade, rollback, internal-mirror delivery, and guarded teardown with reviewable receipts.
- [ ] Retain a bounded RCA reader identity plan and exact apply, then prove the split Core plan consumes the resulting platform output.

## Local terminal hardening rounds (2026-09-12)

The evidence below is local and synthetic at every external effect boundary. Rounds with no new
defect are coverage or falsification rounds, not invented findings. Initial review suggestions
about partial status writes, hidden stderr, and unsafe cleanup success were rejected after source
inspection: status writes use fsync plus atomic replacement, stderr remains inherited, and cleanup
failure blocks the receipt.

| Round | Highest confirmed severity before fix | Critique and outcome | Evidence |
|-------|---------------------------------------|----------------------|----------|
| R01 | High | Failed or signal-terminated children could advance using old handoff status. Exit codes now gate status consumption first. | Coordinator exit-code regressions failed for `1` and `-15` before the fix. |
| R02 | High | A waiting exit could reuse an unchanged status. Exact next attempt, source, run, mode, and status type now gate acceptance. | Current-status suite rejects stale, skipped, malformed, missing, mismatched, and linked inputs. |
| R03 | Medium | EOF in either approval input or the identity path produced an unhandled traceback. EOF now denies new approval with a stable exit. | Both approval prompts and CLI EOF regressions failed before the fix and now pass. |
| R04 | High | Unvalidated review stages could influence approval paths, and invalid counts or digests reached human confirmation. Reviews now validate before rendering and after the approval wait. | Thirteen malformed/tampered review cases reached input before the fix; schema, path, count, digest, extension-field, and expiry tests now pass. |
| R05 | Medium | Short terminals clipped current details and final failure context. Compact rendering keeps these in the reviewed viewport. | ANSI screen regressions at 36x12, 60x14, and 80x14 failed before the fix; 120x30 preserves the full table. |
| R06 | Medium | Restarting the same Rich Live footprint erased approval text. A fresh Live instance preserves review and input history. | ANSI prompt/readback, nested output, cursor, stream ownership, and refresh-thread tests. |
| R07 | Medium | Terminal disconnection while printing failure replaced the original exception or Ctrl-C. Cleanup preserves the original error. | Four injected plain/live output failures reproduced masking before the fix. |
| R08 | Medium | FORCE_COLOR could turn redirected logs into ANSI redraws. The default renderer now uses actual stderr TTY detection. | FORCE_COLOR, NO_COLOR, TERM, redirected logs, and pure JSON coordinator tests. |
| R09 | None | Bounded details and validated review fields resist control characters, extra payload fields, and markup interpretation. | ANSI, carriage return, bidirectional control, markup, and review extension-field tests passed. |
| R10 | None | Counts must not imply readiness, timers must freeze, and approval must not survive expiry. | Zero-time/hour timers, incomplete-ready rejection, expiring confirmation, and managed-host approval validation passed. |
| R11 | None | Download instrumentation must not alter bounds, no-clobber writes, or interruption cleanup. | Empty, exact-limit, oversized, interrupted, existing-file, observed-byte, and bounded-redraw tests passed. |
| R12 | None | Remote failure must remain on the correct phase and never persist a terminal receipt. | All eight application checkpoints plus invalid remote JSON and a synthetic positive path passed without remote execution. |
| R13 | None | Test-only rendering tools must not enter runtime packaging, and the actual wrapper must expose the modes. | Locked runtime export excludes pyte/wcwidth; local wheel contains the three new modules; wrapper help and build passed. |
| R14 | Medium | The final review found a partial Rich startup/resume Ctrl-C window; the shared startup cleanup now restores ownership and preserves the interrupt. Added skipped-attempt and non-string status checks. | Initial and resumed startup regressions reproduced failures, then the complete focused 143-test set passed with 97.05% combined coverage. |

Remaining Low presentation limitations: unreviewed terminal fonts can render spinner glyphs differently,
and viewports smaller than 36x12 should use `--progress plain`. Native Foundation details still
own the terminal temporarily by design so exact review evidence is never concealed. The published
signed kit and real Azure execution remain outside this local verification scope.

The shared editor interpreter reports Low development-only `reportMissingImports` warnings in
some package tests because it does not resolve the separate deployment CLI source root there.
Focused tests explicitly bind that root through `PYTHONPATH`, and the installed wrapper, strict
type checks, and wheel checks pass. This review did not change shared editor configuration or
hide the warnings. These are not runtime import failures in the locked deployment CLI environment.

## Local CLI help hardening rounds (2026-09-12)

These rounds concern command discovery, parser compatibility, static terminal text, and the selected
local installation. They do not repeat or extend the earlier deployment safety claims. A round with
no new defect records a tested invariant or a rejected hypothesis, not an invented finding.

| Round | Highest confirmed severity before fix | Critique and outcome | Evidence |
|-------|---------------------------------------|----------------------|----------|
| H01 | Low | Bare root incorrectly reported a missing command. It now matches explicit help and exits 0. | The initial help suite reproduced the error; root equivalence and explicit-argv tests pass. |
| H02 | Low | Bare groups also failed and gave no navigation. Each group now describes its own commands without selecting an operational handler. | All five groups pass equivalence and handler-denial tests. |
| H03 | Low | Root help lacked explanations and a conventional version alias. Added both without changing version JSON. | Initial alias/description failures are fixed; text and JSON contracts pass. |
| H04 | None | Discovery defaults must not turn malformed leaf invocations into successful help. | Missing values, required inputs, unknown commands/options, and next-help error-stream tests pass with exit 2. |
| H05 | Low | Argparse's long-option abbreviations made option spelling implicit. All descendant parsers now require complete long names. | Abbreviated options and misspelled commands are rejected; command-name abbreviation was never supported by argparse. |
| H06 | None | Source exclusivity, types, defaults, and real leaf handlers must survive registration changes. | Conflicting sources fail; typed local-kit paths, default budgets, and existing preparation/profile tests pass. |
| H07 | None | Help must not sign in, inspect private state, download, prompt, or write. | Sixteen leaf guards and 26 fresh processes deny socket/subprocess/input/write/private-home access; all pass without Azure tools. |
| H08 | None | Static help and version JSON must not inherit Rich color or corrupt streams. | Forced-color, dumb-terminal, no-ANSI, stdout/stderr, and single-JSON-document cases pass. |
| H09 | Low | At 40 columns, argparse's full-path indentation overflowed; two resource-group metavariables also overflowed. | Reproduced Azure lines up to 60 columns and two 41-column option lines; compact usage and metavariables now pass all 22 parser paths at 40, 60, 80, and 160 columns. |
| H10 | Low | Generic path descriptions obscured Console writes and profile creation. Cost, timeout, and omitted-token wording could overstate their guarantees. | Command-specific descriptions now distinguish input/output, a cost review input from a billing cap, Foundation timing from a total deadline, and omission from revocation; source review and help assertions pass. |
| H11 | Medium | Resolving the default home while building every parser made even help/version fail when no home could be determined. | Four failures reproduced; deferred resolution fixes discovery while both default and explicit relative execution paths retain their original values. |
| H12 | None | Shared registration must not reveal an internal recovery option or overwrite explicit descriptions. | Saved-plan hidden-flag and standalone-helper tests pass; existing file-only comparison still writes its private receipt. |
| H13 | Low | Help declarations expanded an already large execution facade. Moved grammar into a dedicated module with an explicit handler map. | All 14 supplied handlers and two local verification registrations remain bound; strict mypy and focused regression pass. |
| H14 | None | A closed help pipe was suspected to raise a traceback. Inspection showed standard argparse intentionally suppresses that output error. | The hypothesis was rejected; root/explicit-help pipe tests preserve standard behavior without added exception masking. |
| H15 | Low | Source tests did not prove the selected installed command had updated. The first outside-checkout smoke test found an older snapshot installation. | Preserved noneditable mode and Python 3.13, reinstalled from the lock with cached artifacts, then passed 14 exit/stream checks and five source/wheel byte comparisons. |
| H16 | None | Final review must distinguish local help completion from deployment readiness and preserve bilingual instructions. | 200 focused tests, 98.97% combined help/parser coverage, Ruff/format, strict mypy, editor checks, and paired command-discovery documentation support only the stated local scope. |

Remaining Low coverage limits: rendering was executed on Python 3.13.14, not every supported Python
release, and the minimum reviewed width is 40 columns. The formatter disables argparse color on
Python 3.14+, but that branch still needs execution on that interpreter. No live Azure operation,
release build, signed-kit modification, or remote publication was used as a help test.
