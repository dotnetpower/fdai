# Deploy and Onboard implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope
| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Protected platform plan and exact apply | implemented | `.github/workflows/deploy-dev.yml`, `.github/workflows/model-settings-projection.yml`, `login-deploy-identity.sh`, `materialize-model-binding-proposal.sh`, `model_binding_proposal.py`, and focused deployment workflow checks | Every protected deploy path selects the stable deploy UAMI by client ID and verifies its token `oid`. The core-model-quorum profile adopts exact-name existing deployments, permits only the initial creation set, an empty converged plan, or the field-pinned legacy primary replacement, and rejects every other change. The operational-history profile additionally permits only a create-before-destroy, same-scope `Storage Blob Data Owner` handoff to the repository-bound deploy UAMI. Plan and apply restoration use the same deployable-capability filter; quorum apply limits readback to its two targets and skips unrelated model drift and app health. Private-runner planning includes immutable apply claims and post-apply checks; no governed UAMI role-migration or platform apply receipt is retained in the repository. |
| Protected document OCR lifecycle | implemented | `document_ocr_proposal.py`; `materialize-document-ocr-proposal.sh`; Document Intelligence Terraform module; focused workflow and Terraform tests | The runner joins exact proposal, policy, and plan-request records before deriving preserve, local-retain, Azure-provision, or deprovision actions. Plan is the default and no apply receipt is yet retained. |
| Installable protected deployment transport | implemented | `fdai_deployment_cli.github_actions`; `validate_deploy_request.py`; focused CLI and workflow checks | Dev and staging requests bind target, region, context, mode, run, and attempt. Neither the client nor the workflow gates apply on a GitHub Environment approval, so an authorized dispatch applies immediately; production inputs remain explicitly unsupported. |
| Protected static publishing | implemented | `.github/workflows/publish-console.yml`; `publish-console.sh`; `test_service_deploy_workflow.py` | The exact-revision release uses the apply-synchronized protected Static Web App binding with an exact Terraform-state fallback, then uploads and verifies the combined Console and Manual Studio artifact without promoting a catalog image, running schema migrations, or refreshing PostgreSQL projections. A successful protected publication and live Manual Studio hash readback remain open. |
| Independently owned runtime services | in-progress | `.github/workflows/service-deploy.yml`; `config/independent-service-live-evidence-manifest.json`; failed run `33985686835` | Each service has a separate root, protected plan, health check, and rollback evidence. Plans remain service-parallel, while apply and state migration now serialize per environment so a concurrent peer writer cannot invalidate isolation evidence. A new successful live receipt is required to restore `validated`. |
| OHL scale-out evidence target and proposal Job | implemented | current change in `infra/` and `services/core-control-plane/src/fdai/delivery/`; focused Terraform and publisher tests report 8 and 13 passed | Both are disabled by default and still need a protected apply. |
| OHL production evidence campaign | in-progress | `config/ohl-scale-out-evidence.json` and `docs/runbooks/ohl-scale-out-evidence.md` | Runtime rollout, governed execution, 100 samples, and the 14-day recurrence window remain open. |
| Local destructive-validation isolation | implemented | `infra/local/docker-compose.yml`, local preparation scripts, and focused migration tests | Runtime uses local PostgreSQL on port `5432`; destructive validation uses a separate local cluster and volume on port `5433`. This does not add an Azure deployment resource. |
| Inventory-backed analyzer Job targets | implemented | `analyzer_tick_cli.py`; `analyzer_targets.py`; `analyzer_tick_job.tf`; focused analyzer and infrastructure tests | The Job merges explicit targets with supported resources from the durable inventory projection under a configured ceiling. Without an inventory DSN it keeps the explicit-only path; with neither source it exits as a clean no-op. |
| Analyzer Job trace-topology binding | implemented | `trace_continuity.py`; `analyzer_tick_cli.py`; `analyzer_tick_job.tf`; `test_detection_readiness.py`; focused trace checks | Optional deployment-supplied topology declarations reuse the existing Job, reader identity, Log Analytics workspace, and Event Bus. Empty configuration preserves the current analyzer-only path and no Azure resource is added. |
| Continuous inventory Job | implemented | `inventory_job.tf`; `inventory_job_config.py`; focused inventory and infrastructure checks | A minute cron drives change drains and due checks. Terraform carries the change floor, progress and absolute deadlines, and shared ARG request budget. Protected apply and live cadence evidence remain open. |
| Global provider-schema Job | implemented | `provider_schema_job.tf`; `provider_schema_watcher_cli.py`; `provider_schema_state_ledger.py`; focused provider, Pantheon, Terraform, and infrastructure checks | A daily read-only Job resolves immutable upstream revisions, persists its append-only ledger through private PostgreSQL, and routes strict material drift through Heimdall's existing shadow topic. A protected apply and scheduled-run receipt remain open. |
| OHL signed observation context | implemented | `infra/main.tf`; `infra/services/core-control-plane/`; `scripts/deployment/service/hydrate_observation_context.py`; focused Terraform and hydration checks | The platform stores a random Ed25519 seed in Key Vault and the Core service consumes only its Managed Identity-backed reference. Protected apply and verified mailbox evidence remain open. |
| Phase 4 measurement Jobs | implemented | `infra/modules/measurement-runners/`; root Terraform composition; focused Terraform plan and Python contract checks | All three jobs are disabled by default, use a dedicated non-executor identity, and expose only nullable outputs until enabled. Protected apply and scheduled-run receipts remain open. |
| Phase 3 scheduler and DB-DR Jobs | implemented | delivery-owned CLIs; Azure/PostgreSQL DB-DR adapters; compute and root Terraform; focused tests | Each job has a distinct non-executor identity and a complete importable entrypoint. Protected apply and scheduled-run receipts remain open. |
| Core handover lifecycle deployment binding | implemented | platform stewardship GitOps output; protected service tfvars materializer; `infra/services/core-control-plane/`; focused workflow, Terraform, Core, and Operator checks | The independent Core service receives only a Key Vault token reference plus separate liveness and content-free knowledge-event intervals. Protected apply, Graph recovery, and scheduled-run receipts remain open. |
| Refreshable stewardship GitHub authentication | implemented | `packages/github-app-auth`; Core and document-ingestion adapters; platform and independent-service Terraform roots | The platform exports exactly one static compatibility or GitHub App private-key reference. Both services share the refreshable async provider contract, while ingestion separately receives the signed-webhook secret and explicit activation flags. Provider registration and live receipts remain deployment-owned. |
| Bot-owned exact service apply | implemented | `request-protected-operation.yml`; `validate_protected_service_apply_request.py`; focused validator and workflow checks (`32 passed`) | Core and document-ingestion apply requests validate one exact protected plan before the bot dispatches the Environment-gated apply. Document database-host and SharePoint transitions are derived from the validated plan mode, so the human reviewer remains distinct without introducing an unbound transition choice. |
| Disconnected runtime preparation and bootstrap | implemented | `fdai_deployment_cli.offline_prepare`, runtime release and support installers, offline staging scripts, `infra/bootstrap`, and focused Python and Terraform checks | Preparation verifies an exact kit and profile without network access or cloud mutation. Offline bootstrap accepts only a pinned managed image or numeric gallery version and disables cloud-init and GitHub registration. |
| Initial PostgreSQL credential bootstrap | implemented | `infra/initial_postgres_credential.tf`, root variable validation, state-store binding, and provider-free Terraform tests | Fresh deployments may explicitly generate one strong state-persistent administrator credential during approved apply instead of transporting it through chat or CLI. Supplied-password behavior remains the default, both inputs together fail closed, and enabling generation for an existing server remains a separately approved credential change. |
### Implementation history
| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-06 | implemented | Added refreshable, repository-scoped GitHub App authentication to Core PR publication and ingestion merge verification, with Key Vault references and backward-compatible static-token normalization. | `current change`; 516 focused tests; strict mypy; platform, Core, and ingestion Terraform validation. | Install the App on the private deployment-config repository and retain token-refresh plus end-to-end handover receipts. |
| 2026-09-06 | in-progress | Serialized service apply and state-migration writers per environment after a Core apply completed and passed health but lost its peer-isolation receipt to a concurrent document-ingestion state update. Service plans remain independently parallel. | Failed protected apply `33985686835`; `current change`; focused service workflow checks. | Publish an exact-green revision and retain one bot-owned no-destroy Core apply whose peer-isolation and live-observation receipts both succeed. |
| 2026-09-06 | implemented | Allowed bot-owned Core applies to preserve standard, database-host, model-binding, and combined database/model modes from the exact validated plan. | `current change`; focused protected service validator and workflow checks. | Publish the change and create a fresh exact-green Core plan; the prior plan remains bound to a red source revision. |
| 2026-09-06 | implemented | Added an account-scoped custom role for the development gateway's governed model deployment operation. | `current change`; `infra/dev_gateway_model_deployment_rbac.tf`; `terraform -chdir=infra validate`; focused development gateway Terraform test reports 9 passed. | Retain one protected plan and apply receipt before treating the role assignment as deployed evidence. |
| 2026-09-06 | withdrawn | Removed the development gateway model-deployment role because conversational resource creation is outside the IaC-managed customer boundary. Existing deployments remain discoverable through the read-only inventory and ontology. | `current change`; model-deployment RBAC and gateway mutation paths are absent; resource-type and relationship catalogs remain. | Retain an authorized live inventory receipt for existing model deployments; no model-deployment apply is planned. |
| 2026-09-06 | implemented | Extended the bot-owned exact service apply path to document ingestion after a direct human-requested apply became permanently ineligible for self-review. The wrapper now derives every service transition from validated plan metadata. | `current change`; protected service apply validator and workflow checks passed 32 cases; Ruff and strict mypy are included in the focused gate. | Publish the change, require exact-main CI, then replace the cancelled direct apply with a bot-requested apply and retain independent approval plus health evidence. |
| 2026-09-06 | implemented | Added a field-pinned operational-history deployer-role handoff after the protected plan exposed the legacy runner principal replacement. The new assignment must be created before the old one is removed, keep the storage scope and role unchanged, and target the repository-bound deploy UAMI. | `current change`; failed plan run `33978555300`; focused workflow guard and Terraform checks. | Publish the change, require exact-revision CI, and retain a non-destructive protected plan before any apply. |
| 2026-09-05 | implemented | Restored the protected deployment and CI boundary after the OCR and operational-history additions. OCR desired-state resolution moved to a focused script, the workflow remains within its reviewed step and line budgets, legacy Terraform fixtures retain the local OCR default, and database integration tests now run against the service-migration head. | Failed CI run `33934394238`; `current change`; focused workflow, Terraform, service-image, migration, distribution, Operator, Ruff, and strict mypy checks. | Publish the fix and require the exact new SHA to pass before dispatching the operational-history plan or apply. |
| 2026-09-05 | implemented | Added separate Core service intervals for scheduled stewardship identity health and handover knowledge lifecycle production. | `current change`; Core service Terraform validation; focused runtime and Operator checks. | Retain protected plan, apply, restart, and provider-outage evidence. |
| 2026-09-05 | implemented | Carried the platform-owned stewardship GitOps binding through the protected service materializer into an independent Core Key Vault secret reference. | `current change`; focused service workflow and Terraform checks. | Retain a protected plan and draft-PR delivery receipt. |
| 2026-09-04 | implemented | Pinned the standalone Document Intelligence module's AzureRM compatibility and committed the provider lock used by its focused module tests. | `current change`; `versions.tf`, `.terraform.lock.hcl`, and 2 passing module tests. | Keep provider upgrades explicit and rerun the module tests before changing the lock. |
| 2026-09-04 | implemented | Added policy-bound protected planning for private Document Intelligence, retained-resource local switching, explicit deprovisioning, and deployment-confirmed Settings projection. | `current change`; deployment request, outbox validator, Terraform integration, and projection tests; 11 focused Terraform tests. | Run protected plan/apply/readback drills and retain their receipts. |
| 2026-09-03 | implemented | Applied the catalog batch's bounded five-minute statement and 60-second connection deadlines to exact PostgreSQL projection readback as well as writes. | Failed run `33738376250` reaching the first large `state_kv` read; `current change`; focused verifier checks | Build and execute the final image, then rerun exact readback. |
| 2026-09-03 | implemented | Ran the PostgreSQL readback verifier in the Core workspace package environment so its persistence adapter and materializer dependencies are installed. | Failed run `33736789541` after migrations and exact execution verification; local Core package import probe; `current change` | Rerun the final exact projection readback. |
| 2026-09-03 | implemented | Added a validated repository binding fallback for the catalog Job name when the existing remote Terraform state predates the new output. | Failed runs `33731367877` and `33735259164` resolving `Microsoft.App/jobs/executions`; `current change`; focused deployment workflow checks | Publish the existing Job name binding and complete PostgreSQL readback. |
| 2026-09-03 | implemented | Raised only the catalog batch writer's connection handshake deadline from 10 to 60 seconds so Azure PostgreSQL can accept the next write after a large JSONB projection saturates the development tier. | Failed executions `caj-fdai-dev-krc-catalog-fu2yh02` and `caj-fdai-dev-krc-catalog-3l97xrr`; `current change`; focused materializer checks | Build and execute the final image, then complete PostgreSQL readback. |
| 2026-09-03 | implemented | Kept execution status and image-digest verification but routed the read through the runner-supported read-only Container Apps execution listing after the deploy identity received a 404 from the raw child-resource endpoint. | Failed run `33731367877`; successful migration stage in that run; `current change`; focused deployment workflow checks | Run final PostgreSQL projection readback. |
| 2026-09-03 | implemented | Removed redundant current-template comparison from the prestarted path and retained the stronger evidence: the successful execution template itself must contain the exact verified OCI digest. | Failed run `33730188615`; local ARM execution readback for `caj-fdai-dev-krc-catalog-yyyalar`; `current change` | Run final VNet-local PostgreSQL projection readback. |
| 2026-09-03 | implemented | Bound prestarted success to the execution template's OCI digest so a successful older image cannot satisfy the final PostgreSQL verification handoff. | ARM execution readback for `caj-fdai-dev-krc-catalog-tkbhzbn`; `current change`; focused deployment workflow checks | Build, run, and read back the final exact image. |
| 2026-09-03 | implemented | Normalized ARM Job image readback and compared the immutable OCI digest rather than requiring an identical registry path representation. | Failed run `33728633253`; `current change`; focused deployment workflow checks | Rerun VNet-local PostgreSQL projection readback. |
| 2026-09-03 | implemented | Removed the remaining `az resource show` dependency from catalog handoff and constructed direct ARM REST URIs from the verified active subscription, Terraform resource group, and Job name. | Failed run `33726464171`; `current change`; focused deployment workflow checks | Rerun VNet-local PostgreSQL projection readback. |
| 2026-09-03 | implemented | Replaced preview Container Apps CLI readback for prebound/prestarted handoff with stable ARM resource and execution APIs. | Failed run `33725040105`; local ARM readback of digest `sha256:18bef10b7171266673e11af58fe97ce46e65d88844ed1774d318eb81d928f8ef` and execution `caj-fdai-dev-krc-catalog-q3v4h6x`; `current change` | Rerun VNet-local PostgreSQL projection readback. |
| 2026-09-03 | implemented | Made the prebound/prestarted catalog handoff verify the exact Core digest and a successful latest execution without asking the VNet runner to repeat the unsupported Container Apps Job management call; also persisted the five-minute PostgreSQL session default in Terraform. | Catalog execution `caj-fdai-dev-krc-catalog-zo4anu4`; `current change`; focused deployment workflow checks | Run the VNet-local exact PostgreSQL projection readback. |
| 2026-09-03 | implemented | Raised only the catalog batch writer's explicit StateStore statement timeout to five minutes so large immutable projections can replace the 15-second interactive default. | Failed Jobs `caj-fdai-dev-krc-catalog-rcy7l2m` and `caj-fdai-dev-krc-catalog-hpv793h`; `current change`; focused materializer checks | Rebuild the exact Core image, rerun the catalog Job, and retain PostgreSQL readback. |
| 2026-09-03 | implemented | Added a fail-closed prestarted-Job mode for tenants whose runner policy blocks the management-plane Job start action after migrations. PostgreSQL readback remains mandatory. | Failed run `33719045341`; `current change`; focused helper and workflow checks | Start the exact-image Job through the verified management plane, rerun the refresh, and retain the PostgreSQL receipt. |
| 2026-09-03 | implemented | Added the repository-pinned uv runtime before integrated service migrations in the catalog refresh workflow. | Failed run `33718185397`; `current change`; focused workflow checks | Re-run the catalog refresh and retain a successful PostgreSQL receipt. |
| 2026-09-03 | implemented | Added a fail-closed prebound-image mode for tenants whose runner policy projection breaks Container Apps update readback. The runner still executes migrations, the Job, and exact PostgreSQL verification. | Failed run `33717083287`; `current change`; focused helper and workflow checks | Bind the exact image through the verified management plane, run the catalog refresh, and retain the PostgreSQL receipt. |
| 2026-09-03 | implemented | Pinned the working Container Apps extension in the runner-temporary Azure CLI environment before updating the catalog Job. | Failed run `33716201387`; local management-plane update with extension `1.3.0b4`; `current change`; focused workflow checks | Re-run the catalog refresh and retain a successful PostgreSQL receipt. |
| 2026-09-03 | implemented | Replaced unsupported Job PATCH with a full version-pinned ARM PUT derived from the current mutable resource after removing readonly fields. | Failed run `33715409524`; `current change`; focused helper and workflow checks | Re-run the catalog refresh and retain a successful PostgreSQL receipt. |
| 2026-09-03 | implemented | Replaced the broken Python 3.14 Azure CLI Container Apps update path with a version-pinned ARM PATCH that preserves the complete catalog container template for update and rollback. | Failed run `33714596790`; `current change`; focused helper and workflow checks | Re-run the catalog refresh and retain a successful PostgreSQL receipt. |
| 2026-09-03 | implemented | Bound rollback to the environment-owned digest-pinned catalog image because the runner's policy-projected ARM response omits container fields. Successful Job execution plus exact PostgreSQL projection readback verifies the new image effect. | Failed run `33713895690`; `current change`; focused helper and workflow checks | Re-run the catalog refresh and retain a successful PostgreSQL receipt. |
| 2026-09-03 | implemented | Made catalog Job image readback wrapper-independent while requiring exactly one named container image. | Failed run `33712083687`; `current change`; focused helper and workflow checks | Re-run the catalog refresh and retain a successful PostgreSQL receipt. |
| 2026-09-03 | implemented | Installed the pinned GitHub CLI before the catalog refresh workflow's first GitHub API check and bound Console static publication to the exact apply-synchronized repository Variables with a Terraform-state fallback. | `current change`; focused protected workflow and static publisher checks. | Retain successful catalog refresh and Console publication receipts for the pushed revision. |
| 2026-09-03 | implemented | Added pinned Node 24 setup to the self-hosted static publisher and read the ARM Job image with an exact JMESPath projection. | Failed runs `33711285426` and `33711285243`; `current change`; focused workflow checks | Re-run both releases and retain successful receipts. |
| 2026-09-03 | implemented | Made legacy empty Terraform outputs fall back to the validated Static Web App binding and accepted flattened ARM REST Job payloads. | Failed runs `33710219597` and `33710214340`; `current change`; focused helper and workflow checks | Re-run both releases and retain successful receipts. |
| 2026-09-03 | implemented | Installed the pinned GitHub CLI before catalog CI verification and allowed the static publisher to use a validated environment-owned Static Web App binding when legacy Terraform state lacks outputs. | Failed runs `33708982779` and `33708752280`; `current change`; focused workflow checks | Re-run both releases and retain successful receipts. |
| 2026-09-03 | implemented | Added a dedicated bot-owned PostgreSQL catalog refresh, independent from static Console publication. | `current change`; exact-source request and refresh workflows plus focused workflow contracts | Retain one successful catalog refresh and Console publication receipt. |
| 2026-09-03 | implemented | Restored the protected Console release to a static-only operation so Manual Studio publication no longer changes the Core catalog Job or PostgreSQL projections. | `current change`; focused deployment workflow checks and repository pre-commit gates. | Publish the exact CI-verified revision and retain matching `/manuals/catalog.json` and `/manuals/library.html` readback evidence. |
| 2026-08-31 | implemented | Added request-bound plan, exact apply, verification-only resume, and sanitized status transport for the installable CLI, with workflow-side target/context recomputation and approval-policy verification. | `current change`; deployment CLI and workflow tests, Ruff, strict mypy, Terraform validation, and reviewability budget | Retain one governed dev or staging plan/apply receipt, then implement workflow-owned N-of-M and production input binding before widening support. |
| 2026-08-29 | implemented | Extended the single checksum-pinned GitHub CLI installation step to isolated Executor image binding after protected plan `33235557463` reached that step without `gh` on the runner. | Failed protected plan `33235557463`; `current change`; focused workflow diet test. | Rerun the exact protected target plan, then apply only its sealed non-destructive artifact. |
| 2026-08-26 | implemented | Decoupled protected deploy authority from runner VM lifecycle. Bootstrap now owns a stable UAMI and five-role manifest, the current VM attaches it alongside the system identity, and all seven `fdai-deploy` workflows require explicit client ID login plus tenant, subscription, and token `oid` verification. | `current change`; bootstrap Terraform validation and plan tests; focused identity, workflow, context, and scenario checks. | Push an exact revision with green required CI, then retain zero-unrelated-destroy bootstrap, platform, and scenario plans plus effective-role readback before candidate creation. |
| 2026-08-21 | implemented | Removed the protected runner's implicit system-pip dependency. The workflow installs the repository-pinned uv release and runs model resolution and production readiness from the frozen Core package environment. | `current change`; failed protected plan run `32434472993`; focused deployment workflow contract, YAML parsing, and dependency command checks. | Rerun the exact Event Bus migration plan and retain its protected plan/apply evidence. |
| 2026-08-24 | implemented | Added a runner-temporary, checksum-pinned `kubelogin` installation to the protected scenario workflow so Azure RBAC kubeconfigs don't depend on mutable VM image state. | Failed scenario run `32780056314`; `current change`; focused workflow, script, checksum, and scenario contract checks. | Complete private AKS substrate preparation and retain the protected apply receipt. |
| 2026-08-21 | implemented | Kept the destructive-plan gate closed while approving one reviewed embedding migration. The gate accepts the `t1.embedding` replacement only when the Terraform plan matches the exact address, account binding, model family, `GlobalStandard` capacity 1 source, and `Standard` capacity 200 target. Delete-only and any drifted replacement remain blocked. | `current change`; `.github/workflows/deploy-dev.yml`; focused destructive-plan checks passed 2 cases. | Apply the exact protected plan, verify the new deployment and runtime binding, retain the receipt, and remove the one-transition approval. |
| 2026-08-24 | implemented | Restored the model-binding-only plan scope guard as a real workflow step and hardened replacement review for migration away from a deprecated source family. The saved plan must retain one valid OpenAI source deployment and account binding, while the target must match the sealed GA family, version, SKU, and capacity exactly. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); `deploy-dev.yml`; executable model-plan and destructive replacement checks. | Retain the exact PTU plan/apply/readback and reverse-plan rollback receipts before raising the path to `validated`. |
| 2026-08-24 | implemented | Parameterized the live resolver's Azure CLI timeout after protected plan `32749593774` stopped in the read-only Model Capacities query at 30 seconds. The protected workflow uses a bounded 90-second deadline and keeps all provider reads fail-closed. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); resolver CLI and protected workflow tests passed 62 cases; strict mypy passed. | Rerun one exact plan and retain the result before apply. |
| 2026-08-25 | implemented | Added a Core-only protected service transition that injects the image-attested model digest and exact Azure/path bindings without permitting unrelated environment drift. Model planning and apply use the active healthy Core revision plus verified image attestation as CAS authority; Terraform output remains diagnostic only. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); service guard, plan bundle, workflow, Terraform validation, and active-revision verifier checks. | Apply the Core transition before rerunning the PTU plan. |
| 2026-08-25 | implemented | Replaced mutable repository policy input with a protected-runner read-only join of one exact Operator model plan proposal and the current revisioned policy. The request id binds the proposal token and policy digest; exact apply still restores only sealed artifacts. | `current change`; `model_binding_proposal.py`; `deploy-dev.yml`; focused proposal, request, lifecycle, and workflow checks. | Retain one governed proposal-to-plan receipt, then complete exact model apply, readback, and rollback evidence. |
| 2026-08-25 | implemented | Extracted model proposal retrieval and state initialization from the protected workflow into one bounded helper after the workflow exceeded its reviewability budget. The workflow stays within both line and step ceilings while helper syntax and proposal guards remain executable contracts. | `current change`; `materialize-model-binding-proposal.sh`; `test_deploy_workflow_diet.py`; focused lifecycle and workflow checks. | Retain the governed proposal-to-plan and exact apply evidence already listed. |
| 2026-08-25 | implemented | Added an exact-revision protected workflow that restores only the missing deployed model Settings projection after repository artifact, active Core runtime, and image-attestation digests agree. A readback verifies the environment and digest before completion. | `current change`; `model-settings-projection.yml`; `materialize-authoritative-settings.py`; focused projection and workflow checks. | Run and retain one projection receipt before submitting the governed model plan proposal. |
| 2026-08-25 | implemented | Registered the model Settings producer in the explicit privileged-workflow inventory and aligned its ancestor check with the protected-source audit contract after required CI rejected the initial workflow. | Failed required CI run `32829880817`; `current change`; CI contract and projection workflow checks passed 45 cases. | Rerun required CI for the exact repair revision before dispatching the projection. |
| 2026-08-13 | implemented | Adopted the implementation ledger without reconstructing earlier provenance and added protected provisioning plus a proposal-only Job for the bounded OHL evidence target. | current change; focused Terraform tests report 8 passed and publisher/workflow tests report 13 passed. | Apply the exact plans, deploy attested runtime images, and complete the live evidence campaign. |
| 2026-08-13 | implemented | Isolated local destructive migration validation from the active local runtime PostgreSQL cluster. | Current change; Compose configuration passed, focused queue and local-environment tests passed (68 tests), and isolated migration upgrade/downgrade checks passed (2 tests). | No remaining implementation work for local validation database isolation. |
| 2026-08-13 | implemented | Corrected the protected platform plan and exact-apply state from `validated` to `implemented`; workflow source proves the mechanism, but the repository does not retain a governed platform apply receipt. | current change; `.github/workflows/deploy-dev.yml`; roadmap, translation, and documentation checks. | Retain a repository-safe governed platform apply receipt before restoring `validated`. |
| 2026-08-16 | implemented | Bounded the deployment waits that reported nothing: the Container App health poll emits one line per iteration and fails explicitly on its 900-second deadline instead of falling through as converged, and every retrying workflow download declares a cumulative `--retry-max-time` window so a retry count times a per-request cap is no longer the only bound. | Current change; focused deployment workflow and gate-parity tests report 71 passed, and `bash -n` on the health script. | No remaining work for these bounds; the platform apply receipt items below are unchanged. |
| 2026-08-16 | implemented | Declared a budget for both protected deploy jobs, which had only the six-hour runner default and held the single self-hosted deploy runner meanwhile: the platform Terraform job is bounded to 180 minutes and the service deploy job to 120 minutes, and the health verifier bounds the recovery verification and readiness-path steps it ran after its own deadline. | Current change; 24 focused deploy-workflow tests passed, `bash -n` on the health script, and both workflow documents parse as YAML. | No remaining work for these budgets; the platform apply receipt items below are unchanged. |
| 2026-08-16 | implemented | Bound the analyzer Job to the durable inventory projection and a reviewed five-type analyzer map. The tick deterministically merges explicit and discovered targets, enforces a configured discovery ceiling, omits unsupported types, and retries on a projection read failure instead of silently reducing coverage. | `current change`; `analyzer_tick_cli.py`, `analyzer_targets.py`, `analyzer_tick_job.tf`, focused analyzer tests, and `test_detection_readiness.py`. | Retain one protected apply and scheduled-run receipt before raising this scope to `validated`. |
| 2026-08-17 | implemented | Added optional distributed-trace topology evaluation to the existing analyzer Job without adding a service, identity, workspace, or scheduler resource. | `current change`; focused behavior and HIL checks passed 55 cases; Terraform formatting, validation, and infrastructure contract checks passed. | Retain a protected exact-revision apply and scheduled `preserve`, `regenerate`, and `drop` receipts before raising this scope to `validated`. |
| 2026-08-17 | implemented | Corrected protected runtime image promotion to read the service-specific Core repository published by the container supply chain instead of the retired monolithic GHCR path. | `current change`; `.github/workflows/deploy-dev.yml`; `test_legacy_platform_imports_the_service_specific_core_image` passed. | Complete a protected plan and exact apply that verify and bind the service-specific Core digest. |
| 2026-08-17 | implemented | Included the analyzer Job in development-gateway targeted plans so trace-topology configuration and the scheduled detector converge with the existing gateway deployment surface. | `current change`; `.github/workflows/deploy-dev.yml`; `test_detection_readiness.py` passed 4 cases and `test_service_deploy_workflow.py` passed 25 cases. | Retain the protected apply and scheduled `preserve`, `regenerate`, and `drop` receipts before raising this scope to `validated`. |
| 2026-08-20 | implemented | Added an explicit protected-plan input for the standalone Operator channel-edge identity and its versionless Key Vault secret scopes. The platform owner grants only ACR pull, semantic Event Hubs transport, and listed secret reads; the Operator service root separately owns the public edge Container App lifecycle. | `current change`; protected deployment suites passed 154 cases; workflow YAML, shell syntax, design-route, and five-distribution checks passed. | Configure a real provider profile in approved credential stores, then retain exact platform and Operator service plan/apply/rollback receipts. |
| 2026-08-20 | implemented | Added per-stage service migration deadlines after a protected Core apply spent its full job budget waiting inside migration. All service and legacy paths now use a 10-second connection deadline and 5-minute lock deadline, while the workflow closes the complete migration stage after 20 minutes. | `current change`; focused service migration and protected workflow checks passed 204 cases. | Rerun the exact protected Core apply and retain the successful migration and post-apply health receipt. |
| 2026-08-20 | implemented | Added a 15-minute PostgreSQL statement deadline to every legacy and service migration connection. It expires before the 20-minute workflow deadline so the database cancels long-running DDL and releases its transaction and advisory locks even when the runner process disappears. | `current change`; focused migration deadline checks; disposable PostgreSQL canceled an over-budget statement and reported zero advisory locks after disconnect; protected runs `32357855293` and `32361126642` exposed an abandoned backend that survived the shell deadline. | Rerun the exact protected Core apply and retain the successful migration and post-apply health receipt. |
| 2026-08-20 | implemented | Wired the continuous inventory contract into the Container Apps Job. The cron runs every minute, while durable scheduling preserves the six-hour routine scan and coalesces observed changes above a 120-second floor. Progress, absolute attempt, and ARG rate budgets match local configuration. | [Issue #139](https://github.com/dotnetpower/fdai/issues/139); current Terraform and focused infrastructure contract checks. | Apply the exact revision through the protected runner, then measure cadence, cost, and one real change-to-reconciliation interval. |
| 2026-08-24 | implemented | Added the global provider-schema Container Apps Job under the read-only inventory identity. The Job retrieves its PostgreSQL DSN through the existing Key Vault reference, hydrates and persists immutable ledger blobs, and uses the authenticated production Pantheon bridge for Heimdall publication. | `current change`; provider-schema checks passed 78 cases; Terraform validation and focused Job checks passed. | Apply the exact revision through the protected runner and retain one scheduled-run plus Saga audit receipt. |
| 2026-08-29 | implemented | Added opt-in durable storage and draft-only GitOps delivery for the existing rule-watcher Job. The Job uses managed-identity Blob access, a Key Vault-backed GitHub credential reference, content-addressed snapshot paths, and a retention policy whose prefix follows the configured container. | `current change`; focused collection tests passed 64 cases; Terraform format and validation passed. | Enable the feature through a protected plan and retain one scheduled delivery receipt before claiming runtime validation. |
| 2026-08-24 | implemented | Added bounded `plan-model-*` and `apply-model-*` request ids for model-binding-only protected deployment. The mode requires a stored environment policy and protected request, permits an intentionally held model quorum during assessment, targets only the Azure OpenAI module, and rejects any plan change outside sealed cognitive deployments. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); `.github/workflows/deploy-dev.yml`; focused protected workflow checks passed 60 cases and YAML parsing passed. | Retain the exact plan, apply, provider readback, runtime digest, and reverse-plan rollback receipts. |
| 2026-08-24 | implemented | Reused the existing central OpenAI Private DNS zone for the disposable scenario endpoint. The scenario state owns only its lab-VNet link, private endpoint, and zone group, while the existing runner and P2S links remain centrally owned. | Failed protected apply `32752288798`; `current change` in `infra/scenario-lab/` and `.github/workflows/sre-demo-lab.yml`; focused Terraform and workflow checks. | Complete the protected scenario apply, approved sweep, and final destroy receipts. |
| 2026-08-24 | implemented | Sealed request kind and model policy provenance through exact apply, rejected duplicate capability bindings, and added independent Azure deployment readback with a tenant-safe digest-bound apply receipt. | [Issue #270](https://github.com/dotnetpower/fdai/issues/270); protected plan verifier, model deployment verifier, workflow, and focused checks passed 107 cases; ten critique rounds left no verified unresolved finding above Low. | Retain the live forward and reverse protected receipts and verify the independently deployed runtimes load the sealed digest. |
| 2026-08-31 | implemented | Replaced privileged executor reuse in the scheduler and DB-DR jobs with distinct job identities, and bound the DB-DR job to a delivery-owned Azure restore plus PostgreSQL integrity, smoke, teardown, and audit path. | `current change`; delivery modules, root and compute Terraform, focused CLI, adapter, verifier, and infrastructure checks. | Retain protected apply and scheduled-run receipts separately; this local change claims none. |
| 2026-08-31 | implemented | Made the three Phase 4 measurement jobs explicit opt-ins and assigned a dedicated identity with only image-pull, state-secret, and optional model-inference access. Plan tests pin the complete enabled shape and disabled outputs. | `current change`; measurement-runner module, root composition, Terraform plan tests, and focused infrastructure contract tests. | Retain protected apply and scheduled-run receipts separately; this local change claims none. |
| 2026-08-30 | implemented | Added the deployment-owned OHL Ed25519 signing seed, exposed only its Key Vault reference and credential lineages to protected service input hydration, and injected the seed into Core through its Managed Identity. | `current change`; platform Terraform validation, 5 service-root plans, and focused hydration and infrastructure contract tests. | Apply the exact platform and Core revisions, then retain a verified mailbox observation before the live drill. |
| 2026-09-06 | implemented | Added disconnected runtime preparation, release and wheelhouse staging, bounded support installation, and pinned-image bootstrap without introducing an application-side resource deployment path. | `current change`; 225 focused Python tests and 13 provider-free Terraform tests passed; Ruff, Console typecheck, build, and documentation gates passed. | Retain one controlled-network installation receipt and one offline runner provisioning receipt before advancing the area to `validated`. |
| 2026-09-06 | implemented | Added explicit initial PostgreSQL credential generation for fresh IaC deployments while preserving protected supplied-password input as the default. The generated value remains only in sensitive Terraform state and the existing sensitive DSN path. | `current change`; 9 provider-free Terraform plans, one root-wiring regression, root formatting, and validation passed. | Retain a protected fresh-environment plan/apply receipt and verify no secret appears in workflow, CLI, or Console output. |

### Remaining work

- [ ] Retain one exact-green bot-owned Core apply after environment-wide writer serialization, with successful health, peer-isolation, and live-observation artifacts.
- [ ] Retain a repository-safe governed platform apply receipt that binds the exact protected plan, source revision, target identity, and post-apply verification, then advance the platform exact-apply scope to `validated`.
- [ ] Record protected apply receipts for the OHL target and exact-revision Core and Executor images, then verify the deployed revisions resolve to the same source commit.
- [ ] Complete the governed `ops.scale-out` drill and retain independent rollback, cleanup, graph-outcome, 100-sample, and 14-day recurrence evidence.
- [ ] Retain a repository-safe protected apply and scheduled-run receipt for the analyzer Job's trace-topology binding, including exact revision, source identity, bounded Log Analytics read, finding publication, and no-resource-addition plan evidence.
- [ ] Retain a protected apply and scheduled provider-schema receipt that binds the source revision, durable generation digest, Heimdall publication, and Saga audit record.
