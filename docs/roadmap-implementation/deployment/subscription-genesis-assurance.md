# Subscription Genesis Assurance implementation ledger

This delivery ledger tracks the controls that make a zero-to-ready run safe, complete, observable,
and recoverable.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Runner-image SKU eligibility preflight | implemented | [Plan selection validator](../../../scripts/deployment/azure/genesis_runner_image_skus.py); [bounded ARM probe](../../../scripts/deployment/azure/genesis_runner_image_sku_probe.py); runner-image planning and apply hooks; 451 focused passing regressions | Rejects restricted or incomplete metadata before review and a new claim. Earlier coverage measurements remain historical; this change makes no new coverage claim. Eligibility alone is not capacity, partial-apply recovery, or release evidence. |
| Legacy bounded image VM selection | implemented | [Original small-VM policy](../../../infra/genesis-runner-image/sku-policy.json); [pair selector](../../../scripts/deployment/azure/genesis_runner_image_sku_choice.py); [versioned input sealing](../../../scripts/deployment/azure/genesis_runner_image_sku_selection.py); existing v1 compatibility tests | The original signed five-SKU contract remains supported, including combined image quota and no-reselection apply. New v2 policy inputs use the full-catalog three-role path below instead of treating preferences as an allowlist. |
| Full-catalog host and optional image VM selection | implemented | [Hardware policy](../../../infra/genesis-runner-image/vm-sku-policy.json); [catalog reader](../../../scripts/deployment/azure/genesis_vm_sku_catalog.py); [role selector](../../../scripts/deployment/azure/genesis_vm_sku_choice.py); [preparation and host preflight](../../../scripts/deployment/azure/genesis_vm_sku_preflight.py); [image requirements](../../../scripts/deployment/azure/genesis_vm_image_requirements.py); focused tests | Connected preparation selects and reserves quota for only the Foundation host, then seals an exact Marketplace version. Artifact-offline image creation separately selects builder and verifier capacity and retains managed-image checks. Successful connected Azure allocation remains unvalidated. |
| Default-route-aware network selection | implemented | [Selector](../../../scripts/deployment/azure/genesis_prepare_inputs.py); [network regressions](../../../tests/integration/scripts/test_genesis_network_layout.py); existing Genesis preparation tests | Only default forwarding routes are excluded from overlap checks. Declared address spaces, non-default routes, complete-evidence validation, and the requirement for two disjoint networks remain enforced. A replacement signed release remains separate. |
| Released default-route correction | in-progress | [Release builder](../../../scripts/deployment/release/build-standalone-deployment-kit.sh); source and artifact comparison in the latest history row | The operator authorized focused commit, push, eligible signed-kit publication, and local transition. This candidate does not certify CI, publication, or Azure readiness. |
| Full-catalog signed-kit preparation | in-progress | Source commit `a0997acf1`; [release builder](../../../scripts/deployment/release/build-standalone-deployment-kit.sh); 112 VM-policy regressions after EOF normalization | Exact-source CI, signed complete assembly, and artifact re-verification remain prerequisites. No kit or deployment receipt is claimed. |
| Foundation state and external control planes | implemented | Genesis image, Foundation apply, Bastion, enrollment, state-handoff modules; `fdai_deployment_cli.bootstrap_reconcile`; focused lifecycle tests | The local path pins source, target, image, toolchain, plans, claims, receipts, fresh runner attestation, target-bound human state actor, and state comparison. It has no governed Azure receipt, so operational validation remains open. |
| Default-route-aware network selection | implemented | [Selector](../../../scripts/deployment/azure/genesis_prepare_inputs.py); [network regressions](../../../tests/integration/scripts/test_genesis_network_layout.py); existing Genesis preparation tests | Only default forwarding routes are excluded from overlap checks. Declared address spaces, non-default routes, complete-evidence validation, and the requirement for two disjoint networks remain enforced. The currently published kit has not been replaced. |
| Released default-route correction | not-started | [Release builder](../../../scripts/deployment/release/build-standalone-deployment-kit.sh); [signed-kit acquisition](../../../packages/deployment-cli/src/fdai_deployment_cli/deployment_kit.py); source and artifact comparison in the latest history row | The local selector fix has not reached an approved signed release. CLI reinstallation and cache deletion cannot supply the missing bundle correction. The release requires a clean exact revision, applicable CI, signing, and explicit publication authorization. |
| Authority, concurrency, cancellation, and recovery | in-progress | Bounded local approval record; immutable image, Foundation, enrollment, and state claims; protected application claims; focused resume tests | Exact approval and verification-only resume are implemented per checkpoint. Local approval is dev-only and rejects a mismatched profile or quorum. Subscription-wide leases, cancellation, authenticated external approval retention, protected quorum execution, and live rollback rehearsal remain open. |
| Database and semantic readiness | implemented | Service migrations; catalog materializer; `DatabaseSemanticReadback`; focused readiness tests | The aggregate contract rejects missing service heads, extensions, role checks, semantic digests, shadow mode, or independent observation. Azure runtime-principal evidence is still required before this area can be validated. |
| Model assurance | in-progress | Resolver and Terraform modules; deployment CLI capacity planner | Workload headroom, shared deployment aggregation, reserve, and existing allocation are local contracts. Terms, race-safe Azure reservation, quantitative probes, and live-call approval remain open. |
| Inventory completeness and progress | implemented | Complete-generation coordinator; shared progress and closure contracts; PostgreSQL and Blob persistence; Operator and Console projection; managed-host initial scan | Durable count-only events, child-source terminal evidence, full-scope independent closure, and fail-closed readiness are implemented and focused-tested. Large-scope exact-revision live Azure evidence remains open. |
| End-to-end operational closure | in-progress | Deterministic guided rehearsal, receipt-bound journal schema v3, aggregate readiness contract, interruption tests, and successful network-isolated air-gap drill | Local closure now requires 13 evidence families, complete child-aware inventory closure, and a zero-change second run. No governed zero-to-ready Azure receipt exists. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-20 | in-progress | Bound inventory recovery to installed producer source, exact sealed completed evidence, original clocks, and active-base validation. Resource-only chunks still cannot certify completeness. | `current change`; shared coordinator, source builder and candidate store; 237 focused coordinator/CLI/disposable PostgreSQL tests passed with stable input digests, including 12 separate-process recovery conditions; Ruff and mypy passed. | Unfinished provider continuation, partitioned ontology publication, and independent initial subscription readiness remain open; no live provider or deployment receipt. |
| 2026-09-20 | in-progress | Pinned the shared ARG source contract to generated queries, reviewed relationships, and request configuration without persisting private values. | `current change`; query/source context tests: 21 passed; focused Ruff and mypy passed. | Provider restart, ARM-overlay revision binding, and initial subscription readiness remain separate evidence requirements. |
| 2026-09-20 | in-progress | Bound inventory source collection context to effective policy, management target/audience, request rate, vocabulary mappings, and signed fallback content without retaining raw target settings in metadata. | `current change`; `inventory_sync_cli_support.py`; 11 new drift and stability regressions; CLI cohort: 94 passed; focused Ruff and mypy passed. | Full source restart still requires query and relationship-catalog revision binding plus complete coverage replay; no deployment or live provider evidence is claimed. |
| 2026-09-20 | in-progress | Documented and connected bounded Resource chunk/checkpoint candidate staging without treating replay as complete subscription coverage. | `current change`; shared inventory coordinator and snapshot store; 25 disposable PostgreSQL chunk checks and 186 owning regressions passed. | Provider-level continuation and independent full-scope closure remain required; no deployment or live subscription receipt is claimed. |
| 2026-09-20 | implemented | Aligned initial inventory assurance with verified-link staging, truncated-property rejection, separate graph/delivery completion, and the current bounded in-memory generation rather than an unimplemented disk-backed stream. | `current change`; shared inventory hardening register H01-H36; current owning cohort: 628 passed and disposable PostgreSQL cohort: 9 passed. | No Genesis deployment or initial-readiness receipt is claimed. Larger subscription ownership still requires a reviewed partition strategy and separately authorized live validation. |
| 2026-09-16 | implemented | Completed the P6 assurance boundary with hash-chained count-only records, serialized concurrent callbacks, immutable Blob duplicate checks, append-only SQL sequencing, least-privilege Operator replay, strict Console decoding, explicit initial scan, and distinct active-generation closure before inventory readiness. Overall subscription readiness remains false. | `current change`; focused contract, persistence, ARG, inventory, migration, Operator, Console, deployment CLI, and Terraform checks. | Retain exact-revision live Azure evidence for managed identity, private Blob, large-scope continuation, browser replay, and final readiness. |
| 2026-09-16 | implemented | Split connected host selection from optional artifact-offline image construction. Connected preparation now evaluates one Foundation host, exact Marketplace metadata, and its checksum-pinned bootstrap without reserving builder or verifier capacity. | `current change`; VM role selection/preflight, Foundation image validation, Terraform roots, and focused tests. | Capture connected Azure evidence for exact Marketplace boot, pinned tool installation, attestation, and zero-change Foundation readback. |
| 2026-09-13 | implemented | Completed 26 bounded VM/signed-kit hardening rounds: 20 production corrections and six rejected hypotheses with regression strengthening. Final independent source review found no confirmed finding above Low in this scope. | `7b4ab4501`; final owning batch: 711 passed; prior unchanged Console configuration/Vite evidence: 205 passed; changed Python Ruff/format and focused strict source typing passed. Integrated current main with no changes to tested deployment source. | Exact pushed-source CI, complete signed build, independent artifact/installed acceptance, and any separately approved Azure execution remain pending. Low cleanup-diagnostic masking and authoritative cancellation reconciliation remain explicit. |
| 2026-09-13 | implemented | Removed raw subprocess/filesystem values from direct Foundation command errors while preserving fixed operation reasons and no-retry behavior. | `current change`; focused redaction and Foundation apply regressions. | Complete release hardening and exact-source artifact verification. |
| 2026-09-13 | implemented | Made complete standalone release output write-once instead of deleting the previous signed archive before a rebuild. | `current change`; executable release-output regressions: 2 passed with the real directory ownership guard. | Complete remaining hardening and exact-source kit validation. |
| 2026-09-13 | implemented | Preserved completed VM discovery snapshots and explicit unknown counters/digests when a later provider read fails. | `current change`; failure and VM integration suites: 23 passed; Ruff, format, and strict helper typing passed. | Complete remaining hardening and exact-source kit validation. |
| 2026-09-13 | implemented | Converted unavailable Azure CLI context paths into value-safe pre-provider failures. | `current change`; `test_genesis_vm_environment.py`: 3 passed; Ruff and format passed. | Complete the remaining hardening and signed-kit validation. |
| 2026-09-13 | implemented | Hardened VM policy file reads against linked, writable, special, oversized, and changing inputs using a held bounded descriptor. | `current change`; policy-reader and VM integration suites: 27 passed; Ruff and format passed. | Complete the remaining explicitly requested signed-kit hardening rounds and exact-source release validation. |
| 2026-09-13 | in-progress | Prepared the full-catalog release candidate and shortened duplicate assurance prose to satisfy the unchanged design-context budget. The policy's final newline was normalized before committing its final digest. | Source commit `a0997acf1`; 112 policy/catalog/image/integration/contract tests passed after normalization; normal commit hooks and design-route checks passed. | Publish the exact candidate, require green CI and matching framework integrity, then build and independently verify a new complete signed kit. Existing deployment state remains untouched. |
| 2026-09-13 | implemented | Replaced preference-only discovery for new v2 inputs with a bounded complete regional catalog and role-based builder, verifier, and Foundation selection. Added combined quota, exact host and actual image disk checks, private replay with explicit unknown counters, and a Foundation expiry fence after host and human-identity reads. Existing claims and v1 policy behavior remain unchanged. | `current change`; new VM catalog, choice, policy, image, preflight, and image-requirements modules; VM choice/catalog/integration/contracts/image-requirements plus existing preparation/Foundation/image/SKU/approval/private-execution tests: 451 passed; Ruff and format: 22 changed Python files; strict source-only mypy: 14 sources. Synthetic compatibility proves selection, not live capacity. | Publish and verify the exact signed kit, obtain current eligible target metadata and new exact approvals, and retain independent image/Foundation effects. Claimed partial-state recovery and complete deployment readiness remain separate. |
| 2026-09-12 | implemented | Added deterministic pre-plan builder/verifier selection from the signed five-SKU small-VM policy. Restricted B2s or a restricted preferred builder can select a compatible D2-series candidate only with complete same-region capability and aggregate quota evidence. Private snapshots allow replay; chosen sizes and evidence digests bind to the exact review and appear before terminal approval. Apply rechecks the sealed pair and never selects another SKU or repeats a retained claim. | `current change`; image/SKU/approval/private-execution tests: 307 passed; five SKU modules: 99.68% combined statement/branch coverage; Ruff and format: 14 changed Python files; strict mypy: eight changed sources, including the image contract, with imported-module diagnostics separate. | Publish and reverify the feature in an eligible signed kit before using it. Foundation VM selection, price quotation, allocation capacity, existing partial-state recovery, new exact approval, and deployment readiness remain separate. |
| 2026-09-12 | implemented | Added an exact-plan image SKU gate after a supervised signed-release apply reached a subscription-restricted builder only after creating networking. Planning and unclaimed apply now use bounded region-filtered ARM metadata, explicit regional/zone semantics, complete unique selections, and post-read expiry/approval checks. Existing claims still resume verification only. | `current change`; runner-image SKU modules; image, SKU, planning-hook, and private-execution tests: 192 passed; new-module statement/branch coverage: 100%. | Publish a separately verified signed kit; resolve target SKU eligibility independently; implement a lineage-preserving new-plan recovery and obtain its exact approval before changing partial effects. The live failure does not support a deployment-success or rollback claim. |
| 2026-09-12 | in-progress | Confirmed recurrence after kit acquisition was repaired: Foundation discovery still executes the older signed selector, not the corrected checkout. The locally retained runtime manifest identifies source `852cf4ea562123654dc7bc72cdb5431f1ad6570b`; all three retained bundle copies lack the corrected selection function. Identical synthetic default-route input reproduces the reported error in the bundled function while the local function selects two disjoint reviewed ranges. | `current change`; read-only function comparison; 26 focused network/preparation tests, Ruff/format, and editor checks passed for the local fix. The isolated source/test candidate is byte-identical. No Azure request, signed-file edit, signing, commit, push, or publication ran in this diagnosis. Remote release freshness was not queried. | Obtain explicit release authorization, commit the focused fix without unrelated changes, publish an eligible exact-source signed kit, verify the bundled regression, and perform an explicit source-bound local transition that preserves prior run evidence. Do not remove default routes, disable signature verification, or pass an unsigned checkout as an offline kit. |
| 2026-09-12 | implemented | Corrected a deployment-preparation failure where one default route exhausted every reviewed CIDR candidate. Local `default` and numeric `0.0.0.0/0` forwarding routes now have consistent semantics; declared ranges and specific route aggregates still block overlaps. Real exhaustion reports safe counts and a review action. | `current change`; `PYTHONPATH=packages/deployment-cli/src python -m pytest -q --no-cov tests/integration/scripts/test_genesis_network_layout.py tests/integration/scripts/test_genesis_prepare.py`: 26 passed after six failing regression cases reproduced the old behavior. | Publish a rebuilt signed kit containing the fix, verify selection through the released entry point, and retain separately approved deployment evidence. Prior local hardening results did not cover this default-route case and did not prove release deployment readiness. |
| 2026-09-12 | implemented | Corrected a deployment-preparation failure where one default route exhausted every reviewed CIDR candidate. Local `default` and numeric `0.0.0.0/0` forwarding routes now have consistent semantics; declared ranges and specific route aggregates still block overlaps. Real exhaustion reports safe counts and a review action. | `current change`; `PYTHONPATH=packages/deployment-cli/src uv run --frozen pytest -q --no-cov tests/integration/scripts/test_genesis_network_layout.py tests/integration/scripts/test_genesis_prepare.py`: 26 passed in the isolated candidate; Ruff and format over the two changed Python files plus strict mypy for the selector passed. | Publish a rebuilt signed kit containing the fix, verify selection through the released entry point, and retain separately approved deployment evidence. Source verification alone is not release readiness. |
| 2026-09-12 | in-progress | Confirmed recurrence in the retained signed kit from source `852cf4ea562123654dc7bc72cdb5431f1ad6570b`, whose three local execution copies lack the corrected selector. Identical synthetic default-route input reproduces the reported error in the bundled function and passes in the local function. The operator then authorized a focused release and local transition, excluding Azure deployment. | `current change`; network regression and read-only function comparison; a separate candidate contains only the selector, owning tests, and paired design/ledger changes. | Obtain exact pushed-source CI, build and verify the new signed kit, publish an immutable release, and perform an explicit source-bound local transition preserving prior state. Do not remove default routes, weaken signatures, or pass an unsigned checkout as a kit. |
| 2026-09-12 | implemented | The live direct-builder run exposed tenant Policy Append adding `FirstPartyUsage=/Unprivileged` to both Firewall public IPs, which AzureRM otherwise planned to replace after creation. Terraform now treats only that field as policy-owned, while independent ARM readback rejects unknown or asymmetric IP tags and a refreshed zero-change plan gates the image receipt. | `current change`; `infra/genesis-runner-image`; runner-image observation and exact-plan contracts; focused policy-tag and convergence regressions | Publish an exact green signed kit, clean the separately claimed partial build graph, and retain one successful image plus Foundation receipt. |
| 2026-09-11 | implemented | The exact live runner-image claim exposed an Azure VM Image Builder incompatibility with enforced Storage Shared Key denial. The replacement makes every retained build resource and lifecycle action plan-visible, runs the build and verifier on private NICs, deprovisions before V2 capture, and reads extension instance views independently. | Issue `#763`; failed supervised run reason `runner_image_apply_or_verification_failed`; sanitized Azure readback classified the service-created staging Storage authentication failure; focused Terraform and Python checks. | Merge the corrected source, complete a separately reviewed exact destroy plan for the failed attempt, and retain one successful policy-compatible image receipt before resuming Foundation. |
| 2026-09-11 | implemented | Split the official Terraform archive digest from the extracted executable digest after an exact live image plan failed safely before Terraform initialization. The planner now authenticates the executable, image customization authenticates both, host attestation rechecks the installed binary, and preparatory child output no longer contaminates JSON stdout. | `current change`; live stable failure `runner image Terraform executable does not match the pinned toolchain`; 54 focused Python tests and 4 runner-image Terraform tests passed. No image plan or apply occurred. | Publish the corrected exact revision, require green CI, rebuild the signed kit, and generate a new image plan. |
| 2026-09-11 | implemented | Returned the legacy deployment owner pair to its document-size ratchet without removing the required Bastion Network Security Group contract, then refreshed every dependent source digest. | Failed exact CI run `34515475731`, job `102999677890`; document-size, derived-source, translation, Korean-quality, punctuation, and link checks passed. | Publish the corrected exact revision and require green CI before any Azure mutation. |
| 2026-09-11 | implemented | Closed the final CI findings by associating the optional Bastion subnet with the complete Azure-required Network Security Group rules and narrowly allowlisting only the public Microsoft package-signing key fingerprint used by the pinned image toolchain. | Failed exact CI run `34514069001`, gitleaks job `102995024352`, and Terraform security job `102995106664`; local gitleaks `8.24.3`, Checkov `3.2.256`, Trivy `0.72.0`, and 20 bootstrap Terraform tests passed after the fixes. | Publish the corrected exact revision and require green CI before any Azure mutation. |
| 2026-09-11 | implemented | Restricted single-human approval files to a matching dev/manual/single-approver profile, bound enrollment receipts to their actor claims and reran Bastion plus GitHub observations before accepting them, bound state authority plus terminal evidence to one authenticated target-scoped human claim, and made remote cleanup intent-first and absence-gated. | `current change`; 142 routed Genesis tests and 784 deployment CLI tests passed; Terraform passed 20 bootstrap, 15 Foundation, and 3 runner-image tests; Ruff, format, strict mypy, package build, ShellCheck, design-route, ledger, translation, Korean-quality, punctuation, readable-Hangul, GUID, link, layout, and size gates passed. | Publish one exact revision, require green CI, then retain protected quorum, interruption, rollback, no-change, and readiness evidence. |
| 2026-09-10 | implemented | Added one-hour exact-evidence local approvals, claim-before-effect ordering, verification-only restart, stdin-only runner registration, sanitized 15-stage status, independently checked Foundation effects, and authority-gated local-state deletion. | `current change`; private Genesis modules and focused tests passed 93 cases; deployment CLI saved-plan tests passed 49 cases. | Run the remaining local Terraform and documentation gates, then retain governed Azure interruption, rollback, no-change, and readiness evidence on one pushed revision. |
| 2026-08-29 | in-progress | Added the assurance contract after a 40-item adversarial review of the subscription genesis plan. | `current change`; critique resolution register, paired owner documents, and focused documentation checks | Implement the open controls and retain exact-revision new-subscription evidence. |
| 2026-08-29 | in-progress | Implemented the pre-login deployment CLI safety kernel and deterministic full-stage rehearsal. | `current change`; package tests passed 25 cases and productization tests passed 3 cases; Ruff and strict mypy passed | Harden the local kernel, then bind protected Azure and operator surfaces. |
| 2026-08-29 | implemented | Completed 75 focused hardening rounds, fixed every review finding above Low, and passed the shipped-wheel air-gap drill without network or DNS. | Campaign commits from `dd28b64d9`; final severity review reported no Medium-or-higher finding; 89 focused tests, Ruff, strict mypy, locked build, isolated install, and air-gap evidence | Collect subscription-backed state, quota, model, database, inventory, and readiness receipts after login. |
| 2026-08-29 | implemented | Extended the campaign through 93 focused hardening rounds and reconfirmed that only Low-or-lower concerns remain before Azure login. | `current change`; final severity review found no Medium-or-higher issue; 92 focused tests, Ruff, strict mypy, lock verification, source and wheel builds, and fresh plus `--skip-stage` air-gap drills passed | Collect subscription-backed state, quota, model, database, inventory, and readiness receipts after login. |
| 2026-08-29 | implemented | Completed hardening round 94 by making license-token FIFO rejection nonblocking after the stable-HEAD audit found the remaining hang risk. | `current change`; focused FIFO regression test and the final deployment CLI gate stack passed 93 tests | Collect subscription-backed state, quota, model, database, inventory, and readiness receipts after login. |
| 2026-08-29 | implemented | Completed hardening round 95 by making profile, plan-input, and journal FIFO rejection nonblocking. | `current change`; three focused FIFO regression tests and the final deployment CLI gate stack passed 96 tests | Collect subscription-backed state, quota, model, database, inventory, and readiness receipts after login. |
| 2026-08-29 | implemented | Completed hardening round 96 by bounding every deployment CLI trust-key input behind a nonblocking no-follow regular-file reader. | `current change`; focused FIFO, symlink, and oversize trust-key regression test and the final deployment CLI gate stack passed 97 tests | Complete the remaining release-script and artifact-reader hardening before login. |
| 2026-08-29 | implemented | Completed hardening round 97 by consolidating release utility key reads behind a bounded descriptor boundary with private-key ownership and mode checks. | `current change`; three focused release utility tests, Ruff, and strict mypy passed | Complete the remaining workdir, artifact-reader, journal-lock, and resumed-release hardening before login. |
| 2026-08-29 | implemented | Completed hardening round 98 by making release workdir sentinel verification nonblocking before descriptor validation. | `current change`; three focused workdir guard tests and Ruff passed | Complete the remaining artifact-reader, journal-lock, and resumed-release hardening before login. |
| 2026-08-29 | implemented | Completed hardening round 99 by closing artifact metadata and hash check/open FIFO races with nonblocking descriptor and identity validation. | `current change`; four focused replacement-race tests and the final deployment CLI gate stack passed 101 tests | Complete the remaining journal-lock and resumed-release hardening before login. |
| 2026-08-29 | implemented | Completed hardening round 100 by bounding journal lock acquisition with a five-second monotonic deadline and pre/post-lock descriptor validation. | `current change`; focused lock-contention regression test and the final deployment CLI gate stack passed 102 tests | Complete resumed-release child-path hardening before login. |
| 2026-08-29 | implemented | Completed hardening round 101 by moving release child-file writes to a held-parent exclusive writer and all resumed drill inputs to bounded nonblocking readers. | `current change`; three focused secure-writer tests, release productization checks, and fresh plus `--skip-stage` air-gap drills | Repeat the final severity audit and close any remaining finding above Low before login. |
| 2026-08-29 | implemented | Completed hardening round 102 by publishing offline-kit metadata and streamed bundle archives through the held-parent exclusive writer. | `current change`; four focused secure-writer tests plus release productization and air-gap checks | Repeat the final severity audit and close any remaining finding above Low before login. |
| 2026-08-29 | implemented | Completed hardening round 103 by rejecting release workdirs whose ancestor ownership and sticky-bit posture permits another UID to swap the validated path. | `current change`; four focused workdir guard tests and fresh plus `--skip-stage` air-gap drills | Repeat the final severity audit and close any remaining finding above Low before login. |
| 2026-08-29 | implemented | Completed hardening round 104 by applying the nonreplaceable ancestor policy and descriptor-mode initialization to CLI plan workdirs. | `current change`; focused replaceable and linked-parent regression test and the final deployment CLI gate stack passed 103 tests | Repeat the final severity audit and close any remaining finding above Low before login. |
| 2026-08-29 | implemented | Completed hardening round 105 by disabling the package cache for shipped-wheel air-gap installation after a same-version cached wheel reproduced stale code. | `current change`; productization contract plus fresh and `--skip-stage` air-gap drills installed the authenticated current wheel | Repeat the final severity audit and close any remaining finding above Low before login. |
| 2026-08-29 | implemented | Closed the pre-login campaign after 105 recorded hardening rounds; the post-H105 severity audit found no Medium-or-higher issue. | `current change`; 103 deployment CLI tests, 11 release boundary tests, Ruff, strict mypy over 365 productization sources, locked source and wheel builds, complete productization, H94-H105 changed tests, and fresh plus `--skip-stage` air-gap drills passed | Collect subscription-backed state, quota, model, database, inventory, rollback, no-change, and readiness receipts after login. |
| 2026-08-29 | implemented | Completed hardening round 106 by rejecting surrounding license-token whitespace and writing issued token files without a trailing newline. | `current change`; focused CLI token-reader and release issuer tests | Revalidate the complete selected-feedback set. |
| 2026-08-29 | implemented | Completed hardening round 107 by making optional inventory resource and page counters monotonic once observed. | `current change`; focused progress transition tests | Revalidate the complete selected-feedback set. |
| 2026-08-29 | implemented | Completed hardening round 108 by sealing a manifest version in provision-event v2 while retaining an immutable `genesis.v1` replay mapping for v1 journals. | `current change`; focused future-version state replay and simulation tests | Revalidate the complete selected-feedback set. |
| 2026-08-29 | implemented | Completed hardening round 109 by traversing journal parent paths through held descriptors and rejecting symlinked ancestors. | `current change`; focused immediate and earlier-ancestor symlink tests | Revalidate the complete selected-feedback set. |
| 2026-08-29 | implemented | Reconciled all 78 selected review comments after hardening round 109; 71 were already addressed, three were duplicates, and the four remaining Medium findings were fixed. | `current change`; final review found no Medium-or-higher issue; 108 deployment CLI tests, 12 release boundary tests, Ruff, strict mypy, locked source and wheel builds, and fresh plus `--skip-stage` air-gap drills passed | Collect subscription-backed state, quota, model, database, inventory, rollback, no-change, and readiness receipts after login. |
| 2026-08-30 | implemented | Added a no-mutation bootstrap reconciliation boundary for the stage before the private runner and backend exist, and made application plan-only fail when its state containers are absent instead of creating them. Journal schema v3 rejects terminal stage claims without receipt digests, legacy journals cannot be extended, and the aggregate readiness contract rejects incomplete evidence, child-source gaps, non-independent inventory observation, and any second-run Terraform change. Thirteen hardening rounds closed every Medium finding. | `current change`; focused bootstrap reconciliation, private-output, plan-input, journal, readiness, simulation, and deployment workflow tests; Ruff and strict mypy; final independent severity audit found no Medium-or-higher issue | Implement the separately approved foundation apply and retain state migration, zero-change, rollback, and independent readback receipts. |
| 2026-08-31 | implemented | Added target-checked request-bound GitHub transport for protected plan, exact apply, verification-only resume, and sanitized status, then made database and semantic readiness independently enumerable and fail-closed. | `current change`; deployment CLI and workflow contract tests, Ruff, strict mypy, fresh-database migration evidence, and Console provisioning tests | Retain the governed foundation, runtime-principal, inventory, rollback, and no-change receipts on one pushed revision. |

### Remaining work

- [x] Implemented full-catalog three-role selection, aggregate 8-vCPU headroom, actual image disk
  checks, v1 compatibility, explicit incomplete evidence, and expiry after preclaim reads;
  the focused VM and existing Genesis boundary suites pass 451 tests.
- [x] Verified same-region compatible selection, aggregate quota, private replay, exact approval
  display, and no-reselection/no-repeat-apply boundaries: 307 focused tests and 99.68% coverage
  across five SKU modules. The prior preflight-only evidence below remains historical.
- [x] Verified the exact-plan SKU preflight, expiry recheck, and no-repeat-apply regressions with
  synthetic provider boundaries: 192 tests passed and both new modules reached 100% coverage.
- [ ] Publish and reverify a signed release containing full-catalog image/host selection and
  preflight before using it in a new deployment context; do not edit an extracted signed cache
  or repeat a retained claim.
- [ ] Add a reviewed partial-image recovery path that preserves prior claims and Terraform lineage,
  requires a new exact approved plan, and retains independent cleanup or completion evidence.
- [x] Bound Foundation VM eligibility, headroom, and actual image requirements before planning
  and new claims in the focused host integration tests. SKU eligibility does not reserve capacity.
- [ ] Retain current eligible target metadata, exact approval, and independent successful
  image/Foundation allocation receipts before advancing VM discovery to operationally validated.
- [x] Reconciled and revalidated all 78 selected feedback comments after 109 recorded hardening
  rounds; the final review left only Low-or-lower concerns.
- [x] The local negative, interruption, artifact, packaging, and fresh plus resumed disconnected
  execution matrix passes without customer data or Azure mutation.
- [x] The local private Foundation path rejects stale or mismatched approval, never repeats a
  claimed effect, keeps registration tokens off arguments and records, and withholds readiness.
- [ ] Publish a signed kit with the corrected selector and confirm that its actual bundled
  preparation code accepts default forwarding routes while rejecting real CIDR conflicts.
- [ ] Bind the local coordinator to that corrected kit through an explicit verified source transition,
  preserving the prior work directory and proving the intended kit revision before another Azure run.
- [ ] Retain one governed empty-subscription run, one supported-upgrade run, one rollback/restore
  run, and one second-run no-change receipt.

## Initial-inventory P6 hardening rounds

This campaign reviewed only the P6 change and its directly affected boundaries. Rejected
hypotheses remain recorded so later work does not duplicate the existing snapshot authority or
second-plan implementation. The final local review found no confirmed Medium-or-higher defect.

| Round | Critique | Outcome | Evidence | Residual risk |
|-------|----------|---------|----------|---------------|
| P6-H01 | Atomic promotion might be missing. | Rejected: the existing PostgreSQL snapshot store stages candidates and swaps the singleton active pointer in one transaction under the promotion lock. No second snapshot authority was added. | Existing snapshot persistence tests plus 178 ARG and inventory regressions | Low: live database isolation remains deployment evidence. |
| P6-H02 | Progress might expose provider identifiers. | Rejected: the strict shared contract contains opaque run identifiers, digests, timestamps, and counters only; extra fields are forbidden and the Operator builds an allowlisted projection. | Four contract tests and strict type checks | Low: application logs remain governed by existing logging policy. |
| P6-H03 | A configured endpoint could exfiltrate a managed-identity token. | Confirmed and fixed: the Blob publisher now accepts only credential-free, port-free Azure public Blob hosts with exactly one container path. | Seven Blob publisher cases | Low: sovereign-cloud Blob suffixes require a reviewed extension. |
| P6-H04 | A partial multi-sink append could reuse a sequence with different content. | Confirmed and fixed: any ambiguous append poisons the recorder; the run fails closed and cannot advance that in-memory chain. | Ambiguous-publisher regression | Low: operator recovery starts a bounded new attempt. |
| P6-H05 | Fallback totals could claim work for a source that never started. | Confirmed and fixed: each source adds its denominator only when its stream begins, and closure no longer overwrites attempted-work counters. | Recorder and 242 inventory/CLI regressions | Low: page totals remain labeled estimates. |
| P6-H06 | The collector could emit 100 percent before independent closure. | Confirmed and fixed: collection stops at `verify`; the distinct closure process appends `complete` only after readback. | Resume/finalization contract regression | Low: live process separation remains deployment evidence. |
| P6-H07 | A later active generation could satisfy an earlier attempt. | Confirmed and fixed: `verify` records the promoted generation digest and closure requires an exact digest match. | Exact-generation positive and negative tests | Low: digest strength depends on SHA-256. |
| P6-H08 | Validating only the final progress record could miss earlier chain tampering. | Confirmed and fixed: closure validates every bounded sequence, content digest, and previous digest from genesis. | Closure chain tests and strict contract validation | Low: the local chain bound caps one closure read at 100,000 records. |
| P6-H09 | Inventory progress could reset the overall provisioning run in Console. | Confirmed and fixed: the reducer tracks a separate inventory run and sequence, preserving readiness, stages, and global progress. | 43 Console reducer and decoder tests | Low: the detailed panel currently favors counts over an inventory-only percentage. |
| P6-H10 | A per-principal SQL predicate might be required for progress replay. | Rejected: progress is deployment-wide, identifier-free operational evidence available to every authenticated reader; it carries no principal-owned content. | Operator SQL and least-privilege migration tests | Low: authenticated reader membership remains an Operator API responsibility. |
| P6-H11 | Managed hosts might need to import a second inventory snapshot. | Rejected: the managed host explicitly invokes the authoritative ARG/ARM collector; importing another snapshot would duplicate authority and bypass current observation. | Standalone initial-scan regression | Low: disconnected provider collection correctly fails closed. |
| P6-H12 | The application could create an unauthorized cross-resource-group Blob role assignment. | Confirmed and removed: the initial scan uses the deploy identity's existing state-account data role; recurring inventory keeps PostgreSQL as current authority. | Root and Foundation Terraform validation | Low: recurring private Blob mirroring is intentionally not claimed. |
| P6-H13 | P6 completion could overclaim whole-subscription readiness. | Confirmed and fixed: terminal receipts expose `inventory_ready=true` but preserve `subscription_ready=false` until non-inventory evidence closes. | Standalone receipt tests and ledger review | Low: exact-revision live aggregate readiness remains open. |
| P6-H14 | Interruption after terminal persistence could make closure unrecoverable. | Confirmed and fixed: closure accepts an already valid complete record and skips duplicate finalization; Blob-first final append makes a PostgreSQL retry exact. | Idempotent finalizer path and duplicate Blob tests | Low: process interruption still leaves a visible resumable state. |
| P6-H15 | Full CI could retain stale 15-phase, two-container, additive-receipt, and file-size assumptions. | Confirmed and fixed after exact-head CI: tests now cover 16 phases and three containers, additive `inventory_ready` remains compatible with older doubles, and progress composition plus value types moved to focused modules below the 800-line ceiling. | CI run `35051674195`, attempt 1; focused 95 deployment and 72 Core tests; Ruff, format, strict typing, and LOC checks | Low: the repaired exact head still requires protected CI. |

## Signed-kit hardening rounds

This bounded campaign reviews VM selection and signed-kit delivery, not the entire repository.
An unconfirmed hypothesis is not a defect; a passing local test is not release or Azure evidence.

| Round | Critique | Outcome | Evidence | Residual risk |
|-------|----------|---------|----------|---------------|
| H01 | Policy check/open and unsafe file identity | Medium: hard-linked and externally writable policy files were accepted. The bounded descriptor reader now rejects them and prevents final-component symlink/FIFO races. | `test_genesis_vm_policy_reader.py` and VM integration: 27 passed | Low: authenticated parent/source authority still belongs to the existing kit verifier. |
| H02 | Private CLI context failure disclosure | Medium: a missing context raised a raw path-bearing filesystem exception. It now fails with a fixed evidence reason before provider reads. | `test_genesis_vm_environment.py`: 3 passed | Low: context availability remains an operator prerequisite. |
| H03 | Partial discovery evidence loss | Medium: a catalog/quota error left no diagnostic record and discarded a completed catalog. Completed inputs now persist before dependent reads, with failed-stage and unknown digest fields. | Failure plus VM integration suites: 23 passed | Low: truncated provider responses never become complete evidence. |
| H04 | Previous signed-release loss on rebuild | Medium: reusing an owned output deleted the signed archive and partial images before new work. Complete builds now require a fresh root; directory and symlink attempts preserve existing bytes. | `test_standalone_kit_release_guards.py`: 2 passed; real ownership guard retained | Low: prior incomplete outputs require deliberate separately scoped retention management. |
| H05 | Aggregate quota and missing-family semantics | Rejected the proposed zero-quota fallback: missing usage is unknown. Added exhaustive same/different-family demand and ordering regressions; no production defect reproduced. | `test_genesis_vm_quota_properties.py`: 40 passing combinations | Low: metadata cannot reserve allocation capacity. |
| H06 | Pagination can reset acquisition bounds | Rejected: the existing reader maintains cumulative bytes/deadline and a visited-page set. Added multi-page counterexamples rather than widening the schema or limits. | Catalog suite: 24 passed | Low: incomplete acquisition remains a blocker, not an empty available catalog. |
| H07 | Gallery region normalization and ambiguous replicas | Rejected the reported region-normalization defect: display/canonical names already normalize. Added mixed-region, alias-duplicate, and definition/version duplicate-JSON regressions. | Image requirements suite: 28 passed | Low: a metadata match is not independent image provenance. |
| H08 | Partial claim may rerank or replay apply | Rejected: a claim without a receipt already selects verification only. Added success/failure readback cases that forbid quota refresh, new claims, and apply while preserving original claim bytes. | Claim/expiry integration slice: 6 passed | Low: failed effect verification remains incomplete and needs a separately approved recovery. |
| H09 | Interrupted manifest/signature writes look shippable | Rejected: the signer removes the previous signature before writing either record, and independent verification rejects both interrupted states. Added executable fault injection using synthetic keys. | Signature interruption suite: 2 passed | Low: an interrupted private stage may retain an unsigned manifest, never an accepted kit. |
| H10 | VM discovery files escape authenticated delivery | Rejected: tracked source discovery includes all seven new VM artifacts and the bundle verifier rejects policy byte changes, source deletion, and unsigned additions. Added actual builder/verifier tests with synthetic keys. | `test_vm_policy_signed_bundle.py`: 4 passed | Low: the real complete release still needs exact-source CI and independent artifact validation. |
| H11 | Direct Foundation timeout leaks private argv | Medium: reproduced six command/helper/terminal cases. Fixed reasons now replace raw subprocess and filesystem exceptions. | Redaction, Foundation apply, and VM integration: 34 passed | Low: private evidence remains local; no retry is granted. |
| H12 | Failed archive checksum reports success | Medium: checksum exit failure, empty output, and malformed output all produced exit zero. A separately checked digest now gates success. | Release guard suite: 5 passed | Low: downstream signatures and exact file verification remain mandatory. |
| H13 | Reusable Console embeds local deployment configuration | Medium: ordinary build mode inherited deployment defaults. The release now uses the existing offline mode and packages only that output with the same installer layout. | Release guard suite: 6 passed; Console configuration/Vite suites: 205 passed | Low: installation-time public bindings remain required. |
| H14 | Complete builds can stall indefinitely before signing | Medium: Docker and npm were unsupervised. Shared total, stage, and no-progress bounds now stop process groups and prevent later signing or success. | Real supervisor and release-stage suites: 21 passed | Low: elapsed build limits do not prove artifact correctness. |
| H15 | Source changes during assembly can retain an earlier revision label | Medium: lock-only checks missed changed service bytes. A private detached source checkout and raw-byte/mode/metadata pin now cover image, Console, support, and signing boundaries. | Source-drift, support-wheel, release-guard, and deadline suites: 55 passed | Low: source identity does not substitute for required CI or artifact acceptance. |
| H16 | Relative signing keys break after source isolation | Medium: reproduced the relative-path regression introduced by H15. Caller-relative paths now become absolute before directory changes, without copying keys. | Release guard suite: 9 passed | Low: signer ownership and trust-root checks remain separate. |
| H17 | CLI wheel staging inherits unrelated virtual environment | Medium: actual uv boundary received the caller's environment. Every CLI build command now uses a private staging environment and clears active-venv selection. | Environment and productization suites: 8 passed | Low: dependency locks and wheel hashes remain mandatory. |
| H18 | Foundation final cleanup escapes the safe error boundary | Medium: reproduced uncaught private paths and premature success. Cleanup is now value-safe, precedes success, and preserves the original failure. | Redaction, Foundation apply, and VM integration: 36 passed | Low: failed cleanup requires private evidence review, not retry authority. |
| H19 | Nested timeout groups survive release cancellation | Medium: a synthetic builder remained alive after the outer deadline. Nested source, wheel, and mirror commands now use signal-forwarding supervisors with shorter inner termination grace. | Actual nested process cancellation, environment, mirror, and source suites passed | Low: retained partial artifacts never authorize resumption or signing. |
| H20 | Packaged Genesis still requires an unshipped source project | Medium: all four fixed child wrappers failed in a bundle-shaped tree. They now receive the installed coordinator interpreter; planning and reverification use that CLI directly. | Packaged launch/plan: 18 passed; remaining Foundation, private execution, image, enrollment, and state tests passed unchanged | Low: a full signed artifact drill remains required before release acceptance. |
| H21 | Nested Genesis cancellation can leave an effect running | Medium: reproduced a surviving synthetic inner effect. Main-thread termination now unwinds through child cleanup using shorter inner grace and restores prior signal handlers. | Heartbeat, private-execution, Foundation apply/redaction suites: 30 passed | Low: cancellation does not erase claims or grant retry. |
| H22 | Fresh Foundation plan requests an uninitialized remote backend | Medium: real provider-free Terraform reproduced the failure. New Foundation roots default local; the signed fixed AzureRM example activates only in the digest-bound migration archive. | Actual local Terraform and state suites: 14 passed | Low: remote migration still requires exact approval and authoritative readback. |
| H23 | Retained Foundation state invalidates verification-only resume | Medium: a real signed execution copy failed after normal state/backup appeared. Every immutable source now matches a fresh verified bundle while only exact private recovery-state paths are admitted. | Stateful snapshot, Foundation apply/state, and private-execution suites: 31 passed | Low: immutable claim and effect readback still gate resume, never a second apply. |
| H24 | Empty signed catalog placeholders fail stateful resume | Medium: H23 reused a nonempty record reader for immutable payloads. A bounded descriptor-safe source reader now accepts authenticated empty files; state remains nonempty and private. | Signed empty-file replay and tamper suite: 7 passed | Low: all source membership and byte equality checks remain exact. |
| H25 | Installed outer timeout kills only the orchestrator | Medium: subprocess.run bypassed nested SIGTERM cleanup. The installed boundary now terminates the group gracefully, reserves inner cleanup time, and preserves actual exit codes. | Installed and Genesis process suites: 12 passed | Low: process termination leaves claims and recovery evidence intact. |
| H26 | Equal inner cleanup grace races across multiple Genesis layers | Medium: a parent could kill its child supervisor before deeper cleanup. Depth-bound grace now strictly decreases; the installed boundary is tested with one, two, and three nested supervisors. | Installed/Genesis process and private-execution suites: 23 passed | Low: forced termination cannot replace later authoritative effect reconciliation. |

## Critique resolution register

The review evaluated the revised plan rather than repeating the first eight findings. `Resolved in
design` means the normative owner now specifies the control; it does not claim implementation.

| ID | Critique | Resolution | Design state |
|----|----------|------------|--------------|
| G01 | The first runner cannot create itself. | Define the bounded local foundation executor and remote handoff. | Resolved in design |
| G02 | Local bootstrap state lacked file and lifetime controls. | Require `umask 077`, regular-file checks, strict modes, no secrets, and bounded deletion. | Resolved in design |
| G03 | "Reconstruct state" could infer ownership from names. | Require Terraform state migration plus exact lineage, address, id, serial, and count comparison. | Resolved in design |
| G04 | Random state-account names break resume. | Derive once, bind to the run, and reuse on resume. | Resolved in design |
| G05 | Blob versioning was best effort in current scripts. | Make versioning and the complete state-storage posture blocking postconditions. | Resolved in design |
| G06 | State locking and lease reachability were not verified. | Require the backend lease path and one run-level lease. | Resolved in design |
| G07 | Provider registrations were listed but not lifecycle-managed. | Add each registration to the manifest with apply and readback. | Resolved in design |
| G08 | Azure Policy, deny assignments, and management locks could invalidate adoption. | Classify them as conflicts unless separately designed and approved. | Resolved in design |
| G09 | RBAC propagation could race the next stage. | Require effective-role observation before dependent work. | Resolved in design |
| G10 | Runner registration tokens could cross Terraform state or command arguments. | Forbid those channels and require provider-hosted authorization plus protected input. | Resolved in design |
| G11 | GitHub repository settings were still manual and incomplete. | Compile idempotent settings from the manifest and report missing references without values. | Resolved in design |
| G12 | Database credentials depended on a workstation-to-GitHub secret flow. | Generate on the private host, store in the approved provider, and consume by reference. | Resolved in design |
| G13 | Entra app registrations, roles, groups, and redirect origins were outside readiness. | Add directory state to planning and independent readback. | Resolved in design |
| G14 | Artifact and tool downloads could use mutable `latest`. | Require signed or checksum-pinned tools, images, providers, and offline equivalents. | Resolved in design |
| G15 | Closed-network and sovereign-cloud behavior was underspecified. | Require cloud-specific endpoints and fail closed when parity is unavailable. | Resolved in design |
| G16 | VPN, hub, spoke, resolver, and endpoint CIDRs could overlap. | Add complete address and route overlap preflight. | Resolved in design |
| G17 | The plan had no subscription-and-environment mutual exclusion. | Add a run lease keyed by cloud, tenant, subscription, environment, and root. | Resolved in design |
| G18 | Idempotency keys lacked declared scope. | Require per-entry keys in the finite manifest under the run lease. | Resolved in design |
| G19 | Approval count ignored high-impact quorum policy. | Require at least one accountable approver and configured quorum for high-impact plans. | Resolved in design |
| G20 | Plan expiry and leases trusted an unspecified clock. | Require authenticated UTC and bounded skew. | Resolved in design |
| G21 | Cancellation semantics could interrupt an effect without closure. | Stop new work, bound in-flight closure, release leases, and use preapproved cleanup only. | Resolved in design |
| G22 | Rollback existed as text but not a prerequisite rehearsal. | Require a tested rollback and backup/restore point before destructive work. | Resolved in design |
| G23 | Cost exposure was not an approval input. | Add monthly cost, one-time validation budget, quota use, egress, and non-scale-to-zero reporting. | Resolved in design |
| G24 | PostgreSQL readiness omitted server parameters, locale, time zone, and extension versions. | Add them to the database manifest and readback gates. | Resolved in design |
| G25 | Migration ordering did not define transaction and cross-service failure behavior. | Record transaction, locks, deadlines, adoption, and rollback per migration branch. | Resolved in design |
| G26 | A migration principal could self-certify runtime access. | Require separate runtime-principal readback. | Resolved in design |
| G27 | "Every catalog entry" could activate collected reference material. | Classify runtime-required, reference-only, optional-disabled, and deployment-overlay entries. | Resolved in design |
| G28 | Default materialization could overwrite deployment-owned settings. | Require release digests and compare-and-set preservation. | Resolved in design |
| G29 | Runtime startup could observe mixed migration/catalog state. | Add one atomic readiness marker binding all heads, digests, roles, and time. | Resolved in design |
| G30 | The TPM formula ignored aggregate reuse and quota dimensions. | Add input/output/cache tokens, RPM, concurrency, SKU units, existing use, reserve, burst, and sharing rules. | Resolved in design |
| G31 | Quota could change between plan and apply. | Compare immediately before creation and force replan on drift. | Resolved in design |
| G32 | Publisher terms, content filters, responsible-AI policy, and preview limits were absent. | Add them as blocking model prerequisites. | Resolved in design |
| G33 | Live model probes incurred cost without separate authority. | Require explicit live-model approval and a cost/token budget. | Resolved in design |
| G34 | Quota increase handling could poll indefinitely or silently shrink. | Treat it as an external prerequisite with bounded status and no silent reduction. | Resolved in design |
| G35 | A full subscription could exceed memory, page, or child-resource limits. | Require staged spill, signed bounds, stable blockers, and a reviewed partition strategy. | Resolved in design |
| G36 | Count and scan denominators could diverge during resource churn. | Label estimates and use final coverage plus overlay closure for completeness. | Resolved in design |
| G37 | Private AKS and child sources could be omitted while claiming completion. | Require endpoint, certificate, audience, identity, and authorization closure per declared child source. | Resolved in design |
| G38 | Progress mixed completion percentage with duration prediction. | Separate exact work counts, estimated denominators, completion weights, and rate-qualified ETA. | Resolved in design |
| G39 | Progress events lacked tamper evidence, bounds, accessibility, and localization requirements. | Add schema version, chain digest, sequence, bounded retention/rendering, screen-reader, and locale contracts. | Resolved in design |
| G40 | Final readiness lacked operational alerts, negative tests, upgrade, teardown, and clean-subscription evidence. | Add operational closure and the complete end-to-end matrix. | Resolved in design |
