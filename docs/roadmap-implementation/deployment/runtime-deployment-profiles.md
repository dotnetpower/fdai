# Runtime Deployment Profile Implementation

This ledger tracks the implementation evidence for new-install runtime selection between Azure
Container Apps and Azure Kubernetes Service (AKS). The canonical design remains in
[Runtime Deployment Profiles](../../roadmap/deployment/runtime-deployment-profiles.md).

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Standalone inventory observation roles | implemented | `standalone_host.py`; `test_standalone_host.py`; 56 focused standalone-host tests, strict mypy, and Ruff pass in the current change. | The substrate target set applies the existing subscription, workspace, cluster, cost, and stage-topic assignments with the inventory identity. Exact deployed role readback and recovered source evidence remain open. |
| AKS-first baseline and staged private networking | in-progress | `infra/modules/network`; `infra/runtimes/aks/cluster`; focused AKS infrastructure tests and Terraform validation pass in the current change | Terraform creates dedicated node and delegated API-server subnets, enables API Server VNet Integration and requires explicit public API CIDRs. CLI CIDR handoff, the default selection, Console-originated private-network planning and Azure receipts remain open. |
| Tenant-time image construction removal | in-progress | Revised runtime and deployment-skill contracts; existing source-image and runner-image implementation paths remain | Tenant provisioning must consume prebuilt signed images only. Removal of local Docker/Buildx, image-capture, and image-build recovery paths requires source and test changes. |
| Public compute pricing and stage review | in-progress | `azure_retail_prices.py`, `deployment_cost.py`, `source_azure.py`, `standalone_review.py`; 92 focused pricing/source/approval checks passed. | Source planning blocks if maximum steady-state compute alone exceeds the ceiling. The public Linux rate was independently read by the implemented client. All other installation/setup costs remain unverified; four existing deployment stages retain identical approval checks. |
| Complete baseline assembly and readback | implemented | `standalone_host.py::_prepare_aks_application`; shared identity/storage/topic outputs; AKS workload sidecar renderer; focused host, infrastructure and native Terraform tests pass in the current change. | The factory renders all five baseline services with distinct document identities, role-scoped DSNs, lexical retrieval, ADLS, exact Event Hubs roles and replica-local ClamAV. Application readback still requires independent deployed health and exact image digests before readiness. |
| BYO-subnet egress and exact workload observations | implemented | `infra/runtimes/aks/cluster/main.tf`; three infrastructure contract cases; cluster Terraform validation and Checkov 16 passed / 0 failed; `aks_readiness.py` with 18 focused tests. | Explicit user-assigned NAT replaces the unsupported managed-VNet mode. Azure Policy-owned public-IP `ip_tags` are preserved while regular tags remain Terraform-owned. Operator DB-role rendering is fixed. Workload readback verifies the complete selected set and Pod digests; full transport, job and Console readiness remains open. |
| AKS security scan remediation | implemented | `infra/runtimes/aks`; two cluster contract tests, two DSN rotation tests, three native workload plan tests; Checkov 3.2.256 reports 54 passed, 0 failed, and 8 resource-local exceptions. | Azure Policy, host encryption, 50-pod node limits, read-only service roots, bounded scratch storage, and explicit image pulls are configured. Scanner limitations, Managed OS disks, and coordinated DSN rotation have narrow documented exceptions. Azure operational evidence remains open. |
| AKS Container Insights collection | implemented | `infra/runtimes/aks/cluster/main.tf`; `tests/integration/infra/test_aks_runtime_security.py`; focused regression and cluster Terraform validation pass in the current change. | The managed-identity addon now has a Terraform-owned DCR and cluster association for the default Container Insights stream and `ContainerLogV2`. An exact apply and fresh workspace-table evidence remain open. |
| AKS Python application telemetry | implemented | `fdai/shared/telemetry/setup.py`; shared Key Vault secret and Core role; `standalone_host.py`; focused telemetry, infrastructure, and deployment CLI tests pass in the current change. | Core selects the Azure Monitor OpenTelemetry Distro only with the injected Application Insights secret and rejects a competing OTLP exporter. Exact image rollout and fresh Application Insights evidence remain open. |
| Runtime profile and CLI grammar | implemented | `cli_parser.py`, `standalone_host.py`, `runtime_profile.py`, and focused CLI and coordinator tests | New installations default to AKS with `postgres-flex`; explicit Container Apps remains available only for compatible existing-installation selection. Runtime selection does not authorize an apply or implicit migration. |
| Container Apps observation identity handoff | implemented | `infra/outputs.tf`; `infra/services/core-control-plane/`; `service-deploy.yml`; `hydrate_observation_context.py`; focused workflow, materialization, service-root, and runtime tests | The exact inventory-reader identity is attached only with deployed observation, removed when disabled, and kept distinct from the FinOps and Resilience executor credential lineages. This source wiring does not itself prove a deployed observation. |
| AKS network and cluster state | implemented | `infra/modules/network`; `infra/runtimes/aks/cluster`; focused AKS infrastructure tests and Terraform validation pass in the current change. | The Standard-tier cluster uses separate system and autoscaled user pools, dedicated node and delegated API-server subnets, API Server VNet Integration, Microsoft Entra RBAC, Cilium, workload identity, managed Key Vault CSI, and a separate backend key. Public baseline planning requires at least one explicit authorized CIDR; private mode requires none. |
| AKS workload state | implemented | `standalone_host.py`; `infra/runtimes/aks/workloads`; native workload and lifecycle tests | Typed Deployments, Services, ServiceAccounts, federated credentials, HPA, PDB, NetworkPolicy, five default CronJobs, CSI secret synchronization and digest-pinned sidecars share one workload state. Operational history uses the read-only inventory identity, private archive URL, service-owned DSN, and fixed `shadow` mode. |
| Compact in-cluster PostgreSQL | implemented | `infra/runtimes/aks/database`; `terraform validate` passes in the current change. | The non-production profile uses one pgvector StatefulSet, one Premium SSD claim, a private load balancer, generated credentials and TLS material, and a Key Vault DSN with `sslmode=require`. It does not claim HA, backup, or point-in-time recovery. |
| Managed-host stage routing | implemented | `standalone_application.py`; `standalone_host.py`; 36 focused coordinator tests pass in the current change. | Shared substrate, AKS cluster, optional database, migrations, workloads, and second plans use distinct exact-plan approvals and recovery receipts. |
| Signed online and offline kit inputs | implemented | `build-standalone-deployment-kit.sh`; `stage-offline-kit.sh`; `mirror-locked-providers.sh`; `test_extract_kubelogin_archive.py`; six extractor tests pass in the current change. | The exact-file-set kit includes the three AKS roots, provider locks, pgvector OCI archive, `kubectl`, and `kubelogin`. A complete kit build remains to be recorded. |
| Capacity and regional feasibility | in-progress | `aks_preflight.py`; structural node floors in `runtime_profile.py`; focused preflight and profile tests | The read-only preflight performs one regional catalog read, serializes only exact selected SKU names, and checks regional restrictions, zones, architecture, host encryption, and family plus total quota. Allocatable workload-envelope calculations and a governed deployment receipt remain open. |
| Operational readiness | in-progress | Local source, test, and Terraform validation evidence only. | No live new-subscription online or artifact-offline AKS receipt exists yet. The path is not classified as production validated. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-16 | implemented | Added the supported Python Azure Monitor OpenTelemetry Distro to Core and routed the existing workspace-based Application Insights connection through a Core-only Key Vault secret and AKS CSI binding. The exporter remains disabled without that binding, and simultaneous Application Insights and OTLP configuration fails before provider installation. | `current change`; Core telemetry setup and dependency lock; shared Terraform secret, role and name-only output; standalone AKS renderer; focused telemetry, infrastructure, Terraform, and deployment CLI checks. | Publish a signed Core image, apply the exact substrate and workload plans, verify healthy replicas, and retain fresh Application Insights telemetry without disclosing the connection string. |
| 2026-09-16 | implemented | Removed the Container Insights association's direct dependency on the managed cluster resource while retaining the same exact authenticated subscription, resource group, and canonical cluster identity. Monitoring-only plans can now exclude unrelated cluster drift. | `current change`; `infra/runtimes/aks/cluster/main.tf`; `tests/integration/infra/test_aks_runtime_security.py`; focused infrastructure and Terraform checks. | Publish through protected CI, generate and approve a DCR-and-association-only plan, then retain fresh workspace and authenticated Console evidence. |
| 2026-09-16 | implemented | Added all existing inventory read and pipeline-stage sender assignments to the standalone substrate Terraform target set. This closes the source defect that could create the identity while leaving ARG, metrics, logs, cost, Kubernetes, and activity publication unauthorized. | `current change`; `standalone_host.py`; `test_standalone_host.py`; 56 focused tests, strict mypy, and Ruff passed. | Apply an exact reviewed substrate plan and retain role readback plus successful provider-source evidence before calling the deployed observation plane validated. |
| 2026-09-16 | implemented | Preserved Azure Policy-owned `ip_tags` on the AKS egress public IP without ignoring ordinary resource tags, cluster security settings, or Container Insights resources. | `current change`; `infra/runtimes/aks/cluster/main.tf`; `tests/integration/infra/test_aks_runtime_security.py`; focused egress regression and cluster Terraform validation pass. | Complete protected CI, then approve only an exact plan with no public-IP or NAT replacement before applying the host-encryption correction and DCR resources. |
| 2026-09-16 | implemented | Added the Container Insights Data Collection Rule and cluster association required by the managed-identity AKS monitoring addon, with the default group stream, one-minute collection, and `ContainerLogV2`. Console publication now prefers an available browser-gateway URL and otherwise preserves the Container Apps FQDN path. | `current change`; AKS DCR regression, publisher regression, cluster Terraform validation, and shell syntax check. | Apply an exact reviewed cluster plan, then retain fresh `KubePodInventory`, `ContainerLogV2`, and successful observation-source evidence before classifying monitoring as validated. |
| 2026-09-16 | implemented | Aligned the public and standalone new-install runtime defaults with AKS and added the fixed-shadow operational-history lifecycle as the fifth AKS CronJob using the existing inventory identity, private archive URL, and service-owned state DSN. | `current change`; focused CLI help, standalone host, AKS runtime security, and lifecycle Job checks passed 201 cases. | Retain exact approved plan, apply, Pod/image, scheduled-job, Console, and rollback evidence before changing operational readiness from `in-progress`. |
| 2026-09-16 | implemented | Completed the five-service AKS renderer with separate Document API and Worker identities, ADLS and pipeline-topic bindings, role-scoped DSNs, lexical retrieval and a digest-pinned replica-local ClamAV sidecar. Added AKS-only pipeline sender/receiver grants and kept the legacy Container Apps ingestion module outside the substrate target. | `current change`; shared outputs and RBAC, `standalone_host.py`, workload sidecar schema and native tests; focused standalone-host and AKS infrastructure tests. | Publish through protected CI, then import only the existing signed image digests, run migrations, apply the five workloads and retain independent Pod/image health evidence. |
| 2026-09-16 | implemented | Added the matching resource-local Checkov exception after its private-cluster rule flagged the same restricted public baseline. The exception does not alter the CIDR, identity, local-account or VNet Integration controls and does not suppress any other Checkov policy. | Failed CI run `35046058193`, job `104636050471`, step `Checkov Terraform scan`, rule `CKV_AZURE_115`; focused AKS infrastructure tests cover both scanner exceptions and the shared compensating controls. | Pass the exact Checkov 3.2.256 scan and protected CI, then retain plan and endpoint readback evidence before any private transition claim. |
| 2026-09-16 | implemented | Added a resource-local Trivy exception for the intentionally restricted public AKS baseline after the scanner treated the variable-driven private transition as always disabled. The exception retains explicit CIDRs, Microsoft Entra RBAC, disabled local accounts and API Server VNet Integration as compensating controls; all other Terraform findings remain enforced. | Failed CI run `35045291706`, job `104633824493`, step `Trivy Terraform misconfiguration scan`, rule `AZU-0065`; exact local Trivy 0.72.0 command reports zero Medium, High or Critical findings; focused AKS infrastructure tests cover the exception and controls. | Retain exact plan and runtime evidence for the restricted endpoint, then complete the separately governed private-cluster transition without broadening the scanner exception. |
| 2026-09-16 | implemented | Decoupled the AKS-required VNet, node subnet and delegated API-server subnet from detailed private networking. AKS baseline materialization keeps private endpoints and hub peering disabled and selects ACR Basic; existing private Container Apps preparation remains private and Premium. | `current change`; `infra/main.tf`; `infra/outputs.tf`; `standalone_host.py`; focused standalone-host and AKS infrastructure tests report 56 passed; shared and cluster Terraform roots validate. | Retain an exact basic-stage plan proving private endpoint and peering changes are absent, then apply only after current approval and implement the separate Console-originated private transition. |
| 2026-09-16 | implemented | Added the AKS basic-stage network shape with a dedicated delegated API-server subnet, API Server VNet Integration, explicit public API authorization ranges and separate cluster-identity network grants. Private mode remains an explicit later selection and rejects retained public CIDRs. | `current change`; `infra/modules/network`; `infra/outputs.tf`; `infra/runtimes/aks/cluster`; `tests/integration/infra/test_aks_runtime_security.py` reports 4 passed; both shared and cluster Terraform roots validate. | Wire the authorized CIDR through the coordinator, make AKS the new-install default, retain an exact plan and apply receipt, then implement the Console-originated private transition. |
| 2026-09-16 | not-started | Revised the target contract to make AKS the new-install default, reserve API Server VNet Integration at basic deployment, defer selected private networking to a Console-originated protected plan, and prohibit image construction during tenant provisioning. | `current change`; design and skill paths only; no implementation or Azure mutation claimed. | Change CLI defaults and schemas, remove tenant image builders, implement the network intent and protected execution states, and retain baseline plus private-transition receipts. |
| 2026-09-15 | validated | Re-ran the bounded three-system and three-user-node profile on corrected pushed revision `a67ef6a2a1f810f25de467db1968198e8c3155d6`. One exact-name projection returned the selected SKU evidence and the profile remained feasible at 32 required and 32 remaining DAsv5-family vCPUs, with no blocker, apply authority, mutation, or deployment-readiness claim. | Read-only Azure receipt `de3415488200bd4dddd5a40702fb3d39207a0bc1ec2044dee6c3f5f28eb8993a`; runtime profile digest `28e02ead14fae8271f9554a76b2b712be741287df0a84205fa73defbc74ad395`; PR #1063 head `a67ef6a2a1f810f25de467db1968198e8c3155d6`. | Preserve this exact profile through plan and approval, add allocatable workload-envelope validation, and complete the governed deployment path. |
| 2026-09-15 | validated | Ran the exact selected-SKU preflight on pushed revision `3fcccb1e19c5156cf3fc2f7592553855e99ae174`. The default five-user-node maximum was truthfully blocked because it required 44 DAsv5-family vCPUs with 32 remaining. A separately digested profile with three system and three user nodes plus one surge node per pool required exactly 32 vCPUs and returned `state=feasible`, `blockers=[]`, `apply_authorized=false`, and `mutation_performed=false`. | Read-only Azure receipt `06cd3d80aa738599b2be1c1942bdc30743b1fab63154a4e0bd9f62e9e7cbcb13`; runtime profile digest `28e02ead14fae8271f9554a76b2b712be741287df0a84205fa73defbc74ad395`; PR #1062 head `3fcccb1e19c5156cf3fc2f7592553855e99ae174`. | Preserve this exact bounded profile through plan and approval, add allocatable workload-envelope validation, and complete the governed deployment path. |
| 2026-09-15 | implemented | Corrected the preceding per-SKU query after independent review and Microsoft Learn evidence showed Azure supports only a server-side location filter and Azure CLI `--size` filters locally after catalog retrieval. One provider request now uses an exact-name JMESPath projection to serialize only the selected rows, including overlapping distinct names, while preserving handled provider-shape errors. | `current change`; Microsoft Learn Resource SKUs List contract; focused shared and distinct-SKU tests; Ruff and strict mypy. | Re-run the exact-revision read-only receipt before plan review; allocatable workload-envelope validation and the governed deployment path remain open. |
| 2026-09-15 | implemented | Replaced the bounded preflight's full regional VM SKU catalog download with one exact `--size` query per distinct selected SKU. A shared system/user SKU is read once, while different SKUs remain independently observed under the same total deadline. | `current change`; focused AKS preflight tests, Ruff, strict mypy, and a read-only provider measurement showing account and quota reads complete quickly while the exact selected SKU read remains within the existing per-call deadline. | Retain the exact-revision read-only receipt, add allocatable workload-envelope validation, and complete the governed deployment path. |
| 2026-09-15 | in-progress | Corrected the shared managed-host image prerequisite's local poweroff wait to use the coordinator's trusted CLI instead of a missing workstation absolute path. Runtime selection, approved state and resource ownership remain unchanged. | Current change; 58 runner-image Python tests, Terraform validation and five Terraform mock tests passed. The [installation ledger](installable-deployment-cli.md) records the actual partial Azure apply and independent stopped-VM observation. | Govern recovery of the retained partial image construction without repeating its apply; runtime and application readiness remain incomplete. |
| 2026-09-15 | implemented | Extended the Container Apps platform-to-service handoff with the exact observation source identity and action-specific executor credential lineages. Workflow validation rejects malformed bindings, hydration adds the read-only identity idempotently, and disabling observation removes only that identity. | `current change`; 577 focused tests, strict mypy, Ruff, ActionType support, service workflow, root Terraform validation, and five service-root Terraform tests. | Retain a current-revision deployed VM-start observation and verify the second plan is zero-change. |
| 2026-09-15 | implemented | Reused one private kubeconfig preparation path for AKS database/application stages, explicitly converted exec authentication to the managed host's selected UAMI MSI mode, and verified current-context authentication before Kubernetes operations. Subscription pinning, no admin credentials, bounded owner-only reads and no interactive fallback are enforced. | Current change; `test_standalone_host.py` (44 passed), including 12 new acquisition/conversion/readback cases, strict mypy and Ruff. Microsoft AKS kubelogin managed-identity documentation confirms the explicit MSI/client-id contract. | Verify the actual attested host's AKS RBAC and cluster connection under an authorized deployment; local command/readback tests do not prove live access or readiness. |
| 2026-09-15 | implemented | Corrected Operator AKS startup when its parsed Kafka identity is the separately declared command identity rather than the primary workload client. The SDK exchanges for that explicit client only; omitted selection retains the primary and unrelated or malformed identities still fail. | Current change; 106 credential/composition tests passed with one optional PDF skip, then the real environment-to-Kafka regression plus credential suite passed 13 cases. Ruff and strict mypy passed. Existing workload Terraform declares the additional federated credential on the Operator ServiceAccount subject. | Verify the exact deployed command identity, federation and minimum downstream roles; no role assignment, token acquisition or full deployment was performed by this local change. |
| 2026-09-15 | in-progress | Added bounded exact-service/region/SKU Linux consumption price selection and a compute-only source cost gate; marked fixed image estimates as unverified policy values. Corrected the common plan-review stage set to accept the AKS runtime and database plans already emitted by the coordinator. | Current change; 35 pricing cases, 25 source-coordinator cases and 32 approval cases passed (92 total); one runner-image review regression passed. The implemented client returned an observed USD hourly rate and exact maximum-node arithmetic without Azure authentication or resource mutation. | Complete combined Foundation/AKS quota and whole-installation/setup pricing, bounded authority validation, source application and five-service assembly, Console, jobs and durable Trial; no full deployment claim. |
| 2026-09-15 | implemented | Bound the existing AKS workload factory's Operator, Executor, Document API and Worker outputs to their required database roles and matching PGOPTIONS without mutating caller inputs or enabling Executor authority cutover. The current Executor renderer previously omitted its mandatory role and could not pass startup configuration validation. | Current change; five focused workload-factory tests passed; the rendered Executor environment passed the real `IsolatedExecutorRuntimeConfig.from_env` parser without network access, with authority cutover and legacy-unbound transition both false. Ruff, format and one-file strict mypy passed. | Retain role-bound DSNs and actual database membership evidence, complete five-service assembly and source application transfer, then verify deployed readiness under approved exact plans. No resource mutation or full rollout is claimed. |
| 2026-09-15 | in-progress | Prevented a partial AKS renderer from defining its own successful rollout: application readback now requires all five baseline service names before querying and verifying their exact Deployment/Pod observations. | Current change; `test_aks_application_readback_requires_complete_baseline` (6 passed) and `test_aks_readiness.py` (18 passed), Ruff and formatting passed. Local inspection found only three generated workloads and missing document identities in `infra/outputs.tf::runtime_identity_bindings`. | Complete independent service identity and role-bound database handoff, document API/Worker configuration and ClamAV before governed activation. This correction blocks false readiness; it does not implement the missing workloads. |
| 2026-09-15 | in-progress | Repaired CI regression omissions in service-owned SDK dependencies, Operator test registration, canonical CLI argument construction, and narrow-terminal digest help. The MSI regression uses the real parser and still stops at signed-kit verification; no source-to-kit fallback or approval rule changed. | Current change; 29 service-suite tests, 2 dependency checks, 1 MSI verification test, and 137 CLI help checks passed (169 total); Ruff and formatting passed. CI run `34919921741`, attempt 1, failed on source `5fd058fdd76e34cf9ac5535ea4586fb55050d53f`; its supply-chain companion passed. | Obtain required CI on the corrected exact source before mutating Azure; regenerate source-bound plans and retain human approval separately. Source application deployment, Console, jobs, and durable Trial remain open. |
| 2026-09-14 | in-progress | Added explicit projected-token credential selection to Core, Operator, isolated Executor, Document API and Document Worker. Preserved service identity binding, attached-MI behavior and token caching; added bounded SDK exchange and transport dependencies to Core and Executor. | Current change; focused mocked token, credential factory, Kafka, service composition and document protection tests pass. SDK credential types are checked without a credential chain or node identity fallback. | Validate real AKS federation and each service's downstream permissions on the exact approved deployment, complete source image transfer and five-service activation, HTTPS/Console, jobs and durable Trial acceptance. |
| 2026-09-14 | in-progress | Corrected capacity parsing after authorized read-only Azure CLI observation returned quota counters as decimal strings. Numeric normalization does not override subscription SKU restrictions. | Current change; 26 preflight cases cover valid strings, invalid representations, aggregate demand and restriction preservation. Read-only candidate checks remained blocked by SKU restrictions; no resource mutation or successful deployment is claimed. | Obtain an available explicitly selected deployment target, revalidate complete capacity and policy, and finish application/Trial acceptance before claiming readiness. |
| 2026-09-14 | in-progress | Added read-only source-mode SKU/zone/host-encryption/architecture and aggregate quota checks, counting both pools at maximum plus simultaneous 33-percent surge. New AKS profiles default to a four-vCPU system SKU while preserving the legacy Container Apps default profile mapping. | Current change; 13 focused preflight cases and runtime-profile/CLI tests pass; Microsoft AKS system-pool requirements require at least four vCPUs. | Retain exact-target observations and add Foundation combined capacity, policy/network/cost checks, apply-time freshness and complete workload-envelope validation. |
| 2026-09-14 | implemented | Replaced the existing-subnet managed NAT mismatch, supplied the required Operator database role, and replaced unsupported `kubectl rollout status --all` with bounded Deployment/Pod JSON verification. | Current change; 121 focused deployment CLI cases, including 18 readiness cases, pass; three infrastructure regressions, Checkov and cluster Terraform validation pass. | Connect the source deployment path, workload identity token consumption, all five baseline services, authenticated Console ingress, end-to-end readiness, and governed live acceptance. |
| 2026-09-14 | implemented | Remediated PR #1000's AKS security scan failure by enabling policy admission and host encryption, increasing node Pod limits, and hardening service filesystem and image-pull settings. Documented resource-local scanner and deployment constraints without a global baseline. | Failed CI `34863327772`, attempt 1, source `6850f3d2d2e51aedc011e7a113910f1d1d9db4d2`; current change; four Python contract cases and three Terraform workload plan cases pass; Checkov 3.2.256 reports zero failures across the three AKS roots. | Complete required CI for the updated PR and retain the separate deployment, host-encryption eligibility, capacity, rotation, and recovery evidence below. |
| 2026-09-15 | in-progress | Added the first implementation ledger. Earlier provenance was not reconstructed. Implemented sealed runtime selection, separate AKS cluster/database/workload states, managed-host routing, typed Kubernetes resources, compact pgvector placement, and signed kit inputs. | Current change paths listed in the scope table; 36 focused coordinator tests, six archive extraction tests, and Terraform validation for the shared, cluster, database, and workload roots pass. | Record complete kit-build evidence, deploy both AKS database profiles online and artifact-offline, close ingress and rollback evidence, and add capacity preflight. |
| 2026-09-15 | implemented | Kept assignment and HIL import grouping inside the existing Operator Service package and runtime. The IAM facade re-exports original adapter and factory objects without wrappers or changes to either renderer. | `current change`; [Operator composition](../../../services/operator-service/src/fdai_operator_service/composition.py); [identity regression](../../../services/operator-service/tests/test_operator_service_full_composition.py); the main implementation session reported 98 Operator composition/full-composition passes, 1 unchanged optional PDF-extra skip, and 39 distinct composition imports. No checks were run for this documentation edit. | Topology, workload identity, readiness, and exact-plan deployment approvals are unchanged. Existing operational criteria below remain open; composition checks do not certify either renderer or a live deployment. |

### Remaining work

- [ ] Replace source-mode local Docker/Buildx and managed-host image-capture paths with a required
  prebuilt signed image manifest, then prove tenant provisioning invokes no image builder.
- [x] Make AKS the new-install CLI default while preserving explicit Container Apps compatibility
  and keeping every apply and platform migration behind its own reviewed plan.
- [ ] Retain an exact approved AKS plan, apply and independent readback proving API Server VNet
  Integration, dedicated node and API-server subnets, and restricted public management access.
- [ ] Apply an exact reviewed substrate plan and retain independent readback for the inventory
  subscription, workspace, cluster, cost, and stage-topic roles plus successful source probes.
- [ ] Apply the AKS Container Insights DCR and association through an exact reviewed cluster plan
  with no public-IP or NAT replacement, then retain fresh `KubePodInventory`, `ContainerLogV2`,
  and successful metrics/log source reads.
- [ ] Roll out the signed Core image with the Key Vault-backed Application Insights binding and
  retain fresh application traces, metrics, and correlated logs from the deployed replicas.
- [ ] Implement the Console network-intent draft, assessment, exact-plan request, approval, apply,
  rollback, and independent readback states without giving the browser or Operator API Azure
  mutation credentials.
- [ ] Verify an eligible current subscription VM through target, identity, route, DNS, TLS and
  backend checks, and add its VNet peering only through the approved network plan.
- [ ] Bind AKS federated token credentials in Core, Operator, Executor, Canary and scheduled jobs;
  prove exact identities without relying on Container Apps identity endpoints.
- [x] Render all five baseline services from their owning startup contracts and verify that missing
  service evidence prevents a ready result. Focused factory, baseline-membership and Terraform
  sidecar tests cover the completed implementation; deployed transport, job and Console evidence
  remains part of operational readiness.
- [x] Replace the legacy three-service AKS input assembly with independent service identity and
  role-bound DSN handoffs, lexical document storage/event settings, and the Worker's required
  ClamAV sidecar. Focused host and native Terraform tests cover the source implementation.
- [ ] Build one complete signed kit and verify that its manifest contains the AKS roots, all three
  provider families, pgvector OCI archive, `kubectl`, and `kubelogin`.
- [ ] Record a successful new-subscription online deployment receipt for AKS with
  `postgres-flex`, including all second-plan and rollout checks.
- [ ] Record a successful new-subscription online deployment receipt for AKS with
  `postgres-aks`, including migration, pgvector extension, persistent-volume, and restart checks.
- [ ] Run both AKS profiles from an artifact-offline kit and record that no provider, image, or
  executable is downloaded after verification.
- [ ] Add subscription quota, regional SKU, availability-zone, and allocatable workload-envelope
  checks, including host-encryption eligibility, that block an infeasible profile before Terraform planning.
- [ ] Demonstrate coordinated `postgres-aks` credential rotation and workload rollout without
  relying on independent DSN expiration or claiming continuity from CSI synchronization alone.
- [ ] Add TLS-terminated operator ingress and prove Console-to-Operator authentication without
  exposing a plaintext public endpoint.
- [x] Add the baseline operational-history schedule to AKS with the same bounded deadline,
  read-only identity, private archive, service DSN and fixed `shadow` posture as Container Apps.
- [ ] Add exact schedule inputs and parity tests for non-baseline optional Container Apps jobs
  before enabling their AKS CronJob counterparts.
- [ ] Demonstrate failed-rollout recovery to the prior healthy workload without repeating an
  ambiguous Terraform apply.
- [ ] Add immutable backup and point-in-time restore for `postgres-aks`, then record a restore
  drill before considering any production database profile.
