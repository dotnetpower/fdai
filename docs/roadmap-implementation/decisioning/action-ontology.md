# Action Ontology implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| ActionType schema and catalog loading | implemented | [`action_type.py`](../../../services/core-control-plane/src/fdai/rule_catalog/schema/action_type.py), [`ontology_action.py`](../../../services/core-control-plane/src/fdai/shared/contracts/models/ontology_action.py), and [`test_action_type_catalog.py`](../../../services/core-control-plane/tests/rule_catalog/test_action_type_catalog.py) | Category, trigger, argument, ceiling, execution-path, probe-reference, canonical required-evidence semantics, and fail-closed catalog constraints are executable. |
| Tier, role, and production downgrade ceilings | implemented | [`ceiling.py`](../../../services/core-control-plane/src/fdai/core/risk_gate/ceiling.py), [`test_ceiling.py`](../../../services/core-control-plane/tests/core/risk_gate/test_ceiling.py), and [`test_approval.py`](../../../services/core-control-plane/tests/core/workflow/test_approval.py) | `prod_downgrade` and environment scope affect deterministic ceilings and human approval requirements. |
| Semantic ActionType, `MutationPlan` V2, and kinetic artifact binding | implemented | [`action_plans.py`](../../../services/core-control-plane/src/fdai/core/ontology_platform/action_plans.py), `core/operational_planning/kinetic_safety.py`, `delivery/kinetic_safety.py`, `delivery/reconciliation_artifacts.py`, `runtime/control_loop.py`, and focused kinetic checks (`119 passed`) | Compilation pins exact declarations and safeguards. The runtime-bound writer revalidates the durable OperationalPlan and proposal lineage, stores only an existing exact V2 plan before every Thor executor, omits raw Action arguments, preserves missing-proposal legacy behavior, and rejects conflicting, orphaned, late, corrupt, or substituted bodies. Deployment composition binds the independently signed effect observer when its complete identity set is present; the governed live receipt remains open. |
| Live blast probe execution | implemented | [`live_probe.py`](../../../services/core-control-plane/src/fdai/core/risk_gate/live_probe.py), [`delivery/live_blast_probe.py`](../../../services/core-control-plane/src/fdai/delivery/live_blast_probe.py), [`delivery/probe_failure_streak.py`](../../../services/core-control-plane/src/fdai/delivery/probe_failure_streak.py), [`runtime/blast_probe.py`](../../../services/core-control-plane/src/fdai/runtime/blast_probe.py), and focused probe tests | Azure observability supplies the read-only signal. One deadline covers signal and streak I/O. The audited CAS streak treats malformed or degraded reads as failures, preserves every stricter `overloaded` result, resets only on a complete result, and lowers the third blind result to `overloaded`. |
| Governance and tool execution coverage | implemented | [`promotion.py`](../../../services/core-control-plane/src/fdai/delivery/promotion.py), [`gitops_pr/governance.py`](../../../services/core-control-plane/src/fdai/delivery/gitops_pr/governance.py), [`retirement.py`](../../../services/core-control-plane/src/fdai/rule_catalog/schema/retirement.py), [`graph_model_promotion.py`](../../../services/core-control-plane/src/fdai/delivery/graph_model_promotion.py), [`exemption_lifecycle.py`](../../../services/core-control-plane/src/fdai/delivery/exemption_lifecycle.py), and [`override_writer.py`](../../../services/core-control-plane/src/fdai/core/risk_gate/override_writer.py) | Promotion requires an authenticated exact distinct-approver attestation and rejects bare decisions; retire and exemption writers publish only canonical shadow review proposals with replayable lifecycle evidence. Expiry reapply binds exact exemption and assignment revisions, requires T0 human approval, and treats broker acceptance as non-terminal. |
| Operator-request trigger closure | in-progress | The schema and loader evidence above plus [Action Ontology Lifecycle](../../roadmap/decisioning/action-ontology-lifecycle.md). | Catalog validity does not prove an authenticated operator request reached judgment, RiskGate, execution, recovery, and audit in one retained production receipt. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-04 | implemented | Hardened live-probe state so one deadline covers every operation, malformed degraded flags fail closed, unavailable streak state is immediately overloaded, and provider or persistence handling can never weaken an overloaded verdict. | `current change`; focused probe and concurrent streak checks; residual hardening rounds 9, 19, 21, 24, and 27. | Retain one governed deployed reading in the resolved ceiling. |
| 2026-09-04 | implemented | Bound the configured live blast probe to a durable audited failure streak. Probe and target identities are stored only as a digest, concurrent increments use bounded CAS, a degraded read cannot reset the streak, and the third consecutive blind result becomes `overloaded`. Existing complete results reset a nonzero streak. | `current change`; focused adapter, concurrent StateStore streak, runtime binding, Azure probe, and control-loop authority checks (`40 passed`), Ruff, and strict mypy. | Retain one governed deployed reading in the resolved ceiling. |
| 2026-08-31 | implemented | Added `governance.reapply-rule-assignment` as a shadow-first, PR-native ActionType. Its scheduled producer carries exact exemption and assignment revisions through EventBus, grants no authority, and requires the ordinary T0 human-approval and execution boundaries. | `current change`; ActionType declaration, expiry command publisher, focused action-catalog and lifecycle checks. | Retain deployment evidence only after a real scheduler and notifier are bound. |
| 2026-08-30 | implemented | Added additive `required_evidence_semantic_refs` to ActionType and cross-validated every ref against the reviewed Property semantic registry. VM start, deallocate, and managed Python execution now cite `runtime.vm.power_state`; matching unresolved EvidenceConflict revisions lower those paths before normal or HIL-resumed executor I/O. Empty legacy fields are omitted from declaration hashing, so unrelated ActionType identities remain stable. | `current change`; focused ActionType, ontology, Property semantic, EvidenceConflict lifecycle, and pre-I/O authority checks. | Add a required semantic ref only with a matching authoritative conflict producer and focused no-authority tests. |
| 2026-08-19 | implemented | Gave the PR executor the path that selected it. `pr_manual` and `pr_native` share `ShadowExecutor` because they differ only in merge policy, so every manual-merge safeguard receipt and audit entry recorded `pr_native`. `execute()` now takes the selected `ExecutionPath`, refuses one it does not serve instead of mislabelling it, and both the intent and terminal audit entries carry it, matching what `direct_api` and `tool_call` already record. No shipped ActionType selects `pr_manual`, so this affects a fork that does. | `current change`; `tests/core`, `tests/pipeline`, `tests/scenarios`, `tests/providers`, `tests/runtime`, and `tests/delivery` passed 7294 focused cases with 4 skips; task-scoped Ruff, format, mypy, and the core import gate passed. | Retain governed enforce-mode promotion evidence before any path leaves shadow. |
| 2026-08-18 | implemented | Carried the dry-run receipt into the terminal audit entry on `direct_api` and `tool_call`, so a closure record proves safeguard 4 without joining back to its intent row. The receipt travels on `ExecutionResult.audit_context`, exactly as the PR path already does, and stays absent when the action was refused before a dry run existed. | `current change`; `tests/core/executor` passed 259 focused cases including three new two-phase receipt tests per path; task-scoped Ruff, format, and mypy passed. | Retain governed enforce-mode promotion evidence before any path leaves shadow. |
| 2026-08-13 | in-progress | Adopted the implementation ledger and separated implemented schema, ceiling, and compiler behavior from incomplete probe and consumer closure; earlier provenance was not reconstructed. | `current change`; the source and focused checks listed in the scope table. | Wire live probes, close the remaining governance writers, and retain operator-request runtime evidence. |
| 2026-08-14 | implemented | Added a separate durable kinetic safety receipt so post-execution reconciliation can resolve only a pre-dispatch exact V2 plan without changing the legacy Action schema or persisting raw Action arguments. | `current change`; `delivery/reconciliation_artifacts.py` and focused adversarial tests passed 15 cases; strict mypy and task-scoped Ruff passed. | Bind a real pre-dispatch writer and verified independent observation source before making the producer operational. |
| 2026-08-14 | implemented | Bound the exact kinetic safety receipt writer before every Core Thor executor. It resolves only a pre-existing correlation-indexed proposal, preserves missing-proposal legacy behavior, and blocks invalid evidence before provider dispatch. | `current change`; `core/operational_planning/kinetic_safety.py`, `delivery/kinetic_safety.py`, `delivery/kinetic_proposal.py`, `runtime/control_loop.py`, and focused kinetic checks passed 115 cases. | Bind a verified independent observation source and retain governed end-to-end closure evidence. |
| 2026-08-14 | implemented | Added dispatch-time cross-record validation so an internally valid proposal cannot substitute its OperationalPlan, Process, selected option, correlation, target, selected ActionType, or plan lineage. | `current change`; `delivery/kinetic_proposal.py`, adversarial substitution tests, and focused kinetic checks passed 119 cases. | Bind a verified independent observation source and retain governed end-to-end closure evidence. |
| 2026-08-14 | in-progress | Integrated `live_probe_ref` into the unified authority pipeline as the pure Axis-E input, with the resolution reason recorded in the resolved ceiling. | `current change`; [`live_probe.py`](../../../services/core-control-plane/src/fdai/core/risk_gate/live_probe.py), [`authority.py`](../../../services/core-control-plane/src/fdai/core/risk_gate/authority.py), [`ceiling.py`](../../../services/core-control-plane/src/fdai/core/risk_gate/ceiling.py), [`test_live_probe.py`](../../../services/core-control-plane/tests/core/risk_gate/test_live_probe.py); focused risk-gate and control-loop authority checks passed 252 cases. | Bind a production probe adapter and its failure-streak source, then retain a governed runtime receipt carrying a real reading. |
| 2026-08-18 | implemented | Added `core/executor/safeguards.py` as the single pre-dispatch safeguard contract. It owns the invariant check, the execution fingerprint, the dry-run receipt, and both lock keys, and `REQUIRED_SAFEGUARDS` declares all seven for every shipped `ExecutionPath`. `direct_api` and `tool_call` now compute a dry-run receipt from their assembled request and persist a pre-effect audit intent in every mode instead of only on enforce or not at all, and `tool_call` holds the idempotency lock in addition to the resource lock. Terminal `tool_call` entries carry `audit_phase` so intent and closure are distinguishable in replay. | `current change`; `tests/core/executor` passed 251 focused cases including the new `test_safeguard_contract.py`; `tests/core` passed 4961 cases; risk-gate, HIL-resume, agent, runtime, and delivery suites passed 3174 cases; task-scoped Ruff, format, and strict mypy passed. | Carry the dry-run receipt into the terminal audit entry on `direct_api` and `tool_call`, and retain governed enforce-mode promotion evidence before any path leaves shadow. |
| 2026-08-27 | implemented | Added the governed promotion dispatcher, replayable governance PR lifecycle publisher, and deployment-supplied live probe adapter contracts. | `current change`; focused delivery tests for promotion, governance PR lifecycle, live probe failure streaks, and O7 batch production passed. | Bind deployment providers and retain authenticated live receipts; no local change grants promotion or merge authority. |
| 2026-08-27 | implemented | Hardened governance delivery against forged review decisions, arbitrary document paths, concurrent PR retries, stale open receipts, and format drift; added a validated retirement loader and active-rule projection. | `current change`; focused governance, GitOps, and governance-catalog tests passed. | Retain authenticated deployment review and merged catalog receipts; no local test claims production state. |
| 2026-08-27 | implemented | Required exact target, revision, and evidence attestation for promotion, rejected non-finite probe deadlines, and aligned governance artifacts with their runtime loaders. | `current change`; focused adversarial promotion, probe, and catalog tests passed. | Retain authenticated deployment review and merged catalog receipts; no local test claims production state. |
| 2026-08-28 | implemented | Hardened `delivery/promotion.py` so a promotion no longer becomes durable on hope. The direct-API executor now snapshots the prior in-memory promotion record and rolls it back through a new `ActionPromotionRegistry.restore` primitive if durable persistence fails, and the governance dispatcher restores a consumed attestation to `pending` if the durable apply that follows fails. A transient persistence failure can therefore no longer leave an unpersisted enforce record or burn the human approval it was attached to. | `current change`; `core/risk_gate/gate.py`; `delivery/promotion.py`; `tests/delivery/test_promotion_executor.py` (`5 passed`); `tests/delivery/test_governance_dispatch.py` (`8 passed`); Ruff, formatter, and strict mypy. | Retain a governed runtime receipt proving a real persistence failure recovers cleanly outside this focused suite. |
| 2026-08-28 | implemented | Closed two gaps the prior restore-based recovery left open. `OperationalPromotionDirectApiExecutor` now holds a per-ActionType `ResourceLockManager` lock (the same in-process pattern the direct-API resource lock already uses) around record, promote, persist, and restore, so two concurrent failed promotions for the same ActionType can no longer interleave and expose an unpersisted enforce record. `StateStorePromotionAttestationStore` replaced the `pending -> consumed` compensating-write model with a leased `pending -> reserved -> consumed` state machine: `consume` reserves with a bounded lease and reclaims an expired reservation on its own, `finalize` is the sole path to `consumed`, and a failed `restore` is now best-effort and never masks the original dispatch failure. An attestation can therefore no longer be permanently stranded by the same store outage that failed the durable apply it was meant to back. | `current change`; `delivery/promotion.py`; `core/executor/lock.py` (reused, unmodified); `tests/delivery/test_promotion_executor.py`; `tests/delivery/test_governance_dispatch.py`; targeted `pytest tests/delivery` (2093 passed); task-scoped Ruff, format, and strict mypy. | Retain a governed runtime receipt proving a real concurrent-promotion race and a real same-store outage both recover cleanly outside this focused suite. |
| 2026-08-28 | implemented | Fixed two review findings left open by the leased reservation model above. First, `consume` now returns a `PromotionReservation` carrying a `fencing_token` (the exact revision its own reserve write produced), and `restore`/`finalize` require that token and refuse to act unless the record's current revision still matches it. Without this, a holder whose lease already expired and was reclaimed by a fresh `consume` call could still `restore` or `finalize` using whatever revision it re-read from the store, unwinding or finalizing the reclaimer's still-active reservation instead of its own expired one. Second, `GovernancePromotionDispatcher.execute` now treats a `finalize` failure as best-effort, matching `restore`'s existing pattern: `finalize` only spends reservation bookkeeping after the guarded executor already durably applied the promotion, so a bookkeeping write failure at that point is a recoverable idempotent success rather than a masking exception, and the stuck `reserved` record self-heals via the same bounded lease. | `current change`; `delivery/promotion.py`; `tests/delivery/test_governance_dispatch.py` (`test_stale_fencing_token_cannot_touch_a_reclaimed_reservation`, `test_finalize_failure_after_durable_apply_is_a_recoverable_success`); focused promotion checks (`17 passed`); task-scoped Ruff, format, and strict mypy. | Retain a governed runtime receipt proving a real reclaimed-lease race and a real post-apply finalize outage both recover cleanly outside this focused suite. |
| 2026-08-28 | implemented | Made the post-apply recovery path safe to retry. The dispatcher keeps a bounded exact-fingerprint receipt cache so same-process retries after `finalize` failure return the confirmed result without invoking the executor, and the operational promotion executor recognizes an exact already-persisted ActionType receipt after restart and returns idempotent success without another registry write. A reused idempotency key with different request content remains blocked. | `current change`; `delivery/promotion.py`; `tests/delivery/test_governance_dispatch.py`; `tests/delivery/test_promotion_executor.py`; focused promotion checks (`17 passed`); Ruff, format, and strict mypy. | Retain a governed runtime receipt proving recovery after a process restart and post-apply finalize outage; no external system was invoked. |
| 2026-08-28 | implemented | Made restart replay monotonic against durable promotion authority. Under the per-ActionType lock, the executor now refreshes the persisted record before replay comparison. An exact receipt returns idempotent success, while an older or otherwise different receipt cannot overwrite newer ENFORCE attribution. | `current change`; `delivery/promotion.py`; `tests/delivery/test_promotion_executor.py`; focused promotion checks (`18 passed`); Ruff and strict mypy. | Retain a governed runtime receipt proving restart replay against a newer persisted promotion; no external system was invoked. |
| 2026-08-28 | implemented | Extended the promotion fence across replicas and authority-store failures. Writer refresh now propagates unavailable or invalid durable authority instead of clearing it, and every promotion persistence carries a monotonic revision committed through atomic compare-and-set. Two replicas that observed the same prior revision cannot both replace its attribution; one succeeds and the other reports a conflict. | `current change`; `delivery/promotion.py`; `delivery/persistence/state_store_action_promotion.py`; focused promotion and persistence checks (`28 passed`); Ruff and strict mypy. | Retain a governed multi-replica runtime receipt; no external system was invoked. |
| 2026-08-28 | implemented | Closed the remaining retry windows around promotion delivery. Initial authority creation now uses atomic create-if-absent with audit, subsequent writes use revision compare-and-set, consumed attestations retain the exact success receipt for restart replay, and restored apply failures raise a typed retryable direct-API error that is audited but deliberately excluded from in-process and durable failed-result deduplication. | `current change`; promotion, persistence, governance-dispatch, and direct-API executor tests (`62 passed`); Ruff and strict mypy. | Retain governed restart and retry receipts from a deployed multi-replica runtime; no external system was invoked. |

### Remaining work

- [x] Persist and resolve the exact V2 plan through the kinetic safety receipt; focused conflict,
  corruption, replay, legacy-missing, argument-redaction, and substituted-body tests pass 15 cases.
- [x] Bind the receipt writer before every Core Thor executor and prove missing proposals preserve
  legacy behavior while invalid or substituted evidence blocks provider dispatch.
- [x] Bind the independently signed effect observation source from one complete deployed identity
  set and prove partial configuration or unavailable exact evidence remains held.
- [x] Integrate `live_probe_ref` into RiskGate evaluation; focused checks prove `quiet`, `active`,
  `overloaded`, unsolicited, unavailable, substituted, opinion-less, degraded, stale, and
  persistently blind outcomes can only preserve or lower autonomy.
- [x] Bind `LiveBlastProbeAdapter` to the configured signal and an audited StateStore failure streak.
- [ ] Retain a governed runtime receipt whose resolved ceiling carries a real probe reading.
- [ ] Retain an authenticated operator-request receipt that binds the exact ActionType and arguments through judgment, RiskGate, execution or typed hold, recovery posture, and audit before marking trigger closure `validated`.
