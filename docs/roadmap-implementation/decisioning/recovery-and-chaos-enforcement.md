# Recovery and Chaos Enforcement implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Impact analysis and envelope compilation | implemented | [`impact_analysis`](../../../services/core-control-plane/src/fdai/core/impact_analysis), [`test_impact_analysis.py`](../../../services/core-control-plane/tests/core/impact_analysis/test_impact_analysis.py) | Bounded traversal, feature calculation, incomplete-evidence refusal, and impact caps have focused coverage. |
| Recovery-plan contracts and state transitions | implemented | [`test_recovery_plan.py`](../../../services/core-control-plane/tests/core/verticals/test_recovery_plan.py), [Ontology contract](../../roadmap/decisioning/recovery-and-chaos-enforcement.md#ontology-contract) | Versioned plans and recovery transitions exist; this does not prove a live recovery outcome. |
| Continuous guard and independent verification | implemented | [`test_impact_analysis.py`](../../../services/core-control-plane/tests/core/impact_analysis/test_impact_analysis.py), [Runtime state machine](../../roadmap/decisioning/recovery-and-chaos-enforcement.md#runtime-state-machine) | Guard and verification mechanics fail closed on stale, incomplete, or over-envelope evidence. |
| Disposable reference scenario substrate | implemented | `infra/scenario-lab/`; `.github/workflows/sre-demo-lab.yml`; `tools/dev-access/scripts/`; protected plan `32726724425`; failed apply `32726945615`; focused scenario-lab and dev-access contracts; Terraform, Trivy, and Checkov validation | The private AKS, VM, MySQL, Azure OpenAI, and evidence target use tagged child resources in an existing protected holding resource group and one disposable state. An opt-in direct P2S path adds peering, gateway transit, private DNS, and minimum operator RBAC without enabling public access. The exact-revision plan is retained, while successful apply, fault injection, and destroy receipts remain open. |
| S1-S14 governed chaos campaign and executor binding | in-progress | [`constitution-traceability.json`](../../../config/constitution-traceability.json), [Delivery status](../../roadmap/decisioning/recovery-and-chaos-enforcement.md#delivery-status) | Scenario taxonomy exists, but constitutional domain coverage remains incomplete and no governed live executor campaign is retained. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-08-24 | implemented | Serialized gateway-transit peering creation after live apply exposed a race between the two directional peerings. Terraform now completes the operator-to-lab peering with `allow_gateway_transit` before creating the lab-to-operator peering with `use_remote_gateways`, matching Azure's remote-gateway prerequisite. | Failed protected applies `32773217323` and `32774040807`; asymmetric peering readback; Microsoft Learn gateway-transit contract; `current change` in `infra/scenario-lab/main.tf` and `tests/integration/infra/test_scenario_lab.py`. | Validate and push the exact revision, converge both peerings to Connected, finish apply, then complete VPN verification, approved sweep, final destroy, and post-run RBAC verification. |
| 2026-08-24 | implemented | Added least-privilege cross-VNet peering authority after protected apply proved that holding-group Contributor cannot write the existing operator VNet. VPN-enabled apply and destroy now validate the configured operator VNet against the active subscription, grant the runner temporary Network Contributor at that exact VNet scope, and revoke only the task-created assignment in the unconditional cleanup path. | Failed protected apply `32770666265`; Azure peering write failure and absent peerings; `current change` in `.github/workflows/sre-demo-lab.yml` and `tests/integration/infra/test_scenario_lab.py`; focused workflow contract checks. | Validate and push the exact revision, finish the no-delete apply, then complete VPN verification, approved sweep, final destroy, and post-run RBAC verification. |
| 2026-08-24 | implemented | Added bounded partial-apply adoption after Azure completed two operator peerings and one MySQL operator DNS link but Terraform exited before recording them. Protected apply and destroy read back only the three deterministic network resources, validate their ARM scope, and import an address only when it is absent from scenario state; raw import output remains runner-local and is shredded. | Failed protected apply `32768750917`; successful Azure peering and link readback; no-op plan `32768446308`; `current change` in `.github/workflows/sre-demo-lab.yml` and `tests/integration/infra/test_scenario_lab.py`. | Validate and push the exact revision, adopt the partial resources, finish the no-delete apply, then complete VPN verification, approved sweep, and final destroy. |
| 2026-08-24 | implemented | Corrected the legacy Private DNS index fallback after the centrally linked runner VNet made phantom-link recreation violate Azure's overlapping-namespace rule. The bounded fallback now reuses the Terraform-owned disposable lab VNet under the stale child name, deletes and waits for that exact child, and retries the same destroy plan; the central runner and P2S links remain untouched. | Failed protected destroy `32761351957`; `current change` in `.github/workflows/sre-demo-lab.yml` and `tests/integration/infra/test_scenario_lab.py`; focused workflow checks. | Validate and push the exact revision, finish legacy duplicate-zone cleanup, then repeat the central-zone plan, apply, VPN verification, approved sweep, and final destroy. |
| 2026-08-24 | implemented | Replaced targeted Terraform link destruction after dependency expansion repeatedly pulled a parent DNS zone into the pre-plan. The protected workflow now extracts only allowlisted VNet-link addresses and ARM ids from remote state, validates every child id, deletes and waits for each exact Azure link, and removes only the independently verified link address from state before building the full destroy plan. | Failed protected destroy `32760003974`; `current change` in `.github/workflows/sre-demo-lab.yml` and `tests/integration/infra/test_scenario_lab.py`; focused workflow contract checks. | Validate and push the exact revision, destroy the partial state, then repeat plan, apply, VPN verification, approved sweep, and final destroy. |
| 2026-08-24 | implemented | Reused the existing central OpenAI Private DNS zone after live apply proved that Azure rejects linking the runner VNet to a second zone with the same namespace. The scenario state now owns only its lab-VNet link, private endpoint, and zone group; the existing runner and P2S links remain centrally owned. | Failed protected apply `32752288798`; unique central-zone and effective-role readback; `current change` in `infra/scenario-lab/`, `.github/workflows/sre-demo-lab.yml`, and focused scenario contracts. | Validate and push the exact revision, destroy the partial duplicate-zone state, then repeat plan, apply, VPN verification, approved sweep, and final destroy. |
| 2026-08-24 | implemented | Corrected the DNS-link destroy allowlist after a partial apply caused Terraform target expansion to include read-only dependency entries. The protected pre-plan now ignores only exact `no-op` actions, still accepts deletion only for VNet-link resources, and counts only actual link deletes. | Failed protected destroy `32753325790`; `current change` in `.github/workflows/sre-demo-lab.yml` and `tests/integration/infra/test_scenario_lab.py`; focused workflow contract checks. | Validate and push the exact revision, destroy the partial state, then bind the scenario private endpoint to the existing central OpenAI DNS zone. |
| 2026-08-24 | implemented | Added one bounded Private DNS reconciliation retry after the serialized link plan still exposed a resource-provider index that reported a runner link to parent deletion while direct child GET and list returned no link. The protected workflow requires Terraform ownership of the exact zone, refuses any visible link, recreates and deletes only the deterministic runner link against the configured private runner VNet, waits for ARM deletion, and retries the same exact destroy plan once. | Failed protected destroy `32744915990`; direct child GET `404`, list count `0`, and zone delete `CannotDeleteResource`; Microsoft Private DNS delete contract; `current change` in `.github/workflows/sre-demo-lab.yml` and `tests/integration/infra/test_scenario_lab.py`. | Validate and push the exact revision, converge the residual DNS-zone state, then repeat apply, VPN verification, approved sweep, and final destroy. |
| 2026-08-24 | implemented | Serialized private DNS link removal ahead of the full scenario destroy after three protected runs exposed Azure's asynchronous child-deletion window. The protected workflow now accepts only delete actions on the declared VNet-link resources, applies that exact child plan, waits for ARM to report every captured link deleted, and then builds the full destroy plan. | Failed protected destroys `32729404757`, `32731946891`, and `32734796056`; `current change` in `.github/workflows/sre-demo-lab.yml` and `tests/integration/infra/test_scenario_lab.py`; focused workflow and Terraform checks. | Validate and push the exact revision, converge the residual DNS-zone state, then repeat apply, VPN verification, approved sweep, and final destroy. |
| 2026-08-24 | implemented | Corrected two live provider constraints exposed by the first protected apply. The disposable MySQL server now uses a deployment-unique private DNS namespace so the runner and operator VNets do not overlap an existing zone, and the Burstable server uses the platform maintenance schedule because Azure no longer accepts a custom window for that tier. | Protected plan `32726724425` reported `52 create / 0 update / 0 delete`; failed apply `32726945615`; `current change` in `infra/scenario-lab/data-services.tf` and `tests/integration/infra/test_scenario_lab.py`; focused test and Terraform format and validation passed. | Destroy the partial failed-apply state, then repeat the exact-revision plan, protected apply, VPN verification, approved sweep, and final destroy. |
| 2026-08-24 | implemented | Corrected the disposable deployment boundary after live preflight found that the private runner could not create resource groups, the original VNet CIDR overlapped an existing network, a non-scenario Key Vault prevented complete prompt cleanup, raw workflow logs were not a safe evidence surface, and VM readiness was not verified. The root now uses a protected holding group, a non-overlapping CIDR, no persistent secret store, temporary scoped authority, runner-local raw logs, repository-safe outcome projection, strict ten-scenario verification, and cloud-init readiness. | `current change`; scenario-lab Terraform validation; scenario and lifecycle contracts `8 passed`; workflow and CI contracts `47 passed`; focused planner behavior `524 passed` | Run the exact-revision protected plan, apply, VPN verification, approved sweep, independent result review, and destroy; retain the protected run and repository-safe summary references. |
| 2026-08-24 | implemented | Added opt-in workstation testing through the existing P2S VPN and hardened the disposable target with Key Vault purge protection, secret expiry, MySQL TLS, and subnet and NIC NSGs. Scanner exceptions are limited to MySQL cross-resource false positives and documented lab tradeoffs. | `current change`; scenario-lab and dev-access contracts `13 passed`; Terraform format and validation; Ruff and ShellCheck; scenario-scoped Trivy and Checkov `0 detected issues` | Run the exact-revision protected plan, regenerate the VPN profile, verify workstation route and DNS, then retain apply, fault, recovery, cleanup, and destroy evidence. |
| 2026-08-24 | implemented | Added an expiry-tagged scenario-lab Terraform root and protected plan, apply, approved-sweep, and destroy workflow. It remains separate from scenario promotion and the governed executor contract. | `current change`; scenario-lab Terraform, runner scripts, workflow, focused contracts `5 passed`, Terraform format and validation, ShellCheck, Ruff, and CI contracts | Run a protected plan from an exact committed revision, then retain apply, readiness, approved fault, independent recovery, cleanup, and destroy evidence. |
| 2026-08-14 | in-progress | Adopted the implementation ledger without reconstructing earlier provenance and separated tested mechanics from operational enforcement evidence. | `current change`; current source, focused tests, and constitutional traceability listed in the scope table. | Bind the governed executor and complete the frozen recovery and chaos campaign. |

### Remaining work

- [ ] Bind an injected `GovernedChaosExecutor` through deployment composition and prove startup
  refuses enforcement when the binding or required authority is absent.
- [ ] Apply and destroy the disposable reference substrate through the protected workflow, then
  retain exact-revision readiness, approved fault, independent recovery, and cleanup receipts.
- [ ] Execute the frozen S1-S14 campaign with approved impact envelopes, continuous stop guards,
  independent recovery verification, and retained replayable receipts.
- [ ] Close the missing constitutional scenario dimensions for recovery and Chaos Engineering before
  claiming domain validation or enforce readiness.
