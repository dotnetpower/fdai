# Operator Console Module Map and Boundaries implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| CLI transport and presentation ownership | implemented | `cli/src/operator-api-session.ts`; `cli/src/data/operator-api.ts`; `cli/src/cockpit*.ts`; `cli/src/terminal-capabilities.ts`; `tools/console.py`; focused TypeScript and launcher checks | Session bootstrap, read transport, terminal state, rendering, and capability detection have separate owners. The launcher targets `fdai_operator_service.main:create_app`; no CLI module resolves Azure identity, imports another service implementation, persists a bearer, or gains execution authority. |
| Current-state activity projection boundary | implemented | `fdai_operator_service/activity_projection.py`; `test_activity_projection.py`; focused persistence and projection tests (`6 passed`) | Durable rows require the hashed correlation reference, use the same activity id as live frames, retain only the newest duplicate, and keep `execution_authority=false`. |
| Governed semantic receipt presentation | implemented | `console/src/deck/backend-normalizers.ts`; `backend-stream.ts`; `transcript-store.ts`; `conversation-trajectory-view.tsx`; Deck source-contract tests; `console-routes.spec.ts`; focused Console tests | The Console parses terminal semantic receipts fail closed, persists and replays the exact typed fields, and renders route, unavailable reason, assurance digests, evidence references, and no-execution-authority state. Source-contract checks accept shared style classes and reviewed chart title components without weakening attachment ordering or review-tool semantics. The authenticated runner binds the terminal receipt to the caller request UUID and reads a cloned response stream so application consumption remains unchanged. Authenticated browser evidence remains pending. |
| Continuous question-space and ontology-structure presentation | implemented | [Continuous Question Space](../../roadmap/interfaces/continuous-question-space.md); `console/tests/live-e2e/ontology-query-assurance.ts`; `console/src/routes/ontology.types.ts`; `console/src/routes/ontology-links.tsx`; focused Console tests | The Console harness owns the fixed bilingual 100-case taxonomy and strict v2 22-cell oracle. The ontology workbench decodes LinkType roles, semantic traits, and independent limitation families fail closed, presents them read-only, and exposes no execution controls. Together they verify exact plan capabilities, transport identity, complete evidence for answered turns, and zero unauthorized execution without becoming runtime infrastructure. |
| Browser-evidence metadata panel boundary | implemented | `console/src/routes/browser-evidence.tsx`; `console/src/panels.tsx`; focused decoder, panel, and router checks (`26 passed`) | The existing Evidence navigation route now consumes only the exact payload-free Operator envelope. It rejects controls and captured or structured payloads, renders no mutation commands, and keeps authenticated deployed read evidence as a separate runtime gate. |
| Console route loading boundary | implemented | `console/src/panels.tsx`; `console/scripts/check-entry-bundle.mjs`; panel and router checks (`26 passed`); Console typecheck and production build | Named route exports share one typed lazy adapter, shared route modules reuse one loader, and the production entry remains below its unchanged raw and gzip budgets. |
| Shared semantic row projection | implemented | `families/conversation/presentation_rows.py`; v1 and v2 presentation modules; focused Operator presentation checks (`82 passed`) | One bounded pure projection keeps direct scalars and lifts only `name`, `type`, `status`, and `location` through at most two nested verified property bags. When readable facts exist, the default table omits opaque `id` and `object_type` columns; an identity-only result still renders those fields. Exact untouched rows remain in technical details. |
| Semantic stream PostgreSQL cancellation | implemented | `postgres_family_store.py`; `test_postgres_family_store_cancellation.py`; focused semantic bridge checks (`76 passed`); Ruff and strict mypy | An AnyIO stream cancellation finishes bounded psycopg query cancellation and connection closure before propagating. It no longer races transaction rollback, and it changes no HTTP contract, database ownership, or execution authority. |
| Workflow Builder control-step authoring | implemented | `console/src/routes/workflow-builder.{model,editor,session,structure,viz}.ts`; `console/tests/e2e/workflow-builder-control-steps.spec.ts`; focused Console checks | The route keeps typed authoring, preview, tab recovery, and catalog-backed structural checks in separate modules. All five runtime control-step kinds survive clone, reorder, validation, and restore. Validation remains server-authoritative, and saved private drafts remain shadow and non-runnable. |
| Process transition projection and request boundary | implemented | `runtime_projection_reader.py`; `process_transition_projection.py`; `process_approval_projection.py`; `process_retry_admission.py`; `family_adapters.py`; `console/src/routes/processes.control.ts`; `console/src/routes/process-control-panel.tsx`; `console/src/routes/processes.transitions.ts`; focused Operator and Console checks | Runtime reads filter immutable requester evidence by authenticated principal, recheck snapshot revision after journal reads, and join the pinned reviewed Workflow plus durable Var approval state. The workflow adapter prechecks revision, role, state, and effect-free retry evidence in the same transaction that persists an inert proposal. Runtime authority is unchanged. |
| Principal-scoped operational evidence response | implemented | `families/conversation/semantic_turn_runtime.py`; `semantic_turn_presentation.py`; focused Core producer and Operator consumer checks | The existing authenticated semantic response retains and presents only a revalidated admitted bundle with exact Context metadata. Wrong identity, purpose, release, cutoff, graph coverage, citation, contradiction, or budget evidence remains unavailable, and both authority flags remain false. |
| Catalog and durable operational panel sources | implemented | `materialize-authoritative-catalogs.py`; `family_adapters.py`; `runtime_projection_reader.py`; `conversation_assurance_reader.py`; Operator read-grant migrations; focused materializer, adapter, composition, and browser checks | Azure WAF, MCSB, capability, promotion-gate, onboarding-baseline, workflow-app, Process, automation-blueprint, monitoring-scope, autonomy, detection-readiness, delivery, forecast, memory, skill-source, configuration-baseline, and principal-scoped conversation-assurance surfaces now read repository or PostgreSQL authority. Assurance detail returns bounded question and answer text only after exact principal, conversation, and turn correlation. Missing measurements remain explicit and no read grants execution eligibility. |
| Settings and component presentation system | implemented | `ui/calm-slate-{tokens,primitives}.css`; `console/src/routes/settings*`; `mocks/ui/components.html`; `mocks/ui/assets/component-registry.json`; focused Console and mock contracts | Settings and design mocks share compact desktop and touch control roles, explicit preference and policy states, responsive tables, and a registry-backed component catalog. Presentation changes no persistence, authorization, evidence, or execution authority. |
| Console web notification control | implemented | [Console Web Notifications](../../roadmap/interfaces/console-web-notifications.md); [implementation ledger](console-web-notifications.md) | Detailed delivery state and remaining work moved to the focused owner. This module map retains only component ownership and dependency direction. |
| Header account identity presentation | implemented | `console/src/components/account-menu.tsx`; `console/src/auth.ts`; focused account tests (`11 passed`), Console typecheck, production build, and synthetic responsive browser measurements | The account panel presents MSAL identity and server-verified FDAI roles. Same-tenant account selection returns through normal IAM startup checks and grants no directory-switching or execution authority. |
| Assurance Twin responsive evidence presentation | implemented | `console/src/routes/assurance-twin.tsx`; `console/src/styles.css`; `console/src/routes/assurance-twin.test.ts`; `console/tests/e2e/assurance-twin-responsive.spec.ts`; focused Console tests (`20 passed`), full Console tests (`2881 passed`), typecheck, production build, and synthetic three-viewport browser measurements | Opaque provenance and withheld identities wrap inside the primary content, table overflow remains in its scroll container, and mobile back and review-detail links expose 44-pixel touch targets. The change adds no evidence writer, approval, or execution authority. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-14 | implemented | Consumed acknowledgement fragments on both mount and `hashchange`, covering same-document navigation for an exact open Incident URL. | `current change`; focused notification checks and browser same-document fragment validation. | No residual exact-client acknowledgement gap remains. |
| 2026-09-14 | implemented | Replaced unacknowledged service-worker messaging with one fragment navigation path, located the originating principal ledger by its opaque claim token, synchronized same-document delivery status, and separated persisted selection from receiver health. | `current change`; focused service-worker, cross-principal, ambiguous-token, and delivery-event tests; Console typecheck and build. | No residual Medium finding from the pre-push code review remains. |
| 2026-09-14 | implemented | Added the canonical authenticated-principal and browser-profile Console web selection row to Settings > Integrations, with the header as a synchronized shortcut and organization routes left read-only. | `current change`; focused notification, Settings, and catalog tests; Console typecheck; authenticated browser selection and responsive geometry checks. | Add another selectable personal channel only after Operator projects a verified active principal endpoint and its delivery path consumes this selection. |
| 2026-09-14 | implemented | Bounded service-worker registration and readiness waits so the Console web control cannot remain indefinitely in its enabling state. | `current change`; focused fake-timer deadline test and shared-session browser reproduction. | Investigate browser-specific registration failures separately; timeout now keeps the UI recoverable. |
| 2026-09-14 | implemented | Added an equivalent compact acknowledged label at narrow widths while retaining the full accessible action and status. | `current change`; paired catalog tests and 320 px Korean 200% text browser measurement. | No known notification-state clipping remains in the scoped header control. |
| 2026-09-14 | implemented | Contained acknowledgement-token generation failures inside the claim result so an entropy error cannot escape the live-stream callback. | `current change`; focused throwing-token-factory test. | No known token-generation exception escapes this browser boundary. |
| 2026-09-14 | implemented | Synchronized Console web selection changes and storage clears across tabs for the current browser principal. | `current change`; focused preference-key isolation tests. | No known tab can retain receiver eligibility after another tab deselects the channel. |
| 2026-09-14 | implemented | Propagated Web Locks leader-acquisition failure to the Console web retry state while ignoring normal cleanup aborts. | `current change`; focused browser stream leadership tests. | No known ready-without-leader state remains after lock acquisition failure. |
| 2026-09-14 | implemented | Added Web Locks to the Console web capability gate so browsers that cannot elect one stream leader report the channel unavailable. | `current change`; focused browser capability tests. | No known ready-without-receiver state remains for missing browser APIs. |
| 2026-09-14 | implemented | Required the current claim token for display completion and failed-send release, replacing any expired same-tag generation atomically. | `current change`; focused stale-callback generation test. | No known tag-only stale callback can mutate a replacement claim. |
| 2026-09-14 | implemented | Moved new-window acknowledgement from query fields to a closed transient fragment so claim values do not enter HTTP requests or referrers. | `current change`; focused fragment decoder and service-worker navigation tests. | No known query-string acknowledgement transport remains. |
| 2026-09-14 | implemented | Made a token-bound notification click atomically establish display and acknowledgement when it races ahead of the display completion callback. | `current change`; focused out-of-order receipt transition test. | No known click-before-display acknowledgement loss remains. |
| 2026-09-14 | implemented | Bound every new Console web acknowledgement to an unpredictable 128-bit per-claim token while retaining tokenless legacy rows for deduplication only. | `current change`; focused ledger, message-decoder, and service-worker tests. | No known tag-only acknowledgement path remains. |
| 2026-09-14 | implemented | Reused one bell component and matched disabled-button semantics in the lazy Console web notification fallback. | `current change`; Console production build and responsive browser geometry. | No known loading-state layout shift remains in the notification control. |
| 2026-09-14 | implemented | Corrected the missing-source notification regression fixture to omit the optional field, matching both the wire shape and strict TypeScript semantics. | `current change`; focused browser notification tests and Console typecheck. | No residual strict-optional mismatch remains in this fixture. |
| 2026-09-14 | implemented | Separated exact-window focus failure from acknowledgement-message failure and reused the focused window for the acknowledgement fallback. | `current change`; focused service-worker failure-path test. | No residual duplicate-window path remains for acknowledgement-message failure. |
| 2026-09-14 | implemented | Made explicit Console web selection and deselection fail closed when principal-scoped preference persistence is unavailable. | `current change`; focused preference-storage failure tests. | No residual success-shaped preference write failure remains. |
| 2026-09-14 | implemented | Synchronized Console web receipt status across tabs while filtering storage events to the current principal's ledger key. | `current change`; focused principal-key storage-event tests. | No residual cross-tab status synchronization work for this local ledger. |
| 2026-09-14 | implemented | Derived the header status from the latest retained Console web delivery rather than the receipt that most recently changed. | `current change`; focused multiple-delivery ordering test. | No residual stale-acknowledgement status overwrite remains. |
| 2026-09-14 | implemented | Required trusted browser message events for Console web acknowledgement and moved acknowledgement time ownership to the receiving page. | `current change`; focused acknowledgement decoder and service-worker tests. | No residual message-timestamp trust remains in this client-local receipt path. |
| 2026-09-14 | implemented | Preserved the legacy browser-ledger timestamp alias on every new receipt write so old and new Console tabs deduplicate during rolling updates. | `current change`; focused browser notification compatibility test. | Remove the alias only after a separately versioned browser-storage migration proves no supported old tab depends on it. |
| 2026-09-14 | implemented | Separated the five-minute duplicate window from seven-day bounded receipt retention so delayed notification clicks remain acknowledgeable. | `current change`; focused browser notification tests. | No residual delayed-click retention work for the client-local channel. |
| 2026-09-14 | implemented | Rejected replay, synthetic-development, and unknown-source stage frames at the Console web notification boundary. | `current change`; focused browser notification tests. | No residual source-admission work for this bounded channel. |
| 2026-09-14 | implemented | Added explicit Console web notification selection and separate browser-local display and click acknowledgement states without adding a service, server writer, or authority path. | `current change`; focused notification and service-worker tests, catalog checks, Console typecheck, production build, and responsive browser measurements. | Retain human-confirmed operating-system notification evidence before claiming live desktop validation; closed-browser Web Push remains separately designed work. |
| 2026-09-13 | implemented | Classified the extracted CLI activity reducer as reviewed structured-event presentation in the semantic-scan baseline, without changing the detector or allowing a natural-language router. | PR #899 CI run `34740647203`, attempt 1; `current change`; 14 semantic/workspace tests and 20 CLI runtime tests passed, including question-independent activity reduction; CLI typecheck passed. | Retain exact-head CI after the baseline correction; runtime authority is unchanged. |
| 2026-09-11 | implemented | Split loopback session bootstrap and terminal capability detection into focused CLI owners, threaded authorization through the existing read client and SSE boundary, fixed the Operator-owned HIL query grouping, and migrated the convenience launcher from the removed monolith path to the independent Operator Service. | `current change`; CLI typecheck and 77 tests; focused HIL/KPI tests passed 4 cases; launcher pytest 5 cases, Ruff, and strict mypy; `check-independent-services` passed; the complete live protected read completed and stopped its owned service. | No remaining ownership or retired-path work for this bounded CLI slice. |
| 2026-09-13 | implemented | Exposed the Sample outcome evidence boundary as an accessible note and added desktop and mobile overflow assertions. | `current change`; `operating-outcomes.tsx`; focused two-project Playwright, Console typecheck, and localization tests passed. | Retain authenticated deployed Live rendering separately from Sample presentation evidence. |
| 2026-09-13 | implemented | Added an explicit Sample-to-Live evidence boundary on Operating Outcomes so fixture metrics cannot imply that linked audit or incident records substantiate them. | `current change`; `operating-outcomes.tsx`; bilingual analytics catalogs; focused localization and Playwright checks. | Retain authenticated deployed Live rendering separately from Sample presentation evidence. |
| 2026-09-09 | implemented | Added a rendered mobile geometry oracle for maximum-length withheld Assurance Twin identities and contained gap lists within the primary viewport. | `current change`; the focused mobile Playwright scenario, Console typecheck, and production build passed. | Retain an authenticated Browser Entra rendering after a trusted producer exists. |
| 2026-09-09 | implemented | Bound every Assurance Twin posture row's claimed scope to the exact durable key selected by the Operator reader and withheld duplicate scope claims even when their digests match. | `current change`; focused Operator projection and runtime-reader checks passed 53 tests, and Ruff passed. | Preserve the same durable-key binding in future posture schema or storage revisions. |
| 2026-09-09 | implemented | Contained long Assurance Twin provenance and table content on narrow screens and enlarged the two primary mobile navigation links without changing desktop density. | `current change`; targeted tests passed 20 cases, full Console tests passed 2881 cases, Console typecheck and production build passed, and synthetic browser checks passed at 1440x900, 993x641, and 390x844. | Retain governed live evidence separately after a trusted Assurance Twin producer is bound. |
| 2026-09-08 | implemented | Removed the preliminary JSON encode/decode cycle from multiplexed non-request Kafka publication while preserving request codec validation. | `current change`; focused semantic Kafka adapter checks passed 25 tests. | Retain transport CPU measurements separately. |
| 2026-09-08 | implemented | Replaced linear repeated encoding of oversized browser transcripts with a logarithmic search for the longest suffix under the existing byte ceiling. | `current change`; focused transcript checks passed 24 tests, and Console typecheck passed. | Retain browser main-thread timing as separate evidence. |
| 2026-09-08 | implemented | Applied missing-assessment hydration to the initially restored session and preserved the same force reason on explicit retry. | `current change`; focused conversation, transcript, and restored-session checks passed 72 tests, and Console typecheck passed. | Retain authenticated initial-load recovery evidence separately. |
| 2026-09-08 | implemented | Made the unavailable assessment action retry both list and exact retained-detail reads with a new detail generation. | `current change`; focused route checks passed 10 tests, and Console typecheck passed. | Retain authenticated recovery evidence separately. |
| 2026-09-08 | implemented | Mapped a scoped retained-assessment `404` to the explicit unavailable state and refresh action. | `current change`; focused route checks passed 10 tests, and Console typecheck passed. | Retain authenticated missing-detail evidence separately. |
| 2026-09-08 | implemented | Carried the missing-assessment recovery reason through the hydrator's internal eligibility check so forced legacy hydration cannot exit immediately. | `current change`; focused conversation, transcript, and restored-session checks passed 71 tests, and Console typecheck passed. | Retain authenticated legacy-cache recovery evidence separately. |
| 2026-09-08 | implemented | Corrected assessment-detail digest verification to the ledger's raw SHA-256 format and bounded request/result lookup to indexed semantic key prefixes. | `current change`; focused assurance reader checks passed 5 tests. | Retain authenticated detail evidence separately. |
| 2026-09-08 | implemented | Made legacy cached Pantheon answers without an assessment identity trigger one durable hydration so existing conversations can acquire the authoritative review target. | `current change`; focused conversation, transcript, and restored-session checks passed 71 tests, and Console typecheck passed. | Retain authenticated legacy-cache recovery evidence separately. |
| 2026-09-08 | implemented | Preserved retained assessment deep links beyond the bounded list window by querying the exact principal-scoped detail endpoint. | `current change`; focused route checks passed 9 tests, and Console typecheck passed. | Retain authenticated old-assessment evidence separately. |
| 2026-09-08 | implemented | Added monotonic request tracking to Conversation assurance list refreshes so out-of-order responses cannot restore stale data. | `current change`; focused route checks passed 9 tests, and Console typecheck passed. | Retain authenticated repeated-refresh evidence separately. |
| 2026-09-08 | implemented | Preserved the validated assessment identity in bounded browser-local transcript serialization and parsing so answer review remains correlated after session restoration. | `current change`; focused transcript and session checks passed 42 tests, and Console typecheck passed. | Retain authenticated reload evidence separately. |
| 2026-09-08 | implemented | Included primary and secondary evidence-posture warnings in the grounded-source button's accessible name. | `current change`; focused grounded-reply checks passed 23 tests. | Retain authenticated assistive-technology validation separately. |
| 2026-09-08 | implemented | Corrected the assessment-detail binding to verify stored question and answer digests and to select the assessed Pantheon answer before a generic semantic fallback. | `current change`; focused assurance reader checks passed 5 tests. | Retain authenticated detail evidence separately. |
| 2026-09-08 | implemented | Added bounded principal-scoped question and answer detail to Conversation assurance by joining the exact stored assessment, semantic request, and terminal result identities. | `current change`; focused assurance reader and Operator family checks passed 49 tests. | Validate an authenticated live assessment detail separately. |
| 2026-09-05 | implemented | Consolidated named Console lazy-route adapters after duplicated wrappers exceeded the protected publish gzip budget. Shared route modules retain one loader, every route remains lazy, and the limits remain unchanged. | `current change`; panel and router checks (`26 passed`); Console typecheck; production build at 480435 raw and 149522 gzip bytes. | No additional work remains for this bundle regression. |
| 2026-09-01 | implemented | Added the accessible header account panel, verified-role presentation, IAM navigation, sign-out, and same-tenant Entra account selection. Hardened duplicate interactive actions, entry-bundle growth, and mobile navigation overlap. | `current change`; focused Console tests (`11 passed`), typecheck, production build, and desktop, constrained, and mobile synthetic browser scenarios passed. | Retain an authenticated Browser Entra receipt for the account-picker redirect and selected-account IAM revalidation. |
| 2026-09-01 | implemented | Unified the Console settings routes and static mocks around shared controls, explicit save and policy states, responsive data layouts, and a registry-backed component gallery with bounded views and accessibility previews. | `current change`; 35 focused Console settings tests, 5 focused component contracts, settings mock contract, Console typecheck and production build, and desktop/mobile browser measurements passed. | Retain an authenticated visual receipt for all settings routes after the standard local Operator API stack is healthy. |
| 2026-08-31 | implemented | Extended the Workflow Builder module boundary from action-only rows to lossless `WAIT` and `APPROVAL` authoring without adding browser or Operator API execution authority. | `current change`; 72 focused Vitest checks, 12 server contract checks, Console typecheck and production build, and synthetic desktop, constrained, and mobile Playwright checks passed. | Add `DECISION`, `PARALLEL`, and `GATE` structural authoring in #396, then principal-scoped transitions in #397. |
| 2026-08-31 | implemented | Added a pure structural validator and lossless editors for `DECISION`, `PARALLEL`, and `GATE`. It reuses the reviewed workflow catalog for gate references and preserves the runtime's fixed all-branches join instead of inventing a second join contract. | `current change`; 81 focused Vitest checks, Console typecheck and production build, and synthetic desktop, constrained, and mobile Playwright checks passed. | Add principal-scoped authoritative Process transitions in #397. |
| 2026-08-31 | implemented | Added principal-scoped authoritative Process state and guarded transition proposal admission. The Console displays step requirements and request acceptance without claiming runtime advancement or operational success. | `current change`; 67 focused backend and runtime checks, 27 focused Console checks, strict Python and TypeScript checks, production build, and synthetic three-viewport Playwright checks passed. | Retain a governed authenticated proposal-consumption and Process-advancement receipt. |
| 2026-08-23 | implemented | Removed the PostgreSQL rollback race observed when an SSE disconnect cancelled a semantic replay query. Single-statement family queries retain PostgreSQL statement atomicity in autocommit mode, use a session statement timeout, and shield only bounded cancellation and close cleanup before re-raising the original cancellation. | `current change`; exact AnyIO plus `pg_sleep` reproduction failed before the fix and passed afterward without psycopg warnings; focused semantic bridge checks passed 76 cases; Ruff and strict mypy passed. | No additional work remains for this cancellation defect. |
| 2026-08-19 | implemented | Extended the read-only assurance harness with the strict v2 declaration, release/evidence, inventory-impact, and Rule-state cells while preserving the fixed 50/50 locale split. | [Issue #233](https://github.com/dotnetpower/fdai/issues/233); [Continuous Question Space](../../roadmap/interfaces/continuous-question-space.md); 100 focused Console tests. | Retain an exact-source authenticated strict-v2 artifact before seeded certification. |
| 2026-08-20 | implemented | Unified readable semantic-row projection across legacy and v2 presentation. The v2 analyzer previously discarded every nested mapping, so a verified Resource row rendered only `id` and `object_type` even though its exact evidence carried name, type, and location. | `current change`; [Issue #241](https://github.com/dotnetpower/fdai/issues/241); focused presentation and semantic bridge checks passed 94 cases; Ruff, formatting, and strict mypy passed. | Retain authenticated three-viewport evidence after restarting the Operator API on the committed source. |
| 2026-08-20 | implemented | Corrected the default verified-row hierarchy after authenticated review showed that visible `id` and `object_type` columns compressed the operator-facing name, type, and location fields. The shared projection now omits those opaque identity columns when readable facts exist and retains them as a fallback for identity-only results; exact rows remain unchanged in technical details. | `current change`; `presentation_rows.py`; focused Operator presentation checks passed 82 cases, Command Deck visual checks passed 19 cases, and Console typecheck and production build passed. | Retain authenticated desktop, constrained-desktop, and mobile evidence after restarting the Operator API on this source. |
| 2026-08-13 | implemented | Adopted the implementation ledger without reconstructing earlier provenance and recorded the durable/live current-state projection identity. | Current source plus `test_activity_projection.py`; the focused projection and persistence suites passed. | Reduce the report-only dependency debt through the reviewed migration families below. |
| 2026-08-13 | in-progress | Added exact typed semantic receipt parsing, stream capture, durable replay, presentation, and authenticated evidence runners. | `current change`; focused Console tests and typecheck pass. | Run the governed request-to-Console and bilingual randomized assurance browser paths and retain both passing records. |
| 2026-08-13 | implemented | Bound authenticated terminal-receipt capture to the caller request UUID and captured the SSE body from `Response.clone()` without consuming the application's original stream. | `current change`; `console-routes.spec.ts`; Console typecheck and Playwright discovery pass. | Execute the authenticated governed runner and retain its passing record before randomized assurance. |
| 2026-08-13 | implemented | Aligned the authenticated governed runner with the current accessible `Run record` trajectory summary so it can reveal nested execution and technical receipt details. | `current change`; `console-routes.spec.ts`; Console typecheck and exact Playwright discovery pass. | Execute the authenticated governed runner and retain its passing record before randomized assurance. |
| 2026-08-15 | implemented | Aligned the registered Browser evidence route with the payload-free Operator metadata contract and strict localized inspection states. | `current change`; `console/src/routes/browser-evidence.tsx`; focused checks `26 passed`; Console typecheck, build, catalog parity, and desktop/mobile fixture rendering passed. | Retain one authenticated deployed read receipt against the migrated Operator role. |
| 2026-08-31 | implemented | Extended the existing semantic response with admitted operational evidence and receipt-verified Context metadata. Operator revalidates the no-authority contract before persistence and returns the exact projection in the terminal response. | [Issue #365](https://github.com/dotnetpower/fdai/issues/365); `current change`; focused semantic bridge and Core projection checks. | Retain the optional exact-revision governed local receipt; deployed authenticated evidence remains separate. |
| 2026-08-27 | implemented | Replaced deployment-level unavailable placeholders with authoritative repository and durable PostgreSQL projections across the catalog and operational Console surfaces. The Operator role receives bounded read-only access to the required evidence tables. | `current change`; focused Operator tests, Ruff, strict mypy, catalog materialization, local migration, and authenticated Browser Entra inspection. | Provisioning progress and live onboarding observation still require their external relays; Python task authoring still requires its governed provider. |


### Remaining work

- [x] Keep CLI authentication bootstrap, HTTP/SSE transport, terminal capability detection, and
	presentation in separate bounded modules; focused TypeScript, Python, and service-boundary checks
	pass and the removed Operator API module path is absent from the CLI and launcher.
- [ ] Move each report-only reverse dependency behind its reviewed neutral contract or provider boundary, then reduce the matching `.check-operator-api-boundaries.debt` budget before making that direction enforceable.
- [ ] Retain passing authenticated request-to-Console and bilingual randomized assurance evidence before declaring semantic receipt presentation ready.
