# Recorded Resource State implementation ledger

This ledger tracks the shared read-only state contract used by Dashboard v2 and Ontology Instances.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Provider-native type coverage detail | validated | active inventory `provider_scope_coverage`; Operator fail-closed decoder; strict Ontology Instances model; bilingual Inspector labels; focused Operator and Console checks; refreshed local generation | All 955 provider objects in the current local generation are mapped, with zero unclassified Resources and zero missing subscription or Resource Group scope. Coverage remains separate from Resource state, carries no raw provider object payload, and grants no catalog or execution authority. Bounded unknown change history and live latency remain outside this state contract. |
| Shared recorded-state API and bounded paging | implemented | `services/operator-service/src/fdai_operator_service/families/operations/instance_states.py`; `recorded_state.py`; focused state, page, instance, route, and composition tests (`187 passed`) | Immutable source reads, independent recorded facts and same-generation continuation; no provider reads, new writer, or execution authority. |
| Common Console decoder and state presentation | validated | `console/src/recorded-resource-state.ts`; `console/src/components/recorded-state-facts.tsx`; `console/src/routes/dashboard-v2.loading.ts`; focused Console tests (`150 passed`) and native Dashboard E2E (`10 passed`); authenticated operator-screen validation | Both screens use one fact contract. Source values remain visible with unknown/stale metadata rather than being recast as health. Typecheck and build passed. |
| Inventory and ontology generation fence | validated | `services/operator-service/src/fdai_operator_service/families/operations/instance_states.py`; `postgres_family_store.py`; local materializer; focused state-page, operations-family, and materialization tests; refreshed local generation | Reads reject an inventory/ontology generation or release mismatch before returning mixed records. The current active snapshot, ontology status, and manifest name the same generation. |
| State-path coverage, applicability, and legacy evidence qualification | validated | `recorded_state.py`; `services/core-control-plane/src/fdai/core/ontology_platform/inventory_projection.py`; `services/core-control-plane/src/fdai/delivery/azure/arg_projection.py`; focused Operator and Core projection tests; bounded live Azure Resource Graph collection | All canonical ResourceType values have reviewed operational and availability missing-state outcomes. Resource-specific VM run-command, registered-domain, search-service, disk, snapshot-access, and private-DNS-link states retain exact values. Immutable Resource and snapshot timestamps qualify legacy values without inventing effective time. |
| Unified Resource Health current state and transition history | validated | `resource_health_inventory.py`; `inventory_sync.py`; `inventory_topology_history.py`; `fdai_service_contracts.recorded_resource_state`; PostgreSQL generation fencing; focused backend and Console tests; refreshed local generation; prior authenticated standard 5273 Browser Entra validation of the original matrix | The reviewed 33-type matrix observed 155 of 157 current targets and preserved two explicit `resource_health_not_modeled` reasons. Present Prometheus rule groups, Managed Grafana, Bastion hosts, and search services all carry exact availability facts. Bounded collection marks only the stable overflow remainder unavailable instead of abandoning the generation. Application Insights remains not applicable, and no availability value is inferred from provisioning. |
| Static Web App default-environment operational state | validated | `static_web_app_inventory.py`; `inventory_sync.py`; `inventory_sync_cli.py`; shared recorded-state contract; 447 focused backend tests; 73 focused Console tests; Ruff, strict mypy, Console typecheck/build; 16 hardening review rounds with no final Low-or-higher finding; current local PostgreSQL generation; authenticated standard 5273 Browser Entra validation | The exact `builds/default` child status is recorded as `staticSiteEnvironmentStatus`. All four current targets show `Ready` with fresh provider metadata in the canonical ontology state and Console Inspector. |
| Azure inventory scope and local refresh parity | validated | shared ARM scope extraction across ARG, direct ARM child, Resource Changes, and Activity Log paths; shared full-refresh enrichment composition; focused scanner and ontology tests (`374 passed`); strict mypy; refreshed local active-generation comparison | Subscription and Resource Group scope survive bounded vendor-property truncation and child collection responses. Local authoritative refresh records the same runtime-call, Resource Health, Static Web App, and Kubernetes source states as the long-running collector without deriving health from provisioning. |
| Dashboard source, Unknown causes, and refresh | implemented | `console/src/routes/dashboard-v2.loading.ts`; `dashboard-v2.model.ts`; `dashboard-v2.tsx`; focused Dashboard and refresh tests | The page names the inventory snapshot source, groups machine reasons, and reuses periodic, focus, online, visibility, and SSE invalidation refresh. |
| Model deployment detail projection | validated | `arm_inventory.py`; `model_deployment.py`; `instance_explorer.py`; `ontology-instances.model.ts`; ARM overlay, relationship mapping, projection, graph, Inspector, and context tests; local reconciliation; authenticated standard 5273 Browser Entra validation | A bounded read-only ARM child overlay fills the ARG omission. The active generation contains 108 deployments with verified parent relationships; the selected Inspector exposes only model name, version, SKU, normalized TPM, and provisioning state. |
| Stable instance graph viewport | implemented | `ontology-instances.css`; `ontology-instances.view.test.ts`; focused Console checks | The graph keeps its bounded viewport height for sparse results without changing recorded-state authority or graph contents. |
| Instance-first ontology entry | implemented | `ontology.tsx`; `ontology-navigation.tsx`; focused ontology route, view, and localization checks; Console typecheck and production build | `/ontology` renders the observed Resource instance workspace without waiting for the declaration graph. Reference views remain reachable through a labeled native disclosure and existing deep links. |
| Kubernetes diagnostic details | implemented | `instance_explorer.py`; `ontology-instances.model.ts`; `ontology-instances-inspector.tsx`; focused Operator and Console checks | Exact UID and resourceVersion plus allowlisted diagnostic facts remain separate from the three recorded-state axes and grant no authority. |

### Hardening review rounds

| Round | Review lens | Highest finding | Outcome |
|-------|-------------|-----------------|---------|
| 1 | Inventory-to-ontology identity, type, and state fidelity | None | Passed against the active local generation. |
| 2 | ARM scope retention after bounded property truncation | High | Resolved by identity-derived scope restoration and conflict checks. |
| 3 | Local refresh and long-running collector enrichment parity | High | Resolved through one shared promotion-enricher composition. |
| 4 | Provider-native type coverage | Medium | Resolved statically for all 23 reviewed baseline types; refreshed provider evidence remains an implementation gate. |
| 5 | Exact child containment | High | Resolved through reviewed provider-parent reuse and nested Subnet correction. |
| 6 | Operational-state applicability closure | Medium | Resolved with complete, mutually exclusive applicability sets. |
| 7 | Availability-state applicability closure | Medium | Resolved with complete, mutually exclusive applicability sets. |
| 8 | Per-target Resource Health failure evidence | Medium | Resolved with allowlisted reasons and prior-qualified-fact retention. |
| 9 | Full reconciliation and accelerator semantic parity | High | Resolved with sparse Activity Log observations, isolated operation outcomes, explicit completeness, and semantic event identity. |
| 10 | Generation-transition reads | Medium | Resolved with two typed, deadline-sharing full traversal retries that discard partial pages. |
| 11 | Malformed and contradictory provider rows | Medium | Resolved by cross-checking scope and provider type against exact ARM identity. |
| 12 | Independent final cross-cutting critique | High | All reported High and Medium findings were remediated and independently reverified; only Low residual findings remain. |
| 13 | Extension-resource ARM identity | High | Resolved by deriving the Resource type from the final provider namespace; all 89 configured live type queries succeeded. |
| 14 | Resource Health unavailable-only promotion | High | Resolved by admitting only the exact allowlisted availability reason map; a complete live generation promoted 1,154 Resources and 3,205 links. |
| 15 | Post-integration independent critique | Low | Passed independently with no Critical, High, or Medium finding; only documented Low residuals remain. |
| 16 | Live Activity Log scope rows | High | Resolved by dropping unreviewed type aliases before identity validation while retaining exact validation for every mapped type. |
| 17 | Final accelerator critique | Low | Passed independently with no Critical, High, or Medium regression; only documented Low residuals remain. |
| 18 | Complete durable-cursor Activity Log traversal | High | Resolved by requiring both the identity-derived and supplied type to be reviewed, preventing parent labels and operation categories from misclassifying child or scope events. |
| 19 | End-to-end critique | Medium | Identified collection-shaped ARM IDs that could create phantom Resource instances; resolved by concrete type/name pair validation. |
| 20 | Collection-identity verification | High | Confirmed concrete ARM identity handling, then found `search-service` incorrectly broadened generic database queries. |
| 21 | Final semantic classification critique | Low | Passed independently with no Critical, High, or Medium finding; the full Core and Operator test tree is green. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-14 | implemented | Made Ontology Instances the default `/ontology` workspace, progressively disclosed definition and topology views, and removed the declaration graph from the instance-loading path. | `current change`; task-owned Console route, navigation, style, catalog, and test paths; focused tests, typecheck, and production build. | Retain authenticated standard-5273 evidence for default instance loading and the expanded reference disclosure. |
| 2026-09-13 | validated | Reconciled the complete local data path after the final hardening round. The active generation contains 1,154 Resources and 3,205 Resource-linked ontology relations; all 955 provider objects are mapped, scope omissions and inventory-to-ontology state mismatches are zero, and Resource Health records 155 facts plus two explicit `not_modeled` reasons across 157 targets. | Local PostgreSQL active-generation inspection; four newly supported Resource Health types all carry exact facts; full stack remained `10/10 ready` across repeated reconciliation loops. | The retained Browser Entra state expired and Azure CLI token acquisition timed out, so authenticated Dashboard paging was not re-observed in this run; focused loader tests and direct generation-fenced data checks remain green. |
| 2026-09-13 | validated | Corrected extension-resource ARM type parsing and admitted the allowlisted Resource Health unavailable-only enrichment through the promotion validator. | `current change`; focused inventory and ARG checks (`220 passed`); all 89 configured provider-type queries succeeded; local authoritative refresh promoted 1,154 Resources and 3,205 links with `complete=True`. | Retain the independent post-integration critique before closing the hardening campaign. |
| 2026-09-13 | implemented | Prevented local catalog evidence health from joining an active inventory snapshot to an older ontology status or manifest generation. A cross-generation state now materializes as explicit unavailable evidence instead of failing startup or combining timestamps. | `current change`; focused catalog materialization regression; reproduced local active/projection generation mismatch. | Re-run the authoritative refresh and retain a same-generation projection receipt before raising this transition to `validated`. |
| 2026-09-13 | implemented | Replaced Resource Health's all-or-nothing target overflow with a stable bounded prefix. Overflow targets retain prior qualified evidence or carry the allowlisted `resource_health_target_limit` reason, while source coverage records the partial result. | `current change`; focused Resource Health and recorded-state checks; Ruff and strict mypy passed. | Retain refreshed provider evidence for Prometheus rule groups, Managed Grafana, Bastion hosts, and search services before restoring the expanded matrix to `validated`. |
| 2026-09-13 | implemented | Classified 23 provider-native Azure types, completed operational and availability applicability for the expanded neutral vocabulary, added four Resource Health targets, four exact child-containment mappings, and three documented operational paths without using provisioning as health. | `current change`; official Microsoft resource and Resource Health references; focused registry, relationship, taxonomy, ARG, Resource Health, recorded-state, and catalog projection checks (`368 passed`); Ruff and strict mypy passed. | Refresh the local active generation and verify zero unreviewed provider types plus exact state and relationship evidence. |
| 2026-09-13 | implemented | Preserved an allowlisted per-resource Resource Health failure reason when no prior availability fact exists, while retaining a prior verified fact ahead of a newer failed read. | `current change`; focused Resource Health and recorded-state checks (`162 passed`); Ruff and strict mypy passed. | Refresh the local active generation and verify provider `not_modeled` targets expose the canonical reason without provider response text. |
| 2026-09-13 | validated | Preserved exact Azure subscription, Resource Group, and provider-type scope after bounded property truncation across full ARG reads, unclassified rows, direct ARM child collections, nested subnets, Resource Changes hydration, and Activity Log deltas. Reused one full promotion-enricher composition for local authoritative refresh and the long-running collector. | `current change`; focused scanner, inventory CLI, refresh-script, and ontology projection tests (`374 passed`); Ruff and strict mypy passed; a new local authoritative refresh retained complete inventory-to-ontology identity, type, state-value, and scope fidelity with explicit source limitations. | Review the active generation's provider-native unmapped types and add only evidence-backed canonical mappings or explicit exclusions. Keep provider-declared Resource Health `not_modeled` results explicit rather than inferring availability. |
| 2026-09-12 | implemented | Added active-generation provider-native type coverage as a separate evidence detail without changing the three recorded Resource state axes. | `current change`; focused Operator inventory/workflow suites passed 83 tests; Console model passed 79 tests; strict mypy and Console typecheck passed. | Retain live provider-change latency and bounded unknown history under Issue #444; neither is a Resource state value. |
| 2026-09-12 | implemented | Aligned the native Dashboard v2 component with the approved decision-first Resource workspace. Returned scope and drill-down counts now share one snapshot surface, the Resource landscape remains primary, evidence coverage aligns beneath it, and the secondary rail switches between Check first records and the selected Inspector. | `current change`; `console/src/routes/dashboard-v2.tsx`; `console/src/routes/dashboard-v2.css`; `console/tests/e2e/dashboard-v2.spec.ts`; native Dashboard E2E (`10 passed`), route-selected Console tests (`102 passed`), typecheck, production build, and entry bundle check. | No remaining work for this presentation-only change. An authenticated standard-5273 screen check remains separate from implementation evidence. |
| 2026-09-11 | implemented | Restricted unavailable projection-source reasons to canonical machine tokens so principal text and provider details cannot cross the shared Operator read boundary. | `current change`; `postgres_family_store.py`; focused principal-text rejection check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Bound Operator runtime-call relationship evidence to the selected inventory generation and rejected stale or substituted link metadata. | `current change`; `postgres_family_store.py`; focused mismatched-generation rejection check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-11 | implemented | Required explicit runtime-call and PostgreSQL-role source states in every current instance-detail response so omission cannot render as availability or measured zero. | `current change`; `ontology-instances.model.ts`; focused missing-source decoder check. | Retain authenticated available-source evidence on standard ports under issue #260. |
| 2026-09-10 | implemented | Added separate Kubernetes identity and diagnostic detail objects without changing recorded-state axes. | `current change`; focused Operator projection, Console decoder, typecheck, and build checks. | Retain authenticated live AKS Inspector evidence under Issue #578. |
| 2026-09-07 | implemented | Preserved the bounded Ontology Instances graph viewport when the returned canvas is shorter than the available inspection area. | `current change`; focused Console view contract, typecheck, and production build passed. | No remaining work for this presentation-only correction. |
| 2026-09-06 | implemented | Added the bounded model deployment detail projection to ontology instance cards, tooltips, the Inspector, and screen context without exposing raw provider properties. | `current change`; focused Operator and Console projection tests, typecheck, build, localization, and hardening review. | Retain an authenticated 5273 screen check before claiming this display path as runtime-validated. |
| 2026-09-05 | in-progress | Started the approved shared recorded-state query after confirming state values were retained in instance properties but lost by the graph/status projection. | `current change`; implementation paths above; local read-only source inspection from the originating Dashboard work. | Finish API contract, paging, native consumers and focused validation. |
| 2026-09-05 | implemented | Added the common recorded-state projection to instance directory/detail and a paged authenticated state route. Dashboard now loads that route without graph fallback; both screens show the same separate facts and metadata. | `current change`; 187 backend checks, 150 Console checks, 10 native browser scenarios, typecheck and build passed. Read-only local checks read one generation in two bulk pages without provider calls. | Retain authenticated operator-screen evidence before live-readiness claims. |
| 2026-09-05 | implemented | Isolated the complete recorded-state change from concurrent document-ingestion edits and documented source declaration and assessment-evidence boundaries. | `current change`; 258 backend checks passed with one optional PDF check skipped; 95 Console checks, typecheck, and build passed; entry gzip is 143109 bytes under the unchanged 150000-byte ceiling. Roadmap tracking and document-size checks passed. | The base snapshot contains an unregistered `test_power_platform_connector.py`; the service-ownership suite reports nine related failures independently of the two new state-test registrations. Preserve that separate connector repair and complete the recorded-state follow-up items below. |
| 2026-09-05 | implemented | Added the inventory/ontology generation fence, restored evidence qualification from immutable timestamps, retained reviewed operational paths, separated Unknown causes, and enabled shared automatic refresh. | `current change`; task-owned source and focused checks listed above. | Retain authenticated operator-screen evidence before claiming live deployment readiness. |
| 2026-09-06 | implemented | Classified missing operational state across all 80 canonical ResourceType values, retained three resource-specific provider state fields, and replaced the Console's generic missing label with reason-specific unavailable and not-applicable labels. | `current change`; 268 focused backend tests and 85 focused Console tests passed; Console typecheck and production build passed; local browser checks covered provider-unavailable, retained-value, and not-applicable outcomes. | Verify a newly collected Azure generation before claiming deployed provider coverage. |
| 2026-09-06 | validated | Ran one bounded live Azure Resource Graph collection into local PostgreSQL and verified the resulting active generation through the authenticated standard Console URL. | `current change`; live collection promoted from `arg`; Application Insights and Log Analytics retained provider-state-unavailable reasons; disk and private-DNS-link records retained their explicit operational values, observation times, freshness, and completeness. | No remaining work in this bounded read-model scope. |
| 2026-09-06 | validated | Extended the closed classification to the newly merged `llm-model-deployment` ResourceType and removed fixed-count assumptions from current scope text. | `current change`; exact catalog-to-classification set equality and 268 focused backend tests passed on the merged main snapshot. | Future ResourceType additions remain blocked until they declare a reviewed operational-state outcome. |
| 2026-09-06 | implemented | Corrected the Application Insights and Log Analytics missing-state result from generic provider unavailability to the exact unbound Azure Resource Health source. | `current change`; the existing exact-target Resource Health reader returned `scope_unreadable` for both local targets, while instance source metadata reported `projection_not_bound`; focused backend and Console checks cover the distinct reason. | Bind and persist Resource Health observations before displaying an availability value; do not infer one from provisioning. |
| 2026-09-06 | in-progress | Revised the state pipeline so reviewed per-resource state enrichment reaches the promoted snapshot, current ontology instance, independent transition history, Operator reads, and conversational functions. | `current change`; design and implementation paths in this ledger. | Complete the implementation, critique, focused checks, live reconciliation, transition coverage, and authenticated browser evidence. |
| 2026-09-06 | validated | Completed and repeatedly critiqued the unified state pipeline, including serialized promotion/projection, prior-generation fencing, chronology guards, partial-source retention, and relationship-independent transition coverage. | `current change`; 260 focused backend tests, Ruff, mypy, 87 focused Console tests, Console typecheck/build, local PostgreSQL reconciliation with 21/21 Log Analytics Resource Health facts, and authenticated browser checks for Application Insights and Log Analytics. | No remaining work in this bounded state-pipeline scope; a production Azure deployment was not performed. |
| 2026-09-06 | validated | Closed the final post-rebase review findings by treating equal-time reason changes as conflicts and replaying a journaled active generation before any later promotion when history publication fails. | `current change`; focused recovery, chronology, journal replay, and enrichment tests plus the repeated state-pipeline gate stack. | No remaining work in this bounded state-pipeline scope; a production Azure deployment was not performed. |
| 2026-09-06 | implemented | Corrected the current status after the final review added ResourceType-gated state paths and the standard services had not yet been restarted from the integrated revision. | `current change`; 313 focused backend tests, Ruff, mypy, 113 focused Console tests, Console typecheck/build, and local PostgreSQL reconciliation with 21/21 Log Analytics Resource Health facts. | Restart the integrated standard services and verify Application Insights and Log Analytics in the authenticated browser before restoring validated status. |
| 2026-09-06 | validated | Restarted the standard Core Runtime and Operator API from integrated revision `7893a3042`, retained the standard 5273 frontend, and verified both target Resource types through Browser Entra. | 317 focused backend tests, Ruff, mypy, 113 focused Console tests, Console typecheck/build; local PostgreSQL generation and watermark readback; authenticated 5273 browser evidence showing Application Insights operational and availability as Not applicable and Log Analytics availability as Available. | No remaining work in this bounded state-pipeline scope; production Azure deployment remains unverified. |
| 2026-09-06 | implemented | Corrected the preceding validated closure after follow-up review found that Core ontology ingestion still accepted generic state paths and the Operator legacy `status` still accepted rejected fields. Centralized reviewed ResourceType paths, independently adjudicated only those paths across repeated observations, and removed `provisioningState` and non-applicable values from legacy operational status. | `current change`; 356 focused backend tests, Ruff, strict mypy, 126 focused Console tests, Console typecheck/build, and final read-only review with no high-confidence findings. | Reconcile and verify the integrated revision through the standard authenticated Console without interrupting unrelated root worktree changes. |
| 2026-09-06 | implemented | Restarted the standard Core Runtime, Operator API, and inventory reconciliation process from integrated revision `21e7641a4`, then promoted one complete standard inventory generation. PostgreSQL confirms that Application Insights, Log Analytics, and resource groups have no operational value or metadata, while all 21 Log Analytics Resources retain Resource Health availability metadata. Existing Browser Entra captures had expired and returned the 5273 sign-in screen, so no replacement browser claim was made. | Current local generation; inventory, ontology manifest, ontology status, and observation watermark generation equality; 11 Application Insights, 21 Log Analytics, and 48 resource-group records checked; standard service readiness passed. | Renew the Browser Entra session without exposing credentials, then verify Application Insights as Not applicable for both axes and Log Analytics as Not applicable operationally and Available for availability. |
| 2026-09-06 | implemented | Completed the selected feedback hardening by bounding values, adjudicating every declared path, preserving valid global conflicts and Kubernetes unknown readiness, and filtering root and nested flat or keyed metadata through the same ResourceType contract. | `current change`; 399 focused backend tests, Ruff, and strict mypy. | Reconcile and verify the integrated revision through the standard authenticated Console without interrupting the separately owned full-stack session. |
| 2026-09-06 | implemented | Hardened the nested metadata filter so only canonical `StateFactMetadata` bound to a present allowlisted value survives. Null, non-mapping, malformed, overflowing, orphaned, and unsupported flat metadata is removed before projection. | `current change`; 423 focused backend tests, Ruff, strict mypy, and final read-only review with no high-confidence findings. | Restart and replay the integrated standard services, then renew the Browser Entra session for the remaining authenticated 5273 check. |
| 2026-09-06 | implemented | Restarted the standard Core Runtime, Operator API, and inventory reconciliation process from revision `84228ac0f`. The standard loop promoted a new complete generation, and the final projector reproduced the stored target facts without target overlays. | Current local generation; inventory, ontology manifest, ontology status, and observation watermark generation equality; 11 Application Insights and 48 resource-group records with no operational value or metadata; 21 Log Analytics records with no operational value or metadata and 21 exact Resource Health availability facts. | Renew the expired Browser Entra session and complete the authenticated 5273 screen check before restoring `validated`. |
| 2026-09-06 | implemented | Corrected the ontology graph's compact node label so an explicitly inapplicable operational axis no longer hides a useful availability or provisioning fact. The selected axis remains visible and no fact is recast as operation or health. | `current change`; `ontologyInstanceNodeState`; 80 focused Console tests and Console typecheck. | Build, restart the standard frontend, and verify Application Insights, Log Analytics, and resource-group nodes through the authenticated 5273 Browser Entra session. |
| 2026-09-06 | validated | Integrated the compact state-axis selector with model deployment details, restarted the standard frontend, and recovered the Browser Entra session through a clean committed Operator API after a dirty root authentication experiment rejected the valid delegated-user token. | Revision `62b8215ba`; 87 focused Console tests, Console typecheck/build, standard Operator and frontend readiness, and authenticated 5273 DOM evidence: Application Insights and resource group show `Provisioning: Succeeded`, Log Analytics shows `Availability: Available`, AKS shows `Operational: Stopped`, and the main region has no horizontal overflow. | No remaining work in the bounded recorded-state path; model deployment screen evidence remains tracked separately below. |
| 2026-09-06 | implemented | Expanded exact Azure Resource Health availability from Log Analytics to the complete reviewed support matrix, including Storage accounts, and hardened bounds, prior-state retention, evidence binding, ARM identifiers, and compact graph precedence through 18 critique rounds. | `current change`; live subscription Resource Health crosswalk, 472 focused backend tests, 140 focused Console tests, Ruff, strict mypy, Console typecheck/build, and final review with no Medium, Low, or higher finding. | Run a standard reconciliation from the integrated revision and verify Storage plus representative compute, data, platform, and network Resources through authenticated 5273 Browser Entra. |
| 2026-09-06 | validated | Restarted the clean integrated Operator and inventory processes, promoted one complete standard generation, and verified the expanded availability matrix in PostgreSQL, ontology projection, and the authenticated standard Console. | Revision `c5380e434`; current local generation; 139 of 141 current targets have exact Resource Health values and metadata, including all 18 Storage accounts. The two provider-unmodeled availability targets are one VM and one SQL database whose independent operational facts remain visible. Browser evidence shows Storage, Azure AI endpoint, NAT Gateway, and Cosmos DB as `Availability: Available`, while VM retains its operational state; no horizontal overflow. | No remaining work in the expanded recorded-state scope; model deployment screen evidence remains tracked separately below. |
| 2026-09-06 | implemented | Completed the comprehensive primary-state matrix so provider-not-exposed Resources use exact availability or exact provisioning before the generic operational-unavailable placeholder. Private Endpoint approval remains relationship evidence and is not recast as operation or health. | `current change`; active-generation audit across all not-exposed ResourceTypes, 130 focused Operator tests, 77 focused Console tests, Console typecheck, and 16 post-implementation hardening rounds with no Low or higher final finding. | Restart the integrated frontend and Operator API, then verify Private Endpoint and representative availability/operational cases through authenticated 5273 Browser Entra. |
| 2026-09-06 | validated | Restarted the clean integrated 5273 frontend, retained the generation-fenced Operator projection, and verified the comprehensive primary-state matrix through authenticated Browser Entra. | `current change`; Private Endpoint shows `Provisioning: Succeeded`, Storage and Cosmos DB show `Availability: Available`, VM shows its exact operational state, and Static Web App retains explicit unavailable because no recorded axis has an exact value. Main-region horizontal overflow is absent. | No remaining work in this comprehensive recorded-state presentation scope; future relationship-condition axes require a separate additive contract rather than overloading operational health. |
| 2026-09-06 | validated | Corrected the provider-evidence gap label so a Resource with no exposed state is not presented as if the Resource itself were unavailable. | `current change`; 91 focused Console tests, Console typecheck/build, and authenticated 5273 Browser evidence showing Static Web App as `Operational: Not provided` with provisioning and availability `Not recorded`; no horizontal overflow. | No remaining work in this bounded evidence-label scope. |
| 2026-09-07 | implemented | Corrected the preceding Static Web App evidence-gap result after direct Azure comparison showed that the parent resource omits state while the exact `builds/default` child exposes `Ready`. Added a bounded promotion enricher, the full documented `BuildStatus` set, reviewed operational path, content-addressed evidence admission, prior-state retention, preview-environment exclusion, cross-enricher generation pinning, source-preserving canonical ontology projection, and evidence-cutoff freshness. | `current change`; exact ARM reads returned four default environments with `Ready`; 447 focused backend tests, 73 focused Console tests, Ruff, strict mypy, Console typecheck/build, localization gates, and 16 hardening review rounds passed with no final Low-or-higher finding. The current local generation contains four `Ready` facts with complete, non-synthetic provider metadata in both inventory and canonical ontology `state`. | Complete the authenticated standard 5273 Browser check before restoring `validated`. |
| 2026-09-09 | validated | Started the complete standard Console topology and closed the Static Web App screen-evidence gap through the authenticated Browser Entra session. | The committed preparation and service workflows reported 10 of 10 services ready. The standard `localhost:5273` instance graph and Inspector showed all four current Static Web Apps as `Operational: Ready`, fresh at evaluation time, and never Not provided. | No remaining work in the Static Web App default-environment state scope. A model deployment screen check remains separately blocked until the active generation contains a target. |
| 2026-09-09 | validated | Corrected deployed-model discovery after read-only Azure comparison found existing child deployments but the active inventory contained none. Added bounded ARM account child collection, exact model deployment projection, and source-specific verified parent mapping without creating or changing Azure resources. | `current change`; 186 focused backend tests, Ruff, strict mypy, and localization checks passed. A local reconciliation recorded 108 deployments and 108 parent relationships in both inventory and ontology. The authenticated standard 5273 graph and Inspector showed model name, version, deployment SKU, normalized TPM, and `Succeeded` provisioning state for one selected deployment. | No remaining work in the model deployment screen-validation scope. |

### Remaining work

- [x] Pass backend tests for independent state axes, evidence gaps and cursor/generation/principal isolation.
- [x] Pass common frontend decoder, multi-page loader, instance rendering and Dashboard browser tests.
- [x] Verify the local read path uses stored instance records without Azure or model calls.
- [x] Retain authenticated operator-screen evidence from the integrated standard services before
  claiming live deployment readiness.
- [x] Diagnose one immutable generation and preserve distinct
  `resource_type_unclassified`, `state_source_not_recorded`, and
  `state_applicability_unknown` outcomes without converting them to not-applicable.
- [x] Classify every canonical ResourceType as source-expected, provider-not-exposed, or
  not-applicable and verify exact set parity against `resource-types.yaml`.
- [x] Record a newly collected Azure generation showing `diskState`, `snapshotAccessState`, and
  `virtualNetworkLinkState` after this change before claiming deployed provider coverage.
- [x] Reconcile a local generation with exact Log Analytics Resource Health facts and verify current
  ontology state and transition coverage.
- [x] Renew the Browser Entra session without exposing credentials and verify the final shared-path
  enforcement through the standard authenticated 5273 Console.
- [x] Reconcile the expanded Resource Health matrix and verify Storage plus representative compute,
  data, platform, and network availability through the standard authenticated 5273 Console.
- [x] Verify all current Static Web Apps show exact default-environment operational state through the
  authenticated standard 5273 Console.
- [x] Verify one selected `llm-model-deployment` card and Inspector through an authenticated standard
  5273 Console and retain non-sensitive evidence for model, version, SKU, TPM, and provisioning state.
- [x] Preserve exact Azure subscription and Resource Group scope through truncation, full scans,
  child-resource scans, and change accelerators; verify zero scope loss and zero inventory-to-ontology
  identity, type, or state-value mismatch in a refreshed local active generation.
- [ ] Refresh a local active generation with a Resource Health `not_modeled` target and verify its
  missing availability fact carries only the canonical per-resource reason through the ontology and
  authenticated Console.
- [ ] Review every provider-native type reported as unmapped by the active coverage manifest, add
  only approved canonical mappings or explicit exclusions, and verify the next full snapshot has no
  unreviewed provider type.
- [ ] Retain authenticated standard-5273 browser evidence that `/ontology` renders the instance
  directory before any declaration-graph request and that the reference disclosure preserves every
  existing reference destination.
