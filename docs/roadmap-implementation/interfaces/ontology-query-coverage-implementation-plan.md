# Ontology Query Coverage Implementation Plan implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

Current planning uses a principal-scoped declaration candidate index built and validated from the
exact manifest. Ranking cannot select an intent or replace full-manifest plan verification.
Candidate counts and omissions remain explicit, secondary intents retain their declarations, and
an oversized capability projection holds instead of silently dropping its tail. The in-process
declaration generation is a catalog read projection, not a durable operational-instance index or
a Rule activation. The dated baseline above is retained history; this section owns current status.
Incremental ontology builds reuse only complete canonical documents. Previous vectors and staging identities are discarded because a matching content digest alone cannot establish their embedding-space provenance; the configured adapter must generate new vectors.
Complete canonical declaration lists skip count/detail repair; the [planning owner](../../roadmap/interfaces/hierarchical-conversation-planning.md) records the exact typed shape and bounded model comparison. This does not widen principal scope or certify full query coverage.
The [retrieval qualification owner](../../roadmap/rules-and-detection/rule-semantic-retrieval.md) requires the evaluator itself to hold when a configured cohort is absent. Policy `1.1.0` additionally binds explicit per-metric sample floors and rechecks them during review while preserving legacy `1.0.0` receipt identities. Complete cohort presence or this policy implementation does not establish adequate real-embedder qualification data or live relevance.
When a semantic judgment was evaluated, declaration-list and declaration-detail frames require an accepted, unambiguous schema-family intent. A rejected or unrelated judgment cannot use a later model frame to widen its declaration scope; it returns the existing typed unavailable outcome before query execution.

### New post-implementation critique series

This series starts after the default runtime and bilingual candidate terminal implementation
(`c4ed2ab712`, `405ebb8e79`) and its failure-isolation repair (`4a9b8ad676`). Earlier preparation
checks are not counted. These local reviews cannot close unqualified real embedding relevance
or replace authenticated Console evidence; overall Medium-or-higher closure remains unproven.

| Round | Hypothesis and inspected boundary | Finding | Change and falsifying evidence |
|-------|-----------------------------------|---------|--------------------------------|
| 1 | Authenticated enrollment might reuse an old principal digest after role, group, purpose, or agent changes. | No new defect found at this boundary. Every changed claim was denied before enrollment, query, or persistence. | Added four `test_enrollment_rejects_changed_claims_before_scope_or_query_admission` cases; all passed. Scope authorization remains server-owned. |
| 2 | The total reconciliation deadline might exclude waiting for the process-local lock. | Confirmed a low-severity deadline contract gap under concurrent callers; the current supervisor is serial, so no live outage is claimed. | A real contended-lock timeout regression failed before repair. Moving the timeout outside lock acquisition made the same check pass without changing source or lifecycle authority. |
| 3 | Source/vector preparation and retired-generation reclamation could overlap across workers. | Medium: owned callbacks had no shared write/cleanup lock, allowing a writer to leave derived rows after reclamation. | Reproduced concurrent write-section entry; injected the existing runtime ResourceLock and used the same scope key for preparation and reclamation. Two worker replicas now serialize and the actual retirement event uses the same key. Sixteen focused checks passed; lock waiting remains inside the original request deadline. Distributed transport-specific lock behavior retains its existing provider test ownership. |
| 4 | Expired enrollment rows could occupy all eight slots until a background cycle. | Low: a new authenticated principal was denied even though all retained enrollments had expired. | The capacity regression failed before repair. Authenticated query ingress now shares the expiry cleanup with reconciliation; nine focused runtime checks passed. Cleanup releases local caches only, without pointer or audit mutation. |
| 5 | Completed durable vector point reads might accept changed/missing rows or altered/missing headers. | No new defect found at this boundary. All four corruption cases failed closed. | Added four `test_completed_vector_point_read_rejects_durable_damage` cases. This establishes the persisted read boundary only, not continuous revalidation of an already prepared cache or real embedding quality. |
| 6 | A successful publication attempt without durable owner processing could permanently suppress ingress recovery. | Medium: dropping the first accepted publication left preparation unstarted because an in-memory attempted flag suppressed replay. | The reproduction failed before repair. Reconciliation now uses durable Muninn completion, replays the exact stored request only inside its original lifetime, and holds expired unprocessed requests. Workers recheck completion and persist results under the shared projection lock. Eighteen focused checks passed, including simultaneous duplicate delivery to two worker instances with one preparation. |
| 7 | Cold-cache restoration might accept missing/substituted independent validation or publish a cache after its active pointer changes. | No new defect found at this boundary. All three adversarial restore checks failed closed. | The actual audited generation test now removes validation, changes its vector target, and simulates pointer movement after loading. The latter clears the cache, denies a subsequent search, and permits a fresh exact restoration. No provider re-embedding or new activation was required. |
| 8 | A worker replica other than query ingress might lose the authenticated source scope. | Medium: the second coordinator could not resolve a valid enrollment because it existed only in the first process. | Added service-owned five-minute enrollment receipts. Each source read revalidates exact principal/role/group/purpose digest and lifetime; changed claims, future/expired receipts and extended lifetimes fail closed. Admission persistence is serialized inside two seconds. Twenty-four focused checks passed. The eight-enrollment bound is per process, not a deployment-wide quota. |
| 9 | Repeated source updates could exhaust the eight retained rollback generations and strand a stale active index. | Medium: the real multi-generation test stalled with stale-content rejection at the retention boundary. | Reconciliation now requests retirement of the oldest inactive retained generation before an overflowing invalidation. The existing Muninn/Heimdall/Saga lifecycle and physical cleanup remain authoritative. The same test now crosses the ceiling, preserves audited tombstones, and returns the latest authorized facts without raising the retention limit. |
| 10 | Reconciliation might publish a successor after pointer CAS but before terminal audit or cache/cleanup completion. | Medium: removing the exact terminal seal still allowed a new invalidation request to be stored and published. | Successors now require the exact Saga terminal and durable Muninn `ready` outcome for that pointer revision and active generation. Missing audit and missing ready both hold before new source reads or requests. Twenty-four focused runtime/candidate checks passed, including repeated retention and recovery. |

Ten new local critique rounds have been executed. This is not overall closure: real semantic
ranking remains unqualified and disabled, authenticated bilingual Console evidence is absent,
and the original 36 findings still require the current evidence reconciliation below. Newly
found local defects were repaired, but these checks do not establish an exhaustive Low-only verdict.
Subsequent reconciliation reproduced original findings 23 and 24 in the current capability
projection and repaired them. Therefore these ten rounds are retained as genuine local review
history, not credited as the requested final ten rounds after all remaining implementation.
That final acceptance gate remains open, alongside live quality and authenticated evidence.

### Original finding reconciliation

This is the current campaign status, not a production-readiness or Low-only verdict. `Local verified`
means a focused test executed successfully on current local inputs. It does not substitute for
independent real-model or authenticated cross-service evidence. Test names below refer to
`services/core-control-plane/tests/`; existing checkpoint evidence remains in the history.

| ID | Original finding | Current status | Current evidence or remaining gate |
|----|------------------|----------------|------------------------------------|
| 1 | Hidden predicate ACL | Local verified | `test_query_gateway::test_gateway_rejects_unreadable_predicates_before_store_access`; verifier hidden-predicate rejection. |
| 2 | Historical graph ACL | Local verified | `test_topology_history::test_historical_query_projects_private_properties_and_rejects_foreign_release`. |
| 3 | Direct metric scope | Local verified | `test_query_hardening::test_unknown_metric_target_never_calls_provider`; `semantic_query_scoped_sources` authorizes exact Resource through the secured gateway. |
| 4 | Lost principal scope | Local verified | `test_query_gateway::test_gateway_binds_authenticated_principal_scope_to_receipt`; real typed Function/runtime principal checks. |
| 5 | Historical release mismatch | Local verified | Historical query test rejects foreign and absent release identities before exposing facts. |
| 6 | Lost multi-hop endpoints | Local verified | `test_two_hop_relationship_keeps_every_reached_endpoint`, transitive and non-transitive cases. |
| 7 | Exact-ID relationship loss | Local verified | `test_exact_ids_preserve_links_between_selected_records`. |
| 8 | Latest context eviction | Local verified | `test_latest_trusted_context_survives_long_earlier_turn`. |
| 9 | Decimal sum precision | Local verified | `test_query_handlers::test_large_sum_preserves_all_integer_digits`. |
| 10 | Numeric-string sorting | Local verified | `test_aggregate_numeric_order_survives_projection`. |
| 11 | Mixed source generations | Local verified | Set-operation lineage rejection and relationship source-generation conflict tests. |
| 12 | Child leak on parent cancellation | Local verified | `test_query_execution::test_executor_drains_children_on_timeout_and_parent_cancellation`. |
| 13 | Timeout waiter leak | Local verified | The same parameterized drain test plus queued-node/semaphore deadline checks. |
| 14 | Unbounded progress observer | Local verified | `test_stalled_progress_cannot_block_query_completion`. |
| 15 | Freshness mismatch | Local verified | Relationship freshness hold and secured graph refresh tests preserve the caller's freshness bound. |
| 16 | Missing cutoff refresh | Local verified | `test_every_current_read_cutoff_is_rebound`; `test_object_set_cutoff_is_refreshed_after_model_planning`. |
| 17 | Project/Order field validation | Local verified | `test_hidden_and_unknown_projection_fields_are_rejected` and verifier field propagation. |
| 18 | Union-bound exception | Local verified | `test_large_union_is_explicitly_truncated`. |
| 19 | Missing selected properties | Local verified | `test_optional_property_projects_null_and_sorts_last`. |
| 20 | Metric time interval | Local verified | `test_invalid_metric_interval_is_rejected_before_io` and equal-duration window tests. |
| 21 | Measuring only first resource | Local verified | `test_metric_scope_series_does_not_sample_an_arbitrary_resource`; ambiguous scopes hold. |
| 22 | Complete manifest 512 cliff | Local verified | `test_semantic_query_descriptor_selector::test_large_release_selects_matching_declaration_without_512_cliff`; complete-manifest verification remains separate. |
| 23 | Capability tail loss | Repaired and locally verified | Reproduced again during reconciliation, then fixed in `6fbedc9227`; oversized capability input now fails explicitly before judgment. |
| 24 | Property-value loss above 32 | Repaired and locally verified | Reproduced again during reconciliation, then fixed in `6fbedc9227`; all selected canonical property tokens are retained within the unchanged byte bound. |
| 25 | Secondary-intent descriptor loss | Local verified | `test_secondary_intents_keep_their_descriptors`. |
| 26 | Durable instance index not wired | Local mechanics verified; qualification open | Default release/bootstrap, authenticated enrollment, real owner events, cache recovery, source invalidation, retirement and candidate terminal projection are connected. Exact-ID factory tests pass; semantic ranking stays disabled pending real qualification. |
| 27 | PostgreSQL full corpus reload | Local DB verified | The isolated cache/tamper integration test passed on 2026-09-22. `_postgres_validation_cache` reuses verified document bodies while still reading MVCC row-version metadata; this does not claim sublinear search. |
| 28 | Memory/PostgreSQL ranking mismatch | Local DB verified | Bilingual durable/local parity passed on 2026-09-22 using the same `rank_documents`. Zero-vector parity proves adapter consistency, not real embedding quality. |
| 29 | Exact-ID embedder dependency | Local instance and Rule DB verified | Prepared instance exact-ID search succeeds with the embedder unavailable. The isolated Rule integration test also passed exact-ID queries with an embedder that raises if called. |
| 30 | Receipt cache role collision | Local verified | `test_identical_results_are_isolated_by_role_and_principal_scope`. |
| 31 | Display receipt expiry | Local verified | `test_scoped_presentation_receipt_rejects_future_or_expired_evidence`. |
| 32 | Raw provider errors exposed | Local verified | `test_provider_errors_never_log_private_message` plus vector provider-redaction tests. |
| 33 | Uncalibrated scoring policy | Open | No passing frozen-policy real embedding qualification exists. Runtime semantic ranking remains closed; no threshold was lowered. |
| 34 | One-sample cohorts and real embedding evaluation | Open; production-shaped diagnostic passed | The declaration diagnostic remains 15/16 with its 24-case held-out set unopened. A separate corpus using the actual instance document builder passed calibration 8/8 and disjoint held-out 12/12 with the policy frozen before held-out calls. Four synthetic Resource objects and one ambiguous/adversarial case per language do not satisfy representative production coverage or per-cohort sample floors. |
| 35 | Documentation mismatch | Updated; overall status remains incomplete | Corrected capability-omission design, documented actual runtime/candidate boundaries, and retained all historical evidence. This table supersedes checkpoint-specific open notes for current campaign status. |
| 36 | Authenticated real end-to-end evidence | UI and stream evidence verified; loaded-source certification open | Two browser-stream canaries and two separate actual Console input/button turns answered in EN/KO with `semantic_answer_verified`. Exact durable Operator projections confirm `query.manifest`, `kinds=[object]`, limit 1000, identical release/manifest/evidence, 1/1 checks, and no execution authority. Both UI answers displayed verified evidence and the no-authority notice without horizontal overflow. The running process was not attested to the latest immutable source, so full certification remains open. |

The 2026-09-22 resumption authorized the required scoped permissions and remaining validation.
The local stack was ready (12/12) and the shared Console was signed in. The initial subscription
mismatch diagnosis was incorrect: the selected artifact contains an all-zero placeholder, not a
different subscription identity. Subsequent read-only Azure account and deployment observations
matched the configured endpoint to exactly one account in the current CLI subscription and
confirmed the selected embedding deployment and model version. There is no target-selection
blocker and no subscription switch is required. A private candidate subsequently bound the exact
observed embedding identity without changing other model policies. Its real calibration failed
at 15/16, so semantic ranking remains disabled and no held-out calls were made. Offline score
analysis found the failing Korean positive ranked Service above Resource by model similarity;
changing only the acceptance threshold cannot establish the correct top result. No question-specific
exception or quality-threshold relaxation was added. The active model metadata update was backed
out after the managed VS Code restart task failed to create a terminal and the runtime pin stayed
unchanged. The original file hash was restored exactly, and the validated candidate and backup
remain private. No cloud role or service process was changed by this attempt. Full qualification
and final post-implementation review remain incomplete.

A separate production-shaped diagnostic used `build_ontology_semantic_generation` to serialize four
synthetic Resource objects, rather than reusing declaration-description prose. Its eight calibration
cases selected a policy with minimum score 0.3, lexical weight 0 and semantic weight 1; the policy
was frozen before all 12 disjoint held-out positive, negative, ambiguous and adversarial cases
passed. This is a candidate-ranking pilot, not an activation receipt: ambiguous and adversarial
cohorts have only one case per language, the corpus covers one synthetic ObjectType, and extra
lower-ranked candidates remain possible. No production ranking policy was changed. A fresh
managed-task attempt also failed through the task-creation tool (`Task not found`); no temporary
task definition remained. The model-file attempt was again restored to its exact active pin.

Current focused evidence: 260 query/gateway/receipt/metric/history/vector/lifecycle/policy checks
passed, including loopback PostgreSQL snapshot, vector and lifecycle fixtures. The capability
repair passed 288 owning planning checks. The dedicated candidate terminal slice passed 252
checks before the unrelated capability repair. Two Rule PostgreSQL integration checks were
explicitly skipped because the existing service role lacks isolated-schema DDL permission.
The isolated-schema harness has separate unit checks; skipped integration checks are not passes.
On 2026-09-22 all three tests in `test_postgres_integration.py` passed in owned temporary schemas
on the existing loopback validation database. Its existing administrator capability sufficed;
no service-role grant, shared-table migration, or shared-table cleanup was performed. This new
result supersedes the earlier permission blocker without relabeling the skipped attempt.

### Preserved prior implementation notes

> **Implementation status (2026-08-10):** Exact ontology releases, semantic candidates, bounded
> ObjectSets, secured query receipts, typed function registration, current inventory projection,
> metric providers, and causal-analysis primitives exist. The production path still uses regex and
> token routing plus an optional serial two-to-three-command read plan. A server-side intent graph,
> principal-scoped release-derived query manifest, `OntologyQueryPlan`, durable semantic index,
> historical topology, and cross-resource temporal provider composition now exist. Production
> turn-completion wiring, durable semantic-index activation publication, and governed
> cross-service and randomized assurance receipts remain incomplete.
> The OQ-01 implementation-free SDK models for the semantic problem frame, query DAG, intent graph,
> task receipt, and structural coverage receipt now ship. Producer and consumer projection wiring
> remains part of OQ-04 and OQ-05.
> OQ-02 now includes a content-addressed principal-scoped manifest builder that filters functions by
> role and purpose and accounts for every supplied release declaration. Every directed LinkType now
> projects deterministic outgoing and incoming endpoint query-side identifiers. The production
> catalog loads the reviewed `Identifiable` Interface, validates explicit bindings for all current
> ObjectTypes, compiles the polymorphic catalog, and includes the declaration in the exact runtime
> release. Wiring this manifest into the narrator and generic query surface remains.
> OQ-03 now includes an exact-release query DAG executor with bounded dependency waves, concurrency,
> timeout, cancellation, blocked-descendant handling, stable failure reasons, and task receipts.
> Built-in handlers now cover secured ObjectSet materialization, union, intersection, subtraction,
> ordering, projection, grouped aggregation, and exact-release query/derive/validate function
> invocation. A deterministic verifier checks the principal manifest, readable properties,
> LinkTypes, closed node arguments, dependency output kinds and aggregate fields propagated through
> ObjectSet, Project, Order, and set-operation dependencies,
> function schemas, and registered
> extension schemas before I/O. Temporal, metric-series, and evidence-join handlers now share the
> verifier and executor handler map. Production composition binds PostgreSQL topology history from
> a non-empty state-store DSN and binds metric/evidence handlers only when both a reviewed registry
> and a non-noop provider are available; otherwise, those capabilities remain typed unavailable.
> The structural query extension adds reviewed LinkType forward and reverse roles and semantic
> traits to the exact principal manifest. `query.resource_class_closure` resolves one explicit
> ResourceClass to registry-digested exact ResourceType ids, and `TYPED_PATH` executes 1-8 ordered,
> endpoint-checked directed steps. The verifier rejects unknown classes, stale registries, invalid
> endpoint chains, unbounded repetition, and attempts to reinterpret ordered paths as traversal
> sets before store I/O.
> OQ-07 now projects current connected VNet peering records in the observed direction and private
> endpoint attachments to their exact private-link service targets. Reverse peering still requires
> an independent remote-VNet observation. It also projects `routes_to` only from explicit ARM
> resource next-hop ids; IPs, prefixes, and hostnames never become ontology edges. Snapshot and
> real-time constraints accept the reviewed peering/routing vocabulary. Provider relationship
> extraction now consumes a reviewed mapping catalog that pins property paths, allowed provider
> types, endpoint orientation, source-schema digest, and evidence policy. Only independently
> verified links whose endpoints occur in one complete inventory generation enter the active graph;
> missing endpoints, ambiguous orientation, stale mappings, duplicate or conflicting observations,
> and partial generations remain absent with stable dropped reasons. Workload/service mappings and
> the production network-path issuer remain.
> OQ-04 now has a schema-constrained model seam that proposes a semantic frame and typed node DAG
> from the whole bounded turn and candidate descriptors. Core rebuilds all digests and authority
> fields, verifies the exact principal manifest, and returns a verified plan, one clarification,
> action-draft handoff, unsupported result, or unavailable result. The compatibility coordinator
> can run this path in shadow and records only disposition and content digests. The Azure adapter now
> issues two bounded JSON-object calls through workload identity, validates both proposal schemas,
> and tries resolved candidates in order. Core composition binds that adapter to the exact release,
> current instance store, principal-scoped manifest, deterministic verifier, and request-role-specific
> secured executor when every prerequisite is available. Live cross-service hardening aligns the
> frame proposal's evidence-requirement identifiers with the shared wire contract before Core
> rebuilds the frame digest. Input-free stage and validation diagnostics preserve failure
> attribution without retaining operator text or provider details.
> OQ-05 now deterministically derives an eight-goal intent graph, binds executor receipts to those
> goals, and projects the internal exact-plan contracts to the Console v2/v1 wire shapes. The
> Console accepts explicit cancellation receipts. Executing the semantic plan and attaching these
> projections to the production turn-completion stream remain.
> OQ-06 now restores a service-owned concrete in-memory semantic index with atomic stage, activate,
> stale-generation, typed search, and rollback behavior. A full ontology generation builder emits
> every principal-manifest declaration plus eligible deployment-local object projections, reuses
> unchanged document instances by digest on incremental builds, independently recomputes coverage
> and document roots, and refuses activation until that validation receipt is bound. The durable
> PostgreSQL adapter, scheduled publisher process, and production descriptor-selector binding remain.
> OQ-08 now includes append-only bitemporal topology contracts, a Core-owned migration for retained
> provider generations, object/link revisions and tombstones, deterministic `graph_at` and
> `topology_diff`, late-evidence replay by `known_at`, incomplete-history semantics, and typed query
> handlers with verifier schemas. The PostgreSQL reader/writer binding and inventory-promotion
> revision publisher remain.
> OQ-09 now includes an exact reviewed metric-concept registry without phrase aliases, authoritative
> metric windows, equal-duration comparison that distinguishes zero from missing data, bounded
> metric-series and evidence-join handlers, and topology-aware temporal support/refutation that
> retains competing explanations. Production metric provider bindings and reviewed catalog data
> now include a reviewed alias-free catalog and a concrete `MetricProvider` window adapter that
> preserves observed zero, reports an empty provider result as incomplete, and converts an explicit
> provider failure into an unavailable incomplete window. The causal join remains unresolved and
> grants no execution authority when either metric window is incomplete. Runtime semantic-turn
> composition currently exposes only ObjectSet and pure set/order/project/aggregate handlers.
> Metric-series and evidence-join handlers remain unavailable until their authoritative providers
> are explicitly bound.
> OQ-05 now also includes an async server-side semantic turn runtime that terminates every accepted
> ordinary-language turn as answer, clarification, hold, unsupported, action draft, or cancellation;
> it executes only verified query DAGs and emits exact Console graph/evidence projections.
> OQ-10 changes the synchronous compatibility coordinator default to exact canonical commands only.
> Natural-language aliases, keyword narration, and canonical-string read plans run only when tests
> or an explicit temporary caller select `legacy`; ordinary language belongs to the semantic runtime.
> OQ-11 adds an executable fast gate over all shipped principal manifests and a bilingual competency
> cohort. It requires complete structural accounting, 100% terminal disposition, zero legacy
> ordinary-language routes, zero unsupported claims, and zero unauthorized executions. Answer
> counts remain reported by cohort rather than asserted as universally complete.
> The committed competency cohort uses `receipt_source=deterministic_fixture`. The gate receipt
> keeps local structural validation in `passed` and reports `production_ready=false` whenever any
> question uses deterministic fixture evidence. A caller that claims production completion must
> set `require_production_ready=True` and supply externally produced `cross_service_e2e` or
> `live_assurance` receipts. This keeps the ordinary fast gate runnable in local CI without turning
> a hand-authored fixture into cross-service or live proof. When catalog structure changes, every
> answered fixture must pin the newly computed release and principal-manifest digests before the
> gate can pass; stale deterministic receipts are never accepted by compatibility.
> The `resource_classified_as` and `Forecast`/`Pattern` catalog revisions each refreshed those
> deterministic fixture digests in the same release change.
> The first epistemic-closure implementation slice now ships immutable finite
> `QuestionUniverseReceipt`, typed `EpistemicStatus`, proof-carrying `EpistemicQuestionRecord`, and a
> zero-threshold `EpistemicCoverageReceipt`. The existing coverage gate requires a matching passed
> epistemic receipt before external receipt sources can produce `production_ready=true`. This is a
> release-gate foundation only: generated question universes, runtime understanding/completeness/
> claim receipts, and provider-backed L3/L4 certification remain incomplete.
> OKQ-01 now has the catalog declaration, deterministic ResourceType mapping digests, fail-closed
> classification projection, and single-writer persistence tests for `resource_classified_as`.
> Production inventory-job injection is complete; the resource-to-Rule query function remains.

### Implementation scope

Durable instance search must not reuse Rule `active` or `discovery` pointers. The storage identity must bind principal scope, exact release, authoritative source generation, and embedding space. Only complete authorized projections can build candidates; model output cannot populate observed instances. An accountable owner stages immutable bounded chunks, an independent validator reconstructs their identities, and event-owned compare-and-set activation preserves the previous generation on failure. Invalidation covers deletion, source drift, ACL changes, and release changes; readers recheck those bindings and reauthorize returned IDs against the authoritative graph before presenting facts. This is the revised implementation boundary, not a claim that its durable writer or runtime consumer already exists.

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Fresh authenticated recovery after expired ingress | implemented | `ontology_index_runtime.py`; `test_fresh_authenticated_query_recovers_expired_unprocessed_ingress`; 25 focused runtime/candidate tests, Ruff, strict mypy and diagnostics passed | Request identity now includes the server-issued enrollment time. A fresh authenticated query can start a new bounded attempt; background reconciliation alone cannot renew the expired attempt. Prior request records and deadlines remain immutable, and same-enrollment redelivery retains the same identity. This repair is not credited to the final ten rounds. |
| Bounded authenticated bilingual manifest canaries | in-progress | `test_semantic_manifest_planning.py`: 57 passed; two browser-stream and two actual UI turns matched durable Operator projections; UI artifact digest `sha256:cdca1ddd31dfb0beba3e9e869ab7a1b075f5a877cfb796f3e59385f218053274` | Both languages selected only object declarations and displayed verified evidence with no execution authority. The 1066x576 viewport showed no dialog overflow (client and scroll width 1010). Loaded-source attestation remains unverified; this row cannot close finding 36. |
| Governed local embedding identity | implemented | `scripts/deployment/local/ensure-local-models.py`; `scripts/deployment/local/bind-existing-model.py`; 20 generator and 23 binder tests passed; Ruff and strict mypy passed | Same-name embedding deployments now retain observed model identity. The private binder accepts `--capability t1.embedding` only for the unchanged unbound legacy selection and current deployment readback; other models, candidates, and review policy remain unchanged. This is local tooling, not a live binding or quality receipt. |
| Reconciliation failure isolation | implemented | `ontology_index_runtime.py`; `test_ontology_index_runtime.py`; ten focused runtime/candidate checks passed; Ruff, strict mypy and diagnostics | Expired authenticated enrollments release local caches before publication recovery. Recovery is bounded to five seconds and each of up to eight scopes to three seconds under the existing 30-second cycle. A failed publication or source scope no longer prevents independent scopes from running; the cycle still reports failure rather than silently claiming success. |
| Candidate-only bilingual terminal projection | implemented | `fdai_core_service/semantic_instance_candidates.py`; service processor and real semantic composition tests: 252 passed; Ruff, strict mypy and editor diagnostics | Exact Function, principal, release, query, digest, receipt and candidate-accounting checks precede presentation. Empty, single, and truncated candidates render identity/type/revision only in English and Korean, remain partial/non-exhaustive, and grant no execution authority. Cross-scope or authority substitutions hold. Existing Rule, Incident, table and extension projections remain compatible. This is local pipeline evidence, not authenticated Console or relevance certification. |
| Production index bootstrap and reconciliation | in-progress | `runtime/ontology_index_runtime.py`; semantic, Pantheon, task, and control-loop bootstrap; 178 focused candidate/composition/bootstrap checks and 25 runtime-construction checks passed | The same source-derived declaration reaches default release and semantic composition. Governed model identity gates real adapter construction. Exact authenticated principal/group scope enrolls without embedding; supervised off-path work publishes immutable preparation/invalidation requests and restores audited caches. Actual factory-to-Pantheon tests cover source-change withdrawal/rebuild, stored-request substitution, enrollment expiry, and denied unqualified semantic ranking. Candidate-specific terminal presentation, further restart/failure isolation checks, and real quality/live evidence remain open. |
| Original preparation request deadline | implemented | `ontology_index_workers.py`; 23 candidate and Function checks passed; Ruff and strict mypy | Stale or future ingress is refused before source/model work. Source and vector preparation share the original 120-second deadline; its remaining-time timeout cancels unfinished I/O. Heimdall rejects expired source validation, and delayed validation cannot renew the activation command deadline. A timed-out preparation leaves the pointer unchanged. These are implementation checks, not post-implementation critique rounds. |
| Exact-release semantic runtime candidate binding | implemented | `semantic_query_instance_candidates.py`; `wire_semantic_query.py`; Azure facade; 61 focused composition regressions passed | An explicit callback and the matching source-derived declaration traverse semantic planning, full manifest verification, contextual Function execution, and authenticated principal binding. An absent callback remains unbound; missing or changed read sets fail before I/O. The Azure facade forwards the callback without model or identity calls. Default bootstrap, source enrollment/reconciliation, candidate-specific terminal projection, and real quality qualification are not yet connected. |
| Bounded principal-isolated candidate preparation | implemented | `ontology_candidate_reader.py`; `ontology_index_workers.py`; 23 focused candidate and Function contract checks passed; Ruff and strict mypy | Prepared generations are keyed by principal, role, and purpose. Independent preparations do not invalidate each other; owner events invalidate only their principal. Eight scopes and 20,000 total cached object documents are the default ceilings, with at most 32 configured scopes. Capacity failure preserves existing caches, cancellation releases pending capacity, and cached metadata no longer retains the complete declaration corpus. Production enrollment, composition, recovery, and quality qualification remain open. |
| Typed instance-candidate Function contract | implemented | `instance_candidate_queries.py`; `ontology_index_workers.py`; 22 focused registry and real-worker candidate checks passed; strict mypy and Ruff | The unregistered Function declares explicit object read sets, accepts only authenticated Bragi operations-review context, and binds principal, release, query, complete result digest, bounded candidate accounting, and no execution authority. The worker projects current secured graph facts from the audited active pointer. Search retains its original release identity instead of resolving a second source to label old results. Production registration, candidate-specific answer projection, multi-principal preparation, and source-change reconciliation remain open; no exact-release catalog or runtime capability was activated. |
| Owned invalidation, rollback, and reclamation | implemented | `ontology_index_workers.py`; `ontology_index_retention.py`; actual owner-event lifecycle and loopback PostgreSQL retirement/retry checks | Typed non-build transitions traverse Huginn, Muninn, Heimdall, and Saga. Rollback requires retained current-source-compatible data; invalidation withdraws current search. Retirement fences the source snapshot identity, so rehashed validation cannot resurrect it. Only the exact retired, terminal-audited generation's derived namespaces are reclaimed, retaining tombstones, Rule state, and audit/command history. Production bootstrap and automated source-change triggers remain open. |
| Concrete ContextIndex worker and journal chain | implemented | `ontology_index_workers.py`; `ontology_index_journal.py`; actual source-to-reader event-chain and replay tests | Muninn preparation, independent Heimdall graph/vector-binding validation, Saga intent and terminal seals, and exact pointer CAS now use the real local adapters through owned topics. Admission rechecks current source; current-index consumption requires matching terminal audit and a stable pointer. Immutable phase results retain pending publication and support bounded owner-local recovery. Model output in these tests is synthetic. Production bootstrap, event-owned retirement/invalidation/rollback, physical reclamation, and real relevance qualification remain open. |
| ContextIndex runtime owner subscriptions | implemented | `agents/_framework/ontology_index.py`; Pantheon runtime, ownership, topics, doc parity, and layout checks: 118 passed; strict mypy | Actual typed ingress traverses Muninn, Heimdall, and Saga subscriptions in order; Saga audit precedes seal publication. Wrong producer, topic, phase, content, and authority fail closed without exposing raw invalid bodies. Ordinary judgment, Rule, document, and learning families stay separate. The tested mechanical callbacks are synthetic; source/lifecycle worker injection and production bootstrap remain open. |
| Isolated pointer transition mechanics | implemented | `ontology_index_lifecycle.py`; `test_ontology_index_lifecycle.py`; 18 focused checks including two-connection local PostgreSQL CAS and reconnection; Ruff and strict mypy | Strict scope, command expiry, expected prior identity, and injected independent admission precede pointer mutation. The pointer and terminal result commit atomically; restart recovers the outcome before a successor overwrites it. Exact replay, invalidation, retained rollback, and retirement barriers are covered. The injected admission in these tests is synthetic: real Heimdall/Saga evidence, agent events, terminal publication, physical reclamation, and production composition remain open. |
| Prepared candidate reader and current-graph reauthorization | implemented | `ontology_candidate_reader.py`; `ontology_candidate_authorization.py`; 103 candidate, vector, snapshot, and secured-gateway checks passed with stable input hashes; Ruff and strict mypy passed | The off-path reader loads one independently source-validated immutable generation. Query ranking never reloads its corpus, exact IDs bypass query embedding, and selected facts are rebuilt through current secured ObjectSets. Deletion, changed content, source drift, duplicates, and concurrent invalidation hold. Empty candidate results do not establish absence. Durable active-pointer selection and production agent/runtime binding remain open. |
| Source-bound inactive vector persistence | implemented | `ontology_vector_store.py`; `test_ontology_vectors.py`; vector and source-snapshot checks: 50 passed, including local PostgreSQL reconnection and tamper rejection | Immutable vector rows bind the exact source snapshot, generation, and configured embedding identity. Completion is published last and pinned across retries; bounded point reads do not reload other vectors. Interrupted work resumes, parent cancellation reaches the provider, and raw provider errors are excluded. This is neither embedding-quality certification nor an active search index. Owner-event lifecycle, current-graph reauthorization, retention, and the runtime candidate consumer remain open. |
| Independent current-source staging validation | implemented | `ontology_snapshot_validation.py`; 105 gateway, ObjectSet and generation checks including local PostgreSQL passed | Reconstructs every candidate from a new complete secured graph read and compares exact content, source, principal, release and expected embedding identities. Fabricated, missing, modified or drifted objects are rejected. Precomputed vectors and index identities are not certified. This is validation-only evidence; the Heimdall event subscriber and Muninn activation consumer remain unbound. |
| Complete multi-type staging source | implemented | `scan_snapshot`; `stage_manifest_from_gateway`; 95 focused gateway, ObjectSet, and generation checks including local PostgreSQL | Reads all manifest ObjectTypes in one source snapshot, preserves ACL and source/release/principal bindings, and fits objects plus declarations within 20,000 documents. Ordinary ObjectSets retain their 1,000 limit. No paging or mixed-generation merge is used; overflow holds before writes. Event-owned activation, retention, durable vector retrieval, and current-graph candidate reauthorization remain open. |
| Secured bounded ontology staging writer | implemented | `OntologyGenerationSnapshotStore.stage_from_gateway`; gateway and generation checks: 68 passed, including local PostgreSQL persistence | Fetches one unfiltered ObjectType through the existing principal/purpose ACL gateway; requires exact release, source generation, complete non-truncated evidence, and visible identities before writes. Removes fields identified by the serialized redaction map and pins the exact projected-result digest. Readers must supply that digest. The existing 1,000-object ceiling remains; multi-type/paged production, accountable event activation, and runtime candidate retrieval are not implemented by this writer. |
| Isolated immutable ontology staging | implemented | `ontology_snapshot_store.py`; 38 generation tests and one local PostgreSQL reconnection/tamper test passed; Ruff and strict mypy | Service-owned StateStore chunks use a separate content-addressed namespace, bounded at 128 rows/512 KiB per chunk and 64 MiB per snapshot. The last-write completion header binds principal, manifest, source generation, release, and embedding metadata. Readers revalidate content; interrupted writes remain invisible. This is staging, not a whole-corpus search index. Authorized source production, retention, event-owned activation, and runtime retrieval remain open. |
| Azure and Incident semantic judgment | validated | `semantic_judgment.py`; `semantic_judgment_grounding.py`; shadow prompts v9-v14 and v41; exact-source cohort `a5d3627b3` | The 16-case v14 shadow cohort reached 100% for primary intent, target extraction, span validity, clarification precision, and secondary recall while every safety counter remained zero. Strict provider schemas retain explicit descriptions for the eight-item ambiguity bounds and the required single clarification question when unsupported length keywords are removed; server validation remains authoritative. This validates the bounded judgment cohort, not enforce promotion or frame-plan production readiness. |
| Provenance-bound operational preflight | validated | `conversation-preflight.v9.yaml`; `conversation_preflight.py`; `conversation_preflight_validation.py`; `semantic_planning.py`; 360 focused tests; authenticated exact-prompt replay | Reviewed operational shapes can remove one serial full-judgment call. Resource collections require deterministic ontology or state-catalog grounding, including state-only collections. Low-confidence, contextual, stale, malformed, unsupported, identity-mismatched, or ungrounded proposals retain full judgment or typed clarification before frame/provider I/O. |
| Semantic wire and Core processing | implemented | `semantic_turn.py`, `semantic_turn_consumer.py`, `semantic_turn_processor.py`; focused semantic tests passed 88 cases | Version 1.2 requests are bounded to 90 seconds, results are idempotent, claims are recoverable, and Rule results remain candidate-only with no execution authority. |
| Operator persistence and Rule projection | implemented | `semantic_turn.py`, `semantic_turn_runtime.py`, `postgres_semantic_turn_store.py`, `test_semantic_turn_bridge.py`; focused semantic tests and a rollback-only PostgreSQL transaction probe | A valid caller-supplied request UUID is preserved through the semantic envelope and correlation identity while the idempotency key remains separate. The Kafka partition key uses the server-derived opaque session reference, so same-session turns retain order and different sessions remain independently schedulable without exposing the raw session id. An omitted request UUID uses the retry-stable deterministic fallback. An explicit Incident context is accepted only when it carries both `incident_id` and `correlation_id`; partial identity is rejected rather than downgraded to an unbound turn. Outbox and result leases are recoverable, malformed ownership fails closed, replay ordering is timestamp-aware, and exact Rule reads are isolated by principal and query digest. `SemanticTurnBridge` remains gated on the authoritative store and semantic transport, while the local narrator's periodic refresh remains an independent Operator lifecycle service when that narrator is configured. |
| Receipt-bound answer delivery | implemented | `semantic_turn_runtime.py`; `backend-stream.ts`; `test_semantic_turn_bridge.py`; `backend-stream-fallback.test.ts`; cross-service round-trip checks | A verified semantic answer streams as cumulative `confirmed` segments bound to its semantic receipt and evidence references. Segment-specific replay resumes after the acknowledged segment. The Console rejects receipt, evidence, text, or terminal mismatches instead of presenting conflicting content. Exported documents and answers without the verified receipt contract remain advisory token streams. |
| Historical topology persistence and publishing | implemented | `inventory_topology_history.py`, `postgres_topology_history.py`, `inventory_sync_cli.py`; focused inventory and topology tests passed 31 cases | Complete promoted observations append bitemporal revisions atomically. Historical and current derived writes are attempted independently, while incomplete observations cannot establish a complete historical baseline. |
| Temporal, metric, and evidence provider composition | implemented | `wire_semantic_query.py`, `bootstrap.py`, `bootstrap_bindings.py`, `test_wire_semantic_query.py`, `test_bootstrap_config.py`; focused composition and provider-selection tests passed 16 cases | One handler map controls verifier availability and execution. Production binds PostgreSQL history from the state-store DSN and binds metric/evidence handlers only for a reviewed registry plus a non-noop provider. |
| Decision-critical secured query admission | implemented | `query_receipt_authority.py`, `query_source_handlers.py`, `wire_semantic_query.py`; focused authority, FunctionType, and composition tests | Issued results remain available for diagnosis, but `verify` and `resolve` require a current shared decision-evidence admission matching the projected result digest, role-and-purpose scope, ontology release, and source generation. Composition exposes an admission-provider seam. It remains unbound by default, so a decision-critical Function holds instead of trusting a self-issued query receipt. |
| Lifecycle-free declaration coverage | implemented | `object-type-lifecycle-classification.yaml`; strict ontology loader and bilingual parity checks | Every lifecycle-free ObjectType is classified exactly once. A new, removed, duplicate, or stale entry blocks catalog loading instead of silently changing ownership semantics. |
| Governed production assurance | in-progress | [Ontology Query Randomized Assurance](../../roadmap/interfaces/ontology-query-randomized-assurance.md) and the verified baseline gap table below | Local checks prove fail-closed composition, but no governed live cross-service receipt proves production readiness. |
| Typed Console assurance runners | implemented | `console-routes.spec.ts`; `ontology-query-assurance.ts`; `ontology-query-assurance.spec.ts`; focused Console checks | One runner verifies publication, Core processing, exact projection read, and authenticated receipt rendering. The seeded 100-turn runner covers 50 English and 50 Korean prompts with a typed-only oracle. Neither implementation is live evidence until its retained artifact passes. |
| T1 clarification and frame-plan alignment | implemented | `semantic_planning_models.py`; `semantic_planning_cascade.py`; `semantic_planning_frame.py`; `semantic_planning_frame_checks.py`; `semantic_planning_alignment.py`; focused planner checks | Frame proposals classify missing user context with bounded `clarification_requirements`. A legitimate T1 clarification terminates without T2. A typed non-direct discourse result terminates before model frame selection and server-owned query fallback. Focused deterministic helpers bind server-owned clarification context and reject a plan that changes the accepted frame or exact capability family. Requests for server-bound context, ambiguous or mixed targets, invalid schemas, and deterministic frame-plan mismatches fail closed without T2; only typed T1 unavailability may use the interactive runtime's bounded fallback. |
| Resource identity clarification presentation | implemented | `semantic_planning_frame_gate.py`; focused English and Korean semantic-planning regressions | A sole `resource_identity` unresolved term with a typed subject or resource-identity requirement produces a localized request for an exact resource name or ID. Model-authored machine tokens cannot leak into operator-facing clarification, and the path performs no provider read or action. |
| Exact semantic prompt profiles and recovery budget | implemented | `core/prompts/profiles.py`; `semantic_query_azure_composition.py`; `semantic_planning_judgment.py`; `semantic_operational_summary_planning.py`; Azure semantic planning, assurance, ontology, proposer, RCA, and review adapters; combined checks (`804 passed`) | Exact active artifacts, final transmitted requests, generated schemas, recovery directives, and rejected assurance requests carry bounded replay evidence. An accepted bound `query.incident_evidence` intent builds its read-only frame and plan deterministically without another model call. If incident frame modeling is still required, it receives only the `Incident` and `query.incident_evidence` descriptors instead of the complete principal manifest; final plan verification always uses that complete manifest. General T2 frame recovery uses a separate compact role instead of appending to the 33,000-character legacy frame. Compact judgment, frame, and plan treatments remain shadow. |
| Ontology manifest listing and declaration cardinality | implemented | `semantic_manifest_planning.py`; `semantic_planning_frame_checks.py`; `semantic_planning_plan_dispatch.py`; `semantic_turn_processor.py`; focused judgment, planning, and bilingual presentation regressions | A validated no-target schema-list request compiles one exact declaration kind into a server-owned `query.manifest` list without another frame or plan model. Existing explicit visibility/current-scope forms remain supported. Compact plural-kind facets with `available`, `queryable`, or `readable`, either separate or combined, use the same current principal manifest even without a redundant scope facet. These machine fields never expand access or infer intent from utterance text. Mixed declaration kinds, ambiguity, secondary intents, and action authority cannot enter this compact path. For a declaration count, one canonical `*Type` target takes precedence over a conflicting frame subject, while non-declaration domain targets are ignored. Complete output renders typed rows or counts and their read-only source, binding the display type to the verified frame rather than a model-authored node id. Unavailable manifest functions, incomplete output, and other measures retain the existing fail-closed behavior. |
| Localized Service Health answer metadata | implemented | `semantic_service_health_format.py`; `semantic_service_health_answer.py`; focused Service Health answer regressions (`13 passed`); Ruff and strict mypy | English and Korean answers use readable labels for reviewed event category, active status, severity, and source completeness. Provider advisory titles remain unchanged evidence text, and unknown provider values remain explicit instead of being guessed. |
| Composite service-to-Agent instance paths | implemented | `ontology_query.py`; `query_gateway.py`; `query_source_handlers.py`; `semantic_relationship_planning.py`; focused contract, query, planner, execution, and assurance checks (`451 passed`) | One consistent graph snapshot preserves each concrete service, workload, resource, and owning Agent path. The terminal receipt binds exact manifest and inventory inputs, principal scope, release, cutoff, source generation, path definition, and result digest. Empty paths make no identity claim, and ownership never grants execution permission. |
| Exact logical-service runtime state | implemented | `semantic_logical_service_planning.py`; `semantic_logical_service_answer.py`; operating-model provider and runtime topic bindings; focused frame, verifier, full DAG, provider, runtime projection, deployment, local parity, and bilingual presentation checks | One exact `BusinessService` or `Workload` id, name, or approved alias resolves through `implemented_by` and `workload_runs_on` to App Service, Container Apps, and Kubernetes Deployment, Service, and Pod Resources. The server-owned plan reads only verified current Resource state. Duplicate identity holds before graph I/O; missing or incomplete mappings never widen to provider names, tags, labels, or fuzzy matches. Presentation reports component observations without inferring aggregate health or cause and keeps `execution_authority=false`. |
| Collection Resource state and evidence-family isolation | implemented | `semantic_resource_state_planning.py`; `resource_state_queries.py`; `semantic_planning_value_filters.py`; `inventory-query-language.yaml`; focused planner, FunctionType, composition, prompt, and presentation checks; full Python regression shard 2 passed 7,294 cases | A server-owned FunctionType filters a secured Resource collection using complete observed `StateFactMetadata`. It accepts the current property-keyed metadata envelope and the retained legacy flat form. The ObjectSet first limits broad reads to manifest-declared Resource types with reviewed operational-state paths and state metadata, preventing unrelated inventory volume from becoming a false empty state result. Typed positive filters and one declared-state exclusion compile to a closed server-owned predicate, while verified rows project only observed provider-neutral `resource_group` and `region` values and preserve missing scope as unavailable. If source coverage is incomplete, verified positive matches remain visible with incomplete posture while zero rows cannot establish absence. The schema-validated language registry keeps current inventory state separate from subscription health, and unbound health, metric, history, contextual, and coverage families return a typed hold instead of unrelated Resource rows. A current-state collection that also requires state-change history holds before provider I/O until a verified join capability is bound. Local authenticated Console observations support the implementation claim but are not governed release evidence. |
| Targetless Resource subtype candidate contract | validated | `semantic_target_candidate_planning.py`; `inventory_query_language.py`; `inventory-query-language.yaml`; focused planning cohort (`204 passed`); authenticated Korean Console matrix | Core converts a singular subtype request without one exact identity into one secured type-filtered ObjectSet and preserves `execution_authority=false`. The schema-validated cardinality signal cannot select a FunctionType, collection always wins over singular, and exact identities bypass candidate reduction. Eight current-source SRE example first turns completed as verified list or candidate answers with every hard-zero presentation counter at zero. Exact-target capability completion remains open. |
| Kubernetes workload target selection and assessment | implemented | Kubernetes rollout, Pod recovery, and Resource event-history planners and FunctionTypes; reviewed metric semantics; provider readers; focused checks; authenticated targetless and exact-target Console receipts | Reviewed terms ground S12 and S1 targets. Exact Deployment and Pod plans verify ownership and same-UID restart deltas, preserve `source_incomplete`, and keep cause and execution authority false. A descriptor-advertised `resource_event.kubernetes` frame compiles the existing two-node plan only when its typed judgment duration equals the bounded frame lookback. When one source-grounded Resource name is present, the server plan preserves it as an additional exact predicate rather than widening to the complete Resource type. The Function can use a receipt-bound UID selector for one exact current child; missing or mismatched identity remains incomplete before provider I/O. Explicit cluster scope remains the only deleted-object history path. The answer names observed rows, source completeness, and the exact limitation; `source_retention_unverified` explicitly states that zero rows do not prove historical absence. Authenticated execution reached the exact Event Function and rendered `source_unavailable`, so successful provider access and durable retention remain open. |
| Exact-target metric series and presentation | validated | `semantic_resource_metric_planning.py`; `resource_metric_queries.py`; `wire_semantic_query.py`; `presentation_artifact_v2.py`; focused checks; authenticated standard-port Console and Browser evidence | The explicit visualization request used `target_resource_metric_series` and `query.resource_metric_series` without T2. The FunctionType returned 20/20 complete endpoint and min/max envelope rows from 1085 source samples with `display_truncated=false`, and Operator compiled the verified time-series block plus exact-values fallback. Aggregate requests remain on `query.resource_metric_inventory`, and the result grants no execution authority. |
| Explicit filtered-resource grounding | implemented | `semantic_planning_value_filters.py`; `semantic_resource_visibility.py`; focused planning and visibility checks | Core preserves every catalog-declared value filter stated in the utterance and one exact free-text subject carried by the frame. It only adds narrowing predicates, requires the subject to occur verbatim in the utterance, and grants no execution authority. Every operational Resource collection excludes `authorization.role-assignment` through one server-owned post-verification filter, while explicit IAM relationship queries retain their dedicated evidence path. |
| Exact-target health evidence assessment | validated | `semantic_health_planning.py`; `resource_health_assessment_queries.py`; production semantic composition; focused checks and authenticated Console receipt | The semantic runtime corrects a preserved read-only health frame only when one exact target and explicit evidence or health-axis wording are present. Core compiles seven bounded nodes and a deterministic assessment FunctionType. Incomplete readiness, application work, dependency, process restart, memory, log, or freshness evidence remains an explicit limitation and cannot be reported as healthy. The same-question runtime completed 7/7 nodes and 13/13 evidence checks with no T2 or unrelated rows. |
| Exact-target request-error and Activity Log correlation | validated | `semantic_error_activity_planning.py`; `resource_error_activity_correlation_queries.py`; Azure metric and read-investigation providers; focused checks and authenticated Console receipt | Core compiles one exact Resource read, equal baseline/current `request.errors` windows, the same-window Activity Log read, and a deterministic no-cause reducer. Complete zero remains distinct from missing evidence, co-occurrence never becomes causation, and no model-authored provider command or execution authority is accepted. The post-restart same-question run completed 5/5 nodes and 11/11 evidence checks; Activity Log returned a verified zero while the unavailable request-error mapping remained an explicit gap. |
| Typed extension answer projection | implemented | `semantic_turn_processor.py`; focused Core processor tests | Verified `TopologyGraphAt`, `TopologyDiff`, `MetricWindow`, and `CausalEvidenceJoin` outputs render as bounded summaries with exact digests, completeness, counts, limitations, and `execution_authority=false`. Raw provider payloads remain excluded and evidence references continue through the existing receipt path. |
| Principal-scoped schema inventory | implemented | `core/ontology_platform/manifest_queries.py`; `composition/wire_semantic_query.py`; focused manifest, handler, composition, and prompt checks (`42 passed`) | `query.manifest` answers declaration inventory questions from the exact role- and purpose-filtered manifest through a bounded generic table and one invocation receipt. It never substitutes arbitrary relationship or instance queries. |
| Incident semantic evidence | implemented | `core/incident/ontology_projection.py`; `core/ontology_platform/incident_queries.py`; focused Core, Operator, and Console checks | Canonical Incident state is queryable as an ObjectSet, T0 records bounded impact evidence, and `query.incident_evidence` preserves distinct canonical incident and audit correlation identities while returning an audit-backed profile, correlated records, a citation-grounded recorded root cause or allowlisted deterministic terminal failure, impact evidence, citations, and explicit gaps. The path remains read-only and grants no execution authority. Authenticated Console evidence remains open. |
| Receipt-bound semantic answer authority | implemented | `functions.py`; `query_execution.py`; `intent_graph.py`; `semantic_turn_processor.py`; `semantic_turn_presentation.py`; focused Core, Operator, and cross-service tests passed | The server function registry is the first authority producer. Query-node and goal receipts keep authority with their evidence references. Subscription health, inventory graph, metering, and ontology manifest remain distinct. Missing or conflicting authorities hold the turn, and model or client authority text cannot override the receipt. |
| Semantic model failure isolation | implemented | `runtime/{readiness,semantic_model_identity}.py`; `semantic_turn_{consumer,processor}.py`; `postgres_semantic_turn_store.py`; `verification-presentation.ts`; focused Core, Operator, and Console checks | General identity loss now degrades authority instead of blocking transport. A five-second model-audience preflight returns `semantic_model_identity_unavailable`, and frame-provider exhaustion remains `semantic_frame_unavailable` with `semantic_planner_unavailable` instead of being mislabeled as an evidence read. Console gives the corresponding recovery step, and the first terminal result fences later competing projections. No lexical natural-language fallback was added. |
| Named resource-group membership and collection filtering | validated | `semantic_judgment.py`; `semantic_planning_frame_{checks,normalization}.py`; `semantic_target_candidate_planning.py`; `semantic_planning_specialized_plans.py`; focused judgment and planning checks; authenticated local Console evidence | Schema-validated `query.contextual_resources` judgment with one grounded, exact `resource_group` name builds `Resource.parent_id` before model frame proposal. A `resource_group` target carrying `resource_collection`, `list`, and `name_filter` facets instead filters Resource objects by the grounded name fragment and `Resource.type=resource-group`; collection intent cannot become child-resource membership. A target value that is itself a declared resource-group type term also uses the type filter, so a generic list cannot become an unfiltered membership query. Incompatible `query.resource_current_state + resource_group` judgment is repaired first. Generic membership applies the reviewed `type != authorization.role-assignment` exclusion while explicit IAM questions stay separate. The authenticated named-group turn returned 24 verified members with no role-assignment row. |
| Operational query integrity | implemented | `semantic_target_{candidate_planning,identity}.py`; `semantic_current_state_planning.py`; `semantic_resource_visibility.py`; `conversation_preflight.py`; Operator semantic store and Kafka adapter; 312 focused Core and prompt tests plus 1,559 Operator tests; targeted Ruff and strict mypy | Unbound collection filters return clarification instead of broad Resource data, exact ARM identities retain `Resource.id`, and one server-owned visibility rule excludes role-assignment objects from all operational Resource collections while preserving IAM evidence. Compact descriptors and deterministic collection frames reduce avoidable model context. Malformed preflight receives one bounded repair attempt. Operator replay honors its requested limit, classifies Rule projection identity conflict separately from a missing request, and rejects physical-topic collision with every configured logical topic. |
| Subscription query model context | validated | `conversation-preflight.v6.yaml`; `conversation_preflight.py`; `semantic_planning_judgment.py`; `semantic_operational_summary_planning.py`; focused scope and judgment tests; issue #151 runtime receipt | Accepted subscription identity reuses one provenance-bound preflight judgment, then builds a deterministic frame and server plan from the exact no-input FunctionType. A rejected, low-confidence, malformed, or nonmatching judgment cannot fall through to that summary. The authenticated receipt completed the exact `subscription_scope_identity` read with one evidence reference and no execution authority. |
| Evidence-free direct semantic response | validated | `semantic_planning.py`; `semantic_runtime.py`; `semantic_turn.py`; `semantic_turn_processor.py`; 586 focused Python checks, 117 focused Console checks, and an authenticated fresh-conversation Browser turn | A validated greeting intent terminates before frame and plan proposal, crosses `core-operator-projection` 1.4 as `direct_response`, and carries localized text without query digests, evidence references, verification claims, or execution authority. |
| ResourceClass closure and ordered typed paths | implemented | [Ontology Structural Model](../../roadmap/architecture/ontology-structural-model.md); `resource_class_closure.py`; `models.py`; `query_source_handlers.py`; `query_verification.py`; `wire_semantic_query.py`; focused catalog, verifier, executor, and composition checks | The principal manifest exposes the no-authority closure FunctionType and LinkType roles and traits. Closure expands only explicit catalog ids, while `TYPED_PATH` preserves step order, endpoint types, bounds, release identity, and incomplete-evidence holds. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-26 | implemented | Replaced a model-authored `resource_identity` token in a direct network-path clarification with deterministic English and Korean display wording derived from the typed requirement. | `current change`; direct authenticated question-bank observation; focused bilingual semantic-planning regressions. | Verify with a distinct future resource-identity clarification after Core restart; the measured question is not retried. |
| 2026-09-26 | implemented | Extended the clarification projection across both typed subject and resource-identity requirements after a distinct Korean configuration-drift question reproduced the raw token. | `current change`; focused four-case bilingual regression. | Restart Core and verify the subject variant with a distinct question; the measured question is not retried. |
| 2026-09-26 | implemented | Localized Service Health event metadata and source completeness in English and Korean answers while retaining provider advisory titles verbatim. | `current change`; focused Service Health answer tests (`13 passed`); Ruff and strict mypy passed. | Restart Core and verify one new Korean browser answer without retrying the measured question. |
| 2026-09-22 | validated | Reused a provenance-bound Resource collection preflight only inside capability-aware planning after deterministic type or current-state grounding. Added exact state-only coverage for `Show resources that are currently not running.` without lexical fallback or scope broadening. | `current change`; the exact regression failed before repair; 360 planning and preflight tests passed; Ruff and strict production mypy passed; authenticated replay returned `answered`, `server_inventory_graph`, five verified claims, and no planning hold. | The answer remains honestly partial while current inventory source completeness is partial; governed release evidence remains separate. |
| 2026-09-22 | in-progress | Separated the failed declaration diagnostic from an actual instance-document-shaped pilot. Canonical synthetic object serialization passed offline checks; a fixed policy chosen on eight calibration cases passed all 12 disjoint held-out cases using the same observed model version. | `current change`; 24 embedding calls total, including four synthetic documents and 20 queries; dataset/policy digest `sha256:5ac35d9d46650516b02e4c994588c0ef2bafd9c64898ff4a803a1f94d84d4266`; calibration 8/8 and held-out 12/12; `production_qualification=false`. | Expand ObjectType/corpus coverage and independent cohort sample counts, verify current-source authorization through the actual reader, and complete governed policy activation. The older declaration failure remains failed. |
| 2026-09-22 | in-progress | Exercised actual Console input and Send controls in separate EN/KO general conversations. Both rendered verified object-only results, matching source evidence, and the no-authority notice. Exact durable projections independently confirmed query scope and verification. | `current change`; two visible UI turns, `semantic_answer_verified`, 1/1 checks, model `narrator-gpt-5-4-mini`, no horizontal dialog overflow; artifact digest `sha256:cdca1ddd31dfb0beba3e9e869ab7a1b075f5a877cfb796f3e59385f218053274`. | Attest the exact running immutable source after managed activation. This closes the DOM observation gap for these two canaries, not global query certification or instance relevance. |
| 2026-09-22 | in-progress | Tried the existing managed restart through a fresh VS Code task definition after the ordinary task launcher failed. Task creation returned `Task not found`; no task file change or process replacement occurred. Restored only the attempt's generated model update after comparing it with the exact original backup. | `current change`; temporary task definition absent; original active model SHA-256 pin restored; validated candidate preserved privately; no process kill, cloud mutation, or authentication substitution. | An operator must restore VS Code task execution or run the existing managed launcher with the preserved candidate; then verify its model pin and process source before runtime acceptance. |
| 2026-09-22 | implemented | Reproduced permanent refusal after an unprocessed request expired, even when the user made a fresh authenticated query. Included the server-issued enrollment time in request identity without extending old deadlines or changing principal scope and lifecycle admission. | `current change`; the focused regression failed before repair; 25 runtime/candidate checks passed, including no automatic renewal, fresh-query recovery, immutable original request and stable new-request replay; Ruff, strict mypy and diagnostics passed. | Complete representative retrieval qualification and managed runtime activation, then retain source-attested UI evidence and the final post-implementation ten-round review. |
| 2026-09-22 | in-progress | Ran one authorized real-embedding calibration against fresh observed versioned identity. Preserved frozen policy candidates and diagnostic synthetic vectors for offline analysis. No policy achieved 16/16, so the independent cohort stayed unopened and semantic ranking stayed disabled. | `current change`; 16 calibration queries plus four synthetic document embeddings; best 15/16; frozen cohort/policy digest `sha256:7374caeec3bc8b5ef15ddb221dc62cc1c1a224775cb1ef9b9e5b4a7ca4fe91e6`; model identity digest `sha256:52a633c781f8c8d5a8d023867b878c303debb812adb6a0980520c64b0464ae0a`. | Revise the retrieval representation or scoring with a documented hypothesis and representative corpus, then pass frozen calibration and disjoint positive, negative, ambiguous and adversarial evaluation. Do not treat four declaration fixtures as instance-search certification. |
| 2026-09-22 | in-progress | Verified two authenticated EN/KO manifest browser-stream turns against their exact durable Operator records. Both returned object-only scope, verified evidence and no execution authority. | `current change`; 57 focused manifest-planning tests passed; 2/2 live canaries verified, about 5.2 and 4.9 seconds; sanitized artifact digest `sha256:3f39bdb49f81c0f5940a9ec826ccc40215c7d15c1cfac3d9b43ef1a0bd4f0a9d`. | Complete loaded-process source attestation and actual Console rendering before closing authenticated end-to-end certification. Final ten post-implementation rounds remain open. |
| 2026-09-22 | in-progress | Applied the observed embedding metadata with the existing backup-preserving binder, but the managed VS Code restart task returned no terminal. The runtime digest remained old despite 12/12 service readiness. Rolled back only the attempt's model-file change after checking for unrelated edits and restored the exact original digest. | `current change`; generated candidate parsed and real provider calls succeeded; active-file rollback verified against its prior SHA-256 pin; no individual process kill, cloud mutation, or authentication substitution. | Restore managed VS Code task execution, reapply fresh observed metadata and verify the new process loads the exact model pin. A healthy old process does not prove activation. Quality-gated semantic search remains unavailable. |
| 2026-09-22 | validated | Corrected the subscription-mismatch diagnosis recorded in `3cc727c307`. The configured subscription field is an all-zero placeholder. Read-only Azure observation matched the configured endpoint to exactly one account in the current subscription and confirmed the selected embedding deployment with model version `1`. | `current change`; artifact digest matched its configured pin; explicit current-subscription account read and deployment read; `verified_same_subscription`; zero context switches, model calls, or cloud mutations. | Bind observed model metadata locally and complete real quality and authenticated EN/KO evidence before the final ten rounds. The earlier target-drift statement is superseded, not evidence of an actual foreign subscription. |
| 2026-09-22 | in-progress | Resumed the authorized live-validation preflight, then held on a mismatch between the current CLI subscription and the selected local model artifact. Preserved the target, role, authentication, and runtime boundaries. | `3b03b73ffb`; local readiness 12/12 and signed-in shared Console; identity preflight `selected_subscription_mismatch`; zero model calls and zero cloud mutations. Independent local DB readback found zero remaining temporary catalog schemas. | Obtain the intended target choice, verify its exact deployment and model identity, then run frozen quality and authenticated EN/KO checks before the final ten rounds. No overall Medium-or-higher closure is claimed. |
| 2026-09-22 | validated | Replaced the pending Rule PostgreSQL evidence with actual isolated execution using the existing local validation database authority. No privileges were added to service roles. | `current change`; `test_postgres_integration.py`: 3 passed, covering bilingual ranking parity, exact-ID model independence, cache tampering and manifest-bound lifecycle. | Real embedding quality, authenticated bilingual certification, and the final post-implementation review gate remain open. |
| 2026-09-22 | implemented | Reproduced missing same-name embedding endpoint metadata and repaired the local generator. Added a constrained existing-artifact embedding mode that seals current readback without changing model selection or other capability policy. | `current change`; the regression failed before repair; generator tests 20 passed, binder tests 23 passed; Ruff and strict mypy passed. | Observe the selected deployment and bind its actual model version before real qualification. Do not invent provenance or enable unqualified ranking. |
| 2026-09-21 | implemented | Reconciliation reproduced original capability-tail and large-property-token loss in current source. Preserved complete canonical property tokens and made byte overflow an explicit pre-judgment failure. Corrected owning designs that allowed omission. | `current change`; both regressions failed before repair; 288 focused planner, query-hardening, manifest and descriptor checks passed. | Reconcile all original findings, complete real quality and authenticated evidence, then satisfy the final post-implementation review gate. The earlier ten local index rounds remain history, not overall acceptance. |
| 2026-09-21 | implemented | Reproduced first-scope starvation and publication failure blocking enrollment cleanup. Moved expiry cleanup before recovery and bounded independent scope work before reporting the first failure. | `current change`; both regressions failed before repair; ten focused runtime/candidate tests, Ruff, strict mypy and diagnostics passed. | Review interrupted request recovery and qualify the real semantic-ranking policy; authenticated Console evidence, all-finding reconciliation, and ten final post-implementation rounds remain open. |
| 2026-09-21 | implemented | Added a dedicated candidate-only final projection and bilingual rendering through the actual semantic DAG-to-Core terminal path. Rejected scope/release/query/authority/accounting substitutions and preserved existing non-candidate projection compatibility. | `current change`; 252 semantic composition and service processor checks passed; Ruff, strict mypy and diagnostics passed. | Review startup/source failure isolation and request recovery, qualify real semantic ranking, retain authenticated EN/KO Console receipts, reconcile all 36 findings, and perform ten new post-implementation critique rounds. |
| 2026-09-21 | in-progress | Connected real controlled-identity adapter construction, authenticated bounded enrollment, owned preparation/source-change reconciliation, and audited cold-cache restoration to default semantic/Pantheon bootstrap and task supervision. The default release and semantic catalog share the source-derived candidate declaration. | `current change`; 178 candidate, Function, composition, bootstrap and configuration tests plus 25 runtime-construction tests passed; ten-source strict mypy passed. Synthetic embeddings validate mechanics only. | Finish candidate-specific terminal projection and source/restart failure review; real semantic ranking remains explicitly closed. Live quality, authenticated EN/KO evidence, original finding reconciliation, and ten post-implementation critique rounds remain open. |
| 2026-09-21 | implemented | Bound new preparation work and activation expiry to the original ingress time rather than renewing the window after validation. Added stale/future rejection, timeout cancellation, and delayed-validation checks using one trusted test clock. | `current change`; 23 focused candidate/Function tests passed; strict mypy and Ruff passed. | Production source/model enrollment, startup/source-change reconciliation, exact-release activation, candidate-only terminal projection, real quality, authenticated bilingual receipts, and ten final rounds remain open. |
| 2026-09-21 | implemented | Added explicit exact-release instance-candidate registration to real semantic runtime composition and callback forwarding through the Azure facade. Kept registration in a focused binder so the main builder remains within 800 lines. | `current change`; 61 semantic/adaptive composition regressions passed; strict mypy and Ruff passed. | Bind the actual controlled source/model bootstrap and principal enrollment, default exact-release declaration, startup/source-change reconciliation, and candidate-specific terminal presentation. Real quality, authenticated EN/KO receipts, original finding reconciliation, and ten post-implementation rounds remain open. |
| 2026-09-21 | implemented | Replaced the single-generation reader cache with bounded principal/role/purpose entries and scoped owner invalidation. Retained per-scope in-flight fences without retaining complete declaration builds in the read cache. | `current change`; 23 focused candidate/Function tests passed, including two real source snapshots, concurrent preparation, scoped invalidation, cancellation cleanup, and scope/document capacity rejection; strict mypy and Ruff passed. | Bind production authenticated enrollment, controlled model identity, the exact-release typed consumer, and source-change/startup reconciliation. Live relevance, authenticated bilingual evidence, original finding reconciliation, and ten new post-implementation rounds remain open. |
| 2026-09-21 | implemented | Added an unregistered principal-bound instance-candidate Function and connected its callback to the real audited worker reader. Closed candidate schemas, declared read sets, response accounting, and exact query/result digest binding. Removed a second manifest resolution that could relabel an earlier search with a different release. | `current change`; 22 registry and real-worker candidate tests passed; strict mypy, Ruff, and editor diagnostics passed. | Complete production composition, multi-principal preparation, source-change recovery, and candidate-only presentation without qualifying synthetic model output. Real embedding qualification, authenticated EN/KO receipts, original finding reconciliation, and ten new post-implementation critique rounds remain incomplete. |
| 2026-09-21 | implemented | Added owner-event invalidation, retained rollback, and retirement with terminal-audited generation-local physical reclamation. Strengthened retirement identity against validation-receipt relabeling and isolated PostgreSQL cleanup fixtures by unique scope. | `current change`; 24 owning lifecycle/candidate tests passed, including actual EventBus withdrawal/restoration and loopback PostgreSQL reclaim/retry; 48 source/vector regressions passed; strict mypy and Ruff passed. | Bind the typed instance-query consumer and source-change reconciliation in production, qualify the controlled embedding identity and real relevance, retain authenticated bilingual receipts, and then run ten new final critique rounds. |
| 2026-09-21 | implemented | Connected the real source, vector, validation, audit-seal, pointer, reader, and publication-journal implementations through ContextIndex callbacks. Reproduced and fixed post-validation source drift and the crash gap between saved phase result and owner-message record. | `current change`; actual EventBus worker-chain, duplicate, missing terminal-audit, source-change, interrupted-message, and acknowledgement-loss recovery regressions passed; strict mypy and Ruff passed. | Bind the actual production source/model/runtime consumer and non-activation lifecycle operations, then complete physical reclamation, real quality evidence, authenticated EN/KO evidence, and ten post-implementation critique rounds. No overall closure is claimed. |
| 2026-09-21 | implemented | Connected the isolated ContextIndex event family to actual Pantheon owner subscriptions and typed ingress. Added Heimdall validation and Saga intent/terminal subscriptions without changing publishing ownership. Reproduced and fixed transport-version mismatch and invalid-body error disclosure. | `current change`; 118 focused runtime, routing, registry, topic, doc-parity, and layout checks passed; strict mypy, Ruff, and both reviewed translation pairs passed. | Replace synthetic routing-test callbacks with the source/vector, validation, audited lifecycle, publication-recovery, and runtime reader bindings. Retain actual quality and authenticated bilingual evidence separately; post-implementation critique rounds have not begun. |
| 2026-09-21 | implemented | Added an isolated, revision-fenced pointer and atomic terminal outcome with injected independent admission. Reproduced and fixed retired-generation reactivation, archived-result identity drift, and the memory/PostgreSQL absent-row CAS mismatch. The revised lifecycle requires independent source validation and Saga intent before Muninn CAS, then durable terminal audit; retirement fences rollback before derived-data reclamation. | `current change`; `ontology_index_lifecycle.py`; 18 focused lifecycle checks including local PostgreSQL passed; strict mypy and Ruff passed. | Connect real Muninn, Heimdall, Saga, and runtime producer/subscriber paths, durable terminal publication, and bounded physical reclamation. Synthetic admission does not close agent ownership. No live authorization renewal or final post-implementation critique completion is claimed. |
| 2026-09-21 | implemented | Connected inactive vector snapshots to a bounded prepared reader and current-graph candidate reauthorization. Reproduced and repaired mutable returned facts and in-flight invalidation; pending preparation cannot resurrect an invalidated cache. | `current change`; reader, authorization, vector, snapshot, and gateway checks: 103 passed, including local PostgreSQL, with unchanged input hashes. Exact-ID queries passed with the embedder unavailable, and query-path StateStore reads remained zero after preparation. Ruff and strict mypy passed. | Wire durable active selection, Muninn-owned ContextIndex events, Heimdall validation, Saga audit, invalidation, retention, and rollback through production composition. Ranking quality, authenticated bilingual receipts, and ten new post-implementation critique rounds remain open. |
| 2026-09-21 | implemented | Added source-bound inactive vector persistence in its own namespace. Fixed a reproduced retry defect that could rebless a changed persisted vector by pinning the first completed header. | `current change`; `ontology_vector_store.py`; vector and source-snapshot tests: 50 passed, including local PostgreSQL, cancellation, provider-failure redaction, model mismatch, resumable writes, and bounded point reads. | Connect isolated retrieval and current-graph reauthorization, Muninn ContextIndex events, independent Heimdall validation, Saga audit, invalidation, retention, and rollback. Real embedding quality and authenticated bilingual receipts remain unqualified. These preparation checks do not count toward the ten post-implementation critique rounds. |
| 2026-09-20 | implemented | Added independent current-graph reconstruction for staged runtime objects, rather than trusting self-consistent stored hashes. Rejected precomputed vectors from source-only validation. | `current change`; `ontology_snapshot_validation.py`; seven focused validation cases and 105 owning checks including local PostgreSQL passed with stable input hashes; Ruff and strict mypy passed. | Connect Muninn-owned ContextIndex preparation and Heimdall validation through typed events before audited CAS activation. Rule lifecycle topics and pointers cannot substitute. Frozen 20-policy calibration failed, best 15/16, so disjoint 24-case live evaluation was not run and no runtime ranking policy changed. |
| 2026-09-20 | implemented | Independently revalidated scanned source declaration refs and object identity before ACL projection; extracted bounded snapshot identity computation. | `current change`; `query_snapshot.py`; three source-identity regressions failed before repair; 98 gateway, ObjectSet and generation checks including local PostgreSQL passed; Ruff and strict mypy passed. | Muninn ContextIndex lifecycle, isolated vector retrieval and current-graph reauthorization remain unwired. Real embedding diagnostic remains 13/16 with no governed model-version binding; authenticated Console evidence is blocked by browser CDP connection timeout. No production readiness or Low-only closure is claimed. |
| 2026-09-20 | implemented | Added an off-path complete multi-type source scan through the secured gateway and wired it to immutable staging. Preserved ordinary query limits and bounded large projection receipts through ordered object hashes. | `current change`; `query_gateway.py`, `object_sets.py`, `ontology_snapshot_store.py`; 95 owning tests passed with stable input hashes; Ruff and strict mypy passed. | Bind agent-owned activation/invalidation, retention and durable candidate search. Authorized real-embedding diagnostic was 13/16, so quality qualification remains failed; it is not evidence of completion. |
| 2026-09-20 | implemented | Added a bounded source writer that reads through the secured ObjectSet gateway, strips machine-marked redactions, rejects incomplete or drifted source evidence, and binds its exact projection to immutable staging. | `current change`; `ontology_snapshot_store.py`; `test_query_gateway.py`; gateway and generation checks passed 68 cases with unchanged input hashes, including local PostgreSQL snapshot verification; Ruff and strict mypy passed. | Add complete multi-type/paged source production, retention, audited owner-event activation/invalidation, and current-graph-authorized candidate consumption. This adapter is not wired as a production index. |
| 2026-09-20 | implemented | Prevented prior embeddings from being relabeled as a new model generation through content-only document reuse. Canonical unembedded document reuse remains intact. | `current change`; `generation.py`; regression failed before repair; 40 owning tests including PostgreSQL snapshot reconnection/tamper passed with unchanged inputs; Ruff and strict mypy passed. | Bind authorized source production, event-owned lifecycle and runtime instance retrieval, and retain real-embedder qualification separately. |
| 2026-09-20 | implemented | Added isolated immutable ontology staging with last-write completion, exact retry, bounded chunks, principal/source binding, and fail-closed restart reads. | `current change`; `ontology_snapshot_store.py`; `test_ontology_generation.py`: 38 local tests and one PostgreSQL reconnection/tamper test passed. The database test writes and removes only its unique synthetic keys. | Build complete authorized source projections, implement audited owner-event activation/invalidation and retention, and bind candidate retrieval to current graph reauthorization. No Rule pointer or production-readiness claim changes. |
| 2026-09-20 | implemented | Bound ontology semantic generation v2 to the explicit principal-scope digest, independently checked its document manifest, and reconstructed exact declaration content during validation. Cross-principal reuse and rehashed tampering now fail closed. | `current change`; two regressions failed before repair; 23 generation/selector tests passed afterward, including 8,500-row incremental coverage and fully rehashed declaration tampering; Ruff and strict mypy passed. | Build the isolated durable instance writer, owner-event lifecycle, source-generation invalidation, and production candidate consumer. These checks do not authorize publishing into the Rule index. |
| 2026-09-20 | implemented | Blocked rejected or unrelated semantic judgments from re-entering through declaration-list/detail frames. Post-routing-repair live evidence had reached a query but selected all declaration kinds after a rejected object-type judgment; that partial response was not accepted as successful validation. | `current change`; six negative cases failed before the guard; 269 planning and manifest tests passed afterward; Ruff and strict mypy passed. | Exact EN/KO schema interpretation and complete query receipts remain unqualified. This fail-closed repair prevents scope widening but does not manufacture a correct model interpretation or close durable indexing and relevance work. |
| 2026-09-20 | implemented | Preserved one declaration kind for compact readable schema-list facets after two authorized EN/KO live canaries selected different scopes. The English result was verified; the Korean result selected all declaration kinds and was partial, so the live batch stopped. The compact path excludes additional negative, historical, or property requirements rather than discarding them. | `current change`; `semantic_manifest_planning.py`; `test_semantic_manifest_planning.py`; 59 focused manifest/declaration/schema tests passed; Ruff and strict mypy passed. | Load the committed repair and retain same-scope bilingual live receipts. These local regressions do not close durable instance indexing, real-embedder relevance, or exact-source certification. |
| 2026-09-19 | implemented | Completed ten local critique rounds for scoped query authorization, historical release interpretation, source lineage, exact algebra, cancellation, candidate accounting, and durable adapter parity. | `current change`; integrated owning checks: 1,888 passed and one existing tsc-unavailable skip; isolated PostgreSQL: three passed; strict mypy: 22 sources; composition ownership: 39 passed. | Durable operational-instance semantic indexing, production-embedder held-out relevance, and exact-source governed live certification remain open. No all-query or production-ready claim. |
| 2026-09-19 | in-progress | Hardened query authorization, scoped receipts, historical releases, traversal, exact arithmetic, cancellation, source lineage, candidate selection, and durable search parity. Reconciled operational preflight to the implemented 0.75 threshold. | `current change`; focused query, planning, composition, and isolated PostgreSQL regressions; strict mypy over 21 changed source files. | Complete the final critique rounds and exact-source verification. Local fixtures do not satisfy governed live certification. |
| 2026-09-20 | implemented | Refreshed the generated question bank and dependent semantic-intent coverage after integrated Console catalog and presentation-type bytes changed their source digests. Question identity, coverage classification, readiness, and authority remain unchanged. | `current change`; canonical generators; exact artifact-equality tests passed 2 cases | Pushed-SHA CI remains authoritative; no runtime or promotion claim is added. |
| 2026-09-17 | implemented | Regenerated question-bank and semantic-intent coverage provenance after the merged Trace presentation catalog changed their source digest. Question identities, coverage states, and authority remain unchanged. | `current change`; canonical generators; focused artifact parity tests | No runtime or promotion claim is added. |
| 2026-09-17 | implemented | Regenerated question-bank and dependent semantic inventory source commitments after newly merged Console catalog bytes invalidated the previous artifacts. | `current change`; canonical builders; 12 focused question-bank and semantic-coverage tests passed; 400 question identities and coverage gaps unchanged. | Exact-head protected delivery under #1241; no model or live campaign was run. |
| 2026-09-17 | implemented | Regenerated the question bank and dependent semantic-intent coverage together after a later Agent Activity Console catalog update changed their authoritative source digest again. Question identity, coverage, readiness, and execution authority remain unchanged. | `current change`; both official generators; focused artifact parity tests | No live or promotion claim is added. |
| 2026-09-17 | implemented | Refreshed the generated question bank and then the dependent semantic-intent coverage artifact after Console catalog source digests changed. Question identity, coverage classification, and execution authority remain unchanged. | `current change`; `build_question_bank.py`; `build_semantic_intent_coverage.py`; focused question-bank and semantic-coverage parity tests | No runtime validation or authority claim is added by this provenance-only refresh. |
| 2026-09-16 | implemented | Added exact logical-service and workload target resolution through approved deployment aliases, server-owned cross-runtime traversal and verified current-state reads. Bound continuous operating-model updates to one logical topic across AKS, Container Apps, and local Redpanda, and added bilingual component-state presentation without aggregate-health or cause inference. | `current change`; 13 critique rounds fixed ambiguous-judgment progression, server-compiler model fallback, alias control characters, and partial-mapping presentation; logical-service planner, query verifier, provider validation, runtime projection, full DAG execution, deployment/local topic, and bilingual presentation tests passed in focused runs; Ruff and strict mypy passed. No Medium-or-higher finding remained. | Governed live cross-service evidence remains required before raising this bounded capability from implemented to validated. |
| 2026-09-14 | implemented | Isolated the Browser Entra ontology-assurance services from ambient Azure CLI-principal preparation by clearing both API and Vite authentication flag pairs in the run-scoped child commands. | `current change`; `run_ontology_assurance.py`; focused assurance process-spec regression. | No query authority or assurance result changed. |
| 2026-09-13 | implemented | Restored the PR gate contract after source integration by isolating compact preflight validation, keeping the v9 classifier below 5,700 characters without dropping reviewed operational signals, and regenerating the CQAS artifact from authoritative sources. | `current change`; `conversation_preflight_validation.py`; `conversation-preflight.v9.yaml`; `build_semantic_intent_coverage.py`; 506 focused tests; enforce-mode file-LOC, Ruff, and strict mypy passed. | Pushed-SHA CI remains authoritative; no live-model, production-readiness, or promotion claim is added. |
| 2026-09-11 | implemented | Refreshed semantic-intent coverage after the recovery decision boundary and agent-owned observation route changed the authoritative source set. | `current change`; semantic coverage and decision-boundary tests passed 15 cases. | No query authority or readiness claim changes in this refresh. |
| 2026-09-11 | implemented | Refreshed the 400-question review bank after the safeguard-bound service-contract projection changed. Question identity and authority remain unchanged. | `current change`; generated question-bank artifacts and focused parity tests. | Retain governed randomized and cross-service assurance evidence before a production-readiness claim. |
| 2026-09-11 | implemented | Rematerialized the 400-question review bank after its generated Console contract source changed. Question identities, readiness classifications, source counts, and execution authority remain unchanged. | `current change`; `build_question_bank.py`; focused question-bank source parity tests. | Retain governed randomized and cross-service assurance evidence before a production-readiness claim. |
| 2026-09-10 | implemented | Compiled an accepted no-target queryable ObjectType inventory into the existing principal-scoped `query.manifest` FunctionType. The deterministic frame and single-node plan preserve canonical result identity, read-only evidence, and zero execution authority without transmitting the full manifest to a model. | `current change`; `semantic_manifest_planning.py`; `semantic_planning_plan_dispatch.py`; focused manifest planning and composition tests; authenticated Browser Entra reproduced the prior `semantic_planner_unavailable` hold after request-token budget rejection. | Publish the deterministic compiler and retain a passing authenticated 5273/8010 no-authority artifact under Issue #262. |
| 2026-09-10 | implemented | Enforced the schema-validated quoted and hypothetical discourse boundary before frame selection. Reported or conditional action language can no longer become a model frame, server-owned target-candidate query, or action draft, while a direct read outside a quoted clause remains eligible for verified planning. | `current change`; [Issue #540](https://github.com/dotnetpower/fdai/issues/540); `semantic_planning_frame_checks.py`; `test_semantic_planning.py`; focused planner regressions for the original Korean case, three Korean paraphrases, English controls, and bilingual direct-read controls. | No residual implementation work for Issue #540. Keep model-profile promotion governed by the existing shadow cohort gates. |
| 2026-09-09 | implemented | Completed 31 critique and hardening rounds across exact profile selection, request budgeting, replay identity, runtime propagation, durable assurance evidence, and semantic coverage regeneration. The final independent review found no Medium-or-higher defect. | Commits through `1048f6feb`; 804 combined tests and 102 contract-pin checks passed; strict mypy and Ruff passed over 23 changed source files; translation, roadmap, document-size, and integrity gates passed. | Keep compact judgment, frame, and plan profiles in shadow until an explicitly authorized model cohort satisfies the promotion gates. |
| 2026-09-09 | implemented | Bound semantic judgment, frame, plan, and recovery prompts to exact profiles with request budgets and replay manifests. Added compact shadow compositions and moved inactive prompt versions outside runtime scanning without changing the active judgment, frame, or plan text. | `current change`; focused prompt, semantic adapter, assurance, and composition checks passed 224 cases; targeted Ruff and strict mypy passed. | Run an authorized model-backed comparison before changing the active compact-profile bindings. |
| 2026-09-09 | implemented | Added the 16-case Korean Azure and Incident contract cohort, typed forbidden actions, bounded capability semantics, strict shadow validation, and a v10 correction after the exploratory v9 live cohort missed accuracy gates. | `current change`; focused contracts, prompts, incident confirmation, composition, and content-free local v8 or v9 aggregate evidence. | Run v10 on one exact-source bilingual cohort and require both unsafe false-positive rates plus invented identity count to remain zero before promotion. |
| 2026-09-09 | implemented | Hardened the cohort and candidate through v13 with corrected metrics, strict schema `1.1.0`, safe target normalization, closed action/discourse invariants, and bounded descriptor projection. | `current change`; 20 critique lenses, repeated content-free live comparisons, connected regression and static gates. | Keep v13 in shadow; require repeated bilingual exact-source improvement and every hard-zero counter at zero before promotion. |
| 2026-09-09 | implemented | Added typed target-family closure and cumulative v14 prompt guidance for compound Service Health and Resource state, action procedures, incident creation, and principal-scope Resource Health history. | `current change`; exploratory synchronized-source cohort reached 100% for primary intent, exact target extraction, exact span validity, clarification precision, and secondary recall with zero safety findings. | Repeat after commit on the clean exact source before validation or promotion. |
| 2026-09-09 | implemented | Closed redundant clarification for a typed read-only error-correlation proposal carrying complete exact Resource and time targets. | `current change`; focused judgment and Golden contract checks. | Repeat on the committed exact source. |
| 2026-09-09 | implemented | Stabilized subtype-only exact-target ambiguity through a deterministic localized clarification that preserves the model-proposed ResourceType target. | `current change`; focused judgment checks and exploratory v14 cohort with all requested metrics at 100%. | Repeat after synchronization on the final clean source. |
| 2026-09-09 | validated | Repeated v14 after upstream synchronization on clean source `a5d3627b3`. | All requested intent metrics and secondary recall were 100%; exact span validity was 100%; both unsafe false-positive rates and invented identity count were 0. | Retain shadow mode until broader bilingual repetition and frame v41 end-to-end evidence authorize promotion. |
| 2026-09-09 | implemented | Bound semantic Kafka records to the opaque session partition key and kept the request UUID for idempotency and correlation only. | `current change`; focused Operator semantic transport tests. | Retain the governed authenticated subscription receipt required by issue #151. |
| 2026-09-08 | implemented | Restored the `check-file-loc` and strict-mypy CI gates for `semantic_planning.py` (813 LOC) and `semantic_runtime.py` (913 LOC, over the 871 baseline) by extracting `PreflightDirectResponseRouter` into `semantic_planning_preflight_router.py` and the thread-cancellation bridge into `semantic_runtime_cancellation.py`, and fixed two strict-mypy `union-attr` errors (the optional cancellation event and optional `SemanticPlanningService` planner call) by narrowing to a local variable and an explicit `is not None` guard. No behavior change. | `current change`; `semantic_planning.py` (734 LOC), `semantic_runtime.py` (805 LOC), Ruff, targeted strict mypy on all four touched modules, `check-file-loc.sh` (enforce mode, 0 failed), and 1703 focused `tests/conversation/` cases passed. | None; both files are now below their respective ceilings. |
| 2026-09-08 | implemented | Bound streamed semantic answer segments to the verified receipt and evidence set, added segment-specific replay, and made the Console reject a conflicting confirmation or terminal answer. | `current change`; `semantic_turn_runtime.py`, `backend-stream.ts`, focused Operator, Console, and cross-service stream tests. | Retain authenticated runtime evidence separately; local stream checks do not establish production readiness. |
| 2026-09-08 | implemented | Closed three operational-summary and presentation gaps. A low-confidence or rejected judgment can no longer fall through to a subscription or Resource summary frame, bounded Unicode subscription names are recognized in English noun order, and truncated Resource wording no longer promises rows absent from the bounded technical payload. | `current change`; focused regression checks, exact Resource variants, 822 Resource-planning tests, and 62 Console stream tests passed. | Keep authenticated runtime evidence and the blocked provider targets separate; no live Azure state or execution authority changed. |
| 2026-09-07 | implemented | Prevented exact Resource current-state questions from being reduced to Resource collection filters. Collection recovery now requires explicit collection cardinality and no exact Resource target; full-judgment recovery preserves a source-grounded name or ID as a typed constraint. A successful provisioning observation without runtime status now remains `not_proven` instead of becoming a false non-running claim. | `current change`; focused exact-name, targetless clarification, collection, preflight-family, current-state provider, Ruff, and strict mypy checks. | Re-ask the exact `aks-fdai-chaos` state question in the authenticated standard Console after the shared CDP browser reconnects. |
| 2026-09-07 | implemented | Centralized operational Resource visibility so model-proposed and server-built collection plans both exclude `authorization.role-assignment`. The exclusion runs after model-operand verification and does not affect explicit IAM relationship output. | `current change`; focused visibility and named-resource-group checks; isolated production-Operator E2E returned `answered`, `semantic_answer_verified`, `resource_list`, zero serialized role-assignment rows, and `execution_authority=false`. | Retain the same result through the authenticated standard Console browser. |
| 2026-09-07 | implemented | Added exact Resource current-state preflight and object-only completeness, and made local inventory refresh preserve configured scope and journal lineage. Scoped graph coverage now ignores unrelated test observations without ignoring pending observations in the active scope. | `current change`; focused preflight, planner, query-gateway, inventory-refresh, source-coverage, Ruff, and strict mypy checks; isolated production-Operator E2E answered through `target_current_state`. | Retain authenticated standard-Console evidence. |
| 2026-09-07 | implemented | Rejected generic product categories as exact operational identities and stopped ambiguous targetless gateway comparisons before frame planning. | `current change`; 238 focused tests; post-fix Browser Entra F4 trace omitted frame-model and provider reads. | Retain exact-target F3/F4 evidence. |
| 2026-09-07 | implemented | Added schema-validated, provenance-bound F1-F4 preflight meaning with fail-closed source, confidence, context, time, family-shape, and Resource identity checks. | `current change`; 177 focused conversation, prompt-registry, and adapter tests, targeted Ruff, and strict mypy passed. | Retain standard-stack answer-token TTFT and complete evidence receipts. |
| 2026-09-01 | implemented | Completed the VPN-routed exact-source canary after bounding semantic judgment variants for change correlation and Resource activity. All ten cases passed, including service-Agent ownership, service current health, change correlation, and bounded Resource activity. The runtime used the private Foundry endpoint through the VPN without granting execution authority. | Source `31002f3db70649ceb6844dc8ea59798ba7aa4d13`; source-bound local ledger digest `sha256:ef474b09662296d2e61a6e74569945afd236d038523795545069f8d11546d779`; exact result 10/10. The stop marker and existing question, evaluation, and regression ledgers retained their original SHA-256 digests after the run. | Propose, but do not start, a 20-case bilingual campaign over the same ten expectations. Keep the 100-case campaign disabled. |
| 2026-09-01 | implemented | Replaced the service-to-Agent mixed-authority hold with one explicit composite read authority and a lineage-preserving instance-path node. The node validates exact LinkType declaration dependencies, executes a principal-scoped current graph read in one store snapshot, and projects identity claims only from complete non-redacted concrete paths. | `current change`; focused service-contract, ontology query, planner, executor, provider, persistence, and assurance checks passed 451 cases; Ruff and strict mypy passed. | Run the bounded 10-case canary. Propose a 20-case campaign only if all 10 pass; do not start the 100-case campaign. |
| 2026-09-01 | implemented | Corrected the complete-empty boundary found by the first canary. A service ownership request with no concrete root-to-Agent path now holds instead of returning an answered result without the required identity facts. | `current change`; focused instance-path and semantic planning checks. The first exploratory EN/KO relationship cohort retained 5/10, including the two expected service-agent empty-path failures. | Rerun the intended 10-case canary cohort. Propose a 20-case campaign only if all 10 pass. |
| 2026-09-01 | implemented | Ran the intended exact-source 10-case canary after the empty-path correction. `service-agent-authority.direct.en` passed with the ownership result held safely when no concrete mapping existed. The cohort completed 8/10; `change-correlation` disposition and `service-current-health` frame mismatches remain outside this ownership slice. | Source `e04ff33323f9eb25366d457ed2cc4b3f930f16d5`; source-bound local ledger digest `sha256:53212597522ce977dee23e7f64f090f56da614249c298e9fa6dc6cf4c389428d`. The existing stop marker and question, evaluation, and regression ledgers retained identical before-and-after digests. | Do not propose the 20-case campaign because the canary was not 10/10. Keep the 100-case campaign disabled. |
| 2026-09-01 | implemented | Resolved the two remaining canary defects without widening authority. An unbound change-correlation judgment now retains its exact `compare/windowed` frame and returns a typed hold. The reviewed service-health question now names a business service deployed on AKS instead of the distinct Kubernetes `Service` object. | Commits `a49aa3812` and `c268e1f79`; semantic planning checks passed 398 cases, Golden dataset and question-bank checks passed 18 cases, and the Golden campaign oracle passed 10 cases. A subsequent exact-source live attempt is not a scored cohort because every configured model request returned HTTP 403 after the Foundry account became private-only; the stop marker and existing ledgers retained identical before-and-after digests. | Run the same 10-case exact-source cohort from the VNet-integrated runner. Propose 20 cases only after a valid 10/10 result. Keep the 100-case campaign disabled. |
| 2026-09-01 | implemented | Aligned semantic judgment with reviewed operating mappings, Rule state and trace, Resource classification, service health, and service ownership. Golden certification now uses the committed expected terminal posture, prevents fresh answer cases from escaping through a safe non-answer, and keeps evidence and authority gates on every disposition. | `current change`; semantic judgment prompt, Golden dataset adapter and certification tests, Operator locale propagation test, focused semantic and Golden checks; the reviewed live canary improved from 0/10 to 5/10 exact contract passes. | Implement verified Rule trace and service ownership execution, align target-candidate terminal semantics, and retain complete relationship evidence before claiming full Golden coverage. |
| 2026-09-01 | implemented | Registered Framework and FrameworkControl as Identifiable implementations. The interface implementation registry now lists all shipped object types. | `current change`; focused ontology catalog and object-type catalog checks passed. | No query-coverage work follows from this interface registration. |
| 2026-09-01 | implemented | Replaced terminal `ontology-query` authority flattening with receipt-derived source authority and typed missing/conflict holds. | `current change`; focused Core, Operator, contract, and cross-service authority tests passed. | Retain a governed live receipt in a separately authorized campaign before raising this authority path to validated. |
| 2026-09-01 | implemented | Filtered declaration selection to values that map to a registered declaration kind after a live variant carried both canonical `ActionType` and the non-declaration `ontology` question domain. The declaration target remains unique without treating domain context as a conflicting type. | `current change`; targetless, conflicting-frame, and additional-domain-target regressions; operational-count controls; Ruff, strict mypy, and anti-hardcoding check. | Commit and remeasure the original and three persisted paraphrases. |
| 2026-09-01 | implemented | Corrected precedence after two live variants carried canonical `ActionType` in the accepted judgment while the later frame proposed `LinkType`. A unique canonical declaration target now overrides that conflicting frame subject; an exact frame subject remains the targetless fallback, and multiple canonical targets remain unresolved. | `current change`; canonical-versus-frame conflict and targetless fallback regressions, operational-count controls, Ruff, strict mypy, and anti-hardcoding check. | Commit and remeasure the original and three persisted paraphrases. |
| 2026-09-01 | implemented | Made declaration-kind normalization frame-first after two remaining variants supplied an exact `ActionType` frame subject while semantic judgment targets were absent or included an extra unbound screen target. A unique canonical target remains a fallback only when the verified frame has no exact subject. | `current change`; frame-first and targetless judgment regression, operational-count controls, Ruff, strict mypy, and anti-hardcoding check. | Commit and remeasure the original and three persisted paraphrases. |
| 2026-09-01 | implemented | Removed the remaining model-node-id dependency after the post-normalization cohort executed valid manifest counts but still selected the generic row-count renderer. The query renderer now receives the verified frame subject and uses that typed declaration kind to label the aggregate value. | `current change`; semantic-turn processor and English and Korean renderer regressions; Ruff and strict mypy. | Commit and remeasure the original and three persisted paraphrases. |
| 2026-09-01 | implemented | Normalized the declaration-count frame from the accepted semantic judgment after three cohort variants preserved canonical `ActionType` and the `count` facet but emitted extra display facets or the schema type name. The normalization consumes only validated machine-contract fields and does not add phrase or token routing. | `current change`; semantic manifest planner, frame checks, accepted-judgment regression, operational-count negative controls, Ruff, strict mypy, and anti-hardcoding check. | Commit and remeasure the original and three persisted paraphrases. |
| 2026-09-01 | implemented | Corrected declaration-count presentation after the live regression cohort showed that the generic renderer reported one aggregate row rather than the count stored in that row. The aggregate now preserves the declaration kind, and the Core renderer reports the distinct typed count only for complete output while naming `query.manifest` as the read-only source. | `current change`; manifest planning, semantic-turn processor, positive and negative planning regressions, and English and Korean presentation regressions. | Remeasure the original and three persisted paraphrases once this candidate is committed. |
| 2026-09-01 | implemented | Added deterministic declaration-cardinality planning after a conversation-assurance failure showed that a valid `query.manifest` count still depended on a model-authored plan. Core now compiles the verified aggregate frame without reinterpreting natural language or adding authority. | `current change`; `semantic_manifest_planning.py`, plan dispatch, and focused positive and operational-count regressions. | Retain a passing live original-and-paraphrase cohort before treating the conversation-assurance failure as resolved. |
| 2026-08-29 | implemented | Migrated the secured ObjectSet query consumption boundary to the shared decision-critical evidence admission. Exact result issuance remains bounded, while resolution and FunctionType verification now reject absent, mismatched, or expired admissions. The composition seam accepts only an injected provider and has no synthetic or default-positive binding. | `current change`; query authority and handler sources, semantic composition, focused authority, Pod telemetry, Pod recovery, rollout, query-handler, and composition tests; Ruff and strict mypy. | Bind a production provider backed by the trusted receipt verifier registry, then retain an authenticated cross-service query bundle. |
| 2026-08-27 | implemented | Preserved one source-grounded Resource name in the server-owned Event ObjectSet and replaced the generic verified-row count with a bilingual Resource Event answer. The answer lists only bounded normalized Event fields and always reports source completeness, the stable limitation, and `execution_authority=false`; retention-unverified zero rows state that they do not prove historical absence. | `current change`; Event planning, FunctionType, reader, composition, processor, and Operator presentation cohort (`287 passed`); Ruff, formatter, and strict mypy passed. After stale Core replacement, the authenticated exact Deployment follow-up ran 2/2 nodes and 8/8 evidence checks and rendered the bounded `source_unavailable` limitation without cause or recovery claims. | Restore authoritative Kubernetes Event source access under the runtime workload identity, then retain an exact-child result carrying `source_retention_unverified` or normalized Event rows. Durable history remains separate open work. |
| 2026-08-27 | implemented | Closed the child-only Kubernetes Event query gap without phrase routing or a new plan shape. The existing dependency-issued ObjectSet carries the inventory UID and cluster through an immutable identity-aware reader capability; legacy readers stay compatible, and Core never exposes other provider properties to the adapter. | `current change`; focused FunctionType, composite, Kubernetes, Azure, immutable identity, legacy compatibility, selector propagation, and forged identity checks (`27 passed`); Ruff, formatter, and strict mypy passed. | Retain authenticated exact-child evidence. Durable retention remains open, so zero rows stay incomplete historical evidence. |
| 2026-08-26 | implemented | Added descriptor-driven Kubernetes Event planning and provider composition to `query.resource_event_history` without adding a phrase route. The server plan keeps exact ObjectSet scope and the requested lookback, the composite preserves evidence from an available family when another is unavailable, and the Kubernetes adapter binds immutable UIDs or the exact selected cluster while excluding provider messages. Judgment-bound normalization requires explicit Kubernetes Event, resource, typed duration, time-range, and ordering evidence, and the typed duration must equal the frame lookback. | `current change`; Resource event FunctionType, Kubernetes and composite readers, runtime binding, shared UID identity helper, focused planning, FunctionType, adapter, routing, composition, and runtime checks; mixed-scope retention, encoded-response rejection, and raw 256 KiB bounds are covered. Semantic judgment v4 and frame v39 produced the canonical Kubernetes family and 3,600-second lookback, and the authenticated exact-cluster Console turn completed 2/2 nodes and 8/8 evidence checks in 6.8 seconds with no execution authority. Its zero rows did not prove historical absence; the hardened adapter reports `source_retention_unverified`. | Add durable event retention, then join change, replica, replacement UID, impact, and new-cutoff evidence before closing S1 cause or recovery. |
| 2026-08-25 | implemented | Extended the S1 exact-Pod semantic slice with reviewed `pod.restart.history` and explicit outgoing owner traversals. The server-owned five-node plan selects one exact Resource, reads a 30-minute same-UID restart delta, resolves one ReplicaSet and one Deployment, and invokes the issued reducer without a model-authored plan. It verifies every secured receipt and ownership link, rejects static dependency substitution, and requires fully recovered owner replicas. | `current change`; focused metric, verifier, Pod reducer, receipt, planner, and production-composition cohort passed 49 cases; semantic rollout and tier-routing regressions passed 370 cases; Ruff, formatter, and strict mypy passed. | Retain authenticated exact-Pod evidence and add independent Kubernetes event, change, replacement-UID, impact, and new-cutoff evidence before closing S1 cause and recovery. |
| 2026-08-25 | implemented | Completed the authenticated targetless S12 turn while preserving source incompleteness. The secured ObjectSet handler now maps an incomplete non-truncated graph to the stable `source_incomplete` table limitation, so zero candidates remain a verified bounded result rather than a capability failure or proof of absence. | `current change`; `query_source_handlers.py`; focused targetless, rollout, receipt, traversal, composition, and incomplete-source checks passed 19 cases; authenticated Console completed 4 observed events and 5/5 evidence checks with `execution_authority=false`. | Populate a complete Kubernetes generation with one exact Deployment, then retain the exact-target four-node assessment and new-cutoff recovery turn. |
| 2026-08-25 | implemented | Closed the S12 unsupported boundary in two stages. The targetless request now recovers an invalid causal frame to exact Kubernetes Deployment candidates, and an exact-target follow-up uses a server-owned ObjectSet, two explicit ownership traversals, and an issued rollout FunctionType without a model-authored plan. | `current change`; focused targetless and rollout vertical checks passed 17 cases; task-scoped Ruff and strict mypy passed. | Restart Core and retain the same-question candidate response, exact-target evidence response, and new-cutoff recovery verification. |
| 2026-08-22 | implemented | Narrowed semantic planning escalation to typed T1-unavailable triggers and added a separate no-T2 golden campaign profile on the authenticated cross-service request. | `current change`; focused shared, Core, Operator, golden adapter, and Console checks passed. | Run the standard-port readiness probe before starting the 560-turn campaign. |
| 2026-08-22 | validated | Validated the distinct exact-target metric-series capability and presentation after completing deterministic recovery for non-causal aggregate, evidence, list, and temporal-comparison T1 frame variants. | `current change`; four focused aggregate/series isolation checks, Ruff, and strict mypy passed; authenticated Console completed 2/2 query nodes and 8/8 evidence checks; technical output reported 20 returned and total rows, complete evidence, `min_max_envelope_v1`, and no display truncation; three viewport checks found 20 nonblank bars and no document or Deck overflow. | Complete the remaining exact-target activity and causal cells; broader governed request-to-Console and randomized assurance remain separate open evidence. |
| 2026-08-22 | implemented | Added a distinct exact-target metric-series capability after the authenticated Console showed that a visualization request was reduced to one seven-day average row. The frame, verified plan, FunctionType, and presentation intent now preserve the requested trend without browser inference. | `current change`; focused metric FunctionType, T1 planning, composition visibility, prompt-registry, and Operator presentation checks passed 43 cases; touched production files have no Pylance diagnostics. | Restart the local Core and Operator services and retain authenticated three-viewport evidence before claiming runtime validation. The broader exact-target 7/7 matrix remains open. |
| 2026-08-22 | validated | Added a server-owned candidate query shape for targetless Resource subtype questions and a schema-validated singular-versus-collection gate. Invalid or unavailable singular model frames can recover before T2, while collection requests and exact targets retain their existing capability paths. | `current change`; focused planner checks passed 204 cases; Ruff, formatter, and strict mypy passed; authenticated current-source Console evidence completed all eight Korean SRE example first turns with zero context-required, unsupported, held, unverified, or semantically substituted answers and no execution authority. | Add typed exact-target ingress, bounded seven-day activity, official Container Apps `MemoryPercentage`, and deterministic causal completion. Then retain a 7/7 exact-target receipt before expanding the validated claim. |
| 2026-08-21 | implemented | Ran 30 distinct Korean Console questions derived from the SRE Agent comparison surface and corrected the state and evidence-family substitutions they exposed. Core now compiles collection state through `query.resource_state_inventory`, rejects ungrounded model enum operands and display-field existence predicates, keeps catalog-declared subscription health outside current inventory state, and stops evidence coverage or missing screen scope before a broad Resource answer. Frame prompt revisions v31-v34 remain candidate guidance; exact manifests, server-owned plans, and deterministic capability alignment remain authoritative. | `current change`; focused semantic planning, tier-routing, FunctionType, composition, prompt-registry, and Operator presentation checks; authenticated local Console post-fix observations returned verified stopped-database state rows while contextual, mixed health, inventory-coverage, and unbound evidence families held without unrelated rows or execution authority. | Bind dedicated collection health, metric, event-history, inventory/state coverage, and contextual-resource FunctionTypes. Carry a typed selected screen/group identity across Console, Operator, and Core, then retain a governed bilingual campaign artifact before raising runtime assurance. |
| 2026-08-21 | validated | Replaced the generic evidence hold for an exact request-error and Activity Log correlation question with a server-owned five-node plan and a source-derived deterministic reducer. The reducer compares contiguous equal-duration windows, preserves verified zero separately from unavailable telemetry, and fixes `causal_claim_supported=false` and `execution_authority=false`. | `current change`; 218 focused planner, reducer, composition, processor, and Operator presentation tests; Ruff, formatter, strict mypy, ontology-query coverage, independent-service, file-LOC, fanout, and diff checks; authenticated Console completed 5/5 nodes and 11/11 evidence checks from six sources in 5.8 seconds with `plan_source=server_target_error_activity` and no T2. | Bind an authoritative Azure mapping for `http.server.request.error.count`. The current Container Apps direct Metrics map exposes CPU and response time but not request errors, so error trend and correlation remain unproven while exact Activity Log zero is retained. |
| 2026-08-21 | validated | Closed a same-question exact-target health substitution defect. A `validate/evidence_validation` frame previously built the principal-scope ObjectSet and returned 20 of 586 unrelated rows. The specialized health family now binds the target, current state, bounded activity, three reviewed metrics, and the deterministic insufficient-evidence reducer before I/O. | `current change`; authenticated Azure/FDAI comparison; 202 focused planner, function, processor, presentation, and composition checks; Ruff, formatter, strict mypy, ontology-query coverage, independent-service, and design-route gates; post-restart Console completed 7/7 nodes and 13/13 evidence checks in 6.7 seconds with `execution_authority=false`. | Process restart, runtime log, memory, dependency, and successful-work evidence remains open provider work and must stay explicit in the answer. |
| 2026-08-13 | in-progress | Adopted the implementation ledger without reconstructing earlier provenance. | Current source, tests, and status evidence listed in the scope table. | Obtain the live assurance and provider-composition evidence listed below. |
| 2026-08-13 | implemented | Hardened Rule-search contracts, Core idempotency and claims, Operator retries and leases, exact persistence, replay, and ownership validation through more than ten adversarial review rounds. | `current change`; `pytest -q services/core-control-plane/tests/test_semantic_turn_processor.py services/operator-service/tests/test_semantic_turn_bridge.py services/operator-service/tests/test_operator_workflow_family.py tests/integration/test_semantic_turn_roundtrip.py` passed 88 cases; task-scoped Ruff passed; a rollback-only PostgreSQL transaction probe passed. | Production validation remains blocked on governed live receipts. |
| 2026-08-13 | implemented | Added deterministic inventory-promotion topology revision publishing and an append-only bitemporal PostgreSQL reader/writer with independent current and historical derived writes. | `current change`; focused inventory and topology tests passed 31 cases; task-scoped Ruff and mypy passed. | Bind the historical reader to the production semantic query runtime and obtain governed runtime evidence. |
| 2026-08-13 | implemented | Bound temporal topology, metric-series, and evidence-join capabilities into exact-release semantic runtime composition. Optional metric dependencies are atomic, and absent providers remain unavailable to both the verifier and executor. | `current change`; `wire_semantic_query.py`, `bootstrap.py`, `test_wire_semantic_query.py`, and `test_bootstrap_config.py`; focused checks passed 16 cases. | Record governed request-to-Console and randomized assurance receipts. |
| 2026-08-13 | implemented | Moved concrete semantic-query provider selection into the runtime binding helper so the process entry point stays within its reviewed composition fanout. | `current change`; `bootstrap.py`, `bootstrap_bindings.py`, and `test_bootstrap_config.py`; focused provider-selection tests passed 3 cases. | Record governed request-to-Console and randomized assurance receipts. |
| 2026-08-13 | in-progress | Added authenticated request-to-Console and deterministic bilingual randomized-assurance runners with typed receipt oracles and derived readiness counters. | `current change`; focused Console tests, assurance-oracle tests, typecheck, and Playwright discovery pass. | Execute both runners against the authenticated local stack and link the two passing retained evidence records. |
| 2026-08-13 | implemented | Preserved a valid caller-supplied request UUID across the Operator semantic envelope and correlation identity without conflating it with the idempotency key; retained the deterministic UUID fallback for callers that omit request identity. | `current change`; `semantic_turn.py`; `test_semantic_turn_bridge.py`; focused caller-identity and retry-stability tests pass. | Obtain the governed authenticated receipt for the exact request identity before running randomized assurance. |
| 2026-08-14 | implemented | Added additive `operator-core-request` 1.3.0 with a server-owned `bound_context`, so a Console incident conversation delivers its bound incident identity to Core instead of dropping it. Core appends the binding as the last `system` context turn, which the planner's bounded window cannot discard. | `current change`; `semantic_turn.py` (contract, Operator, Core), `contract_codecs.py`, `compatibility-manifest.json`; core processor 32, Operator bridge 42, contracts 76, and route targets 326 passed; independent-services and ontology-query-coverage gates passed; strict mypy passed. | The bound incident is still not answerable: the semantic query manifest exposes no incident ObjectSet or incident-evidence FunctionType, so an incident investigation resolves to clarification. |
| 2026-08-14 | implemented | Moved the semantic request publisher to the 1.3.0 producer codec. The envelope builder already validated 1.3.0, so the stale publisher silently rejected every bound turn and the Console request never reached Core. | `current change`; `semantic_kafka.py`; Operator adapter and bridge suites passed 51 cases; a restarted local stack answered a bound incident turn in 6.4 s and Core logged the three planning stages with no decode failure. | Incident competency remains the open gap. |
| 2026-08-14 | in-progress | Projected canonical Incident instances and added the exact-release, correlation-scoped `query.incident_evidence` FunctionType over the append-only audit source. The function exposes profile, correlated records, truncation, and evidence gaps while fixing `cause_claim_supported=false` and `execution_authority=false`. | Commit `285341732` plus `current change`; focused Incident, registry, adapter, and semantic-composition checks passed 62 cases; task-scoped Ruff and strict mypy passed. | Add the A3 answer contract and action-draft-only next safe step, then retain authenticated Console evidence. |
| 2026-08-14 | in-progress | Corrected the initial incident FunctionType contract so canonical `incident_id` and audit `correlation_id` remain distinct from the bound Console context through verified plan execution and evidence projection. | `current change`; the incident FunctionType tests and end-to-end semantic composition regression passed, bringing the focused slice to 63 cases. | Restart the local stack, verify the same authenticated incident turn, then retain governed evidence before changing the capability state. |
| 2026-08-14 | in-progress | Aligned semantic frame and plan prompt v2 with the distinct bound identities so the model preserves both `incident_id` and `correlation_id` in the verified Function node instead of producing an invalid partial argument set. | `current change`; the focused prompt registry contract passed 5 cases. | Restart the local stack and verify the same authenticated incident turn before changing the capability state. |
| 2026-08-14 | implemented | Completed the incident answer projection with localized readable content, replayable observed progress, exact collapsed technical output, explicit causal and evidence gaps, read-only evidence collection as the default next step, and verified incident identity preserved through regeneration. | `current change`; focused Core (`36 passed`), Operator (`46 passed`), cross-service (`1 passed`), and Console (`74 passed`) checks plus Console typecheck and task-scoped Ruff passed. An authenticated Browser Entra observation rendered the same verified three-record result after regeneration but wasn't retained as a governed artifact. | Retain the governed request-to-Console and Korean equivalent evidence before promoting this scope to `validated`. |
| 2026-08-14 | implemented | Made semantic regeneration retry-stable by preserving the original bounded history and verified request identity, binding the request identity to Operator idempotency, propagating the canonical locale, and retrying repeated Azure throttling or schema-invalid candidates within the adapter budget. | `current change`; focused Console stream, normalizer, session, and event checks passed 128 cases; the Azure semantic-planning adapter passed 5 cases; Console typecheck and task-scoped Ruff passed. A retained authenticated Korean Browser working-tree run passed both turns with one five-stage Core planning cycle. | Commit and centrally validate the bounded change, then retain an exact-source authenticated Browser artifact before promoting the incident answer row to `validated`. |
| 2026-08-14 | validated | Closed the bounded authenticated incident-answer evidence gap after central validation. | Source revisions `7f2b740b1` and `244d003ef` have central receipts; the retained post-validation Korean Browser artifact records matching request, incident-binding, and technical-output digests across regeneration, with five observed phases and no execution authority. | The full request-to-Console runner and bilingual randomized cohort remain open production-assurance work. |
| 2026-08-14 | in-progress | Reopened incident assurance because the current bounded action-draft and browser-readiness changes require a new governed artifact. | `current change`; focused semantic, Console, and browser-assurance checks. | Retain the replacement request-to-Console and bilingual randomized evidence before restoring `validated`. |
| 2026-08-15 | implemented | Kept local narrator refresh independent from semantic transport while preserving transport-gated `SemanticTurnBridge` activation. | `current change`; focused production composition and semantic bridge tests passed 2 cases; task-scoped Ruff passed. | Retain the governed request-to-Console and bilingual randomized evidence before changing production-assurance state. |
| 2026-08-15 | implemented | Added machine-readable clarification requirements and rejected T1 frame proposals that ask for principal scope or purpose already bound by Core. Semantic frame prompt v4 tells both tiers to treat those values as trusted server context. | `current change`; focused semantic planner checks passed 21 cases, prompt and Azure adapter checks passed 10 cases, and task-scoped Ruff and strict mypy passed. | Restart Core after central validation and rerun the governed request-to-Console assurance path. |
| 2026-08-15 | implemented | Added bounded projection for verified topology graph, topology diff, metric window, and causal join outputs. | `current change`; focused Core processor tests passed 43 cases, and task-scoped Ruff and strict mypy passed. | Retain authenticated temporal and causal answer evidence after the planner contract is active. |
| 2026-08-15 | implemented | Added exact-release schema inventory querying through `query.manifest`, closed function-table conversion, and explicit frame/plan grammar for inventory, property-filter, topology, aggregation, and causal questions. | `current change`; focused manifest, handler, composition, relationship, semantic composition, and prompt checks passed 42 cases; task-scoped Ruff and strict mypy passed. | Preserve a clean passing 14-cell artifact, then run the seeded 100-case cohort and keep readiness blocked on any missing answer evidence. |
| 2026-08-15 | implemented | Verified direct ObjectSet aggregate fields against the exact row schema before execution, allowing only invalid T1 plans to retry the plan stage with T2. | `current change`; focused verifier and tier-routing checks passed 16 cases; task-scoped Ruff and strict mypy passed. | Rerun the held Korean aggregation cell before retaining clean 14-cell and seeded 100-case evidence. |
| 2026-08-15 | implemented | Propagated aggregate field schemas across table-transform dependencies and preserved dotted projected fields for deterministic downstream aggregation. | `current change`; focused verifier, handler, and tier-routing checks passed 24 cases; task-scoped Ruff and strict mypy passed. | Rerun the held Korean aggregation cell before retaining clean 14-cell and seeded 100-case evidence. |
| 2026-08-15 | implemented | Rebound every model-proposed ObjectSet cutoff to the single trusted evaluation time before plan verification, preventing current-state execution from rejecting a model timestamp that drifted outside the secured gateway window. | `current change`; the focused semantic-planning suite passed 15 cases, including a stale model cutoff regression. | Restart the exact-source Core stack and retain clean 14-cell and seeded 100-case Browser evidence. |
| 2026-08-16 | implemented | Loaded the durable ontology release history before startup reads persisted Incident objects. The incident projection previously ran before catalog synchronization, so every object pinned to an earlier catalog digest failed closed and the runtime could not start after any catalog change. | `current change`; the focused catalog-ontology suite passed 6 cases including a bootstrap ordering guard; task-scoped Ruff and strict mypy passed; the local stack started and answered a bound incident turn. | None for this row. |
| 2026-08-16 | implemented | Made the bound incident read server-owned. Planning now rewrites the identifiers and the record window of a proposed `query.incident_evidence` node from the conversation binding and re-verifies the plan, so the answer no longer depends on the model retyping a UUID and correlation identity it only holds as prose. A profile whose identity anchor falls outside the sampled audit window is now an evidence gap rather than a contradiction, and every fail-closed hold names its failing check in the runtime log. | `current change`; focused conversation, processor, and contract suites passed 606 cases; task-scoped Ruff and strict mypy passed; four consecutive live bound-incident turns answered in both locales after two consecutive holds on the same input before the change. | Retain the governed request-to-Console and bilingual randomized evidence for the restored incident answer. |
| 2026-08-16 | implemented | Restored the exact `query.ontology_relationships` Function envelope in semantic plan prompt v8 after the prompt upgrade dropped its `object_types`, limit, and no-`root_ids` constraints. Schema relationship plans now use only one or two exact ObjectType names from the validated frame and cannot reinterpret them as runtime object identities. | `current change`; `test_wire_ontology_relationships.py::test_semantic_prompts_select_exact_relationship_function` passed. | Retain the strict bilingual relationship cells and seeded cohort evidence on a centrally validated descendant. |
| 2026-08-16 | implemented | Removed the model from the anchored incident read entirely. The typed frame still decides that a turn wants incident evidence; the node is then built from the binding and no plan proposal is requested, while the same builder and verifier still apply. The binding answers an `incident_reference` clarification so an anchored turn cannot ask which incident it is anchored to, and the English-only `this incident` substring route is removed so no utterance substring decides a route. | `current change`; focused conversation checks passed 458 cases and the local service log runner passed 11 cases; task-scoped Ruff and strict mypy passed; three consecutive live anchored turns logged `plan_source="bound_incident"` and answered. | Retain the governed request-to-Console and bilingual randomized evidence. |
| 2026-08-16 | implemented | Closed four critique findings around the anchored read: the retargeting rewrite is gone, the window follows the evidence capability's record bound, the answer states the verified total and its truncation, and a projection whose durable request is absent is quarantined after bounded retries instead of stalling every later projection. | `current change`; focused Core checks passed 913 cases and Operator checks passed 310 cases; a restarted Operator drained a wedged backlog of 20 unmatched projections and returned healthy. | Retain the governed request-to-Console and bilingual randomized evidence. |
| 2026-08-16 | implemented | Added a cross-owner guard over the answer presentation boundary and bounded the projection conflict counter. | `current change`; the guard reads the Console's own slot allowlist and bounds and checks every emitted artifact in both locales; Operator checks passed 318 cases; a mutation to an unknown slot fails the guard. | Retain the governed request-to-Console and bilingual randomized evidence. |
| 2026-08-16 | implemented | Held incident evidence whose recorded times decrease, because the answer names the latest records by slicing the tail and nothing verified the reader's order. | `current change`; focused processor checks passed 54 cases; two live incident correlations answered with the invariant active. | Retain the governed request-to-Console and bilingual randomized evidence. |
| 2026-08-16 | implemented | Narrowed the binding's answer to an `incident_reference` question. Only a turn that will read the anchored incident is answered by the binding; on any other output shape the question stands, so a proposed plan cannot read a different incident behind a question the operator never saw. | `current change`; focused conversation checks passed 458 cases; removing the shape condition fails the new case; four live anchored turns answered in both locales with `plan_source="bound_incident"`. | Retain the governed request-to-Console and bilingual randomized evidence. |
| 2026-08-16 | implemented | Reported the incident evidence the read already carried. `query.incident_evidence` dropped each record's actor, so a counted record could not be attributed, and both surfaces reported a status where the profile already held title, severity, vertical, and the first and last recorded time. The projection keeps the actor, both surfaces list every populated profile field and a bounded recorded-activity table titled with its own bound, and the next safe step follows the measured gaps. An unrecorded status is still stated so listing populated fields cannot make it disappear, and the reported count stays the verified total. | `current change`; `incident_queries.py`, `semantic_turn_processor.py`, and the Operator presentation; focused Core checks passed 388 cases and Operator checks passed 322 cases; task-scoped Ruff and strict mypy passed; three mutations each failed exactly one guard. | Retain the governed request-to-Console and bilingual randomized evidence. |
| 2026-08-16 | implemented | Let the Korean next safe step read as a Korean sentence. Deriving the step from several measured gaps joined polite imperatives with a comma, so several steps now open with a lead-in and stand as their own sentences. | `current change`; focused processor checks passed 60 cases; the new case pins both the single-step and multi-step forms. | None for this row. |
| 2026-08-17 | implemented | Exposed recorded grounded RCA, impact evidence, and citations through the Incident semantic path. T0 records the existing finding severity and resource-type scope as bounded impact evidence while leaving unknown measurements empty. The Core projection requires a grounded hypothesis with matching citations before supporting a cause, and the Operator and Console render the three evidence sections in both locales without adding action authority. | `current change`; focused Core, Operator, and Console checks passed 138 cases; Ruff, strict mypy, and Console typecheck passed. | Retain authenticated Browser evidence for a recorded-RCA incident and an incident with explicit evidence gaps. |
| 2026-08-17 | implemented | Added an optional durable semantic outbox namespace that binds append, lease claim, request lookup, and projection ownership to one Operator run. The default namespace remains byte-compatible with production. Isolated assurance Operators use the run id so a concurrently running standard Operator cannot publish a measured request through another Core generation. | `current change`; focused Operator bridge, composition, and supervisor checks passed 114 cases; task-scoped Ruff and strict mypy passed. | Retain a fresh strict artifact on the centrally validated namespaced runner before starting the seeded cohort. |
| 2026-08-17 | implemented | Closed the active incident's three evidence gaps by projecting allowlisted notification terminal failures from their exact audit rows. `route_unresolved`, `trust_mismatch`, and `escalated_to_hil` yield bounded T0 cause, impact, and citation records; successful or unknown outcomes yield none. | `current change`; focused Incident, semantic processor, and Operator presentation checks passed 133 cases; Ruff and strict mypy passed. | Restart the exact-source local stack and retain authenticated positive-path evidence. |
| 2026-08-17 | implemented | Replaced semantic plan prompt v8 with v9 and pinned the verifier's exact declaration-aggregation and property-predicate envelopes. Declaration counts use one `query.manifest` dependency and one bounded aggregate output; existence, equality, and membership filters use only their accepted operand fields. | `current change`; two focused prompt contract tests passed against an isolated tracked catalog containing v9; task-scoped Ruff and strict mypy passed. | Retain a fresh strict artifact before starting the seeded cohort. |
| 2026-08-17 | implemented | Converted an explicit `MetricProviderError` in the read-only metric window adapter into a bounded unavailable incomplete window. Partial samples are discarded, the requested concept and window remain cited, and causal joins stay `UNRESOLVED` with `execution_authority=false`. Provider identity drift and malformed data still fail closed. | `current change`; focused metric and causal semantics checks passed 12 cases; task-scoped Ruff and strict mypy passed. | Retain fresh strict causal cells against the exact-source stack. |
| 2026-08-17 | implemented | Canonicalized a causal evidence join that redundantly names an ancestor scope already required by both metric-window dependencies. Planning removes only that transitive edge before rebuilding the exact plan digest; the principal manifest, metric concepts, verifier, and execution handlers remain unchanged. | `current change`; the focused semantic composition regression reproduces the prior `semantic_plan_invalid` result and now passes with the verified four-node read-only DAG. | Obtain a central validation receipt and retain a fresh English causal cell before rerunning the strict cohort. |
| 2026-08-17 | implemented | Built evidence-validation plans from the verified principal-scoped `Resource` descriptor after the typed frame is accepted. Frame prompt v8 keeps principal-scope evidence checks complete without a narrower model-selected subject, and plan prompt v10 records that Core owns the single secured ObjectSet plan. The exact release, manifest, role, purpose, ObjectSet schema, plan digest, and execution handler checks remain unchanged. | `current change`; focused tier-routing and prompt-registry regressions pass and prove that neither T1 nor T2 proposes the evidence plan. | Obtain central validation, then retain fresh bilingual evidence-validation cells before rerunning the strict cohort. |
| 2026-08-17 | implemented | Closed an overlap between the default semantic outbox claimant and run-scoped assurance rows. The namespace previously changed the key prefix, but the default `prefix%` claim still matched nested namespaced keys. Durable requests now carry an explicit namespace token, and claim, authenticated read, and projection ownership require exact namespace equality while legacy default rows map to the empty token. | `current change`; the focused Operator bridge suite passes 64 cases, including default-versus-namespaced claim, append, read, and projection regressions; task-scoped Ruff and strict mypy pass. Two prior strict artifacts advanced only 12/14 and 9/14 dedicated request topics, and bounded high-watermark reads found the missing five request/projection pairs on the standard physical stream. | Retain a fresh strict artifact with exact run-scoped transport coverage before starting the seeded cohort. |
| 2026-08-17 | implemented | Bound the runner's exact transport observation into each raw artifact as hashed topic identities and phase-specific counts, then required the same evidence in the repository-safe projector. This prevents an artifact that satisfies answer fields but lacks exact run-scoped request/projection coverage from being promoted later outside the runner. | `current change`; focused runner and baseline-projector checks passed 13 cases; task-scoped Ruff and strict mypy passed. | Retain a fresh strict artifact carrying exact 14/14 transport evidence. |
| 2026-08-17 | implemented | Assigned Vite dependency-cache ownership to each supervised assurance run. The prior exact-source attempt reached every service readiness check but the Browser preamble imported a stale shared MSAL optimizer URL before the first measured turn. The runner now supplies a cache under its run root while ordinary Console starts keep the standard default. | `current change`; the failed attempt ran zero questions and produced no artifact; focused Console cache checks passed 2 cases, the supervisor suite passed 9 cases, and Console typecheck, Ruff, and strict mypy passed. | Retain a fresh strict artifact from the centrally validated cache-isolated source. |
| 2026-08-17 | in-progress | The cache-isolated strict run completed all 14 live Browser turns but advanced its dedicated request and projection topics by only 6 each. Exact transport gating rejected the artifact and kept the seeded cohort closed. | [Issue #63](https://github.com/dotnetpower/fdai/issues/63); centrally validated source `40fbd0c41eda506e6976e3090fab3bd9502b98f0`; run `issue63-40fbd0c41e-20260817T084406Z`; 14 live, 0 resumed, transport 6/6. | Do not use the semantic dispositions as release evidence because eight turns crossed a non-owned physical stream. |
| 2026-08-17 | implemented | Changed run-scoped outbox keys from a child of the default key prefix to a sibling prefix. This closes ownership against stale default processes that still execute the former broad prefix claim, without changing production default keys or weakening exact namespace equality. | `current change`; focused bridge checks passed 62 cases; Ruff and strict mypy passed. | Retain a fresh strict artifact with exact 14/14 request and projection transport before starting the seeded cohort. |
| 2026-08-17 | implemented | Separated seeded question taxonomy from its broad operation buckets and versioned semantic frame prompt v9 after the first exact-transport full cohort exposed 11 answered capability mismatches. Prompt-specific valid plans remain accepted, while relationship traversal, causal evidence, retained-generation comparison, and evidence-property selection retain distinct capability requirements. | `current change`; focused prompt registry checks passed 5 cases and the assurance oracle passed 99 cases. | Obtain central validation and run one strict cohort before any further seeded run. |
| 2026-08-17 | implemented | Bound `validate` frames to the `evidence_validation` output family and moved topology cutoff ordering into deterministic plan verification. A misclassified evidence request now retries only the frame before Core builds the principal-scoped ObjectSet, and a topology snapshot whose event cutoff exceeds its knowledge cutoff is rejected before provider execution. Empty or incomplete retained history still returns typed incomplete evidence and is never synthesized. | `current change`; focused semantic-planning and query-verifier checks passed 41 cases; task-scoped Ruff and strict mypy passed. | Obtain central validation, then retain one strict 14/14 answered and complete-evidence artifact before the runner may start the seeded cohort. |
| 2026-08-17 | implemented | Versioned the semantic frame prompt to v10 after exact-source strict evidence showed one evidence-completeness request reaching bounded plan proposal instead of the server-owned evidence plan. The prompt now requires operation `validate` and output shape `evidence_validation` together for complete-scope and evidence-gap validation; the existing deterministic bidirectional frame guard remains authoritative. | `current change`; focused prompt-registry regression passes. | Obtain central validation, then retain a strict 14/14 answered and complete-evidence artifact before the runner may start seeded. |
| 2026-08-17 | implemented | Versioned the semantic plan prompt to v11 after the next strict run moved the sole unsupported cell to temporal comparison. The prompt now pins a generic three-node topology-diff DAG: two dependency-free `topology_at` sources with trusted cutoffs and one baseline-then-current `topology_diff` output. It adds no question route or provider data. | `current change`; focused prompt-registry regression passes. | Obtain central validation, then retain strict 14/14 evidence before seeded starts. |
| 2026-08-17 | implemented | Resolved only the remaining principal-scope subject clarification for a typed `validate` and `evidence_validation` frame. Core binds the existing readable `Resource` descriptor and builds the same secured ObjectSet; a concrete subject or any other clarification category remains unresolved. | `current change`; focused positive and negative tier-routing regressions pass. | Obtain central validation, then retain strict 14/14 evidence before seeded starts. |
| 2026-08-17 | implemented | Extended the server-owned evidence resolver to the typed `resource_identity` clarification when the evidence frame has no concrete subject, and bound `explain_change` bidirectionally to the `causal_evidence` output family. Concrete evidence subjects remain unresolved, and a misclassified causal frame retries through the bounded T1-first cascade instead of terminating before plan selection. | `current change`; focused evidence and causal tier-routing regressions pass. | Obtain central validation, then retain strict 14/14 evidence before seeded starts. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v11 with a final answer-shape audit. Cardinality and grouping stay `aggregation_table` even over schema declarations, while evidence sufficiency and coverage stay the typed `validate` and `evidence_validation` pair even when the request names resources, properties, identities, or relationships. No question literal, keyword router, provider fact, or authority was added. | `current change`; the focused prompt-registry contract passes. | Obtain central validation, then retain strict 14/14 evidence before seeded starts. |
| 2026-08-18 | implemented | Versioned the semantic plan prompt to v12 and closed the generic causal scope envelope. A principal-scoped causal plan uses an unfiltered `Resource` ObjectSet with no predicates, traversal, or root ids; model-selected scope restrictions remain terminal denials and cannot be retried into broader access. | `current change`; the focused prompt-registry contract and existing no-T2 scope-denial regression pass. | Obtain central validation, then retain strict 14/14 evidence before seeded starts. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v12 and separated evidence-property membership from evidence sufficiency. Requests to list or identify matching objects require `property_filtered_resources` with a nonempty predicate, while completeness and gap judgments retain the `validate` and `evidence_validation` pair. | `current change`; the focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v13 and separated evidence-contract subjects from runtime object properties. Claims, evidence references, verification coverage, and gaps use `validate` plus `evidence_validation`; only runtime ontology objects selected by a readable evidence-valued property use `property_filtered_resources`. | `current change`; the focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic plan prompt to v13 and pinned ObjectSet predicates to direct keys from the selected descriptor's readable `properties` map. Predicate inputs cannot use projected row paths or natural-language aliases; downstream `properties.<name>` paths remain presentation-only. | `current change`; the focused ObjectSet prompt contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v14 and preserved the axis defining membership. Added, removed, or changed objects across retained generations remain `temporal_comparison` even for list answers, while current objects selected by an evidence state remain `property_filtered_resources`. | `current change`; the focused frame prompt contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Added deterministic frame invariants and prompt v15 for two typed boundaries exposed by randomized assurance. `ontology_manifest` now accepts only canonical declaration-kind subjects, and a `select` resource listing cannot silently discard nonempty measure concepts; invalid T1 frames retry through the bounded T2 frame cascade before planning. | `current change`; full semantic planning tier-routing and prompt-registry tests pass, with Ruff and strict mypy green. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Central changed-tests falsified both proposed deterministic invariants: legitimate Pod telemetry path queries use a `Resource` subject and nonempty measure concepts with a resource-list output. Removed the runtime invariants and retained prompt v15 only for the generic answer-shape distinction between declaration inventory, runtime membership, and explicit grouping. | Failed central source `90c592bfcc673e4764e4480be7ffa54c5b66b0b8`; both Pod telemetry composition regressions now pass locally with the adjacent prompt suite. | Centrally validate the corrected prompt-only v15 slice, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v16 and separated an ordinary incident noun from an authoritative incident reference. `incident_evidence` now requires one exact trusted binding or both exact request identities; generic support or refutation of a stated cause remains `explain_change` with `causal_evidence`. | `current change`; focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic plan prompt to v14 and added a final closed-shape audit for generic causal evidence: one unfiltered visible Resource scope, two scoped metric windows using the supplied cause and effect concepts, and one evidence-join output. Specialized incident functions, filtered scopes, roots, and standalone ObjectSet outputs are excluded from this family. | `current change`; focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v17 and added a final family audit for two ambiguous surfaces. A question asking whether a change preceded an observed regression remains causal evidence, while ontology objects in the current inventory generation select the principal-scoped declaration manifest; retained-generation object deltas remain temporal comparison. | `current change`; focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v18 and added a final relationship-scope audit. The specialized schema relationship family requires one or two exact ObjectType declarations; current visible or readable inventory connectivity and containment remain operational topology traversal. | `current change`; focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v19 and made a generic visible-resource causal request complete when it states one cause and one effect concept. The frame must return causal evidence without asking the operator for resource identity, incident binding, a narrower subject, or provider metric identifiers; grounding remains a later verifier concern. | `current change`; focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | implemented | Versioned the semantic frame prompt to v20 and made a requested runtime relation verb dominate ontology nouns. Visible resources that depend on, route to, connect to, attach to, or contain another object remain topology traversal even when the target is called an ontology object; declaration manifests never answer runtime membership. | `current change`; focused prompt-registry contract passes. | Obtain central validation, then rerun strict before any seeded cohort. |
| 2026-08-18 | validated | Completed the governed strict-to-seeded ontology assurance on centrally validated source `a38922762ed805794b11bb9c6aaef43916f6f6c4`. Strict passed 14/14 and seeded passed 100/100 with exact transport, 85 evidence-complete answers, 9 governed action drafts, 6 governed clarifications, zero capability mismatches, zero unsupported operational claims, and zero unauthorized executions. | Run `issue63-a38922762e-20260818T034940Z`; repository-safe baseline [`ontology-query-randomized-assurance-2026-08-18.json`](../../baselines/ontology-query-randomized-assurance-2026-08-18.json). | Preserve the raw local artifact and validate the baseline commit on the latest integration source. |
| 2026-08-18 | implemented | Kept the verified-rows answer table readable. The table projected every ontology field, so an open-shape property bag rendered as several hundred characters of serialized JSON in one cell and the answer repeated machine output the technical-details trajectory already carries. The table now projects scalar fields only and lifts named scalar leaves out of a nested bag; the exact untouched row is unchanged. | `current change`; [Issue #180](https://github.com/dotnetpower/fdai/issues/180); focused Operator checks passed 392 cases with one new nested-bag regression; task-scoped Ruff and strict mypy passed; a live authenticated turn rendered id, object type, name, and type columns instead of a JSON blob. | Retain the governed request-to-Console and bilingual randomized evidence for the readable answer table. |
| 2026-08-18 | implemented | Bound each resource subtype's own request terms to the `Resource.type` value domain. The domain projected only category and explicit query groups, so a question naming one subtype had no declared value to bind and the planner fell back to an existence predicate that selected the whole ObjectType. The plan-shape log now names the selected ObjectType and each filtered property with its operator, and never a predicate operand. | `current change`; [Issue #183](https://github.com/dotnetpower/fdai/issues/183); focused composition, conversation, ontology-platform, and rule-catalog checks passed 2222 cases; task-scoped Ruff and strict mypy passed; a live turn moved from `object_set[Resource;type exists]` returning 504 rows to `object_set[Resource;type equals]` returning the 42 resource groups. | Retain the governed request-to-Console and bilingual randomized evidence for the narrowed property filter. |
| 2026-08-18 | implemented | Made a verified list answer readable at a glance. A complete categorical result now renders a bounded bar distribution, a truncated result states how many of the verified total are listed and where the rest stay, named readable fields lead the table ahead of the opaque identifier, and a single-item summary fills its row instead of leaving empty grid cells. An incomplete or capped result renders no chart so a partial count cannot read as the whole population. | `current change`; [Issue #184](https://github.com/dotnetpower/fdai/issues/184); focused Operator checks passed 394 cases with two new chart regressions; Console typecheck, task-scoped Ruff, and strict mypy passed; a live authenticated turn listed 20 of 42 verified rows with its bound stated. | Retain the governed request-to-Console and bilingual randomized evidence for the chart and row-bound notice. |
| 2026-08-18 | implemented | Made the semantic turn's observed phases addressable steps on the stream. The Operator emitted only progress events, so the Console's stepped observed-process timeline had nothing to render and a turn showed one frozen line until the answer arrived. Each observed phase now also emits a bounded step, and the waiting step reports running until a terminal projection exists and is settled before the terminal event for every disposition. | `current change`; [Issue #187](https://github.com/dotnetpower/fdai/issues/187); focused Operator checks passed 394 cases; Ruff and strict mypy passed; a live turn rendered a running waiting step and five completed steps. | Core publishes one terminal projection, so planning substages stay unobserved by the stream and are not reported. |
| 2026-08-18 | implemented | Reported the verified query on its own step. The query and its row counts already travelled in the terminal projection, but nothing mapped them onto the evidence step, so the Console's command block had no source. The evidence step now carries a bounded read-only execution record built from the projected intent graph and the reported output counts. | `current change`; [Issue #188](https://github.com/dotnetpower/fdai/issues/188); focused Operator checks passed 396 cases; Ruff and strict mypy passed; a live turn rendered the ObjectSet definition as code. | A multi-goal plan reports no command, so a composed query stays visible only in technical details. |
| 2026-08-19 | implemented | Restricted `query.manifest` aggregation to frames that request the exact same canonical declaration kinds. Incident and audit-log count questions can no longer substitute declaration rows and become unsupported after the bounded T1/T2 proposals fail deterministic verification, while matching declaration counts remain valid. | `current change`; [Issue #174](https://github.com/dotnetpower/fdai/issues/174); the complete semantic planning tier-routing suite passed 40 cases, including the three reproduced operational-count questions and a declaration-kind mismatch retry. | Rerun governed conversational assurance before treating this correction as fresh production evidence. |
| 2026-08-20 | implemented | Preserved all explicit filters when a model proposed only `Resource.name exists`. Core now combines the exact frame-carried name fragment with the catalog-declared resource-type value group, producing a narrowed name fragment and resource-type predicate instead of executing the complete Resource set. A subject absent from the operator utterance is never promoted to an operand. | `current change`; `semantic_planning_value_filters.py`; `test_semantic_planning.py`; the focused semantic-planning file passed 27 cases in English and Korean. | Restart Core and retain an authenticated Console result that shows the narrowed verified query, readable table, bounded trace, and no horizontal overflow. |
| 2026-08-27 | implemented | Added a server-owned contextual Resource FunctionType and exact screen/resource-group binding. The Console publishes selected resource ids, Operator validates the typed context, and Core compiles an id-filtered ObjectSet before the contextual read. A widened or incomplete context returns a typed limitation, never the principal-visible Resource set. Incident presentation now exposes only profile, correlated evidence, explicit gaps, and a candidate `SemanticOperation.ACTION_DRAFT`. | `current change`; focused contextual FunctionType, semantic planning, service-contract, Operator envelope, and Console context checks passed; authenticated evidence remains open. | Retain authenticated screen/resource-group and incident Console receipts. |
| 2026-08-27 | implemented | Context selections now use an opaque server-issued token bound to the authenticated principal, ordinary lowercase role scope, purpose, ontology release, source generation, complete flag, and exact ids. Operator transports it through the dedicated redaction allowlist, resolves it before building the semantic turn, and rejects client-forged or recomputed ids and tokens lost after restart. Core requires the capability on the contextual FunctionType itself, rejects disconnected specialized plans, and intersects every explicit utterance filter in an object-only contextual read. | `current change`; focused bilingual Core/Operator/Console tests, typecheck, Ruff, and pre-commit passed. | Retain authenticated receipt after the server issues a complete selection token. |
| 2026-09-01 | implemented | Closed the terminal evidence-lineage gap from `QueryNodeResult` through `GoalTaskReceipt` and `intent_graph_evidence` to the Operator `done` payload. Every projected claim now names evidence references from completed goals, and trajectory or execution detail is emitted only for an exact matching read receipt. Complete and incomplete zero-row results remain distinct, while provider failures retain the bounded reason and observation time without provider text. | `current change`; focused Core and Operator semantic suites passed 207 cases; Conversation Assurance contract checks passed 78 cases; Ruff and formatting passed. | Run a separately authorized governed campaign before treating the implementation checks as live Conversation Assurance evidence. |
| 2026-09-08 | implemented | Exposed the exact bounded ObjectSet definition only after its goal, completed receipt, and numeric row-count output agree. The same no-authority Run record carries returned, total, completeness, and truncation metadata, while running progress and contradictory counts retain the capability-only form. | `current change`; [Issue #241](https://github.com/dotnetpower/fdai/issues/241); 153 focused Operator tests, Ruff, formatting, strict source mypy, Console typecheck, and the authenticated three-viewport Run record scenario passed. | None for verified ObjectSet and row-count visibility. |
| 2026-09-17 | implemented | Refreshed the deterministic answered competency receipts to the current structural release and Reader manifest. Fixture evidence still reports `production_ready=false`. | `current change`; `uv run python scripts/quality/architecture/check-ontology-query-coverage.py` passed with 10 questions and four principal manifests. | Retain cross-service and live-assurance receipts before requesting production readiness. |
| 2026-09-17 | implemented | Corrected resource-group collection normalization after an authenticated Korean name-filter question was widened into exact-parent membership and returned child Resources. Collection, list, and name-filter facets now compile the grounded fragment as `Resource.name contains` plus `Resource.type=resource-group`, while exact named-group requests retain `Resource.parent_id`. | `current change`; `semantic_target_candidate_planning.py`; `semantic_planning_frame_normalization.py`; `test_semantic_planning.py`; six live-shape and neighboring regressions, nine resource-group planning cases, and all 200 semantic-planning cases passed. After managed `11/11` restart, the same authenticated local Console question returned eight bounded Resource Group rows, 4/4 completed evidence phases, and no child-resource types in 5.4 seconds. | No remaining implementation work for this bounded normalization defect. Governed production assurance remains open. |
| 2026-09-13 | implemented | Corrected two live-answer planning defects: mixed English/Korean value terms no longer match inside longer ASCII product names, a validated subtype target disambiguates output-field type words, and an explicit Korean 24-hour recent-state window no longer becomes the one-hour default. | `current change`; exact production-vocabulary reproduction; focused preflight, value-grounding, semantic-planning, and assurance tests passed 803 cases. | Restart Core and retain fresh authenticated answers before raising these corrections to validated. Transition coverage remains incomplete where the retained source cannot prove the whole requested interval. |
| 2026-09-11 | implemented | Refreshed semantic-intent coverage after the recovery decision boundary and agent-owned observation route changed the source set. | `current change`; focused coverage and decision-boundary tests passed 15 cases. | No query authority change. |
| 2026-09-11 | implemented | Refreshed the 400-question review bank after the safeguard-bound service-contract projection changed. | `current change`; generated artifacts and focused parity tests. | Retain governed randomized and cross-service assurance evidence before a production-readiness claim. |
| 2026-09-11 | implemented | Rematerialized the 400-question review bank after its generated Console contract source changed. | `current change`; `build_question_bank.py`; focused source parity tests. | Retain governed randomized and cross-service assurance evidence before a production-readiness claim. |
| 2026-09-09 | validated | Closed the current-subscription semantic path with an authenticated verified read after the accepted-judgment and session-partition hardening reached `main`. | Issue #151 evidence comment; merged revision `33f830431`; sanitized runtime receipt `sha256:580e1c309d249699e6e352d37279b1e3f640b46e0c0890342e9a1f769fb0a8b7`. | None for current-subscription identity. |
| 2026-09-09 | implemented | Bound semantic Kafka publication to the opaque session partition key instead of the per-turn request UUID. | `current change`; focused Operator semantic transport tests. | Retain the governed authenticated subscription receipt required by issue #151. |
| 2026-09-08 | implemented | Closed rejected operational-summary fallback, mixed-language named-subscription detection, and truncated Resource-list overclaiming without changing provider reads or execution authority. | `current change`; focused regressions, 822 Resource-planning tests, and 62 Console stream tests passed. | Keep the named blocked provider targets and authenticated runtime evidence open until their exact evidence exists. |
| 2026-09-08 | validated | Corrected collection state reads to decode property-keyed `state_fact_metadata.state`, retain legacy flat metadata compatibility, prefilter broad Resource scopes to reviewed state-capable types with observed state metadata, preserve verified positive matches when source coverage is incomplete, and render the verified names and observed states instead of only a row count. | `current change`; 34 focused Resource state FunctionType, deterministic planner, and answer renderer tests; targeted Ruff and strict mypy; standard-stack 9/9 readiness. In the authenticated Console, three fresh Korean stopped-database questions each returned the same five verified MySQL and PostgreSQL names. The broad non-running Resource control returned verified stopped or deallocated VM, Kubernetes, MySQL, and PostgreSQL rows without exposing `authorization.role-assignment`. | Retain the same behavior in the governed bilingual release campaign; local source coverage remains explicitly incomplete and does not establish exhaustive absence. |
| 2026-09-07 | validated | Stabilized the reviewed Resource configuration and gateway-diagnostic families across Korean and English compact preflight. Core now retains one exact runtime identifier, drops server-owned subscription scope and generic gateway labels before authority binding, derives only typed server-owned recent windows, and normalizes redundant non-authority facets. The verified DAG remains the sole read authority, and Console retains its explicit partial-evidence hold. | `current change`; 1,184 focused Python tests passed with one optional dependency skipped; the narrowed planning slice passed 387 tests; Ruff, strict mypy, Console tests, typecheck, production build, and documentation gates passed. Authenticated standard-Console evidence covered six final Korean fresh-conversation turns and three English parity turns, all with one model call, bounded 2,236-2,315 token totals, sub-100-ms progress, deterministic frames and plans, explicit evidence holds, and no `authorization.role-assignment` output. The Korean Resource Group query returned the 12 observed names with one 2,249-token model call, and Service Health returned nine complete advisories with one 2,190-token call. | No remaining work for the bounded Resource configuration, gateway diagnostic, Resource Group filter, and Service Health paths in this change. |
| 2026-09-07 | validated | Removed full-manifest schema injection from subscription identity, subscription Service Health, and Resource Health collection planning. Compact preflight v6 now promotes targetless subscription families, and subscription identity and Service Health reuse deterministic function-backed frames and server plans. | `current change`; 349 focused tests passed; targeted Ruff and strict mypy passed; the isolated production-adapter Korean subscription turn retained `answered`, `subscription_scope_identity`, and authoritative read evidence while total model tokens fell from 153,512 to 1,978 and duration fell from 14.012 to 2.800 seconds. The authenticated standard Console independently showed one 1,984-token preflight call, the exact v6 system message, candidate-only typed output, deterministic function execution, `server_subscription_scope`, and 1/1 verification. | No remaining work for this bounded subscription token regression. |
| 2026-09-07 | implemented | Hardened the complete operational chat query path against scope broadening, hidden role-assignment rows, ARM identity loss, oversized planner context, malformed preflight output, Rule projection conflict misclassification, replay over-return, and Kafka physical-topic collision. | `current change`; task-owned Core, Operator, prompt-source guard, and focused regression paths; 312 Core and prompt tests and 1,559 Operator tests passed; targeted Ruff and strict mypy passed. | Restart the standard local services with the exact changed inputs and retain authenticated Console evidence for the corrected collection, exact-name, exact-ARM-id, conflict-posture, and progressive-rendering scenarios. |
| 2026-09-07 | implemented | Added a compact LLM-classified Resource collection family that reuses the preflight result for type-only and type-plus-current-state lists. Core still grounds every filter through reviewed ontology and state catalogs, and every property-filtered plan source now receives the nonempty-predicate alignment check. Strengthened the route after an observed `배포된 llm 모델이 뭐야` turn incorrectly entered the evidence-free adaptive path. | `current change`; prompt v5; Korean and English AKS, DB, MSSQL, and deployed-LLM preflight cases; the deployed-LLM path compiles only `Resource.type=llm-model-deployment` and no free-text name filter; 358 focused preflight, planning, prompt, vocabulary, and catalog-grounding tests passed; Ruff and strict mypy passed; Core restarted with the changed input digest. A fresh authenticated isolated Console turn passed with an answered `property_filtered_resources` receipt, a verified read, no identity clarification, first token within 5 seconds, and 4.3-second test duration. | Retain Browser Entra evidence when the shared CDP session is available; ambiguous, named-scope, metric, history, relationship, diagnosis, document, and action questions continue through full semantic judgment. |
| 2026-09-07 | implemented | Distinguished a declared resource-group type phrase from an exact named resource-group target before membership normalization. A generic `리소스그룹 목록` request now compiles `Resource.type=resource-group` instead of `Resource.parent_id` with no predicate. | `current change`; the original Core log recorded `property_filtered_resources`, `parent_id,type`, and `object_set[Resource;no-predicate]`; focused semantic planning passed 167 cases, with two bilingual regressions; Ruff and strict mypy passed; managed Core restart used the changed source digest. | A fresh authenticated re-ask remains optional live model validation and was not repeated automatically. |
| 2026-09-07 | implemented | Rejected generic APIM, gateway, backend, and GPT category labels as exact operational identities and terminated ambiguous targetless comparisons before frame planning. | `current change`; 238 focused tests; post-fix Browser Entra F4 trace had no frame call or provider read. | Retain exact-target F3/F4 evidence. |
| 2026-09-07 | implemented | Added the bounded F1-F4 preflight proposal and verified reuse path without widening read capability or execution authority. | `current change`; 177 focused tests, targeted Ruff, and strict mypy passed. | Retain standard-stack answer-token TTFT and complete evidence receipts. |
| 2026-09-06 | implemented | Preserved the stable `semantic_frame_unavailable` reason when semantic model candidates cannot produce a frame. The terminal result now reports planner unavailability instead of claiming that an authoritative evidence read was attempted and held. | `current change`; `semantic_turn_processor.py`; focused processor checks passed 3 cases; changed Python files passed Ruff. | Restore private Azure OpenAI reachability and retain one authenticated incident turn that reaches the bound incident evidence plan. |
| 2026-08-28 | implemented | Normalized two internally inconsistent model axes for principal-scoped current resource listings. `resource_list` no longer requests one exact `resource_identity`, and a model-invented historical scope is removed when the reviewed inventory language finds no temporal signal. Exact-target and genuinely temporal requests remain unchanged. | `current change`; focused semantic planning checks passed 485 cases; full Console typecheck and production build passed; after managed Core and Operator restart, two authenticated Korean Browser phrasings returned the verified principal-visible resource list with 918 total rows and 20 bounded display rows instead of clarification. | None for the unfiltered current resource-list regression. |
| 2026-08-27 | implemented | Refreshed the deterministic answered-question fixtures against the current structural ontology release and Reader manifest, and narrowed design routing by removing the redundant full Operator Console owner from this query-specific context. | `current change`; `config/ontology-query-competency.json`; `scripts/lib/design-routes.json`; ontology query coverage and design-route checks. | Governed cross-service and live assurance remain separate from deterministic fixture validation. |
| 2026-08-24 | validated | Retained an authenticated fresh-conversation Browser turn for the typed greeting response and removed the internal route marker from the visible source badge. | `current change`; 586 focused Python checks and 117 focused Console checks passed; the standard 6/6 local stack rendered the Korean greeting with zero query rows, source or verification badges, citations, Investigation activity, Run records, or horizontal overflow; Core recorded zero frame, plan, query-execution, and executor-dispatch events. | None for the bounded greeting direct-response path. |
| 2026-08-24 | implemented | Added a typed evidence-free direct-response path for greeting intent and preserved query planning for greeting-prefixed operational intent. | `current change`; focused service-contract, Core planning, processor, coverage, Operator, channel, and Console decoder regressions. | Retain an authenticated fresh-conversation Browser turn proving a greeting renders without `query.manifest`, verified rows, or execution evidence. |
| 2026-08-24 | implemented | Refreshed the four answered deterministic competency fixtures against the current structural ontology release and Reader principal manifest without changing their fixture-only evidence authority. | `current change`; `config/ontology-query-competency.json`; `uv run python scripts/quality/architecture/check-ontology-query-coverage.py` passed with 10 questions, 4 principal manifests, and 10 question receipts. | Governed cross-service or live assurance remains required before `production_ready=true`. |
| 2026-08-24 | implemented | Aligned query coverage with the implemented structural model by accounting for ResourceClass closure, ordered typed paths, and LinkType roles and semantic traits in the manifest, verifier, executor, and baseline. | `current change`; paired query-plan and structural documents plus focused catalog, query, composition, and documentation gates. | No remaining query-plan documentation work for this bounded structural handoff. Governed production assurance remains open below. |
| 2026-09-04 | implemented | Isolated model authentication from semantic transport, enforced one authoritative terminal result, corrected named resource-group membership, and removed non-selectable role assignments from its generic member set. General identity readiness now degrades capability instead of stopping event consumption, every ordinary semantic turn performs a bounded model-audience preflight, and Core builds the named-group `Resource.parent_id` frame directly from compatible typed judgment. | `current change`; focused Core, Operator, judgment, planning, prompt, and composition checks; changed Python modules passed Ruff and strict mypy; authenticated local Console evidence returned 24 verified group members with `parent_id contains` plus `type not_equals authorization.role-assignment`, no role-assignment display row, and zero semantic consumer lag. | Retain a separately authorized wrong-tenant live hold before raising model-identity failure isolation to validated. |

### Remaining work

- [x] Connect isolated durable instance generations, owned activation, principal isolation, complete source binding, cache recovery, invalidation, rollback and bounded retirement to the runtime; local implementation evidence is recorded above. This does not qualify semantic ranking or live deployment.
- [x] Run Rule PostgreSQL parity/cache and lifecycle checks in owned isolated schemas: all three integration tests passed on 2026-09-22 using the existing validation database authority, without service-role DDL grants or shared-table cleanup.
- [x] Verify the configured model endpoint's actual subscription membership: read-only Azure observation on 2026-09-22 confirmed the current subscription. The all-zero artifact subscription field is missing metadata, not target drift.
- [ ] Restore the managed VS Code restart task, apply fresh observed embedding metadata, and verify the actual process consumes the new model digest. Preserve the original backup and reject unrelated source/configuration drift.
- [ ] Under bounded live authorization, bind observed embedding-space/model-version identity and pass frozen calibration plus disjoint English/Korean positive, negative, ambiguous and adversarial cohorts before enabling semantic ranking.
- [ ] After all remaining implementation and required evidence are complete, perform at least ten final critique/hardening rounds and retain a supported Medium-or-higher closure assessment. The ten local index rounds above are not credited to this final gate after findings 23 and 24 were reopened.
- [x] Retain authenticated EN/KO object-only manifest receipts: 2026-09-22 browser-stream canaries and exact durable Operator projections both verified complete evidence and no execution authority. Loaded-source and DOM certification remain separate open gates below.
- [x] Observe both EN/KO manifest answers through actual Console input and Send controls: 2026-09-22 UI receipt records verified evidence, no-authority notice and no dialog overflow. Loaded-source certification remains open.
- [ ] Run an authorized active-versus-compact judgment, frame, and plan cohort on one exact source
  revision before changing any active semantic prompt profile.
- [ ] Bind the secured query admission provider to the trusted verifier registry and retain one authenticated `DecisionCriticalEvidenceReceipt` plus independent bundle from the cross-service query path before restoring a positive decision-critical Function result.
- [ ] Retain authenticated S12 and S1 exact-target evidence from a complete Kubernetes generation. S12 needs Deployment rollout and new-cutoff recovery receipts. S1 additionally needs event, change, replica, and replacement-UID evidence so current Pod state cannot become a cause or historical recovery claim.
- [ ] Complete a separate 7/7 exact-target Container Apps follow-up receipt. The verified `MemoryPercentage` aggregate and authenticated bounded chart are retained; add typed ingress projection, bounded seven-day activity, and deterministic causal completion without generic ObjectSet substitution.
- [ ] Record one governed request-to-Console receipt that covers Operator publication, Core processing, exact Operator projection read, and authenticated Console rendering; rerun the bilingual randomized assurance cohort and link both passing evidence records.
- [x] Bind `query.resource_event_history` to independently routed Azure Resource Health and Kubernetes Event sources with exact scope, bounded lookback, explicit limitations, and no execution authority.
- [x] Bind dedicated `query.resource_health_inventory`, `query.resource_metric_inventory`, inventory/state coverage, and contextual-resource FunctionTypes. Preserve typed unavailable results until each authoritative evidence source and verifier schema is present.
- [x] Carry one authenticated selected screen or resource-group identity through the Console, Operator, and Core contracts, then prove that a contextual collection query cannot widen to the principal-visible Resource set.
- [x] Project canonical `Incident` instances into the current ontology store for bounded `ObjectSet` selection.
- [x] Register the read-only `query.incident_evidence` FunctionType over correlation-scoped audit evidence with an optional citation-grounded recorded cause, impact evidence, citations, and explicit gaps.
- [x] Constrain the incident answer to profile, correlated evidence, and explicit gaps, and represent the next safe step only as a candidate `SemanticOperation.ACTION_DRAFT`; retain the authenticated Console receipt.
- [x] Deliver the bound conversation context from the Console through Operator to Core as additive typed request state.
- [x] Compose the durable semantic index, historical topology reader, metric-series, and evidence-join providers with typed unavailable behavior and focused checks.
- [ ] Complete a separate 7/7 exact-target Container Apps follow-up receipt. The verified
  `MemoryPercentage` aggregate and authenticated bounded chart are retained; add typed ingress
  projection, bounded seven-day activity, and deterministic causal completion without generic
  ObjectSet substitution.

- [ ] Bind dedicated `query.resource_health_inventory`, `query.resource_metric_inventory`, `query.resource_event_history`, inventory/state coverage, and contextual-resource FunctionTypes. Preserve typed unavailable results until each authoritative evidence source and verifier schema is present.

- [ ] Carry one authenticated selected screen or resource-group identity through the Console, Operator, and Core contracts, then prove that a contextual collection query cannot widen to the principal-visible Resource set.

- [ ] Constrain the incident answer to profile, correlated evidence, and explicit gaps, and represent the next safe step only as a candidate `SemanticOperation.ACTION_DRAFT`; retain the authenticated Console receipt.
