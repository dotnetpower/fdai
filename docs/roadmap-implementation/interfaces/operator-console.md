# FDAI Console Conversations implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope
| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Governed sign-in authority copy | implemented | `console/src/i18n/messages.{en,ko}.json`; catalog parity, punctuation, and readable-Hangul checks | Sign-in describes a governed operator surface. Typed requests remain server-authorized, managed-resource execution stays outside the browser, and high-risk actions still require human approval. |
| Deployment-owned browser binding | implemented | `standalone_application.py`; `genesis_entra.py`; `publish-console.sh`; focused standalone, Entra, and publisher tests | After AKS convergence, the deployment coordinator registers the exact Static Web Apps redirect and publishes the signed prebuilt Console with APIM runtime bindings. Browser and Operator identities gain no deployment or execution authority. Fresh-subscription operational evidence remains open. |
| Bundled Manual Studio discovery | implemented | `console/src/components/help-center.tsx`; `console/src/components/help-center.test.ts`; focused Help Center tests | A production Console without an explicit Manual Studio override resolves the catalog from its bundled same-origin `/manuals` path. An explicit validated HTTPS base URL still selects separate hosting. |
| Scope-consistent Operator Memory review | implemented | `runtime_projection_reader.py`; `operator-memory.tsx`; Operator checks (`26 passed`), memory model (`2 passed`), type checks, five local PostgreSQL temporary-table scenarios, and isolated browser measurements | Both collections use the same parameterized predicates before the row limit. Candidate scope is visible; English keyboard filtering and Korean 1440/993/390/320 px bounded layout passed with explicit synthetic rows. No mutation controls or live-data claims are added. |
| Provisioning replay source and evidence workspace | implemented | `services/operator-service/src/fdai_operator_service/runtime.py`; `console/src/routes/provision*.tsx`; `console/tests/e2e/provisioning-progress.spec.ts`; focused Console and Operator checks; Console production build | The Operator source manifest now owns the durable replay only with PostgreSQL configured. The Console resets Sample and Live state, presents distinct source/run states, and follows the mock's run-summary plus Stages, Readiness, and Resources hierarchy through 320 CSS pixels without gaining deployment authority. |
| Focused Sample VM review journey | implemented | `console/src/routes/live*`; `operations.sample*`; `hil-queue.tsx`; bilingual Live catalog; focused unit and browser tests, typecheck, production build, independent review, and three-viewport browser measurements | Sample mode presents one stable synthetic `ops.start-vm@1.0.0` correlation from question through effect verification and one distinct pending-approval correlation with coherent Incident, Trace, Audit, and RCA drill-downs. Synthetic references without a Sample detail projection are not linked into Live data. It grants no authority and changes no Live runtime behavior. |
| Process workspace design parity | implemented | `console/src/routes/processes.tsx`; `processes.css`; `operations.sample-processes.ts`; `processes-mock-parity.spec.ts`; `workflow-process-transitions.spec.ts`; 43 focused unit tests, typecheck, and 19 desktop/responsive browser scenarios passed | Loaded counts, nine facts, selected-run provenance and collapsed evidence follow the mock. Sample has three generic runs without permitted transitions; no Live source or authority changed. Light/dark status text and keyboard focus contrast are measured. |
| Reports evidence workbench parity | implemented | `console/src/routes/reports*`; bilingual Console catalogs; `console/tests/e2e/reports*.ts`; 17 focused unit tests; 12 desktop, constrained, and mobile browser scenarios; final production build; six Operator report projection tests; UI rubric review | Reports presents server-derived catalog, source, renderer, variable, widget, and provenance state without copying mock values. Awaiting render, zero widgets, partial or unavailable evidence, and synthetic evidence remain distinct. Browser fixtures are explicitly synthetic; authoritative Incident RCA materialization remains server-owned. |
| Private local Console preparation boundary | validated | `prepare-console-full-stack.sh`; focused startup tests; standard local supervisor receipt (`10/10` ready) | Only the two server-owned no-deployment scope selectors are read from the gitignored Console environment when process values are absent. Duplicate keys fail closed, the selectors grant no execution authority, and an explicit resolved-model override binds its absolute path and file bytes into both preparation cache identities. |
| CLI protected-session and terminal experience | implemented | `cli/src/operator-api-session.ts`; `cli/src/terminal-capabilities.ts`; `cli/src/cockpit*.ts`; `fdai_operator_service/postgres_sql.py`; `tools/console.py`; CLI tests (`93 passed`); focused HIL/KPI tests (`5 passed`); launcher tests (`8 passed`); strict type checks; live loopback Azure CLI read | The CLI bootstraps only the server-owned loopback session, keeps its bearer in memory, and attaches it to snapshot, chat, and SSE reads. Unavailable sources remain unavailable, while count-only approvals retain their authoritative total without exposing details. The full-screen cockpit exposes read-only, auth, connection, model, timing, help, no-color, reduced-motion, and TTY-fallback state without receiving action authority. |
| Generated TypeScript service-contract view | implemented | `console/src/generated/service-contracts.ts`; `packages/service-contracts/contract-generation.json`; focused generation check and Console typecheck | Console compiles the same manifest-owned N/N-1 schema projection as the backend Python package. Runtime payload validation remains server-owned, and the generated view grants no authority. |
| Generated conversation coverage chain | implemented | Console catalogs; generated question bank and semantic-intent coverage artifacts; focused artifact-equality tests | Joined catalog changes regenerate the question bank before dependent semantic coverage so both artifacts bind the final source digests without changing question identity or runtime authority. |
| Document ingestion capability and version guidance | implemented | `document-ingestion.tsx`; `document-library*.tsx`; focused Console and browser checks | The picker follows server-advertised formats and rejects unsupported or legacy Office extensions before transfer. The library groups exact same-name records, loads immutable history on demand, identifies the active version, and binds preview, download, and delete to the selected version. Server and worker validation remain authoritative. |
| Document OCR provider settings | implemented | `document-ocr-settings.tsx`; model settings decoder and command client; bilingual route catalog; focused Console checks and production build | Owners can request local or Azure OCR, retry a protected plan, retain Azure while using local OCR, or request removal. The panel grants no apply authority and displays only deployment-confirmed effective state. |
| Durable/live current-state activity identity | implemented | `read_investigation_latency.py`; `fdai_operator_service/activity_projection.py`; focused persistence and projection tests (`6 passed`) | Snapshot replay and live frames converge on one hashed-correlation activity id without persisting the operator question, resource identity, or execution authority. |
| Optional Console projection availability | implemented | `console/src/api-transport.ts`; `console/src/api.ts`; `console/src/api-cost-governance-projection.ts`; bilingual Cost Governance route catalogs; focused transport and route tests and `npm --prefix console run typecheck` | Typed optional-source absence renders as unavailable. Only explicitly classified source-gate `503` responses join `404` and `501`; generic service `503`, authentication, unexpected server, and decoder failures remain errors. Cost Governance uses one complete reason vocabulary for its type and decoder and localizes each known value without accepting unknown contract data. |
| Runtime-neutral API base publication | implemented | `scripts/deployment/azure/publish-console.sh`; focused publisher and service-deployment workflow regressions (`8 passed`) | Publication prefers a profile-provided HTTPS browser gateway and otherwise preserves the Container Apps FQDN. Both paths retain root Operator API routes and the same bearer boundary. |
| Vertical outcome evidence states | implemented | `console/src/routes/vertical-outcomes.tsx`; `console/src/routes/analytics-data.ts`; `console/src/routes/dashboard.sample.ts`; `console/src/styles.css`; bilingual analytics catalog; `console/src/routes/analytics-hubs.test.ts`; `console/src/routes/analytics-data.test.ts`; `console/tests/e2e/dashboard-essential.spec.ts`; `console/tests/e2e/vertical-outcomes.spec.ts`; focused analytics, localization, and browser checks; Console typecheck and production build | The three canonical domains remain visible when attribution is absent. Missing domain values and zero-event savings remain unavailable across primary and comparison views, while an observed zero with a non-empty sample remains measured. Live drill-downs preserve only supported filters; Sample links do not claim operational records. Request-keyed rendering returns to loading immediately when the client or data mode changes. Sample KPI and autonomy fixtures pass their live decoders. The native comparison table has one focusable domain link per row and bounded overflow through 320 CSS pixels. |
| Framework control status separation | implemented | `wara_projection.py`; `best-practice-controls.model.ts`; `wara-controls.model.ts`; `wara-controls.tsx`; focused control model tests and Console typecheck | WAF and WARA catalog presence and mapping are displayed separately from scope-bound evaluation, applicability, and satisfaction. The WARA consumer requires exact active coverage and no authority before exposing source, evidence, and limitation metadata. |
| Rules catalog workbench and findings fallback | implemented | `rule-catalog*.tsx`; bilingual Governance catalog; Operator workflow adapter; focused Rules unit and browser checks; Console production build | The production route follows the approved Catalog views rail and detail-table hierarchy using authoritative catalog values. Known rules without a findings provider render an explicit not-evaluated state instead of an operational error. |
| Workflow builder published-definition workspace | implemented | `workflow-builder*.tsx`; `workflow-builder.workspace.css`; bilingual Workflow catalog; focused unit, type, build, and three-viewport browser checks | The default route follows the reviewed 220/catalog, ordered-contract, and 310/inspector hierarchy using authoritative workflow and ActionType values. Optional definition and Python-task failures do not hide the built-in catalog, and the browser gains no execution authority. |
| AKS commerce business-impact workspace | implemented | `families/aks_commerce`; `console/src/routes/aks-commerce*`; focused Operator and Console tests, typecheck, build, and desktop/constrained/mobile browser checks | The browser renders one server-authored projection, keeps Sample explicit, and derives no impact, authority, or effect success. Live authenticated source evidence remains separate. |
| Agent Activity durable projection resilience | implemented | `fdai_operator_service/postgres_sql.py`; `fdai_operator_service/activity_projection.py`; `console/src/routes/agents.model.ts`; `console/src/routes/agent-activity-log-model.ts`; `console/src/routes/agent-live-activity.tsx`; focused Operator projection and Console activity tests, typecheck, and local performance measurements | The projection reads exact persisted inventory counts with a bounded mixed-version fallback. The browser preserves all fetched operational rows while hydrating and ordering each bounded source once. Newly appended non-replay rows receive a reduced-motion-safe three-second neutral tint, and all log text uses shared roles at or above the caption floor. The local measurements do not claim production latency. |
| Audit Trace navigation eligibility | validated | `fdai_operator_service/projection_logic.py`; `agent-activity-log-model.ts`; `agent-live-activity.tsx`; `rule-trace*.tsx`; `rule-trace*.css`; focused Operator and Console tests; desktop and mobile Playwright checks; authenticated standard-port Browser inspection | Only audit-backed rows link to Trace. The projection preserves action id, attempt, execution path, and outcome. The Console keeps summary, lookup, ordered stages, selected evidence, action-attempt lifecycle, and exact timeline in one bounded workspace across all data states. Missing evidence remains not recorded, no-publication remains not attempted, and pending or unknown execution never appears complete. Localized labels preserve raw evidence and exact timestamps. |
| Recent Trace discovery and decision-first summary | validated | `fdai_operator_service.projection_logic.rule_fire_trace`; `rule-trace-discovery.tsx`; `rule-trace-workspace.tsx`; `rule-trace-experience.css`; 19 focused Operator tests; 25 focused Console tests; 11 desktop and mobile browser scenarios; isolated real-data browser check; Console typecheck and production build | The selected response owns authoritative metadata. Console reuses the bounded Audit page for recent presentation-only grouping, prioritizes decision and effect, separates terminal from latest activity, exposes copy feedback and evidence presence, compresses non-action traces, and reflows summary, controls, context, paths, and the exact timeline through 320 px. Eighteen post-implementation critique rounds found and corrected seven defects; no Medium-or-higher residual remains. The final real-data browser used repository test authentication because the shared VS Code Browser CDP session was unavailable and its retained Entra state had expired. |
| Inventory provider-execution boundary | implemented | `discovery_receipts.py`; `inventory-execution-display.ts`; focused Azure delivery tests (`18 passed`), parser tests (`11 passed`), and Console typecheck | New server receipts derive placeholder-only commands from registered plans. The Console independently rejects shell controls, redirects, environment assignments, executable shell words, live GUIDs, raw ARM ids, credentials, continuation tokens, and provider errors. |
| Channel-neutral terminal response parity | implemented | [Operator Console Progressive Conversations](../../roadmap/interfaces/operator-console-progressive-conversations.md); `test_rich_contract.py`; focused channel contract tests (`36 passed`) | Teams and Slack preserve one canonical terminal answer, limitations, evidence references, no execution authority, and the final confirmed update through durable replay. Production A3 publishers and governed runtime receipts remain open. |
| Read-only Conversation Search interactions | implemented | `conversation-search.tsx`; `conversation-search.model.ts`; `conversation-search.test.ts`; focused route tests (`5 passed`) and Console typecheck | Bounded filters, safe text highlights, exact context toggling, empty and unavailable states, and fail-closed decoder errors add no approval or execution authority. |
| Incident conversation RCA presentation | implemented | `incident_queries.py`; `semantic_turn_processor.py`; `semantic_turn_presentation.py`; `presentation-artifact.ts`; focused Core, Operator, and Console checks (`138 passed`) | A recorded cause requires a grounded hypothesis with matching citations or an allowlisted deterministic terminal failure grounded on its exact audit row. T0 records bounded impact evidence, and both locales render root cause, impact, and citations without execution authority. Missing evidence stays explicit. |
| Incident attention conversation isolation | implemented | `incident-attention.tsx`; `semantic_turn_processor.py`; `backend-context.ts`; `grounded-reply.tsx`; focused Core tests (`143 passed`) and Console tests (`63 passed`) | The attention control starts one fresh, operator-requested conversation per click instead of submitting during component mount or reusing an earlier incident transcript. A frame-unavailable runtime hold remains planner-unavailable and renders a localized model-connectivity recovery step instead of becoming an evidence or scope clarification. Server verification with zero evidence references no longer presents unrelated screen context as verified sources. |
| Structured incident evidence readability | implemented | `structured-reply.tsx`; `presentation-value.ts`; `structured-reply.css`; focused Console checks (`19 passed`) and typecheck | The Console preserves exact RFC 3339 values in semantic markup while rendering explicit operator-local time, elapsed span, readable actor and machine-token labels, responsive evidence rows, scoped chat connectivity, and 44 px mobile controls. Five browser critique rounds left only Low residual horizontal scrolling for the bounded mobile source list; no governed browser artifact was retained. |
| Natural-language lead for structured answers | implemented | `grounded-reply.tsx`; `structured-reply.css`; focused Console presentation checks, typecheck, production build, and authenticated three-viewport Browser inspection | A superseding table, chart, timeline, or other presentation artifact now follows the canonical verified answer text. The Console does not call a model or derive new facts for the lead, and unverified artifacts remain hidden behind the existing verification gate. |
| Command Deck workspace visual hierarchy | implemented | `console/src/deck/command-deck-presenters.tsx`; `console/src/deck/intro-suggestions.ts`; `console/src/deck/command-deck-workspace-visual.test.ts`; `console/src/styles.css`; focused presenter and starter checks; exact typecheck and production build; authenticated desktop, constrained-desktop, and mobile Browser inspection | A new empty workspace centers one composer with five contract-backed Resource state, Resource Health, and Service Health prompt cards. Selecting a card fills the existing composer and uses the normal semantic path. Browser-authored screen facts do not become answer authority. |
| Observed activity provenance | implemented | `semantic_turn_runtime.py`; `conversation_activity.py`; `conversation_channel.py`; `backend-normalizers.ts`; `investigation-timeline.tsx`; focused Core, Operator, and Console checks; authenticated three-viewport Browser review | Lifecycle-only events lead with their recorded human explanation and state that no external command or provider request ran. Query execution records distinguish inter-service transport from the actual execution interface. Ontology reads identify the Core typed-query executor, operation, and provider-neutral source without inventing a CLI command, public endpoint, or provider implementation. Historical records without additive provenance remain explicit rather than inferred. |
| Exact contextual Resource selection | implemented | `context.tsx`; `backend-context.ts`; `ontology-instances.tsx`; `context_selection.py`; `instance_explorer.py`; focused Console context, Operator envelope, and Core contextual FunctionType checks | A complete screen publishes an opaque server-issued selection token bound to the authenticated principal, ordinary lowercase role scope, purpose, exact release, source generation, completeness, and Resource ids. The token travels in the dedicated `context_capability` redaction allowlist; Operator resolves it before building `conversation_context`, and client-supplied or recomputed ids and tokens missing after restart are rejected. Core requires the capability on the contextual FunctionType itself, rejects disconnected specialized nodes, and intersects explicit utterance filters before the object-only read. Incomplete results remain held or unavailable and never broaden to the principal-visible Resource set. Authenticated Browser receipt remains open. |
| Ontology relationship presentation accounting | implemented | `ontology-instances.model.ts`; `ontology-instance-graph.tsx`; `ontology-instances-inspector.tsx`; `ontology-instance-refresh.ts`; bilingual route catalog; focused Console checks | The instance surface separates response, focus-graph, Inspector-only, and IAM-only counts. Indirect relationships are locally paged without being described as ordered paths or server continuations. Visible selected-instance views revalidate every 15 seconds and on browser resume, while exact provider states use text-bearing semantic badges. |
| Target-bound causal presentation | implemented | `semantic_turn_presentation.py`; `test_semantic_turn_bridge.py`; focused bilingual and causal matrix checks (`31 passed`) | A multi-output causal result renders the exact target, symptom concept, aligned baseline and current windows, measured change, and at least two competing hypotheses. Each hypothesis retains `supported`, `refuted`, or `unresolved`, bounded evidence detail, and limitations. Incomplete, stale, or conflicting evidence remains a limitation and never becomes a causal claim. The projection grants no approval, mutation, or execution authority. Authenticated post-commit validation remains open in Issue #244. |
| Ontology assurance cohort release oracle | implemented | `console/tests/live-e2e/ontology-query-assurance.{ts,spec.ts,test.ts}`; focused assurance tests (`101 passed`) | Full-cohort operation coverage compares the result histogram with the deterministic generated cohort rather than duplicating fixed counts. Missing or substituted operations fail, while extension operations keep their authored bounded distribution. |
| Navigation preference type safety | implemented | `console/src/navigation-preferences.ts`; focused navigation and Cost Governance route tests (`8 passed`); Console typecheck | Preference de-duplication preserves the `PanelGroup` type while Cost Governance navigation remains derived from server availability. The preference store cannot enable the package or grant access. |
| Settings modal workspace | implemented | `console/src/components/settings-overlay.tsx`; `console/src/router.ts`; `console/tests/e2e/settings-overlay.spec.ts`; focused Console unit tests (`35 passed`), typecheck, production build, and three-viewport Browser inspection | Settings opens over the unchanged current route, keeps section navigation transient, traps focus, closes with Escape, and restores the original screen without adding browser history. Direct Settings URLs remain supported. |
| Overview and Command Deck presentation resilience | validated | `console/src/routes/dashboard.executive.tsx`; `console/src/routes/dashboard.loading.ts`; `console/src/deck/structured-reply.css`; `console/src/styles.css`; focused Console tests, typecheck, production build, and authenticated Browser validation | Optional Cost Governance authorization gaps render as unavailable, absent autonomy metrics no longer produce a synthetic summary, timestamps use locale-aware formatting, narrow Explorer layouts reflow through container queries, and planning motion respects reduced-motion settings without implying action authority. |
| Adaptive semantic presentation layouts | implemented | `presentation_artifact_v3.py`; `presentation_artifact_v2.py`; `semantic_turn_presentation.py`; `presentation-artifact.ts`; `structured-reply.tsx`; `structured-reply.css`; focused Operator and Console presentation checks, typecheck, production build, and three-viewport mock inspection | Schema v3 binds server-selected operational brief and Markdown document layouts to the full render-affecting artifact with a SHA-256 assembly digest. Console renders typed layout metadata without keyword routing, preserves v1/v2 replay, and shows only bounded input categories rather than prompt or memory content. |
| Complete conversation document export | implemented | typed `Document` judgment and deterministic frame; `document_export.py`; principal-owned semantic outbox source binding; authenticated document routes; Console preview and download controls; focused Core, Operator, and Console checks | A follow-up document request reuses the latest verified answered turn, rejects partial or mixed unsupported content, and includes every projected row up to the truthful 40-row and 16-column bound. Markdown is always available for a valid artifact; PDF is advertised only when its encoder is bound. Authenticated three-viewport Browser evidence remains open. |
| Receipt-bound semantic answer authority | implemented | Core function, query-execution, and intent-graph receipts; Operator semantic terminal projection; focused authority integration suite passed | Operator derives terminal authority only from completed Core receipts bound to the same evidence references. It preserves subscription health, inventory graph, metering, and ontology manifest sources and holds missing or conflicting authority. |
| Service Health and multi-source condition presentation | implemented | `semantic_turn_processor.py`; `semantic_turn_presentation.py`; `presentation_artifact_v2.py`; bilingual focused checks | Service Health leads with yes, no, partial yes, or unknown. Mixed condition answers expose ordered source verifications without inventing a composite authority. |
| Teams Workflows one-time diagnostic | implemented | `settings-system.tsx`; `settings-teams-workflow.*`; Operator `teams_workflow_diagnostics.py`; focused Console and Operator tests | Settings > Integrations accepts a transient masked public-cloud webhook URL from an Owner, sends one fixed synthetic card, and clears the value. The API returns no URL and persists only digest-based prepared/completed metadata. Production bindings remain deployment-managed. |
| Incident email template preview | implemented | `notification_template_preview.py`; packaged `templates/incident-opened.html`; `settings-email-template.css`; `settings-email-template-preview.spec.ts`; focused Operator checks (`94 passed`, one optional PDF skip); focused Playwright (`3 passed`); Console production build | Settings > Integrations serves the reviewed synthetic Calm Slate specimen from a service-owned package resource. Its sandboxed preview centers at no more than 760 CSS pixels, retains the 640 pixel desktop sheet, and reflows without horizontal overflow at 993 and 390 pixel widths. The email contains no approval or execution control. A cross-client email matrix is not claimed. |
| Teams A1 protected preparation request | implemented | `settings-teams-a1-onboarding.tsx`; runtime Settings model and command; Operator IAM runtime-settings route and PostgreSQL adapter; focused Console and Operator tests | One Owner action persists a revisioned, environment-bound proposal with `execution_authority=false`. The Console reports only the durable request state; provider consent, app installation, plan execution, and apply remain separate. |
| Cost Governance card drill-down and title accessibility | implemented | `cost-governance-workspace.tsx`; `card-drilldown.test.ts`; `title-inventory.test.ts`; `incident-attention.tsx`; `browser-notification-control.tsx`; focused card drill-down, title inventory, and catalog tests; Console typecheck | Overview, Resource Efficiency, Optimization Cases, and Outcomes cards link to narrower sections or cross-surface routes via `cost-text-action` anchors validated by drill-down tests. `CardHeader` and `UnavailablePanel` are registered as visible-title components. Redundant `title` attributes on `aria-label`-bearing topbar controls are removed to prevent double tooltip announcements. No approval, mutation, or execution authority changed. |
| Unavailable document action accessibility | implemented | `document-library-row.tsx`; `document-library-row.test.ts`; `knowledge-navigation.spec.ts`; focused unit and desktop browser checks (`4 passed`), Console typecheck, and production build | Unavailable Preview, Download, and Delete controls remain keyboard focusable and expose their existing explanations while omitting activation handlers. Transient pending actions retain native disabled behavior. |
| Durable queue timing presentation | implemented | `backend-types.ts`; `backend-parsers.ts`; `turn-timing.test.ts`; focused parser checks (`11 passed`) and Console typecheck | The Console accepts existing v1 envelopes and the Core-owned schema v2 `durable_queue` phase while retaining timing bounds, v1 phase closure, and unknown-phase rejection. Timing metadata remains read-only and grants no execution authority. |
| Semantic Incident creation | implemented | Core incident-draft projection; Operator `/chat/action/confirm`, source revalidation, and durable outbox; Console confirmation client; focused cross-service checks | The browser confirms only server-projected fields. HTTP `202` means queued, and the authoritative Incident roster proves completion after Core applies the versioned no-authority request. |
| Authenticated projection and responsive route repair | implemented | `fdai_operator_service/postgres_sql.py`; `fdai_operator_service/conversation_assurance_reader.py`; `console/src/styles.css`; `console/src/routes/reports.tsx`; focused Operator tests (`104 passed`); focused Console layout and Report tests; Console typecheck, production build, and authenticated three-viewport Browser inspection | HIL counts and conversation-assurance details no longer fail on PostgreSQL operator or placeholder parsing. Live health and document identity rows remain bounded at desktop, constrained-desktop, and mobile widths. An untouched required Report variable remains an initial prompt instead of an error alert. |
| Shared full-stack preparation isolation | implemented | `prepare-console-full-stack.sh`; `prepare-local-runtime-env.sh`; focused environment and cache-input tests | An explicitly enabled owner-only Kubernetes fleet binding changes only the Core inventory environment. Attachment, ingestion, conversation, and executor service environments remain unchanged. |
| Incident attention stream poll coalescence | implemented | `incident_attention_poller.py`; route composition; 20-subscriber concurrency regression; complete Operator composition suite | One Operator replica performs at most one authoritative incident snapshot read per two-second interval while preserving connection-specific authentication and replay cursors. Live post-restart profile evidence remains open. |
### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-20 | implemented | Coalesced concurrent authenticated incident-attention SSE polls inside each Operator replica without changing the wire contract, durable source, authorization, or replay cursor semantics. | `current change`; focused poller tests and complete Operator composition suite passed 101 cases with one optional PDF skip. | Reprofile a stable restarted Operator process and retain the DB call and event-loop comparison. |
| 2026-09-18 | implemented | Made production Guides use the Console's same-origin `/manuals` path by default and packaged the allowlisted Manual Studio files in deterministic prebuilt Console artifacts. The installer binds share metadata separately from the rollback-compatible runtime schema, and the publisher verifies bundled manuals without granting browser authority. | `current change`; focused Console runtime and Help Center tests; focused deployment packaging and publisher tests; Console typecheck, production build, bundle check, Ruff, strict mypy, and shell syntax. | Publish the exact merged Console artifact and retain authenticated Guides drawer, catalog, cover, and manual-open evidence. |
| 2026-09-17 | implemented | Defaulted production Manual Studio discovery to the same-origin `/manuals` artifact so a prebuilt AKS Console does not depend on a compile-time URL that installation-time configuration cannot replace. | `current change`; Help Center URL regression tests passed 10 cases; live Static Web Apps readback confirmed the catalog and library already exist under `/manuals`. | Publish the corrected Console from an exact protected `main` revision, then retain authenticated browser evidence. |
| 2026-09-17 | implemented | Corrected the bilingual sign-in footer so it describes the Console as governed rather than read-only while retaining policy and human-approval boundaries. | [Issue #1318](https://github.com/dotnetpower/fdai/issues/1318); `current change`; bilingual catalogs and owner design; catalog parity, punctuation, and readable-Hangul checks passed. | Retain protected CI and merge evidence; no browser, Operator, approval, or execution authority changed. |
| 2026-09-17 | validated | Validated the corrected decoder on all four standard-port Cost Governance Live routes through the explicit authenticated Azure CLI debug profile without claiming Browser Entra evidence. | [Issue #1291](https://github.com/dotnetpower/fdai/issues/1291); `12/12` local services ready; four Operator responses returned `200`; all routes stayed in Live mode with no panel, page, or API errors, no raw provider identity, and no document or main-region overflow at 1440 x 900, 993 x 641, or 390 x 844. | Protected CI and merge remain authoritative for delivery. |
| 2026-09-17 | implemented | Kept degraded Cost Governance Live projections readable by accepting and localizing the complete service-contract readiness reason vocabulary while preserving visible failure for unknown values. | `current change`; [Issue #1291](https://github.com/dotnetpower/fdai/issues/1291); Cost decoder and catalog tests (`13 passed`), Console typecheck and production build, and localization checks. | Retain authenticated standard-port evidence for all four Cost Governance routes and protected CI. |
| 2026-09-17 | implemented | Kept built-in workflow review available when optional definition and Python-task capabilities are unavailable, and aligned the production route with the approved published-definition workspace. | `current change`; task-owned Workflow builder source, bilingual catalogs, route-owned CSS, and focused tests; 33 unit and localization checks, typecheck, production build, and desktop, constrained, and mobile Playwright scenarios passed. | Retain protected delivery evidence separately; no execution, promotion, or managed-resource authority changed. |
| 2026-09-17 | implemented | Aligned Rules with the approved catalog workbench, preserved URL-backed selection and focus restoration, and restored the known-rule no-provider findings response. | `current change`; task-owned Rules, bilingual catalog, Operator adapter, and focused test paths; 4 browser scenarios, 15 Console unit and localization checks, 43 Operator workflow checks, production build, catalog parity, punctuation, and readable-Hangul checks passed. | Retain protected delivery evidence separately; no synthetic override, shadow-accuracy, assessment, or execution claim was added. |
| 2026-09-17 | implemented | Added Kubernetes fleet binding path and byte identity to shared Console preparation without propagating the selector or binding into attachment, ingestion, conversation, or executor environments. | `current change`; focused local environment and cache tests; shell syntax checks. | Live external-cluster collection remains an explicitly selected AKS evidence-plane validation, not a Console capability claim. |
| 2026-09-17 | implemented | Kept the document-version UI projection bounded to action metadata while splitting preview rendering from history orchestration. | `current change`; issue #1277; focused Console unit, type, build, keyboard, mobile, and version-action browser evidence. | Authenticated deployed document-storage evidence remains separate. |
| 2026-09-17 | implemented | Removed the oversized sticky Trace correlation toolbar so it scrolls with the route and cannot cover timeline or selected-evidence content. | `current change`; focused Trace scroll geometry regression at desktop and mobile widths; Console typecheck and production build. | Retain exact-head CI and protected delivery evidence; authenticated browser observation remains separate. |
| 2026-09-17 | implemented | Separated Waterfall disclosure, correlation identity, and Trace navigation so only a labeled action leaves the route while the identifier stays selectable text and Back restores the prior Waterfall context. | `current change`; focused Waterfall unit, localization, keyboard, desktop, constrained, and mobile browser checks. | Retain exact-head CI and protected delivery evidence; isolated browser fixtures do not claim authenticated runtime observation. |
| 2026-09-17 | implemented | Added one-row-per-file version history and selected-version actions, paired with server-owned unchanged-upload reuse and immutable changed-content successors. | `current change`; issue #1277; 71 backend checks, 18 Console checks, strict type checks, production build, and 4 desktop/mobile browser scenarios. | Authenticated deployed document-storage evidence remains separate. |
| 2026-09-17 | implemented | Added an authenticated AKS commerce projection route and localized Console workspace for service state, SLOs, dependency path, aggregate signals, evidence gaps, governed action state, and independent effect state. | `current change`; focused Operator and Console tests, typecheck, build, and two Playwright scenarios across 1440, 993, and 390 pixel widths. | Retain authenticated deployed projection evidence after an approved scenario deployment. |
| 2026-09-17 | implemented | Added a three-second neutral fade for newly appended non-replay Agent Activity rows, raised every log label, control, row, badge, identifier, and footer from ad hoc 8.75-11.5 pixel sizes to shared 11-15 pixel typography roles, and restored keyboard focus after explicit refresh. | `current change`; focused activity-log model and style regressions; isolated production-component browser measurements at desktop, constrained, mobile, keyboard refresh, and reduced motion. | Retain exact-head CI and protected delivery evidence; isolated browser fixtures do not claim authenticated runtime observation. |
| 2026-09-17 | in-progress | Corrected unfiltered compaction evidence in the scoped Operator Memory review and exposed candidate scope in the table. | `current change`; five new regression cases reproduced missing query bindings before the fix. | Complete focused checks and isolated UI evidence, then protected delivery in campaign order; authenticated live readback remains separate. |
| 2026-09-17 | implemented | Verified the scoped read and candidate presentation without writing operational records. | `current change`; 26 Operator and 2 model tests; actual PostgreSQL queries over five connection-local synthetic TEMP-table scenarios followed by rollback; English keyboard filtering, Korean four-width no-overflow checks, reviewed narrow screenshot; both Console type targets and Operator mypy passed. | Protected delivery remains ordered under #1237; authenticated live readback is not implied by isolated evidence. |
| 2026-09-17 | implemented | Removed Agent Activity's repeated retained-snapshot scans and growing-prefix sorts while preserving the 500-row activity contract, source bounds, ordering, deduplication, and filters. | `current change`; #1232; 500-row Operator projection reduced from 15,382.4 ms to 47.6 ms with zero count mismatches; a 500-row Console fixture rendered in 828 ms; 103 focused Console checks and strict typecheck passed. | Retain exact-head CI and deployed migration evidence; these local measurements do not claim production latency. |
| 2026-09-17 | implemented | Bound the Approval card's Incident link to the server-projected canonical-Incident availability flag while retaining Audit, Trace, and RCA for non-Incident operational correlations. | `current change`; HIL queue decoder and presentation tests; authenticated Live browser verification | Retain protected CI and merge evidence. |
| 2026-09-17 | implemented | Aligned Reports with the approved evidence workbench, split route state from presentation, added honest source and zero-widget states, and retained manual refresh for reports without variables. | `current change`; [Issue #1227](https://github.com/dotnetpower/fdai/issues/1227); task-owned Reports source, catalogs, and browser fixtures; focused units, three-viewport browser checks, final integrated-browser target and overflow measurements, production build, Operator report tests, localization gates, and independent-service check passed. | Retain exact-head protected CI and one authenticated post-merge report render before raising this bounded row to `validated`. |
| 2026-09-16 | implemented | Bound deployed Console redirect and runtime configuration to the standalone deployment coordinator after AKS convergence, without granting browser or Operator deployment authority. | `current change`; focused standalone application, Entra redirect, and Console publisher tests. | Retain fresh-subscription authenticated browser and post-login API evidence. |
| 2026-09-16 | implemented | Repaired every pending-approval Sample drill-down by adding one correlated Incident, Audit, and Trace evidence set, a clean no-recorded-RCA state, and non-link presentation for synthetic references without a detail projection. Runtime browser alerts retain an explicit Live evidence selection. | `current change`; `operations.sample-evidence.ts`; `hil-queue.tsx`; `browser-notifications.ts`; focused Console tests, typecheck, production build, and `approvals-sample.spec.ts` at 1440x900, 993x641, and 390x844 | Retain protected CI and merge evidence. No Live approval or execution authority changed. |
| 2026-09-16 | implemented | Aligned Console publication and its shared deployment regression with the profile-provided AKS gateway while preserving the existing Container Apps fallback. | `current change`; publisher and service workflow assertions; 8 focused tests passed. | Retain protected CI and deployed authenticated stream evidence before classifying the source change as operationally validated. |
| 2026-09-16 | implemented | Regenerated the question bank and dependent semantic-intent coverage after Incident title catalog additions changed their source digests. | `current change`; official generators; focused question-bank and semantic-intent artifact-equality tests passed. | No remaining work for this deterministic source-digest refresh. |
| 2026-09-16 | implemented | Restored the provisioning replay source declaration, isolated Sample and Live state, and aligned the production route with the approved evidence hierarchy without adding mock-only data. | `current change`; `services/operator-service/src/fdai_operator_service/runtime.py`; `console/src/routes/provision*.tsx`; 47 focused Console tests, 97 Operator composition tests with one optional PDF skip, four Playwright scenarios, production build, and independent-service boundary check passed. | Retain exact-head protected CI and merge evidence under #1149; isolated browser timings remain synthetic rather than field percentiles. |
| 2026-09-16 | implemented | Replaced the churning Sample Live presentation with one stable synthetic VM-start journey and two supporting decision cases. Added readable approval, provider, independent-observation, reconciliation, and recovery status while keeping completed Incident and pending Approval correlations distinct. | `current change`; Console Live and Sample routes; 30 focused tests, typecheck, production build, catalog and punctuation checks, independent review, and 1440x900, 993x641, and 390x844 browser measurements with zero horizontal overflow. | Retain a separately governed operational receipt before presenting any live Azure effect or measured business outcome. |
| 2026-09-15 | implemented | Bound the legacy and staged runtime-environment cache identities to the explicit resolved-model override path and bytes so changing the artifact regenerates model bindings. | [Issue #1090](https://github.com/dotnetpower/fdai/issues/1090); `current change`; `prepare-console-full-stack.sh`; focused preparation and startup tests passed 78 cases; shell syntax, diff, and routed owner checks passed, including 2538 tests with 18 optional skips against loopback PostgreSQL. | No runtime authority changed; retain governed narrator runtime receipts through the narrator-routing owner. |
| 2026-09-15 | implemented | Corrected static catalog discovery to resolve the actual imported `t` binding when a route also imports formatting helpers from another catalog. | Current change; #1084; `catalog-usage.test.ts` passed 4 checks and Console typecheck passed; CI run 34969490657 identified the prior false missing-key result. | Protected publication and merge remain tracked in #1084. |
| 2026-09-15 | implemented | Delivered Process workspace presentation with explicit three-run Sample evidence, collapsed detail sections, event deep links and direct mock-geometry regression. | Current change; #1084; 43 unit tests, Console typecheck, 12 desktop, 3 constrained, 3 mobile and 1 320px scenario passed. | Protected publication and merge are tracked in #1084; no deployment or live effect claimed. |
| 2026-09-15 | implemented | Preserved independent Live, audit-parent, and expanded-conversation bounds, made every Live coverage state drillable, decoded the authority-free ARG total envelope, and exposed optional rule findings as measured counts or explicit not-evaluated state. | `current change`; 96 focused Console and 83 focused backend checks; Console typecheck passed. | Retain equivalent deployed-revision Live evidence; no document, approval, mutation, or execution authority changed. |
| 2026-09-14 | validated | Added allowlisted private local preparation selectors for an existing Azure read scope without widening browser, provider, or execution authority. | `current change`; `prepare-console-full-stack.sh`; focused setup and startup tests passed 46 cases; standard local supervisor reported `10/10` ready. | No implementation work remains for this bounded local preparation boundary. |
| 2026-09-14 | implemented | Replaced the incident-email placeholder markup with the reviewed packaged Calm Slate specimen and aligned the sandboxed Console preview to the verified email canvas. | `current change`; template parity and safety checks passed 2 cases; Operator composition passed 94 cases with one optional PDF skip; responsive Console Playwright passed 3 cases; Console production build passed. | Run the separately governed Outlook, Gmail, and Apple Mail matrix before claiming cross-client pixel parity. |
| 2026-09-14 | validated | Completed 18 post-implementation Trace critique rounds. The campaign corrected selected-summary contradictions, untrusted source tokens, unexplained empty filters, raw source labels, mobile timeline width, real-data non-action overflow, collapsed narrow toolbar actions, and fragmented read-only boundary copy. | `current change`; 19 focused Operator tests and 25 focused Console tests passed; 11 desktop and mobile browser scenarios passed; Console typecheck, production build, entry-bundle, catalog parity, readable-Hangul, translation, and roadmap-ledger checks passed; an isolated authenticated browser rendered the real local 500-record Audit sample as 25 bounded correlations and a complete two-record read Trace at 1904, 993, 390, and 320 px in 1.14 seconds without API errors or horizontal overflow. | Low residual only: the filtered Trace exposes entry and previous-hash references but does not independently verify the complete audit ledger chain. The final shared VS Code Browser Entra pass remains unavailable because CDP timed out and the retained state expired; the isolated real-data pass used repository test authentication and grants no authority. |
| 2026-09-14 | implemented | Stacked the read-only boundary explanation below its title at 320 px after final visual review exposed a narrow vertical sentence fragment. | `current change`; 320 px boundary-coordinate regression and repeated real-data screenshot review. | Repeat the final residual-severity review. |
| 2026-09-14 | implemented | Restored the base one-column toolbar cascade at 320 px so copy and refresh controls no longer collapse beside the two-column related-evidence grid. | `current change`; 320 px read-only Trace control-width regression and repeated real-data browser evidence. | Complete the final residual-severity review. |
| 2026-09-14 | implemented | Wrapped long canonical action kinds and compact stage paths at 320 px after a real audit-backed read Trace exposed 17 px of internal workbench overflow that the action fixture did not reproduce. | `current change`; focused read-only Trace browser regression and isolated real-data browser validation. | Repeat the final real-data pass and residual-severity review. |
| 2026-09-14 | implemented | Reflowed the exact audit timeline into labeled vertical rows on mobile while retaining semantic table markup. | `current change`; 390 px and 320 px Trace browser assertions. | Complete authenticated-data and residual-severity validation. |
| 2026-09-14 | implemented | Replaced the raw selected-Trace source token with a localized display label while retaining the canonical token in the wire response and view context. | `current change`; focused Trace presentation tests and Console typecheck. | Continue the bounded post-implementation hardening rounds. |
| 2026-09-14 | implemented | Added an explicit no-match state for recent Trace type filters instead of rendering an unexplained empty list. | `current change`; focused Trace discovery browser test. | Continue the bounded post-implementation hardening rounds. |
| 2026-09-14 | implemented | Restricted selected Trace source authority metadata to the canonical `operator-audit-log` token so an unknown source cannot appear as verified provenance. | `current change`; focused Trace decoder tests and Console typecheck. | Continue the bounded post-implementation hardening rounds. |
| 2026-09-14 | implemented | Required selected Trace kind, action-attempt count, effect-observation count, and RCA evidence metadata to match the ordered steps before rendering decision summaries. | `current change`; focused Trace decoder tests and Console typecheck. | Continue the bounded post-implementation hardening rounds. |
| 2026-09-14 | implemented | Added bounded recent Trace discovery from the existing Audit page, decision-first selected summary, explicit completeness and terminal-versus-latest state, localized actor and stage-less action labels, copy success/failure feedback, related-evidence presence, compact non-action lifecycle, counted timeline disclosure, and responsive context disclosure. | `current change`; 25 focused Console unit tests and 11 focused desktop/mobile Playwright scenarios passed; Console typecheck and catalog parity passed. | Run production build, authenticated standard-port Browser verification, and at least 10 residual critique rounds. |
| 2026-09-14 | in-progress | Added deterministic selected-Trace metadata for source authority, completeness, time bounds, latest activity, target scope, action attempts, effect observations, and recorded Incident or RCA evidence. | `current change`; focused Operator Trace projection tests. | Implement bounded Audit-page discovery and the Console decision-first and responsive presentation. |
| 2026-09-14 | not-started | Revised Trace discovery to reuse the existing bounded authoritative Audit page after aggregate-route review showed that a new top-level route widened unrelated design surfaces. Browser grouping remains presentation-only and exposes `next_cursor` incompleteness. | `current change`; bilingual owner design revision. | Implement selected-trace server metadata and the Console discovery and presentation layers without a new read-model route. |
| 2026-09-14 | not-started | Approved the bounded recent Trace discovery and decision-first summary design after revising it to expose sample incompleteness and evidence-presence flags without claiming destination availability. | `current change`; bilingual owner design. | Implement the typed query, Operator route and projection, Console workspace, responsive states, and focused verification matrix. |
| 2026-09-14 | validated | Completed 24 focused Trace critique and hardening rounds in 15 task commits. The campaign hardened correlation identity, positive ordering, approval classification, bounded completeness, producer contradictions, optional-field integrity, joined-row provenance, audit hashes, root entry shape, actor attribution, and owning test integration. One impossible-state defense was identified and corrected append-only. The final review found no Medium-or-higher residual Trace defect. | `current change`; 120 Operator tests passed; 20 focused Trace unit tests passed; 10 desktop and mobile Playwright scenarios passed; Console typecheck, production build, entry-bundle, catalog parity, readable-Hangul, translation, roadmap-ledger, and independent-service checks passed. | Low residual only: the filtered Trace displays entry and previous-hash references but does not independently verify the complete audit ledger chain. A global catalog-usage check still reports two unrelated pre-existing browser-notification keys. |
| 2026-09-14 | implemented | Restored each Trace step's recorded actor to the wire model, view context, stage rail, and evidence detail while retaining the canonical action kind separately. | `current change`; focused Operator projection, Console decoder, context, and browser tests. | Complete the final residual review. |
| 2026-09-14 | implemented | Required every PostgreSQL Trace audit `entry` to remain a JSON object so an array or scalar cannot be normalized into an empty evidence record. | `current change`; focused Operator Trace reader tests. | Complete the final residual review. |
| 2026-09-14 | implemented | Validated every optional Trace producer field when present so malformed text, workflow, or attempt values cannot be erased into not recorded. | `current change`; parameterized Operator Trace projection tests. | Complete the final residual review. |
| 2026-09-14 | implemented | Corrected the nullable audit-reference hardening after verifying that the audit ledger schema requires event id, entry hash, and previous hash. The browser again requires all three instead of defending an impossible source state. | `current change`; base audit migration inspection plus focused Operator projection and Console decoder tests. | Complete malformed optional-field and final residual review. |
| 2026-09-14 | implemented | Kept absent event and previous-hash references nullable and normalized `None` or `null` string sentinels to not recorded instead of displaying them as identifiers. | `current change`; focused Operator projection and Console decoder tests. | Complete the final residual review. |
| 2026-09-14 | implemented | Preserved and displayed each Trace step's previous entry hash alongside its entry hash without claiming that the filtered trace verifies the complete ledger chain. | `current change`; focused Operator projection, Console decoder, context, and browser tests. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Preserved each joined Trace step's source event id and source correlation id so event-correlated executor rows do not appear to have been recorded directly under the requested correlation. | `current change`; focused Operator projection, Console decoder, context, and browser tests. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Rejected whitespace-only stage, decision, reason, and terminal-stage values so empty strings cannot become recorded summaries or unlabeled statuses. | `current change`; focused Console Trace decoder tests and Console typecheck. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Rejected non-positive action attempts and conflicting top-level versus workflow action attempt identities instead of hiding one value through fallback precedence. | `current change`; focused Operator Trace projection tests. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Rejected audit entries whose `pipeline_stage` and `stage` fields conflict, and surfaced the malformed authoritative projection as unavailable. | `current change`; focused Operator Trace projection tests. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Added a dedicated Trace query that includes executor rows through the correlated event id and probes a 501st row. Both server and browser reject over-limit traces instead of presenting a newest-500 truncation as complete. | `current change`; focused Operator Trace tests, focused Console Trace tests, and Console typecheck. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Limited approval lifecycle evidence to explicit approval request, decision, approved, rejected, timeout, and resolution records. Unrelated HIL delivery or notification traffic no longer implies approval, and explicit rejection renders as failed. | `current change`; focused Trace lifecycle tests and Console typecheck. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Rejected zero audit sequences before Trace can order them or build an Audit entry link. | `current change`; focused Trace route tests and Console typecheck. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | implemented | Rejected a Trace response whose body identifies a different correlation from the requested route, preventing a misrouted projection from being presented as the selected evidence. | `current change`; focused Trace route tests and Console typecheck. | Continue the bounded Trace hardening campaign. |
| 2026-09-14 | validated | Aligned the populated and non-populated Trace route with its reviewed evidence-workspace hierarchy. Added localized stage and action labels with raw-value retention, ordinal stage markers for large audit sequences, bounded long-trace scrolling, synchronized keyboard selection, responsive single-column metrics, and complete loading, empty, unavailable, and error states. | `current change`; 17 focused route tests passed; 10 focused Playwright scenarios passed across desktop and mobile; Console typecheck, production build, entry-bundle, catalog parity, readable-Hangul, punctuation, and authenticated standard-port Browser checks passed. The authenticated local correlation rendered two ordered records in a 1,320 px workspace with a 276 px stage rail and no horizontal overflow; the scoped lookup completed in 219 ms. | Retain a governed Browser Entra artifact from a current-revision action whose independent observation reaches terminal closure. |
| 2026-09-13 | implemented | Completed 18 focused critique and hardening rounds for Vertical Outcomes. Corrected stale and absent data rendering, zero-value semantics, supported drill-down filters, table accessibility, 320-pixel reflow, Korean copy, and Sample contract consistency; the final review found no Medium-or-higher residual defect. | `current change`; focused Vitest checks passed 78 cases; focused Playwright checks passed 13 cases; Console typecheck, production build, entry-bundle check, catalog parity, translation, implementation-ledger, and commit hooks passed. | Retain authenticated Browser Entra and human assistive-technology evidence before claiming those validation levels; bounded mobile table scrolling remains intentional. |
| 2026-09-13 | implemented | Prevented a zero-event Cost Governance row from presenting zero monthly savings as a measured result while preserving an observed zero for a non-empty sample. | `current change`; `console/src/routes/vertical-outcomes.tsx`; `console/src/routes/analytics-hubs.test.ts`; focused analytics tests passed 18 cases; Console typecheck passed. | No remaining implementation work for this bounded evidence-state correction. |
| 2026-09-13 | implemented | Distinguished an explicitly classified optional projection outage from a generic HTTP `503`, so a service failure remains visible instead of appearing as an unwired feature. | `current change`; `console/src/api-transport.ts`; `console/src/api.ts`; 13 focused Vitest files passed 130 tests; Console typecheck passed. | No remaining implementation work for this bounded error-classification correction. |
| 2026-09-11 | implemented | Prevented the initial Report screen from presenting an untouched required variable as an error while retaining validation after partial input. | `current change`; [Issue #785](https://github.com/dotnetpower/fdai/issues/785); focused Console Report and layout tests passed 19 cases; Console typecheck passed; authenticated Browser inspection covered the initial Report state. | Recheck every route against the final pushed revision and attach the resulting evidence to Issue #785. |
| 2026-09-11 | implemented | Corrected two authenticated PostgreSQL projection parsing defects and two responsive layout overrides that hid available data or clipped narrow-screen content. | `current change`; [Issue #785](https://github.com/dotnetpower/fdai/issues/785); focused Operator tests passed 104 cases; focused Console layout tests passed 7 cases; Console typecheck and production build passed; authenticated Browser inspection covered 49 primary routes and 7 Settings routes at desktop, constrained-desktop, and mobile widths. | Recheck every route against the final pushed revision and attach the resulting evidence to Issue #785. |
| 2026-09-11 | implemented | Completed 14 adversarial CLI hardening rounds across authentication, redirect handling, runtime decoding, response and state bounds, cancellation, terminal control safety, IME and CJK geometry, duplicate frames, launcher cleanup, projection truth, and approval-queue SQL scope. The final SQL path uses an escaped `hil_park:` namespace and literal search semantics, and local screen commands remain outside server conversation history. | `current change`; all 93 CLI tests, 8 launcher tests, and 5 focused HIL/KPI tests passed; TypeScript typecheck, Ruff, strict mypy, diff check, design routes, roadmap tracking, punctuation, and translation checks passed; direct local PostgreSQL count, page, and literal-search queries completed. | No confirmed Medium-or-higher residual remains. Low maintainability work may split the API decoder helpers from the current client module without changing its contract. A separate governed model conversation receipt remains required before claiming narrator runtime validation. |
| 2026-09-11 | implemented | Migrated the CLI launcher to the independent Operator Service, added loopback-only Azure CLI session bootstrap, propagated the opaque bearer across all read transports, preserved partial and count-only projection truth, fixed the HIL decision-key SQL precedence defect, removed client-inferred risk, and adopted a calm coding-agent terminal hierarchy with complete Korean labels and accessibility fallbacks. | `current change`; CLI typecheck and all 77 tests passed; focused HIL/KPI tests passed 4 cases; launcher pytest passed 5 cases with Ruff and strict mypy; service-boundary, design-route, catalog, translation, Hangul, punctuation, direct PostgreSQL, and complete live protected-read checks passed. | No implementation work remains for this bounded CLI slice. A separate governed model conversation receipt is still required before claiming narrator runtime validation. |
| 2026-09-10 | implemented | Replaced Settings page navigation from the Activity Bar with a modal workspace that preserves the current route and mounted screen. Settings section changes use an in-memory route, while close, Escape, and direct-entry fallback restore a stable underlying view. | `current change`; `console/src/app.tsx`; `console/src/router.ts`; `console/src/components/settings-overlay.tsx`; `console/tests/e2e/settings-overlay.spec.ts`; focused unit tests passed 35 cases; Console typecheck and production build passed; desktop, constrained-desktop, and mobile Browser geometry had zero document or dialog overflow. | No remaining implementation work for this bounded Settings navigation change. |
| 2026-09-09 | implemented | Connected the Teams A1 Settings guide to one Owner-only protected-plan request while keeping provider credentials and deployment authority outside the browser. | `current change`; focused Operator and Console tests, Ruff, strict mypy, typecheck, and production build. | Bind the durable proposal to the protected provisioning workflow, then retain provider and deployment receipts. |
| 2026-09-08 | implemented | Extended the bounded Console timing contract so existing v1 envelopes remain readable and Core's schema v2 durable queue phase does not invalidate the complete turn timing envelope. | `current change`; `backend-types.ts`; `backend-parsers.ts`; `turn-timing.test.ts`; focused parser checks passed 11 cases; Console typecheck passed. | No remaining implementation work for this bounded compatibility correction. |
| 2026-09-06 | implemented | Removed Dashboard facts and records from incident-attention submissions while retaining the exact incident binding and minimal locale, route, and principal metadata. The verified answer now comes from `query.incident_evidence` rather than presenting current-screen data as an incident source. | `current change`; `command-deck.tsx`; `use-command-deck-events.ts`; focused Console checks passed 38 cases; Console typecheck; authenticated Browser Entra request inspection and verified incident answer. | No remaining code work for this bounded context-isolation change. |
| 2026-09-06 | implemented | Distinguished semantic planner unavailability from source unavailability in the Command Deck and rendered a localized recovery step for model connectivity or runtime restoration. | `current change`; `verification-presentation.ts`; `grounded-reply.tsx`; bilingual Console catalogs; focused Console checks passed 63 cases; Console typecheck and authenticated Browser Entra reproduction. | Restore private Azure OpenAI reachability, then retain one authenticated incident answer receipt. |
| 2026-09-06 | implemented | Stopped labeling Dashboard snapshot context as evidence for a server-verified zero-evidence hold. Live reproduction proved that the incident binding reached Core, no read was performed, and Azure OpenAI planning calls returned HTTP 403 before an incident plan could be produced. | `current change`; `console/src/deck/backend-context.ts`; focused Console checks passed 55 cases; Console typecheck; live Browser Entra response and local Core log. | Restore a locally reachable Azure OpenAI data plane, then retain an authenticated incident answer receipt before raising this bounded slice to validated. |
| 2026-09-06 | implemented | Preserved frame-unavailable runtime results as planner outages instead of recasting them as authoritative evidence holds. | `current change`; `services/core-control-plane/src/fdai_core_service/semantic_turn_processor.py`; focused Core checks passed 143 cases; Ruff and format checks passed. | Restore a locally reachable Azure OpenAI data plane, then retain an authenticated incident answer receipt before raising this bounded slice to validated. |
| 2026-09-06 | implemented | Stopped labeling Dashboard snapshot context as evidence for a server-verified zero-evidence hold. Live reproduction proved that the incident binding reached Core, no read was performed, and Azure OpenAI planning calls returned HTTP 403 before an incident plan could be produced. | `current change`; `console/src/deck/backend-context.ts`; focused Console checks passed 57 cases; Console typecheck; live Browser Entra response and local Core log. | Restore a locally reachable Azure OpenAI data plane, then retain an authenticated incident answer receipt before raising this bounded slice to validated. |
| 2026-09-06 | implemented | Removed unsolicited incident investigation submission during attention-control mount, isolated every explicit click in a fresh bound conversation, and preserved receipt-bound semantic planner outage guidance. | `current change`; `console/src/components/incident-attention.tsx`; `console/src/deck/grounded-reply.tsx`; focused incident, Deck event, and grounded-reply tests passed 30 cases. | Retain an authenticated Browser Entra receipt that exercises one attention click and one planner-unavailable response before raising this bounded slice to validated. |
| 2026-09-05 | implemented | Kept unavailable document actions keyboard focusable so their explanations remain discoverable without allowing activation. Pending actions continue to use native disabled behavior. | `current change`; `document-library-row.test.ts` passed 2 cases, the desktop `knowledge-navigation.spec.ts` passed 2 cases, and Console typecheck and production build passed. | No remaining work for this bounded accessibility change. |
| 2026-09-05 | implemented | Allowed the durable SSE replay header through cross-origin preflight and retained immediate authoritative graph revalidation. | `current change`; focused Operator CORS checks passed, browser replay requests carrying `Last-Event-ID` returned HTTP 200, and the connected stream automatically refreshed the AKS root and its new VM/NIC branch. | Repeat one corrected provider transition to retain the final timing receipt. |
| 2026-09-05 | in-progress | Added a durable inventory invalidation SSE and a Console consumer that immediately revalidates the selected ontology instance. When SSE is unavailable, the status surface shows a monotonic countdown to the next 15-second poll. | `current change`; Operator SSE checks passed 140 cases and Console SSE/countdown checks passed 112 cases. | Synchronize the shared main checkout, run the clean integrated typecheck/build, and retain one real state-transition browser receipt before raising this slice to implemented. |
| 2026-09-05 | implemented | Added a clearly visible semantic badge for provider state and automatic selected-instance revalidation without changing inventory authority. Hidden tabs do not poll, overlapping triggers coalesce, browser resume refreshes immediately, and a failure leaves the last verified graph visible with an explicit warning. | `current change`; focused Console checks passed 104 cases; typecheck, production build, and three-viewport browser geometry passed with zero document overflow. Critique prevented compound negative provider states from matching a success badge. | Verify one real provider state transition after the inventory authority records the new evidence. |
| 2026-09-05 | implemented | Added truthful relationship presentation accounting to the ontology instance workspace. The graph description now reports rendered and returned counts separately, indirect relationships page locally, and an independent graph-to-response consistency status rejects stale or duplicate graph keys. | `current change`; focused Console checks passed 89 cases and the exact typecheck passed before unrelated concurrent Console edits introduced their own errors. | Retain a task-owned clean build and authenticated three-viewport browser receipt. |
| 2026-09-04 | implemented | Added bilingual Document OCR provider controls with revision fencing, plan retry, effective-state readback, and separate local-switch and resource-removal intents. | `current change`; Console model and command tests, full Console unit suite, typecheck, and production build. | Retain an authenticated browser artifact after protected provider deployment. |
| 2026-09-05 | implemented | Made document upload guidance capability-driven, added localized format labels and preflight rejection, and rendered bounded warnings for server-verified extraction outcomes. | `current change`; focused Console tests passed 8 cases; Console typecheck passed; the corresponding Python document and deployment checks passed 206 cases. | Retain authenticated Browser evidence for supported, rejected, warning, and OCR-unavailable upload states before raising this row to validated. |
| 2026-09-03 | implemented | Replaced unassessed empty-Deck examples with five bilingual questions backed by implemented semantic functions and generated question-bank contracts. | `current change`; focused Console intro, Core semantic planning, and question-bank checks. | Retain authenticated bilingual browser receipts and recheck the accepted desktop geometry. |
| 2026-09-02 | implemented | Replaced row-count Service Health fallback and investigation-centric holds with direct operator conclusions. Added schema-v2 source verification only for the explicit multi-source resource-condition shape while preserving single-source wire compatibility. | `current change`; Core, Operator, service-contract, and cross-service regression checks. | Retain one governed bilingual Browser Entra response for each new answer shape. |
| 2026-09-01 | implemented | Added card drill-down links across all four Cost Governance surfaces so each visual card navigates to a narrower section or cross-surface route. Registered `CardHeader` and `UnavailablePanel` as visible-title components in the title inventory. Removed redundant `title` attributes from topbar controls that already carry `aria-label` to prevent double tooltip announcements. | `current change`; `cost-governance-workspace.tsx`; `card-drilldown.test.ts`; `title-inventory.test.ts`; `incident-attention.tsx`; `browser-notification-control.tsx`; focused card drill-down, title inventory, and catalog tests; Console typecheck. | No remaining work for this drill-down and accessibility change; governed browser artifacts remain open in the broader Console validation backlog. |
| 2026-09-01 | implemented | Removed terminal `ontology-query` authority flattening and made receipt authority plus evidence-reference binding the only verified semantic answer source. Missing and conflicting receipt authorities now remain held and unverified. | `current change`; Core, Operator, service-contract, and cross-service focused tests passed. | Retain a governed live receipt through a separately authorized assurance campaign before upgrading this row to validated. |
| 2026-09-01 | implemented | Added digest-bound schema-v3 adaptive response layouts. Typed health, error-correlation, and incident evidence renders as an operational brief, typed ontology documents render as Markdown documents, and all other verified shapes retain their existing deterministic visual selection. | `current change`; focused Operator presentation and semantic-bridge checks passed 196 cases; focused Console presentation checks passed 31 cases with typecheck; production build and final browser geometry are recorded by this change. | Retain a governed Browser Entra receipt from a live schema-v3 semantic response before upgrading this row to validated. |
| 2026-09-01 | implemented | Added read-only WARA Operator routes, durable catalog projection, strict Console decoding, complete filter state, bilingual labels, and desktop/constrained/mobile evidence review. | `current change`; Operator workflow and materializer tests passed; WARA Console model tests, typecheck, and build passed; browser geometry recorded no horizontal overflow at 1440x900, 993x641, and 390x844. | A governed live-Azure assessment projection remains separately authorized and is not claimed by the local visual fixture. |
| 2026-08-31 | implemented | Separated framework catalog and mapping state from scope-bound evaluation, applicability, and satisfaction in the Controls view. Kept all WARA/APRL reference controls on the read-only ontology surface with no browser or Operator API query execution. | `current change`; control model, list/detail presentation, bilingual labels, focused Console tests, typecheck, and production build. | Add a dedicated WARA facet only if operator demand justifies a surface beyond ontology search. |
| 2026-08-31 | implemented | Added the checksum-pinned TypeScript projection of every compatibility-manifest N/N-1 schema to Console's strict typecheck without moving runtime validation or authority into the browser. | `current change`; generated TypeScript artifact, generation drift check, and Console typecheck. | Regenerate through the pinned command whenever a manifest-owned schema changes. |
| 2026-08-29 | in-progress | Hardened optional Overview data handling and responsive presentation while keeping Command Deck planning feedback non-authoritative and motion-aware. | `current change`; `console/src/routes/dashboard.executive.tsx`; `console/src/routes/dashboard.loading.ts`; `console/src/deck/structured-reply.css`; `console/src/styles.css`; focused test files. | Record the focused Console test and typecheck results before raising the implementation-scope state. |
| 2026-08-28 | implemented | Restored the static mock's Visualization group and replaced a left-edge selection accent with the complete quiet outline required by the Console visual boundary. | `current change`; focused mock visual-boundary tests passed 5 cases and Console typecheck passed. | No governed runtime or production Console evidence is claimed from the static mock. |
| 2026-08-28 | implemented | Kept unavailable Cost Governance placeholders out of the pending retrieval trace, declared the separately owned Overview measurement route unavailable, and preserved raw PostgreSQL stream fallback behind the semantic bridge. | `current change`; focused retrieval-trace, Operator composition, semantic bridge, and service-suite ownership tests; Console typecheck. | Retain authenticated live projection evidence separately; these local checks do not prove runtime availability. |
| 2026-08-28 | implemented | Preserved typed navigation groups through preference de-duplication after the Cost Governance navigation integration was rebased onto the current explorer shell. | `current change`; `console/src/navigation-preferences.ts`; focused navigation and Cost Governance route tests passed 8 cases, and Console typecheck passed. | No remaining work for this type-only integration repair; operational Cost Governance validation remains in its owning delivery ledger. |
| 2026-08-26 | implemented | Replaced the empty Command Deck's pill quick starts with a situational card hierarchy: a 2 x 2 prompt grid plus one featured current-screen Checklist card. The change is presentation-only and preserves the existing prompt selection, composer input, submit, conversation history, and post-first-turn layout behavior. | `current change`; `console/src/deck/command-deck-presenters.tsx`, `console/src/deck/command-deck-workspace-visual.test.ts`, and `console/src/styles.css`; focused presenter checks passed 11 cases and empty-state visual checks passed 2 cases; the exact `HEAD` plus staged snapshot passed Console typecheck and production build. Authenticated desktop, constrained-desktop, and 390 px mobile Browser checks measured zero document and card horizontal overflow and retained 44 px mobile targets. | No bounded implementation work remains for this presentation slice. Browser observations are local presentation evidence, not a governed runtime receipt. |
| 2026-08-25 | implemented | Completed the final presentation pass for the empty Command Deck. Grouped layout and close actions as right-edge window controls with a 12 px viewport inset, replaced the text close mark with a consistent icon and local danger hover, removed the repeated screen badge, widened and centered the composer, used an icon send action, centered quick starts and suggestions, and localized context-aware starter questions in English and Korean. | `current change`; focused Command Deck checks passed 46 tests; Console source and test typechecks, catalog parity, readable Hangul, punctuation, production build, and entry bundle gate passed; authenticated English desktop and Korean desktop/mobile Browser checks measured a 12 px close inset, 44 px mobile actions, localized prompts, no control overlap, and zero horizontal overflow. | No bounded implementation work remains. The Browser observations are local presentation evidence, not a governed runtime receipt. |
| 2026-08-25 | implemented | Simplified the refined Command Deck after direct review. Removed the visible current-screen information control, count, state, and right-side panel while preserving the same internal view snapshot for answer grounding. Moved new conversation and history into compact header icon actions, removed the empty-state toolbar, and made the transcript consume the recovered height. | `current change`; focused Command Deck checks passed 39 tests; Console source and test typechecks passed; production build and entry bundle gate passed; authenticated desktop and mobile Browser checks measured no current-screen control or panel, 44 px mobile header actions, correct centered and bottom composer states, and zero horizontal overflow. | No bounded implementation work remains. The Browser observations are local presentation evidence, not a governed runtime receipt. |
| 2026-08-25 | implemented | Reworked the new-conversation workspace around one centered composer, screen context, operating-domain quick starts, and bounded suggestions. Active or restored conversations retain the bottom composer. The header now leads with the conversation title and route, hides search until turns exist, reduces healthy connectivity to a tooltip-backed status dot, and gives new conversation, history, and screen information distinct icon-and-label controls. Starting a new conversation closes auxiliary panels. | `current change`; focused Command Deck checks passed 39 tests; Console source and test typechecks passed; production build and entry bundle gate passed; authenticated 1440 x 900, 993 x 641, and 390 x 844 Browser checks measured unchanged routes, 44 px mobile controls, correct centered-to-bottom composer transitions, no panel overlap, and zero horizontal overflow. | No bounded implementation work remains. The Browser observations are local presentation evidence, not a governed runtime receipt. |
| 2026-08-22 | implemented | Completed a second opened-shell parity pass after direct authenticated comparison exposed remaining specimen shortcuts. The adaptive shell now matches production search count and previous/next navigation, `Ctrl+K`, search-specific and deck-level `Escape`, workspace `Tab` focus wrapping, source-readiness loading/error/ready states, history count and new action, current/other/agent groups, favorite and remove actions, keyboard and pointer resizing from 180 to 360 px, stale digest refresh, the production chat glyph and Bragi avatar asset, and production display copy. Mobile hides layout selectors, keeps search and close available, hides resize, and holds every visible shell input and button to at least 44 px. | `current change`; focused visual contracts passed 52 cases; production and direct static-specimen Playwright passed 4 cases; full Console typecheck passed; Vite production build completed in 11.68 seconds and the entry bundle gate passed. Authenticated `/architecture` comparison measured 240 px history, 280 px digest, an 8 px resize handle, stale freshness, a 70 px composer, and zero overflow. The master mock measured the corresponding 240/280 px panels, a 69 px composer, and zero desktop, constrained, mobile, outer, iframe, and root overflow; direct Playwright also exercised all source and keyboard states. | Static rows and source states remain synthetic and grant no runtime authority. The authenticated Architecture route's 418-resource map delayed the lazy Deck launcher beyond the first 10-second probe, but it mounted without an import error and the opened shell was then measured; no governed runtime artifact is claimed. |
| 2026-08-22 | implemented | Corrected the preceding workspace-specimen claim after re-review found that its desktop history opened by default, its visible shell controls were mostly inert, its digest and source-readiness states were absent, and its shell geometry remained mock-local. Production and the adaptive specimen now consume nine shared shell roles; both start transcript-first with history and digest closed, use a 240 px bounded history and 280 px digest, preserve the constrained overlay, and expose functional search, filters, conversation selection, new conversation, source readiness, digest, layout, close, jump, attachment, and send controls with synchronized accessibility state. | `current change`; the focused visual matrix passed 52 cases; production and direct static-specimen Playwright passed 4 cases; full Console typecheck passed; Vite production build completed in 11.01 seconds and the entry bundle gate passed. Browser checks exercised 1440 x 900, the 745 x 589 content frame produced by the 993 x 641 master viewport, and 390 x 844 in order, retained screenshots, measured zero root and document horizontal overflow, and verified 44 px visible mobile toolbar and dismiss controls. | The repository-root mock navigation and production Activity rail remain separate host surfaces. Browser evidence here is synthetic presentation evidence; it does not replace governed authenticated runtime evidence. The earlier `charts-composed.tsx` typecheck blocker is no longer present in the current working tree. |
| 2026-08-22 | implemented | Wrapped the same adaptive answer DOM in a production-shaped workspace specimen instead of treating the design-mock index as the Console shell. The optional mode adds a 50 px Deck header, search and layout controls, a 220 px desktop conversation history, a 36 px workspace toolbar, a scroll-owned transcript, a 69 px composer, a 1100 px constrained-desktop conversation overlay, and an 82% mobile conversation drawer with close and scrim behavior. | `current change`; `mocks/ui/deck-sources-v2.html` and focused visual contracts passed 52 cases; three production Playwright checks passed; Console Vite build completed in 11.16 seconds and the entry bundle gate passed. Browser checks measured desktop workspace 1129 x 694 with header 50 px, conversations 220 px, toolbar 36 px, composer 69 px, and overflow 0; the 993 px outer viewport used a 240 px overlay over the full transcript; mobile drawer close restored the transcript with overflow 0. | The repository-root design-mock navigation remains a host tool outside the specimen, just as the production Activity rail remains outside the Deck overlay. Full Console typecheck remains blocked by three unrelated `exactOptionalPropertyTypes` errors in `console/src/components/charts-composed.tsx`. |
| 2026-08-22 | implemented | Added an explicit unverified and expanded adaptive-response state so production comparisons no longer contrast a real unsupported answer with a synthetic verified success. The specimen now mirrors the bounded answer, unsupported-claim actions, 2/2 evidence summary, unobserved Plan and Collaboration phases, unverified Verification phase, expanded six-row process record, and 44 px mobile answer controls while preserving the existing compact and verified flows. | `current change`; `mocks/ui/deck-sources-v2.html`, `ui/calm-slate-primitives.css`, and focused visual contracts passed 51 cases; three production Playwright checks passed; the exact Console Vite build completed in 12.02 seconds and the entry bundle gate passed. Browser checks covered the same-state mock at 1440 x 900 and 390 x 844 with zero horizontal overflow. | The design-mock index and production Activity rail, conversation history, search, locale, and operator-selected disclosure state remain intentionally different shells or session state. Full Console typecheck is currently blocked by three unrelated `exactOptionalPropertyTypes` errors in `console/src/components/charts-composed.tsx`; this change does not modify that file. |
| 2026-08-22 | implemented | Extended Command Deck parity from shared panel geometry to 45 shared visual roles covering operator and agent turns, Bragi identity, source metadata, answer typography, completed-work summaries, answer controls, Run record hierarchy, preparation sources, and the composer. Production and the adaptive mock now share a 780 px reading measure, a 38 px desktop work summary, a 54 px desktop Run record, bounded three-card source exposure, and compact two-row mobile Run records with one-row 44 px composer controls. | `current change`; `ui/calm-slate-primitives.css`, production Deck presenters, `mocks/ui/deck-sources-v2.html`, and focused parity tests passed 50 cases; three focused production Playwright tests passed, including 1440 x 900, 993 x 641, and 390 x 844 plus delayed preparation geometry; Console typecheck, production build, and entry bundle passed. Static mock browser checks measured matching desktop roles with zero overflow and mobile Run record 75.6 px, work summary 66 px, composer 67 px, and zero overflow. | The authenticated desktop Console was reviewed before its access check expired during the mobile resize. Synthetic production Playwright and static mock evidence cover all three viewports, but retain a fresh authenticated mobile observation before claiming governed runtime validation. |
| 2026-08-22 | implemented | Restored the documented answer-first terminal hierarchy by removing the forced-open state from completed observed work. Stable grounding and Run record geometry now comes from shared Calm Slate primitives consumed by both the production Console and adaptive-response mock, while exact execution evidence remains available after operator expansion. | `current change`; `ui/calm-slate-primitives.css`, `console/src/deck/investigation-timeline.tsx`, `console/src/deck/retrieval-trace.tsx`, `console/src/deck/conversation-trajectory-view.tsx`, `mocks/ui/deck-sources-v2.html`, and focused visual contracts passed 48 cases; Console typecheck and production build passed; focused production Playwright passed at 1440 x 900 and across 1440 x 900, 993 x 641, and 390 x 844 with zero document and transcript horizontal overflow; the 1440 x 900 static mock measured zero outer and iframe overflow. | The prior authenticated Browser Entra session required a new interactive sign-in after reload, so this change claims synthetic production and static-mock presentation evidence only. Retain a fresh authenticated terminal-answer observation before claiming governed runtime validation. |
| 2026-08-21 | implemented | Replaced the empty terminal-style lifecycle event with its recorded explanation and neutral facts, and added strict optional execution provenance across Operator activities, channel-neutral durable replay, Console decoding, browser cache, and Run record replay. Ontology ObjectSet reads now identify the internal Core interface, executor, operation, provider-neutral source, transport, and non-applicable endpoint before human-readable query scope, exact JSON, result counts, completeness, and raw output. Historical records state that provenance was not recorded. | `current change`; focused channel and Operator activity checks passed 128 cases; focused Console provenance and workspace checks passed 84 cases; Ruff, strict mypy, Console typecheck, production build, entry bundle, catalog parity, readable Hangul, punctuation, and whitespace passed; authenticated 1440 x 900, 993 x 641, and 390 x 844 checks found zero document, transcript, or panel overflow and no composer overlap. | Browser observations are presentation evidence, not a governed runtime artifact. New turns receive provenance; immutable historical turns remain explicitly unavailable. |
| 2026-08-21 | implemented | Matched the production `Preparing answer` surface to the running adaptive-response v2 mock after direct comparison exposed oversized shared typography, blank stage icons, and over-compressed source rows. The live component now uses the mock's compact badge, glyph hierarchy, 7 px panel, 36 px header, 30 px stages, 88 px source slot, and 11/10.5/9.5/8.5 px text roles. The source window stays fixed while previews stream and no card-level scan animation competes with observed progress. | `current change`; focused visual contracts passed 31 cases; Console typecheck, production build, entry bundle, catalog parity, readable Hangul, punctuation, and editor diagnostics passed. The standard authenticated Console measured a 38.45 px source row against the mock's 38.57 px row with zero panel or document overflow; screenshots were reviewed. | Browser comparison is presentation evidence, not a governed runtime artifact. Keep semantic stages, source counts, and authority unchanged while retaining broader conversation evidence separately. |
| 2026-08-13 | implemented | Adopted the implementation ledger without reconstructing earlier provenance and recorded the current-state activity identity contract. | Current source plus `test_read_investigation_latency.py` and `test_activity_projection.py`; the focused suites passed. | Record governed cross-service parity evidence for snapshot-first hydration and live convergence. |
| 2026-08-13 | implemented | Standardized optional projection absence across production Console routes without hiding unexpected failures. | Current change; focused route tests passed (`64 passed`) and Console typecheck passed. | Keep Browser Entra route assurance in the full-stack validation campaign. |
| 2026-08-14 | implemented | Restored the durable Agent Activity projection by escaping the psycopg wildcard, preserving valid rows with an out-of-range optional duration, and keeping the audit timeline available during typed optional-source absence. | `current change`; focused Operator tests passed (`25 passed`), focused Console tests passed (`8 passed`), and Console typecheck and catalog parity passed. | Retain a governed Browser Entra artifact for the existing snapshot-first and live-convergence exit criterion. |
| 2026-08-14 | implemented | Restricted Trace links to durable audit rows and presented an absent audit correlation as unavailable rather than failed. | `current change`; `agent-live-activity.test.ts` and `rule-trace.test.ts` passed 26 focused tests, Console typecheck passed, and the authenticated Browser Entra page showed a non-linked inventory correlation plus a neutral Trace unavailable state. | Retain the observed Browser Entra result as a governed artifact before claiming runtime validation. |
| 2026-08-14 | implemented | Added the server-owned provider-execution receipt producer and hardened the Console parser against executable or sensitive provider text. | `current change`; focused Azure delivery tests `8 passed`, Console parser tests `6 passed`, and Console typecheck passed. | Retain the read-only live canary receipts tracked by the Azure discovery owner document. |
| 2026-08-14 | implemented | Rejected environment assignments in both server-rendered and browser-accepted provider commands. | `current change`; focused Azure delivery tests `10 passed`, Console parser tests `7 passed`, and Console typecheck passed. | Retain the read-only live canary receipts tracked by the Azure discovery owner document. |
| 2026-08-14 | implemented | Preserved registered placeholders while rejecting redirects, control characters, and executable shell words in both server and browser provider-command validators. | `current change`; focused Azure delivery tests `18 passed`, Console parser tests `11 passed`, and Console typecheck passed. | Retain the read-only live canary receipts tracked by the Azure discovery owner document. |
| 2026-08-14 | implemented | Restored localized unavailable copy for the capability directory and removed persistent top and left edge accents from legacy Console design mocks. | `current change`; `mock-visual-boundary.test.ts` and `catalog-usage.test.ts` passed 6 focused tests, and catalog parity passed. | Keep the visual-boundary and catalog-usage checks in the operator-surface CI lane. |
| 2026-08-14 | implemented | Verified the shared terminal response reducer explicitly for Teams and Slack without adding vendor-specific authority. | `current change`; `test_rich_contract.py` passed 36 focused cases, with detailed ownership retained in [Operator Console Progressive Conversations](../../roadmap/interfaces/operator-console-progressive-conversations.md). | Implement and exercise production A3 publishers before claiming channel runtime validation. |
| 2026-08-14 | implemented | Added a bounded deterministic spring-settle to the exact-release Catalog topology without changing stored coordinates or leaving a persistent simulation. | `current change`; focused topology tests report 12 passed, Console typecheck passed, and the authenticated standard-port browser rendered a nonblank canvas without overflow. | Retain the separate authenticated Context snapshot evidence tracked by the wire-contract owner. |
| 2026-08-14 | implemented | Added focused Conversation Search route interactions while preserving the Console as a read-only projection surface. | `current change`; route model and focused tests `5 passed`; Console typecheck. | Retain governed cross-surface search evidence under the Conversation Search owner. |
| 2026-08-17 | implemented | Preserved recorded grounded RCA, bounded T0 impact evidence, and matching citations across an incident-bound semantic answer and the strict Console artifact decoder. The previous hardcoded "causal analysis hasn't been implemented" limitation is retained only for legacy replay and no longer describes current results. | `current change`; focused Core, Operator, and Console checks passed 138 cases; Ruff, strict mypy, and Console typecheck passed. | Retain a governed Browser artifact for a newly recorded RCA with all three evidence sections. |
| 2026-08-17 | implemented | Projected the active incident's recorded `route_unresolved` notification failure as a deterministic T0 cause, route-outcome impact row, and exact audit citation. The read path changes no audit or notification state and rejects successful or unknown outcomes as causes. | `current change`; focused Core and Operator checks passed 133 cases; Ruff and strict mypy passed. | Restart Core and verify the three evidence blocks in the authenticated Browser. |
| 2026-08-17 | implemented | Hardened structured incident evidence for operator reading without changing the verified artifact or machine records: explicit local time and timezone, preserved raw timestamps, elapsed span, readable actors and tokens, responsive native tables, scoped readiness copy, non-overlapping latest-message navigation, and 32/44 px controls. | `current change`; `presentation-value.test.ts` and `command-deck-workspace-visual.test.ts` passed 19 focused cases; Console typecheck passed; five authenticated browser critique rounds ended with 12 of 12 final desktop/mobile checks passing and only Low residual source-list scrolling. | Retain the broader governed incident presentation artifact already tracked below; no Medium-or-higher implementation finding remains in this readability slice. |
| 2026-08-17 | implemented | Refined the live ontology assurance oracle so each fixed question identity declares its valid typed capability family instead of inheriting one broad operation-level capability. The oracle still rejects answered plans that omit the relationship, causal, temporal, filter, or evidence capability the exact question requires. | `current change`; `ontology-query-assurance.test.ts` passed 99 cases and Console typecheck passed. | Retain a passing strict artifact before running another full seeded cohort. |
| 2026-08-17 | implemented | Corrected the prompt-specific oracle so evidence-validation questions continue to require Core's server-owned secured ObjectSet plan. A topology or metric capability can supply evidence to another answer shape, but it doesn't replace the evidence-validation contract. | `current change`; `ontology-query-assurance.test.ts` passed 99 cases and Console typecheck passed. | Retain a passing strict artifact before running another full seeded cohort. |
| 2026-08-20 | implemented | Unified the live ontology assurance Browser extractor and checkpoint validator on one typed capability registry. The first strict-v2 rerun exposed that the Browser-local allowlist discarded newly added declaration functions even though the verified intent graph carried them. The shared registry includes every strict-v2 function and `metric_scope_series`, so a future capability addition cannot update only one assurance boundary. | [Issue #233](https://github.com/dotnetpower/fdai/issues/233); `current change`; live run `question-space-final-9fb7cb213-20260820-r6`; 100 focused assurance tests and Console typecheck passed. | Retain a passing strict-v2 artifact before starting the seeded 100-case cohort. |
| 2026-08-20 | implemented | Bound seeded operation coverage to the authoritative generated cohort after extension families made the prior uniform ten-per-operation gate arithmetically impossible. The exact histogram check rejects a missing result or a same-size operation substitution without copying the generator's distribution into the release oracle. | [Issue #233](https://github.com/dotnetpower/fdai/issues/233); `current change`; run `question-space-final-b4604e07b-20260820-r11` completed seeded judgments 100/100 with all hard-zero counters at zero; focused assurance tests passed 101 cases. | Retain a passing exact-source seeded artifact after central validation. |
| 2026-08-18 | implemented | Aligned the production Command Deck with the reviewed mock's calm transcript hierarchy without hiding verification, evidence, or observed work. One reading measure now owns answers and structured tables, opaque values wrap in place, long desktop tables retain their headers, and the composer keeps a narrower command weight. | `current change`; `command-deck-workspace-visual.test.ts`, `investigation-timeline.test.ts`, and `conversation-trajectory-visual.test.ts` passed 29 focused cases; Console typecheck passed; ten isolated authenticated questions covered prose, lists, unavailable or clarification states, one-row tables, and 20-row English and Korean tables with zero horizontal overflow at desktop and 390 px widths. | Browser observations were not retained as a governed artifact. Keep the existing bounded mobile source-strip scrolling as a Low residual and retain broader governed conversation evidence separately. |
| 2026-08-18 | implemented | Made `Preparing answer` show the observed content behind each stage instead of compressing labels and values into one row. Screen context includes the route, headline, and visible fact count; routing keeps its recorded reason; backend progress keeps check and source counts; and source previews show kind, title, and bounded detail in an open three-card window. | `current change`; `retrieval-trace.tsx`, `styles.css`, localized Console catalogs, and the focused `investigation-timeline.test.ts`; authenticated desktop and 390 px checks showed open details, stage-specific text, at most three source cards in the DOM, and zero horizontal overflow. | Browser observations were not retained as a governed artifact. Later server stage events can enrich the same typed progress seam without browser inference. |
| 2026-08-20 | implemented | Hardened the production Command Deck and its static mock through five independent critique rounds. Mobile conversation history now overlays a full-width transcript with a reachable dismiss action; durable history exposes loading, empty, unavailable, error, and retry states; compact header and composer controls preserve the reading area; and the mock uses the production conversation and current-screen context model. | `current change`; [Issue #237](https://github.com/dotnetpower/fdai/issues/237); focused Console checks passed 53 cases; Console typecheck and production build passed; authenticated production and synthetic mock checks covered 1440 x 900, 993 x 641, and 390 x 844. The five rounds exercised responsive geometry, history state semantics, accessibility, mock parity, and regression truthfulness with at least ten checks per round. | Only Low bounded horizontal scrolling remains for the mobile evidence-source strip. Browser observations were not retained as a governed artifact. |
| 2026-08-20 | implemented | Repeated Command Deck hardening against a restored authenticated conversation and active rendering transitions for 20 independent critique rounds with at least ten checks per round. Conversation history now overlays constrained workspace, dock, and floating layouts without collapsing the transcript; pending and settled activity remain visible without reserving blank space; completed activity is collapsed by default; final investigation answers retain identity and time; long structured tables stay dense until the narrow mobile breakpoint; opaque identifiers remain exactly inspectable; and reduced-motion transitions are true jump cuts so workspace restoration cannot retain floating geometry. | `current change`; [Issue #237](https://github.com/dotnetpower/fdai/issues/237); focused Console checks passed 47 cases; Console typecheck, production build, and bundle check passed; authenticated production and static mock checks covered 1440 x 900, 993 x 641, and 390 x 844 plus workspace, dock, and floating transitions. | Only Low bounded horizontal scrolling remains for the mobile evidence-source strip. Browser observations were not retained as a governed artifact. |
| 2026-08-20 | implemented | Corrected the initial default-screen hydration boundary after an authenticated first-open regression exposed `Conversation history unavailable` as the entire transcript. Default screen history now restores in the background and falls back to the normal transcript intro when its optional source is unavailable, while an explicitly selected durable thread or agent conversation retains visible loading, unavailable, error, and retry states. | `current change`; [Issue #237](https://github.com/dotnetpower/fdai/issues/237); focused hydration, conversation-index, and workspace visual checks passed 46 cases; Console typecheck passed; the authenticated 993 x 641 screen reproduced the unavailable source and then rendered the intro with the history panel closed and zero horizontal overflow. | Browser observations were not retained as a governed artifact. Explicit saved-conversation failures remain fail-closed and visible. |
| 2026-08-20 | implemented | Removed the misleading always-visible `Evidence sources` strip when the authoritative manifest provides no verified availability signal. Missing or non-authoritative fixed slots remain `unknown` internally, but an all-unknown projection with no actionable state no longer occupies the transcript header. Any verified available or unavailable source still renders the linked strip, and a zero-height grid slot preserves transcript and composer geometry when the strip is absent. | `current change`; [Issue #237](https://github.com/dotnetpower/fdai/issues/237); source projection, API gating, manifest decoding, and workspace visual checks passed 32 cases; Console typecheck, production build, and bundle check passed; the authenticated 993 x 641 screen confirmed the strip absent, a 0 px slot, a 435 px transcript, a 70 px composer, and zero horizontal overflow. | Browser observations were not retained as a governed artifact. The local source manifest remains authoritative and no healthy state is inferred from missing entries. |
| 2026-08-20 | implemented | Restored the desktop Command Deck hierarchy after settled execution details and two-line preparation stages displaced the verified answer. Completed observed work now remounts as one collapsed disclosure, and the desktop preparation header and stage detail prefer one bounded line with ellipsis. | `current change`; `investigation-timeline.tsx`, `styles.css`, `investigation-timeline.test.ts`; focused Console checks passed 30 cases; a fresh authenticated desktop mount showed a 38 px closed activity row followed by the verified table with zero body and transcript horizontal overflow. | Browser observations were not retained as a governed artifact. Keep responsive validation downstream of the accepted desktop baseline. |
| 2026-08-20 | implemented | Restored target-bound causal multi-output rendering before the generic v2 single-output compiler and exposed the evidence dimensions the Core result already carried. The structured answer now preserves exact target identity, symptom, aligned time windows, observed change, three hypothesis dispositions, evidence grade, sample count, falsifiers, and limitations in both locales. | `current change`; [Issue #244](https://github.com/dotnetpower/fdai/issues/244); focused bilingual projection checks passed 4 cases and the complete Python and Console regression matrix passed 64 cases. | Restart the exact committed Core and retain one authenticated slowdown answer plus desktop viewport evidence before changing this row to `validated`. |
| 2026-08-20 | implemented | Completed twelve independent answer-state parity rounds against the Command Deck and adaptive-response mocks. Connected investigations now keep one agent header; primary prose and copy omit only matching terminal machine suffixes; source and context holds use attention styling; unverified references never display a support check; tables use a flat report hierarchy; time series keep one ordered axis; signed comparisons retain direction; and the mobile overlay grid keeps Send inside the viewport. | `current change`; [Issue #246](https://github.com/dotnetpower/fdai/issues/246); focused answer UI matrix passed 164 cases; Console typecheck, production build, entry bundle, and synthetic Playwright at 1440 x 900, 993 x 641, and 390 x 844 passed. | No Medium-or-higher presentation finding remains in this bounded slice. Governed authenticated answer evidence and the existing Low bounded mobile source-strip scrolling remain separately tracked. |
| 2026-08-20 | implemented | Corrected the preceding parity conclusion after an authenticated screenshot showed that terminal answers still retained the transient starting card and expandable lifecycle rows, displacing the answer. A completed flow now replaces that transient surface with one static observed-work summary, keeps exact query and output evidence only in the collapsed Run record, and presents semantic evidence holds as `Source unavailable` rather than `Unsupported claim`. Mobile terminal summaries and reply actions use compact semantic grids. | `current change`; reopened [Issue #246](https://github.com/dotnetpower/fdai/issues/246); focused lifecycle and visual checks; mock-aligned execution Playwright; authenticated 1440 x 900, 993 x 641, and 390 x 844 geometry with zero horizontal overflow. | No Medium-or-higher terminal-answer hierarchy finding remains. Exact execution evidence remains inspectable in the Run record, and no authority changed. |
| 2026-08-25 | implemented | Aligned the production Incident workspace with its accepted design mock while keeping every summary, response stage, and accountable-Agent contribution grounded in the existing Incident and audit projections. Outcome evidence remains available through an accessible collapsed disclosure, and the Console gains no execution authority. | `current change`; [Issue #272](https://github.com/dotnetpower/fdai/issues/272); focused Incident Vitest `55 passed`; desktop/mobile Playwright `6 passed`; production build and entry-bundle gate passed; authenticated browser checks at 1440 x 900, 993 x 641, and 390 x 844 measured zero document and primary-region horizontal overflow. | No remaining implementation work for the bounded Incident mock-alignment slice. |
| 2026-08-28 | implemented | Closed two gaps in the exact contextual Resource selection row above. `backend-context.ts` spread the entire `ViewSnapshot`, including `contextIdentity`'s principal, scope digest, and release digest capability metadata, into the narrator-facing `view_context` instead of only the opaque `selectionToken` that `context_capability` already allowlists; it now builds `view_context` through a `narratorViewFields` helper that strips `contextIdentity` first. Separately, `ontology-instances.tsx`'s exact-screen selection identity counted hidden `authorization.role-assignment` resources toward `resourceIds`, even though the same screen already excludes them from the rendered graph and legend; the identity builder moved to `ontology-instances.model.ts` as `ontologyInstanceContextIdentity` and now filters through the existing presentation-resource guard first. | `current change`; `console/src/deck/backend-context.test.ts` (`22 passed`) and `console/src/routes/ontology-instances.model.test.ts` (`30 passed`); Console typecheck passed. | Retain governed authenticated evidence for the corrected selection identity; no capability, authority, or wire-contract shape changed. |
| 2026-08-29 | validated | Hardened optional Overview data handling, narrow-width presentation, structured incident evidence, and visible no-authority planning progress. | `current change`; focused Console and Operator tests passed, Console typecheck and production build passed, and the authenticated standard-port Browser loaded Overview and retained zero body, main, and Deck overflow while returning grounded PostgreSQL state rows. | Keep the validated layouts and optional-source behavior covered while extending other query families. |
| 2026-08-27 | implemented | Added an Owner-only one-time Teams Workflows test under Settings > Integrations without turning the Console into a secret store or production binding editor. | `current change`; focused Operator composition, route, and transport checks passed 77 cases with one optional PDF skip; focused Console settings checks passed 14 cases; scoped Console typecheck, Vite build, and entry-bundle check passed; desktop, constrained desktop, and mobile Playwright checks passed without overflow. | Retain a governed live provider receipt before claiming operational validation. |
| 2026-09-04 | implemented | Restored the canonical verified answer as a natural-language lead above every structured artifact that contains content beyond its overview. The existing server text remains the only summary authority, while the component retains exact rows, points, evidence references, and limitations below it. | `current change`; focused Console presentation tests passed 50 cases; Console typecheck and production build passed; authenticated 1065 x 917, 993 x 641, and 390 x 844 inspection confirmed summary-before-component ordering and zero document overflow. | Retain a governed Browser artifact before raising this row to validated. |


| 2026-09-16 | implemented | Restored the missing semantic Incident confirmation route and completed its source-bound Operator-to-Core transport. Transient Console failures remain retryable with the original key, and retries cannot duplicate or merge later Incident episodes. | [Issue #1125](https://github.com/dotnetpower/fdai/issues/1125); `current change`; focused Python and Console tests, typecheck, production build, Ruff, strict mypy, and independent-service checks. | Retain authenticated post-merge evidence from Confirm through the observed `incident.open` roster record. |

### Remaining work
- [x] Align Reports with the approved evidence workbench, keep required-variable and zero-widget states distinct, preserve server-owned provenance, and pass the focused checks tracked by [Issue #1227](https://github.com/dotnetpower/fdai/issues/1227).
- [x] Match the bounded Process workspace to the master mock and retain read-only Sample, guarded transitions, status/theme contrast, and 320-1440px evidence; focused checks for #1084 passed.
- [x] Implement and validate the bounded Trace discovery and decision-summary contract, including authenticated populated, incomplete-index, no-action, mobile, and copy-feedback evidence.
- [ ] Recheck all 49 primary routes and 7 Settings routes against the final pushed revision, verify that available authoritative data renders without unexpected failures, and attach the evidence to [Issue #785](https://github.com/dotnetpower/fdai/issues/785).
- [x] Keep the CLI loopback session in memory, preserve unavailable projections, and provide the
	read-only coding-agent cockpit plus text fallback; all 77 CLI tests, launcher checks, strict type
	checks, and the protected local read pass.
- [x] Keep Settings section navigation in a modal workspace over the unchanged current route and restore the original screen on close; focused unit tests, Console typecheck, production build, and three-viewport Browser checks pass with no horizontal overflow.
- [ ] Record a governed cross-service receipt showing that snapshot-first `GET /agents/activity` hydration and a newer live frame converge on the same current-state activity id without duplicate Console rows.
- [ ] Retain a governed Browser Entra artifact showing that a non-audit operational correlation has no Trace link and that a manual absent-audit lookup renders a neutral unavailable state.
- [ ] Retain a governed Browser Entra artifact showing root cause, impact evidence, and grounded citations from a newly recorded RCA incident; historical incidents without impact rows must remain explicitly incomplete.
- [ ] Retain a governed Browser Entra artifact showing that one incident-attention click creates one fresh bound conversation, sends one investigation request, and preserves actionable planner-outage guidance without mixing an earlier transcript.
- [x] Keep structured incident evidence readable at desktop and mobile widths while preserving exact machine values; focused tests, typecheck, and five browser critique rounds pass with no Medium-or-higher residual finding.
- [x] Keep Command Deck answers, detailed preparation stages, structured evidence, and the composer on one bounded reading hierarchy; focused tests, typecheck, and desktop/mobile browser canaries pass with no horizontal overflow.
- [ ] Retain a governed Browser Entra receipt proving one live schema-v3 operational brief and one Markdown document preserve the server-selected layout, assembly digest, evidence references, and no-execution authority after durable replay.
- [ ] Retain a governed Browser Entra artifact proving the canonical natural-language lead remains above a structured table and chart after durable replay.
