# Console Evidence and Resilience implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope
| Area | State | Evidence | Notes |
|------|-------|----------|-----------|
| Governed ontology assurance provenance | in-progress | `console/tests/live-e2e/ontology-query-assurance*.ts`; `console/tests/live-e2e/assurance-budget.ts`; `console/tests/live-e2e/assurance-checkpoint.ts`; focused Vitest: 143 passed | The strengthened release gate requires complete verified answers for every operation in both locales and rejects any ambient or incident-bound chat request. The run is bounded by a derived budget with per-question and no-progress deadlines and resumes from an evidence-identity-bound checkpoint, and an exhausted budget never reports a ready artifact. |
| Exact-release ontology catalog and declaration workbench | implemented | `ontology_console_projection.py`; `ontology_{declaration,dependents,evidence_health,release_diff}_projection.py`; `materialize-authoritative-catalogs.py`; Console workbench routes; focused Python and Console checks | One producer supplies summary, declaration detail, dependents, sanitized evidence health, release comparison, and Catalog topology with exact release identity and no mutation authority. Receipt-bound Context evidence remains open. |
| Kubernetes lifecycle evidence presentation | implemented | `analyzer_lifecycle_projection.py`; `detection-readiness.tsx`; focused Operator, Console, typecheck, build, and three-viewport Playwright checks | The authenticated detection-readiness response separates server-authored current state from retained failure, replacement, publication, and recovery history. Incomplete, conflicting, missed, and duplicate evidence remain explicit; the Console derives no lifecycle edge and exposes no cause claim or execution authority. |
| Operational ontology instance explorer | implemented | `instance_explorer.py`; `postgres_family_store.py`; `ontology-instances*.{tsx,ts,css}`; focused Operator and Console checks; strict mypy, Ruff, task-scoped editor diagnostics, production Vite build, and authenticated three-viewport inspection | The `Instances` view binds an active-generation Resource directory and one exact selected Resource to a bounded bidirectional graph, allowlisted properties, newest-first exact-resource FDAI audit facts, independent source availability, release identity, generation, cutoff, and truncation. Role assignments remain available to IAM evidence but are omitted from the default instance directory, graph, Inspector, and screen context. A Resource Group or Subscription root shows at most 36 type-diverse direct relationships; another selected Resource keeps only its immediate owning Resource Group and omits indirect peer scope ancestors. An exact AKS node-Resource-Group mapping orders the managed Resource Group, contained VMSS, VM instances, and NICs without inventing links. The graph uses wheel zoom and native full screen, and its Inspector collapses without losing the selected detail view. |
| Instance relationship evidence posture | implemented | `instance_explorer.py`; strict `ontology-instances.model.ts`; localized Inspector; focused Operator and Console checks | Every direct relationship reports current, stale, or unavailable evidence separately from completeness. Provider configuration observation is distinct from independently verified observation evidence, and future or expired cutoffs retain provenance without being presented as complete. |
| VM network path summary | implemented | `ontology-instances.model.ts`; relationship Inspector; authenticated active-generation comparison | The Inspector derives only bounded paths present in the returned graph, preserves every stored edge orientation, and distinguishes current configuration evidence from verification. A complete NAT egress chain is summarized separately from the flat path list; absent ingress remains unknown when relationship coverage is incomplete or a VM backend association is not modeled. |
| Active-inventory impact scope | implemented | `inventory_impact.py`; strict `blast-radius.model.ts` decoder; Operator and Console focused checks; authenticated local Browser observation | The Console renders a bounded stored-direction traversal with exact release, snapshot generation and cutoff, completeness, truncation, and unverified edge status. The response contains no provider properties and grants no execution or mutation authority. |
| Observed network focus and path presentation | implemented | `architecture-network-{focus,map,tools,icons,route}.ts*`; focused Console checks (`71 passed`); dense sequential three-viewport Playwright (`1 passed`); authenticated mobile observation | The Console keeps `InventoryGraphResponse` authoritative, rebuilds 2D presentation geometry from typed containment and attachment evidence, returns unknown instead of an absence claim when coverage is incomplete, and exports self-contained identifier-free SVG and PNG with the same typed routing. No governed runtime artifact is retained. |
| Semantic model and relationship direction | implemented | `ontology-semantic-model.ts`; `ontology-semantic-map.tsx`; catalog topology renderer and inspector; focused Vitest: 23 passed; Console typecheck passed | The four reviewed semantic bands, five operational lenses, arrowheads, and separate incoming and outgoing relationships are implemented. Authenticated desktop and mobile evidence remains open. |
| Agent Activity heartbeat presentation | validated | `console/src/routes/agents.model.ts`; `console/src/routes/agents.model.test.ts`; `docs/baselines/agent-activity-heartbeat-assurance-2026-08-14.json`; focused Vitest: 31 passed; authenticated Browser Entra assurance | Three successive heartbeat timestamps advanced across two refreshes, all three authenticated self checks succeeded, and zero runtime-initialization rows appeared. |
| Command Deck JSON readability | implemented | `json-code-block.tsx`; `model-trace-waterfall.tsx`; focused Vitest (`16 passed`); focused Playwright (`1 passed`); Console typecheck and production build | Model request and response JSON uses the shared syntax-highlighted surface with structured indentation and copy controls. Each layer in a grouped request is classified independently, nested object or array JSON strings expand within fixed bounds, and adjacent prose or malformed text keeps its literal presentation. The browser fixture is synthetic presentation evidence, not governed runtime evidence. |
| Web presentation module registry | implemented | `console/src/deck/presentation-modules/`; `presentation-artifact.ts`; `structured-reply.tsx`; focused Console checks (`115 passed`), typecheck, production build, and synthetic three-viewport Playwright | The thin shell delegates every accepted block to one closed registry entry. V2 charts require descriptions and exact tables; keyboard focus and reduced-motion rules are pinned. The synthetic fixture passed 1440 x 900, 993 x 641, and 390 x 844 with zero document, workspace, or chart overflow; no governed runtime receipt is claimed. |
| Shared Console and mock typography | implemented | `ui/calm-slate-tokens.css`; `ui/calm-slate-primitives.css`; `console/src/shared-style-tokens.test.ts`; [Issue #212](https://github.com/dotnetpower/fdai/issues/212) | Semantic page title, page subtitle, lead, section title, panel title, body, compact, label, and caption roles share one size and line-height contract. Representative Console and mock selectors consume the same tokens; specialized route text remains route-owned. |
| Static mock interaction assurance | implemented | `index.html`; `mocks/ui/components.html`; `mocks/ui/nav-redesign-v3.html`; `mocks/ui/finops-resource-efficiency.html`; focused mock and Console visual contracts; [Issue #215](https://github.com/dotnetpower/fdai/issues/215) | The master and nested shells preserve exact component-section links. The navigation specimen mirrors the compact Activity Bar, overlay Explorer, visibility controls, and Cost Governance entry, while the FinOps specimen pins its four read-only visualization views and synthetic-evidence boundary. |
| Cross-tab SSE and incident resilience | validated | Cross-tab stream hooks; `incidents.milestones.ts`; incident projections; `docs/baselines/console-cross-tab-sse-assurance-2026-08-14.json`; `docs/baselines/incident-rca-report-assurance-2026-08-15.json`; focused Console and Operator tests | Cross-tab leadership and failover passed, and authenticated Incident detail retained eight milestones, same-snapshot analytics, and unavailable source and plan context without claiming notification delivery. |
| Optional report PDF control | validated | `console/src/routes/reports.tsx`; service-local Operator PDF adapter; `docs/baselines/incident-rca-report-assurance-2026-08-15.json`; focused Console and Operator tests | Catalog and runtime registry jointly advertised `pdf`; authenticated Browser Entra rendered the report and verified a bounded 38809-byte PDF while preserving no-RCA unavailable behavior. |
| Conversation search request resilience | implemented | `console/src/routes/conversation-search.tsx`; `console/src/routes/conversation-search.test.tsx`; focused Console tests (`22 passed`) and typecheck | Search generations reject stale result and context writes. A generation-scoped in-flight key suppresses duplicate context requests before rerender without mixing results across searches. |
| Supervised assurance dependency cache | implemented | `console/vite.config.ts`; `scripts/automation/run_ontology_assurance.py`; focused Console and supervisor checks (`11 passed`) and typecheck | Ordinary Console starts retain Vite's standard dependency cache. Each supervised ontology-assurance Console receives a cache inside its run root so concurrent optimizers cannot invalidate measured Browser imports. |
| Console startup connection state | implemented | `console/src/app.tsx`; `console/src/routes/login.tsx`; focused startup and login tests (`3 passed`); Console typecheck; authenticated Browser fault injection | The first Operator API network failure replaces the route skeleton with the nebula service-starting state, keeps data closed, and automatically returns to the requested route after a successful retry. HTTP and authentication failures retain the existing recovery flow. |
| Activity Bar conversation entry | implemented | `console/src/components/navigation-shell.tsx`; `console/src/deck/command-deck-launcher.tsx`; `console/tests/e2e/conversation-entry.spec.ts`; focused Console tests | The utility rail selects general chat and its pressed state, while the bottom and keyboard entries select screen chat. Localized right/top tooltips explain both paths on hover and keyboard focus. The progressive-conversations owner tracks draft and context isolation; this row claims no model or resource-execution validation. |
| Architecture initial selection state | implemented | `architecture.tsx`; `architecture-map.tsx`; focused Vitest, Playwright, typecheck, and production build; [Issue #271](https://github.com/dotnetpower/fdai/issues/271) | Map mode keeps resources, relationships, map controls, the legend, and the relationship index hidden until the operator selects a resource. Selection, scope, presentation mode, and the centered prompt remain available. |
| Architecture canonical resource visuals | implemented | `architecture-map.model.ts`; `architecture-resource-abbreviations.ts`; focused architecture-map catalog coverage test | Every canonical resource type, including `llm-model-deployment`, has an explicit layer, color token, and stable abbreviation instead of silently using the generic fallback. |
| Command Deck JSON contrast | implemented | `console/src/styles.css`; `console/src/deck/command-deck-workspace-visual.test.ts`; focused Vitest: 10 passed; authenticated browser inspection | Syntax-highlighted JSON keeps the fixed dark code surface despite the global light `pre` style. Browser inspection is not retained as governed runtime evidence. |
| WARA catalog provenance and bounded rendering | implemented | `materialize-authoritative-catalogs.py`; `family_adapters.py`; `wara-controls.model.ts`; `wara-controls.tsx`; focused Python and Console checks | Every detail exposes the exact pinned APRL source and optional Learn link with version, revision, digest, retrieval time, license, and query digest. Fifty-row URL-addressable pages preserve the complete 456-record lifecycle without horizontal overflow. |
| Live late-subscriber and idle-source recovery | implemented | `streaming/live_stream.py`; `live_stage_kafka.py`; `use-live-stream.ts`; focused Operator and Console Live tests | The Live Kafka relay configures its hub to retain at most 256 accepted stage frames observed during the preceding 60 seconds. A fresh validated Huginn heartbeat keeps source readiness distinct from current control-loop work and expires after 15 seconds without observation. Neither path fabricates a stage or substitutes for durable History. |
### Implementation history
| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-07 | implemented | Added bounded recent-stage replay and a fresh runtime-source marker so opening Live between control-loop events reports a healthy source with no current work instead of remaining in `Awaiting source`. | `current change`; Live stream, relay, hook, catalogs, and focused tests; Operator pytest passed 16 cases; Console Vitest passed 22 cases; Ruff, mypy, and Console typecheck passed. | Governed runtime validation remains separate; replay resets on Operator API restart and the source marker expires without a fresh Huginn observation. |
| 2026-09-06 | implemented | Distinguished the general rail entry from the screen launcher through labels, pressed state, and localized shared tooltips. | `current change`; `navigation-shell.test.ts`; `conversation-entry.spec.ts`; focused Console checks and synthetic three-viewport geometry. | No governed model-answer or execution receipt is claimed by these presentation checks. |
| 2026-09-06 | implemented | Added the missing Architecture-map visual contract for model deployment instances. | `current change`; canonical architecture-map test passed 83 cases; Console typecheck and production build passed. | Authenticated visual evidence remains separate from this deterministic catalog mapping. |
| 2026-09-01 | implemented | Added source-visible WARA details, authoritative active inventory counts, bounded pagination, selected-row controls, and responsive two-column rendering. | `current change`; Python checks passed 45 cases; Console checks passed 4 cases; typecheck, production build, catalog parity, and live three-viewport measurements passed. | A governed live-Azure WARA receipt remains separate from this catalog presentation evidence. |
| 2026-08-31 | implemented | Added a read-only Kubernetes lifecycle section to Detection readiness. The API, not the browser, groups bounded analyzer receipts into current and historical assessments; the Console renders publication, recovery, duplicate delivery, and complete, incomplete, conflicting, or missed evidence with accessible disclosure and responsive wrapping. | `current change`; focused Python and Operator API checks passed 90 cases; focused Console checks passed 5 cases; typecheck and production build passed; synthetic 1440 x 900, 993 x 641, and 390 x 844 Playwright checks passed 3 cases with no measured horizontal overflow. | Authenticated governed runtime evidence remains separate and was not produced. |
| 2026-08-28 | implemented | Aligned the static navigation specimen with the production Console shell and added an interactive Cost Governance specimen with overview, resource efficiency, optimization-case, and settled-outcome views. | `current change`; `mocks/ui/nav-redesign-v3.html`; `mocks/ui/finops-resource-efficiency.html`; `console/src/components/mock-visual-boundary.test.ts`; focused mock parity test passed. | Governed runtime validation remains separate from these synthetic presentation specimens. |
| 2026-08-26 | implemented | Routed every valid model-request JSON layer through the shared highlighted JSON block, including JSON adjacent to prose under one grouped role. Request keys, strings, numbers, and literals now have distinct syntax color, structured indentation, a JSON label, and a copy control; adjacent prose and non-JSON text retain the literal fallback. | `current change`; `model-trace-waterfall.tsx`; `styles.css`; `agents-incident-deck.spec.ts`; focused Vitest passed 16 tests; focused desktop Playwright passed 1 test with mixed-group, JSON token, copy-control, nested-value, fallback, and zero-overflow assertions; reviewed synthetic element captures | No bounded request JSON presentation work remains. Governed runtime validation remains separate from the synthetic browser fixture. |
| 2026-08-26 | implemented | Made the model-provider waterfall own the compact run-record typography scale in both embedded and standalone direct-response placements. Narrow Command Deck containers now reflow the six-column lane to a three-column layout instead of overflowing. | `current change`; `styles.css`; `model-trace-waterfall.test.ts`; `agents-incident-deck.spec.ts`; focused Vitest passed 7 tests; focused desktop Playwright passed 1 test with computed `13px` heading, `12px` notice and model text, and zero panel overflow; Console Vite production build passed | No bounded model-trace work remains. The full Console typecheck remains independently blocked by the existing private `sourceCards` import in `retrieval-trace.test.ts`. |
| 2026-08-26 | implemented | Made captured model request and response JSON readable when a payload contains object or array JSON serialized inside another JSON string. The formatter expands only valid structured values within fixed depth and node bounds, preserves ordinary or malformed strings, and leaves trace hashes and stored payloads unchanged. | `current change`; `json-code-block.tsx`; `model-trace-waterfall.tsx`; focused Vitest passed 15 cases; focused desktop Playwright passed with zero escaped quote sequences and zero document, request, or response overflow; Console typecheck and production build passed. | Retain an authenticated captured model trace separately before claiming governed runtime validation. |
| 2026-08-25 | implemented | Narrowed default instance presentation to operational Resources and immediate scope context, and completed the source path that previously stopped an AKS VMSS at its template. | Focused Console checks passed 59 cases; typecheck and build passed. The authenticated VNet view showed zero role assignments and one direct owner Resource Group. The AKS view showed managed Resource Group level 1, VMSS level 2, and VM plus NIC level 3 in the initial viewport with zero desktop/constrained overflow. | Mobile hierarchy inspection was obscured by an independently opened Command Deck; mobile document overflow and 44 px controls still passed. Governed Browser retention remains separate. |
| 2026-08-25 | implemented | Corrected the Ontology Instances presentation after a representative VM's four-edge NAT egress path was hidden inside 420 indirect relationships and the depth-eight response was described as one hop. The new summary shows only evidence-backed stored edges and keeps absent ingress unknown. | `current change`; active snapshot contained 897 Resources and 1,642 links with exact inventory/ontology generation agreement; authenticated Console returned 197 Resources and 424 links without graph truncation; 56 focused tests, typecheck, production build, entry check, and three-viewport Browser checks passed. | No bounded implementation work remains. The Browser observation is local presentation evidence, not a governed runtime receipt. |
| 2026-08-25 | implemented | Separated relationship freshness and verification level in the Ontology Instances Inspector. Stale or future-cutoff evidence keeps its source, mapping, cutoff, and ceiling while remaining incomplete; provider configuration no longer reads as independent verification. | `current change`; focused Console tests passed 45 cases, source and test typechecks passed, production build and entry bundle check passed, and focused Operator tests passed 4 cases. | Retain authenticated refreshed-generation Browser evidence after the task-owned commit. |
| 2026-08-25 | implemented | Added an icon-only conversation control above the Activity Bar's bottom utilities. It defers one pre-mount toggle until the lazy Deck listener is ready, opens a fresh general conversation through the existing Deck lifecycle, closes on a second activation where the rail remains exposed, and changes no route or execution authority. | `current change`; `open-deck.test.ts` and `navigation-shell.test.ts` passed 23 focused tests; Console source and test typechecks and production build passed; authenticated 1440 x 900, 993 x 641, and 390 x 844 Browser checks measured 44 px controls, unchanged routes, no launcher overlap, and zero horizontal overflow. | No bounded implementation work remains. The Browser observations are local presentation evidence, not a governed runtime receipt. |
| 2026-08-22 | implemented | Added a separate 2D Network mode to Architecture without changing inventory or execution authority. It selects one observed VNet focus, exposes six presentation filters, traces shortest typed paths, keeps incomplete absence unknown, provides keyboard-equivalent SVG controls, and exports one identifier-free observed-topology report as SVG or PNG. | `current change`; focused network model, layout, renderer, route, and localization checks; source TypeScript check; synthetic Playwright passed `1440x900`, `993x641`, and `390x844` sequentially with zero horizontal overflow, two-hop path evidence, sanitized downloads, and 44 px mobile export controls; authenticated mobile showed eight boundaries, four nodes, four links, and one observed hop. | Retain governed exact-source desktop and mobile Browser evidence before changing this area to `validated`. |
| 2026-08-22 | implemented | Hardened Network mode and its downloaded report against dense dual-VNet topology. Peer focus now retains the peer VNet, direct subnets, and bounded attached resources; one obstacle-aware router is shared by the live SVG and export; VNet peering uses a direct boundary corridor; and the self-contained export preserves official icons, typed direction, evidence posture, and sanitization without runtime icon fetches. | `current change`; focused network and icon checks passed 74 cases; Console typecheck and production build passed; sequential `1440x900`, `993x641`, and `390x844` Playwright retained five directly reviewed captures and verified official icons, nine typed links, direct bidirectional peering, two-hop highlighting, sanitized SVG content, nonblank PNG, 44 px mobile nodes and controls, panning, and zero document overflow. | Retain governed exact-source desktop and mobile Browser evidence before changing this area to `validated`. |
| 2026-08-22 | implemented | Added a distinct Ontology `Instances` workbench over one active Resource generation. Operators can search and select one Resource, pivot across direct incoming and outgoing neighbors, inspect allowlisted identity and snapshot facts, review exact-resource FDAI audit activity, and see Azure event sources as unavailable instead of inferred. | `current change`; focused Operator tests passed 39 cases; Console decoder, ontology, and localization tests passed 22 cases; strict mypy, Ruff, task-scoped editor diagnostics, production Vite build, and entry bundle check passed. Authenticated desktop, constrained-desktop, and mobile checks found no document or workbench overflow, all mobile controls measured 44 px, and the canvas contained 20,735 nontransparent samples. | Promote a complete inventory generation containing a representative Container App, then retain exact-target selection, neighbor-pivot, and event evidence. Bind Resource Health and Activity Log through reviewed Operator projections before presenting those event families. |
| 2026-08-23 | implemented | Expanded the Ontology Instances reviewed icon set and simplified graph controls. The selected graph enters native full screen, ordinary wheel input controls zoom, and the detail Inspector collapses and restores its active view. The current active-snapshot audit maps 55 of 58 Resource types to reviewed product icons; role assignments, data collection endpoints, and intentionally unclassified resources keep the generic fallback. | `current change`; Azure asset integrity (`107 passed`); focused icon, view-control, and localization tests; authenticated desktop, constrained-desktop, and mobile checks measured exact full-screen bounds, Inspector space recovery, 44 px mobile controls, working wheel zoom, and zero document overflow. | Retain governed exact-source Browser evidence before changing this bounded area to `validated`. |
| 2026-08-23 | implemented | Bounded high-cardinality Resource Group and Subscription roots to a type-diverse direct membership view. Scope roots no longer expand every child branch or add relationships induced only because both children are visible. Full response counts, completeness, truncation, and Inspector evidence remain unchanged, and selecting a child restores that Resource's bounded branch context. | `current change`; the focused graph model passed 25 tests. The authenticated `rg-fdai-dev-krc` response remained incomplete at 200 Resources and 478 links with `resource_limit`, while presentation fell from 197 nodes and 450 edges to 37 unique nodes and 36 direct edges across 28 Resource types. Desktop, constrained-desktop, and mobile checks measured zero duplicate occurrences, coordinate collisions, non-LR containment or dependency edges, and document overflow. | Preserve a governed exact-source Browser receipt before changing this bounded area to `validated`. |
| 2026-08-23 | implemented | Added an evidence-gated AKS infrastructure hierarchy. The Console activates it only for the exact `azure.aks-attached-to-node-resource-group` mapping, then ranks the managed Resource Group before its stored contained infrastructure and ranks recorded identity and outbound-IP targets after their VMSS or Load Balancer source. Stored source and target values remain unchanged. | `current change`; the focused Console set passed 47 cases; typecheck and production build passed. The refreshed authenticated AKS view rendered main Resource Group `-2`, AKS `0`, managed Resource Group `1`, VMSS and Load Balancer `2`, and identity and outbound IP `3`. All three viewports retained 7 nodes and 10 stored edges with zero duplicate occurrences, collisions, non-LR edges, or document overflow. | Preserve a governed exact-source Browser receipt before changing this bounded area to `validated`. |
| 2026-08-13 | in-progress | Adopted the implementation ledger without reconstructing earlier provenance, and bound ontology assurance artifacts to exact source, configuration, workspace, authentication, request, and projection provenance. | Current change in `console/tests/live-e2e/ontology-query-assurance*.ts`; focused Vitest: 25 passed; Console typecheck passed. | Obtain the exact centralized receipt, then run one authenticated probe before the seeded bilingual 100-case cohort. |
| 2026-08-14 | implemented | Stopped periodic `Runtime agent initialized` snapshots from reappearing as chronological activity after each page refresh while preserving current state and heartbeat freshness. | `current change`; `agents.model.test.ts` passed 31 focused tests, and the authenticated browser showed zero initialization rows across two refreshes. | Retain the two-refresh Browser Entra result as a governed artifact before claiming runtime validation. |
| 2026-08-14 | implemented | Restored the fixed dark surface beneath syntax-highlighted Command Deck JSON after the global `pre` background overrode it. | `current change`; task-owned Console CSS and visual contract test; focused Vitest passed 10 tests; Console typecheck passed; authenticated browser computed the intended dark surface and token colors. | No bounded residual work remains; the focused regression test owns future theme changes. |
| 2026-08-14 | implemented | Reserved ordinary Operator API capacity under HTTP/1.1 by electing one cross-tab reader per attention and notification channel and sharing validated attention snapshots with follower tabs. | `current change`; task-owned stream hooks and focused Vitest passed 8 tests; Console typecheck passed. | Retain a governed three-tab Browser Entra artifact that proves a fresh Dashboard access check completes while SSE readers are enabled. |
| 2026-08-14 | implemented | Extended principal-scoped cross-tab stream leadership and added bounded incident milestones, action confirmation projections, and deterministic resilience baselines. | `current change`; focused Console and Operator tests plus Console typecheck. | Retain authenticated multi-tab and incident Browser evidence before claiming runtime validation. |
| 2026-08-14 | validated | Retained authenticated Browser Entra resilience artifacts and rejected ambiguous local-date timestamps at the SSE and cross-tab boundaries. | `current change`; strict stream timestamp tests passed 9 cases; authenticated Playwright passed both resilience cases; the tracked cross-tab and heartbeat artifacts bind source revision `848e1021786c2bb7f3fb0a533d9d113c3020d5cf` and one workspace patch digest. | Retain governed incident-detail evidence separately and complete the strengthened ontology cohort after a centrally validated revision has semantic-planning capacity. |
| 2026-08-14 | implemented | Unified the Operator ontology registry and Catalog topology under one exact-release producer, added InterfaceType and FunctionType nodes, and removed the SPA's generated topology copy. | `current change`; materializer parity tests passed 2 cases, focused Console tests passed 13 cases, and Console typecheck passed. | Render the reviewed Semantic model and retain a receipt-bound Context snapshot plus authenticated Browser evidence. |
| 2026-08-14 | implemented | Made the four-band Semantic model the default Ontology view, retained the dense graph as Catalog topology, and rendered canonical direction in both the semantic inspector and topology canvas. | `current change`; focused ontology Vitest passed 23 cases, catalog parity passed, and Console typecheck passed. | Retain authenticated desktop and mobile Browser evidence and bind an authoritative Context receipt before runtime evidence can appear. |
| 2026-08-14 | implemented | Completed ten independent critique rounds and fixed every verified Medium-or-higher finding in the bounded slice: fail-closed response decoding, canonical bands, profile-derived action membership, self-loops, relationship flags, keyboard controls, accessible landmarks and focus, topology bounds, and localized node kinds. | `current change`; focused Python tests passed 7 cases, ontology Vitest passed 27 cases, catalog parity and Console typecheck passed, and the Core import boundary passed. | Residual implementation findings are Low. Principal-scoped Context transport and authenticated Browser evidence remain explicit validation work rather than inferred availability. |
| 2026-08-14 | implemented | Authenticated Browser checks found and fixed an inert topology keyboard path and a 390 px intrinsic-width overflow. The Semantic model then rendered four bands, five lenses, one exact release, explicit Context unavailability, no body overflow, no node overlap, and no clipped node controls; the Catalog topology canvas contained nonblank pixels. | Local Browser Entra at 5273 plus `current change`; focused keyboard, geometry, decoder, semantic, i18n, and type checks passed. | The browser observations were not retained as a governed artifact, and hidden-tab `requestAnimationFrame` throttling prevented a reliable screenshot-based keyboard-motion receipt. |
| 2026-08-14 | implemented | Added an opt-in Incident RCA PDF download control without adding browser authorization or analysis behavior. | `current change`; catalog-plus-registry availability check, stale-download suppression, service-local PDF route, and focused Console and Operator tests. | Retain one exact-revision authenticated roster-to-RCA-to-report/PDF artifact. |
| 2026-08-14 | implemented | Added an exact-source Browser Entra runner for the Incident roster, RCA evidence, report envelope, PDF response, no-RCA state, and authentically unavailable source or plan context. | `current change`; `incident-rca-report-assurance.spec.ts`, Console typecheck, and focused Playwright discovery. | Execute the runner only after its source commit has a centralized receipt, then retain the redacted artifact. |
| 2026-08-14 | implemented | Bound the Incident assurance runner to JSON Operator responses so a same-path SPA document cannot satisfy an API evidence wait. | `current change`; `incident-rca-report-assurance.spec.ts`, Console typecheck, and focused Playwright discovery. | Centrally validate this runner revision, then rerun and retain the redacted artifact. |
| 2026-08-14 | implemented | Navigated the Incident assurance runner through the canonical `/root-cause-analysis` route so an unmatched legacy path cannot fall back to Overview. | `current change`; `incident-rca-report-assurance.spec.ts`, Console typecheck, and focused Playwright discovery. | Centrally validate this runner revision, then rerun and retain the redacted artifact. |
| 2026-08-14 | implemented | Fixed the Console RCA decoder after authenticated assurance exposed that it rejected the server's newest-first hypothesis order. | `current change`; `api-operations.ts`, `api.test.ts`, 13 focused decoder tests, and Console typecheck. | Centrally validate the fix, then rerun and retain the redacted artifact. |
| 2026-08-14 | implemented | Prevented stale or duplicate conversation-search context responses from replacing the current Console state. | `current change`; focused route and decoder tests passed 22 cases, Console typecheck passed, and catalog parity passed. | Governed Browser evidence remains part of the broader Console assurance campaign rather than this local request-state check. |
| 2026-08-15 | validated | Retained one authenticated Incident-to-RCA-to-report/PDF artifact after hardening JSON response binding, canonical navigation, newest-first decoding, and audit-backed report materialization. | `current change`; `docs/baselines/incident-rca-report-assurance-2026-08-15.json`; source `014974045e70e35c26e489fa238345cf70bc3ca3` is centrally validated. | No residual work remains for Incident detail or RCA PDF Browser evidence. |
| 2026-08-15 | implemented | Registered the ontology relationship-direction components in the Console visible-title inventory, which matches the `h4` headings they render and unblocks the shared validation queue. | `current change`; full Console Vitest suite passed 1782 cases. | No residual work; the inventory contract still owns future title-prop additions. |
| 2026-08-15 | implemented | Bounded the governed ontology assurance runner with a derived run budget, per-question and no-progress deadlines, adaptive request spacing, bounded transport retry, per-question progress output, and a provenance-bound resumable checkpoint. | `current change`; focused Vitest passed 79 live-evidence cases and the full Console suite passed 1782 cases; Console typecheck passed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Clamped every assurance turn to the remaining run budget, retired the checkpoint before publishing the artifact, required at least one live turn for a pass, separated turn errors from deadline breaches, and scoped the default checkpoint path per cohort. | `current change`; focused Vitest passed 80 live-evidence cases; Console typecheck passed; the first independent review's two Medium findings are closed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Corrected the preceding row: checkpoint retirement runs after the artifact is published and before the assertions, so a failed publish cannot destroy a complete cohort while a later run still cannot replay one. | `current change`; focused Vitest passed 86 live-evidence cases. | No residual work for checkpoint retirement ordering. |
| 2026-08-15 | implemented | Bound the assurance checkpoint to an evidence identity covering result schema, seed, ordered cohort, and authentication rather than the whole run configuration, so a per-run session id or an adjusted pacing, deadline, or retry knob no longer discards completed turns; every retained result now records its producing run and an unattributed result cannot pass. | `current change`; focused Vitest passed 80 live-evidence cases and the full Console suite passed 1793 cases; Console typecheck passed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Added the target stack origin to the checkpoint binding so an interrupted cohort cannot resume against a different deployment, stopped persisting budget-derived non-results as permanent failures, derived the retry count from retained evidence, and validated retained result shape on load. | `current change`; focused Vitest passed 89 live-evidence cases; Console typecheck passed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Separated the stalled-question bound from the run-budget bound so a stall is recorded as a real failure and never aborts the cohort as budget exhaustion, forwarded the result predicate on the checkpoint read path, disclosed the question that stopped a budget-limited run, and extended the typecheck gate to `console/tests`. | `current change`; focused Vitest passed 95 live-evidence cases; `npm run typecheck` now covers `console/tests` and passed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Each expired attempt now publishes the bound that truncated it, so `per_attempt_deadline_exceeded`, `stalled_question`, and `question_budget_exhausted` are distinguishable evidence, and the Console typecheck runs inside the enforced operator-surface gate rather than only in a dispatch-only workflow. | `current change`; focused Vitest passed 98 live-evidence cases; full Console suite passed 1802 cases; `npm run typecheck` passed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | A cohort completed but interrupted before publication no longer loses its evidence: it republishes as `run_mode: resumed_replay`, the checkpoint is retired only once its cohort published, and the artifact counts stalled questions separately from per-attempt deadline breaches. | `current change`; full Console suite passed 1802 cases; `npm run typecheck` and the typecheck-parity contract passed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | A replayed cohort republishes retained evidence but never carries release authority: it reports `receipt_source: resumed_replay` and cannot be production-ready, an interrupted run is labelled `interrupted` rather than a replay, and checkpoint load now validates every retained field the pass criteria read. | `current change`; focused Vitest passed 102 live-evidence cases; `npm run typecheck` and typecheck-parity passed. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Replaced replay publication with live re-verification: a fully covered checkpoint releases its last question back to the live stack, so a resumed run always proves the current stack, never becomes a sink that deletes its checkpoint and fails, and cannot launder stale answers because every retained answer must report the same ontology release and principal manifest generation. | `current change`; focused Vitest passed 105 live-evidence cases; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Made the live proof falsifiable: a resumed run releases the cohort tail through the last answer-required question, republished answers must reproduce the ontology release and principal manifest that a live answer confirms, a complete-but-failing cohort keeps its checkpoint, an interrupted live run reports `interrupted_partial` rather than a replay, and the checkpoint-trust, pass, retirement, and checkpoint-path policy moved into tested pure functions. | `current change`; focused Vitest passed 123 live-evidence cases; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Made a resumed run converge: only a verified turn resumes so a flaky turn is re-attempted rather than inherited forever, exactly one answer-required proof question is released so a resume costs one turn, every preamble step declares its own timeout because Playwright disables navigation and action timeouts by default, a selection that cannot prove a generation is rejected, and retained locale, operation, and attempt outcomes are validated as governed values. | `current change`; focused Vitest passed 126 live-evidence cases; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Closed the remaining resume traps: the live proof question is now the last one an earlier run answered with a generation digest rather than one picked by operation, a complete cohort retires its checkpoint only when the outcome the runner asserts is satisfied, an abandoned turn no longer leaks an authenticated stream onto the reused page, a missing receipt is reported as the transport outcome it was, and the retained result types and attempt outcomes are shared so a new field breaks the build instead of the checkpoint. | `current change`; focused Vitest passed 129 live-evidence cases; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Turned every detected fault into a recoverable one: a checkpoint that mixes ontology generations is discarded instead of being refused forever, the checkpoint file is keyed by the whole binding so alternating revisions or target stacks no longer overwrite each other, a page that cannot be reset stops the run rather than publishing turns taken on it, and `console/scripts` joined the typecheck gate, which exposed and fixed a real type break. | `current change`; focused Vitest passed 134 live-evidence cases; full console suite 1,838 passed; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Repaired the enforced gate and the remaining resume traps: the tests typecheck now resolves its project from the repository root instead of aborting the operator gate before the console build and CLI checks, a truncated run keeps its checkpoint because proving nothing is not a contradiction, a mid-run release rotation prunes superseded turns instead of deleting every completed turn, an incoherent verification artifact fails a governed refusal too, and the partial file carries the writing process. | `current change`; focused Vitest passed 139 live-evidence cases; typecheck-parity 6 passed; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Removed the last replay fixpoint: a cohort that passed every turn but missed its release criteria now releases the answer-required turns that only refused, so a later run re-attempts them instead of republishing the same block, and a pacing or checkpoint-write fault becomes a governed stop reason instead of escaping the test without an artifact. The gate parity test also pins the whole invocation, and an operator-configured checkpoint path is trimmed. | `current change`; focused Vitest passed 141 live-evidence cases; typecheck-parity 6 passed; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-15 | implemented | Extended the governed stop path over the whole question: a page fault during the intra-question retry wait, or with pacing disabled by a permitted override, now stops the run with `page_unavailable` instead of escaping the test body, a budget-truncated attempt no longer spends a reload it cannot use or report a failed context reset as the cause, and a budget stop at the top of the loop names its question. | `current change`; focused Vitest passed 143 live-evidence cases; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-16 | implemented | Disclosed the resume decision before the cohort loop. A checkpoint that failed its binding check restored nothing silently, so an operator could only learn it from the final artifact; the run now prints one `assurance-resume` line carrying the stored, resumed, and outstanding counts. | `current change`; focused Vitest passed 143 live-evidence cases; `npm run typecheck` passed both projects. | Run the bounded cohort at a release boundary and retain the resulting artifact. |
| 2026-08-16 | implemented | Added an independent operation-to-plan capability oracle to the authenticated ontology cohort. The runner now retains Core's projected exact-plan capabilities and rejects an evidence-complete answer when its generated operation required a different capability, so a manifest query cannot satisfy aggregation and an unfiltered ObjectSet cannot satisfy a property filter. Run configuration `1.4.0` prevents pre-oracle checkpoints from resuming, and artifact `1.3.0` reports the mismatch count. | `current change`; focused assurance Vitest passed 96 cases, Console and test typecheck passed, and Playwright discovered the exact live test. | Run the strict bilingual gate and seeded 100-case cohort from a centrally validated descendant, retaining only artifacts with zero capability mismatches. |
| 2026-08-16 | implemented | Extended retained exact-plan evidence with `metric_scope_series` so a generic causal plan's bounded visible-resource metric reads remain observable in the artifact. The operation oracle still requires the final `evidence_join`; the additive capability cannot satisfy causal analysis by itself or weaken the zero-mismatch gate. | `current change`; focused assurance Vitest passed 96 cases and Console plus test typecheck passed. | Run the strict bilingual gate and seeded 100-case cohort from a centrally validated descendant. |
| 2026-08-17 | implemented | Isolated ontology assurance from the Console's page-load incident auto-investigation. The harness supplies an empty incident-attention stream, observes every chat POST, records ambient and incident-bound request counts, and fails both the cohort and immutable artifact gates unless both remain zero. | `current change`; focused assurance Vitest passed 124 cases, strict/full supervisor checks passed 7 cases, Console typecheck passed, and Playwright discovered the exact live test. | Retain a fresh strict artifact with zero ambient and bound requests before starting the seeded cohort. |
| 2026-08-17 | implemented | Isolated each supervised assurance Console's Vite dependency cache after an exact-source strict attempt reached service readiness but failed its Browser preamble on a stale shared MSAL optimizer URL. The attempt ran zero measured questions, produced no artifact, and did not start the seeded cohort. | `current change`; focused cache checks passed 2 cases, the supervisor suite passed 9 cases, and Console typecheck, Ruff, and strict mypy passed. | Obtain central validation and retain a fresh strict artifact from the cache-isolated source. |
| 2026-08-17 | in-progress | The cache-isolated strict Browser cohort completed 14 live turns, but exact topic evidence retained only 6 request and 6 projection records. The supervisor rejected the artifact and did not start the seeded cohort. | Run `issue63-40fbd0c41e-20260817T084406Z` on centrally validated source `40fbd0c41eda506e6976e3090fab3bd9502b98f0`; Playwright passed, immutable transport ownership failed. | Retain a fresh strict Browser artifact only after the sibling outbox prefix has central validation. |
| 2026-08-19 | implemented | Centralized the Console and static mock semantic typography scale, connected representative shell text to it, and added an English/Korean responsive specimen to the existing component gallery. | Commit `1e4063e`; [Issue #212](https://github.com/dotnetpower/fdai/issues/212); shared style contract test passed 2 cases; Console typecheck and production build passed; browser checks at 1440 px and 390 px found no document, section, or long-identifier overflow. | No work remains for the shared semantic roles. Specialized route typography remains owned by its route and should adopt the scale when changed. |
| 2026-08-19 | implemented | Registered the dedicated Typography page in the repository-root design mock index used by the shared session browser, in addition to the direct mock sidebar and nested kit landing. | `current change`; [Issue #212](https://github.com/dotnetpower/fdai/issues/212); focused navigation contract and mock visual-boundary tests; the shared browser selected `#mocks/ui/typography.html`, marked the root Labs item active, and loaded the dedicated page in the preview frame. | No entry-point work remains for the Typography mock. |
| 2026-08-19 | implemented | Hardened all 23 Components demonstrations and both mock shells after a 30-finding critique. Search, exact-section routes, truthful display terms, synthetic and dark preview states, keyboard tabs and dialogs, responsive navigation, and accessible table and touch contracts now share one reference surface. | `current change`; [Issue #215](https://github.com/dotnetpower/fdai/issues/215); mock Node contracts passed 12 cases; Console visual and token contracts passed 5 cases; browser checks at 1440 px, 993 px, and 390 px reported zero actionable semantic, overflow, sub-12-text, and touch-target findings. | No bounded residual work remains for the Components reference gallery or its two static shells. |
| 2026-08-19 | implemented | Added the ObjectType declaration workbench with exact property, lifecycle, provenance, relationship, governed-action, evidence-health, dependent, and release-compatibility sections. The authenticated local Console showed `Decision` unavailable evidence and `Resource` aggregate evidence, no raw resource ids, no execute control, and no document overflow at 1440 x 900, 993 x 641, or 390 x 844. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; 50 focused Python tests, 43 focused Console tests, typecheck, production build, Ruff, and strict mypy passed. | Retain the Browser result as a governed exact-source artifact and bind a secured principal-scoped Context receipt before changing this area to `validated`. |
| 2026-08-19 | implemented | Replaced the disconnected Impact scope projection with a bounded active-snapshot traversal and exposed it from the Resource declaration workbench. The authenticated local Console returned one complete depth-one edge with exact release and cutoff, fixed no-authority flags, and no document overflow at 390 px. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; focused Operator, migration, Console, type, and production-build checks. | Retain the observation as a governed exact-source artifact and bind a secured principal-scoped Context receipt before changing this area to `validated`. |
| 2026-08-19 | implemented | Hardened the Impact scope decoder to reject an edge whose source or target is absent from the reached-node projection. A malformed upstream graph can no longer render as a coherent bounded result. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; `blast-radius.model.test.ts` passed 9 focused cases and Console typecheck passed. | Continue the independent hardening rounds and retain governed exact-source Browser evidence after the bounded slice has no Medium-or-higher finding. |
| 2026-08-19 | implemented | Enforced the producer's breadth-first edge-depth invariant at the Console boundary. An edge must leave the preceding depth and cannot point to a node deeper than its own traversal wave; valid back and cycle edges to earlier depths remain accepted. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; `blast-radius.model.test.ts` passed 11 focused cases and Console typecheck passed. | Continue adversarial decoder review until no Medium-or-higher incoherent graph shape remains. |
| 2026-08-19 | implemented | Required producer-valid provenance for every reached node. The target root has no incoming link type, and each non-root node must name a requested LinkType backed by a matching incoming edge at the same breadth-first depth. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; `blast-radius.model.test.ts` passed 14 focused cases and Console typecheck passed. | Re-run the adversarial decoder review for duplicate and bounded-array residuals before closing this round. |
| 2026-08-19 | implemented | Rejected duplicate impact relationships at the Console trust boundary. The final adversarial decoder review retained only Low producer-bound and timestamp-leniency residuals; no Medium-or-higher graph-coherence finding remains. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; `blast-radius.model.test.ts` passed 15 focused cases and Console typecheck passed. | Keep producer array bounds and stricter RFC 3339 parsing as Low defense-in-depth candidates rather than release blockers. |
| 2026-08-19 | implemented | Exposed the selected Impact scope view through `aria-pressed` on all three segmented buttons instead of relying on visual active styling. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; the focused control contract passed 2 cases, Console typecheck passed, and the authenticated 390 px Browser exposed exactly one pressed view. | Continue the accessibility round with the separately measured focus-indicator and target-size findings. |
| 2026-08-19 | implemented | Added an explicit `:focus-visible` outline to segmented view controls. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; the focused control contract passed 2 cases, Console typecheck passed, and authenticated keyboard navigation measured a solid 2 px outline with 2 px offset. | Continue the accessibility round with the separately measured 22 px target-size finding. |
| 2026-08-19 | implemented | Raised the Impact scope segmented buttons to 44 px minimum height at mobile widths without changing dense controls elsewhere. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; the focused control contract passed 2 cases, Console typecheck passed, and the authenticated 390 px Browser measured all three buttons at 44 px with one pressed state and the visible focus outline preserved. | Re-run the accessibility critique and retain only Low residuals before closing the round. |
| 2026-08-19 | implemented | Stacked the Impact scope topology before the Console shell narrows below its two-column minimum. The constrained desktop no longer creates a main horizontal scrollbar. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; focused control tests, typecheck, and production build passed; authenticated 1440 x 900, 993 x 641, and 390 x 844 Browser checks measured zero document, main, layout, or control-overlap findings. | Only Low visual residuals remain: mobile graph whitespace, intentional table scrolling, and linked identifier ellipsis. |
| 2026-08-19 | implemented | Made edge verification status visible instead of accepting `unverified` as hidden machine metadata. The edge table localizes the status, and the published view context preserves the exact machine value. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; focused Console and catalog tests passed 11 cases, typecheck and production build passed, and the authenticated Browser rendered one `Verification` column with the current `Unverified` edge. | Re-run localization and documentation critique; governed exact-source Browser retention remains separate. |
| 2026-08-19 | implemented | Bound the optional Architecture map to the impact projection's inventory snapshot. Exact generation wins when available, legacy graphs must match the cutoff, and a changed impact snapshot resets and refetches the map instead of combining generations. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; focused Console and catalog tests passed 25 cases, typecheck and production build passed, and the authenticated Map tab rendered for the matching local cutoff without a mismatch state. | Continue the concurrency and determinism recritique; governed exact-source Browser retention remains separate. |
| 2026-08-19 | implemented | Completed 12 independent critique-and-hardening rounds over wire invariants, traversal correctness and bounds, SQL privileges, API errors, adversarial decoding, accessibility, responsive layout, localization, migration rollback, concurrency, and evidence authority. Every verified Medium-or-higher finding was fixed and rechecked before the next round. | [Issue #223](https://github.com/dotnetpower/fdai/issues/223); `current change`; task-owned focused checks, exact commit checks, production build, and authenticated three-viewport observations cited in the preceding rows. | Residual implementation findings are Low: producer-bound array limits in the decoder, strict RFC 3339 defense in depth, linked identifier ellipsis, mobile graph whitespace, and intentional evidence-table scrolling. Governed exact-source Browser retention and a principal-scoped Context receipt remain separate evidence delivery. |
| 2026-08-19 | in-progress | Accepted the Web presentation registry design after separating artifact validation, block lookup, and per-kind rendering. | `current change`; this owner document pair. | Split the renderer, add accessible v2 chart modules, and retain desktop, constrained, and mobile evidence before changing the scope state. |
| 2026-08-19 | implemented | Split structured replies into a thin shell and per-kind registry modules, added strict v2 parsing, and rendered bar, coverage, time-series, comparison, and timeline blocks with semantic descriptions, keyboard-reachable values, reduced-motion behavior, and collapsed exact tables. | `current change`; [Issue #234](https://github.com/dotnetpower/fdai/issues/234); focused parser, registry, and assurance checks passed 115 cases; Console typecheck and production build passed. | Retain desktop, constrained, and mobile Browser evidence with long Korean, timestamps, opaque identifiers, and unavailable states. |
| 2026-08-19 | implemented | Exercised the v2 time-series module through the real Command Deck reducer, parser, registry, and renderer at desktop, constrained, and mobile sizes. The fixture included long Korean, exact timestamps, opaque ids, keyboard focus, an exact-value disclosure, and reduced motion. | `current change`; [Issue #234](https://github.com/dotnetpower/fdai/issues/234); focused Playwright passed in 7.4 seconds at 1440 x 900, 993 x 641, and 390 x 844 with zero measured document, workspace, or chart overflow; screenshots were reviewed. | Governed authenticated runtime evidence remains separate; this synthetic Browser check proves presentation mechanics only. |
| 2026-08-21 | implemented | Replaced the Dashboard skeleton with the existing nebula sign-in surface when authenticated bootstrap first observes an Operator API network failure. The screen names service startup, retries automatically, and leaves the requested route closed until access verification succeeds. | `current change`; `bootstrap-retry.test.ts` and `login.test.ts` passed 3 focused tests; Console typecheck passed; authenticated Browser fault injection showed the startup state, zero horizontal overflow, and automatic `/overview` recovery without reload. | No governed Browser artifact was retained. Manual access retry and sign-in recovery remain unchanged after the bounded startup schedule is exhausted. |
| 2026-08-22 | implemented | Added shared Preact trend, comparison-bar, distribution, and density-heatmap primitives and adopted them across Operating Outcomes, Process reports, legacy Command Deck charts, and verified v2 bar and time-series presentation. Every supported mark is keyboard inspectable, exact values remain visible or available in the existing table disclosure, median and baseline labels are localized, and the static gallery exposes synthetic provenance plus an explicit data-inspection command. | `current change`; `console/src/components/charts.{tsx,css}` and focused chart, analytics, Process, registry, and workspace Vitest checks passed 35 cases; Console typecheck and catalog parity passed; the static gallery contract passed 8 cases; isolated Playwright rendered the production primitives at 1440 x 900, 993 x 641, and 390 x 844 with zero document, panel, or heatmap overflow and reviewed screenshots. | The standard authenticated route could not produce a runtime observation while the local Operator API was unavailable. Retain a governed authenticated chart observation separately before claiming runtime validation. |
| 2026-08-22 | implemented | Completed the chart family with the exact Tremor Raw `500` palette in its published order: blue, emerald, violet, amber, gray, cyan, pink, lime, and fuchsia. The typed inventory now covers all 13 official chart and data-visualization exports: AreaChart, BarChart, BarList, CategoryBar, ComboChart, DonutChart, LineChart, ProgressBar, ProgressCircle, SparkAreaChart, SparkBarChart, SparkLineChart, and Tracker. FDAI scatter and heatmap views use the same palette and accessibility contract, and the Process report paths now consume the shared donut, scatter, spark, progress-bar, and progress-circle primitives. | `current change`; `chart-colors.ts`, `charts-series.tsx`, `charts-compact.tsx`, `charts-scatter.tsx`, the Process graph renderer, and the complete static gallery; focused chart and Process checks passed 19 cases, Console typecheck passed, the mock contract passed 8 cases, and isolated Playwright rendered 16 production panels at 1440 x 900, 993 x 641, and 390 x 844 with zero document, panel, or heatmap overflow. The session browser rendered six family cards at three, two, and one columns with the exact nine computed hex values and zero card overflow. | Governed authenticated runtime evidence remains separate. The retained browser evidence is synthetic presentation evidence and grants no measurement or action authority. |
| 2026-08-22 | implemented | Added category-wide pointer and keyboard inspection to full-size area, line, grouped-bar, and combo charts. One active x-axis slice now shows every finite series value in a structured tooltip with a vertical guide and color-matched points. Trend categories, donut and pie visuals, and progress bars and circles also expose exact values directly. The static family specimens now provide equivalent three-series and per-mark inspection instead of decorative-only diagrams. | `current change`; `tooltip.tsx`, `charts-series.tsx`, `charts-compact.tsx`, `charts.tsx`, `charts.css`, the production fixture, and `mocks/ui/components.html`; focused chart, tooltip, and Process checks passed 27 cases; Console typecheck, production build, and entry bundle passed; the static gallery contract passed 8 cases; and isolated Playwright passed at 1440 x 900, 993 x 641, and 390 x 844 with zero measured overflow. Browser review measured a 53 x 139 px production category target with three tooltip rows and active guide points, plus a 44 x 122 px static target with the same exact Thursday values. | Governed authenticated runtime evidence remains separate. These synthetic checks prove presentation and interaction mechanics only and grant no measurement or action authority. |
| 2026-08-22 | implemented | Added the dashboard compositions that the primitive inventory did not cover. `TREMOR_CHART_COMPOSITIONS` now pins seven supported families: portfolio performance, interactive KPI area, current-versus-previous line, allocation donut, custom-tooltip bar, responsive monitoring area, and uptime category. The shared `MetricChartFrame`, `MetricSeriesChart`, and `MetricDonutChart` compose KPI values, change context, summaries, bounded date ticks, y-axis values, legends, responsive variants, and active-datum updates without duplicating chart geometry. The portfolio specimen matches the reviewed Tremor pattern with 17 dates, three holdings, six bounded date ticks, currency formatting, and one three-row grouped tooltip. | `current change`; `charts-composed.tsx`, extended `charts-series.tsx`, the 23-panel production fixture, and seven static dashboard-composition cards; focused chart, tooltip, and Process checks passed 29 cases; Console typecheck, production build, and entry bundle passed; the static gallery contract passed 8 cases; and isolated Playwright passed at 1440 x 900, 993 x 641, and 390 x 844. Browser checks found zero document, fixture, panel, card, and iframe overflow; static compositions rendered as three columns on desktop, two in the constrained iframe, and one on mobile, with the mobile portfolio tooltip clamped inside the 375 px iframe. | Governed authenticated runtime evidence remains separate. The values and browser observations are synthetic presentation evidence and do not grant measurement or action authority. |
| 2026-08-22 | implemented | Corrected the completeness boundary after direct review of `https://www.tremor.so/charts`: 13 exported component names and seven composition families did not cover the page's 33 published examples. `TREMOR_CHART_CATALOG` now pins all 33 headings in official order. The shared renderers now implement stacked and percent area and bar geometry, axis titles, start/end ticks, horizontal bars through `layout="vertical"`, grouped bars, conditional colors, custom tooltip content, configurable styling and radius, gradient bars, donut and area callbacks, four ProgressCircle variants with radius and child metrics, and CategoryBar markers. | `current change`; `chart-colors.ts`, `charts-series.tsx`, `charts-compact.tsx`, `charts.tsx`, the 56-panel production fixture, and the static 33-card catalog; focused chart, tooltip, and Process checks passed 31 cases; Console typecheck, production build, and entry bundle passed; the static gallery contract passed 8 cases; and three-viewport Playwright exercised exact catalog order and variant behavior in 7.2 seconds. Browser checks measured stacked area paths, a 100% domain, axis titles, a 288 x 6 px horizontal bar, alternating conditional colors, gradient and marker geometry, and zero document, fixture, panel, grid, card, iframe, and outer overflow. The static catalog rendered 3 x 11, 2 x 17, and 1 x 33 at desktop, constrained, and mobile sizes. | Governed authenticated runtime evidence remains separate. The catalog fixtures are synthetic and prove presentation mechanics only; they do not grant measurement or action authority. |
| 2026-08-22 | implemented | Re-audited every chart function and all 33 catalog examples through two independent reviews, then fixed every confirmed Medium-or-higher finding. Line and area charts preserve non-finite gaps, percent charts reject negative inputs instead of hiding them, area gradients are real SVG gradients, CategoryBar uses all nine Tremor colors and abstains from a marker when its total is zero, marker animation respects reduced motion, Donut supports hole-aware pointer selection and first-use arrow-key navigation, and BarList no longer announces negative values while drawing zero-width bars. Native button roles are preserved inside Donut and Tracker lists, and series charts now implement keyboard-selectable `onValueChange` with a persistent guide and `aria-pressed`. | `current change`; chart source and the 62-panel adversarial fixture; focused chart, tooltip, and Process checks passed 33 cases; Console typecheck and production build passed; the static contract passed 8 cases; and two three-viewport Playwright checks passed in 9.7 seconds. Browser checks confirmed two gap-preserving area segments plus one isolated point, rejection of invalid percent data, nine distinct CategoryBar colors, no zero-total marker, Donut keyboard and hole callbacks, gradient and heatmap differentiation, ProgressCircle variant colors, marker reduced motion, signed and single-point bounds, native list/button semantics, 33 named focus targets, and zero production or static overflow. A second independent audit found no remaining Medium-or-higher chart defect after the BarList correction. | Governed authenticated runtime evidence remains separate. The adversarial fixtures remain synthetic presentation evidence and do not grant measurement or action authority. |
| 2026-08-24 | implemented | Hid the Architecture Map resources and relationship controls until an operator selects a resource, while preserving the selection, scope, and presentation-mode controls. | [Issue #271](https://github.com/dotnetpower/fdai/issues/271); `current change`; focused Vitest passed 62 cases, Playwright passed 2 cases, Console typecheck passed, and the production build passed. | No implementation work remains for the initial Map selection state. |

### Remaining work
- [ ] Promote a complete active inventory generation containing a representative Container App,
  then retain authenticated exact-target evidence for selection, one-hop neighbor pivot, durable
  FDAI activity, independent source gaps, keyboard interaction, and three-viewport overflow.
- [ ] Bind bounded Resource Health and Azure Activity Log projections to the instance explorer,
  preserving source-specific time, completeness, and evidence references without provider access
  or browser query authority.
- [ ] Retain one passing governed artifact from the seeded bilingual 100-case cohort after the exact centralized receipt and authenticated probe exist.
- [x] Retain a governed Browser Entra artifact that shows an open agent stream, refreshed heartbeat time, and zero `Runtime agent initialized` activity rows across two page refreshes.
- [x] Retain a governed three-tab Browser Entra artifact that shows a fresh Dashboard access check completing while background notifications and active-tab attention streams remain enabled.
- [x] Retain governed incident-detail Browser evidence for the milestone, source, response-plan, and same-snapshot outcome presentation.
- [x] Retain authenticated roster-to-RCA-to-report/PDF evidence that binds one source revision and workspace digest without claiming unavailable RCA facts.
- [ ] Retain authenticated Browser evidence that the Semantic model and Catalog topology display one matching ontology release and that Context remains unavailable without a secured receipt.
- [ ] Retain a governed exact-source Browser artifact for the ObjectType workbench that covers the
	complete and unavailable evidence paths, exact release identity, keyboard navigation, and three
	viewport overflow checks.
- [x] Render a bounded active-inventory impact result with exact release and cutoff, explicit completeness and truncation, and no execution or mutation authority.
- [x] Keep shared Console and static mock page title, page subtitle, body, compact, label, and caption typography aligned through semantic tokens and the responsive component-gallery specimen.
- [x] Keep the Components gallery and both static shells searchable, exact-linkable, keyboard accessible, overflow-free, and explicit about synthetic preview data through focused contracts and three-viewport browser checks.
- [x] Implement the Web block registry and accessible v2 chart modules, then verify keyboard,
  reduced-motion, exact-value fallback, Korean text, opaque identifiers, timestamps, empty and
  unavailable states at 1440 x 900, 993 x 641, and 390 x 844.
- [x] Keep Architecture Map resources and relationships hidden until selection, with focused unit,
  browser, typecheck, and production-build evidence recorded in Issue #271.
