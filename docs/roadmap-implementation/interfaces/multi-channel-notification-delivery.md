# Multi-Channel Notification Delivery implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work for
[Multi-Channel Notification Delivery](../../roadmap/interfaces/multi-channel-notification-delivery.md).

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Fan-out delivery design and ownership | implemented | [Multi-Channel Notification Delivery](../../roadmap/interfaces/multi-channel-notification-delivery.md); this ledger | The design is accepted and owned. No runtime behavior changed in this transition. |
| Explicit fan-out route schema | implemented | [`matrix.py`](../../../services/core-control-plane/src/fdai/core/notifications/matrix.py), [`notifications-matrix.yaml`](../../../config/notifications-matrix.yaml), [`test_matrix.py`](../../../services/core-control-plane/tests/notifications/test_matrix.py) | A2/A4 routes use explicit `fanout` plus `channels`; legacy A1/A3 failover remains compatible. Unknown, duplicate, and mixed route shapes fail load. |
| Channel bindings, enablement, and startup validation | implemented | [`bindings.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/bindings.py), [`delivery.py`](../../../services/core-control-plane/src/fdai/runtime/delivery.py), [`runtime_settings.py`](../../../services/core-control-plane/src/fdai/delivery/runtime_settings.py), [`test_bindings.py`](../../../services/core-control-plane/tests/notifications/test_bindings.py) | Named JSON bindings carry enablement and trust tiers while secret-bearing values resolve through named environment variables. Enabled incomplete bindings fail startup. |
| Dispatch plan and per-channel durable delivery | implemented | [`delivery.py`](../../../services/core-control-plane/src/fdai/core/notifications/delivery.py), [`postgres_notification_delivery.py`](../../../services/core-control-plane/src/fdai/delivery/persistence/postgres_notification_delivery.py), [`durable_notifications.py`](../../../services/core-control-plane/src/fdai/core/incident/durable_notifications.py), [`test_fanout_delivery.py`](../../../services/core-control-plane/tests/notifications/test_fanout_delivery.py) | Frozen parent plans and stable per-channel child rows preserve leases, attempt ceilings, accepted confirmation deadlines, and terminal state across re-entry. Incident checkpoints wait for terminal fan-out state. |
| Router fan-out, failure isolation, and partial outcome | implemented | [`router.py`](../../../services/core-control-plane/src/fdai/core/notifications/router.py), [`test_fanout_delivery.py`](../../../services/core-control-plane/tests/notifications/test_fanout_delivery.py) | The router sends all eligible targets with bounded concurrency, isolates channel failures, and audits aggregate outcomes plus exclusions. |
| Teams Workflows webhook transport | implemented | [`teams.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/teams.py), [`test_adapters.py`](../../../services/core-control-plane/tests/notifications/test_adapters.py) | The Workflows envelope includes `contentUrl`, enforces 28 KB, bounds `429` backoff, supports unauthenticated or workload-identity modes, and reports provider success as `accepted`. |
| Delivery effect verification | implemented | [`notification_receipt.py`](../../../packages/service-contracts/src/fdai_service_contracts/notification_receipt.py), [`notification_receipt_ingress.py`](../../../services/operator-service/src/fdai_operator_service/notification_receipt_ingress.py), [`receipt.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/receipt.py), [`consumers.py`](../../../services/core-control-plane/src/fdai/runtime/consumers.py), [`test_notification_receipt.py`](../../../packages/service-contracts/tests/test_notification_receipt.py), [`test_notification_receipt_ingress.py`](../../../services/operator-service/tests/test_notification_receipt_ingress.py), [`test_notification_receipt_consumer.py`](../../../services/core-control-plane/tests/runtime/test_notification_receipt_consumer.py), [`test_workflow_receipts.py`](../../../services/core-control-plane/tests/notifications/test_workflow_receipts.py) | An authenticated Operator ingress verifies one signed callback, publishes one schema-validated envelope, and a Core consumer applies `delivered` or `retryable_failed` with prepared and completed observation audit phases. |
| A2/A4 delivery activation | implemented | [`local_binding.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/local_binding.py), [`delivery.py`](../../../services/core-control-plane/src/fdai/runtime/delivery.py), [`notification_binding.py`](../../../packages/service-contracts/src/fdai_service_contracts/notification_binding.py), `infra/services/core-control-plane/**`, [`test_local_binding.py`](../../../services/core-control-plane/tests/notifications/test_local_binding.py), [`test_teams_notification_activation.py`](../../../tests/integration/infra/test_teams_notification_activation.py) | Deployed activation is an explicit Terraform input with a read-only Key Vault secret reference; local activation reads the encrypted Operator-owned record through a delivery-side store. An unconfigured placeholder endpoint fails startup instead of activating a destination. |
| Source-attributed integration readiness | implemented | [`integration_readiness.py`](../../../services/core-control-plane/src/fdai/delivery/integration_readiness.py), [`runtime_settings.py`](../../../services/core-control-plane/src/fdai/delivery/runtime_settings.py), [`materialize-authoritative-settings.py`](../../../scripts/deployment/local/materialize-authoritative-settings.py), [`test_integration_readiness.py`](../../../services/core-control-plane/tests/delivery/test_integration_readiness.py) | One shared projection serves both the deployed and local venues and reports distinct A1 send, A1 callback, A2, A4, and A3 rows with their owning runtime. Readiness is configuration completeness only and never a provider health claim. |
| Console one-time delivery diagnostic | implemented | Operator `teams_workflow_diagnostics.py`; Console `settings-teams-workflow.*`; focused Operator and Console tests | An Owner can save and test one public-cloud URL with fixed synthetic content. The binding read returns metadata only, the Console never receives or prefills the URL, and saving or testing does not activate runtime delivery. |
| Channel capability-state, presentation, and shadow-delivery contracts | implemented | [`capability.py`](../../../services/core-control-plane/src/fdai/shared/providers/notifications/capability.py), [`presentation.py`](../../../services/core-control-plane/src/fdai/shared/providers/notifications/presentation.py), [`shadow.py`](../../../services/core-control-plane/src/fdai/core/notifications/shadow.py), [`test_channel_foundation.py`](../../../services/core-control-plane/tests/notifications/test_channel_foundation.py) | `ChannelCapabilityState` separates available/enabled/configured/mode; `render_presentation` fails closed on unsupported interactive content, oversized fields, and secret-like text; `ShadowNotificationChannel` renders and durably records without any network call and satisfies `NotificationRouter` dispatch unchanged. Focused tests cover unavailable, shadowed, rejected, fallback, and repeated-dispatch idempotency outcomes. |
| Teams and Slack provider shadow delivery | implemented | [`teams.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/teams.py), [`slack.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/slack.py), [`shadow_recorder.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/shadow_recorder.py), [`notification_registry.py`](../../../services/core-control-plane/src/fdai/runtime/notification_registry.py), [`test_provider_shadow_delivery.py`](../../../services/core-control-plane/tests/notifications/test_provider_shadow_delivery.py) | Explicit shadow bindings run the same bounded provider renderer as enforce mode, persist the exact provider JSON through the runtime StateStore, and require no endpoint or HTTP client. Teams emits an accessible, severity-aware Adaptive Card; Slack emits a severity-colored Block Kit attachment with no interactive action block. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-08-26 | not-started | Adopted this owner document and ledger after review found that outbound routing stops at the first successful channel, so a deployment with several enabled channels reaches only one. Recorded the fan-out target-set model, per-channel durable delivery, partial-success outcome, and the Teams Workflows webhook binding that replaces the retired Office 365 connector transport. Earlier provenance was not reconstructed. | `current change`; [`router.py`](../../../services/core-control-plane/src/fdai/core/notifications/router.py) first-success return; [`test_router.py`](../../../services/core-control-plane/tests/notifications/test_router.py) fallback assertions; [`notification_delivery.py`](../../../services/core-control-plane/src/fdai/core/incident/notification_delivery.py) audit-id-only claims; [`teams.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/teams.py) connector-shaped payload | Implement the seven sequencing steps in the owner document, starting with the explicit fan-out route schema. |
| 2026-08-27 | implemented | Added explicit fan-out routes, named fail-closed bindings, frozen per-channel delivery state, bounded concurrent dispatch and retries, Teams Workflows transport, and authenticated independent publication receipts. Preserved legacy A1/A3 failover and made incident replay wait for terminal channel state. | `current change`; notification, incident checkpoint, and runtime settings tests passed 162 cases; Ruff passed the task-owned Python files; strict mypy passed the 14 changed notification source files. | Capture governed Teams and PostgreSQL runtime receipts before promoting this scope from `implemented` to `validated`. |
| 2026-08-27 | implemented | Added a bounded Console diagnostic for a transient Teams Workflows URL without storing or returning the signed URL and without changing the deployment binding. | `current change`; focused Operator checks passed 77 cases with one optional PDF skip; Console settings checks passed 14 cases; scoped typecheck, Vite build, entry-bundle check, and three-viewport Playwright validation passed. | Capture the governed live test receipt and retain the existing authenticated publication callback evidence before claiming validation. |
| 2026-09-04 | implemented | Corrected the prior `Delivery effect verification` claim: the receipt code existed but had no ingress and no composition, so no receipt could ever be applied. Added the shared `notification-delivery-receipt` contract and schema, an authenticated bounded Operator ingress with a durable idempotent record, a schema-validated broker handoff on `fdai.notifications.delivery-receipts`, and a Core consumer that applies the transition. | `current change`; [`test_notification_receipt.py`](../../../packages/service-contracts/tests/test_notification_receipt.py) 8 cases; [`test_notification_receipt_ingress.py`](../../../services/operator-service/tests/test_notification_receipt_ingress.py) 12 cases; [`test_notification_receipt_consumer.py`](../../../services/core-control-plane/tests/runtime/test_notification_receipt_consumer.py) 5 cases; [`test_workflow_receipts.py`](../../../services/core-control-plane/tests/notifications/test_workflow_receipts.py) 6 cases. | Capture a governed runtime receipt from a real Teams Workflow through the deployed ingress, broker, and PostgreSQL child before promoting to `validated`. |
| 2026-09-04 | implemented | Made A2/A4 activation explicit and separate from saving, added the deployed Terraform enable plus binding inputs with a read-only Key Vault secret reference, added the local delivery-side encrypted binding store, and made an unconfigured placeholder endpoint fail startup. | `current change`; [`test_local_binding.py`](../../../services/core-control-plane/tests/notifications/test_local_binding.py) 7 cases; [`test_bindings.py`](../../../services/core-control-plane/tests/notifications/test_bindings.py) 10 cases; [`test_teams_notification_activation.py`](../../../tests/integration/infra/test_teams_notification_activation.py) 9 cases; `terraform validate` passed for the platform, core-control-plane, and operator-service roots. | Record a deployed activation receipt that proves a real saved endpoint reached a Teams channel. |
| 2026-09-04 | implemented | Removed the saved endpoint from the Operator binding read response and the Console entirely, replacing it with `visible`, `configured`, `binding_version`, an observation time, and a `saved_at` value only when a durable Operator save record proves that version. | `current change`; [`test_teams_workflow_diagnostics.py`](../../../services/operator-service/tests/test_teams_workflow_diagnostics.py); [`test_operator_iam_family.py`](../../../services/operator-service/tests/test_operator_iam_family.py); Console `settings-teams-workflow.test.ts` 15 cases; Console typecheck and build passed. | None for this scope; an Owner replaces a binding by submitting a new URL. |
| 2026-09-09 | implemented | Added the channel capability-state contract (`available`/`enabled`/`configured`/`mode`), the pre-render fail-closed presentation boundary (unsupported interactive content, bounded field limits, secret-like text), and `ShadowNotificationChannel`, a `NotificationChannel` that renders and durably records a message with no network transport for a binding still in `ChannelMode.SHADOW`. `NotificationRouter`, fan-out delivery, and the one-audit-entry invariant are unchanged; the shadow channel is registered like any other adapter at the composition root. A same-day critique pass hardened case-insensitive interactive-key matching, bounded and scanned metadata keys (not only values), an https-only link scheme rejecting executable links, a deterministic idempotent shadow `record_id` derived from `channel_id + correlation_id + audit_id + category` (satisfying the pre-existing "Adapters MUST implement idempotent send" contract), a timezone-aware shadow clock check, a non-empty `channel_id` check on `ChannelCapabilityState`, and an immutable (`MappingProxyType`) presentation-envelope metadata mapping. | `current change`; [`test_channel_foundation.py`](../../../services/core-control-plane/tests/notifications/test_channel_foundation.py) 31 cases covering capability-state, presentation rejection (including the new scheme/key-case/key-bound cases), shadow recording and idempotency, unavailable/shadowed/rejected/fallback router outcomes, and repeated fan-out dispatch idempotency; full `services/core-control-plane/tests/notifications` suite passed 178 cases; task-owned Ruff format/lint passed; strict mypy passed the 5 changed notification source files (tests are outside the project's `[tool.mypy] files` scope). | None for this foundation scope; a concrete vendor adapter's own promotion from shadow to enforce is tracked by that adapter's owning row. |
| 2026-09-09 | implemented | Reworked the synthetic secret-like test fixture so the presentation boundary still receives the same prohibited shape while source scanning no longer mistakes the fixture for a credential. | `current change`; [`test_channel_foundation.py`](../../../services/core-control-plane/tests/notifications/test_channel_foundation.py) passed 31 cases; Ruff passed the changed test; the generic API-key source pattern returned no match. | Confirm the pull request's pinned gitleaks job passes before merge. |
| 2026-09-09 | implemented | Bound explicit Teams and Slack shadow modes to the shared composition path. Both modes use the same pure provider renderer; shadow persists the immutable provider payload through the runtime StateStore without resolving an endpoint or constructing a transport. Added provider-specific truncation markers, canonical metadata rendering, Slack fact escaping, read-only Slack links, and per-binding readiness rows. | `current change`; focused adapter, binding, readiness, and shadow tests passed 69 cases; Ruff passed the changed Python slice; strict mypy passed 12 changed source files. | Keep both providers in shadow until separately reviewed promotion evidence authorizes enforce mode. |
| 2026-09-09 | implemented | Hardened both shadow recorders to reject one stable record id carrying different bounded content instead of silently preserving a conflicting first write. | `current change`; focused provider-shadow conflict tests. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Rejected duplicate keys at every depth of the notification binding JSON so a later authority or endpoint field cannot silently replace the reviewed value. | `current change`; focused binding parser regression test. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Bounded one notification binding map to 64 entries before adapter or readiness construction. | `current change`; focused binding-count regression test. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Split Slack facts into deterministic groups of at most 10 so the shared metadata ceiling cannot produce an invalid Block Kit section. | `current change`; focused Slack provider-limit regression test. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Split definitive Slack connection failures from acknowledgement ambiguity so a lost response cannot trigger unsafe fallback redelivery. | `current change`; focused Slack transport classification tests. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Lowered a Slack webhook HTTP 200 from `delivered` to `accepted`; provider acknowledgement no longer substitutes for independent publication observation. | `current change`; focused Slack receipt regression test. | Keep Slack in shadow until an independent publication receipt path and promotion evidence are reviewed. |
| 2026-09-09 | implemented | Removed untrusted Teams and Slack response bodies from delivery exceptions so reflected message content cannot reach router audit text. | `current change`; focused provider-rejection redaction tests. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Added a provider-independent 64 KiB shadow payload ceiling so an injected renderer cannot persist an unbounded record. | `current change`; focused oversized-renderer rejection test. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Applied one 1-128 character ASCII channel-id contract across binding, capability, shadow, Teams, and Slack boundaries. | `current change`; focused invalid-channel-id cases. | Continue the bounded channel-adapter critique campaign. |
| 2026-09-09 | implemented | Completed 12 channel-adapter critique rounds. Nine material fixes now reject conflicting shadow records, duplicate binding keys, oversized binding maps, invalid Slack field groups, ambiguous Slack acknowledgements, unsupported delivery-success claims, reflected provider error bodies, oversized injected renderer payloads, and invalid channel ids. Three additional hypotheses were rejected after tracing the trusted channel timestamp path, backward-compatible explicit mode contract, and provider byte ceilings. No finding above Low remains in this bounded slice. | `current change`; full notification and readiness slice passed 208 cases; Teams/Slack static mock parity passed 21 cases; strict mypy passed 24 source files. | Keep Teams and Slack in shadow until each provider's separately reviewed promotion and governed live receipt evidence exists. |
| 2026-09-17 | implemented | Applied the provider-native rich card hierarchy from the adjacent PyHookKit reference without importing its provider-specific domain model. Teams now includes accessibility fallback text, a semantic severity label, and stronger title/body hierarchy. Slack now places the existing read-only Block Kit content in a severity-colored attachment. Shadow and enforce continue to use the same pure renderers. | `current change`; [`teams.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/teams.py), [`slack.py`](../../../services/core-control-plane/src/fdai/delivery/notifications/slack.py), [`test_provider_shadow_delivery.py`](../../../services/core-control-plane/tests/notifications/test_provider_shadow_delivery.py), and [`test_adapters.py`](../../../services/core-control-plane/tests/notifications/test_adapters.py); focused renderer tests passed 20 cases and adapter tests passed 42 cases. | Keep Teams and Slack in shadow until each provider's separately reviewed promotion and governed live receipt evidence exists. |

### Remaining work

- [x] Implement the bounded source behavior in the seven sequencing steps and retain focused
  evidence in `test_matrix.py`, `test_bindings.py`, `test_fanout_delivery.py`,
  `test_adapters.py`, and `test_workflow_receipts.py`.
- [x] Connect the publication receipt handler to a real call site: an authenticated Operator
  ingress, a schema-validated broker handoff, and a Core consumer that applies the delivery
  transition, proven by `test_notification_receipt_ingress.py` and
  `test_notification_receipt_consumer.py`.
- [ ] Capture a governed runtime receipt that proves a real Teams Workflow accepted a card, posted
  it, called the deployed ingress with a valid signature, and converged the PostgreSQL child to
  `delivered`. Only live infrastructure can produce this receipt; the in-process path is covered.
- [ ] Capture a deployed activation receipt that proves `enable_teams_notification_delivery` plus a
  saved Key Vault endpoint reaches a Teams channel, and that the seeded placeholder does not.
- [x] Add and document the shared capability-state, presentation, and shadow-delivery contracts,
  and cover unavailable/shadowed/rejected/fallback outcomes plus router idempotency with focused
  tests, proven by `test_channel_foundation.py`.
- [x] Bind Teams and Slack provider renderers to explicit shadow-mode configuration, persist the
  exact provider payload without network transport, and expose per-binding readiness without
  endpoint values.
