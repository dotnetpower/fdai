# Console Operations implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Migrated implementation notes

> **Implementation status:** The Operations navigation, incidents, approvals, processes, scheduler
> runs, provisioning, onboarding, and bounded investigations are shipped as separate domain views.
> Console action dispatch persists payload-bearing receipts before broker publication and recovers
> pending delivery after restart. Workflow approvals enforce their durable role and distinct quorum
> at both callback and conversation-tool boundaries. Both boundaries normalize principal identities
> before enforcing no-self-approval. Pending access-grant review enforces App Role,
> no-self-approval, expiry, quorum, and exact revision without applying permission. The federated Tasks view, cross-domain
> projection metadata, and hardening of the remaining domain routes are proposed.

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Approval projection unavailable state | implemented | `console/src/routes/hil-queue.tsx`; `console/src/routes/hil-queue.test.ts`; `console/tests/live-e2e/console-routes.spec.ts`; focused Vitest (`5 passed`) and live Playwright (`1 passed`) checks | The Approvals route presents optional projection absence as a neutral unavailable state. Unexpected failures remain errors, and the browser gains no approval or execution authority. |
| Onboarding readiness unavailable state | implemented | `console/src/routes/onboarding.tsx`; `console/src/routes/onboarding.test.ts`; `console/tests/live-e2e/console-routes.spec.ts`; focused Vitest (`6 passed`) and live Playwright (`1 passed`) checks | The Onboarding route presents optional probe endpoint absence as unavailable while preserving unexpected failures as errors. |
| Command Deck live assurance timeout budget | implemented | `console/tests/live-e2e/console-routes.spec.ts`; focused Playwright test discovery (`2 tests`) | Per-test budgets exceed the existing server-answer assertion budgets, so the global default cannot preempt the intended live checks. This does not weaken answer, grounding, or verification assertions. |
| Architecture relationship and dense-map rendering | implemented | `console/src/components/architecture-map.model.ts`; `console/src/components/architecture-map-renderer.ts`; architecture inspector, relation-index, and map tests; focused Vitest (`54 passed`) and live `/architecture` Playwright (`1 passed`) checks | The view recognizes authoritative `peered_with` relationships. Dense maps retain reflections through a bounded 48-node pass that prioritizes selected and highlighted resources, preventing the route fallback from blocking the operator view. No governed runtime or operational receipt is retained, so the evidence supports `implemented`, not `validated`. |
| Authenticated semantic receipt evidence harness | validated | `console/tests/live-e2e/browser-entra-state.ts`; `console/tests/live-e2e/console-routes.spec.ts`; `console/tests/live-e2e/ontology-query-assurance*.ts`; `.fdai/live-validation/ontology-query-assurance-cohort-c16eb06755c44fc155773f1a5a85a0c23eb930a8/` | The harness restores an existing Browser Entra MSAL session into `sessionStorage` before first navigation, consumes the bootstrap once, judges the semantic receipt before opening success-only details, and serializes the seeded cohort with a 15-second request interval. It retries an allowlisted receipt-less transport interruption once after 60 seconds and retains every attempt in the governed artifact. `FDAI_E2E_ASSURANCE_QUESTION_IDS` selects exact generated question ids for a bounded diagnostic probe; a subset may pass but can never set `production_ready`. Receipt-bearing, malformed semantic, and non-transport outcomes remain fail-closed and are never retried. Principal and App Role validation remain with the Operator API. The retained full cohort passed all 100 authenticated questions with complete locale and operation coverage, no retry exhaustion, and `production_ready=true`. |
| Incident roster filter presentation | implemented | Issue #149; `console/src/routes/incident-clarity.css`; authenticated standard-port desktop and 390 px Browser checks; `npm --prefix console run build` | Owning vertical and Severity use the compact Calm Slate form treatment, align with the search controls, retain keyboard focus visibility, and wrap without overlap on narrow screens. |
| Console human-authority derivation | implemented | `handover-editor.tsx`; `handover-editor.test.ts`; focused Console tests (`26 passed`) and typecheck | FDAI App Roles are defined on the API application, so the console SPA id token carries no `roles` claim. Authority-bearing affordances derive from the server-owned `/iam` capability projection, keep the id-token claim only as a fallback, render a distinct resolving state, and fail closed when the projection is unavailable. The server remains the only enforcement point. |
| Ontology instance workspace presentation | implemented | `ontology-instance-graph.tsx`; `ontology-instances-inspector.tsx`; `ontology-instances.tsx`; focused route tests, typecheck, and production build | Graph-first controls, selection and legend overlays, and Inspector collapse state remain read-only presentation and add no graph or execution authority. |
| Principal-scoped Process controls | implemented | `runtime_projection_reader.py`; `process_transition_projection.py`; `process_approval_projection.py`; `process_retry_admission.py`; `family_adapters.py`; `processes.control.ts`; `process-control-panel.tsx`; `processes.transitions.ts`; focused backend and Console checks | Process detail joins the requester-scoped snapshot, journal, pinned Workflow catalog revision, and durable Var approval state. The Console renders requirements and only revision-bound requests. Stale, unavailable, role-invalid, self-approval, timeout, and invalid states fail closed, while `202` remains request acceptance rather than success. |
| Settings and component presentation system | implemented | `ui/calm-slate-{tokens,primitives}.css`; `console/src/routes/settings*`; `mocks/ui/components.html`; `mocks/ui/assets/component-registry.json`; focused Console and mock contracts | Settings and design mocks share compact desktop and touch control roles, explicit preference and policy states, responsive tables, and a registry-backed component catalog. Presentation changes no persistence, authorization, evidence, or execution authority. |
| Scale-aware resource Dashboard mock | implemented | `mocks/ui/dashboard.html`; `mocks/ui/assets/dashboard-resource-data.js`; `dashboard-resource-views.js`; `dashboard-resource-preview.js`; `dashboard-resources.js`; `console/tests/e2e/dashboard-dense-mock.spec.ts` (`5 passed`) and the three other Dashboard mock specs (`11 passed`) | Large examples now default to dense individual cells, bounded by available width and 14 rows (maximum 476). Comfortable/list views retain 48 records and groups retain six. The 1440x900 shell measured 420 cells per page and 270 fully visible before scrolling. Pointer/focus previews, non-jumping pinned inspection, touch targets, snapshot continuity, read states, and responsive geometry passed. No production capacity claim. |
| Resource-first native Console presentation | implemented | `console/src/routes/dashboard-v2.tsx`; `dashboard-v2.model.test.ts`; `dashboard-v2-map.tsx`; `dashboard-v2-preview.tsx`; `console/src/components/searchable-select.tsx`; `console/tests/e2e/dashboard-v2.spec.ts` | Additive `/dashboard-v2` over the authenticated stored inventory projection; `/overview` remains unchanged. No mock fixtures enter the route. Focused semantics/navigation checks passed 55 tests, nine isolated browser scenarios passed, and typecheck/build passed with unchanged entry budgets. Live authenticated evidence remains open. |
| Recorded Resource state query adoption | implemented | [Recorded-state implementation ledger](recorded-resource-state.md) | Dashboard now uses the common ontology instance state API, with bounded pages, same-generation counts and explicit independent state facts. Query completion does not prove tenant-wide inventory or current health. |
| Resource-type autocomplete mock | implemented | `mocks/ui/assets/dashboard-type-picker.js`; `dashboard-resource-data.js`; `console/tests/e2e/dashboard-type-mock.spec.ts` (`5 passed`) | A 35-type synthetic catalog supports label, explicit alias, and Azure-native type searches with at most 12 suggestions. Selection is distinct from draft text. Counts use received scope before type/state filters, including explicit zero observations. Keyboard, IME, touch, cancellation, and viewport-clamped popup behavior are covered. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-05 | implemented | Replaced Dashboard's graph-status consumption with the common recorded Resource state API and aligned the Ontology inspector and graph labels. Operational, provisioning and availability facts retain their own values and evidence rather than sharing one status fallback. | `current change`; [recorded-state ledger](recorded-resource-state.md) contains focused API, loader and native UI validation. | Authenticated screen evidence and provider-level completeness remain separate from this stored-state query. |
| 2026-09-05 | implemented | Excluded `authorization.role-assignment` from every Dashboard operational resource surface using the Ontology presentation policy. Corrected the local inventory projection producer to retain promoted snapshot identity, source, lowercase observation kind, age, pending changes, and newer-failure evidence; v2 accepts the canonical lowercase kind and explains unknown-state reasons. | `current change`; `resource-presentation.ts`, Dashboard/ontology models, `refresh-authoritative-inventory.py`, focused Console tests (`71 passed`), producer tests (`5 passed`), and two focused native browser scenarios. Read-only local inspection identified missing provenance despite real status values; snapshot-only local rematerialization used no Azure calls. | States absent from source data remain unknown. Independent availability and authenticated browser validation remain separate evidence requirements. |
| 2026-09-05 | implemented | Resumed the native Dashboard v2 checkpoint, verified bounded resource rendering, truthful source-state handling, summary drill-down, type selection and dismissal, non-jumping inspection, Korean/touch layouts, and original Dashboard navigation. Kept one active resource preview and loaded shared tooltip positioning on demand rather than increasing the entry budget. | `current change`; 55 focused model/navigation checks, 7 shared-tooltip checks, 9 isolated native-route E2E scenarios, typecheck and build passed; entry JS 464923 raw / 143263 gzip, 57 lazy imports. | The standard local route was opened but required Entra sign-in; no authenticated resource-data validation is claimed. Complete the separate full-scope read contract and runtime evidence before claiming inventory-wide readiness. |
| 2026-09-05 | in-progress | Checkpointed the additive native Dashboard v2 route, bounded map, searchable type selector, and inventory projection decoder before an operator-requested pause. | `current change`; task-owned Console source and catalogs. No production readiness or passing native-route visual receipt is claimed. | Resume only after the operator confirms continuation; finish source-semantics review, focused native-route tests, build, and desktop/mobile verification. |
| 2026-09-05 | implemented | Replaced the Dashboard Type select with an accessible autocomplete while retaining the canonical applied filter until an explicit selection or clear. Reused mock combobox presentation with a dedicated committed-value and IME controller; catalog-only types never fabricate observed resources. | `current change`; `console/tests/e2e/dashboard-type-mock.spec.ts` passed five scenarios; the 16 existing Dashboard scenarios passed. | Production adoption must use an authoritative versioned type catalog and server-scoped counts; no provider or production Console integration was added. |
| 2026-09-01 | implemented | Moved the settings and component presentation design boundary from the oversized module map to its focused Console operations owner without changing runtime behavior. | `current change`; paired roadmap docs; document-size, implementation-tracking, translation, and punctuation checks. | Retain an authenticated visual receipt for all settings routes after the standard local Operator API stack is healthy. |
| 2026-08-31 | implemented | Added authoritative principal-scoped Process step state and guarded transition requests without moving decision or execution authority into Operator Service or Console. | `current change`; 67 focused backend and runtime checks, 27 focused Console checks, Ruff, strict mypy, Console typecheck and production build, catalog and localization gates, and synthetic desktop, constrained, and mobile Playwright checks passed. | Retain a governed authenticated runtime receipt from accepted proposal through authoritative Process state change; acceptance alone is not that evidence. |
| 2026-08-18 | implemented | Stopped reporting a control-gap count while a gap source was unavailable. The Overview Control assurance card summed four sources and contributed `0` for each missing one, so it rendered `0 gaps` beside `Promotion readiness: Evidence unavailable` and `Policy escapes: Evidence unavailable`. A counted gap is still reported because it is a true lower bound; only a zero now requires that every source was measured. | `current change`; `dashboard.model.test.ts` 15 passed including four new cases, mutation-verified by restoring the measured flag; full Console suite `1878 passed` across 222 files; Console typecheck passed. Measured on the local console: the card showed `Incomplete / 0 gaps` with three of its four sources unavailable. | Wiring the promotion-gate and policy-escape sources so they can be measured at all remains separate work. |
| 2026-08-18 | implemented | Stopped reading a completed compliant evaluation as a blocked issue. T0 records `no_rule_denied` only when every candidate rule ran and none denied, but records written before the `control_loop.compliant` action kind existed carry that reason under an abstain kind. The audit log is hash-chained and cannot be rewritten, so the incident milestone and blocking-reason readers now classify by the recorded reason. | `current change`; `incidents.milestones.test.ts` and `incidents.overview.test.ts` passed 14 focused cases including two new ones, full Console suite `1872 passed` across 222 files, and Console typecheck passed. Measured scope: the local audit store holds 819 such records, all written before 2026-08-18T04:05Z; every later compliant decision uses `control_loop.compliant`. | None for this reader correction; rule-catalog coverage for genuinely uncovered resource types stays open on #166. |
| 2026-08-18 | implemented | Derived Agent oversight knowledge-handover authority from the server-owned `/iam` capability projection instead of the console SPA id-token `roles` claim, which the API-scoped App Role model never populates. The gate keeps the id-token claim as a fallback, shows a resolving state while the projection loads, and fails closed when it is unavailable. | `current change`; `handover-editor.test.ts` and `handover.test.ts` passed 20 focused cases, `index.test.ts` catalog parity passed 6 cases, and Console typecheck passed. | Retain a governed Browser Entra artifact showing an authorized operator reaching the ownership-proposal form. |
| 2026-08-18 | validated | Audited every registered Console route against the local Operator API and repaired the stale deck transcript padding assertion that commit `bb56775e8` had left behind. | Current change; console `src/deck/investigation-timeline.test.ts` 11 passed; an authenticated pass over all 44 registered routes produced no `404`, no page exception, and no unhandled transport code, with `/agent-activity`, `/llm-cost`, `/architecture`, and `/rules` rendering measured content. | Surfaces whose evidence is produced at runtime still render a declared unavailable reason; `promotion-gate.list` still has a reader and no writer. |
| 2026-08-17 | implemented | Separated a conclusive deterministic pass from an abstention. T0 records `control_loop.compliant` when every candidate rule evaluated and none denied, the control loop no longer runs RCA or escalates that event to the adaptive tiers, and the incident view renders it as a success milestone instead of a blocking issue. | Current change; `test_control_loop_e2e.py` 47 passed and `t0_deterministic` suite green (109 total), both mutation-verified; `incidents.milestones.test.ts` 5 passed; measured before the change: 795 of 1641 abstentions carried `no_rule_denied` across 441 incident correlations. | Historical audit rows keep the previous `control_loop.abstain` kind because the log is append-only, so mixed history is expected until the retention window rolls. |
| 2026-08-13 | implemented | Adopted the implementation ledger without reconstructing earlier provenance and aligned the Approvals route with the typed unavailable-source boundary. | `current change`; task-owned source and tests in the scope table; `vitest run src/routes/hil-queue.test.ts` passed 5 tests and the focused live `/approvals` Playwright check passed. | Complete and record the projection, request, interaction, and measurement exit evidence below. |
| 2026-08-13 | implemented | Aligned Onboarding with the typed unavailable-source boundary and restored explicit outer budgets for the two Core-dependent Command Deck live checks. | `current change`; task-owned source and tests in the scope table; `vitest run src/routes/onboarding.test.ts` passed 6 tests, the focused live `/onboarding` Playwright check passed, and Playwright discovered both Command Deck checks. | Recover the Core semantic consumer, execute both Command Deck checks, and complete the remaining exit evidence below. |
| 2026-08-13 | validated | Added symmetric peering presentation and bounded the optional reflection pass for dense Architecture maps while retaining selected and highlighted resources. | `current change`; task-owned source, catalogs, and tests in the scope table; focused architecture Vitest passed 54 tests and the live `/architecture` Playwright check passed in 4.9 seconds. | Continue the full-route assurance campaign and complete the remaining exit evidence below. |
| 2026-08-13 | in-progress | Added one-time Browser Entra `sessionStorage` restoration to the authenticated governed-receipt and seeded bilingual assurance runners. | `current change`; focused Console typecheck passed and the design-route gate accepted this owning document. | Run both authenticated browser paths and link the two passing retained evidence records before claiming readiness. |
| 2026-08-13 | implemented | Corrected the Architecture-map classification because focused browser execution was not retained as governed runtime or operational evidence. | `current change`; Architecture source and focused checks listed in the scope table; repository evidence records contain no retained receipt for this run. | Retain a governed runtime or operational receipt before restoring `validated`. |
| 2026-08-13 | implemented | Made authenticated receipt failures visible before success-only detail expansion and serialized the seeded bilingual assurance cohort with a fixed 15-second interval to avoid test-generated provider concurrency. | `current change`; `console/tests/live-e2e/console-routes.spec.ts`; `console/tests/live-e2e/ontology-query-assurance.spec.ts`; focused Console typecheck and diff check passed. | Run both authenticated browser paths and retain their passing evidence records before claiming readiness. |
| 2026-08-13 | implemented | Preserved transport source metadata and added one bounded retry for allowlisted receipt-less interruptions without changing the fail-closed semantic receipt oracle. The governed artifact records the retry policy, each attempt, actual protected request count, and exhausted retry count. | `current change`; `console/tests/live-e2e/ontology-query-assurance*.ts`; focused ontology assurance Vitest passed 31 tests and Playwright discovered both configured live projects without executing external requests. | Obtain the exact centralized validation receipt, then retain one passing authenticated bilingual 100-question artifact with no exhausted transport retries. |
| 2026-08-13 | implemented | Added deterministic exact-id selection for bounded authenticated diagnostic probes while keeping full-cohort coverage mandatory for `production_ready`. Unknown, duplicate, and empty question ids fail before a live request. | `current change`; `console/tests/live-e2e/ontology-query-assurance*.ts`; focused ontology assurance Vitest passed 37 tests and Playwright discovered the configured live test without executing external requests. | Obtain the exact centralized validation receipt, rerun the six receipt-less cases as a non-readiness probe, then retain a new full 100-question artifact. |
| 2026-08-13 | validated | Retained a new authenticated bilingual full-cohort artifact after the six previously receipt-less questions passed a bounded diagnostic probe. | Governed schema `1.1.0` artifact under `.fdai/live-validation/ontology-query-assurance-cohort-c16eb06755c44fc155773f1a5a85a0c23eb930a8/`, at exact centrally validated source revision `c16eb06755c44fc155773f1a5a85a0c23eb930a8` and clean workspace patch digest `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; Playwright passed in 1.3 hours; 100 protected requests produced 100 authoritative outcomes with 50 questions per locale and 10 per operation, zero failures, retries, exhausted retries, duplicate request or projection ids, unsupported operational claims, or unauthorized execution claims. | Keep future readiness claims bound to a new exact validated revision and governed full-cohort artifact when the exercised behavior changes. |
| 2026-08-17 | implemented | Replaced browser-default Incident vertical and severity selects with the compact Console form treatment. | Issue #149; `current change`; `console/src/routes/incident-clarity.css`; authenticated desktop computed-style and screenshot checks, a 390 px no-overlap check, and `npm --prefix console run build` passed. | No remaining work for the Incident filter presentation. |
| 2026-08-24 | implemented | Restored the graph-first Ontology instance workspace and Inspector-owned responsive controls while preserving the existing read-only API and authority boundary. | `current change`; focused route tests passed 6 cases; Console typecheck and production build passed. | No remaining work for this bounded presentation slice. |
| 2026-09-05 | implemented | Added a scale-aware resource Dashboard study with bounded active-view DOM, subscription/group drill-down, persistent selected evidence, and explicit read-state specimens. Separated the synthetic data/query adapter from the renderer and controller, and preserved the independent historical performance example. | `current change`; `npm --prefix console run test:e2e:quick -- tests/e2e/dashboard-mock.spec.ts tests/e2e/dashboard-resources-mock.spec.ts tests/e2e/dashboard-scale-mock.spec.ts` passed 11 scenarios. | Replace the local generator and in-memory aggregation before production adoption; retain exact authenticated and measured server evidence. |
| 2026-09-05 | implemented | Superseded the group-first default and blanket 48-resource limit with a dense honeycomb, explicit density choice, hoverable same-snapshot previews, roving keyboard focus, and a nonmodal pinned Inspector that preserves map position. Comfortable/list/group bounds remain unchanged. | `current change`; `console/tests/e2e/dashboard-dense-mock.spec.ts` passed five scenarios, including real pointer targets, keyboard-triggered scrolling, Escape dismissal, no scroll/focus jump, 993x641 and 390x844 geometry, and wide coarse-pointer tap input. The adapted baseline/scale tests passed 11 scenarios. | These are synthetic presentation checks. Production still needs server-scoped aggregation, snapshot-bound pages, localization, and governed runtime evidence. |

### Remaining work

- [ ] Retain a governed Browser Entra artifact showing an assigned Contributor, Approver, or Owner reaching the knowledge-handover ownership-proposal form, and a Reader still seeing the locked panel.
- [ ] Record the deterministic rebuild, digest-change, and cache-loss drill evidence defined by the Phase 1 exit criteria.
- [ ] Pass the failure-injection and authorization-boundary matrix defined by the Phase 2 exit criteria for every shipped request route.
- [ ] Record keyboard, conflict, retry, compensation, and rollback drill evidence defined by the Phase 3 exit criteria.
- [ ] Freeze reviewed baseline windows and pass the shadow measurement comparison defined by the Phase 4 exit criteria.
- [x] Retain one passing authenticated bilingual 100-question ontology assurance artifact that records all transport attempts and reports zero exhausted transport retries.
- [x] Keep the Incident vertical and severity filters aligned with the compact Console form treatment on desktop and narrow screens.
- [ ] Before inventory-wide Dashboard claims, review a versioned server-scoped read projection with same-snapshot counts, stable cursors, exact filter semantics, and explicit freshness/completeness; test unauthorized scopes and expired snapshots.
- [x] Port the bounded resource Dashboard presentation without synthetic fixtures and add English/Korean catalog strings; native-route isolated browser checks passed nine scenarios.
- [ ] Retain authenticated desktop/mobile and server latency evidence for Dashboard v2 at agreed representative inventory sizes.
