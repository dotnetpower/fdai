# Phase 0 - Instrumentation and Unblocking implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Migrated implementation notes

> **Implementation status**: Telemetry, configuration, and event contracts; PostgreSQL
> migrations; the frozen `v2026.07` scenarios; `tools/reference_agent/`;
> `tools/baseline_run.py`; baseline reports; provider fakes; the pgvector/Redpanda local preset;
> and the exemption schema and template are implemented. Entra app/group provisioning, PR-trailer
> no-self-approval CI, the exemption auto-expiry/digest job, and production validation of all P0
> exit evidence are incomplete. The task tables preserve the original plan and acceptance criteria;
> this callout describes the current repository state.

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| W1 persistence, required CI, and golden telemetry | in-progress | `alembic/versions/20260705_0001_base.py`; `.github/workflows/ci.yml`; `services/core-control-plane/tests/persistence/test_migrations_integration.py`; `services/core-control-plane/tests/telemetry/test_golden_fixture.py` | The base migration, required merge gate, and deterministic golden metrics fixture exist. This classification change did not rerun the database integration or retain applied-environment and live telemetry evidence. |
| W1 service boundaries, contracts, config, telemetry, cache, and dashboard | in-progress | `pyproject.toml`; `packages/service-contracts/contract-generation.json`; `packages/service-contracts/src/fdai_service_contracts/generated/`; `console/src/generated/service-contracts.ts`; `scripts/quality/contracts/generate_service_contracts.py`; `services/core-control-plane/src/fdai/shared/contracts/`; `services/core-control-plane/tests/contracts/`; `services/core-control-plane/src/fdai/shared/config/`; `services/core-control-plane/tests/config/test_yaml_provider.py`; `services/core-control-plane/src/fdai/shared/telemetry/`; `services/core-control-plane/tests/telemetry/test_dashboard_status.py`; `services/core-control-plane/tests/scenarios/test_v2026_07_replay.py`; `alembic/versions/20260831_0089_t2_cache_lifecycle.py`; `services/core-control-plane/src/fdai/delivery/persistence/postgres_t2_cache.py`; `docs/dashboards/phase-0-kpi.json`; `.github/workflows/ci.yml` | Required-CI independent-service enforcement, generated Python and TypeScript contract views, N/N-1 compatibility enforcement, bounded file-backed config, ingest-to-audit correlation, and the PostgreSQL T2 cache lifecycle exist with focused local evidence. The dashboard contract distinguishes source-bound Phase 0 panels from explicitly unavailable later-phase metrics and rejects missing, stale, conflicting, mismatched, future, or synthetic live values. Non-deferred dashboard panels, later-phase live sources, cache latency evidence, and production dashboard evidence remain incomplete. |
| W2 frozen scenario assets and replay | in-progress | `services/core-control-plane/tests/scenarios/schema.json`; `services/core-control-plane/tests/scenarios/cross-objective/schema.json`; `services/core-control-plane/tests/scenarios/test_frozen.py`; `services/core-control-plane/tests/scenarios/test_v2026_07_replay.py`; `services/core-control-plane/tests/scenarios/manifests/v2026.07.json`; `.github/workflows/ci.yml` | Schema, balance, synthetic-scope, replay, service-owned immutable-directory protection, bilingual natural-language checks, customer-data scrubbing, and a frozen cross-objective conflict spec that the manifest inventories by its own id exist. Capability-pack coverage remains incomplete. |
| W3 governed baseline cohort claim | implemented | `config/sre-cohort-claim-policy.json`; `services/core-control-plane/src/fdai/core/measurement/cohort_claim_policy.py`; `packages/service-contracts/src/fdai_service_contracts/baseline_cohort.py`; `services/core-control-plane/src/fdai/core/measurement/baseline_cohort_claim.py`; `tools/cohort_receipt.py`; `tools/baseline_run.py`; focused policy, contract, claim, and runner tests | The claim is measured against a trusted versioned repository policy loaded independently of evidence, which pins every required success metric, every zero-threshold guard, the real frozen scenario-set content digest, and the 30-sample floor, while the trusted caller supplies the expected revision. Every evaluated arm fact is canonically hashed and each admission is bound to that hash, both arms MUST be distinct, and admissions come only from an injected trusted provider or a separately verified proof bundle. The artifact declares neither its origin nor its revision shape: the import origin is an evaluator parameter from the trusted importer channel, a governed external claim also needs a trusted cohort-level admission over the complete receipt digest, and every pinned revision MUST be an immutable full 40- or 64-hex commit digest. The committed synthetic-harness baseline therefore stays ineligible, and the retained non-synthetic cohort itself is still open. |
| W3 synthetic baseline mechanics | in-progress | `tools/reference_agent/`; `tools/baseline_run.py`; `docs/baselines/v2026.07.json`; `docs/baselines/v2026.07.md`; `services/core-control-plane/tests/tools/test_baseline_runner.py` | Deterministic tooling and committed synthetic artifacts exist. The current report does not satisfy every W3 artifact acceptance condition and explicitly remains `synthetic-harness`, incomplete, and not claim eligible. |
| W4 Azure infrastructure and identity blocker | in-progress | `infra/main.tf`; `infra/modules/operator-api/container-app/`; `infra/tests/`; `docs/runbooks/entra-app-registration.md`; `services/operator-service/tests/test_operator_iam_family.py` | Infrastructure and identity configuration surfaces exist. Deny-by-default Azure Policy, the least-privilege probe, approval-bot registration, complete group and Conditional Access bindings, recertification, and governed tenant evidence remain incomplete. |
| W5 exemption lifecycle | in-progress | `services/core-control-plane/src/fdai/rule_catalog/schema/exemption.schema.json`; `services/core-control-plane/src/fdai/rule_catalog/schema/exemption_lifecycle.py`; `services/core-control-plane/src/fdai/delivery/exemption_lifecycle.py`; `rule-catalog/action-types/governance.reapply-rule-assignment.yaml`; `services/core-control-plane/tests/exemption/`; `services/core-control-plane/tests/rbac/test_no_self_approval.py`; `scripts/governance/exemption-expire.py`; `docs/runbooks/exemption-workflow.md` | The scheduled expiry command, exact assignment binding, audited retry/unknown behavior, shadow-first ActionType, and revision/requester digest are implemented without direct provider mutation. The exact PR-trailer identity flow and live scheduler/notifier deployment evidence remain incomplete. |
| W6 provider contracts and local stack | implemented | `services/core-control-plane/tests/providers/{conftest.py,test_contracts.py}`; `PostgresStateStore`; `EventHubsKafkaBus`; `infra/local/docker-compose.yml`; `provider-contracts-docker` CI job | Default pytest stays offline. The same assertions pass against fakes and lifecycle-managed PostgreSQL/Redpanda, including pgvector, audit, duplicate, Kafka order/resume/DLQ, loopback-only access, and exact cleanup. |
| Sequenced task timeline | implemented | `docs/diagrams/fdai-roadmap-phases-phase-0-instrumentation-01.diagram.yaml`; `npm --prefix tools/architecture-diagrams run check` | The maintained bilingual FDAI diagram replaces the former Mermaid rendering without changing task dependencies or implementation authority. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-08-31 | in-progress | Closed the last two ways a cohort artifact could still speak for itself. `artifact_origin` is gone from `BaselineTreatmentCohortReceipt`, and the origin is now an `import_origin` evaluator parameter supplied by the trusted importer channel, defaulting to the fail-closed repository origin. A governed external claim additionally needs a trusted cohort-level `DecisionEvidenceAdmission` bound to the complete cohort `receipt_digest`, on top of both arm admissions, so a relabelled or rehashed artifact rehashes out of its admission and reports `cohort_not_admitted`. The importer refuses any bundle that carries `artifact_origin` at either level. `fdai_revision` is now a `CommitRevision`, the same immutable full 40- or 64-hex commit digest operational promotion requires, in the receipt, in both arms, and in the trusted `CohortClaimRequirement`, and `--cohort-revision` refuses `main`, `HEAD`, a tag, or a `refs/heads/` name before any evidence is read. | `current change`; `packages/service-contracts/src/fdai_service_contracts/baseline_cohort.py`; `services/core-control-plane/src/fdai/core/measurement/{baseline_cohort_claim.py,cohort_claim_policy.py}`; `tools/{cohort_receipt.py,baseline_run.py}`; `uv run pytest -q --no-cov packages/service-contracts/tests services/core-control-plane/tests/core/measurement services/core-control-plane/tests/tools` passed 539 focused cases, with the six `test_operational_promotion.py` failures identical at `2ca190d9e`. | A governed external cohort still needs a real trusted importer channel and a real cohort-level admission provider in deployed runtime. |
| 2026-08-31 | implemented | Replaced the expiry-due audit stub with a typed, replay-stable assignment reapply proposal through the registered action pipeline and added the exact-revision 14-day digest contract. | `current change`; expiry lifecycle schema/delivery, ActionType catalog entry, focused lifecycle and catalog tests. | Retain live scheduler and notifier evidence separately; complete the remaining W5 review-identity evidence. |
| 2026-08-31 | implemented | Replaced the circular all-metrics Phase 0 dependency with a source-bound or explicitly unavailable panel contract. Added deterministic panel resolution that never turns missing, stale, conflicting, mismatched, future, or synthetic live observations into zero or success. | `current change`; dashboard descriptor, telemetry reducer, and focused dashboard descriptor/status/golden-fixture tests. | Bind the named Phase 1 and Phase 2 authoritative sources and retain production dashboard evidence before claiming those metrics are live. |
| 2026-08-31 | implemented | Replaced zero-argument fake-only factories with lifecycle-managed fake, PostgreSQL, and Redpanda fixtures in one contract file. Real mode uses an isolated temporary database, UUID-scoped broker state, bounded consumers, exact cleanup, and a loopback-only network guard; default pytest remains Docker-free. | `current change`; offline matrix 16 passed and 1 real-only skip; real loopback matrix 26 passed; post-run database, topic, and group cleanup checks passed; CI contract and workflow wiring checks. | Keep the explicit Docker job green when provider adapters or local ports change. |
| 2026-08-31 | implemented | Reconciled the L4 cache schema and lifecycle around one Core-owned PostgreSQL writer. Exact catalog-digest partitions replace the default sink; TTL reads use the indexed query; promotion and rollback retain authoritative state; rotation preserves active and rollback partitions, serializes concurrent attempts, returns duplicate receipts, and rolls back DDL and receipt creation together on failure. | `current change`; migration, adapter, service command, 13 focused local PostgreSQL, migration, and CLI checks including indexed hit/miss under a 250 ms statement timeout; 58 service-migration ownership checks. | Use the service command with deployment-owned scheduling and retain operational latency receipts before claiming runtime validation. |
| 2026-08-31 | implemented | Approved and implemented one repository-owned, checksum-pinned generator that projects every distinct manifest-declared N/N-1 JSON Schema into Python types for all five services and TypeScript types for Console. The offline CI drift gate rejects missing, stale, or hand-edited output while the existing compatibility manifest rejects same-major breaking changes and requires a complete producer-consumer matrix. | `current change`; generation policy, generator, Python and TypeScript outputs, CI wiring regression, focused generation tests, service compatibility tests, Python static checks, and Console typecheck. | Regenerate through the pinned command whenever a manifest-owned schema changes. |
| 2026-08-31 | in-progress | Corrected the W3 cohort claim trust boundary after review. The evaluated requirement now comes from the trusted versioned `config/sre-cohort-claim-policy.json`, loaded independently of evidence and floor-checked in code against all five documented success metrics, all four zero-threshold guards, the real content digest of the committed frozen scenario set, complete evidence, and the 30-sample floor, with the expected revision supplied by the trusted caller through `--cohort-revision`. The importer parses the retained receipt only and refuses any bundle that carries its own requirement, admission, verification bundle, or eligibility verdict, so a repository importer without an injected provider is always ineligible. Admissions come only from an injected trusted shared provider or from the existing verifier registry over a separately supplied five-proof bundle, and each one is rechecked against `cohort_arm_fact_digest`, the canonical hash of every evaluated arm fact, so an invented or cross-arm digest admits nothing. Both arms MUST hold distinct report digests and evidence receipts, and the published `artifact_origin` is read back from the validated contract rather than from the presence of a CLI path. | `current change`; `config/sre-cohort-claim-policy.json`; `services/core-control-plane/src/fdai/core/measurement/{cohort_claim_policy.py,baseline_cohort_claim.py}`; `packages/service-contracts/src/fdai_service_contracts/baseline_cohort.py`; `tools/{cohort_receipt.py,baseline_run.py}`; `docs/baselines/v2026.07.json`; `uv run pytest -q --no-cov packages/service-contracts/tests services/core-control-plane/tests/core/measurement services/core-control-plane/tests/tools` passed 510 focused cases, with the six `test_operational_promotion.py` failures identical at `e118a95c9`. | The retained non-synthetic baseline and treatment cohort still needs deployed-runtime evidence and a real trusted admission provider. |
| 2026-08-31 | in-progress | Added the W3 governed cohort claim path. `BaselineTreatmentCohortReceipt` embeds one `DecisionCriticalEvidenceReceipt` per arm and binds the frozen scenario-set version and digest, one pinned `fdai_revision`, each arm's report and provenance digest, its sample count, every success metric as a confidence interval that contains its absolute value, the completeness and provenance references, and every zero-threshold guard. Eligibility exists only as the output of `evaluate_cohort_claim`, which fails closed with named reasons for a missing receipt, synthetic evidence, a rejected claim preflight, evidence without a current independent admission, a digest, revision, or scenario-set mismatch, incomplete metrics, intervals, provenance, or guards, a breached guard, a cohort under 30 samples in either arm, and a repository-origin artifact. `tools/baseline_run.py` gained an optional `--cohort-receipt` preflight and a `--require-claim-eligible` gate that exits 4, while `docs/baselines/v2026.07.json` stays a 12-sample synthetic harness reporting `claim_eligible=false`. | `current change`; `packages/service-contracts/src/fdai_service_contracts/baseline_cohort.py`; `services/core-control-plane/src/fdai/core/measurement/baseline_cohort_claim.py`; `tools/cohort_receipt.py`; `tools/baseline_run.py`; `docs/baselines/v2026.07.json`; `uv run pytest -q --no-cov packages/service-contracts/tests services/core-control-plane/tests/core/measurement services/core-control-plane/tests/tools services/core-control-plane/tests/scenarios` passed 603 focused cases; the six failures in `tests/core/measurement/test_operational_promotion.py` are identical at `c1cb634d9` without this change. | The evaluator is proved only against fixtures, so retaining governed non-synthetic baseline and treatment cohorts with a current independent admission stays open. |
| 2026-08-31 | in-progress | Corrected the full-loop effect evidence recorded below after review. The projection that dropped an observation the contract cannot represent left the verification verdict `verified`, so a real `verify_effect` result whose observation fell inside the prediction window but after `recorded_at` still raised the contract validation error inside dispatch; admissibility now runs before the decision, the shadow effect audit entry, and the projection, and holds that evidence as `observation_not_yet_recorded` while an already-held verdict keeps its own reason. The frozen replay also read wall clock for action creation, dispatch, and recording, so its frozen prediction and observation preceded the action they described; `ControlLoop` and `ActionBuilder` now share one injected clock, the shadow effect entry records the action creation and dispatch window beside the raw value, and the replay asserts the creation, prediction, dispatch, observation, and recording order it claims. | `current change`; `services/core-control-plane/src/fdai/core/mscp_profile/{effect_verification.py,response_outcome.py,shadow_effect.py}`; `services/core-control-plane/src/fdai/core/control_loop/{orchestrator.py,_execution.py}`; `services/core-control-plane/src/fdai/core/executor/action_builder.py`; `services/core-control-plane/tests/scenarios/{test_v2026_07_replay.py,test_frozen.py}`; `uv run pytest -q --no-cov services/core-control-plane/tests/scenarios services/core-control-plane/tests/core/mscp_profile services/core-control-plane/tests/core/executor services/core-control-plane/tests/pipeline` passed. The new frozen `not_yet_recorded` case is mutation-verified: reverting the hold fails it with the contract validation error. | The observation source is still an in-process frozen provider replayed in shadow, so the deployed-runtime observation and the remaining packs stay open. |
| 2026-08-31 | in-progress | Added the SRE `successful_full_loop` asset. The frozen `sre.cluster-diagnostics-missing.001` scenario replays through the real control loop on the shipped `ShadowExecutor`, and the MSCP expected-effect provider and independent effect observer are bound through the shipped seam so the replay may close `executed` / `auto` only after an independent authoritative observation matches the pre-dispatch prediction. The observer sees the action and the prediction and never the executor receipt, the publisher, or the audit store; unbinding it leaves a published shadow PR the loop still refuses to call success. Missing, stale, incomplete, and conflicting effect evidence each end `abstain` with a pinned verification status, reason, and response label, the terminal dispatch, effect-verification, and measurement audit entries share one action and event lineage, and every execution result and published PR stays in shadow. A defect surfaced by the stale case is fixed here: an out-of-window or not-yet-recorded observation raised a contract validation error during dispatch, so the response-outcome projection now drops an observation the contract cannot represent while the shadow effect audit entry keeps the raw value. | `current change`; `services/core-control-plane/src/fdai/core/mscp_profile/response_outcome.py`; `services/core-control-plane/tests/scenarios/enrichment/v2026.07/sre-cluster-diagnostics-missing-001.json`; `services/core-control-plane/tests/scenarios/{test_v2026_07_replay.py,test_frozen.py,manifests/v2026.07.json}`; `uv run pytest -q --no-cov services/core-control-plane/tests/scenarios` passed 137 cases. | The observation source is an in-process frozen provider replayed in shadow, so a deployed-runtime authoritative observation and recurrence closure remain open, as do the four other capability packs. |
| 2026-08-31 | in-progress | Closed the three remaining cross-objective review findings. The free-form ActionType-to-direction map is gone: every option now carries a typed frozen `objective_effects` record that the conflict schema validates and the new `DomainOptionEvidence` contract admits, and the conflict relation is the independent check `conflicting_objective_effects`, which raises arbitration only when two domains hold opposite-signed utilities on the same governed objective id. A negative control that gives both domains the effects one replay actually produced declines instead, so the relation cannot be satisfied by a recommendation label. Each replay's canonical event id, cited rule ids, terminal audit idempotency key, and dry-run receipt digest now travel as option evidence references into the specialist contributions, the arbitration request, the decision case, and the terminal verdict, and the ingress rejects a payload whose only lineage is a synthetic `specialist:*` marker. The unavailable-owner close is automatic: the composition root binds a runtime availability probe through `runtime_health.bind_availability_probe`, and Forseti closes the request it just published as an explicit `hil` verdict with no ActionType, no initiator, and no winning domain, so no test drives that closure by hand. | `current change`; `services/core-control-plane/src/fdai/core/decision_case/domain.py`; `services/core-control-plane/src/fdai/agents/{forseti.py,_framework/forseti_decision_helpers.py,_framework/runtime_health.py,_framework/runtime.py}`; `services/core-control-plane/src/fdai/core/operational_planning/coordinator.py`; `services/core-control-plane/tests/scenarios/cross-objective/{schema.json,v2026.07-sre.json}`; `services/core-control-plane/tests/scenarios/test_v2026_07_replay.py`; `uv run pytest -q --no-cov services/core-control-plane/tests/scenarios services/core-control-plane/tests/agents/test_arbitration.py services/core-control-plane/tests/core/decision_case services/core-control-plane/tests/core/operational_planning services/core-control-plane/tests/core/operational_context` passed 396 focused cases. | The conflict evidence is still a shadow replay over the in-memory bus, so the deployed-runtime arbitration receipt stays open, and no shipped rule catalog emits signed objective effects yet, so the frozen enrichment still supplies them. |
| 2026-08-31 | in-progress | Corrected the cross-objective conflict evidence recorded below after review. Arbitration no longer receives hand-authored advice or a synthetic target: each option's recommendation is derived from the ActionType its own replay produced, and the contested target is derived from the frozen ontology graph as the one resource both replayed targets reach. Forseti is bound to a materialized operational context admitted through the shared decision-evidence contract, so the arbitration request and the terminal verdict both carry the canonical decision case, its context snapshot identity, and its per-domain evidence references, and Saga retains the terminal verdict in the append-only audit chain. Missing arbitration authority no longer ends without a terminal result: it closes through the shipped `conflicts_require_hil` degradation policy and Forseti's existing no-rule-match judgment as a shadow-only `hil` verdict that carries no ActionType, no initiator, and no action authority, with retained audit evidence and no newly invented authority. The frozen conflict artifact is now inventoried directly by its own id through the manifest `conflict_spec_ids` field, and the frozen schema, GUID, customer-data, ASCII, and duplicate-key guards now cover it. | `current change`; `services/core-control-plane/tests/scenarios/cross-objective/{schema.json,v2026.07-sre.json}`; `services/core-control-plane/tests/scenarios/{test_v2026_07_replay.py,test_frozen.py,manifest.schema.json,manifests/v2026.07.json}`; `uv run pytest -q --no-cov services/core-control-plane/tests/scenarios services/core-control-plane/tests/agents/test_arbitration.py services/core-control-plane/tests/core/operational_planning services/core-control-plane/tests/core/operational_context services/core-control-plane/tests/core/decision_case services/core-control-plane/tests/core/risk_gate/test_precedence.py` passed 404 focused cases. | The evidence is still a shadow replay over the in-memory bus, so a deployed-runtime arbitration receipt on the production bus remains open, as do SRE `successful_full_loop` and the four remaining packs. |
| 2026-08-31 | in-progress | Added the W2 cross-objective conflict asset. A frozen customer-agnostic conflict spec and its Draft 2020-12 schema compose three options that each replay a frozen v2026.07 scenario through the real control loop, so eligibility is a runtime property. The conflict crosses the shipped governed boundary, Odin remains the sole arbitration owner, an unresolved highest-precedence objective produces an `arbitration_unresolved` HIL verdict instead of a local tie-break, and removing the owner leaves the conflict held. Deterministic replay reproduces the pinned decision and evidence digests. | `current change`; `services/core-control-plane/tests/scenarios/cross-objective/{schema.json,v2026.07-sre.json}`; `services/core-control-plane/tests/scenarios/test_v2026_07_replay.py`; `manifests/v2026.07.json`; `uv run pytest -q --no-cov services/core-control-plane/tests/scenarios services/core-control-plane/tests/agents/test_arbitration.py services/core-control-plane/tests/core/risk_gate/test_precedence.py` passed 209 focused cases. | Five of the six dimensions in the `sre` pack now have executable evidence; SRE `successful_full_loop` and the four remaining packs are still open, and the conflict evidence is a shadow replay over the in-memory bus. |
| 2026-08-29 | implemented | Added an end-to-end correlation regression using a frozen shipped scenario, the real control loop, OPA evaluation, and the risk gate. Ingest, route, verify, gate, audit stage events, and durable audit rows all retain the same explicit correlation id. | `current change`; focused frozen-scenario replay and telemetry correlation tests. | Complete the remaining W1 contract, cache, and dashboard exits below. |
| 2026-08-29 | implemented | Extended frozen-scenario scrubbing beyond UUIDs. Recursive key-and-value checks now reject non-placeholder tenant and subscription ids; non-synthetic Azure resource-group and resource names; non-example URI authorities and email domains; non-loopback IPv4 and IPv6 addresses; and populated credential, password, secret, API/access key, token, private-key, connection-string, or SAS fields across snake, kebab, and camel key forms. Documented example, placeholder, redacted, and loopback values remain allowed. | `current change`; focused adversarial scrub cases and full frozen-set tests. | Complete all six dimensions in every capability pack. |
| 2026-08-29 | implemented | Added a bounded `YamlFileConfigProvider` that opens one regular file without following symlinks, reads at most 1 MiB plus one byte, and rejects unavailable, blocking, oversized, non-UTF-8, duplicate-key, unsupported, non-mapping, and schema-invalid configuration before returning one cached `AppConfig`. It shares the existing JSON Schema and Pydantic boundary with the environment provider, does not retain payload-bearing parser exceptions, and does not merge secret or environment values. | `current change`; focused config provider and loader tests; Ruff and strict mypy. | Complete the remaining W1 contract, correlation, cache, and dashboard exits below. |
| 2026-08-29 | implemented | Added the independent-service ownership and dependency-direction checker to the required lint job and pinned that wiring with a CI-contract regression. | `current change`; `uv run python scripts/quality/architecture/check-independent-services.py`; focused CI-contract test. | Complete the remaining W1 config, contract, correlation, cache, and dashboard exits below. |
| 2026-08-29 | in-progress | Corrected the frozen-scenario CI path to the service-owned version directory. Existing version directories now reject additions, edits, deletes, renames, symlinks, and other type changes while a new version directory remains additive. Replaced the English-only prose rejection with field-aware checks that pass Korean natural-language payloads through the Event contract while rejecting non-ASCII scenario ids, source ids, rule ids, resource refs, paths, and nested machine values. | `current change`; focused scenario tests and CI-contract regression; CI contract, language, and roadmap checks. | Expand synthetic-data scrubbing beyond UUIDs and complete all six dimensions in every capability pack. |
| 2026-08-29 | in-progress | Audited W1-W6 against current source, tests, CI, and runtime evidence, then replaced the aggregate scope row with bounded areas. The audit keeps every area `in-progress` until its cited acceptance checks are rerun or the required runtime evidence exists, and keeps synthetic mechanics separate from live validation. | `current change`; `docs/roadmap/phases/phase-0-instrumentation.md`; exact source and test paths in the scope table; repository and CI wiring review only. | Complete the observable exits below. Live Azure, IdP, and non-synthetic baseline evidence require separately authorized operations. |
| 2026-08-20 | in-progress | Adopted the implementation ledger; earlier work-item provenance was not reconstructed. Replaced the task timeline with a repository-owned bilingual diagram. | `current change`; Phase 0 roadmap pair, diagram specification, and focused diagram checks. | Review each W1-W6 acceptance check against current implementation and CI evidence before raising any scope row to implemented or validated. |

### Remaining work

- [x] Classify every W1-W6 work item against its declared acceptance check and split independently provable scope.
- [x] Add `check-independent-services.py` to required CI and pin the lint-job wiring with a focused CI-contract regression.
- [x] Add a bounded file-backed config provider and prove missing, symlinked, non-regular, malformed, duplicate-key, oversized, non-UTF-8, non-mapping, and schema-invalid files fail fast without retaining payload-bearing parser errors.
- [x] Prove one frozen shipped scenario retains the same explicit correlation id across ingest, route, verify, gate, audit stage events, and durable audit rows.
- [x] Generate checksum-pinned Python and TypeScript contract views from every manifest-owned N/N-1 schema, reject generated drift in CI, and retain additive and breaking-change compatibility checks under [issue #332](https://github.com/dotnetpower/fdai/issues/332).
- [x] Reconcile T2 cache TTL, exact partition ownership, promotion, rollback, idempotent rotation, failure rollback, concurrency, and bounded indexed hit/miss checks under [issue #333](https://github.com/dotnetpower/fdai/issues/333).
- [ ] Complete the non-deferred KPI panel contract in [issue #334](https://github.com/dotnetpower/fdai/issues/334).
- [x] Correct the frozen-scenario immutability path, including type changes, and prove `test_frozen.py` accepts English and Korean natural-language values while rejecting non-ASCII machine identifiers and paths.
- [x] Add tenant, subscription, Azure resource, endpoint, email, IP, and sensitive-value scrub cases to `test_frozen.py` while preserving documented synthetic placeholders.
- [ ] Complete all six dimensions in every capability pack tracked by [issue #76](https://github.com/dotnetpower/fdai/issues/76).
- [x] Define the governed baseline and treatment cohort claim contract, importer, and deterministic evaluator, and give `tools/baseline_run.py` an optional receipt preflight that fails closed for a missing receipt, synthetic evidence, a digest, revision, or scenario-set mismatch, incomplete metrics, provenance, or guards, an undersized cohort, and a repository-origin artifact. Evidence: `packages/service-contracts/src/fdai_service_contracts/baseline_cohort.py`, `services/core-control-plane/src/fdai/core/measurement/baseline_cohort_claim.py`, `tools/cohort_receipt.py`, and the focused contract, claim, and runner tests.
- [x] Move the evaluated cohort requirement into a trusted versioned repository policy, bind every admission to a canonical arm-fact digest, obtain admissions only from an injected trusted provider or the existing verifier registry, and derive the published artifact origin from the validated contract. Evidence: `config/sre-cohort-claim-policy.json`, `services/core-control-plane/src/fdai/core/measurement/cohort_claim_policy.py`, and the focused policy, contract, claim, and runner tests, including the invented-digest, trust-input, and repository-origin negative cases.
- [x] Remove `artifact_origin` from the cohort evidence contract, derive the origin only from the trusted importer channel, require a trusted cohort-level admission over the complete receipt digest in addition to both arm admissions, and pin `fdai_revision` to an immutable full 40- or 64-hex commit digest in the receipt, the trusted requirement, and the CLI. Evidence: `CommitRevision`, `import_origin`, and `cohort_not_admitted` in `packages/service-contracts/src/fdai_service_contracts/baseline_cohort.py`, `admitted_cohort_claim_digest` in `services/core-control-plane/src/fdai/core/measurement/baseline_cohort_claim.py`, `require_commit_revision` in `services/core-control-plane/src/fdai/core/measurement/cohort_claim_policy.py`, and the focused contract, policy, claim, and runner tests, including the relabelled-artifact, declared-origin, invented cohort-admission, and movable-revision negative cases.
- [ ] Retain one non-synthetic reference baseline and FDAI treatment on the identical frozen scenario set with complete metrics, minimum sample size, confidence intervals, and `claim_eligible=true` under [issue #76](https://github.com/dotnetpower/fdai/issues/76).
- [ ] Complete the approved identity and policy validation profile, least-privilege probe, and governed tenant evidence under [issue #335](https://github.com/dotnetpower/fdai/issues/335).
- [x] Replace the exemption expiry stub with the approved scheduled, audited assignment re-apply and digest path under [issue #336](https://github.com/dotnetpower/fdai/issues/336).
- [x] Register lifecycle-managed PostgreSQL and Redpanda fixtures in the shared provider contract matrix, enforce loopback-only access and exact cleanup, and run the explicit Docker CI job under [issue #337](https://github.com/dotnetpower/fdai/issues/337).
