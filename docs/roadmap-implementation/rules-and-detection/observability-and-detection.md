# Observability and Detection implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Event correlation | implemented | `services/core-control-plane/src/fdai/core/event_ingest/correlator.py`; `services/core-control-plane/tests/core/event_ingest/test_correlator.py` | Deterministic grouping, episode bounds, and stable incident identity are covered by focused tests. |
| Anomaly and composite detection | implemented | `services/core-control-plane/src/fdai/core/detection/anomaly.py`; `seasonal.py`; `composite.py`; focused `tests/core/detection/test_*.py` | Cold start, flat baselines, quorum, duplicate collapse, and explainable scores fail closed. |
| Governed detection policy | implemented | `config/detection-governance-policy.json`; `core/detection/governance_policy.py`; `runtime/forecast_learning.py`; focused policy and runtime tests | Signal-class methods, cold-start floors, forecast families and horizons, correlation bounds, promotion thresholds, and change-window treatment are versioned and strictly loaded. The current executable binding enforces forecast targets; the other sections remain reviewed contracts for fixed evaluators. |
| Forecasting and outcome closure | implemented | `services/core-control-plane/src/fdai/core/detection/forecast.py`; `forecast_outcome.py`; `forecast_closure.py`; focused forecast tests | Prediction, censoring, and closure contracts are implemented. Promotion still requires measured deployment evidence. |
| Configuration drift | implemented | `services/core-control-plane/src/fdai/core/detection/configuration_drift.py`; `configuration_drift_service.py`; focused configuration-drift tests | Frozen baselines, deterministic comparison, review, and reporting remain evidence-only. |
| Live configuration observation | validated | `services/core-control-plane/src/fdai/delivery/azure/configuration_drift.py`; `runtime/configuration.py`; `infra/services/core-control-plane/`; protected plan/apply `34527374087` / `34527618632`; sanitized receipt digest `5799fe224642e170ae15f64f41671b3bac5cccf12e653cb117b1e7316d752022` | The reviewed 664-resource intended state is frozen in a content-addressed private Blob and loaded through Managed Identity. Independent deployed binding and Blob readback plus fresh complete ARG observation reported 664 current resources, 2,165 findings, one failed finding, and a failed drift verdict. Mutation, approval-request, mitigation-execution, and unsupported-claim counters were all zero. |
| Scheduled analyzer delivery | validated | `services/core-control-plane/src/fdai/delivery/analyzer_tick.py`; `analyzer_tick_cli.py`; `analyzer_run_receipt.py`; `analyzer_receipt_store.py`; protected plan `34448549192`; protected apply `34448855115`; live execution `ca-fdai-dev-krc-core-analyzer-spx114u` | The digest-pinned Job completed successfully, consulted inventory, resolved eight targets, and reported available metric access. Publish or receipt failures remain retryable. |
| Inventory-backed target resolution | validated | `services/core-control-plane/src/fdai/delivery/analyzer_targets.py`; `services/core-control-plane/src/fdai/core/investigation/analyzers.py`; focused analyzer-target and receipt tests; live execution `ca-fdai-dev-krc-core-analyzer-spx114u` | A deployed tick resolved eight inventory-discovered targets with zero configured targets after a current-image ARG snapshot promotion. The path granted no execution authority. |
| Distributed trace continuity | validated | `core/detection/trace_continuity.py`; `core/rca/trace_continuity.py`; `delivery/azure/trace_continuity.py`; `delivery/trace_continuity_tick.py`; analyzer Job binding; focused detector, RCA, source, tick, Incident, HIL, and Terraform checks; protected Operator apply `34493869398`; protected Core apply `34496652371`; live recovery report at `2026-09-10T15:45:18Z` | Live telemetry proved preserve remains continuous, regenerate and drop produce findings, five correlated discontinuities open one durable Incident with five members, and the normal Job observes delayed telemetry through a 900-second lookback. A human-authenticated guidance receipt retained no execution authority; a fresh four-hop preserve scenario closed independently as continuous in 88 seconds with zero findings; and a second human request closed the development Incident through three audited lifecycle transitions. |
| Governed operational accuracy | validated | `core/detection/forecast_operational_metrics.py`; `delivery/persistence/postgres_forecast_episode.py`; `delivery/forecast_evidence_query_cli.py`; focused reducer and local PostgreSQL tests; deployed snapshot at `2026-09-10T11:00:02Z`; [Resolved decisions](../../roadmap/rules-and-detection/observability-and-detection.md#resolved-decisions) | The deployed repeatable-read snapshot reports zero episodes and zero scorable outcomes, preserving `null` precision, recall, missed-breach, interval-coverage, lead-time, and abstention rates. This validates truthful denominator reporting but does not satisfy the 14-day and 30-scorable-episode promotion gate. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-11 | validated | Completed the configuration-baseline evidence loop. The final exact-revision plan contained one in-place Core update and zero create, delete, or replace actions. Apply, health, peer isolation, deployed binding readback, private Blob digest readback, and fresh complete ARG comparison succeeded. The governed result truthfully remains failed because one of 2,165 findings failed; no remediation, approval, or execution was requested. | Exact protected main CI `34526653322`; supply-chain `34526654407`; protected plan/apply `34527374087` / `34527618632`; receipt digest `5799fe224642e170ae15f64f41671b3bac5cccf12e653cb117b1e7316d752022`; merged PRs #717, #722, #724, #725, #726, #727, #730, #732, #734, and #737. | Re-evaluate forecast promotion no earlier than `2026-09-24T11:00:02Z` under #718, and promote only if at least 30 scorable episodes and every policy threshold exist. |
| 2026-09-11 | in-progress | The first post-activation standard Core plan failed closed before baseline readback because the pinned `uv` setup still ran only for apply or the initial transition. Core plans now install the pinned evidence toolchain whenever they can materialize the persistent baseline binding. | Protected plan run `34515181044`; `current change`; focused service-deploy workflow tests. | Merge the toolchain condition and rerun the exact-revision standard Core plan. |
| 2026-09-11 | in-progress | The exact protected apply updated Core and passed service health, but post-apply configuration readback selected the wrong container name and failed before producing the drift receipt. The verifier now selects the deployed `core-control-plane` component, every Core plan preserves the reviewed binding when both protected envelope secrets exist, and every successful Core apply independently verifies an active binding. | Protected plan/apply `34512921177` / `34513181278`; `current change`; focused workflow and baseline readback tests. | Merge the readback fix, run a fresh standard exact-revision Core plan that preserves the deployed binding, apply it only after confirming no unrelated create, delete, or replace, and retain the sanitized receipt. |
| 2026-09-11 | implemented | Corrected the private Blob metadata contract after the third protected plan failed closed with `InvalidMetadata` before writing the baseline or creating a Terraform plan. Baseline and receipt uploads now use the provider-valid `fdai_sha256` key, and Managed Identity readback verifies the corresponding exact response header. | Protected plan run `34511282839`; `current change`; focused Blob source and workflow tests. | Merge the metadata correction and produce a new exact-revision Core plan. |
| 2026-09-11 | implemented | Corrected the intended-state freeze boundary after the second protected plan proved that current ARG state can legitimately differ from the approved snapshot. The protected workflow now accepts the reviewed JSON only through a bounded compressed secret envelope, decodes and revalidates every baseline identity field on the VNet runner, canonicalizes the bytes, and uploads the exact approved content to private Blob. Fresh ARG is used only for post-apply drift observation, and current finding counts are reported rather than required to equal review-time counts. | Protected plan run `34509564462`; `current change`; focused baseline-envelope and workflow tests. | Merge the corrected freeze boundary, produce a new exact-revision Core plan, and continue only if it contains no unrelated create, delete, or replace. |
| 2026-09-11 | implemented | Hardened the live Azure Resource Graph projection after the first protected plan failed closed before Blob upload or Terraform planning. ARG omitted a false boolean projection, so the adapter now emits and strictly accepts non-empty `present` or `missing` tokens and still preserves an explicitly present empty scalar. | Protected plan run `34507098843`; `current change`; focused Azure configuration-observation tests. | Produce a new exact-revision protected plan and continue only if it contains no unrelated create, delete, or replace. |
| 2026-09-11 | implemented | Added deployment-owned private Blob delivery for reviewed configuration baselines. The Core runtime now reads one content-addressed Blob through Managed Identity, and the protected service transition reconstructs the approved ARG snapshot, verifies its canonical digest and resource count, limits the plan to the Core evidence-binding boundary, shreds temporary source material, and emits only a sanitized post-apply receipt. | `current change`; `services/core-control-plane/src/fdai/delivery/azure/configuration_drift.py`; `services/core-control-plane/src/fdai/runtime/configuration.py`; `infra/services/core-control-plane/`; `.github/workflows/service-deploy.yml`; `scripts/deployment/{azure/configuration_baseline_evidence.py,service/guard_plan.py}`; focused tests passed 80 cases; strict mypy passed for the Azure configuration-drift adapter; Core service Terraform validation succeeded. | Merge the protected delivery path, then retain an exact-revision Core plan with no unrelated create, delete, or replace, apply it, and preserve the sanitized independent Blob/ARG readback. Forecast promotion remains blocked until 14 actual days and 30 scorable episodes exist. |
| 2026-09-10 | validated | Completed the human-mediated trace recovery loop. Exact server-owned target projection enabled Incident intervention, the Operator and Core outbox/consumer lifecycles delivered typed requests, human guidance retained no managed-resource authority, and a fresh four-hop preserve scenario independently closed as continuous before human development closure. | Merged PRs #703, #707, #709, #710, #713, and #714; protected Operator plan/apply `34492885738` / `34493869398`; protected Core plan/apply `34496396894` / `34496652371`; authenticated guidance and closure audits; recovery report at `2026-09-10T15:45:18Z` with four spans, four hops, one trace, one completion marker, one continuous scenario, zero findings, and 88-second latency; closed issue #142. | Load a deployment-owner-reviewed configuration baseline. Accumulate the policy-required 14 days and 30 scorable forecast episodes before any promotion claim. |
| 2026-09-10 | validated | Completed the durable analyzer evidence loop after solo-development Environment approval was made non-review-gated. One protected apply deployed Incident and forecast evidence queries plus the separated trace lookback. The durable audit contains one Incident with five member Events; the forecast snapshot explicitly reports zero episodes and null rates; and a normal analyzer execution observed delayed telemetry through the 900-second lookback with one continuous scenario and two published findings. | Protected plan/apply `34468186170` / `34468468269`; Incident query execution `ca-fdai-dev-krc-core-analyzer-3rxwg9a`; forecast query execution `ca-fdai-dev-krc-core-analyzer-zdifajr`; analyzer execution `ca-fdai-dev-krc-core-analyzer-pxup19f`; merged PRs #696, #697, and #698; session receipt `observability-live-evidence/analyzer-live-receipt-20260910.json`. | Retain independent human action approval and verified recovery closure for issue #142. Load a deployment-owner-reviewed configuration baseline. Accumulate the policy-required 14 days and 30 scorable forecast episodes before any promotion claim. |
| 2026-09-10 | validated | Completed protected analyzer image convergence and retained live inventory and trace evidence. A deployed tick discovered eight targets with no configured targets and available metric access. Synthetic-tagged live telemetry produced one continuous preserve result, two regenerate/drop findings, and five correlated findings with five successful Event publications; the temporary 900-second trace window was read back and restored to 60 seconds after the bounded run. | Merged PRs #659, #667, #673, #677, #684, and #686; protected plan/apply runs `34448549192` and `34448855115`; analyzer execution `ca-fdai-dev-krc-core-analyzer-spx114u`; live report times `2026-09-10T09:16:00Z` and `2026-09-10T09:21:03Z`; session receipt `observability-live-evidence/analyzer-live-receipt-20260910.json`. | Complete issue #142 after resolving the active ActionRun fence and retaining one Incident, independent human approval or fully safeguarded action, and verified recovery closure. Configuration-baseline and governed operational-accuracy evidence remain open. |
| 2026-09-10 | in-progress | Added a context-bound analyzer-only protected deployment route, executable scope guard, and two-phase state-address reconciliation that validates every legacy/current pair before changing Terraform state. Unsafe broad plans remained blocked throughout the live hardening loop. | Merged PRs #643, #646, #650, and #654; green required PR CI; protected runs `34426702835`, `34427451334`, `34427521275`, `34428877985`, `34430430009`, and `34432091729`; PR #659 contains the state-only correction. Run `34432091729` proved that legacy target expansion admitted unrelated dependencies and the bounded guard rejected them. | Merge PR #659 after the unrelated `main` LOC regression is corrected, then retain one zero-destroy plan, exact apply, and successful analyzer tick receipt. Base run `34431586744` currently fails because `delivery/persistence/postgres.py` has 810 lines against the 800-line ceiling; observability code did not change that file. |
| 2026-09-10 | implemented | Corrected the earlier campaign subtotal after the final review and upstream merge. Twenty-seven focused hardening commits fixed every observed policy, metric, SQL, asset-resolution, and receipt-integrity finding above Low. | `current change`; the merged bounded observability slice passed 167 cases; local PostgreSQL forecast evidence checks passed 5 cases; the final independent review found no issue above Low. | Retain the four governed live evidence classes listed below under issue #608. |
| 2026-09-10 | implemented | Added the governed detection policy and startup forecast-target enforcement, deployment accuracy reduction over a repeatable-read PostgreSQL snapshot, and retry-safe complete analyzer Job attempt receipts. Twenty-four focused hardening commits fixed every observed policy, metric, SQL, asset-resolution, and receipt-integrity finding above Low. | `current change`; policy and runtime checks passed 101 cases; the bounded observability slice passed 124 cases; analyzer receipt checks passed 58 cases; local PostgreSQL forecast evidence checks passed 14 cases. | Retain the four governed live evidence classes listed below. |
| 2026-09-09 | implemented | Added a deterministic T1 trace-cause boundary that distinguishes instrumentation, collector, and header propagation only from one exact-scope, time-bounded, independently cited signal and never returns a remediation reference. | `current change`; full RCA, detector, and Incident-chain slice passed 294 cases; strict mypy passed 26 RCA source files. | Bind authoritative cause-evidence producers and retain the governed live approval or safeguarded-action and recovery receipts under issue #142. |
| 2026-08-29 | implemented | Hardening round 3 reviewed 26 Azure drift-adapter lenses and distinguished a valid empty ARG location from a missing field. Globally scoped resources now use the stable `global` region token instead of failing the complete observation. | `current change`; focused Azure configuration drift tests. | Retain a governed current-state drift receipt. |
| 2026-08-28 | in-progress | Added and runtime-bound a scope-pinned Azure Resource Graph configuration observation source. It accepts only ordered scalar property paths, projects no arbitrary property bag, replaces raw provider ids with stable digest suffixes, marks absent properties unknown, and fails without an observation on incomplete configuration, scope escape, truncation, pagination, size, HTTP, or row-shape errors. Startup requests workload identity when the capability is explicitly enabled and installs the read-only tool only after every prerequisite validates. Terraform exposes the complete input set as an opt-in contract and emits no drift environment by default. | `current change`; `delivery/azure/configuration_drift.py`; `runtime/{bootstrap,bootstrap_plan,configuration}.py`; `infra/services/core-control-plane/`; focused Azure adapter, runtime binding, drift service, and infrastructure checks passed 69 cases; Ruff and strict mypy passed; Core service Terraform validation succeeded. | Load reviewed deployment baseline content and retain a governed current-state drift receipt. |
| 2026-08-14 | in-progress | Adopted the implementation ledger without reconstructing earlier provenance and corrected the analyzer delivery claim to match the current tree. | `current change`; current source and focused tests listed in the scope table. | Restore analyzer delivery and retain governed accuracy evidence. |
| 2026-08-15 | implemented | Added the analyzer tick runner and the `fdai.delivery.analyzer_tick_cli` entry point the Terraform job configures, publishing one canonical window-keyed Event per finding with reported publish failures. | `current change`; `services/core-control-plane/src/fdai/delivery/analyzer_tick.py`; `pytest services/core-control-plane/tests/delivery/test_analyzer_tick.py` (10 passed). | Retain deployed accuracy evidence; target resolution is the configured list only. |
| 2026-08-16 | not-started | Corrected three claims this document made about code that does not exist as described. The frozen-baseline bullet named `delivery/azure/configuration_drift.py` and an Azure Resource Graph query; no such module exists and the only shipped observation source is file-backed. Live configuration observation is now a separate `not-started` scope row rather than being implied by the `implemented` drift row. | `current change`; `find services -name "configuration_drift*.py"` returns only `core/detection/*` and `delivery/configuration_drift.py`, whose module docstring reads "File-backed baseline sources"; `grep -rn bind_configuration_drift` shows runtime bootstrap never calls it. | Build the Azure observation adapter, or record a decision that drift stays evidence-replay-only. |
| 2026-08-16 | not-applicable | Repointed two stale references: the RCA projection moved to `services/operator-service/src/fdai_operator_service/rca_projection.py`, and the shared category list omitted `compliance` and used a hyphen where `Category` uses `config_drift`. | `current change`; `find services -name "rca_projection*.py"`; `services/core-control-plane/src/fdai/shared/contracts/models/enums.py` `Category` has five members. | None; both are now exact. |
| 2026-08-16 | implemented | Resolved analyzer-tick targets from the durable inventory projection in addition to the configured list. A reviewed neutral resource-type map selects the analyzer kind, configured targets keep priority, discovered targets are bounded and deterministically ordered, and unmapped types, unusable or stale observed state facts, and a failed projection read fail closed instead of narrowing coverage silently. | `current change`; `services/core-control-plane/src/fdai/delivery/analyzer_targets.py`; `pytest services/core-control-plane/tests/delivery/test_analyzer_targets.py services/core-control-plane/tests/delivery/test_analyzer_tick.py` (24 passed); strict mypy and Ruff passed the changed files. | Record deployed-runtime evidence that an inventory-discovered resource joins a live tick. |
| 2026-08-16 | implemented | Hardened inventory-backed resolution after review. The discovered bound now stops one row below the durable store's own query limit so the documented maximum cannot raise inside the projection read, `FDAI_ANALYZER_MAX_DISCOVERED_TARGETS` is rejected at parse time with the environment key named, a state fact whose evidence cutoff is not timezone-aware is skipped as unusable instead of raising, truncation is reported only when a target was actually withheld, and the deployed job binds the `FDAI_INVENTORY_DSN` key the CLI reads. Determinism is now claimed only for an untruncated projection. | `current change`; `services/core-control-plane/src/fdai/delivery/analyzer_targets.py`; `pytest services/core-control-plane/tests/delivery/test_analyzer_targets.py services/core-control-plane/tests/delivery/test_analyzer_tick.py services/core-control-plane/tests/delivery/test_analyzer_tick_routed.py` (30 passed); `pytest tests/integration/infra/test_detection_readiness.py` (3 passed). | Record deployed-runtime evidence that an inventory-discovered resource joins a live tick. |
| 2026-08-17 | in-progress | Accepted the deterministic distributed-trace continuity design after rejecting an inventory-resource mapping that would have misrepresented a trace topology as a managed Resource. | `current change`; this document; [issue #142](https://github.com/dotnetpower/fdai/issues/142). | Implement and focus-test the source, detector, shared analyzer Job binding, and governed Event path, then retain live `preserve`, `regenerate`, `drop`, approval, and recovery evidence. |
| 2026-08-17 | implemented | Implemented the distributed-trace continuity detector, strict workspace-based Application Insights source, shared analyzer Job runner and configuration, and repeated-finding Incident handoff. The KQL uses the documented `Id`, `OperationId`, `Properties`, and `TimeGenerated` columns. | `current change`; focused behavior and HIL checks passed 55 cases; strict mypy and task-scoped Ruff passed; `terraform -chdir=infra validate` succeeded. | Deploy the exact validated revision to the observation lab and retain live `preserve`, `regenerate`, `drop`, approval, and recovery evidence before advancing this scope to `validated`. |

### Remaining work

- [x] The analyzer entry point the Terraform job configures exists, publishes canonical window-keyed Events, and reports publish failures with a non-zero result, proven by `services/core-control-plane/tests/delivery/test_analyzer_tick.py`.
- [x] Analyzer targets resolve from the configured list plus the durable inventory projection through a reviewed neutral resource-type map, and unmapped types, unusable or stale observed state facts, and a failed projection read fail closed, proven by `services/core-control-plane/tests/delivery/test_analyzer_targets.py`.
- [x] Implement a bounded live `ConfigurationObservationSource` for Azure, evidenced by focused
  adapter and drift service tests.
- [x] Bind the Azure source from complete explicit runtime configuration and prove startup workload
  identity selection and capability installation with focused tests.
- [x] Expose the complete configuration through an opt-in Container Apps Terraform contract that
  emits no drift environment by default, and pass module contract tests and Terraform validation.
- [x] Deliver reviewed baseline content through a content-addressed private Blob and Managed
  Identity reader without committing tenant or resource values, proven by focused runtime,
  service-plan, workflow, and Terraform checks.
- [x] Load reviewed deployment baseline content and retain a governed current-state receipt before
  advancing live configuration observation to `validated`.
- [x] Record deployment evidence for detector precision, recall, missed breaches, interval coverage, forecast lead time, and abstention rates: the `2026-09-10T11:00:02Z` repeatable-read snapshot reports zero episodes and scorable outcomes with all denominator-dependent rates explicitly `null`, without promotion authority.
- [x] Record deployed-runtime evidence that inventory-discovered resources join a live analyzer tick without a deployment edit: protected apply `34448855115` and execution `ca-fdai-dev-krc-core-analyzer-spx114u` retained a report with inventory consulted, zero configured targets, eight discovered targets, eight total targets, and metric access available.
- [x] Retain content-addressed analyzer attempt receipts and expose read-only forecast accuracy
  metrics, proven by focused analyzer tests and local PostgreSQL forecast evidence tests.
- [x] Add evidence-bounded trace RCA that distinguishes instrumentation, collector, and
  header-propagation causes only from one independently cited signal, with no remediation authority.
- [x] Complete [issue #142](https://github.com/dotnetpower/fdai/issues/142): protected Operator/Core deployment delivered one human guidance receipt with `execution_authority=false`; a fresh preserve run produced four spans, four expected hops, one trace, one completion marker, `continuous=1`, zero findings, and 88-second recovery latency; and a second human request closed the development Incident through three audited lifecycle transitions.
- [x] Resolve signal-class methods, baseline history, correlation defaults, forecast horizons,
  change-window treatment, and promotion thresholds in
  `config/detection-governance-policy.json`, with exact loader checks in
  `services/core-control-plane/tests/core/detection/test_governance_policy.py`.
- [ ] Re-evaluate forecast promotion at or after `2026-09-24T11:00:02Z` under
  [issue #718](https://github.com/dotnetpower/fdai/issues/718). Keep observation mode unless the
  governed snapshot contains at least 30 scorable episodes and satisfies every promotion threshold.
