# AKS Commerce Business Scenario Implementation

This ledger records delivery state for the bounded AKS commerce scenario defined by the
[owner design](../../roadmap/operations/aks-commerce-business-scenario.md).

## Implementation status

### Implementation scope

| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Independent acceptance execution-quarantine closure | implemented | `acceptance_closure.py`; material/dispatch/runtime wiring; focused effect, I/O, safeguard, Pantheon and Incident runtime tests | Original Action and released plan, command/provider receipt, target generation, post-release signed orders/resources and atomic predecessor checks are connected through Heimdall. Delayed evidence re-enters through existing at-least-once delivery without redispatch. Thor alone terminalizes the exact ActionRun, and the episode-bound canonical Incident follows its legal lifecycle. No live evidence is implied. |
| Optional production recovery composition | implemented | `runtime/aks_commerce.py`, `bootstrap_pantheon.py`, extension `acceptance_runtime.py` and package entry point; six composition cases, installed entry-point load and four Core wheel checks passed | Exact configured Rule, durable promotion registry, existing isolated safeguard port and explicit approval policy are mandatory. No provider I/O occurs at construction; defaults and unrelated dispatch remain unchanged. Live observation issuance, eligible promotion and independent effect closure remain open. |
| Signed acceptance preparation and current approval chain | implemented | `acceptance_action.py`, `acceptance_preparation.py`, `acceptance_authority.py`, `acceptance_material.py`, `acceptance_dispatch.py`; 56 acceptance I/O tests | Actual fixed agents prepare original material before two-person Var review. Durable approval readback, original Rule/catalog binding, current promotion/risk/safety and final safeguard boundary checks are implemented. Source authority and transport inputs are synthetic in tests; production startup and effect closure remain unbound. |
| Business topology and package assets | implemented | `extensions/aks-commerce/resources`; shared contract and package tests | The package is inert, immutable, customer-agnostic, and adds no agent or authority. |
| Messaging, browser, Kubernetes, and SLO observations | implemented | Azure metric templates, browser policy, synthetic journey, observation source, and focused tests | Live source identity and availability remain deployment-bound. |
| Bounded synthetic order request policy | implemented | `extensions/aks-commerce/tests/test_browser_policy.py`; 11 focused tests, Ruff, and strict mypy passed | Request-time expiry, one order POST, denied-request rejection, exact order HTTP response, completion timestamps, and cooperative timeout are covered with a fake browser. Real browser, worker isolation, authenticated authorization, and live order evidence remain unverified. |
| Concrete acceptance observation and verified publication | implemented | `acceptance_kubernetes.py`, `acceptance_issuance.py`, `acceptance_receipts.py`, `acceptance_store.py`, `acceptance_runtime.py`; focused acceptance I/O tests | GET-only exact Kubernetes snapshots, independent injected-key receipt issuance, append-only activation/revocation, pinned-key verification, immutable audited storage, and the one-shot Analyzer entry point are implemented. Authenticated probe collection, private-key mount, trust-owner runtime binding, database grants, job scheduling, and actual provider evidence remain unbound. |
| Acceptance-specific post-action effect verification | implemented | `effect.py::verify_order_acceptance_effect`; `acceptance_closure.py`; focused effect and lifecycle tests | Exact action-owner expectation, post-apply distinct probes and resource observations, current signature verification, and exact desired replicas gate the result. Heimdall publishes the verified closure, Thor terminalizes the exact ActionRun, and Incident resolution uses the detector episode binding. No live recovery is claimed. |
| Deterministic business-impact assessment | implemented | `fdai_aks_commerce.assessment`; complete, backlog, DLQ, regression, held, and recovered regressions | The reducer never converts missing evidence into health. |
| Positive health and recovery evidence | implemented | `extensions/aks-commerce/tests/test_assessment.py`; 14 focused tests passed | Failed order availability, breached SLOs, and unready workloads without a classified cause return `held` with `health_not_proven`. This does not add a zero-replica detector or incident publisher. |
| Canonical Analyzer incident bridge | implemented | `extensions/aks-commerce/src/fdai_aks_commerce/coordinator.py`; `extensions/aks-commerce/tests/test_coordinator.py`; 12 focused tests passed | Complete degraded assessments adapt to the shared Analyzer publication ledger and Huginn/Heimdall Incident lifecycle. Exact target, freshness, replay, incomplete/synthetic rejection, and uncertain-send behavior are covered. Live sources and runtime registration are not bound. |
| Order-acceptance demo integration | in-progress | `extensions/aks-commerce/src/fdai_aks_commerce/acceptance.py`; 33 coordinator tests passed, including 21 acceptance cases | The typed expiring intent, sample/unknown/freshness holds, exact receipt-digest verification interface, inert scale candidate, and canonical Incident path are implemented. Actual observation and receipt-verifier adapters, runtime registration, approval/action submission, and independent live effect closure remain unbound. |
| Operator and Console projection | implemented | Operator family tests; Console decoder tests, typecheck, build, and two-route browser scenarios | Authenticated live data remains a separate deployment claim. |
| Governed Kubernetes actions and verification | implemented | Core and isolated Executor Kubernetes adapters; business-effect tests; ActionType catalog regressions; `config/action-type-runtime-support.json` | The isolated Executor route is conditional and namespace-scoped. The commerce effect verifier remains scenario-specific; no live receipt is recorded. Enforce remains blocked without promotion, approval, and live effect evidence. |
| Public AKS API and HTTPS scenario-lab profile | implemented | Terraform validation, scenario-lab tests, and `prepare-commerce.sh` shell validation | No tenant, DNS, certificate, or Terraform plan is selected. The management API retains Entra RBAC without an IP allowlist. |
| Live Azure scenario validation | not-started | #1207 | Requires a separately approved exact plan and live evidence campaign. |

### Implementation history

| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-17 | implemented | Completed scanner coverage for the intentional absent API IP allowlist and regenerated the question bank plus dependent semantic-intent inventory after the latest protected-main Console catalog change. No question identity, runtime authority, or AKS access posture changed. | Failed CI run `35236145181`, jobs `105253149062` and `105253149251`; Checkov `3.2.256` reports 185 passed and 0 failed; exact generated-artifact equality tests passed. | Publish the repair and retain a separately approved exact live plan and Azure receipt. |
| 2026-09-17 | implemented | Added scanner-specific exceptions for the disposable lab's intentional public API and absent IP allowlist. The exceptions cover only `AZU-0041` and `AZU-0065`; Entra RBAC and disabled local accounts remain unchanged. | Failed CI run `35234924105`, job `105248372677`, step `Trivy Terraform misconfiguration scan`; exact local Trivy `0.72.0` scan reports zero Medium, High, or Critical findings; 12 focused tests passed. | Publish the repair, obtain a new protected-main exact plan, and retain the separately approved live Azure receipt. |
| 2026-09-17 | implemented | Changed the dedicated scenario-lab AKS management endpoint from private-cluster mode to public access without an IP allowlist. Entra RBAC and disabled local accounts remain the management authorization boundary. | `current change`; `infra/scenario-lab/aks.tf`; `tests/integration/infra/test_scenario_lab.py`; 12 focused tests and `terraform validate` passed. | Publish the change and retain a separately approved plan and live Azure receipt before claiming runtime validation. |
| 2026-09-17 | implemented | Regenerated the System Knowledge catalog after the Pantheon owner document SHA changed during CI integration repair. Commerce records, behavior, evidence, and authority are unchanged. | `current change`; official System Knowledge catalog generator; derived-source gate. | Retain exact-head protected CI and the existing separately approved live scenario work. |
| 2026-09-17 | implemented | Added an independent no-I/O Ed25519 acceptance receipt issuer and append-only trust activation/revocation lifecycle. The verifier checks revocation on every use, same-reason redelivery preserves the first marker, and source, issuer, trust owner and executor identities remain distinct. | `current change`; `acceptance_issuance.py`; focused signed retention and revocation replay test | Bind authenticated collection, owner-only key injection, service-specific database grants and scheduling before any live evidence claim. |
| 2026-09-17 | implemented | Connected delayed independent closure replay to Heimdall's owned observation topic, Thor's exact durable ActionRun terminal state, and the canonical detector-episode Incident. Duplicate delivery does not redispatch the provider mutation or add another Incident transition. | `current change`; focused closure, Heimdall publication, Thor identity/replay, runtime composition and Incident episode tests | Bind the authenticated worker, receipt issuer and revocation operations, current policy, database grants, schedule and reviewed Rule, then obtain separate approval for a live rehearsal. |
| 2026-09-17 | implemented | Retained configured-target original release context without changing initial closure decisions; linked original command IDs; connected Heimdall's exact non-shadow attempts to independently signed post-release observation and the existing atomic closure resolver. Historical replay preserves one original result. | `current change`; 173 focused tests passed, strict mypy on nine source files, Ruff, editor diagnostics and both installed provider entry points passed. Test authority, transport and observations are synthetic; no live provider effect occurred. | Add delayed-evidence redrive and ActionRun/Incident/Console terminal propagation, actual observation issuance/trust/policy/DB/scheduling, reviewed Rule configuration, and separately approved live rehearsal. |
| 2026-09-17 | implemented | Bound the installed acceptance provider in standard Pantheon startup, reused existing runtime-owned preparation/risk/approval/dispatch dependencies, and required exact detector-to-Rule identity. | `current change`; 140 focused I/O/runtime/bootstrap-model checks, four Core packaging checks, installed entry-point load, and 18 post-Rule-binding preparation/composition checks passed; counts overlap. | Supply the reviewed acceptance Rule and real observation/trust/approval/promotion inputs, then finish independent effect closure, Console linkage and separately approved deployment/rehearsal. No live effect or activation is claimed. |
| 2026-09-17 | implemented | Added original Action/Rule material, Forseti-owned preparation before approval publication, durable current Var approval readback, shared risk/promotion checks, and the guarded isolated dispatch adapter. Missing preparation lowers to shadow and the shipped scale quorum requires two people. | `current change`; 291 focused agent/runtime/layout/commerce tests passed; after Rule-digest binding, all 56 owning acceptance I/O tests passed. Counts overlap; no live approval or effect is implied. | Bind the reviewed acceptance Rule and production startup, actual observer issuance/trust/policy/DB/scheduler inputs, independent effect closure and Console linkage, then perform separately approved deployment and rehearsal. |
| 2026-09-17 | implemented | Connected signed stored acceptance evidence to Forseti's registered candidate source and added a current-evidence wrapper around Thor's existing executor. Fixed the source binding to the canonical Analyzer event name. | `current change`; 147 Wave 3 and acceptance I/O tests passed, including five real-agent bus cases; strict mypy on eight modules passed. | Complete deployed original Action preparation, current independent approval readback, seven-safeguard publication and effect closure; producer issuance, Console linkage and live rehearsal also remain open. No live order, grant, action or promotion was performed. |
| 2026-09-17 | implemented | Aligned the acceptance job with the shared venue and bus-security resolver and made entry-point failure output a fixed unavailable record instead of raw provider exceptions. | `current change`; 32 acceptance I/O/runtime tests, strict mypy and Ruff passed. | Independent producer issuance, operational policy and approval/Thor lifecycle binding, deployment and live recovery remain open. |
| 2026-09-17 | implemented | Added acceptance-only independent post-action verification with exact target and replica expectation, no pre-action observation reuse, all probe timestamps after apply, signature revalidation and final freshness recheck. | `current change`; 11 effect tests, strict mypy and Ruff passed. Old, unsigned, sample, wrong-target, failed-order and overscaled observations never verify recovery. | Bind the trusted ActionRun/provider-receipt expectation and Heimdall effect lifecycle through the existing governed path; no action was executed, retried or closed. |
| 2026-09-17 | implemented | Added concrete exact-target Kubernetes reads, retained Ed25519 verification, immutable audit-backed observations, and the deployed acceptance Analyzer entry point. Synthetic journey success now additionally requires the exact order POST's 2xx response. | `current change`; 31 acceptance I/O tests and 11 browser-policy tests passed; six-module strict mypy and scoped Ruff passed. The lockfile adds only already-resolved direct dependencies; no package version changes. | Implement and bind independent receipt issuance and authenticated probe collection, current operational policy reads, agent-owned candidate admission and effects, deployment grants and scheduling, and live end-to-end evidence. Mock HTTP and cryptographic unit tests do not prove a deployed connection or successful order. |
| 2026-09-17 | implemented | Added a separate acceptance-only reducer and receipt-gated Analyzer without changing the fulfillment projection or removing synthetic-traffic provenance. Inert scale candidates retain the exact UID, resource version and reviewed replica floor; maintenance, HPA or writer uncertainty withholds them. | `current change`; 33 focused coordinator tests, Ruff, strict mypy and editor diagnostics passed. Distinct acceptance observations open one canonical Incident; replay is suppressed and resource refresh does not retime the same failed orders. | Bind actual read-only observations and independently authenticated receipts, register the detector, submit candidates through the existing approval and Thor workflow, and retain live effect evidence. The test verifier is not a deployable authenticator; no live Incident, proposal, order, permission, or action was created. |
| 2026-09-17 | implemented | Revalidated standing-authorization expiry before each synthetic request, limited order submission to one POST before dispatch, rejected denied-request journeys despite success dialogs, added an overall cooperative timeout, recorded completion time, and corrected the Playwright Python `first` property. | `current change`; 9 focused browser-policy tests, Ruff, strict mypy, and editor diagnostics passed. | Authenticate worker authorization and isolation, exercise the real HTTPS storefront, and bind order-acceptance evidence without treating synthetic traffic as fixture data. No live order, grant, or deployment occurred. |
| 2026-09-17 | implemented | Added an evidence-only commerce ResourceAnalyzer adapter and clarified that the isolated Executor is Thor's execution runtime, not a separate executing agent. | `current change`; `AksCommerceAnalyzer`; 12 focused coordinator tests including canonical publication, one Incident after distinct repeated observations, duplicate suppression, and uncertain-send reconciliation; Ruff and strict mypy passed. | Bind live observation sources and runtime registration; implement the order-acceptance profile; prepare the exact approved Thor-owned execution and least-privilege deployment plan. No live action or permission was changed. |
| 2026-09-17 | implemented | Removed false healthy/recovered fallthrough when a complete observation frame contains a failed order availability signal, breached SLO, or unready workload but no known degraded classification. | `current change`; `extensions/aks-commerce/src/fdai_aks_commerce/assessment.py`; `uv run pytest -q --no-cov extensions/aks-commerce/tests/test_assessment.py` passed 14 tests. | Connect the order-acceptance detector and incident ingress; configure and independently validate a separately approved deployed executor and minimum target permissions. |
| 2026-09-17 | implemented | Registered the four exact-target Kubernetes actions on the isolated Executor while retaining the commerce verifier as conditional, scenario-specific effect evidence. | `current change`; isolated Executor adapter and tests; runtime-support manifest; namespace Role and RoleBinding. | Retain a separately approved commerce recovery receipt and independent business-effect observation. |
| 2026-09-17 | implemented | Refreshed the shared System Knowledge catalog after document-version owner sources changed, without altering the dedicated AKS commerce route or scenario evidence. | `current change`; canonical catalog compiler and derived-source gate. | No AKS scenario behavior or deployment authority changed. |
| 2026-09-17 | implemented | Regenerated the question bank and downstream semantic-intent coverage after upstream Console navigation catalogs changed their authoritative source digest. | `current change`; CI run `35165469714` attempt 1 failed only the question-bank artifact-equality node at head `efaa1beb2`; both official generators and focused source-equality tests pass locally. | Retain a passing exact-head protected CI run and merge evidence. |
| 2026-09-17 | implemented | Synchronized the upstream Environment and deployment Settings consolidation while preserving the dedicated `/aks-commerce` route and panel registration. | `current change`; panel registry and router regressions keep legacy provisioning routes hidden but addressable and leave the AKS commerce route unchanged. | Protected CI and merge evidence remain pending; navigation changes grant no deployment authority. |
| 2026-09-17 | implemented | Synchronized the scenario-lab plan-first safeguard regression with upstream replacement-path parsing. | `current change`; the workflow contract now asserts both changed-field and `replace_paths` summaries while excluding before/after values. | Protected CI and merge evidence remain pending; no Azure plan or apply ran. |
| 2026-09-17 | implemented | Regenerated semantic-intent coverage after the merged question-bank sources changed their source identity. | `current change`; CI run `35162639926` attempt 1 failed only the semantic-intent artifact-equality node at head `7e461ec80`; the official generator and focused contract tests pass locally. | Retain a passing exact-head protected CI run and merge evidence. |
| 2026-09-17 | implemented | Aligned exact regression baselines with the 54-entry ActionType catalog, Kubernetes Core scale-out support, refreshed Cost release identities, and regenerated the question bank from its reviewed sources. | `current change`; CI run `35128385850` attempt 1 was reproduced at head `c2aa15b0d`; all five exact failing nodes and the ActionType, runbook, Cost convergence, and question-bank source-equality checks pass locally. | Retain a passing exact-head protected CI run and merge evidence. |
| 2026-09-17 | implemented | Registered the AKS commerce Operator test in the canonical service-suite owner manifest and refreshed the reviewed release-derived semantic surface and Cost package pins after the Kubernetes ActionType ontology change. | `current change`; the release-pin generator measured all seven held-out retrieval cases, retained immutable receipt history, validated all 16 F1-F8 fixtures, and became idempotent; the exact failed semantic catalog, runtime rule-generation, Operator family, and service-suite tests pass. | Re-run exact-head CI; no runtime promotion, deployment, or live authority changed. |
| 2026-09-17 | implemented | Registered the route-local AKS commerce translator with the Console catalog-usage gate so every literal key resolves against its English source. | `current change`; exact CI step reproduced locally and the focused catalog-usage test passed. | Re-run the complete operator-surface step before publication. |
| 2026-09-17 | implemented | Routed the Operator AKS commerce dependencies through the existing `family_adapters` facade so the shared composition root stays below its reviewed import-fanout limit. | `current change`; Operator boundary checker reports 39 unique FDAI imports and focused composition tests pass. | No remaining source work for this fanout correction. |
| 2026-09-17 | implemented | Registered truthful conditional Core runtime support for Kubernetes restart, scale, and rollout rollback while preserving unsupported isolated-Executor and live-evidence states. | `current change`; action-type runtime-support checker passed with 54 catalog entries, 49 bound entries, and 5 explicit unsupported entries. | Bind and validate the isolated Executor only through a separately governed delivery. |
| 2026-09-17 | implemented | Added the immutable scenario package, workload SLOs, Azure messaging mappings, read-only browser policy, synthetic order journey, deterministic business-impact reducer, durable projection path, Operator and Console workspace, exact-target Kubernetes adapter, independent business-effect closure, and optional public-HTTPS scenario-lab profile. | `current change`; focused Python, ActionType, Operator, Console, browser, Terraform, shell, type, lint, and documentation checks listed in the scope table. | Retain the separately approved live Azure receipt before raising any area to validated or promoting an action. |
| 2026-09-17 | in-progress | Defined the generic AKS commerce business topology, evidence, browser, authority, action, and public-exposure boundaries without changing the agent pantheon. | `current change`; owner design, implementation ledger, and #1207. | Implement each scope row, run focused checks, and retain separately approved live evidence. |

### Remaining work

- [x] Retain exact original release context for prepared acceptance Actions and reconcile quarantine only from independently verified post-release evidence; focused retention, signature, atomic-store and Heimdall tests passed.
- [x] Connect delayed independent-evidence redrive and propagate only the exact verified closure to ActionRun, Incident and Console terminal state without replaying a mutation; focused closure, agent and Incident episode tests cover replay and identity mismatch.
- [x] Bind the implemented preparation, current authority and isolated dispatch adapters in production startup with fail-closed reviewed-Rule and configuration checks; focused composition and installed entry-point tests passed.
- [ ] Supply the reviewed acceptance Rule and exact source configuration, then retain an actual Var-to-command receipt with independently verified effect evidence.
- [x] Record passing package and contract tests for the business topology and immutable assets.
- [x] Record passing adapter and reducer tests for complete, unavailable, stale, conflicting, and recovered observations.
- [x] Record passing Operator, Console, typecheck, build, and declared viewport evidence.
- [x] Record passing Kubernetes action shadow, rollback, idempotency, and independent-effect tests; approval remains the shared Var boundary.
- [x] Record passing Terraform format, validation, and scenario-profile tests without applying Azure changes.
- [ ] Retain one separately approved live Azure receipt for the public AKS API, public HTTPS access, degraded business impact, governed recovery, and independently verified recovery.
- [ ] Validate an order-acceptance-only evidence profile with reviewed minimum replicas, observation freshness, failed-order threshold, and explicit RabbitMQ scope without marking missing fulfillment evidence healthy.
- [x] Implement the acceptance-only typed intent, conservative reducer, inert exact-target candidate and receipt-gated Analyzer; preserve synthetic-traffic provenance and verify the canonical Incident path locally.
- [x] Implement independent injected-key receipt issuance and append-only trust activation/revocation with exact replay and current verifier checks; no key mount, job, or database grant is implied.
- [ ] Bind actual observation and receipt-verification adapters, register the acceptance Analyzer, and connect its inert candidate to current shared approval and Thor admission before any live incident or restoration claim.
- [ ] Prove automatic incident creation and duplicate suppression through the existing agent-owned event ingress before an operator question.
- [x] Exercise the commerce adapter through the shared Analyzer publisher, Huginn, Heimdall, and Incident lifecycle with deterministic local evidence; one Incident and replay suppression are proven by `test_coordinator.py` (12 passed). This does not satisfy the live item above.
- [ ] Bind one deployed Executor identity to the exact demo Deployment scale subresource with a reviewed plan and effective permission readback; retain denied out-of-scope checks.
- [ ] Retain current promotion, distinct human approval, all seven safeguards, and independent resource plus order-acceptance observations for one bounded recovery.
