# Agent Pantheon implementation ledger

This delivery ledger preserves reviewable implementation scope, append-only transitions,
and resumable work while the roadmap owner remains focused on normative design.

> **Current handover delivery (2026-09-15):** The second local main merge succeeded and was published
> as `c8edd2769` in [PR #1014](https://github.com/dotnetpower/fdai/pull/1014).
> [CI run 34921323157, attempt 1](https://github.com/dotnetpower/fdai/actions/runs/34921323157/attempts/1) failed on that head.
> Focused source repairs are implemented locally with main-session checks reported below; delivery
> repair remains in progress under [#946](https://github.com/dotnetpower/fdai/issues/946).
> The latest published head remains `c8edd2769`. No new head, whole-repository validation, remote CI
> success, protected merge, deployment, or promotion is claimed; this documentation edit ran no checks.

## Implementation status

### Implementation scope
| Area | State | Evidence | Notes |
|------|-------|----------|-------|
| Handover source-check owner chain | implemented | [Core source binding](../../../services/core-control-plane/src/fdai/runtime/core_handover.py); [semantic owner binding](../../../services/core-control-plane/src/fdai/runtime/handover_semantics.py); [recorded Core and SEM checkpoints](../../internals/handover-lifecycle-hardening-20260914.md) | Current goal/reviewer/admission/retrieval readers feed Huginn, Forseti, Saga, Muninn, Norns, and Mimir on existing topics. Independent source checks and owner-local CAS preserve withdrawal; explicit digest conflicts still require Odin clarification. |
| Norns compilation and Mimir private-package retention | implemented | [Semantic compiler/reviewer](../../../services/core-control-plane/src/fdai/rule_catalog/pipeline/distill/handover_semantics.py); [retention policy](../../../services/core-control-plane/src/fdai/rule_catalog/pipeline/distill/handover_retention.py); [recorded retention evidence: 60 passed](../../internals/handover-lifecycle-hardening-20260914.md#semantic-retention-review-evidence) | Norns retains private typed Rule/ontology candidates behind existing gates. Mimir independently admits sources before content and recompiles without a model, retires monotonically, and scrubs only with exact current legal-hold false proof. Unknown policy or outage never erases. Claim, receipt, digest, and audit survive without catalog, graph, mutation, or promotion authority. |
| Fixed-owner isolated membership and inverse | implemented | [Runtime composition](../../../services/core-control-plane/src/fdai/runtime/human_access_runtime.py); [owner callbacks](../../../services/core-control-plane/src/fdai/runtime/human_access_workflow.py); [Vidar recovery callbacks](../../../services/core-control-plane/src/fdai/runtime/human_access_recovery.py); [main integration checkpoint](../../internals/handover-lifecycle-hardening-20260914.md#main-integration-and-delivery-checkpoint): 26 recorded post-correction isolated and real-SQL recovery/fixed-agent checks | Forseti retains original material, Var joins fresh independent human slots, Muninn prepares by exact CAS, and Thor alone dispatches through the isolated seven-safeguard path. Independent Heimdall evidence and shared release closure precede effect recording. Vidar proposes/finishes separately approved owned inverses under the same target generation; cases remain degraded/supersedable. No role or topic is rebound. |
| Final integrated handover source critique | implemented | [FI-01 through FI-12](../../internals/handover-lifecycle-hardening-20260914.md#final-integrated-critique-after-remaining-source-implementation); [MI-01 through MI-10](../../internals/handover-lifecycle-hardening-20260914.md#final-merge-specific-integrated-critique); [main integration checkpoint](../../internals/handover-lifecycle-hardening-20260914.md#main-integration-and-delivery-checkpoint) | Ten additional merge-specific rounds supplement, rather than recount, EX-01 through EX-20 and FI-01 through FI-12 at the earlier checkpoint. The second local merge is published as `c8edd2769` in PR #1014, but CI attempt 1 failed. Huginn's two typed notices now use separate local variable names; roles and topics are unchanged. Focused local source repair is implemented, not a new published head or passing CI. Reviewed translation refresh, generation, repair hooks/PR update, and exact-head protected CI/merge remain under #946; full UI/assistive and live promotion evidence stay separate. |
| Scoped ownership review and projection | implemented | [Scoped runtime](../../../services/core-control-plane/src/fdai/runtime/scoped_duties.py); [H10 source checkpoint](../../internals/handover-lifecycle-hardening-20260914.md#h10-connected-reviewobservation-checkpoint); [Console ledger](../interfaces/operator-console-module-map.md) | Existing fixed owners review exact user/group/schedule duties, scope, current coverage, and supersession. H10 UI is implemented; neither current schedule coverage nor an ownership review grants personal IAM or future authority. |
| Saga handoff mutation checkpoint | implemented | `agents/{saga.py,_framework/saga_handoff.py,_framework/state_store_issue_tracker.py}`; `runtime/bootstrap_lifecycle.py`; focused Saga and runtime checks | Saga durably checkpoints mutation, audit, and required publication; bus absence remains retryable. The issue adapter keeps closure metadata outside bounded comments and validates the next state before CAS, including at the operation cap. |
| Norns handoff-learning application | implemented | `agents/{norns.py,_framework/norns_issue_dedup.py,_framework/norns_learning.py,_framework/runtime_operational_agents.py,_framework/runtime.py}`; focused Saga, Norns, runtime, and layout checks | Norns journals operation, count, and candidate delivery. Exact pending recovery pauses behind a queued head, while every later successful flush advances that head and continues loading durable candidates behind it. |
| Var approval idempotency lineage | implemented | `agents/var.py`; `agents/_framework/{runtime,execution_safety,runtime_operational_agents,var_decisions}.py`; focused Var and runtime checks | Var joins decisions into an audited CAS aggregate and recovers its outbox at startup. Enforce readiness requires the explicit `var_state_store`, so process-local quorum cannot enter mutation mode. |
| Vidar rollback claim and publication replay | implemented | `agents/vidar.py`; `agents/_framework/{runtime,execution_safety,bus_bridge}.py`; focused Vidar, bridge, and runtime checks | Vidar binds every replay and executor invocation to one stable effect-bearing command projection and owner-token lease. Regenerated delivery metadata is excluded, while command substitution, live claims, and terminal ambiguity fail closed. |
| Fixed registry, roles, and package boundary | implemented | [`pantheon.py`](../../../services/core-control-plane/src/fdai/agents/_framework/pantheon.py), [`test_framework_layout.py`](../../../services/core-control-plane/tests/agents/test_framework_layout.py), [`test_pantheon_doc_parity.py`](../../../services/core-control-plane/tests/agents/test_pantheon_doc_parity.py) | The fixed 15 names, catalog layers, ownership, and public package boundary are machine-checked. |
| Typed pub/sub ownership and concurrent runtime | implemented | [`topics.py`](../../../services/core-control-plane/src/fdai/agents/_framework/topics.py), [`runtime.py`](../../../services/core-control-plane/src/fdai/agents/_framework/runtime.py), [`runtime_subscriptions.py`](../../../services/core-control-plane/src/fdai/agents/_framework/runtime_subscriptions.py), [`test_topics.py`](../../../services/core-control-plane/tests/agents/test_topics.py), [`test_pantheon_concurrency_proof.py`](../../../services/core-control-plane/tests/agents/test_pantheon_concurrency_proof.py) | Focused tests cover topic ownership, partitioning, all 15 consumer identities, and non-stealing fan-out. Runtime composition also injects Norns' default-off candidate publication ceiling. |
| Initial cross-vertical arbitration composition | implemented | `agents/_framework/cross_vertical_candidates.py`; `agents/{forseti,odin,saga,thor}.py`; `tests/agents/test_cross_vertical_composition.py`; frozen `v2026.08` scenario | Loki, Heimdall, and Njord retain distinct candidate identities, Forseti alone raises one request, Odin records `win`/`defer`/`hil`, Saga audits, and Thor alone publishes ActionRuns. |
| Mimir Rule generation accountability | implemented | [`mimir.py`](../../../services/core-control-plane/src/fdai/agents/mimir.py), [`runtime.py`](../../../services/core-control-plane/src/fdai/agents/_framework/runtime.py), [`runtime_subscriptions.py`](../../../services/core-control-plane/src/fdai/agents/_framework/runtime_subscriptions.py), [`test_wave2_governance.py`](../../../services/core-control-plane/tests/agents/test_wave2_governance.py), [`test_runtime.py`](../../../services/core-control-plane/tests/agents/test_runtime.py) | Mimir alone receives activation commands and terminal results. It delegates exact activation to the injected binder and stores projection-only receipts without index, policy, approval, mutation, or execution authority. |
| Rule generation build, validation, and activation chain | implemented | `mimir.py`; `heimdall.py`; `runtime.py`; `runtime/rule_generation_documents.py`; focused worker, runtime, activation, and bootstrap checks | Production startup freezes strict promoted-surface documents, persists a replay-identical reconciliation request, and routes it through Mimir and Heimdall. Mimir binds the exact independent receipt before publishing activation without gaining policy or execution authority. Governed live evidence remains open. |
| Judgment, approval, execution, audit, and recovery separation | implemented | [`test_runtime_chain.py`](../../../services/core-control-plane/tests/agents/test_runtime_chain.py), [`test_decision_case_e2e.py`](../../../services/core-control-plane/tests/agents/test_decision_case_e2e.py), [`test_thor_durable.py`](../../../services/core-control-plane/tests/agents/test_thor_durable.py), [`test_change_management_chain.py`](../../../services/core-control-plane/tests/agents/test_change_management_chain.py) | Forseti optionally resolves an exact proposal into its existing Verdict and derives planned-change graph evidence from one exact verified operational context snapshot rather than a caller freshness boolean. Thor preserves the resulting verdict after independent validation. Synthetic tests prove the role boundary but not a live production outcome. |
| Conversational and handoff mechanics | implemented | [`test_conversational_port.py`](../../../services/core-control-plane/tests/agents/test_conversational_port.py), [`test_prompt_deliberation.py`](../../../services/core-control-plane/tests/agents/test_prompt_deliberation.py), [`test_wave7_workflows.py`](../../../services/core-control-plane/tests/agents/test_wave7_workflows.py) | One shared structured judgment owns routing and action posture. The bounded read-only deliberation path rejects action requests into the typed pipeline, evaluates T1 answer signals before optional T2 synthesis, and never restores lexical fallback. |
| KPI evidence states, promotion checks, and degradation drills | implemented | [`test_wave8_kpi_degradation.py`](../../../services/core-control-plane/tests/agents/test_wave8_kpi_degradation.py) | Missing or unmeasured KPI evidence fails promotion closed, and injected failures exercise declared degradation behavior. |
| Trace-continuity evidence handoff | implemented | `huginn.py`; `heimdall.py`; `test_trace_continuity_chain.py` | Huginn preserves only allowlisted bounded continuity fields, and Heimdall carries a recognized observed reason into the anomaly and Incident candidate. AgentSpec, topics, judgment, approval, and execution authority are unchanged. |
| Heimdall repeated-event idempotency | implemented | `huginn.py`; `heimdall.py`; `test_wave3_pipeline.py`; focused duplicate-evidence, event-time, and recurrence checks in the current change | Huginn preserves validated source event time. Heimdall counts a non-empty Event evidence key once per bounded event-time episode, emits one same-severity candidate during a continuous episode, and assigns a new opaque episode identity after a quiet repeat window. At-least-once or delayed replay cannot manufacture the threshold, and a closed Incident cannot absorb a later episode. AgentSpec and authority remain unchanged. |
| Provider-schema drift handoff | implemented | `provider_schema_review.py`; `heimdall.py`; `test_provider_schema_drift.py` | Heimdall validates one digest-bound, no-authority provider-schema review package and publishes only a bounded shadow `object.drift` signal. Fetching, parsing, baseline storage, policy, judgment, approval, and catalog mutation remain outside the agent. AgentSpec is unchanged. |
| Cost Governance specialist binding | implemented | `njord.py`; `_framework/factory.py`; `test_njord_cost_governance.py` | Composition injects package-neutral cost advisory and activation readers into Njord. Njord remains the sole `CostAnomaly` and `Budget` publisher, Freyr remains the capacity specialist, and disabled packages produce no new cost advisory publication. AgentSpec and topic ownership are unchanged. |
| Capacity graduation recommendation chain | implemented | `core/capacity/graduation.py`; `capacity-graduation-policy.yaml`; `freyr.py`; `forseti.py`; `thor.py`; focused controller and chain tests | Freyr alone publishes content-addressed shadow recommendations from complete capacity evidence plus current Njord cost evidence. Forseti records an observation-only judgment, and Thor explicitly ignores that verdict kind. No topology ActionType or apply authority is added. |
| Evidence-conflict ownership and durable projection | implemented | `core/ontology_platform/evidence_conflict.py`; `delivery/evidence_conflict.py`; `heimdall.py`; `muninn.py`; focused lifecycle, chain, and authority tests | Huginn carries deterministic candidates, Heimdall alone publishes immutable active/resolved revisions, and Muninn owns append-only history plus a CAS current projection. Related ActionTypes are lowered to shadow before normal or HIL-resumed executor I/O. |
| Prospective-lineage ownership and readiness | implemented | `core/operational_planning/prospective_lineage.py`; `delivery/prospective_lineage.py`; `forseti.py`; `muninn.py`; `saga.py`; `thor.py`; focused planning and chain tests | Forseti alone publishes the finalized pre-execution envelope. Muninn materializes the exact subgraph, Saga seals the same digest, Thor preserves both references, and predispatch requires both receipts before executor I/O. |
| Workflow action-attempt lineage | implemented | `agents/thor.py`; `agents/_framework/action_run_lineage.py`; `runtime/t2_route_registry.py`; direct, tool, HIL, observation, and trace projection paths; focused Thor and lifecycle checks | Thor preserves the positive workflow attempt and a supplied action identifier through durable ActionRun replay. Correlation remains distinct and an absent action identifier is never synthesized. |
| Operational-learning case review | implemented | `core/operational_learning/{case_review,patterns,catalog}.py`; `agents/norns.py`; `tests/agents/test_governed_learning_loop.py` | Norns holds incomplete, stale, conflicting, synthetic-live, duplicate, or mixed-release case cohorts before publication. Mimir independently revalidates the same immutable records without gaining promotion authority. |
| Live operational KPI validation and enforce promotion | not-started | [Goals and Metrics](../../roadmap/architecture/goals-and-metrics.md) | No retained live-shadow cohort, operational KPI receipt set, independent promotion review, or actual pantheon enforce promotion is evidenced here. |
### Implementation history
| Date | State | Change | Evidence | Remaining |
|------|-------|--------|----------|-----------|
| 2026-09-15 | implemented | Extracted Var's pending ticket data into its existing private decision-record module while retaining public imports, fields, defaults, mutability, and all approval ownership. | `current change`; failed CI `34925133389`, attempt 1, head `cc73a0789`, job `104241520105`, repository-boundary step; reproduced 817-line violation. Var is now 789 lines and the helper 509; 111 focused layout, approval, shadow, and actual SQL inverse tests pass, along with strict typing and Ruff/format. | The enforced 800-line ceiling is unchanged. Complete exact-head protected CI and merge in #946/#1014; no live approval, deployment, or promotion evidence is implied. |
| 2026-09-14 | implemented | Connected content-free source notices to actual fixed owners and Saga-sealed materialization, with monotonic withdrawal and independent Mimir revalidation. Extracted Forseti's receipt-only assignment boundary without changing its role or registry. | `current change`;37 focused agent/layout/parity tests;14 final source type checks; [review record](../../internals/handover-lifecycle-hardening-20260914.md). | Preserve H10, alternate source bindings, semantic compilation, current provider proof, and independent promotion gates; no live model, IAM, or deployment operation was performed. |
| 2026-09-15 | implemented | Added active durable ActionRun identity preflight before lifecycle-rank suppression, idempotency reservation, resource claim, or execution; extracted claim-time parsing to keep the provider adapter under its LOC ceiling. | `current change`; Issue #977; active-row and cross-replica conflict regressions; strict mypy, Ruff, agent-import, and LOC gates. | Resolve the two Low-severity residuals tracked in the focused implementation owner. |
| 2026-09-15 | withdrawn | Withdrew inactive-row generation replacement and made ActionRun correlation globally immutable across live Thor state, Var ticket claims, durable tombstones, terminal deletion, and released resource claims. | `current change`; Issue #977; live/durable reuse, legacy tombstone, released-claim, stale authority, and shadow restart regressions passed 300 tests; strict mypy, Ruff, agent-import, and LOC gates. | Resolve the two Low-severity residuals tracked in the focused implementation owner. |
| 2026-09-15 | implemented | Made inactive Thor ActionRun tombstones generation-aware so a distinct stable idempotency generation can CAS-replace a reused correlation while the completed generation remains suppressed. | `current change`; Issue #977; tombstone replacement and reused-correlation partial-quorum restart regressions; strict mypy, Ruff, agent-import, and LOC gates. | Resolve the two Low-severity residuals tracked in the focused implementation owner. |
| 2026-09-15 | implemented | Persisted production Thor ActionRuns in shadow and enforce modes so durable Var partial quorum and its matching HIL run survive restart together. | `current change`; Issue #977; partial-quorum restart plus runtime/bootstrap suites passed 137 tests; strict mypy, Ruff, agent-import, and LOC gates. | Resolve the two Low-severity residuals tracked in the focused implementation owner. |
| 2026-09-15 | implemented | Added lifecycle-stable ActionRun identity to Thor, Var, and Vidar authority messages, identity-scoped durable approval and rollback records, and stale-correlation rejection. | `current change`; Issue #977; focused identity, correlation-reuse, and full Agent regressions; strict mypy, Ruff, agent-import, and LOC gates. | Persist Thor HIL runs in shadow mode, then resolve the two Low-severity residuals tracked in the focused implementation owner. |
| 2026-09-15 | implemented | Moved detailed durability, restart, typed-handoff, and conversational re-entry contracts from the legacy oversized Pantheon owner into the existing focused implementation owner while preserving the fixed role summary and links. | `current change`; paired owner documents, document-size and design-route gates, and the already passing 1,372-test campaign aggregate. | Resolve the two Low-severity Saga checkpoint and issue-operation identity residuals tracked in the focused implementation owner. |
| 2026-09-14 | implemented | Replaced producer-controlled ingestion bounds with Huginn's injected timezone-aware UTC clock. A producer cannot authorize its own future source timestamp by omitting or future-dating `ingested_at`. | `current change`; `huginn.py`; focused trusted-clock and future-time regressions. | Retain a governed future-time rejection receipt. |
| 2026-09-14 | implemented | Added a bounded anomaly idempotency key derived from episode identity and severity. Retrying an unaccepted threshold can no longer duplicate downstream judgment while a severity escalation remains a distinct publication. | `current change`; `heimdall.py`; focused anomaly-key and retry regressions. | Retain a governed broker-redelivery receipt through Forseti. |
| 2026-09-14 | implemented | Allowed duplicate delivery to retry a repeat threshold only when its anomaly publication or Incident handoff remains unaccepted. The duplicate never increments the evidence count, and an accepted episode remains suppressed. | `current change`; `heimdall.py`; focused transient-handoff duplicate regression. | Retain a governed broker-redelivery recovery receipt. |
| 2026-09-14 | implemented | Rejected a source Event time later than its supplied ingestion time before Huginn publication. A future timestamp can no longer evict valid Heimdall repeat history or suppress later detection. | `current change`; `huginn.py`; focused timestamp-boundary regression. | No remaining implementation work for this timestamp ordering boundary. |
| 2026-09-14 | implemented | Changed repeated-event evaluation to use Huginn's validated source Event time when available, with arrival time isolated as the legacy fallback. Rapid replay of historically distant Events can no longer look like a current burst. | `current change`; `huginn.py`; `heimdall.py`; focused normalization and replay-window tests. | Retain a governed delayed-replay receipt before claiming deployed validation. |
| 2026-09-14 | implemented | Added bounded episode identity and accepted-episode state to Heimdall. A continuous burst emits one candidate unless severity increases, while a burst after a quiet repeat window receives a distinct Incident identity. | `current change`; `heimdall.py`; focused episode, severity-escalation, and closed-Incident recurrence tests. | Retain a governed recurrence receipt after closing a prior Incident. |
| 2026-09-14 | implemented | Made Heimdall's repeated-event threshold count each non-empty idempotency key at most once inside an episode. Duplicate delivery now records a bounded behavior outcome and cannot create a candidate. | `current change`; `heimdall.py`; focused Wave 3 and framework-layout checks. | Retain a governed broker-redelivery receipt before claiming deployed validation. |
| 2026-09-15 | implemented | Added an explicit `var_state_store` runtime binding, required it independently in enforce readiness, removed the Muninn-store fallback, and bound the production durable store through the exact parameter. | `current change`; Issue #977; 249 focused enforce-readiness, Var outbox, T2 recovery-chain, runtime wiring, bootstrap, catalog, and framework-layout checks; 170 Cost package boundary checks with one environment skip; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for enforce-mode Var durability. |
| 2026-09-15 | implemented | Replaced whole-envelope Vidar hashing with one stable effect-bearing rollback-command projection used identically for digesting and executor input. Regenerated `terminal_at` and envelope metadata no longer collide, while params and lineage substitution still fails closed. | `current change`; Issue #977; 181 focused Vidar terminal-replay, command-substitution, T2 recovery-chain, runtime, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for stable rollback-command projection. |
| 2026-09-15 | implemented | Moved StateStore issue closure metadata out of the bounded occurrence-comment list and validated the generated next state before CAS. Closing exactly at the operation cap now remains canonical and restart-readable. | `current change`; Issue #977; 240 focused issue-cap, close, rehydrate, restart, runtime, bootstrap, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for issue-close capacity consistency. |
| 2026-09-15 | implemented | Required successful `object.issue` publication before Saga seals a handoff completion receipt. A busless typed handoff now retains mutation and audit checkpoints, fails retryably, and resumes publication after bus binding and redelivery. | `current change`; Issue #977; 239 focused Saga late-bus, redelivery, restart, runtime-builder, bootstrap, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for handoff publication completion. |
| 2026-09-15 | implemented | Made the common Norns flush iteratively recover the next durable pending candidate after each publish or hold. A gate, bus, or rate-limited head pauses safely, and the next flush continues candidates behind it instead of requiring another restart. | `current change`; Issue #977; 188 focused Norns blocked-head, multi-candidate, public-flush, workflow, runtime, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for queued-head Norns recovery continuation. |
| 2026-09-15 | implemented | Added an explicit `vidar_state_store` runtime binding, required it independently in enforce readiness, removed the unrelated Muninn-store fallback, and bound the production durable store through the exact parameter. | `current change`; Issue #977; 241 focused enforce-readiness, runtime wiring, Vidar restart, bootstrap, catalog, and framework-layout checks; 170 Cost package boundary checks with one environment skip; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for enforce-mode Vidar durability. |
| 2026-09-15 | implemented | Removed Saga's unused process-local completion receipt mirror in durable mode and applied the same deterministic LRU issue-projection cap during live mutation and startup rehydration. Eviction leaves durable StateStore issue and operation records intact. | `current change`; Issue #977; 236 focused Saga durable-local receipt, live/startup projection bound, runtime, bootstrap, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for Saga process-memory bounds. |
| 2026-09-15 | implemented | Replaced Norns newest-first startup scans with exact-field pending queries. Pending operations advance before the next query, candidates recover and flush one at a time, terminal history cannot displace them, and exceeding the intentional bound fails explicitly. | `current change`; Issue #977; 185 focused Norns no-scan, multi-candidate startup, pending-operation, workflow, runtime, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for pending-only Norns startup recovery. |
| 2026-09-15 | implemented | Added bounded Var startup recovery for terminal decision aggregates and unpublished final approvals. Exact-field pending queries reconstruct deterministic final payloads and drain the outbox before consumers without requiring a repeated human decision. | `current change`; Issue #977; 193 focused Var terminal-CAS crash, final-checkpoint crash, quorum, runtime-startup, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for automatic Var approval outbox recovery. |
| 2026-09-15 | implemented | Replaced the runtime's ephemeral Saga issue adapter with a StateStore-backed CAS adapter that persists current issue state and exact operation results, including pre-checkpoint crash replay, and rehydrates before consumers start. | `current change`; Issue #977; 233 focused Saga, runtime-builder, pre-checkpoint restart, projection-rehydrate, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | A live GitHub adapter must preserve the same durable provider-side operation contract. |
| 2026-09-15 | implemented | Added bounded Norns startup recovery for pending issue operations and candidates before consumers start, and moved durable delivery completion into the shared flush implementation used by typed handlers, startup, and public batch ticks. | `current change`; Issue #977; 159 focused Norns pending-operation, pending-candidate, public-flush, runtime-startup, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for Norns pending-candidate startup recovery. |
| 2026-09-15 | implemented | Stored the complete rollback-command digest beside each in-process Vidar cache entry and validated it before publication replay. A changed same-process delivery now collides exactly as it does after restart. | `current change`; Issue #977; 97 focused Vidar cache/durable identity and framework-layout checks; strict mypy, Ruff, and agent-import gates. | No source-level work remains for cache-bound rollback identity. |
| 2026-09-15 | implemented | Made Vidar terminal replay validate the complete canonical schema, revision, command and fencing identities, bounded fields, state, and receipt invariants. `succeeded` without a non-empty bounded rollback receipt now fails closed before Thor can release the resource. | `current change`; Issue #977; 97 focused Vidar malformed-terminal, success-receipt, and framework-layout checks; strict mypy, Ruff, and agent-import gates. | No source-level work remains for canonical rollback terminal validation. |
| 2026-09-15 | implemented | Expanded Vidar's rollback request digest to the canonical complete failed ActionRun supplied to the executor. Params, action identity, workflow lineage, rollback data, or any other input substitution now collides instead of replaying an unrelated terminal receipt. | `current change`; Issue #977; 95 focused Vidar command-identity and framework-layout checks; strict mypy, Ruff, and agent-import gates. | No source-level work remains for complete rollback-command identity. |
| 2026-09-15 | implemented | Extended Norns issue dedup into a resumable application journal. Operation claims remain pending until a CAS-persisted fingerprint count is applied, candidate state remains pending until publish or deterministic hold, and restart rebuilds incomplete work without duplicate evidence. | `current change`; Issue #977; 180 focused Saga, Norns, workflow, interrupted-claim, count-restart, candidate-rebuild, runtime, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for durable handoff-learning application. |
| 2026-09-15 | implemented | Made an unexpired Vidar rollback claim a retryable handler failure instead of a successful no-op. EventBusBridge retry or DLQ now retains the failed ActionRun until owner completion or lease-expired redrive can close it. | `current change`; Issue #977; 140 focused Vidar, EventBusBridge, and framework-layout checks; strict mypy, Ruff, and agent-import gates. | No source-level work remains for durable live-claim redrive. |
| 2026-09-15 | implemented | Added a durable Norns claim for Saga's stable `object.issue` idempotency key before fingerprint learning. Accepted-then-timeout replay no longer increments the occurrence counter twice, and operation/fingerprint collisions fail closed across restart. | `current change`; Issue #977; 177 focused Saga, Norns, workflow, runtime, accepted-timeout, restart, catalog, and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for handoff-learning replay deduplication. |
| 2026-09-15 | implemented | Added an audited CAS decision aggregate for Var. Each normalized principal decision is immutable and ticket-bound, so pre-final quorum survives restart, combines across replicas, and rejects decision replacement or ticket collision before one deterministic final approval is stored. | `current change`; Issue #977; 165 focused Var, restart, replica, collision, runtime, catalog, and framework-layout checks; strict mypy and Ruff. | No source-level work remains for durable pre-final approval quorum. |
| 2026-09-15 | implemented | Added a bounded owner-token lease to Vidar rollback claims. A concurrent replica now leaves a live claim untouched, verified expiry is required before `execution_unknown`, and the terminal revision CAS fences late owner completion. | `current change`; Issue #977; 91 focused Vidar and framework-layout checks; strict mypy, Ruff, agent-import, and LOC gates. | No source-level work remains for live rollback-claim fencing. |
| 2026-09-15 | implemented | Closed Saga's mutation-to-checkpoint crash window with a stable provider operation id, durable claim, validated checkpoint and completion receipt, and runtime StateStore injection. The additive idempotent adapter preserves the legacy direct-escalation Protocol while typed handoffs fail closed without provider-side replay protection. | `current change`; Issue #977; 102 focused Saga, async-adapter, runtime, catalog, and framework-layout checks; strict mypy and Ruff. | No source-level work remains for durable handoff mutation replay. |
| 2026-09-14 | implemented | Added a StateStore-backed Vidar rollback claim, terminal transition, publication receipt, and process-local serialization. Restart or claim ambiguity resolves to `execution_unknown` without repeating provider recovery. | `current change`; Issue #977; focused Vidar concurrency, interrupted-claim, restart, runtime, and framework-layout checks. | No source-level work remains for durable rollback claim replay. |
| 2026-09-14 | implemented | Added a correlation-serialized Var finalization outbox, atomically stored final and publication receipts, and injected the runtime StateStore so publish failure, concurrency, and restart replay one canonical approval. | `current change`; Issue #977; focused Var concurrency, retry, restart, runtime, and framework-layout checks. | No source-level work remains for Var final approval replay. |
| 2026-09-14 | implemented | Split Saga handoff materialization into mutation, audit, and publication checkpoints with stable operation ids, so an audit or bus retry does not add a duplicate GitHub comment. | `current change`; Issue #977; focused Saga and framework-layout checks. | No source-level work remains for handoff mutation replay. |
| 2026-09-14 | implemented | Carried the source ActionRun idempotency key into Var's common approval envelope and rejected malformed explicit keys before ticket creation. | `current change`; Issue #977; focused Var and framework-layout checks. | No source-level work remains for approval idempotency lineage. |
| 2026-09-14 | implemented | Split Vidar's completed rollback state from broker publication completion, allowing a failed `object.rollback` publish to retry with the same idempotency key without rerunning the provider rollback. | `current change`; Issue #977; focused Vidar and framework-layout checks. | No source-level work remains for rollback publication replay. |
| 2026-09-14 | implemented | Preserved the optional action identifier and four-field workflow reference, including its positive attempt, through Thor's in-memory and durable ActionRun forms. T2 route audit can now retain the same lineage when supplied without changing Thor's sole-executor role or any ActionType binding. | `current change`; `agents/thor.py`; `runtime/t2_route_registry.py`; focused ActionRun round-trip, T2 route, and framework-layout checks. | Retain one deployed ActionRun trace that carries the lineage through independent effect observation and recovery. |
| 2026-08-31 | implemented | Bound Norns and Mimir to the same immutable operational-case review contract while preserving Norns as candidate writer and Mimir as rule steward. Publication remains inert and the authoritative registry requires independent reviewed replay. | `current change`; Story #370 focused agent and governed-loop checks. | Live operational validation remains with the operational-learning owner. |
| 2026-08-31 | implemented | Added the initial three-vertical candidate join without changing the fixed 15 names or role bindings. Duplicate, reordered, concurrent, timeout, rollback, and partial-subscriber paths fail closed, and the complete frozen scenario remains shadow-only. | `current change`; focused Story #349 regression passed 103 cases, including layout and pantheon parity. | Live-authoritative Phase 3 outcome evidence remains outside this local implementation claim. |
| 2026-08-30 | implemented | Added Forseti-owned ProspectiveLineage without changing judge, approver, executor, auditor, or recovery assignments. Odin's winning option is finalized with typed arguments before Process recording; Muninn and Saga independently close materialization readiness; Thor preserves but cannot self-create the evidence. | `current change`; focused planning, finalizer, materializer, Saga seal, Thor preservation, and kinetic readiness checks. | Retain one governed pinned prospective-to-observed replay after binding the independent observer. |
| 2026-08-30 | implemented | Added the generic EvidenceConflict lifecycle without changing the fixed roster. Heimdall publishes exact-generation active/resolved revisions, Muninn advances one durable current slot only through supersession, and expiry never clears an unresolved conflict. Canonical Property semantic intersections lower only related ActionTypes before normal and HIL-resumed executor I/O. | `current change`; focused conflict lifecycle and agent-chain checks passed 7 cases; authority checks passed 2 cases; read-shadow and composition checks passed 24 cases; ontology, topic, Ruff, and mypy checks passed. | Add further semantic dependencies only when another authoritative conflict producer exists for those properties. |
| 2026-08-30 | implemented | Added Freyr's capacity-first graduation recommendation chain without changing the fixed roster or execution roles. The versioned policy covers four optional transitions; missing, stale, incomplete, synthetic, or over-budget evidence holds. Forseti judges only in observation mode and Thor cannot dispatch the result. | `current change`; controller checks passed 7 cases; Freyr-Forseti-Thor runtime checks passed 3 cases; specialist checks passed 30 cases; ontology and topic checks passed. | Retain exact-revision transition evidence and add a separately reviewed ActionType before any topology apply path exists. |
| 2026-08-30 | implemented | Carried the resolved autonomy ceiling into Thor's durable ActionRun and enforced it at dispatch, approval, execution, and restart. Added a Saga-owned pre-execution intent receipt, authorized and expiring approval, monotonic CAS lifecycle persistence, permanent completion deduplication, distributed resource locking, persistent single-use claims, terminal replay, and `execution_unknown` restart handling. Command acceptance remains explicitly pending independent effect verification. | `current change`; 28 critique and hardening rounds; focused Thor, Var, Pantheon runtime, provider, bootstrap, and operational-context checks. | Retain the governed live-shadow, independent effect, degradation, KPI, and promotion evidence below. |
| 2026-08-28 | implemented | Made the bounded Njord scope-list contract exercise accepted package-backed samples instead of the intentionally inert package-absent path. | `current change`; focused conversational-port and Cost Governance Njord tests. | Live-authoritative evidence remains required before promotion. |
| 2026-08-28 | implemented | Restored Njord's declared read-tool fact scopes after optional package injection while keeping disabled ingestion inert and moving legacy anomaly tests to an explicitly enabled package provider. | `current change`; focused arbitration, charter, conversation-tool, and Cost Governance Njord tests. | Retain live-authoritative Njord evidence before any promotion review. |
| 2026-08-29 | implemented | Added optional Cost Governance advisory injection without changing the fixed roster, AgentSpec, topic ownership, or authority roles. | `current change`; focused Njord, pantheon layout, ownership, disablement, and drain tests. | Retain a live-authoritative Cost Governance cohort before any enforce promotion. |
| 2026-08-29 | implemented | Closed the shadow-review evidence path through Var, Saga, and Norns without changing AgentSpec, topics, ownership, model policy, or action authority. Var records a distinct human comparison, Saga republishes the owned audit entry, and Norns upgrades the original dwell sample by stable id instead of counting another sample. | `current change`; `var.py`; `saga.py`; `norns_learning.py`; focused discovery shadow-review, dwell, and framework-layout checks. | Retain a governed live review cohort before any promotion decision. |
| 2026-08-21 | implemented | Extracted content-addressed Bragi Turn and handoff payload construction into a private publication helper and moved semantic capability projection into the existing routing helper solely to restore the framework line ceiling. Bragi still publishes the same `object.turn` and `object.handoff-escalation` topics; AgentSpec, ownership, LLM policy, typed proposal entry, and action authority are unchanged. | `current change`; focused Bragi and framework checks passed 95 cases; changed-file Ruff, format, and mypy passed; the repository LOC gate passed with zero hard failures. | Retain the same governed operator-path, KPI, and promotion evidence already required by this design. |
| 2026-08-13 | in-progress | Adopted an evidence-bounded implementation ledger without reconstructing earlier delivery history. | current change | Collect live operational evidence and complete an independently reviewed promotion before claiming validation or enforce use. |
| 2026-08-19 | implemented | Bound Heimdall's remaining production security-correlation thresholds to bounded startup settings without changing its AgentSpec, topics, owned objects, deterministic hot path, or authority. Raw Huginn ingress handler construction moved to the private subscription-wiring module solely to preserve the framework line ceiling; normalization and drop semantics are unchanged. | [Issue #219](https://github.com/dotnetpower/fdai/issues/219); focused settings, Heimdall injection, framework layout, raw-ingress, and threshold-source checks pass 134 cases. | Runtime exit-gate evidence and independent promotion outcomes remain unchanged. |
| 2026-08-19 | implemented | Bound Norns' inert candidate publication to the default-off discovery activation decision without changing its AgentSpec, topics, ownership, LLM policy, or authority. A closed gate retains the bounded pending queue for later evaluation. | `current change`; focused discovery activation, Norns, startup wiring, collector, and infrastructure checks. | Retain governed collector and activation-transition receipts before claiming operational validation. |
| 2026-08-13 | implemented | Bound validated Rule generation command ingress and terminal-result accountability to Mimir without widening its authority. | `current change`; focused Mimir, runtime, bootstrap, activation, and publication checks passed 32 cases. | Retain a governed live activation-result receipt before claiming operational validation. |
| 2026-08-13 | implemented | Extracted private Rule-generation subscription wiring to restore the runtime module's framework-layout boundary without changing topics, ownership, or authority. | [`runtime_subscriptions.py`](../../../services/core-control-plane/src/fdai/agents/_framework/runtime_subscriptions.py); framework layout, runtime, and pantheon parity checks passed. | Retain the same governed live evidence required by the implementation scope. |
| 2026-08-13 | in-progress | Assigned the pre-activation build chain to Mimir and independent generation validation to Heimdall without granting either agent activation or execution authority. | `current change`; focused ownership, handler, parity, and runtime wiring checks passed 221 tests; the exact chain and forged/unbound checks passed. | Add the production catalog-reconciliation trigger and Mimir activation-command publication. |
| 2026-08-13 | implemented | Completed the production Rule generation chain with authoritative embedding identity, strict startup document snapshots, replay-identical reconciliation requests, exact staged-receipt binding, and Mimir-owned activation-command publication after Heimdall validation. | `current change`; `rule_generation_documents.py`, `mimir.py`, `activation.py`, semantic-index adapters, and focused worker, runtime, activation, and bootstrap checks. | Retain governed live build, validation, activation, and projection receipts before claiming operational validation. |
| 2026-08-14 | implemented | Made Thor validate and durably preserve an optional argument-bound kinetic proposal without changing topics or action authority. | `current change`; focused contract, Thor, durable replay, layout, and role checks. | Bind the Forseti producer and Core pre-dispatch consumer before claiming an end-to-end kinetic handoff. |
| 2026-08-14 | implemented | Injected an optional exact proposal source into Forseti and preserved matching proposals on resolved and human-review Verdicts. Missing proposals retain legacy behavior; invalid source records lower authority to deny. | `current change`; `forseti.py`, `factory.py`, `runtime.py`, and focused producer, Forseti, Thor, factory, and framework checks. | Bind the source in production composition and complete the pre-dispatch receipt and independent-observation path before claiming runtime validation. |
| 2026-08-14 | implemented | Bound the Core pre-dispatch kinetic receipt consumer before every Thor-owned executor without changing AgentSpec, topics, judgment, approval, or execution ownership. | `current change`; `core/operational_planning/kinetic_safety.py`, `delivery/kinetic_safety.py`, `runtime/control_loop.py`, and focused kinetic, HIL, and Thor composition checks passed 115 cases. | Bind the Forseti source in production composition, add the independent observer, and retain governed live evidence. |
| 2026-08-14 | implemented | Required deterministic T1 answer evaluation before Bragi's presentation-only deliberation can invoke optional T2 synthesis. | `current change`; 36 focused deliberation tests prove T2 is not called for conflict-free or uncomparable claims and remains bounded for a structured conflict. | Retain governed operator-path evidence without changing AgentSpec, topics, or authority. |
| 2026-08-15 | implemented | Made each bridge consumer close its subscription inside its own task so broker teardown runs during shutdown instead of interpreter finalization. No AgentSpec, topic, ownership, LLM, or safeguard change. | `current change`; [`bus_bridge.py`](../../../services/core-control-plane/src/fdai/agents/_framework/bus_bridge.py) and [`test_subscription_lifecycle.py`](../../../services/core-control-plane/tests/agents/test_subscription_lifecycle.py); focused agent, delivery, runtime, and provider checks passed 3127 cases. | Confirm the residual consumer stop timeout is absent from a live local shutdown. |
| 2026-08-16 | implemented | Made Thor apply the dispatch-time kinetic proposal binding again when it rehydrates a durable ActionRun, so a tampered row cannot restore another correlation's exact arguments. No AgentSpec, topic, ownership, or authority change. | `current change`; [`thor.py`](../../../services/core-control-plane/src/fdai/agents/thor.py) and [`test_thor_durable.py`](../../../services/core-control-plane/tests/agents/test_thor_durable.py); focused kinetic, durable-replay, runtime, and factory checks passed 140 cases. | Retain governed live evidence for the rehydration path alongside the existing kinetic remaining work. |
| 2026-08-17 | implemented | Preserved bounded distributed-trace continuity evidence from Huginn normalization through Heimdall's repeated-event anomaly and Incident candidate. Unknown reason codes and forged action fields are dropped, and the sensing path gains no judgment or action authority. | `current change`; `test_trace_continuity_chain.py`; focused trace-to-Incident chain passed. | Retain governed live trace-discontinuity, Incident, approval, and recovery evidence in issue #142. |
| 2026-08-21 | implemented | Completed the semantic-judgment migration regressions without restoring phrase-based routing. Bragi uses the shared judgment boundary for operator routing, read-investigation ownership, action posture, and read-only deliberation; unavailable or rejected meaning fails closed. AgentSpec, topics, ownership, model policy, and action authority are unchanged. | `current change`; focused agent, deliberation, read-investigation, and semantic-tool checks passed 304 cases; diff-scoped changed tests passed 3176 cases with 7 environment-gated skips. | Retain governed operator-path evidence and the existing live KPI and promotion evidence; no authority or promotion state changed. |
| 2026-08-23 | implemented | Added Heimdall's read-only terminal `object.action-run` subscription. The handler restores exact correlation-indexed pre-dispatch artifacts, binds collection to Thor's terminal timestamp, invokes an injected independent collector, and writes only verifier-accepted post-terminal observations. Heimdall ownership, publications, deterministic hot path, LLM policy, and action authority remain unchanged. | `current change`; Pantheon parity, runtime topic, handler, artifact, Azure collector, and composition checks. | Bind the deployment-owned signed-context issuer and retain a governed live closure receipt. |
| 2026-08-24 | implemented | Bound strict provider-schema review packages to Heimdall's existing `Drift` ownership. The scheduled composition uses the production Pantheon bridge, rejects extra or authority-bearing fields before transport, emits a digest-only shadow signal, and makes no catalog change. | `current change`; focused provider-schema review, Heimdall ownership, framework-layout, durable-ledger, and infrastructure checks. | Retain Saga-audited drift evidence from a deployed revision before claiming operational validation. |
| 2026-09-15 | implemented | Superseded earlier missing Core binding, H10, semantic-retention, isolated-executor, target-lock, and effect-ingress claims with the connected owner paths above. Original Action/HIL identity, preparation CAS, current kill/health and principal-ActionType policy, one durable dispatch intent, and independently observed fresh inverse remain enforced. | `current change`; [execution checkpoint](../../internals/handover-lifecycle-hardening-20260914.md#execution-source-critique-checkpoint): recorded 132 owning execution tests and a separate 21-test actual loopback SQL/fixed-agent selection; they overlap and are not summed. The same record retains Core/SEM, retention 60, and H10 UI 95 unit/6 Playwright evidence at their respective checkpoints. Provider HTTP and Owner observations were synthetic. | Final integrated critiques, latest static/docs reconciliation, and delivery remain in progress. Live identity, Graph, GitHub, Teams, drills, cohorts, and promotion remain blocked under #458. All new ActionTypes stay shadow-default; no local authority cutover or whole-task/production validation is claimed. |
| 2026-09-15 | implemented | Completed 12 distinct final integrated critique rounds after remaining source implementation; no unresolved confirmed Medium/High source finding. Fixed names, role ownership, and authority boundaries remain unchanged. | `current change`; [FI-01 through FI-12](../../internals/handover-lifecycle-hardening-20260914.md#final-integrated-critique-after-remaining-source-implementation); 92 SQL/fixed-agent/migration and 136 HIL/bootstrap/layout/doc-parity checks, overlapping and not summed. | Translation SHA refresh, generation, hooks, publication/CI, full UI/assistive evidence, and live identity/effect/drill/cohort/promotion receipts remain pending; #458 stays open. |
| 2026-09-15 | implemented | Reconciled current fixed-owner and documentation status with local main integration while preserving earlier history. Direct Executor audit retains `actor=fdai_executor_service.effect_executor` and `owner_agent=Thor` without inventing `producer_principal`; roles and topics are unchanged. | `current change`; [main integration checkpoint](../../internals/handover-lifecycle-hardening-20260914.md#main-integration-and-delivery-checkpoint) records normal hooks passing for earlier local commit `342576571`, ten additional MI rounds, and 26 post-correction isolated/real-SQL checks. Local EN/KO synchronization and canonical 14-record generation are complete. | Current integration is uncommitted and unpushed; integration hooks, publication, and exact-head protected CI/merge remain under [#946](https://github.com/dotnetpower/fdai/issues/946). Full UI/assistive evidence and [#458](https://github.com/dotnetpower/fdai/issues/458) remain open. |
| 2026-09-15 | in-progress | Recorded existing handover source publication and the incomplete second merge after main's agent-authority/replay, conversation-assurance, and Teams-onboarding changes. Preserved both parents' history and the combined original-Action/HIL, independent-effect/inverse, private-semantic, and durable owner contracts. | `current change`; [PR #1014](https://github.com/dotnetpower/fdai/pull/1014); source parent `34d536fca892338ff772f0dcbcbeaeb96c3dcc3d` and main parent `e7433259baac0373726ced890645d808d69a9b54`. Reported successful [CI run 34918409337](https://github.com/dotnetpower/fdai/actions/runs/34918409337) belongs only to the prior main parent. This documentation edit adds no validation evidence. | Complete second-merge semantic translation review before SHA refresh, canonical generation, normal integration hooks, and the existing-PR update; retain exact-new-head protected CI/merge under [#946](https://github.com/dotnetpower/fdai/issues/946). Full UI/assistive evidence and #458 operational gates remain open. |
| 2026-09-15 | implemented | Corrected the incomplete-merge status: `c8edd2769` was published in PR #1014, but CI 34921323157 attempt 1 failed. Local Huginn repair uses separate variables for its two typed notices; fixed roles and topics are unchanged. | `current change`; [Huginn](../../../services/core-control-plane/src/fdai/agents/huginn.py); main session reports regular mypy passing for Huginn and [Core bootstrap](../../../services/core-control-plane/src/fdai/runtime/bootstrap_core.py), 2 files with normal import processing, not silent imports. | Local repair only; latest published head remains `c8edd2769`. Reviewed refresh/generation, repair hooks/publication, and exact-head protected CI/merge remain under #946; deployment and #458 operational gates remain open. |
### Remaining work
- [x] Complete 12 distinct final integrated source critiques after remaining implementation;
  [FI-01 through FI-12](../../internals/handover-lifecycle-hardening-20260914.md#final-integrated-critique-after-remaining-source-implementation), not earlier execution-specific rounds, close the source review with no unresolved confirmed Medium/High finding. The later [MI-01 through MI-10](../../internals/handover-lifecycle-hardening-20260914.md#final-merge-specific-integrated-critique) add ten distinct prior-merge local integration rounds without recounting EX or FI evidence; none is new evidence for this second merge.
- [x] Complete the prior-merge local semantic EN/KO review, translation SHA refresh, and canonical
  14-record catalog generation, as recorded at the [main integration checkpoint](../../internals/handover-lifecycle-hardening-20260914.md#main-integration-and-delivery-checkpoint), not for the current second-merge bytes.
- [ ] Complete semantic review of the local CI-repair translations, then let the main implementation
  session refresh only reviewed translation SHAs and regenerate the canonical catalog. This edit leaves SHAs and generated artifacts untouched.
- [ ] Commit the local CI repairs through normal hooks, update the already published
  [PR #1014](https://github.com/dotnetpower/fdai/pull/1014), and retain passing exact-head protected CI and merge evidence under [#946](https://github.com/dotnetpower/fdai/issues/946). The second merge is already published as `c8edd2769`; its failed attempt 1 and focused local repair checks do not establish delivery completion.
- [ ] Retain complete UI/assistive evidence in the [handover plan](../../roadmap/interfaces/human-agent-assignment-implementation-plan.md#current-change-evidence-and-remaining-scope); bounded synthetic route checks do not close it.
- [ ] Retain current deployed identity, Graph/GitHub/Teams, independent effect and inverse drills,
  and promotion/cohort evidence under [#458](https://github.com/dotnetpower/fdai/issues/458). Empty readiness `source_gaps` does not change `shadow` or `operationally_ready=false`.
- [ ] Retain a live-shadow cohort with measured per-agent and system KPIs, sample counts,
  confidence intervals, guard metrics, and authoritative outcome receipts on one pinned revision.
- [ ] Demonstrate the declared degradation behavior against operational dependencies rather than
  only injected failures, without widening any agent's authority.
- [x] Bind the argument-bound kinetic proposal producer and optional Forseti source through the existing Verdict path without changing AgentSpec, ownership, subscriptions, or publications.
- [x] Complete the Core pre-dispatch receipt consumer before every Thor-owned executor without changing AgentSpec, topics, or agent authority.
- [x] Bind the independent observer through the existing ActionRun topic without changing execution authority.
- [ ] Bind the deployment-owned signed-context issuer and remaining Forseti lineage properties, then retain governed live evidence.
- [x] Complete the Rule generation chain with a production trigger, exact Heimdall receipt binding,
  and Mimir-owned activation publication; focused checks reach `activated` without added authority.
- [ ] Complete an independent promotion review for each eligible capability and retain the
  authoritative promotion receipt before reporting enforce operation.
