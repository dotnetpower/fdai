{
  // FDAI developer tasks. The two folderOpen tasks make trunk-based
  // (no-branch) collaboration safe out of the box:
  //   - "hooks: install" wires the shared pre-commit and pre-push gates.
  //   - "git: auto-pull" keeps local `main` close to the remote so
  //     conflicts surface early. It only rebases a clean working tree.
  // First-open note: VS Code asks once to allow automatic tasks
  // ("Manage Automatic Tasks" -> Allow). Trust the workspace to enable.
  "version": "2.0.0",
  "tasks": [
    {
      "label": "hooks: install (core.hooksPath)",
      "type": "shell",
      "command": "git config core.hooksPath .githooks && chmod +x .githooks/* scripts/git-auto-pull.sh 2>/dev/null; echo 'pre-commit and pre-push hooks installed (core.hooksPath=.githooks)'",
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "panel": "shared",
        "close": true
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    },
    {
      "label": "git: auto-pull (background)",
      "type": "shell",
      "command": "bash scripts/git-auto-pull.sh",
      "isBackground": true,
      "problemMatcher": [],
      "presentation": {
        "reveal": "never",
        "panel": "dedicated",
        "group": "git-auto-pull"
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    },
    {
      "label": "git: pull now (rebase, autostash)",
      "type": "shell",
      "command": "git pull --rebase --autostash",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "focus": true
      }
    }
  ]
}
