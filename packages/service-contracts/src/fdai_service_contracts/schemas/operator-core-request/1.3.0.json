{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fdai.dev/service-contracts/operator-core-request/1.3.0",
  "title": "OperatorCoreRequestEnvelope",
  "description": "Transport-facing governed request metadata with an optional bounded semantic turn. It grants no execution authority.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "request_id",
    "correlation_id",
    "idempotency_key",
    "resource_ref",
    "request_kind",
    "requested_at"
  ],
  "properties": {
    "schema_version": {"type": "string", "pattern": "^1\\.\\d+\\.\\d+$"},
    "request_id": {"type": "string", "format": "uuid"},
    "correlation_id": {"type": "string", "minLength": 1, "maxLength": 512},
    "idempotency_key": {"type": "string", "minLength": 1, "maxLength": 512},
    "resource_ref": {"type": "string", "minLength": 1, "maxLength": 512},
    "request_kind": {"type": "string", "enum": ["query", "proposal", "semantic_query"]},
    "requested_at": {"type": "string", "format": "date-time"},
    "context_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "semantic_turn": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "utterance",
        "principal",
        "session_id",
        "turn_id",
        "turn_sequence",
        "locale",
        "purpose",
        "deadline_at",
        "prior_turns",
        "execution_authority"
      ],
      "properties": {
        "utterance": {"type": "string", "minLength": 1, "maxLength": 32000},
        "principal": {
          "type": "object",
          "additionalProperties": false,
          "required": ["subject_id", "roles"],
          "properties": {
            "subject_id": {"type": "string", "minLength": 1, "maxLength": 256},
            "roles": {
              "type": "array",
              "minItems": 1,
              "maxItems": 4,
              "uniqueItems": true,
              "items": {"type": "string", "enum": ["Reader", "Contributor", "Approver", "Owner", "BreakGlass"]}
            }
          }
        },
        "session_id": {"type": "string", "minLength": 1, "maxLength": 256},
        "turn_id": {"type": "string", "minLength": 1, "maxLength": 256},
        "turn_sequence": {"type": "integer", "minimum": 0},
        "locale": {"type": "string", "minLength": 2, "maxLength": 35},
        "purpose": {"type": "string", "minLength": 1, "maxLength": 128},
        "deadline_at": {"type": "string", "format": "date-time"},
        "view_context_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
        "bound_context": {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind"],
          "properties": {
            "kind": {"type": "string", "enum": ["incident"]},
            "incident_id": {"type": "string", "minLength": 1, "maxLength": 256},
            "correlation_id": {"type": "string", "minLength": 1, "maxLength": 512}
          }
        },
        "prior_turns": {
          "type": "array",
          "maxItems": 12,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["role", "content"],
            "properties": {
              "role": {"type": "string", "enum": ["user", "assistant"]},
              "content": {"type": "string", "minLength": 1, "maxLength": 8000}
            }
          }
        },
        "cancelled": {"type": "boolean", "default": false},
        "execution_authority": {"const": false}
      }
    }
  }
}
