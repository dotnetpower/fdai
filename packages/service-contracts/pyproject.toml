[project]
name = "fdai-service-contracts"
version = "0.1.0"
description = "Implementation-free contracts for independently released FDAI services"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["cryptography>=46,<51", "jsonschema>=4.23,<5", "pydantic>=2.11,<3"]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fdai_service_contracts"]

[tool.hatch.build.targets.sdist]
include = ["src/fdai_service_contracts", "pyproject.toml", "README.md"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src/fdai_service_contracts"]
