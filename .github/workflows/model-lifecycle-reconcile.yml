name: model-lifecycle-reconcile

on:
  schedule:
    - cron: "23 3 * * 1"
  workflow_dispatch:
    inputs:
      commit_sha:
        description: "Exact protected origin/main commit to reconcile."
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: model-lifecycle-reconcile
  cancel-in-progress: false

jobs:
  reconcile:
    if: github.ref == 'refs/heads/main'
    runs-on: [self-hosted, fdai-deploy]
    timeout-minutes: 30
    env:
      ARM_SUBSCRIPTION_ID: ${{ vars.ARM_SUBSCRIPTION_ID }}
      AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      DEPLOY_RUNNER_CLIENT_ID: ${{ vars.DEPLOY_RUNNER_CLIENT_ID }}
      DEPLOY_RUNNER_PRINCIPAL_ID: ${{ vars.DEPLOY_RUNNER_PRINCIPAL_ID }}
      AZURE_REGION: ${{ vars.AZURE_REGION }}
      MODEL_RESOLVER_DEPLOYER_OBJECT_ID: ${{ vars.MODEL_RESOLVER_DEPLOYER_OBJECT_ID }}
      MODEL_RESOLVER_ACCOUNT_NAME: ${{ vars.MODEL_RESOLVER_ACCOUNT_NAME }}
      MODEL_RESOLVER_RESOURCE_GROUP: ${{ vars.MODEL_RESOLVER_RESOURCE_GROUP }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Verify protected workflow source
        working-directory: ${{ runner.temp }}
        env:
          TARGET_COMMIT_SHA: ${{ inputs.commit_sha != '' && inputs.commit_sha || github.sha }}
          GITHUB_TOKEN: ${{ github.token }}
          PROTECTED_ORIGIN_URL: ${{ github.server_url }}/${{ github.repository }}.git
          PROTECTED_WORKFLOW_PATH: .github/workflows/model-lifecycle-reconcile.yml
        run: |
          set -euo pipefail
          if [[ ! "$TARGET_COMMIT_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "caller commit must be a lowercase 40-character git SHA" >&2
            exit 1
          fi
          guard_repo="$RUNNER_TEMP/fdai-model-lifecycle-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}.git"
          rm -rf -- "$guard_repo"
          git init --bare "$guard_repo" >/dev/null
          trap 'rm -rf -- "$guard_repo"' EXIT
          auth_header="$(printf 'x-access-token:%s' "$GITHUB_TOKEN" | base64 | tr -d '\n')"
          GIT_CONFIG_COUNT=1 \
            GIT_CONFIG_KEY_0=http.extraheader \
            GIT_CONFIG_VALUE_0="AUTHORIZATION: basic $auth_header" \
            git -C "$guard_repo" fetch --force --no-tags \
              "$PROTECTED_ORIGIN_URL" \
              "+refs/heads/main:refs/remotes/origin/main" "$TARGET_COMMIT_SHA"
          unset auth_header
          if ! git -C "$guard_repo" merge-base --is-ancestor "$TARGET_COMMIT_SHA" \
            refs/remotes/origin/main; then
            echo "caller commit is not an ancestor of protected origin/main" >&2
            exit 1
          fi
          if ! git -C "$guard_repo" diff --quiet \
            "$TARGET_COMMIT_SHA:$PROTECTED_WORKFLOW_PATH" \
            "refs/remotes/origin/main:$PROTECTED_WORKFLOW_PATH"; then
            echo "model lifecycle controls differ from protected origin/main" >&2
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.commit_sha != '' && inputs.commit_sha || github.sha }}
          fetch-depth: 0

      - name: Authenticate and verify stable deploy identity
        run: >-
          bash scripts/deployment/azure/login-deploy-identity.sh
          "$ARM_SUBSCRIPTION_ID" "$AZURE_TENANT_ID"
          "$DEPLOY_RUNNER_CLIENT_ID" "$DEPLOY_RUNNER_PRINCIPAL_ID"

      - name: Resolve lifecycle candidate
        id: candidate
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/model-lifecycle"
          evidence="$RUNNER_TEMP/model-lifecycle/evidence.json"
          candidate="$RUNNER_TEMP/model-lifecycle/candidate.json"
          deprecations="$RUNNER_TEMP/model-lifecycle/deprecations.json"
          python3 -m pip install --quiet 'pyyaml==6.0.3'

          if [[ ! "$MODEL_RESOLVER_DEPLOYER_OBJECT_ID" =~ ^[0-9a-fA-F-]{36}$ \
            || -z "$MODEL_RESOLVER_ACCOUNT_NAME" \
            || -z "$MODEL_RESOLVER_RESOURCE_GROUP" ]]; then
            python3 scripts/deployment/azure/model_lifecycle_reconciler.py \
              --current resolved-models.json --provider-error unavailable --out "$evidence"
            echo "status=abstained" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          set +e
          PYTHONPATH="$GITHUB_WORKSPACE/services/core-control-plane/src:$GITHUB_WORKSPACE/packages/service-contracts/src" \
            python3 -m fdai.rule_catalog.schema.llm_resolver_cli \
              --registry rule-catalog/llm-registry.yaml \
              --region "$AZURE_REGION" \
              --subscription-id "$ARM_SUBSCRIPTION_ID" \
              --deployer-object-id "$MODEL_RESOLVER_DEPLOYER_OBJECT_ID" \
              --use-azure-cli \
              --assess-fail-on critical \
              --out "$candidate"
          resolver_exit=$?
          set -e
          if [[ "$resolver_exit" -ne 0 ]]; then
            python3 scripts/deployment/azure/model_lifecycle_reconciler.py \
              --current resolved-models.json --provider-error unavailable --out "$evidence"
            echo "status=abstained" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          set +e
          az cognitiveservices account list-models \
            --name "$MODEL_RESOLVER_ACCOUNT_NAME" \
            --resource-group "$MODEL_RESOLVER_RESOURCE_GROUP" \
            --only-show-errors --output json > "$RUNNER_TEMP/model-lifecycle/provider-models.json"
          provider_exit=$?
          set -e
          if [[ "$provider_exit" -ne 0 ]]; then
            python3 scripts/deployment/azure/model_lifecycle_reconciler.py \
              --current resolved-models.json --provider-error unavailable --out "$evidence"
            echo "status=abstained" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          python3 - "$RUNNER_TEMP/model-lifecycle/provider-models.json" "$deprecations" <<'PY'
          import json
          import sys

          source, destination = sys.argv[1:]
          with open(source, encoding="utf-8") as stream:
              raw = json.load(stream)
          if not isinstance(raw, list):
              raise SystemExit("provider model response must be an array")
          notices = []
          for item in raw:
              if not isinstance(item, dict):
                  raise SystemExit("provider model entry must be an object")
              model = item.get("model") if isinstance(item.get("model"), dict) else item
              family = model.get("name") or model.get("family")
              retirement = (
                  item.get("retirementDate")
                  or item.get("deprecationDate")
                  or model.get("retirementDate")
              )
              if isinstance(family, str) and isinstance(retirement, str):
                  notices.append({"family": family, "retirement_date": retirement})
          with open(destination, "w", encoding="utf-8") as stream:
              json.dump(notices, stream, separators=(",", ":"), sort_keys=True)
              stream.write("\n")
          PY
          python3 scripts/deployment/azure/model_lifecycle_reconciler.py \
            --current resolved-models.json \
            --candidate "$candidate" \
            --deprecations "$deprecations" \
            --out "$evidence"
          status="$(python3 -c 'import json,sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["status"])' "$evidence")"
          echo "status=$status" >> "$GITHUB_OUTPUT"
          echo "evidence=$evidence" >> "$GITHUB_OUTPUT"

      - name: Retain sanitized reconciliation evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: model-lifecycle-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/model-lifecycle/evidence.json
          if-no-files-found: error
          retention-days: 30

      - name: Open idempotent draft registry proposal
        if: ${{ steps.candidate.outputs.status == 'proposal' }}
        env:
          EVIDENCE_PATH: ${{ steps.candidate.outputs.evidence }}
        run: |
          set -euo pipefail
          read -r proposal_digest activation_authority < <(
            python3 -c 'import json,os; value=json.load(open(os.environ["EVIDENCE_PATH"], encoding="utf-8")); print(value.get("proposal_digest", ""), str(value.get("activation_authority", True)).lower())'
          )
          if [[ ! "$proposal_digest" =~ ^[0-9a-f]{64}$ \
            || "$activation_authority" != "false" ]]; then
            echo "model lifecycle proposal is not safe to publish" >&2
            exit 1
          fi
          branch="automation/model-lifecycle-${proposal_digest:0:12}"
          if [[ -n "$(gh pr list --head "$branch" --state open --json number --jq '.[0].number // empty')" ]]; then
            echo "An open draft already owns proposal_digest=$proposal_digest"
            exit 0
          fi
          proposal="config/model-lifecycle-proposals/${proposal_digest}.json"
          mkdir -p "$(dirname "$proposal")"
          cp "$EVIDENCE_PATH" "$proposal"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git switch -c "$branch"
          git add -- "$proposal"
          git commit -m "chore(models): propose lifecycle mapping review"
          git push --set-upstream origin "$branch"
          gh pr create --draft \
            --base main \
            --head "$branch" \
            --title "chore(models): review lifecycle proposal ${proposal_digest:0:12}" \
            --body "This draft carries sanitized model-family and deprecation evidence. It has no activation authority and does not deploy, promote, or swap a live mapping. Compatibility impact and the exact proposal digest are recorded in the added manifest."
