name: check-translations

# Enforces the bilingual doc pair rule from
# .github/instructions/language.instructions.md.
# Every in-scope English `foo.md` (root README + docs/**) must have a
# matching `foo-ko.md` whose front-matter `translation_source_sha` equals
# `git hash-object foo.md`.

on:
  pull_request:
    paths:
      - "README.md"
      - "README-ko.md"
      - "docs/**/*.md"
      - "scripts/quality/localization/check-translations.sh"
      - ".github/workflows/check-translations.yml"
  push:
    branches: [main]
    paths:
      - "README.md"
      - "README-ko.md"
      - "docs/**/*.md"
      - "scripts/quality/localization/check-translations.sh"
      - ".github/workflows/check-translations.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Run translation pair check
        run: bash scripts/quality/localization/check-translations.sh
